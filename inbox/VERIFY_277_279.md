# 가이드 검증 — 개정 277·278·279 반영분

**2026-08-16 · 소스를 직접 확인했습니다**

---

# 1. ○ 열 21개 — v1 22개에 근접

```
사진 등급 순위 비율 차종 트림 연식 주행 외장 내장
{축 열 동적} 가격 시세차 월 변동 경과 딜러 정직도 비고
```

```
시세차 · 변동 · 경과 · 정직도   ★ 전부 들어갔습니다
축 열은 {{ a.label }} 로 동적    — 좋습니다.  축이 늘면 열도 늡니다
```

**★ 「시세차」가 들어간 것이 가장 큽니다.** 이 도구가 하는 일이 그것입니다.

---

# 2. ○ 반응형 — 규격대로입니다

```css
@media (min-width:1400px)   트림을 줄이지 않는다      ← 넓으면 더
@media (max-width:1099px)   표 → 카드 2열 그리드
@media (max-width:639px)    카드 1열
```

```css
.rows, .rows tbody, .rows tr, .rows td { display:block; }
.rows thead { display:none; }   /* 머리말은 칸마다 data-label 로 붙는다 */
.rows tbody { display:grid; grid-template-columns:1fr 1fr; }
```

**★ 주석이 정확합니다.**

```
/* <1100 — 표를 카드로 바꾼다.  접는 것이 아니라 모양을 바꾸는 것이다 */
```

**`data-label` 로 머리말을 칸에 붙인 것이 좋습니다.** 값이 안 사라집니다.

---

# 3. ○ 미리보기 · 정렬 드롭다운

```
#peek   position:fixed · 사진 184px · pk-t · pk-p
        「★ v1 원본의 값을 그대로 옮겼다」
<select>  1곳
```

---

# 4. ★ 확인이 필요한 것 3가지

## ① 좁은 폭에서 실제로 몇 개가 보이나

```
필수   360px 에서 22개 값이 다 보이는지 세십시오
       ★ CSS 가 있다고 보이는 것이 아닙니다
확인   .rows td 중 display:none 이 걸린 것이 있습니까
       있으면 그 값은 사라진 것입니다 (V11-70 위반)
```

## ② 축 열이 몇 개인가

```
v1    외장 · 내장 · HUD · 보증 · 사고 · 보험 · 렌트   7열
v2    {{ a.label }} 동적
확인   실제로 몇 개가 나옵니까
      7개보다 적으면 어느 것이 빠졌습니까
```

## ③ 시세차를 어떻게 계산했나

```
확인   result_axis 의 price 축 값입니까
      아니면 따로 계산합니까
      ★ 「시세」가 무엇인지가 이 값의 뜻을 정합니다
        같은 차종·트림·연식의 중앙값입니까
        감가 곡선의 이론가입니까
필수   화면에 그것을 밝히십시오 — title 이나 각주로
```

---

# 5. 다음

```
1  위 ①②③ 을 숫자로 답하십시오
2  V11-68 ~ V11-71 을 실제로 돌려 결과를 보고하십시오
   지금은 제가 손으로 셌습니다
3  S27 결과 — 화면 없는 기능이 몇 개입니까
4  그 뒤 마스터가 휴대폰에서 봅니다
```

**★ 마스터가 보기 전에 360px 에서 직접 확인하십시오.**
