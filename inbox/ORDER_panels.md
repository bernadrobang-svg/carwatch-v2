# ★★★ 개정 418 — 사고가 있는데 성능부가 「이상 없음」인 836건을 캔다

**마스터 지적 — 「미분류 판넬은 수리된 영역이구만」**
**「사고 이력이 있는데 성능부에 오류가 없다면 이상하잖아」**

`SPEC-2026.08.20-r418`

---

# 1. 무엇이 이상한가

```
V3-50 실측   성능부 무사고 · 보험 수리비 있음   836건 / 1,949건 (42.9%)
★ 보험에 수리비가 찍혔다는 것은 어딘가를 고쳤다는 뜻이다
★ 고쳤는데 성능점검부가 「이상 없음」일 수는 없다
  → 성능부가 안 말한 것이거나, 우리가 못 읽은 것이다
```

**개정 414 는 이것을 감점으로 다뤘다. 그것으로는 부족하다.**

```
감점은 「믿을 수 없다」는 표시일 뿐이다
★ 어느 판넬을 손댔는지 원문이 갖고 있다면 그것을 읽어야 한다
★ 못 읽고 있는 것이면 골격 30 · 외판 20 이 통째로 틀린 값이다
```

---

# 2. 지금 코드가 무엇을 전제하나

```python
# analyze/axis/state.py::_rank_worst
got = [t.get("title") for t in (el.get("statusTypes") or [])]
if any(t in titles for t in got):
    n += 1
```

```
★ statusTypes 가 비면 그 판넬은 세지 않는다 = 조용히 「이상 없음」이 된다
★ 같은 파일 머리가 이것을 금지하고 있다 —
  「원문이 없는데 「없음」으로 두는 것 — 「확인 안 됨」이다 (개정 323)」
  축 단위로는 지키는데 판넬 단위로는 안 지킨다
★ v182 실측 — etcs 의 statusTypes 는 53,880개 중 3개만 차 있고
  값이 statusItemTypes 로 옮겨 갔다.  outers 도 같은 변경을 겪었을 수 있다
```

---

# 3. 세 갈래로 가른다 — 데이터로

| | `outers` 에 무엇이 실리나 | 상태가 빈 판넬 | 지금 코드 |
|:--:|---|---|:--:|
| **A** | 전 판넬(약 20개)이 다 실린다 | 이상 없음 | 맞다 |
| **B** | **손댄 판넬만 실린다** | **교체·수리인데 등급 미상** | **틀리다** |
| **C** | 전 판넬이 실리되 미분류가 따로 있다 | 확인 안 됨 | 둘 다 아니다 |

```
★ B 이면 836건의 원인이 이것이다.  골격 30 · 외판 20 이 통째로 틀린 값이다
★ 가이드 판단은 B 쪽이다.  다만 짐작이라 실측 전에 규격을 안 바꿨다
```

## 무엇을 재나

```
① 매물당 outers 판넬 수 분포
   ★ 늘 20개 안팎이면 A · 0~몇 개로 들쭉날쭉하면 B
   최소 · 최대 · 중앙 · 0개인 매물 수를 낸다

② statusTypes 가 빈 판넬 수  ↔  보험 수리비 유무의 상관
   ★ 빈 판넬이 많을수록 보험 수리비가 많으면 B 다
   두 무리로 갈라 낸다 — 보험 수리비 있음 / 없음

③ statusItemTypes 로 값이 옮겨 갔는가 — etcs 처럼
   outers 아래 키 이름을 전수로 세어 낸다

④ ★ 836건 쪽과 무사고 쪽의 outers 를 견준다
   보험 수리비가 있는 매물의 outers 가 없는 매물보다 판넬이 많은가

⑤ 원문 조각 넷
   무사고 · 사고 1회 · 사고 2회 이상 · ★ 836건에 든 매물
```

```
필수   ★ 원문 body 를 직접 열어 센다.  파서 결과만 보지 않는다 (S43-②)
필수   갈래를 A · B · C 중 하나로 못 박고 근거를 숫자로 낸다
필수   ★ 「모르겠습니다」도 답이다.  셋 중 아무것도 아니면 그렇게 적는다
금지   갈래를 정하지 않고 코드를 고치는 것
```

---

# 4. 갈래가 정해지면

## B 이면 — 가이드가 규격을 다시 쓴다

```
★ 코드를 먼저 고치지 않는다.  갈래만 내고 멈춘다
★ 「상태가 빈 판넬 = 손댄 자리」이면 배점 곡선을 다시 정해야 한다.
  판넬 수를 그대로 세면 거의 전건이 0점이 된다.  가이드가 정한다
```

## A 이면 — 836건의 원인이 따로 있다

```
그때는 「보험에는 있는데 성능부에 없다」가 사실이다
★ 개정 414 의 감점이 맞는 답이 된다.  그대로 둔다
```

## C 이면

```
미분류 판넬을 「확인 안 됨」으로 다뤄야 한다.  개정 323 과 같은 자리다
```

---

# 5. 개정 414 는 어떻게 하나

```
필수   ★ 개정 414 구현을 멈추지 않는다.  둘은 따로다
      414 는 「보험과 성능부가 어긋나면 나쁜 쪽」이라는 원칙이다
      418 은 「성능부를 제대로 읽고 있나」다
필수   다만 recalc 는 418 갈래가 나온 뒤에 한 번만 돌린다
      ★ 두 번 돌리면 등급 분포 전후 표를 두 번 만들어야 한다
```

---

# 6. 마스터 몫 (S41)

```
★ 엔카 성능점검부 화면을 실제로 열어 판넬 그림이 어떻게 보이는지
  전 판넬이 회색으로 다 보이는가, 손댄 것만 색이 붙는가
★ AI 는 그 화면을 못 본다.  데이터 갈래와 화면이 같은 말을 하는지 마스터가 보신다
```
