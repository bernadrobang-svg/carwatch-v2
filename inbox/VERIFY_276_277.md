# 가이드 검증 — 개정 276·277 반영분

**2026-08-16 · 소스를 직접 확인했습니다**

---

# 1. ○ 된 것 — 확인했습니다

## 링크 6개 전부

```
dealer_shop · year_month · mileage_km · price_won · monthly_won · status_label
```

## 엔카 원문 링크

```python
# report/screens/build.py:230
encar_url = encar_tpl.format(source_id=sid) if sid and encar_tpl else None
```

```html
{% if v.encar_url %}· <a href="{{ v.encar_url }}" target="_blank" rel="noopener">
```

**★ 템플릿을 config 에서 읽는 것이 좋습니다.** 주소가 바뀌어도 코드를 안 고칩니다.

## 툴팁 16곳

## 필터 단추 on/off

```python
FILTER_BUTTONS = (
    ("min_grade", "A", "A 이상만", "S · A 만 봅니다"),
    ("min_grade", "B", "B 이상만", ...),
    ("axis", "history.damage", "사고 양호", ...),
    ("axis", "history.rental", "렌트 아님", ...),
    ("year", "2024", "2024년 이후", ...),
)
```

```python
# ★ 다시 누르면 꺼진다 — 켜져 있으면 그 조건을 뺀 주소를 준다
if on: got.pop(URL_NAME[field], None)
```

**★ 그리고 이 주석이 정확합니다.**

```
# ★ 위험 축은 「점수를 받았다 = 그 일이 없다」다.  bucket=1 이 양호다
AXIS_BUTTON_BUCKET = "1"
# ★ 축 단추는 「점수를 받은」 구간이다 — bucket=1.  0 으로 걸면 정반대가 된다
```

**「사고 양호」를 `bucket=0` 으로 걸면 사고 난 차만 나옵니다. 그걸 잡았습니다.**

---

# 2. ★ 아직 안 된 것 — 개정 277 (v1 원본)

```
listings 열 수   13   (v1 은 22)
```

| v1 열 | v2 |
|---|:--:|
| 시세차 | **없음** |
| 변동 | **없음** |
| 경과 | **없음** |
| 정직도 | **없음** |
| 외장 · 내장 (열로) | **없음 — 칩에 뭉침** |
| HUD · 보증 · 사고 · 보험 · 렌트 (열로) | **없음 — 칩에 뭉침** |

```
data-peek   미리보기      없음
<select>    정렬 드롭다운  없음
```

---

# 3. ★ 특히 「시세차」

```
v1    가격 옆에 「시세보다 얼마」
v2    없음
```

**이 도구가 하는 일이 「시세보다 싼 차를 찾는 것」입니다.**
**그 값이 화면에 없습니다.**

```
확인   result_axis 의 price 축에 그 값이 있습니까
      있으면 열 하나만 늘리면 됩니다
      없으면 어디서 계산하는지 알려 주십시오
```

---

# 4. ★ 축을 열로 가르는 것

```
v1   외장 · 내장 · HUD · 보증 · 사고 · 보험 · 렌트  → 7열
v2   칩 하나에 전부
```

```
★ 열로 갈라야 「이 차만 HUD 가 없다」가 세로로 눈에 들어옵니다
  칩은 한 줄씩 읽어야 합니다
```

**다만 휴대폰 폭이 문제입니다.**

```
확인   휴대폰에서 22열을 어떻게 낼지 정해야 합니다
      ① 가로 스크롤 — v1 은 PC 라 되던 것
      ② 자주 보는 축만 열로 (HUD · 사고 · 보증) 나머지는 칩
      ③ 폭에 따라 접기
★ 제 판단은 ②입니다.  마스터가 휴대폰으로만 씁니다
  다만 어느 축이 「자주 보는」 것인지는 마스터가 정할 일입니다
```

---

# 5. 다음

```
1  시세차 · 경과 · 변동 · 정직도 열 추가
   ★ 시세차가 가장 급합니다
2  자주 보는 축 3~4개를 열로 (어느 것인지 먼저 물으십시오)
3  정렬 드롭다운 8종
4  data-peek 미리보기
5  V11-68 · V11-69 구현 — 그래야 남은 것이 숫자로 보입니다
```

**5번을 먼저 하십시오. 지금은 「13 vs 22」를 제가 손으로 셌습니다.**
