# 검토 9 — 판정 240건 · `excluded` 분해 · 2026-08-16

**★ 파이프라인이 끝까지 돌았습니다.**

---

# 1. 성과

```
core_listing  7,661 · 범위 안 3,472 · 차종 미정 4,189
판정          240건
등급          C 90 · B 63 · E 35 · NOT_RATED 30 · D 20
매핑 실패      0
사전 미분류     18종
등록부 미분류    91건
check_all     통과 196 · fatal 4
```

**`/listings` 200 · `/why` 7절 · `/market` · `/dealers` 전부 200.**

---

# 2. ★ `excluded` 분해가 이번 작업의 핵심입니다

```
축 전체 4,046행 · excluded 1,017행 (25%)

missing               565   원문이 없다 (record·inspection 404 · 카탈로그 미수신)
no_mention            227   원문은 있는데 그 항목을 언급 안 함 (틴팅)
catalog_missing       195   카탈로그를 안 받아 사양 축을 못 봄
expected_unavailable   30   감가 곡선 구간 밖 — 없는 것이 정상
```

```
★ 셋은 뜻이 다르다.  v1 은 전부 0점으로 눌렀다
```

**이 한 줄이 v2 를 v1 과 가르는 자리입니다.**

```
missing               우리가 못 받았다        →  다시 받으면 된다
no_mention            받았는데 말이 없다      →  「모른다」가 맞다
expected_unavailable   원래 없는 것           →  없는 게 정상이다
```

**`spec.tinting` 227건을 「없다」가 아니라 「모른다」로 둔 판단이 맞습니다 (A-4).**

---

# 3. ★ `spec.hda` 225건이 `catalog_missing` — 다음 작업입니다

```
raw_response catalog 21건 · 매물 238건
```

**카탈로그를 21건만 받았습니다. 그래서 사양 축이 통째로 excluded 입니다.**

```
★ 이건 데이터 부족이지 결함이 아닙니다
  S7 을 더 돌리면 분모가 늘어 등급이 올라갑니다
필수   다만 「왜 분모가 짧은지」가 화면에 나와야 합니다
       지금은 「확인 못 한 것」 절이 그 일을 하고 있습니다
```

---

# 4. `NOT_RATED` 30건 — 분모 50.0

```
만점 555 중 분모가 50.0
→ 등급을 안 매기는 것이 맞습니다
```

**「분모가 짧으면 등급을 매기지 않는다」가 지켜졌습니다.**

---

# 5. `E` 35건 사유

```
리스·렌트 상품 18 · 골격 손상 6 · 계약중·판매완료 6
수리비 10% 초과 5 · 저당 3
```

**절대 배제가 사유별로 갈렸습니다. 규격대로입니다.**

---

# 6. ★ 다만 — 확인이 필요한 것 하나

```
「1·2·3 은 이미 끝나 push 돼 있다 (커밋 1b2838c)」
```

**제가 그 커밋을 GitHub 에서 못 찾았습니다. 작업 기록도 `outputs/` 에 없습니다.**

```
확인   NULL 4,189건의 원인이 셋 중 무엇으로 확정됐습니까
       ① 쿼리가 차종을 안 걸었나
       ② 응답에 다른 차종이 섞였나
       ③ 분류 규칙이 안 맞나
확인   그 작업 기록이 outputs/ 에 있습니까
       없으면 규칙 6(작업 기록) 위반입니다
```

**「범위 안 3,472 · 차종 미정 4,189」라는 숫자만 있고 원인이 없습니다.**

---

# 7. 남은 fatal 4건

```
V1-16  이번 run_id 밖의 행을 보지 않음 — 1
V2-18  parse_rule 재처리 후 전 봉투가 현재 parse_version — 30
V4-11  unclassified 존재 — 27
V4-20  dict_option_code 에 문장(공백·한글)이 없음 — 10
```

```
V4-11 은 등록부 미분류 91건과 이어집니다 — /admin/registry 에서 분류하면 됩니다
V4-20 은 옵션 코드 사전에 문장이 섞인 것입니다
```

---

# 8. 다음 순서

```
1  ★ NULL 4,189건 원인 — 커밋 1b2838c 의 결론을 알려 주십시오
   작업 기록이 없으면 지금 써서 push 하십시오 (규칙 6)

2  큐 (개정 261) — A(실행기) 와 B(큐 폐기) 중 판단
   여러 번 물었는데 아직 답을 못 받았습니다

3  S7 카탈로그를 더 받아 spec 축 분모를 늘린다
   21건 → 모델 수만큼

4  V4-11 (unclassified 27) — /admin/registry 에서 분류

5  facet 수신 — /admin/collect 「facet 만」
   마스터가 눌러야 합니다
```

**1번과 2번이 밀려 있습니다. 그것부터 답해 주십시오.**
