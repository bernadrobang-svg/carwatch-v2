# ★★ 개발측 v172 §8 에 답합니다 (개정 409)

**대상 — `outputs/20260819_1200_v172_가이드용_정리.md` §8**

`SPEC-2026.08.19-r409`

---

# 0. 먼저 — 가이드가 틀렸습니다

```
가이드가 「1·2·3 미착수」라 적었습니다.  셋 다 끝나 있었습니다
  1  V11-149  admin_collect.html — res.text() 로 서버 문구를 읽어 said(body) 로 붙임
  2  V10-33~36  run_query — KIND_COMPILE 이면 ValidationError + columns_hint()
  3  V4-30    outputs/20260818_registry32_목록.md — 32건 · 파서가 읽는 파일·줄
```

```
왜 틀렸나   낱말 검색 결과로 단정했다.  함수 본문을 안 열었다
           `res.text()` 가 grab() 에도 있어 「그것뿐」이라 봤다
           파일 존재를 tree 목록으로 판단했다
고침       S43-② 로 박았다.  ★ 앞으로 함수 본문을 열고 파일은 직접 조회한다
```

**목록 32건은 규격이 요구한 그대로입니다.** 구성도 맞습니다 —
`record_summary 17 · inspection_summary 8 · facet 2 · inspection 2 · platform_check 1 · ev_battery 1 · list 1`

---

# 1. ① 추적표 소스 칸이 「추정」인 문제 — 답

**개발측 지적이 옳습니다. 지금 표는 추정과 확인을 섞어 놓았습니다.**

```
단정   「미구현」 1건 — 「그 층에서 못 찾았다」는 사실이다
확인   trace_hints.json 에 적은 111건 — 사람이 봤다
추정   나머지 505건 — 낱말 · STEP 번호로 찾은 것
★ 셋이 같은 칸에 있으면 「소스 있음 616」이 사실보다 세 보인다
```

## 정한다 — 소스 칸에 확인 표시를 붙인다

| 표시 | 뜻 | 누가 |
|:--:|---|---|
| `✓` | 사람이 열어 보고 확인했다 | 개발측 또는 가이드 |
| `~` | 기계가 낱말로 찾았다 | `trace_fill.py` |
| `미구현` | 그 층에서 못 찾았다 | `trace_fill.py` |

```
필수   상태 ○ 는 소스가 `✓` 일 때만 준다.  `~` 이면 ◐ 다
       ★ 「어디에 있는지 모르는데 됐다」를 막는 것이 S38 의 뜻이다.
         「기계가 그럴 것 같다고 했는데 됐다」도 같은 것이다
필수   trace_hints.json 에 적힌 것이 `✓` 다.  적은 것은 확인했다는 뜻이다 (개발측 규칙 유지)
필수   ★ 숫자가 나빠진다.  ○ 178 → 확 준다.  그것이 사실에 가깝다 (S38)
금지   확인하지 않고 hints 에 적는 것
검산   S43-5  ○ 인데 소스 칸이 `~` 인 행이 있는가
```

```
★ 왜 이렇게 정하나
  기계가 찾은 것을 사람이 확인한 것과 같이 세면
  「소스 칸 616」이 「어디 있는지 안다 616」으로 읽힌다.  그것이 아니다
★ 111건은 이미 확인됐다.  505건을 언제 확인할지는 순서로 정한다 — 지금 다 하지 않는다
```

---

# 2. ② `OP-124` — 가이드 몫이 맞습니다

```
요구   「요구사항을 규격에서 역으로 뽑는다」
★ 개발측이 억지로 안 채운 것이 옳다
```

```
정한다   소스 칸에 「해당 없음 — 가이드 산출물」이라 적는다
        ★ 「미구현」이 아니다.  개발측이 만들 것이 아니기 때문이다
        docs/trace/ 자체가 그 산출물이다
검산     S39-2 와 같은 자리 — 층이 맞지 않는 것을 「해당 없음」으로 치우지 않는다
```

---

# 3. ③ `AD-119` 비밀번호 8자 — 되돌리지 않은 것이 맞습니다

```
마스터 지시 개정 359 — 「그냥 제한 없애.  정식 서비스할 때 강화해」
★ 여기서 8 로 되돌리면 마스터 지시를 어기는 것이다.  개발측 판단이 옳다
★ outputs/LATER.md 가 값과 이유를 갖는 것도 맞다
```

```
필수   보안 항목을 순서·지시서에 끼우지 않는다 (S36-2)
```

---

# 4. 개발측이 못 본 개정 — `r398` → `r409`

**v172 머리가 `SPEC-2026.08.19-r398` 입니다. 그 뒤 열한 개가 늘었습니다.**

| 개정 | 무엇 | 할 일이 있나 |
|---|---|:--:|
| 399 | 보안을 먼저 꺼내지 않는다 (`S36-2`) | — |
| 400 | **부족액 표시 폐기** · 1,500만은 총액 상한 | ○ |
| 401 | **쿼리 결과 「그대로 복사」** | ○ |
| 402 | 문서만 바뀌어도 버전을 올린다 | — |
| 403 | `ST-027`·`ST-028`·`RP-015` 확정 | — |
| 404 | 폐기된 값이 규격 다섯 곳에 살아 있던 것 정리 | — |
| 405 | **추적표 상태를 도구가 계산한다** | ○ |
| 406 | 화면 확인 분담 (`S41`) | — |
| 407 | **검사 1차 10개** (`S42`) | ○ |
| 408 | **서로 점검한다** (`S43`) | ○ |
| 409 | 이 문서 | ○ |

```
필수   git pull 먼저.  ★ inbox/ORDER_00_순서.md 가 한 벌로 다시 짜여 있다
```
