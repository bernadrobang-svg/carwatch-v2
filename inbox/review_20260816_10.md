# 검토 10 — NULL 원인 확정 · 큐 A · 진행 화면 · 2026-08-16

---

# 1. ★ NULL 4,189건 — 결함이 아니었습니다

```
① 목록 쿼리   8묶음 전부에 제조사·모델군 조건 있음.  정상
② 다른 차종    전부 우리가 고른 8종 안.  섞인 것 없음
③ 분류        여기다.  다만 규칙이 의도대로 걸러냈다
```

```
그랜저 1,291    가솔린 706 + 하이브리드 585   →  GRANDEUR_LPG 는 LPG 만
스포티지 1,195   가솔린 511 + 하이브 421 + 디젤 263  →  SPORTAGE_LPI 는 LPG 만
GV70 1,046     가솔린 995 + 디젤 51        →  GV70_EV 는 전기만
G80 533        가솔린 3.5T 477 + 디젤 56    →  G80_25T 는 2.5
```

**`collect_group` 이 `ModelGroup` 단위라 그 안의 다른 연료·트림이 함께 옵니다.**
**`fuel_match`·`trim_include` 가 걸러낸 결과가 54% 입니다.**

**★ 셋을 숫자로 배제하고 확정한 것이 정확합니다.** 짐작으로 안 넘어갔습니다.

---

# 2. ★ 그런데 검사가 진짜 결함 2건을 잡았습니다

```
V2-31 passed=False
  차종 미정인데 판정된 매물 2건
  차종 미정인데 status='active' 1건 — S5 가 가져간다
```

| 결함 | 원인 |
|---|---|
| 미정인데 `active` | S6 재분류가 `target_key` 만 바꾸고 `status` 를 안 바꿨다 |
| 미정인데 판정됨 | 대상에서 빠진 매물의 옛 판정이 남았다 |

```
S6 는 target_key IN (scope) 로 범위를 잡아 NULL 행을 아예 못 본다
→ S6 만 고쳐서는 이미 생긴 행을 못 치운다.  S9 에서 함께 치운다
```

**★ 「검사를 만들었더니 다른 것이 나왔다」 — 이게 검사를 만드는 이유입니다.**

---

# 3. 큐 — A 를 골랐고 근거가 맞습니다

```
전 차종 3.8시간을 HTTP 요청이 붙들 수 없다
recalc_job 에 진행 컬럼이 이미 있다
개정 250 이 CLI 를 금지했다
```

**systemd 가 아니라 웹 서버 안의 스레드로 한 것도 맞습니다.** 서비스가 하나면 됩니다.

## ★ 그리고 잠금 범위 정정이 큽니다

```
전에는 큐가 밀리면 운영·조정 둘 다 409 라
큐를 푸는 화면조차 못 열었다
```

**이건 「고장 나면 고칠 수도 없는」 상태였습니다.** `STEP 132a` 대로 조정만 잠그게 고친 것이 맞습니다.

---

# 4. 진행 화면 — 규격대로입니다

```
/admin/status  GET 만 받는다
meta refresh 5초 — JS 가 꺼져도 돈다
① 도는 것 ② 끝난 것 ③ 받았다↔쓰였다 ④ 실패
```

**`meta refresh` 를 쓴 것이 좋습니다.** JS 없이 도는 것이 규격입니다.

---

# 5. 규칙 6 위반을 스스로 밝혔습니다

```
★ 이 기록은 늦게 썼다.  작업은 1b2838c 로 끝났는데
  outputs/ 기록을 그때 남기지 않았다 — 규칙 6 위반이다
```

**밝힌 것이 맞습니다. 다음부터 그때그때 남기면 됩니다.**

---

# 6. `S7` 카탈로그 — 거의 다 받았습니다

```
트림 조합 상한 26 · 지금 21
```

**상세를 더 받으면 나머지도 드러납니다. 지금 못 세는 게 정상입니다.**

---

# 7. 이제 전 차종입니다

```
check_all   통과 200
남은 것     범위 안 3,472 중 판정 240 → 3,232건
```

## 순서

```
1  중단·재개를 먼저 확인한다
   10,148 호출이 중간에 끊기면 처음부터 다시 하면 안 된다
   실제로 중단해 보고 재개해 보십시오

2  전 차종 S5 → S6 → S9 → S10 + S7
   마스터가 /admin/status 를 열어 두고 지켜봅니다

3  끝나면 숫자
   판정 몇 건 · 등급 분포
   excluded 사유별 (missing · no_mention · catalog_missing · expected_unavailable)
   ★ 카탈로그를 늘린 뒤 spec 축 excluded 가 얼마나 줄었는가
   사전 미분류 몇 종 · 등록부 미분류 몇 건 · 매핑 실패 몇 건
   check_all fatal 몇

4  그 뒤 마스터가 화면에서 봅니다
   /listings 에 3,000건 넘게 뜨는지
   /why 에 근거가 나오는지
```

**1번을 건너뛰지 마십시오. 그것만 확인하면 바로 시작하십시오.**
