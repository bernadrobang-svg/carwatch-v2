# ★★ 가이드가 정한 것 셋 — 개정 396 · 397 · 398

**마스터 — 「전부 다 해야 하지 않아?」**
**★ 개발측이 기다리던 판단이다. 이제 돌리면 된다.**

`SPEC-2026.08.19-r398`

---

# 1. 개정 396 — `V11-12` · `/reports` 가 라우팅 표에 없다

```
실측 08-19 07:16   메뉴에 「/reports」가 경로 그대로 떴다
원인               web/app.py:97  LABELS 에 없으면 경로를 그대로 낸다
                  ★ :53 주석이 이미 그 경고를 적어 뒀다.  또 반복됐다
                  ★ V11-54 는 관리 메뉴표만 본다.  일반 메뉴는 아무도 안 본다
```

**고쳤다 — `docs/chapters/61-web.md`**

```
① 라우팅 표에 두 행을 넣었다
   /reports        GET  view_reports          user  리포트  구현
   /reports/{name} GET  view_report_download  user  —      구현
② STEP 142a 신설 — 메뉴 이름 표.  LABELS · MENU_TIPS 의 정본이다
```

**개발측이 할 것**

```
web/app.py   LABELS 에 "/reports": "리포트"
             MENU_TIPS 에 "/reports": "낸 리포트를 화면에서 읽고 내려받습니다"
             ★ 표에서 그대로 옮긴다.  새로 짓지 않는다
검산         V11-54   관리 메뉴 + STEP 142a 둘 다와 대조하게 넓힌다
             V11-150  ★ 신설 — 메뉴 라벨이 「/」로 시작하면 실패
```

---

# 2. 개정 397 — 층이 안 맞는 29건 (v169 §5)

**★ 층은 「그 일이 실제로 실행되는 자리」다. 디렉터리 이름이 아니다.**

| R | 요구 | 전 | **후** | 왜 |
|---|---|---|---|---|
| WT-019 | 연락함 — 언제·누구에게·무슨 말 | 판정 | **저장** | 사람이 적는 메모다. 판정이 아니다 |
| WT-020 | 보러 감 — 언제·무엇을 봤나 | 판정 | **저장** | 〃 |
| WT-021 | 끝 — 샀다·안 샀다·왜 | 판정 | **저장** | 〃 |
| SC-051 | 「일반 1년 10개월 (16/20)」 | 판정 | **화면** | 표시 문구다 |
| WB-001 | 가변형 반응형 — 값을 버리지 않는다 | 화면 | **화면** (유지) | 미구현이다. 층이 아니라 소스가 없다 |
| WB-015 | v1 22열 · 축을 열로 | 판정·화면 | **화면** | 열 배치다 |
| WB-040 | 이유를 못 대면 추천 안 한다 | 판정 | **화면** | 판정 결과를 읽어 목록에서 거른다 |
| WB-065 | 비교는 차이만 | 판정 | **화면** | v170 에서 `compare.html` 로 구현됐다 |
| WB-073 | 관리 화면 — 큐만 보지 않는다 | 운영 | **화면** | 관리 화면 요건이다 |
| VF-009 | `kind` 없으면 `Check` 생성 거부 | 판정 | **검사** | `validate/base.py` 다 |
| VF-014 | 차수를 늘리면 정규식도 함께 | 화면 | **검사** | 검사 정의 규칙이다 |
| VW-010 | 세 값을 한 셀에 | 화면 | **화면** (유지) | 미구현이다 |
| VW-015 | 표본 부족 딜러는 흐리게 | 판정 | **화면** | 표시 규칙이다 |
| VW-020 | 축 칩은 한 번에 (`IN`) | 판정 | **저장** | 조회 방식이다 |
| MS-023 | 사이트별로 거를 수 있게 | 화면 | **화면** (유지) | ★ 진짜 미구현. 개발 대상이다 |
| MS-027 | 같은 점수면 사이트 보증 높은 쪽 앞 | 판정 | **화면** | 정렬 규칙이다 |
| AD-007 | 변경 전 세션은 전 경로 403 | 수집 | **운영** | 계정·세션이다 |
| AD-050 | 원문을 가공하지 않고 그대로 (P3) | 수집 | **저장** | 저장 규칙이다 |
| AD-067 | 중단할 수 있다·어디까지 남긴다 | 수집 | **운영** | 실행 제어다 |
| AD-085 | 큐만 보지 않고 실제 도는 것을 본다 | 저장 | **운영** | 〃 |
| AD-092 | 조각 하나는 `max_form_bytes` 안 | 수집 | **저장** | `store/chunk.py` 가 이어붙인다 |
| CO-003 | 브라우저로 받는다 — 마스터 회선 | 수집 | **수집·화면** | 사람이 눌러야 한다. 화면이 반이다 |
| CO-024 | 재수집·재파싱·재판정을 가른다 | 수집 | **운영** | 실행 지시다 |
| CO-026 | 받은 그대로 둔다 (P3) | 수집 | **저장** | AD-050 과 같은 자리 |
| CO-036 | 다중 사이트 — 화면에 출처를 낸다 | 화면 | **화면** (유지) | 미구현이다 |
| DI-014 | 판정 축의 `dict_enum` 0행이면 실패 | 사전 | **검사** | 검사다 |
| OP-136 | HTTPS · 쿠키 Secure·HttpOnly | 저장 | **운영** | 배포·서버 설정이다 |
| ST-026 | 미확정이 해소되면 본문도 고친다 | 사전 | **`RULES.md`** | 문서 규칙. **소스가 없는 게 맞다** |
| OP-024 | 이 문서만으로 구현할 수 있어야 한다 | 운영 | **`RULES.md`** | 〃 |

```
바뀐 것 21건 · 유지 6건 (진짜 미구현) · RULES.md 로 뺀 것 2건
필수   trace_fill.py 를 다시 돌린다.  ★ 유지 6건은 층이 아니라 소스가 없는 것이다
       그 6건을 「미구현」으로 두고 개발 대상에 넣는다
금지   유지 6건을 층을 바꿔 억지로 찾히게 만드는 것
검산   S39-1  층이 안 맞는 것이 0 인가 (미구현은 제외)
```

---

# 3. 개정 398 — `V3-38` · facet 이 준 「기타 색상」

```
값     color_ext 「기타 색상」 — facet 이 주는데 dict_enum 에 없다
축     ⑥ 취향 · 색상 10점 (f-table)
       흰·검정 10 · 회색·은색 7 · 그 밖 3
```

**정한다 — 「그 밖」으로 등록한다. 3점.**

```
dict_enum   axis=color_ext · raw="기타 색상" · mapped="other" · status=confirmed
사유         엔카가 분류를 포기한 값이다.  흰·검정·회색·은색이 아님은 확실하다
             ★ 「모름」이 아니다.  사이트가 준 값이다.  0점 + 「확인 안 됨」이 아니다
출처         [판단]  가이드가 정함
위험         ⑥ 취향은 등급에 안 들어간다 (f-table).  순위에만 영향한다
검산         V3-38  판정 축의 값이 전부 사전에 있는가
             V3-78  ★ 신설 — mapped="other" 인 값이 그 축 전체의 20% 를 넘으면 warn
                    넘으면 사전이 게을러진 것이다
```

```
금지   「기타 색상」을 0점으로 두는 것 — 「모름」과 「그 밖」을 가른다
```

---

# 4. 남은 하나 — `V4-11` 은 아직 못 정한다

```
★ 등록부 32건의 「쓴다/안 쓴다」는 값 분포를 봐야 정한다
★ 개정 390 의 목록 32줄이 나오면 그날 바로 정한다.  대기 중이다
★ V1-11 은 손댈 것이 없다 — 다음 전체 실행이 끝나면 사라진다
```
