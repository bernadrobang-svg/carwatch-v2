# 검토 13 — HTTPS · V11-72 · 2026-08-16

---

# 1. ★ HTTPS 가 섰습니다

```
https://43.201.16.78.sslip.io      200 · 0.175초
http  → 308 로 보냄
:8765 → 000 (밖에서 닫힘)
```

## 잘한 것

```
Caddy 를 root 로 돌리지 않고 CAP_NET_BIND_SERVICE 로 80·443 만
앱을 127.0.0.1 로 — ★ Caddy 를 세운 뒤에 (순서를 지켰다)
보안 그룹을 안 건드리고 닫혔다
deploy/ 에 Caddyfile · 유닛 · README
  「서버에서 손으로 고친 것이 저장소에 없으면 다시 세울 때 잃는다」
```

**★ `deploy/` 를 남긴 것이 특히 좋습니다.** 인스턴스가 죽어도 다시 세웁니다.

## 그리고 실측으로 확인했습니다

```
쿠키 Secure 는 문서를 믿지 않고 실측했다
임시 echo 서버를 8799 에 띄워
Caddy 가 X-Forwarded-Proto: https 를 정말 붙이는지 확인하고 걷어냈다
```

**「문서에 그렇게 적혀 있다」로 넘기지 않았습니다.**

```
혼합 콘텐츠 0개 · 사진 50장 · 엔카 원문 100곳 전부 https
```

---

# 2. ★ 제 검증이 틀렸습니다

```
제가 한 것   템플릿을 grep 해서 「{% if %} 로 다 막혀 있다」
실제        /listings?dealer= 가 5곳 있었다
```

**개발측 지적이 맞습니다.**

```
템플릿을 읽어 「막혀 있다」고 하는 것은 검사가 아니다
```

**`V11-72` 를 「전 화면 29개를 실제로 렌더해 본다」로 만든 것이 정답입니다.**
**제가 소스만 읽고 판단한 것이 잘못입니다.**

---

# 3. ★ `null` 의 진짜 원인

```
carwatch.service 가 0.0.0.0:8765 를 못 잡아 재시작 루프에 빠져 있었다
개발 서버가 127.0.0.1:8765 를 쥐고 있어 밖에서 통째로 안 보였다
```

**「사이트에 접근할 수 없음」이 링크 문제가 아니라 서버가 죽어 있던 것이었습니다.**

**★ 그리고 그것을 `CLAUDE.md` 에 금지로 못 박은 것이 맞습니다.**

---

# 4. ★ 검사가 거짓 숫자를 낸 것도 잡았습니다

```
V11-70 이 세는 칸 수가 26 · 화면에 나오는 것은 25
JS 안의 문자열 ' + label + ' 이 한 칸으로 세어졌다
```

```
검사가 거짓 숫자를 내면 검사가 아니다
```

**정확합니다.**

---

# 5. 검증 답 확인

```
① 360px 에서 사라지는 값 0개
   display:none 은 thead · #peek 둘뿐
② 축 7열 — v1 의 7개가 전부 + HDA · 선루프
③ 시세차 = 감가 곡선의 이론가 (STEP 70)
   title · 각주 둘 다에 적음
V11-61~72 전부 통과 · S27 화면 없는 기능 0개
```

**③이 특히 중요합니다.**

```
「중앙값이 아니라 감가 곡선의 이론가」를 화면에 밝혔다
```

**같은 「시세차」라도 무엇과 비교했는지에 따라 뜻이 다릅니다. 그걸 적었습니다.**

---

# 6. 남은 것

```
check_all   통과 222 · fatal 4
```

```
확인   fatal 4 가 무엇입니까
      V4-20(옵션 코드에 문장) 이 아직입니까
```

## 그리고 아직 안 한 것

```
1  시세 화면 hist 히스토그램      ★ 아직 표입니다
2  dealers quad 4분면
3  watch spark
4  recommend pbar
5  사전 확정 화면 (⑨)
6  관리 화면 전반 (⑩)
7  딜러 보유 차종 (⑤)
```

**★ 마스터가 곧 새 주소로 봅니다.**
**그때 시세가 여전히 표면 또 지적이 나옵니다. 1번을 먼저 하십시오.**
