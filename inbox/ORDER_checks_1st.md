# ★★★ 개정 407 — 검사 1차 10개 (요구 19건)

**가이드 실측 08-19 — 검사가 없는 요구 289건 중 골랐다 (`S42`)**

`SPEC-2026.08.19-r407`

---

# 1. 왜 열 개만인가

```
검사 없음 289건
  번호는 정해졌는데 코드가 없다   32건 (고유 검사 15개)   ★ 여기부터
  번호조차 없다                257건
```

```
★ 289건을 다 만들지 않는다.  검사 자체가 이미 세 번 거짓을 냈다
  V3-22 거짓 경보 · S34-3 옛 형식 · V11-134 두 번 거짓 통과
★ 규격이 이미 「이 검사가 필요하다」고 번호까지 정한 것부터 만든다
★ 15개 중 V9-01·03·04·05·08 은 K카 대기다 (요구 13건) → 이번은 10개 · 요구 19건
```

---

# 2. 1차 10개

| 검사 | 무엇을 재나 | 요구 | 기준 |
|---|---|---|:--:|
| `V11-45` | 실패·진행·최초 계정 안내가 화면에 나오는가 | AD-003·004·072·073·075 | ① |
| `V11-103` | 목록이 오래되면 알리는가 · 가격 변동이 목록에서 오는가 | CO-023 · AD-115 · AD-118 | ① |
| `V11-85` | 트림이 `Badge` + `BadgeDetail` 로 세부등급까지 나오는가 | SC-025·026 · WB-022 | ② |
| `V3-58` | 배터리 SOH 가 **가점**인가 (축이 아니다 · 분모 675 그대로) | SC-012 · SC-049 | ①④ |
| `V3-66` | 구간 사이가 **선형 보간**인가 | SC-007 | ⑤ |
| `V3-50` | 성능부와 보험이력을 대조하는가 | SC-038 | ① |
| `V11-100` | 옵션이 「4종 715만」처럼 값과 함께 나오는가 | WB-021 | ② |
| `V11-96` | ♡ 가 제목 줄에 있는가 | WB-026 | ② |
| `V11-94` | 추천 조건이 한 줄로 나오는가 | WB-039 | ② |
| `V7-14` | `gone` 매물을 목록에서 지우지 않는가 | WT-005 | ③ |

```
기준  ① 조용히 틀린다  ② 같은 값이 두 곳 이상  ③ 되돌릴 수 없다
      ④ 이미 한 번 어긋났다  ⑤ 사람이 못 잰다
```

## ★ V3-58 을 특히 조심한다

```
개정 380  「배터리 SOH 는 가점 (최대 +30).  없다고 감점하지 않는다」
★ 축으로 만들면 전기차 778건 중 745건이 0점을 받는다 (실측 33건만 SOH 가 있다)
필수   가점이 분모를 늘리지 않는지 함께 잰다 — 분모는 675 그대로다
필수   ★ SOH 가 없는 표본으로 되살린다.  있는 표본만 보면 통과한다
```

---

# 3. 만들 때 반드시 (S42)

```
필수   ★ 「실패하는 표본」으로 되살려 확인한다
       ★ 통과만 보고 「된다」 하지 않는다.  V11-134 가 그래서 두 번 거짓 통과했다
필수   실패했을 때 무엇을 하라는지 함께 낸다 — Check.action (개정 47)
필수   정적 검사는 문자열 상수만 본다 (개정 53).  서술을 잡지 않는다
금지   「코드에 있다」로 통과시키는 것.  ★ 실제로 부른다 (V11-99)
검산   S42-2  ★ 새 검사마다 실패 표본으로 되살린 기록이 작업기록에 있는가
```

**작업기록에 이 표를 채운다.**

| 검사 | 통과 표본 | ★ 실패 표본 | 무엇으로 실패시켰나 |
|---|---|---|---|
| `V3-58` | | | SOH 없는 전기차 |

---

# 4. K카 대기 5개 — 지금 만들지 않는다

```
V9-01 · V9-03 · V9-04 · V9-05 · V9-08     요구 13건
★ 사이트가 하나뿐이면 「축 × 사이트」 표가 늘 통과한다.  뜻이 없다
★ K카가 들어온 뒤에 만든다 (15번)
```

---

# 5. 그 밖 257건은 남긴다

```
★ S42 의 기준 다섯에 안 걸리면 만들지 않는다
★ 「검사 없음」으로 남기는 것도 판단이다.  빈 칸으로 두는 것과 다르다
필수   4시간 점검이 「검사 없음 N건」을 매번 낸다 — S42-1
필수   ★ 줄지 않아도 된다.  줄어야 할 것만 줄면 된다
```
