# ★★★ 「확인 필요」 16건 — 전부 닫는다 (개정 400~403)

**마스터와 함께 정리 — 08-19**

`SPEC-2026.08.19-r403`

---

# 0. 먼저 — 13번을 잘못 묶었다

```
!  47건  = 결함이다.  질문이 아니다 → guide/02_결함대장.md 로 간다
?  19건  = 확인 필요.  중복 3쌍(ST-027 · RP-015 · AD-119)을 빼면 16건
★ 가이드가 「미정·질문 행」이라 한 데 묶었다.  성격이 다르다
```

---

# 1. ★ 마스터가 정하신 것 — 개정 400 · 401

## 개정 400 — `VW-027` 부족액 표시를 폐기한다

**마스터 확정 — 「선납금은 1500만원으로 고정했어. 그 안에 취득세 등록비**
**사이트 수수료 포함이야. 따라 이 기능은 필요없어. 1500만원에 모두**
**해결되면 전액 현금이 되겠지. 1500은 사정을 봐서 일괄로 바꾸는 기준값으로」**

```
★ 1,500만은 「차값에 더하는 선납」이 아니라 총액 상한이다
  그 안에 취득세 · 등록비 · 사이트 수수료가 들어간다
★ 화면은 「전액 현금」인가 아닌가 둘뿐이다.  부족액을 내지 않는다
★ 1,500만은 일괄로 바꾸는 기준값 — config.finance.cash_limit 하나
```

```
반영   docs/chapters/41-view.md  「부족액을 표시한다」 폐기 표시
검산   V11-151  부족액 문구가 화면에 있는가 (있으면 실패)
       V11-152  cash_limit 이 config 한 곳에서만 읽히는가
```

**나중에 — `outputs/LATER.md`**

```
마스터 — 「나중에 공개용으로 바꾸게 되면 개인별 설정을 만들어서 바꿀 수 있게 할거야」
★ 지금은 하나다.  사람마다 다른 값은 정식 서비스 때 (S36 과 같은 자리)
```

## 개정 401 — `AD-120` 「그대로 복사」 단추를 넣는다

**마스터 확정 — 「넣는다」**

```
필수   쿼리 탐색 결과에 「그대로 복사」 단추
필수   ★ 붙여넣어 바로 쓸 수 있는 형태 — 탭 구분 (표에 붙는다)
       근거 [판단] 마스터가 결과를 다른 곳에 옮겨 보신다 (개정 358 — 「내가 보려고 만든 창」)
필수   내려받기(CSV)와 함께 둔다.  대신하지 않는다
검산   V10-37  결과 표 위에 복사 단추가 있는가
```

---

# 2. 이미 답이 나온 것 — 가이드가 닫는다 (10건)

| R | 물음 | 답한 개정 | 결론 |
|---|---|---|---|
| `RG-010` | 분모가 같은 매물끼리만 비교 | 351 | **지운다** — ⑤ 50점이 사이트 차이를 담는다 |
| `WT-028` | 계약 4단계 완료 시각·메모 | 362 | **지운다** — 연락함·보러 감·끝 셋 |
| `VW-028` | `page_size` 를 URL 로 안 받는다 | 356 | **받는다** · 상한 `page_size_max` |
| `VW-026` | 전액 현금 판정식 | 354 · **400** | **맞다** — 1,500만 총액 상한 |
| `RP-016` | 「추정」 표시 | 363 | **붙인다** |
| `VW-029` | 「추정」 표시 (중복) | 363 | 〃 |
| `RP-029` | 자주 쓰는 쿼리 저장 | 358 | **저장한다** |
| `WT-029` | 같은 `vehicle_id` 를 묶는다 | 355 | **묶는다** · 「N번 재등록」 표시 |
| `OP-130` | 한 번에 3건까지 고친다 | 360 | **상한 없다** · 하나씩 고치고 하나씩 커밋 |
| `AD-119` | 비밀번호 8자 | 359 | `outputs/LATER.md` |

```
★ 열 건 다 마스터가 이미 답하신 것이다.  추적표가 옛 문구를 들고 있었다
★ 가이드가 답을 받고 추적표를 안 고쳤다 — 그래서 「확인 필요」로 남았다
필수   앞으로 마스터 답을 받으면 그 자리에서 규격과 추적표 근거를 함께 고친다
검산   S34-4  ★ 답이 나온 개정이 있는데 「?」로 남은 행이 있는가
```

---

# 3. 가이드가 정한 것 — 개정 402 · 403 (4건)

## `OP-131` — 문서만 바뀌어도 버전을 올린다

```
전   「문서만 바뀌면 버전을 안 올린다」  ★ 조사 없음
후   개정 번호가 늘면 버전을 올린다.  문서·코드를 가르지 않는다
근거 [판단]  개발측은 작업기록에 버전을 적는다 (00_버전.md).
            문서가 바뀌었는데 버전이 같으면 한 버전이 두 뜻을 갖는다
            ★ 실제로 가이드는 늘 올려 왔다.  규칙이 실제와 달랐다
```

## `ST-027` — 확보율은 둘 다 낸다

```
후   한 줄에 둘 다 — 「원문 85.2% · 컬럼 61.3%」
     기본 정렬·경고는 컬럼 기준
근거 [판단]  둘은 다른 것을 잰다.  하나만 내면 다른 하나가 숨는다
            판정이 읽는 것은 컬럼이므로 경고는 컬럼 기준이 맞다
★    마스터께 여쭐 일이 아니다 — 「이게 뭐야? 내가 알아야 해?」 (개정 361)
```

## `ST-028` — `display_name` 이 비면 `login_name` 을 쓴다 (유지)

```
근거 [판단]  빈 이름을 내면 「누가 한 일인가」가 사라진다.
            「이름 없음」으로 채우면 두 사람이 같아 보인다
```

## `RP-015` — 전 요소를 `ReportMeta` 에서 (유지)

```
전   「근거가 약합니다.  리포트를 쓰는 사람이 있습니까」
후   유지한다
근거 [판단]  개정 357 로 리포트를 화면에서 읽게 됐다 — 쓴다.
            손으로 조립하면 리포트마다 형식이 갈린다.  V8-02 가 이미 지킨다
```

---

# 4. 개발측이 할 것

```
1  V11-151 · V11-152 · V10-37 세 검사를 만든다
2  41-view 의 「부족액」 화면·코드를 뺀다
3  쿼리 탐색에 「그대로 복사」 단추 (탭 구분)
4  trace_fill.py 를 다시 돌린다 — ? 16건이 닫힌다
★  추적표는 손으로 고치지 않는다.  규격이 바뀌었으니 도구가 따라온다
```
