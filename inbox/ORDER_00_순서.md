# ★★★ 다음 순서 — 지금 것을 끝낸 뒤 이것부터

**개정 395 · `SPEC-2026.08.19-r395`**

**★ 마스터가 부르실 필요가 없다. 지금 하는 것을 커밋하고 `git pull` 한 뒤 이 파일을 읽는다.**

---

# 0. 먼저

```
1  지금 돌리는 것을 끝내고 커밋한다.  중간에 버리지 않는다
2  git pull
3  작업기록 머리에 SPEC-2026.08.19-r395 를 적는다
   ★ v169 는 r365, v170 은 번호 없이 적었다.  버전이 없으면
     「어느 규격으로 만들었나」가 안 남는다 (00_버전.md)
```

---

# 1. 순서 — 위에서부터 하나씩. 하나씩 커밋

| # | 무엇 | 지시서 | 검산 | 크기 |
|:--:|---|---|---|---|
| 1 | **조각 실패 때 서버 문구를 낸다** | `ORDER_chunk_utf8.md` §3-3 | `V11-149` | JS 3줄 |
| 2 | **쿼리 탐색 오류 분류** | `ORDER_query_error.md` | `V10-33`~`V10-36` | 중 |
| 3 | **등록부 32건 목록 32줄** | `ORDER_registry32_list.md` §2 | `V4-30` | 명령 한 줄 |
| 4 | **조각 절단점을 글자 경계로** | `ORDER_chunk_utf8.md` §3-1·3-2 | `V11-147`·`V11-148` | JS 4줄 + 서버 |
| 5 | **run_tests 실패 2건을 닫는다** | — | — | `test_endtoend.py` · `test_web.py` |
| 6 | 등록부 32건 값 분포 | `ORDER_registry32_list.md` §3 | — | 3 이 끝난 뒤 |

```
필수   1 을 4 보다 먼저 한다
       ★ 1 없이 4 를 고치면 또 400 이 났을 때 「저장 400」만 뜬다.
         원인이 안 보이는 채로 고치게 된다
필수   5 를 뒤로 미루지 않는다.  실패를 남긴 채 다음으로 넘어가지 않는다 (S38)
금지   4 를 먼저 집는 것.  「작아 보인다」는 순서의 근거가 아니다
```

---

# 2. 배점 정본이 바뀌었다 — 개정 392

```
docs/chapters/30-score/f-table.md
  ⑤ 사이트 보증 = 5-0 하나 (50).  ★ 5-2 우수등급 10 · 5-3 점검 출처 5 는 폐기
  총점 675 · 등급 기준 625 · 24축
```

```
필수   채점 코드에서 5-2 · 5-3 축을 뺀다
필수   갈래 합이 갈래 표기와 같은지 기계로 잰다 — V3-77
       ★ 이번 65 ≠ 50 을 아무도 못 잡은 것이 이 검산이 없어서다
검산   V3-76  ⑤ 의 하위 축 합이 50 인가
       V3-77  ★ 전 갈래에서 하위 축 합 = 갈래 표기인가
```

---

# 3. 개발측이 손대지 않는 것

```
V11-12   /reports 가 라우팅 표에 없다        ← 가이드가 한다
층이 안 맞는 29건 (v169 §5)                  ← 가이드가 판단한다
V4-11 의 「쓴다/안 쓴다」 판단                  ← 3 번 목록이 나온 뒤 가이드가 한다
V3-38 「기타 색상」                          ← 사전 확정. 가이드가 정한다
V1-11                                      ← 옛 기록. 다음 전체 실행 뒤 사라진다
```

```
★ 이것들을 fatal 이라고 손대지 않는다.  손대면 두 사람이 같은 것을 고친다
```

## ★ 08-19 — 가이드가 넷 중 셋을 냈다 (개정 396·397·398)

```
inbox/ANSWER_guide5.md 를 읽는다.  개발측이 바로 돌릴 수 있다
  396  LABELS · MENU_TIPS 에 /reports 를 넣는다 (표에서 그대로 옮긴다)
       V11-54 를 STEP 142a 표까지 보게 넓힌다 · V11-150 신설
  397  층 29건이 정해졌다.  trace_fill.py 를 다시 돌린다
  398  「기타 색상」을 dict_enum 에 other · confirmed 로 넣는다 · V3-78 신설
남은 것  V4-11 은 위 3번 목록이 나온 뒤 가이드가 정한다
```

## ★ 08-19 — 「확인 필요」 16건이 닫혔다 (개정 400~403)

```
inbox/ANSWER_q16.md 를 읽는다
  400  부족액 표시를 뺀다.  1,500만은 총액 상한 · V11-151 · V11-152
  401  쿼리 결과에 「그대로 복사」 단추 (탭 구분) · V10-37
  402·403  가이드가 정했다.  소스에 할 일은 없다
그다음  trace_fill.py 를 다시 돌린다 — ? 16건이 닫힌다
★ 추적표는 손으로 고치지 않는다.  규격이 바뀌었으니 도구가 따라온다
```

## ★★★ 08-19 — 추적표 상태를 도구가 계산한다 (개정 405)

```
inbox/ORDER_trace_state.md 를 읽는다.  ★ trace_fill.py 를 고치는 것이 먼저다
  상태를 소스·화면·검사 세 칸에서 유도한다
  ★ 「!」·「?」 는 사람 몫이다.  도구가 덮지 않는다
  S38-4 · S38-5 신설
그다음  trace_fill.py --write 를 돌리고 ★ 실제 숫자를 적는다 (예상값이 아니다)
★ 지금 「✗ 46」은 소스가 비어 있던 시절의 흔적이다.  진짜 미착수는 0건이다
```

---

# 4. 폐기

```
inbox/ORDER_registry32_targets.md   ★ 폐기 — ORDER_registry32_list.md 로 대체 (개정 390)
                                    ★ 목록 없이 값 분포부터 요구한 것이 잘못이었다
```
