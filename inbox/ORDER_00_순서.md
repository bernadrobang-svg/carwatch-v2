# ★★★ 다음 순서 — 위에서부터 하나씩

**`SPEC-2026.08.19-r407`**

**★ 마스터가 부르실 필요가 없다. 지금 것을 커밋하고 `git pull` 한 뒤 이 파일을 읽는다.**

---

# 0. 먼저

```
1  지금 돌리는 것을 끝내고 커밋한다.  중간에 버리지 않는다
2  git pull
3  작업기록 머리에 SPEC-2026.08.19-r407 을 적는다
   ★ v169 는 r365, v170·v171 은 번호 없이 적었다.
     버전이 없으면 「어느 규격으로 만들었나」가 안 남는다
```

---

# 1. 순서

| # | 무엇 | 지시서 | 검산 |
|:--:|---|---|---|
| **1** | **조각 실패 때 서버 응답 본문을 낸다** | `ORDER_chunk_utf8.md` §3-3 | `V11-149` |
| **2** | 쿼리 탐색 오류 분류 | `ORDER_query_error.md` | `V10-33`~`V10-36` |
| **3** | 등록부 32건 **목록 32줄** | `ORDER_registry32_list.md` §2 | `V4-30` |
| **4** | 조각 절단점을 글자 경계로 · 조각별 대조 | `ORDER_chunk_utf8.md` §3-1·3-2 | `V11-147`·`V11-148` |
| **5** | `run_tests` 실패 2건을 닫는다 | — | `test_endtoend` · `test_web` |
| **6** | `LABELS`·`MENU_TIPS` 에 `/reports` | `ANSWER_guide5.md` §1 | `V11-54` 확장 · `V11-150` |
| **7** | **추적표 상태를 도구가 계산한다** | `ORDER_trace_state.md` | `S38-4`·`S38-5` |
| **8** | 층 29건 반영 후 `trace_fill.py` 재실행 | `ANSWER_guide5.md` §2 | `S39-1` |
| **9** | 「기타 색상」을 `dict_enum` 에 `other`·confirmed | `ANSWER_guide5.md` §3 | `V3-78` |
| **10** | 부족액 표시를 뺀다 · 쿼리 「그대로 복사」 | `ANSWER_q16.md` §1 | `V11-151`·`V11-152`·`V10-37` |
| **11** | **검사 1차 10개** | `ORDER_checks_1st.md` | `S42-2` |
| **12** | 배점 정합 검사 | `ANSWER_q16.md` · `f-table` | `V3-76`·`V3-77` |

```
필수   1 을 4 보다 먼저 한다
       ★ 1 없이 4 를 고치면 또 400 이 났을 때 「저장 400」만 뜬다.
         원인이 안 보이는 채로 고치게 된다
필수   7 을 8 보다 먼저 한다.  도구를 고치고 한 번만 돌리면 된다
필수   5 를 뒤로 미루지 않는다.  실패를 남긴 채 다음으로 넘어가지 않는다 (S38)
금지   작아 보인다고 4·11 을 먼저 집는 것
```

---

# 2. ★ 배점 코드는 이미 맞다 — 고치지 마라

```
실측   config/scoring.json  total_points 675 · grade_base_points 625 · 성분 24개
      warranty.site 50.  ★ 5-2 우수등급 · 5-3 점검 출처 성분이 처음부터 없다
★ 틀린 것은 f-table 문서였고 개정 392 로 고쳤다
필수   12번은 검사만 만든다 — V3-76 · V3-77
       f-table 의 갈래 합과 scoring.json 성분 합을 대조한다
금지   채점 코드를 건드리는 것
```

---

# 3. 완료 기준 (S38)

```
필수   규격 · 소스 · 화면 · 검사 네 칸이 다 차야 ○ 다
필수   4번은 저장된 body 의 U+FFFD 가 0개이고
       sha256 이 브라우저가 보낸 chunk_hash 와 같아야 완료다
       ★ 「400 이 안 났다」는 완료 근거가 아니다
필수   11번은 검사마다 「실패하는 표본」으로 되살린 기록을 적는다 (S42-2)
       ★ 통과만 보고 「된다」 하지 않는다.  V11-134 가 두 번 거짓 통과했다
```

---

# 4. 손대지 마라 — 가이드 몫이다

```
V4-11    등록부 32건의 「쓴다/안 쓴다」 — 3번 목록이 나온 뒤 가이드가 정한다
V1-11    옛 기록.  다음 전체 실행 뒤 사라진다
OP-124   「요구사항을 규격에서 역으로 뽑는다」 — 개발 산출물이 아니다
화면 확인  ★ 시안·정본 대조는 가이드가 한다 (S41)
```

---

# 5. 지금 하지 않는 것

```
V9-01·03·04·05·08     K카 대기.  사이트가 하나뿐이면 늘 통과해 뜻이 없다
검사 나머지 257건      S42 기준 다섯에 안 걸리면 만들지 않는다
K카 통합             마스터 회선이 필요하다
보안                 정식 서비스 전에 한다 (S36).  ★ 순서에 끼우지 않는다
```

---

# 6. 보고

```
필수   outputs/ 에 작업기록 md · 머리에 SPEC 버전
필수   못 한 것과 판단이 필요한 것을 따로 적는다
필수   숫자를 적는다 — 「검사 없음 289 → 270」
금지   「했습니다」만 적는 것
```
