# 검토 21 — 밤샘 일괄 · 규격 충돌 해소

**2026-08-17 · 개발측 밤샘 작업(01:50~09:00 · 커밋 10개)을 검증했습니다**

---

# 1. ★★ 잘한 것

## ① 엔카 API — 브라우저 없이 답을 냈습니다

```
세 화면이 같은 번들 하나를 쓰고 부르는 주소가 전부 문자열로 있다
app-DATxvME_.js 548KB · /vN/ 47종
```

**★ 그리고 이 발견이 큽니다.**

```
보증 · 신차가 · 옵션가는 이미 받아서 저장까지 돼 있었습니다.  화면이 안 냈을 뿐입니다
```

**제가 「안 받고 있다」고 했는데 「받았는데 안 썼다」였습니다. 제 판단이 틀렸습니다.**

```
새로 찾음   vehicles/car/options/standard   옵션 코드→이름 전체 사전 28KB
           record/{id}/summary            use · 전손 · 침수 · 도난 · 저당
```

## ② 판단이 전부 「덜 하는 쪽」이었습니다

```
extend_warrant 를 뺐다        160건 전량 404 · 3,470 요청을 쓸 값이 없다
축을 안 만들었다               개정 292 가 곧 다시 정하므로
CORE 열 이름을 고쳤다          이미 받은 827행의 endpoint 도 함께 맞췄다 — 버리지 않았다
```

**★ 「덜 하는 쪽」 규칙을 정확히 지켰습니다.**

## ③ ★ 스크린샷이 민무늬였던 것을 잡았습니다

```
V11-82(CSS 지문)를 붙이자 스크린샷 도구가 ?v= 를 못 따라가
CSS 가 하나도 안 걸린 화면을 찍고 있었다
★ 실서비스는 멀쩡했고 스크린샷만 깨져 있었다
```

**어제 제가 스크린샷으로 판단했습니다. 그 도구가 스스로를 속이고 있었습니다.**

## ④ `V4-20` 176건이 사라진 이유

```
options.etc 가 딜러 자유 입력이었다
```

**「옵션 코드에 문장이 섞였다」가 아니라 「거긴 원래 자유 입력」이었습니다. 원인을 정확히 짚었습니다.**

---

# 2. ★★ 규격 충돌 — 제 잘못입니다 · 개정 298

```
개발측 지적   개정 289(분모 고정) ↔ 0장 7.1(분모 시험 6종)
```

**7.1 이 이렇게 적혀 있었습니다.**

```
B  한 축 수집 실패     → 그 축 분모 제외
C  구조적 부재         → 그 축 분모 제외
F  분모 최소 기준 미만  → 등급 생성 금지
config.scoring.min_denominator_ratio
```

**★ 이 절은 「분모가 줄면 부풀려진다」를 알면서도 「그러니 줄이되 최소치를 두자」로 갔습니다.**
**절반만 맞았습니다.**

## 해소 — B · C · F 폐기

```
남는다  A  전 축 정상        정상 점수
       D  전 축 수집 실패    점수 생성 금지
       E  금지 근거만 존재    점수 생성 금지
새로   G  분모는 늘 555.  예외 없다
       H  못 본 축은 0점.  사유를 화면에
       I  확인율을 함께 낸다 — 「555 중 350점 확인 (63%)」
       ★ 「등급 생성 금지」는 확인율로 판단한다.  분모로 하지 않는다
폐기   min_denominator_ratio
```

**`run_tests` 실패 5건이 전부 이것에서 나왔습니다. 개정 298 을 반영하면 풀립니다.**

---

# 3. ★ 실측 결과 — 중요한 것 셋

## ⑤-1 등급 인플레

```
price 를 못 봤는데 등급을 매긴 것        202건
history.damage 를 못 봤는데 매긴 것    1,182건
warranty.general                       37건
```

**★ `history.damage` 1,182건이 큽니다. 사고를 모르는데 등급이 나왔습니다.**

## ⑤-2 배점 현황 — 개정 292 가 뒤집으려는 것

| 갈래 | 지금 | 개정 292 |
|---|--:|--:|
| price | 200 (36%) | 값 250 (45%) |
| **spec** | **90 (16%)** | 사양 75 + 취향 50 |
| **history** | **55 (10%)** | **상태 180 (32%)** |

**★ 지금은 상태(55)가 사양(90)보다 작습니다.**
**마스터 지적 「깡통에 HUD 만 있어도 만점」이 이 숫자로 확인됐습니다.**

## ⑤-3 축 × 차종

```
★ 전기차 safety.warranty_product 0%   (G80_EV · GV60 · GV70_EV · MODEL_Y)
★ SPORTAGE_LPI price 0%               보정계수 1.517 이 범위 밖
★ spec.tinting 전 차종 0~12%
```

**개정 293 이 예측한 그대로입니다.**

---

# 4. 등급 경계 제안 — 받습니다

| 등급 | 경계 | 근거 |
|---|--:|---|
| S | 75% | 상위 1% |
| A | 69% | 상위 5% |
| B | 60% | 상위 20% |
| C | 53% | 상위 40% |

```
★ 백분위를 먼저 정하고 그 자리의 비율을 반올림했다
```

**방법이 맞습니다. 그대로 쓰십시오.**

---

# 5. 다음 — 순서

```
1  ★ 개정 298 반영 — 분모 시험 B·C·F 폐기
   ★ run_tests 실패 5건이 이것입니다.  가장 먼저
2  개정 292 배점 구현 + 등급 경계 (75/69/60/53)
3  V3-40 이 잡은 1,182건 — 사고를 모르는데 등급
   ★ 개정 287 대로 NOT_RATED 로
4  ② 수집 마저 (34% → 100%)
5  ③ 축 추가 — 배점이 확정된 뒤
6  전기차 safety.warranty_product 0% — ev-battery 로 대신
7  SPORTAGE_LPI price 0% — 보정계수 범위 밖.  1년 미만은 신차가 대비로
```

**★ 1·2 가 오늘의 핵심입니다.**
**그 둘이 끝나면 등급이 처음으로 뜻을 갖습니다.**
