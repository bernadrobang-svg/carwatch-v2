# 검토 17 — 관리 화면을 처음 봤습니다

**2026-08-16 · `admin_targets` · `admin_dict` 스크린샷 확인**

---

# 1. ★ `file://` 로 찍는 발상이 정확합니다

```
outputs/render/ 의 HTML 을 file:// 로 찍는다
그것은 이미 관리자로 만든 것이라 세션을 흉내 낼 필요가 없다
★ 화면과 스크린샷이 같은 HTML 에서 나온다 — 둘이 어긋날 수 없다
```

**그리고 이것이 좋습니다.**

```
도구가 「내용이 있는가」를 스스로 본다
「관리자만 볼 수 있습니다」가 들어 있으면 빈 장으로 세어 README 와 로그에 적는다
```

**★ 「36장 찍었다」를 스스로 인정하고 검사로 만들었습니다.**

---

# 2. `admin_targets` — 마스터 지적 ⑧ 이 눈에 보입니다

## 잘 된 것

```
메뉴 이름이 전부 들어감 — 진행 모니터 · 목록 반입 · 브라우저 수집 · 사전 확정
안내가 정확 — 「facet 확인 없이 targets.json 에 쓰지 않습니다」
             「사이트가 열거값을 주므로, 사람이 적으면 표기가 어긋납니다」
절차 표시 — ① 모델 지정 → ② facet 확인 → ③ 트림 선택 → ④ 등록 → ⑤ 시험 수집 → ⑥ 확정
등록된 차종 10종 표
```

## ★ 안 된 것 — 지적 ⑧ 그대로

```
차종 키      GV70_EV          ← 사람이 칩니다
이름         제네시스 GV70 전기  ← 사람이 칩니다
수집 묶음     GV70             ← 사람이 칩니다
site_query   facet 으로 확인한 q 값  ← ★ 엔카 쿼리 문법을 알아야 합니다
구분         국산 (CarType.Y)   ← 드롭다운 하나뿐
```

**`STEP 149r` 대로 고치십시오.**

```
필수   제조사 → 모델군 → 연료 → 트림 을 드롭다운으로
       ★ raw_facet · 목록 원문에 다 있는 값입니다
필수   고른 값에서 차종 키 · 이름 · 수집 묶음 · site_query 를 만들어 줍니다
필수   「이 조건에 지금 매물 N건」을 즉시
```

## 그리고 열 잘림

```
등록된 차종 표에서 「확정」이 「확 / 정」으로 두 줄
「G80 2.5T」가 「G80 / 2.5T」로 두 줄
```

```
필수   좁은 폭에서 관리 표도 카드로 바꾸십시오
       ★ listings 는 카드로 바꿨는데 관리 표는 그대로입니다
```

---

# 3. ★★ `admin_dict` — 360px 에서 완전히 깨졌습니다

**이건 심각합니다.**

```
값 예시 열이 세로로 한 글자씩 떨어집니다

  프
  론
  트
  휀
  더
  (좌)
  ·
  프
  론
  트
  …
```

```
★ 한 글자가 한 줄입니다.  화면이 무한히 길어집니다
★ 「사유」 입력칸과 「이 축 35종 확정」 단추가 표 위에 겹쳐 있습니다
```

## 원인

```
표의 「값 예시」 칸이 폭 제약 없이 긴 문자열을 받습니다
좁은 폭에서 word-break 가 글자 단위로 끊습니다
```

## 고칠 것

```
필수   360px 에서 카드로 바꿉니다 (listings 처럼)
필수   값 예시는 3~5개만 · 나머지는 「외 N종」
       ★ 35종의 값을 다 보여 줄 이유가 없습니다
필수   축별로 접습니다 — <details>
       지금 확정 대기 35종이 한 화면에 펼쳐집니다
필수   겹침을 없앱니다 — 폼과 표가 겹치면 안 됩니다
검산   V11-78  좁은 폭에서 글자가 세로로 떨어지지 않는가
       ★ 한 칸의 폭이 3글자 미만이면 실패로 봅니다
```

## 잘 된 것도 있습니다

```
안내가 정확합니다
  「사전은 「같은 값을 같은 것으로 인정하는 규칙」입니다.
   기계가 정하면 「흰색」과 「화이트」를 다른 것으로 두거나
   「진주색」을 흰색에 넣어 버립니다.  그 판단은 사람이 합니다」
「35종을 하나씩 누르지 않습니다.  다만 몇 종인지 먼저 봅니다」
사유 필수
```

**제가 규격에 쓴 문장이 그대로 화면에 있습니다.**

---

# 4. 다음

```
1  ★★ admin_dict 360px 깨짐 — 가장 급합니다
   글자가 세로로 떨어집니다.  쓸 수 없습니다
2  V11-78 신설 — 한 칸 폭이 3글자 미만이면 실패
   ★ 다른 화면에도 같은 일이 있는지 전수로 잡힙니다
3  관리 표를 좁은 폭에서 카드로 (listings 처럼)
4  admin_targets 드롭다운 (STEP 149r)
5  V11-77 남은 4개 — dealers quad · watch spark · recommend pbar · why curve
6  전 화면 title 툴팁
7  dealers 139KB · 720행
```

**1·2 를 먼저 하십시오.**
**★ `V11-78` 이 있으면 나머지 화면의 같은 결함도 한 번에 드러납니다.**
