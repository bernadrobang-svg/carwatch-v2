# ★★ v2 의 패착 — 개정 279

**마스터 진단 — 「CLI 있다고 개무시하니 웹이 개판이 됐다. v2 의 패착은 CLI 때문이다」**

---

# 1. 진단이 정확합니다

```
CLI 가 있으니
  → 「기능은 CLI 로 되니까 화면은 나중에」
  → 화면은 「있기만 하면」 통과
  → 시안 255종 중 58종만 반영해도 검사가 안 잡음
  → 시세가 표 · 링크 반만 · 툴팁 4곳 · 시세차 없음
```

**★ 개발측이 게을러서가 아닙니다.**
**「CLI 로 되니 화면이 못해도 일이 굴러갔다」가 원인입니다.**

---

# 2. 검사도 그렇게 만들어졌습니다

```
check_screens   절 이름만 봄
V11-*           경로 · 권한 · 쿼리 수만 봄
★ 「쓸 수 있는가」를 보는 검사가 없었습니다
```

**제 규격 잘못입니다.**

---

# 3. ★ v1 이 왜 나았나

```
v1 은 화면밖에 없었다.  거기서 안 되면 아무것도 안 됐다
그래서 화면에 전부 넣었다 — 22열 · 미리보기 · 단추 · 드롭다운
```

```
v2 는 CLI 가 있어서 화면이 「보조」가 됐다
```

---

# 4. 규격 — 개정 279

```
필수   화면이 본체다.  진입점은 화면이 부르는 도구다
필수   기능이 완성됐다는 판단은 화면에서 한다
       CLI 로 되는 것은 완성이 아니다
필수   화면을 못 만들면 그 기능은 미완이다
       「CLI 로 됩니다」로 넘기지 않는다
필수   검사는 「돌아가는가」가 아니라 「쓸 수 있는가」를 본다
       ★ 절이 있는가가 아니라 시안대로 보이는가
       ★ 링크가 걸렸는가 · 값이 몇 개 보이는가 · 몇 초 걸리는가
금지   진입점에만 있는 기능
금지   「화면은 나중에」
검산   S27  기능마다 화면이 있는가
```

---

# 5. 지금부터

```
★ 앞으로 「기능을 만들었다」는 화면이 됐을 때만 씁니다
★ 작업 기록에 「화면에서 확인」을 넣으십시오
  숫자만이 아니라 「화면에서 눌러 봤다」를
```

## S27 을 만드십시오

```
run.py 가 받는 명령 · tools/ 의 스크립트를 전부 세고
그 각각에 대응하는 화면이 있는가
없으면 그 기능은 미완이다
```

**★ V11-45 와 비슷하지만 다릅니다.**

```
V11-45   CLI 로만 되는 기능이 없는가        — 있으면 안 됨
S27      기능마다 화면이 있는가              — 목록으로 센다
```

---

# 6. 지금 밀린 것

```
1  시세차              ★ 이 도구의 핵심 값
2  반응형 세 단계       ★ 360px 이 기준
3  경과 · 변동 · 정직도 열
4  정렬 드롭다운 8종
5  data-peek 미리보기
6  V11-68 ~ V11-71
7  S27
8  ⑨ 사전 확정 화면 · ⑩ 관리 화면 전반
```

**1·2 를 먼저 하십시오.**
