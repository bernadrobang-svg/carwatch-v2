# ★★ 색인 넷 — 개정 343

**마스터 물음 — 「가이드의 인덱스, 그리고 코드와 DB는 인덱스 정리 되었니?」**

**셋 다 안 돼 있었습니다.**

---

# 1. 실측

| 색인 | 상태 |
|---|---|
| `docs/INDEX.md` | ★ 새 파일(`61-web/` · `f-table`)을 모름 — **제가 안 고쳤습니다** |
| `docs/SOURCE.md` | ★ `61-web/` 을 모름 |
| `docs/CHECKS.md` | ★ **검사 표가 비어 있음** |
| `docs/SCHEMA.md` | ★ **없었음** — DB 표 43개를 아무도 안 세었습니다 |

---

# 2. 제가 한 것

## `INDEX.md` 다시 만듦

```
63파일 · 줄 수 · KB
★ 800줄 넘는 것에 ★ 표시
정본 표 — 배점 · 화면 · DB · 검사 · 소스 · config · 원문
```

## `SCHEMA.md` 신설 — DB 표 43개

| 갈래 | 파일 | 표 |
|---|---|--:|
| 원문 | `01_raw.sql` | 3 |
| **정형** | `02_core.sql` | **13** |
| 사전 | `03_dict.sql` | 3 |
| 판정 | `04_result.sql` | 5 |
| 등록부 | `05_meta.sql` | 1 |
| 감사 | `06_audit.sql` | 2 |
| 관심 | `07_watch.sql` | 8 |
| 관리 | `08_admin.sql` | 8 |

```
★ core_listing 이 117열입니다
★ 표마다 열 수와 DDL 주석을 함께 냈습니다
```

---

# 3. ★ 개발측이 할 것 — 기계로 갱신

```
필수   색인 넷을 기계로 만든다.  손으로 적지 않는다
       INDEX.md    지시서 파일 · 줄 수 · 800줄 표시
       SOURCE.md   소스 파일 · 줄 수 · 무엇이 어디에
       CHECKS.md   검사 코드 → 규격 위치 · 소스 파일
       SCHEMA.md   DB 표 · 열 수 · 무엇
필수   ★ 4시간 점검이 갱신한다 (S29-0)
       ★ 파일이 늘거나 줄면 색인이 함께 바뀝니다
필수   색인과 실제가 다르면 실패다
검산   S28-10  SCHEMA.md 의 표가 DDL 과 같은가
       S28-11  INDEX.md 의 파일이 실제와 같은가
       S28-12  SOURCE.md 의 파일이 실제와 같은가
       S28-13  CHECKS.md 의 검사가 코드와 같은가
```

```
★ 왜 기계로 하나
  손으로 적으면 오늘처럼 빠집니다
  ★ 제가 부록 F·G 를 옮기고도 INDEX 를 안 고쳤습니다
```

---

# 4. ★ `CHECKS.md` 가 비어 있는 것이 큽니다

```
검사가 300개 가까이 되는데 색인이 없습니다
★ 「V11-79 가 어디 있나」를 grep 으로 찾아야 합니다
★ 「규격에 있는데 코드에 없는 검사」를 못 찾습니다
```

```
필수   기계로 만드십시오
       validate/ 를 훑어 Check(...) 를 뽑고
       docs/ 를 훑어 「검산 V*-*」 를 뽑아 잇습니다
필수   ★ 둘이 안 맞으면 그것이 결함입니다
       규격에 있는데 코드에 없는 검사 몇 개
       코드에 있는데 규격에 없는 검사 몇 개
```

---

# 5. 순서 — 어제부터 밀린 것 포함

```
1  ★★★ 목록 중간 폭 깨짐 (개정 337)
2  ★★★ 미분류 349건 — 판정 막는 것만 (개정 341)
3  ★★ CHECKS.md — 기계로.  규격↔코드 차이를 숫자로
4  S28-10~13 · 4시간 점검이 색인 갱신
5  V11-110 · V3-68 경로를 새 정본으로 (개정 342)
6  차트 (개정 340) · S29-4 (개정 339)
```

**★ 3 이 나오면 무엇이 안 만들어졌는지 한 번에 드러납니다.**
