# ★★ UI 지적 10건 — 개정 276

**2026-08-16 · 마스터 지적**

---

# 1. 지적

| # | 무엇 |
|:--:|---|
| ① | 링크·툴팁이 없다 |
| ② | 상세에서 엔카로 가는 링크가 없다 |
| ③ | 시세가 여전히 표다 |
| ④ | 정렬에 C·D·미분류가 잔뜩 — 볼 필요 없는 것이 앞에 |
| ⑤ | 딜러가 보유한 차종이 안 보인다 |
| ⑥ | 필터가 상단에 없다 |
| ⑦ | 필터 버튼 온·오프가 없다 |
| ⑧ | 차종 추가 때 차종키를 사람이 안다고 전제 |
| ⑨ | 사전 확정 화면이 보기 힘들다 |
| ⑩ | 관리 화면 UI 전반이 엉망 |

---

# 2. ★ 뿌리

```
규격에 「시안이 없는 화면은 어떻게 만드나」가 없었다
→ 「기능이 되면 됐다」로 만들었다
→ 검사가 통과시켰다
```

**제 규격 잘못입니다. 시안 있는 화면만 규정했습니다.**

---

# 3. 신설 규격 5건

## `STEP 149p` — 시안이 없는 화면

```
필수   시안이 있는 화면의 관행을 따른다
필수   값이 다른 화면으로 이어질 수 있으면 링크로 만든다
       ★ 차종·딜러·등급·축·매물번호는 전부 링크다
필수   줄임말·코드·기호에는 title 을 단다
필수   원문이 있는 것은 원문 링크를 준다
필수   고르는 칸은 고를 수 있게 만든다
필수   목록은 사람이 볼 순서로 낸다
금지   「기능이 되니 됐다」로 두는 것
검산   V11-61 · V11-62
```

## `STEP 149q` — 원문으로 가는 링크

```
필수   /why 에 엔카 원문 링크
       http://www.encar.com/dc/dc_cardetailview.do?carid={source_id}
필수   목록에서도 갈 수 있게
필수   새 탭 · 「엔카에서 보기」라고 적는다
근거   우리 판정은 참고다.  실제 매물은 엔카에 있다
      사람은 결국 거기서 사진을 보고 전화한다
      그 길을 막으면 도구가 아니라 벽이 된다
검산   V11-63
```

## `STEP 149r` — 고르는 칸은 고르게

```
근거   「KOLEOS_HEV」를 외워 치라는 것은 도구가 아니다
필수   이미 아는 값은 목록으로 — 제조사 · 모델군 · 연식 · 연료 · 트림
       ★ 원문(facet · 목록)에서 뽑아 채운다.  우리가 아는 값이다
필수   차종 키는 사람이 짓지 않는다.  고른 값에서 만들어 준다
필수   고른 것이 몇 건인지 즉시 — 「이 조건에 지금 매물 148건」
금지   코드·키를 사람이 외워 치게 하는 것
검산   V11-64
```

## `STEP 149s` — 목록의 기본 순서

```
필수   기본은 「사람이 먼저 볼 것」 — 비율 높은 순
필수   E · NOT_RATED 는 뒤로
필수   ★ C 이하도 뒤로 보낼 수 있게 — 「A 이상만」 단추
필수   기본으로 무엇이 걸려 있는지 적는다
금지   사람이 원하지 않는 것을 앞에 두는 것
검산   V11-65
```

## `STEP 149t` — 필터는 위에

```
필수   목록 위에.  스크롤 없이 보인다
필수   자주 쓰는 조건을 단추로 — 누르면 켜지고 다시 누르면 꺼진다
       등급 · 예산대 · 연식 · 연료 · 사고 유무
필수   켜진 단추는 amber
필수   「전부 지우기」 · 지금 몇 건인지
금지   조건을 걸려면 표의 값을 눌러야만 하는 것
       ★ 표를 눌러 거는 것은 두되, 위에도 있어야 한다
검산   V11-66 · V11-67
```

---

# 4. 그리고 — 개별 지적

## ③ 시세가 표

```
앞서 CSS 를 이식했다는데 화면이 그대로입니다
확인   .hist 가 app.css 에 들어갔는가
      market.html 이 <table> 대신 .hist 구조를 쓰는가
★ CSS 만 옮기고 HTML 구조를 안 바꾸면 그대로 표입니다
```

## ⑤ 딜러가 보유한 차종

```
필수   딜러 행에 그 딜러의 차종 분포를 낸다
       「G80 12 · GV70 8 · 그랜저 3」
필수   차종을 누르면 그 딜러의 그 차종 매물로
```

## ⑨ 사전 확정 화면

```
필수   축별로 접어서 낸다 — 41종이 한 화면에 펼쳐지지 않게
필수   값마다 몇 건에서 나왔는지 · 예시 매물 링크
필수   자주 나온 순으로
```

---

# 5. 지시

```
★ 마스터가 실제로 쓰는 화면입니다.
  「기능이 되면 됐다」가 아니라 「쓸 수 있는가」로 보십시오.

1  ③ 시세부터 — CSS 를 옮겼는데 왜 표인지 확인
   HTML 구조를 시안대로 바꾸십시오
2  ② 엔카 원문 링크 — 한 줄이면 됩니다
3  ⑥⑦ 필터를 위로 + 온·오프 단추
4  ④ 기본 정렬 + 「A 이상만」
5  ① 링크·툴팁 — 전 화면
6  ⑧ 차종 추가 드롭박스
7  ⑨ 사전 확정 화면
8  ⑤ 딜러 차종
9  ⑩ 관리 화면 전반

V11-61 ~ V11-67 을 먼저 만드십시오.
그래야 무엇이 남았는지 숫자로 압니다.

화면 하나 고칠 때마다 push 하십시오.
```
