# 규칙 11 — 소스와 검사도 색인이 있어야 한다

**마스터 물음 — 「소스도 그런 식으로 처리하나? 검사도?」**

---

# 1. 지금 상태

## 소스 — 챕터 단위 색인만 있습니다

```
MAPPING.md   30-score → analyze/ · score/
             61-web   → web/
```

**그런데 큰 파일이 있습니다.**

```
validate/v11_web.py       2,104줄
web/views.py              1,795줄
web/static/app.css        1,494줄
report/screens/build.py   1,139줄
collect/runner.py         1,096줄
report/screens/admin.py     927줄
store/adminops.py           904줄
```

**★ 「`web/views.py` 를 보라」는 1,795줄을 통째로 읽으라는 뜻입니다.**

## 검사 — 색인이 없습니다

```
검사 227개가 validate/ 10파일에 흩어져 있습니다
  v11_web    73개 · 2,104줄
  v3_logic   32개
  v2_load    31개
  v10_admin  24개
  …
```

```
「V11-79 를 고쳐라」
→ 어느 파일인지 grep 해야 압니다
→ 파일을 열면 2,104줄
```

## 그리고 규격 ↔ 검사가 안 이어져 있습니다

```
지시서   「검산 V11-79」
소스     validate/v11_web.py 어딘가
★ 「V11-79 가 규격 어디에서 왔나」를 역으로 못 찾습니다
```

---

# 2. 만들 것 — 셋

## ① `docs/CHECKS.md` — 검사 색인

```
| 코드 | 무엇 | 규격 | 소스 | 등급 |
|---|---|---|---|---|
| V11-79 | 축 칸에 맨 숫자가 나오지 않음 | 61-web STEP 149n | validate/v11_web.py | fatal |
| V3-41  | 전 매물의 분모가 만점과 같음 | 30-score a-frame | validate/v3_logic.py | fatal |
```

```
필수   227개 전부
필수   검사 코드 → 규격 위치 · 소스 파일
필수   기계로 만든다.  손으로 적지 않는다
       ★ validate/ 를 훑어 Check(...) 를 뽑고
         docs/ 를 훑어 「검산 V*-*」 를 뽑아 잇는다
필수   둘이 안 맞으면 그것이 결함이다
       규격에 있는데 코드에 없는 검사 · 코드에 있는데 규격에 없는 검사
검산   V11-90  CHECKS.md 가 실제 코드·규격과 일치하는가
```

## ② `docs/SOURCE.md` — 소스 색인

```
| 파일 | 줄 | 무엇 | 챕터 | 주요 함수 |
|---|--:|---|---|---|
| web/views.py | 1,795 | 화면 핸들러 | 61-web | view_listings · view_why · … |
| validate/v11_web.py | 2,104 | 화면 검사 | 61-web · 20-verify | 73개 검사 |
```

```
필수   200줄 넘는 파일은 「무엇이 어디에」를 적는다
       예   web/views.py
              1~200    공통 · 세션
              200~600  목록
              600~900  근거
필수   기계로 만든다
검산   V11-91  SOURCE.md 가 실제 파일과 일치하는가
```

## ③ 큰 파일을 쪼갠다

```
확인   2,104줄 · 1,795줄이 적정한가
       ★ 한 파일이 크면 색인이 있어도 결국 다 읽게 됩니다
제안   validate/v11_web.py 를 나눈다
         v11_route.py    경로 · 권한
         v11_render.py   렌더 결과
         v11_visual.py   시안 대조
       web/views.py 를 나눈다
         views_public.py  매물 · 근거 · 시세
         views_admin.py   관리
★ 다만 계층은 지킨다 (STEP 15a).  나누기만 한다
필수   나눌지 말지는 개발측이 판단하고 근거와 함께 보고한다
```

---

# 3. ★ 왜 이것이 필요한가

```
지금   「V11-79 를 고쳐라」  →  2,104줄을 읽는다
앞으로  CHECKS.md 로 파일·줄을 짚고  →  그 부분만 읽는다
```

```
★ 그리고 규격 ↔ 코드 대조가 자동으로 됩니다
  「규격에 검산 V3-45 가 있는데 코드에 없다」를 검사가 잡습니다
  지금은 사람이 세야 합니다
```

---

# 4. 확인해 주십시오

```
1  지금 소스를 어떻게 찾습니까
   grep 입니까 · 파일을 통째로 읽습니까
2  한 작업에 소스를 몇 줄 읽습니까
3  큰 파일(2,000줄)을 읽을 때 어떻게 합니까
   ★ 부분만 읽습니까 · 전체를 읽습니까
```

---

# 5. 순서

```
1  CHECKS.md 를 기계로 만드십시오  ← 가장 급합니다
   ★ 규격 ↔ 코드가 안 맞는 검사가 몇 개인지 그것으로 압니다
2  SOURCE.md
3  큰 파일 분할 여부 판단
4  규칙 10·11 을 CLAUDE.md 에
```

**★ 1번이 검사를 늘려 온 지금 가장 필요합니다.**
**227개가 규격과 맞는지 아무도 확인한 적이 없습니다.**
