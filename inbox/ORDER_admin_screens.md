# ★★★ 관리 화면 설계 — 개정 369

**마스터 지적 — 「이놈은 사이즈만 크지 왜 이리 낭비가 많아.**
**제대로된 설계나 가이드 줬니?」**

**★ 안 줬습니다. 제 잘못입니다.**

---

# 1. 무엇이 잘못이었나

```
61-web/   목록 · 추천 · 상세 · 비교만 정했습니다
13장      관리 요구 172건에 대한 화면 설계가 없었습니다
★ 그래서 「화면 없음」이 115건으로 떴습니다
```

## ★ 그런데 115건 중 96건은 「해당 없음」이었습니다

```
화면이 정말 필요한 것   19건
화면이 필요 없는 것     96건
  「금지 화면 숨김만으로 권한을 대신」   ← 서버 규칙
  「금지 파일을 직접 편집」            ← 규칙
  「resume_point 를 남긴다」          ← 저장 규칙
  「금지 도구가 core_ 를 직접 UPDATE」  ← 코드 규칙
```

```
★ 개발측이 금지·코드 규칙에까지 화면 칸을 요구했습니다
★ 「해당 없음」으로 바꿔야 합니다 (V11-123)
```

---

# 2. 관리 화면 19개 — `docs/chapters/61-web/h-admin.md`

| # | 경로 | 누가 |
|--:|---|---|
| 1 | `/admin` | 관리자 |
| 2 | `/admin/status` | 관리자 |
| **3** | **`/admin/collect`** | **마스터** |
| 4 | `/admin/import` | 관리자 |
| 5 | `/admin/run` | 관리자 |
| **6** | **`/admin/dict`** | **마스터** |
| **7** | **`/admin/registry`** | **마스터** |
| **8** | **`/admin/targets`** | **마스터** |
| 9 | `/admin/scoring` | 관리자 |
| 10 | `/admin/config` | 관리자 |
| 11 | `/admin/account` | 관리자 |
| 12 | `/admin/audit` | 관리자 |
| **13** | **`/admin/query`** | **마스터** |
| 14~17 | 탐색 넷 | 관리자 |
| 18 | `/reports` | 관리자 |
| 19 | `/notready` | 관리자 |

**★ 「마스터」 다섯이 가장 중요합니다. 매일 손대시는 화면입니다.**

---

# 3. 공통 규칙

```
필수   무엇을 하는 화면인지 한 줄로 맨 위에
필수   실패하면 무엇이 왜 실패했는지 화면에 — 「로그를 보라」 금지
필수   407·403·429·502 를 그대로 낸다.  삼키지 않는다
필수   되돌릴 수 없는 것은 미리보기를 먼저
필수   결과를 화면에서 본다.  파일을 내려받게 하지 않는다
검산   V11-124 · V11-125 · V11-126
```

---

# 4. 화면마다 무엇이 있어야 하는지 적었습니다

```
collect    범위 → 조회 → 확인 → 저장 · 차종 단위 진행 · 막힘 갈래
dict       축별 접기 · 값·건수·출처·제안 · 미리보기 · [확정][폐기][나중에]
registry   ★ 판정 막는 32건만 · 값 분포 · 쓰임 · 어디 있었나 · 선택지
targets    ★ 드롭다운 (국산/수입 → 제조사 → 모델그룹 → 모델)
query      큰 붙여넣기 상자 · 복사 · 저장 · 실패 사유
status     ★ 큐·도는 것·마지막 처리 셋 · 4시간 점검 결과
admin      할 일이 있으면 크게 — 「미분류 32건이 판정을 막고 있습니다」
…          19개 전부
```

---

# 5. 할 일 — 순서

```
1  ★ 「화면 없음」 354건을 다시 가른다
   화면 필요 · 해당 없음 · 미정
   ★ 13장은 115 → 19 로 줄어듭니다
   ★ 다른 장도 같은 비율일 것입니다

2  ★ 남은 「화면 필요」를 h-admin.md 대로 만든다
   ★ 마스터가 쓰는 다섯부터 — collect · dict · registry · targets · query

3  V11-123 ~ V11-127

4  끝나면 숫자로 — 「화면 없음 354 → N」
```

---

# 6. ★ 제 잘못을 적습니다

```
S27 「화면이 본체다」를 규격에 넣어 놓고
★ 정작 규격을 쓸 때 화면을 같이 안 정했습니다
★ 관리 화면 19개에 대한 설계를 오늘에야 만들었습니다
```
