# 서버가 살아 있는가 — ★ 실측 규격

```
version  SPEC-2026.08.25-r727
follows  `docs/INDEX.md`
sources  실측 08-23
checks   S46-38 · S46-39
```


`SPEC-2026.08.23-r600` · 2026-08-23 · **마스터가 AWS 에서 원인을 잡으셨다**

```
★★ 08-23 — ★ 서버가 ★ 세 번 죽었다.  ★ 한 번은 ★ 3시간 15분 안 살아났다
★ 가이드는 ★ 그 사이 ★ 열 번 넘게 재시도하며 ★ 「배포 검증 못 함」만 적고 있었다
★★★ ★ 원인을 ★ 마스터가 잡으셨다.  ★ 그것이 ★ 가장 비싼 시간이다 (오판 #70)
```

---

# 0. ★★★ 08-23 정정 — ★ 1장의 진단이 ★ 틀렸다 (개정 596)

```
★★ 개발측이 ★ v210 §6ⓐⓑ 에서 ★ 근거를 대고 반증했다.  ★ 가이드가 재서 ★ 받아들인다
★ ★ 아래 1장은 ★ 마스터 진단을 ★ 가이드가 ★ 검증 없이 그대로 옮겨 적은 것이다 (오판 #72)
```

| 적었던 것 | ★ 실측 |
|---|---|
| 「BrokenPipeError 로 ★ 프로세스가 **통째로 죽는다**」 | ★ **안 죽는다.** 표준 라이브러리 `socketserver.py:317` 이 `except Exception:` 으로 **이미 잡는다** |
| 「`StartLimitIntervalSec` 이 없어 ★ 재시작을 포기했다」 | ★ **되살릴 것이 없었다.** 08-23 journal 에 `Main process exited`·`Scheduled restart` 가 ★ **0건** |
| 「그래서 3시간 15분」 | ★ **다른 까닭이다 — 아래 0a장** |

```
★ ①②③ (예외 방어 · service 값)은 ★ 옳고 해가 없다 — ★ 그대로 둔다
★ ★ 그러나 ★ **오늘의 사고를 막는 것은 ★ 그것이 아니다**
```

---

# 0a. ★★★ 진짜 원인 — ★ 빈 연결 ★ 하나가 ★ 서버 전체를 멎게 한다

```
★★ 개발측 실측 (8799 · 고치기 전/후 ★ 둘 다 같다)
   붙기 전          /listings = 200 (0.7초)
   ★ 빈 연결 하나 뒤 /listings = ★ 못 받음 (8.0초 TimeoutError)
   그 연결 닫은 뒤   /listings = 200 (1.2초)

★ `serve()` 192행이 ★ `HTTPServer` — ★ 단일 스레드다
★ `handle_one_request` 의 ★ `rfile.readline()` 에서 ★ **영원히** 기다린다
★ `BaseRequestHandler.timeout` 이 ★ `None` 이라 ★ 깨어나지도 않는다
★★ ★ 붙기만 하고 ★ 아무것도 안 보내는 연결 ★ 하나면 ★ 서버가 멎는다
```

## ★ journal 이 맞물린다

```
마지막 로그   2026-08-23 11:59:29   (그 줄이 BrokenPipeError 다)
그 뒤        15:15:12 재부팅까지 ★ 아무것도 안 찍혔다
그 사이      PID 1409411 은 ★ 살아 있었다 — ★ 그래서 systemd 가 손대지 않았다
11:59 → 15:15 = ★ **3시간 16분**  = ★ 마스터가 말씀하신 「3시간 15분」
```

```
★★★ ★ 뜻 —
   ★ 프로세스는 ★ 죽은 것이 아니라 ★ **멎은 것**이다
   ★ `Restart=always` 도 `StartLimitIntervalSec=0` 도 ★ 이 사고에는 ★ 아무 일도 안 한다
   ★ ★ 브라우저의 preconnect · 죽은 keep-alive ★ 하나로 ★ 재발한다
★ ★ 그리고 ★ 개정 594 의 「사슬」도 ★ 절반만 맞았다 —
   ★ ② 단일 스레드가 ★ 진짜였고 · ★ ④⑤ (BrokenPipe 사망 · 재시작 포기)는 ★ 아니었다
   ★ `/compare` 117초는 ★ **원인이 아니라 ★ 같은 병의 다른 증상**이다 —
     ★ 한 요청이 오래 돌면 ★ 그동안 전부 막히는 것이 ★ 똑같다
```

## ★★ 그러므로 ★ 고칠 것 — ★ 가이드 판정 (개발측 안 ①② 를 ★ 받아들인다)

```
★ ① `serve()` 의 ★ `HTTPServer` → ★ `ThreadingHTTPServer`
★ ② `Handler.timeout = 30` · `daemon_threads = True`
★★ ★ 이 둘이 ★ 진짜 고침이다 — ★ 명령서 ★ 1b장
★ ★ 개발측이 ★ 안 하고 ★ 지시를 기다린 것이 ★ 옳다 (규칙 2 · 금지 8)
```

---

# 0b. ★★★ 고쳐졌다 — ★ 가이드가 ★ 배포에서 ★ 직접 봤다 (개정 599)

```
★★ 여태 아무도 못 하던 시험이다.  ★ 「마스터 화면이 그 자리에서 멎는다」고 ★ 8799 에서만 쟀다
★ ★ 이제 배포(8765)에서 ★ 직접 했다.  ★ 안 멎는다
```

| 무엇 | 고치기 전 (개발측 8799 실측) | ★ 고친 뒤 (★ 가이드 배포 실측) |
|---|---|---|
| 붙기 전 | `/listings` 200 · 0.7초 | 200 · 5.7초 |
| ★ **빈 연결 하나 뒤** | ★ **못 받음 · 8.0초 TimeoutError** | ★★ **200 · 3.5초** |
| ★ **빈 연결 여섯 뒤** | (안 쟀다) | ★★ **200 · 3.5초** |
| 24초 동안 이어서 | — | ★ 200 · 2.5초 · **줄곧 살아 있다** |
| 빈 연결 닫은 뒤 | 200 · 1.2초 | 200 · 1.7초 |

```
★★★ ★ 「빈 연결을 붙여 둔 채 ★ 다른 요청이 200 으로 오는 것을 ★ 봤다」
★ 완료 조건 그대로다.  ★ 「고쳤다」가 아니라 ★ 「봤다」다
```

## ★ `/compare` 도 ★ 이제 남을 안 막는다

```
★ `/compare` 가 ★ 126.2초 도는 ★ 그 동안에 —
   /          → ★ 200 · 3.2초
   /listings  → ★ 200 · 2.3초
   /admin     → ★ 403 · 0.4초 (쿠키 없이 부른 것 — 정상)
★★ ★ 개정 594 의 「사슬」 ② 가 ★ 끊겼다 — ★ 한 요청이 ★ 더는 전체를 막지 않는다
★ ★ 그러나 ★ `/compare` 자체는 ★ 9,299,483B · 126.2초 ★ 그대로다 —
   ★ 그 화면을 여는 사람은 ★ 여전히 ★ 2분을 기다린다.  ★ 상한이 ★ 아직 필요하다 (명령서 8단계)
```

## ★ 무엇이 들어갔나 (실측 · `web/server.py`)

```
17행   from http.server import BaseHTTPRequestHandler, ★ ThreadingHTTPServer
111행  timeout = IDLE_TIMEOUT          ★ 안 보내는 연결을 버린다
229행  httpd = ★ ThreadingHTTPServer((host, port), make_handler(app))
231행  httpd.daemon_threads = True
```

---

# 1. ★ 처음 진단 (★ 0장이 정정했다 — ★ 기록으로 남긴다)

```
메모리 ✘ (`dmesg` OOM ★ 0건)     디스크 ✘
★ web/server.py:113  `_send` 의 ★ self.wfile.write(body)
  → ★ BrokenPipeError [Errno 32] 로 ★ 프로세스가 ★ 통째로 죽는다
  → ★ 171행 `_error` 경로에서 터진다 — ★ 오류 화면을 보내려는데 ★ 상대가 이미 끊었다
  → ★ 클라이언트 타임아웃 · ★ 브라우저를 닫기만 해도 난다
```

## ★ 가이드가 함께 잰 것

| 무엇 | 실측 |
|---|---|
| `_send` 113행 | ★ `try` 가 **없다** |
| `deploy/carwatch.service` | `Restart=always` · `RestartSec=5` 는 **있다** |
| 〃 | ★ **`StartLimitIntervalSec` 가 없다** → systemd 기본(10초에 5번)에 걸려 **재시작을 포기한다** |
| `serve()` 192행 | ★ **`HTTPServer` — `ThreadingHTTPServer` 가 아니다** (별개 문제) |

```
★★ ★ 뜻 — ★ 마스터가 ★ 화면을 닫기만 해도 ★ 사이트가 죽는다
★ 그리고 ★ 연속으로 죽으면 ★ systemd 가 ★ 되살리기를 포기한다 — ★ 그래서 3시간 15분이다
★ 고치는 것은 ★ 개발측이다 (명령서 1장).  ★ 가이드는 ★ 코드를 손대지 않았다
```

---

# 1a. ★ `/compare` — ★ 같은 병의 ★ 다른 증상 (개정 594 · ★ 0a 가 뜻을 고쳤다)

```
★ 08-23 배포가 살아난 뒤 ★ 화면마다 응답 시간을 쟀다
```

| 화면 | 상태 | 크기 | 시간 |
|---|:--:|--:|--:|
| `/` · `/listings` · `/market` · `/notready` | 200 | ~346KB | ★ **0.6 ~ 1.9초** |
| `/dealers` · `/recommend` · `/reports` | 200 | ~182KB | 0.7 ~ 1.4초 |
| `/detail/166` · `/why/166` · `/admin` · `/admin/collect` | 200 | ~66KB | 0.8 ~ 2.1초 |
| ★★★ **`/compare`** | 200 | ★ **9,299,975B (9.3MB)** | ★★★ **117.4초** |

```
★★ ★ `/compare` ★ 하나만 ★ 다른 화면의 ★ 60배 느리다
★ 밀린일 22 는 ★ 「8.7MB · 29초」였다 — ★ **9.3MB · 117초로 ★ 네 배 나빠졌다**
```

## ★★★ 사슬 — ★ 이렇게 죽는다

```
① `/compare` 가 ★ 117초를 돈다
② ★ `serve()` 가 ★ `HTTPServer` — ★ 단일 스레드다 (192행)
   → ★ 그 117초 동안 ★ 다른 요청이 ★ 전부 막힌다
③ ★ 막힌 사람들이 ★ 브라우저 타임아웃으로 ★ 끊는다
④ ★ 서버가 ★ 뒤늦게 응답을 쓰려 한다 → ★ `_send` 113행 `wfile.write`
   → ★ **BrokenPipeError** → ★ 프로세스 사망
⑤ ★ `StartLimitIntervalSec` 이 없어 ★ systemd 가 ★ 재시작을 포기
   → ★ **3시간 15분**

★★ ★ 개정 596 정정 — ★ ④⑤ 는 ★ 일어나지 않았다 (0장).  ★ 프로세스는 ★ 안 죽는다
★★★ ★ 남는 것은 ★ ①②다 — ★ **한 요청이 오래 돌면 ★ 그동안 전부 막힌다**
   ★ 빈 연결 하나(0a장)와 ★ **같은 병**이다.  ★ 뿌리가 ★ 단일 스레드다
★ ★ 그러므로 ★ `/compare` 상한(⑤)은 ★ 여전히 필요하나 ★ **`ThreadingHTTPServer`(1b장)가 먼저다**
```

## ★ 그러므로 ★ 명령서 1단계에 ★ 하나를 더한다

```
⑤ ★★ `/compare` 를 ★ 당장 막는다 —
   ★ 고르는 법 둘 (개발측이 정하고 ★ 작업기록에 까닭을 적어라)
     ⓐ ★ 비교 대상 수에 ★ 상한을 둔다 (예 10대) — ★ 넘으면 400 과 안내
     ⓑ ★ 응답 크기·시간 상한을 두고 ★ 넘으면 ★ 자르고 안내한다
   ★ ★ 어느 쪽이든 ★ **9.3MB 를 그대로 내보내는 것을 멈춘다**
   ★ 화면을 ★ 지우지 마라 — ★ `demoted_menu` 에 있는 화면이다 (개정 427)
★ 그리고 ★ ①~④ 를 한 뒤에 ★ 여쭐 것 —
  ★ `HTTPServer` → `ThreadingHTTPServer` (밀린일 97).
  ★ 사슬 ②를 끊는 것이라 ★ 값이 크다.  ★ 그러나 ★ ①~⑤ 가 먼저다
```

```
검산  S46-9 ★ 신설 — ★ 배포 주소의 ★ 모든 화면이 ★ 10초 안에 응답하는가
      ★ 넘으면 실패.  ★ 지금 돌리면 ★ `/compare` 에서 실패가 난다 — ★ 그것이 첫 일이다
```

---

# 2. ★★★ 503 을 가르는 법 — ★ 정정한다 (오판 #63 이 불완전했다)

```
★ 개정 555 에서 ★ 「503 에 `server` 헤더가 없고 본문이 `upstream connect error` 면
  ★ 우리 조사창 프록시다」라고 ★ 적었다.  ★ ★ 그것이 ★ 절반만 맞았다
★★ ★ 우리 서버가 ★ 죽어 있을 때도 ★ 똑같은 꼴이 온다 —
   ★ 프록시가 ★ 상대에 못 붙은 것이지 ★ 어느 쪽이 문제인지는 ★ 안 알려 준다
```

| 본문 끝 문구 | 뜻 | 실측 예 |
|---|---|---|
| `remote reset` · `remote connection failure` | ★ 상대가 ★ **붙자마자 끊었다** — 차단일 수 있다 | 다나와 · 벤츠 · BMW 코리아 |
| ★ **`connection timeout`** | ★ **상대가 ★ 응답을 안 한다 — ★ 꺼져 있을 수 있다** | ★ **우리 배포 서버 (08-23)** |

```
필수  ★ 503 을 보면 ★ 본문 ★ 마지막 문구까지 읽는다
필수  ★ `connection timeout` 이면 ★ **「상대가 꺼졌나」를 ★ 먼저 의심한다** —
      ★ 우리 서버라면 ★ 죽은 것이다.  ★ 남의 사이트라면 ★ 잠시 뒤 다시 재 본다
금지  ★ `upstream connect error` 만 보고 ★ 「우리 창 탓」으로 ★ 단정하는 것
      ★ ★ 08-23 에 ★ 가이드가 ★ 정확히 그렇게 단정하고 ★ 세 시간을 흘려보냈다
검산  ★ 502(0B)와 503 이 ★ 섞여 나오면 ★ **상대 서버가 오르내리는 중이다** —
      ★ 프록시 탓이 아니다.  ★ 08-23 실측이 그 꼴이었다
```

---

# 3. ★★ 감시가 없다 — ★ 이것이 더 크다

```
★ 서버가 죽은 줄을 ★ 마스터가 ★ 화면을 열어 보고 아셨다
★ ★ 아무도 · 아무것도 ★ 알려 주지 않았다
★ ★ 가이드는 ★ 「503 이 계속 난다」를 ★ 네 바퀴에 걸쳐 적으면서도
   ★ ★ 「서버가 죽었을 수 있다」에 ★ 이르지 못했다
```

| 없는 것 | 무슨 일이 생기나 | 누가 |
|---|---|:--:|
| ★ **감시·알림** | 죽어도 ★ 아무도 모른다 | ★ **마스터** |
| ★ **SSM 역할** | 서버에 들어갈 길이 ★ SSH 하나뿐 · ★ 그것마저 배너를 못 보냈다 | ★ **마스터** |
| ★ **탄력적 IP** | 껐다 켜면 ★ 주소가 바뀐다 — ★ 문서·규격의 IP 가 전부 죽는다 | ★ **마스터** |

```
★ 셋 다 ★ AWS 콘솔에서 ★ 마스터만 하실 수 있다 — ★ 밀린일 94·95·96
★ 가이드가 할 수 있는 것 — ★ 배포 상태를 ★ 바퀴마다 재고 ★ 기록에 남기는 것뿐이다
```
