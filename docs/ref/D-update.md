# 부록 D. 현행화 규칙

```
version  SPEC-2026.08.25-r727
follows  ★ 참고 — 정본이 아니다
sources  실측 08-22
checks   S46-38 · S46-39
```


**부록만 고치면 되는 것과, 본문도 함께 고쳐야 하는 것을 나눈다.**

| 바뀐 것 | 부록 | 본문 |
|---|:--:|:--:|
| 검사 항목 추가 | **A · E** | STEP 5.2b 한 줄 |

```
★ 부록 A 는 항목 목록, 부록 E 는 규격표다.  둘 다 고친다
사고   본문만 고치고 부록 A·E 가 「11종」·「15종」으로 남아 있었다
      부록이 낡으면 다음 사람이 옛 개수를 믿는다
```
| `config` 키 추가 | B | **STEP 6 표 필수** |
| `config` 값 변경 | B | — |
| 표본 추가 | C | — |
| 표본이 검증하는 규칙 변경 | C | **해당 STEP 필수** |
| 배점 · Component 변경 | B | **STEP 68 필수** |

```
검사   check_spec ⑤   본문 config 표 ↔ 부록 B 예시
      check_spec ⑦   본문 배점 표 ↔ 부록 B scoring 예시
      check_src S13  부록 B 예시 ↔ 실제 config 파일
```

**세 검사가 본문 → 부록 → 파일 사슬을 잇는다. 한 곳만 고치면 걸린다.**

---

---

