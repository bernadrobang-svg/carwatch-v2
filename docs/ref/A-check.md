# 부록 A. 문서 자체 점검 (`check_spec.py`)

```
version  SPEC-2026.08.24-r685
follows  ★ 참고 — 정본이 아니다
sources  실측 08-22
checks   S46-38 · S46-39
```


## 두 방향의 실패 — 이 문서가 실제로 겪은 것

```
V4-13    검사가 과하면  →  정상에서 실패가 쏟아져 사람이 검사를 끈다
STEP 5.2b 검사가 느슨하면 →  통과했다고 믿게 만든다.  검사 없는 것보다 나쁘다
```

**둘 다 이 문서를 만들며 한 번씩 겪었다. 검사를 추가할 때 돌아올 지점이다.**

### 5.2b 정의서 완결성 자체 점검

**「정의서 없는 장은 착수하지 않는다」를 선언했으므로 문서 자체를 점검한다.**

```
tools/check_spec.py — 3종 검사

```
① 코드 펜스 짝수      미닫힘이 있으면 이후 전 블록의 렌더링이 뒤집힌다
                    이 문서는 코드 블록으로 규격을 구분하므로 치명적이다
② 유형별 필수 항목    헤딩의 [판정] 표기를 보고, 첫 코드 블록의 라벨을 검사한다
                    라벨은 줄 첫머리에 온다 — 목적 · 원천 · 값규칙 · 근거 · 금지 · 검산
③ 참조 무결성        STEP 참조가 실재 헤딩을 가리키는가
```

```
④ 구조체 정의       class 정의 ∪ (필드 타입 · 반환 화살표 · **함수표 「출력」 열**)
                  표에만 있는 반환형이 정의를 빠뜨리는 경로다
⑤ config 대조      본문의 config/*.json · config.{파일}.{키} → 전량 표에 있는가
⑥ 접미사           _cnt(원문 집계) 와 _count(우리 산출) 가 섞였는가
⑦ 산술 검산 ★      표에서 세는 수와 문서가 적어둔 수가 같은가
                  Σ(축 배점) == total_points
                  count(Component) == 본문의 「N Component」 전건
⑧ 컬럼 참조        `테이블.컬럼` 참조가 DDL 에 있는가
                  컬럼명을 바꿀 때 참조가 따라오지 않는 것을 잡는다
```

```
실사고   watch_track 의 상태 컬럼을 listing_status 로 바꿨는데
        CHECK 선언과 본문 표가 옛 이름을 가리켰다.  ⑧ 이 잡았다
```

```
★ ⑦ 이 없으면 배점을 조정할 때마다 같은 자리가 어긋난다
  실사고 — spec 성분이 6개에서 7개로 늘었는데 옛 개수가 6곳에 남아
  S9 검증식이 잘못된 행 수를 기대할 뻔했다.  ⑦ 검산이 잡았다
```

```
fatal   ① 전건 · ② [판정] 유형 · ③ 전건 · ④ 전건 · ⑦ 전건 · ⑧ 전건
참고    [판정] 외 유형의 항목 누락.  유형마다 필요한 것이 다르다
번호    25a · 82b · 103a 같은 삽입 스텝이 있다.  개수를 상수로 박지 않는다
```

**②의 판정 방식** — 본문 어딘가에 그 단어가 있는지가 아니라,
**헤더 블록의 줄 첫머리 라벨**로 본다. 느슨하게 검사하면 통과해 버린다.
실제로 느슨한 검사에서 「전건 통과」가 나왔다가, 라벨 기준으로 바꾸니 13건이 드러났다.
    STEP · 목적 · 원천 · 입력 · 출력 · 사용필드 · 근거 · 금지 · NULL/실패 · 검산
  누락이 있으면 목록을 출력하고 실패한다
```

**원칙만 선언하고 문서에 적용하지 않으면 v1 과 같아진다.**


---

