# 아키텍처 2판 — 파일 수집 · 적재 · 판정

```
version  SPEC-2026.09.01-r1042
follows  docs/chapters/10-collect/00-intro.md · docs/chapters/11-store/a-key.md
sources  마스터 확정 08-29 · 개발측 물음 58개(`outputs/QUESTIONS_ARCH_20260829.md`)
         가이드 답(`outputs/ANSWERS_ARCH_20260829.md`) · 실측 08-29
checks   S46-151 · S46-166 · S46-169 · V2-01(★ 08-29 에 받았다 · 6장)
```

★ **1판을 대신한다.** ★ 1판은 ★ **08-29 에 지웠다** — ★ 판을 둘 두지 않는다 (감독 지적 ③).
★★ ★ **3걸음을 채웠다** — ★ `V2-01` 을 받았다 (6장).  ★ 열쇠는 `(site, source_id)` 다.

---

# 1. 왜 바꾸나 — ★ 오늘 잰 것이 까닭이다

| 잰 것 | 수 | 무엇을 말하나 |
|---|--:|---|
| A 등급이 엔카에만 | 엔카 **166** · 아홉 곳 **0** | ★ 견줄 수 없다 |
| C40 1위 근거 있는 축 | **635 / 910** | ★ 상세를 못 받아 275점이 빈다 |
| 같은 차 짝 | **0건** | ★ 「엔카에서 싸게」를 못 한다 |
| `raw_response` | 194,583행 · `body` **258.7MB** | ★ DB 가 커진다 |
| `outputs/check-tmp` | **12GB** | ★ 검사가 흘린 것 (08-29 에 지웠다) |
| 살아 있는데 죽인 매물 | ★ **74대** (K카 12 · 다섯 곳 61 · 렉서스 1) | ★ 목록에 없다고 죽이면 안 된다 |

```
★★ ★ **마지막 줄이 이 설계의 까닭이다** — ★ 「목록에 없다 = 팔렸다」가 ★ **틀렸다**
   ★ ★ 3걸음이 ★ 그것을 ★ **상세로 확인하고 죽이는 자리**다
```

---

# 2. 다섯 걸음

```
 1걸음 · 목록 받기      사이트 → 파일.  ★ DB 를 안 연다
        raw/{site}/list/{YYYY-MM-DD}/page-{묶음키}-{NNNN}.json
        ★ 엔카도 여기 들어온다 — 마스터 회선이 받아 준 것도 파일로
        ★ origin 을 파일 안에 적는다 (collector · browser · import)
                        ↓
 2걸음 · 목록 → 임시표  파일 → stage_{run_id}.db   ★ 통신 없다
        ★ 함께 outputs/stage/{run_id}.jsonl 을 뜬다 — ★ 가이드가 그것으로 검증한다
                        ↓
 3걸음 · ★ 무엇을 더 받을지 정한다        ← ★★ **비어 있다.  V2-01 을 기다린다**
                        ↓
 4걸음 · 상세 받기      「받을 것」만 → 파일
        raw/{site}/detail/{YYYY-MM-DD}/{앞2}/{source_id}.json
                        ↓
 5걸음 · 상세 → 임시표 → 맞대기 → 판정.  ★ 바뀐 것만 재판정
```

## ★ 파일 꼴 (물음 #6~#16 의 답)

```
한 파일  {"site","endpoint","source_id","url","http_code","status","fetched_at","body"}
누름     store/raw.pack_body(`CWZ1`) 를 그대로 쓴다 — ★ 새로 만들지 않는다
반쪽     `.part` 로 쓰고 os.replace.  ★ 2걸음이 `.part` 를 안 읽는다
다시 받음 덮어쓰지 않는다 — `{source_id}__{HHMMSS}.json` 으로 붙인다 (P3)
실패     status='error' 로 ★ 남긴다.  ★ 「없음으로 저장하지 않는다」는 core_listing 규칙이다
자리     config/deploy.json 의 work_dir 아래 raw/ — ★ 코드에 안 박는다
```

## ★ 임시표 (물음 #17~#21·#55 의 답)

```
꼴    stage_{run_id}.db  ★ 따로 둔 파일이다 — ★ 본 DB 와 잠금을 안 다툰다
칸    목록·상세가 주는 칸만.  ★ core_listing 을 늘리지 않는다
PII   ★ 안 들어간다 — split_pii 를 2걸음에서 먼저 부르고 plate_hash 만 담는다
자식  stage_record · stage_inspection 도 둔다 — ★ 「임시표만 버리면 된다」가 깨지지 않게
클렌징 ★ 회차가 끝나면 파일째 지운다.  ★ 다음 회차 시작 때 남은 것이 있으면 먼저 지운다
      ★ ★ 「살아 있나」는 시각이 아니라 ★ collect_run 표로 본다
```

## ★ 적재 로그 (물음 #36~#39 의 답)

```
표    load_log — 한 파일 = 한 줄
칸    file_path(열쇠) · site · endpoint · source_id
      file_bytes · file_hash(sha256 · ★ 압축 전 원문) · parse_version
      loaded_at(★ 적재한 날) · run_id
      rows_in · rows_new · rows_changed · rows_same · status
필수  넣기 전에 file_path 로 대본다 — ★ 이미 있으면 안 넣는다
필수  파일이 다시 받아졌으면 file_hash 가 다르다 — ★ 그때만 다시 넣는다
필수  파서가 바뀌면 parse_version 이 다르다 — ★ 그때도 다시 넣는다
금지  ★ 지우는 것.  ★ 한 줄이 작다 (24,717줄이 몇 MB 다)
★ core_listing.first_seen(매물 기준)과 ★ 섞지 않는다 — load_log 는 파일 기준이다
```

---

# 3. 상태 — ★ 사이트가 말한 대로

| 칸 | 무엇 | 누가 정하나 |
|---|---|---|
| `sales_status` | ★ **사이트가 준 값 그대로** (`resvYn=Y` · `CONTRACT` · `listed` …) | 사이트 |
| `status` | 우리 판정 (`new·active·gone·relisted·out_of_scope`) | 우리 |

```
★ 우리 말로 옮기는 것은 ★ dict_enum 이 한다.  ★ 화면은 mapped 를 본다
★ 실측 08-29 — CONTRACT 830 · RESERVED 98 · K카 resvYn=Y 118
★★ ★ 팔린 차·계약 중은 ★ /listings 조회에서 빠지고 ★ /sold 와 통계에는 나온다
   ★ ★ core_listing 에는 ★ 남는다 (P3)
```

---

# 4. ★★★ 목록에 없다고 죽이지 않는다 (08-29 에 74대를 죽였다)

```
★ 목록에서 사라진 것은 ★ **gone 후보**다.  ★ 바로 gone 이 아니다
★ ★ **상세를 눌러 확인한 뒤** 죽인다.  ★ 확인이 안 되면 ★ 안 죽인다

「살아 있음」의 근거 — ★ 사이트마다 다르다 [실측 08-29]
  K카      data.rvo.carCd 가 있다 (없는 매물도 200·3,256B 를 준다)
  렉서스    car_detail 이 있다 (200·12,585B) ↔ 없으면 200·71B
  헤이딜러  403 은 ★ 오류가 아니라 「판매중 아님」이다
  그 밖    그 사이트 parse_detail 이 값을 뽑는다
★★ ★ **낱말로 가르지 마라** — 보배 「판매완료 573」은 ★ 딜러 실적이다
★ 가드 「끝까지 받았나」는 ★ 「이 창구를 끝까지」이지 ★ 「이 창구가 전량인가」가 아니다
```

## ★★★★★ 4-2. ★ **언제 「판매 완료」로 매기나** (마스터 확정 08-29)

```
★ 마스터 — 「★ **그냥 목록이 없으면 확인을 바로 하는 게 낫지 않아?
   ★ 그래서 하루로 일단 잡고 ★ 팔 일이 아니라 삼 일로 해서**」
★ ★ **맞다.  ★ 내 안(사흘 뒤 확인 · 열흘 뒤 포기)이 늦었다**
```

| 갈래 | 언제 | `status` | `sales_status` | 목록에 |
|---|---|---|---|---|
| 사이트가 말했다 | `sales_status` 가 「판매완료·계약중」 | `gone` | 사이트 원문 | ★ 안 나온다 |
| 상세가 없어졌다 | ★ **그 회차에 바로 눌러** 「없음」 | `gone` | `detail_gone` | ★ 안 나온다 |
| 상세가 살아 있다 | 눌러 보니 있다 | ★ **`active`** | — | ★ **나온다** |
| 못 받는다 | 403·503·토큰 | 그대로 | — | 나온다 |
| ★ **사흘 못 받았다** | 잇달아 3회 | `gone` | ★ `unreachable` | 안 나온다 |

```
★ **부하를 쟀다** [실측 08-29] —
   ★ 한 바퀴 gone 후보 ★ **100건 안팎** · K카 상세 ★ **건당 0.85초**(표본 5건 · 4.3초)
   ★ ★ 곧 ★ **100건이면 85초** — ★ 하루 한 바퀴에 ★ 1분 반이면 된다.  ★ **미룰 까닭이 없다**

필수  ★ 목록에서 빠지면 ★ **그 회차에 바로 누른다**.  ★ 미루면 ★ 그 사이 마스터께서 팔린 차를 보신다
필수  ★ **못 받은 것만** 다음 바퀴로.  ★ 살아 있는 것은 ★ 그 자리에서 되살린다
필수  ★ 세는 법은 ★ `last_seen` 과 오늘의 차 — ★ 새 칸을 만들지 않는다
금지  ★ 확인 없이 죽이는 것 (08-29 에 74대)
검산  ★ `S46-169`
```

---

# 5. 지금 꼴과 무엇이 다른가

| | 지금 | 바꾼 뒤 |
|---|---|---|
| 원문 | `raw_response`(258.7MB) | ★ **파일** |
| 넣기 | 값이 같아도 다시 쓴다 | ★ **달라진 것만** |
| 임시표 | 없다 | ★ `stage_{run_id}.db` |
| 적재 로그 | 없다 | ★ `load_log` — 두 번 안 넣는다 |
| 상세 받기 | 전건 (KB 4,320 = 432회차) | ★ **새 것·바뀐 것만** |
| gone | 목록에 없으면 바로 | ★ **상세로 확인한 뒤** |
| 검증 | 가이드가 못 한다 | ★ `outputs/stage/{run_id}.jsonl` |

---

# 6. ★★★★★ 3걸음 — ★ **채웠다** (`V2-01` 실측 08-29)

★ **개발측이 쟀다.** ★ 그 수로 3걸음을 채운다 — ★ 비었던 자리다.

## ★ 잰 것

| 사이트 | ok 원문 | CORE 행 | 차 | 갈래 |
|---|--:|--:|--:|---|
| 엔카 · BMW | 8,925 · 23 | 같다 | **0** | — |
| K카 | 72 | 463 | ★ **+391** | ⓑ |
| KB | 267 | 479 | ★ **+212** | ⓑ |
| 리본카 | 916 | 1,107 | ★ **+191** | ⓑ |
| 헤이딜러 | 113 | 272 | ★ **+159** | ⓑ |
| 보배 | 223 | 239 | ★ **+16** | ⓑ |
| 현대인증 | 210 | 0 | ★ **−210** | ⓐ |
| 볼보 | 152 | 3 | ★ **−149** | ⓐ |
| **합** | **10,901** | **11,511** | **+610** | |

```
★★★ ★ **차가 한 곳이 아니다 — ★ 일곱 곳이고 ★ 양쪽으로 난다**
★ ⓑ 근거 없는 행 ★ **969건** — ★ 「어떤 원문이라도 있는 것」이 ★ **0건**이다
★ ⓒ 3,570(엔카)은 ★ **한 매물에 봉투가 여덟 갈래**인 정상이다 — ★ 결함이 아니다
★ ⓓ 414 는 ★ 마지막 봉투가 `ok` 가 아닌 것 — ★ `detail_status` 가 이미 적고 있다
```

## ★★★★ 뿌리 — ★ **엔카 원문이 다른 사이트 매물에 붙어 있다 (2,406건)**

```
★ 개발측이 파고 찾았다 [실측 08-29] —
  `listing_id` 로 찾으면 원문 7건 · `source_id` 로 찾으면 ★ **0건**
  ★ ★ 그 7건이 ★ **다 엔카 것**인데 ★ 매물은 ★ KB 다
★ 얼마나 — ★ **2,406건** (KB 882 · 현대 731 · 보배 385 · 헤이딜러 140 · 렉서스 80 · BMW 72)
★ 언제 — ★ **08-24~25 뿐**이고 ★ 그 뒤로 ★ **0건**이다
★ 까닭 — ★ S5 가 `core_listing` 을 ★ 사이트로 안 좁히고 긁어
   ★ ★ **다른 사이트 매물번호를 엔카 주소로 불렀다** (`collect/runner.py:383` `_scope()`)
   ★ ★ **08-27 에 이미 고쳐졌다** — ★ 남은 것은 찌꺼기다
★ ★ 그 원문의 `source_id` 가 엔카에 매물로 있는지도 셌다 — ★ **0/2,406**
   ★ ★ 곧 ★ **어느 매물에도 옳게 이어 붙일 수 없는 원문**이다
```

## ★★★ 그래서 3걸음을 이렇게 짠다

```
★★★★★ 열쇠는 ★ **`(site, source_id)`** 다.  ★ **`listing_id` 로 맞대지 마라**
   ★ ★ 개발측 말이 맞다 — ★ `listing_id` 는 ★ 다른 사이트를 가리킨 적이 있다

맞대기 (임시표 ↔ core_listing · ★ `(site, source_id)` 로)
  임시표에 있고 DB 에 없다   → ★ 상세를 받는다
  둘 다 있고 값이 다르다     → ★ 상세를 다시 받는다
     ★ 「다르다」의 잣대는 ★ **값·주행·연식·`sales_status` 넷만** 본다 (물음 #22)
     ★ `last_seen`·`collected_at`·`parsed_at` 은 ★ **안 본다** — 늘 달라진다
  둘 다 있고 값이 같다       → ★ 안 받는다      ← ★ 여기서 대부분이 걸러진다
  DB 에 있고 목록에 없다     → ★ 4-2 장으로 (★ 그 회차에 바로 상세를 누른다)

★★ ★ **양쪽으로 센다** — ★ 「원문 → 코어」 한 방향만 보면 ★ ⓐ(−210·−149)를 못 본다
   ★ ★ 회차마다 ★ **ok 원문 수 · CORE 행 수 · 차**를 ★ 사이트마다 나란히 낸다

★ 못 재는 사이트 — ★ `load_log.file_hash` 로 가른다 (물음 #23)
   ★ ★ 리본카는 ★ 사이트맵 `lastmod` 가 있다 [실측 08-29 · 매물 1,082건 전건]
```

## ★★★ ⓑ 969건 — ★ **채운다.  ★ 못 채우면 이렇게 한다** (마스터 물음 08-29)

```
★ 마스터 — 「★ **채우는데 못 채우면?**」
★ ★ 옳은 물음이다 — ★ 「채운다」만 적으면 ★ **못 채운 것이 그대로 남는다**
```

### ★ 눌러 보면 셋 중 하나다 [실측 08-29 · K카]

| 무엇 | 어떻게 아나 | 그러면 |
|---|---|---|
| ★ **살아 있다** | K카 상세 **71,282B** · `data.photoList` 가 있다 | ★ **채운다** — `detail_status='ok'` |
| ★ **없어졌다** | K카 상세 ★ **3,186B** · `data` 에 `returnUrl`·`message` 만 | ★ **`gone` ＋ `sales_status='detail_gone'` (★ `detail_status` 가 아니다 — 그 칸은 다섯으로 묶여 있다)** (4-2 장 그대로) |
| ★ **못 받는다** | 403 · 503 · 토큰 만료 | ★ **그대로 두고 다음 바퀴** |

```
★ ★ **셋 다 「못 채움」이 아니다** — ★ 둘째는 ★ **답이 나온 것**이다.  ★ 죽이면 된다
★ ★ 남는 것은 ★ **셋째뿐**이고, ★ 그것은 ★ 4-2 장이 이미 정했다 —
   ★ ★ **잇달아 사흘 못 받으면 ★ `gone` ＋ `sales_status='unreachable'`**
```

### ★★★ 969건에 ★ **엔카는 0건이다** (마스터 물음 08-29)

```
★ 마스터 — 「★ **969건 중에서 엔카인 거는 삭제가 아닐까 하고 ★ 엔카가 아닌 비율은**」

★ 세어 보니 —
  K카 391 · KB 212 · 리본카 191 · 헤이딜러 159 · 보배 16  = ★ **969**
  ★ ★ **엔카 0건 · 엔카 아닌 것 969건 (100%)**

★★ ★ 곧 ★ **「엔카인 것은 지운다」로 가를 자리가 없다** — ★ 한 건도 없다
   ★ ★ 까닭이 뒤집혀 있다 — ★ **엔카 원문이 남의 매물에 붙은 것**이지
      ★ ★ **엔카 매물에 원문이 없는 것이 아니다**
   ★ ★ 엔카는 ok 원문 8,925 · CORE 8,925 로 ★ **딱 맞는다** (표 그대로)
```

★ **다만 마스터 뜻이 맞는 자리가 있다** — ★ 969건의 **원문 쪽**은 다 엔카가 남긴 찌꺼기다.
  ★ ★ 그 찌꺼기 원문(2,406건)은 ★ **어느 매물에도 못 붙인다**(`source_id` 가 엔카에 0/2,406).
  ★ ★ 그것은 ★ **지운다.  ★ 다만 원문 파일은 남긴다** (P3) — ★ **이어 붙인 것만 끊는다**

```
★★★★★ 마스터 확정 08-29 — 「★ **그런 거 일괄로 지우고 다시 받자**」
필수  ① ★ 잘못 이어 붙인 것을 끊는다 — `raw_response.listing_id` → NULL (2,406건)
       ★ ★ 원문 줄은 ★ **안 지운다** (P3).  ★ 이음만 끊는다 — ★ 되돌릴 수 있다
필수  ② ★ 969건의 `detail_status` 를 비운다 — 「받은 적 없음」으로
       ★ ★ `status` 는 ★ **`active` 그대로** — ★ 목록에서 빼지 않는다
필수  ③ ★ 다시 받는다 — ★ 969건 × 0.85초 = ★ **약 14분** · 사이트마다 끊어서
금지  ★★ ★ **`core_listing` 행을 지우는 것** — ★ 되돌릴 수 없다
       ★ ★ 969건에 ★ **살아 있는 차가 섞여 있다**.  ★ 08-29 에 그렇게 74대를 죽였다
금지  ★ 원문 줄 자체를 지우는 것 (P3)
```

★ ★ **개발측 표의 합이 안 맞는다** — ★ 여섯 줄을 더하면 **2,290** 인데 합계가 **2,406** 이다.
  ★ ★ **116 이 어디 것인지** 함께 세어 달라 (명령서 0e).

### ★★ 그래서 969건이 사흘 뒤에 이렇게 갈린다

```
채워졌다        → active.  ★ 근거가 생긴다
없어졌다        → gone · detail_gone.  ★ /sold 로 간다
사흘 못 받았다  → gone · unreachable.  ★ 목록에서 빠진다
★★ ★ **어느 쪽이든 ★ 「근거 없는 행」은 사흘 안에 0 이 된다**
```

### ★ 지키는 것

```
필수  ★ 채우는 동안 ★ **목록에서 빼지 않는다** — ★ 살아 있는 차를 못 보시게 되면 안 된다
      ★ ★ 대신 화면에 ★ 「근거를 다시 받는 중」이라 적는다
필수  ★ 한 번에 다 하지 않는다 — ★ 969건 × 0.85초 = **14분**이지만
      ★ ★ 사이트마다 **끊어서** 돌린다 (K카 391 · KB 212 · 리본카 191 · 헤이딜러 159 · 보배 16)
필수  ★ 「없어졌다」와 「못 받았다」를 ★ **꼴로 가른다** — ★ 크기·키로 가른다.  ★ 200 으로 가르지 마라
금지  ★ 못 채웠다고 ★ **그 자리에서 죽이는 것** — ★ 08-29 에 74대를 그렇게 죽였다
검산  ★ `S46-170` — ★ 「근거 없는 행」(원문 0건인 `detail_status='ok'`)이 몇인가
      ★ ★ 지금 **969** · ★ 사흘 뒤 **0** 이어야 한다
```

★ **이 셈이 맞는지는 돌려 봐야 안다** — ★ 969건 중 ★ 몇이 살아 있고 몇이 없어졌는지는
  ★ ★ **아직 모른다**.  ★ 개발측이 첫 100건을 눌러 ★ 갈래 셋의 비율을 내면 ★ 그때 적는다

---

# 8. ★★★★★ 08-29 — ★ **왜 열둘 사이트에 A 가 0 인가** (개발측이 쟀다)

```
★ 잣대 ①(아홉 사이트 A)이 ★ 하루 종일 ★ **0** 이었다.  ★ 개발측이 까닭을 찾았다
★★★ ★ **판정이 틀린 것이 아니다 — ★ 여섯 사이트는 ★ 만점을 받아도 A 에 못 닿는다**
```

| 사이트 | 매물 | 늘 0인 축 | 배점 | ★ 천장 | A 가능 |
|---|--:|--:|--:|--:|:--:|
| 엔카 | 9,324 | 1 | 15 | **98.4%** | ○ |
| K카 | 25 | 11 | 258 | 71.6% | ○ |
| 현대인증 | 174 | 12 | 271 | 70.2% | ○ |
| 헤이딜러 | 92 | 16 | 377 | 58.6% | ★ **못 한다** |
| KB차차차 | 123 | 17 | 423 | 53.5% | ★ **못 한다** |
| 리본카 | 63 | 18 | 431 | 52.6% | ★ **못 한다** |
| 볼보 셀렉트 | 132 | 18 | 511 | 43.8% | ★ **못 한다** |
| 보배드림 | 47 | 18 | 511 | 43.8% | ★ **못 한다** |
| BMW | 9 | 19 | 526 | 42.2% | ★ **못 한다** |
| 렉서스 인증 | 7 | 21 | 636 | ★ **30.1%** | ★ **못 한다** |

```
★ A 는 ★ **65%** 다.  ★ 여섯 곳은 ★ 그 선 아래에 천장이 있다
★ ★ K카·현대인증은 ★ 70% 대라 ★ **한 축만 더 비어도 떨어진다**
★★ ★ 곧 ★ 마스터께서 「K카·헤이딜러에서 먼저 사고 싶다」 하신 것이
   ★ ★ **지금 구조로는 아예 안 된다** — ★ 그 사이트 차는 ★ **A 가 될 수 없다**
```

## ★★ 3단계는 ★ 무엇부터인가 — ★ 배점 큰 순

| 순 | 무엇을 끌어오나 | 한 대당 | 어디서 | 짝이 필요한가 |
|:--:|---|--:|---|:--:|
| 1 | `state.*` 성능기록부 여덟 | ★ **209점** | 짝지어진 엔카 매물 (`plate_hash`) | ★ **필요하다** |
| 2 | `taste.option`·`hud`·`sunroof` | **73점** | 같은 트림 카탈로그 (갈래 ③) | ✘ |
| 3 | `warranty.*` 셋 | **90점** | 사이트 규칙표 | ✘ |
| 4 | `history.*` 셋 | **51점** | 엔카 성능기록부 | ★ 필요하다 |

```
★ 1＋2 만 채워도 ★ 천장이 ★ **+282점 ≈ +31%p** — ★ 리본카 52.6% → **83.7%**
★★ ★ 다만 ★ **1은 짝이 있어야 된다**.  ★ 지금 짝 **161대** · 아홉 사이트 매물 **672건**
   ★ ★ 곧 ★ **511건은 짝이 없어 1을 못 받는다**
★★★ ★ 그러므로 ★ **2(카탈로그)가 먼저다** — ★ 짝이 없어도 ★ **트림만 알면 된다**
★ ★ 이것은 ★ **가이드 안**이다.  ★ 차례는 ★ 마스터께서 정하신다
```

---

# 9. ★★★★★ 08-29 마스터 확정 — ★ **P3 의 대상이 바뀐다**

```
★★★ 마스터 — 「★ **`raw_response` 는 DB 를 크게 만드는 원인이라서 파일로 대체한 거야.
   ★ 파일을 만들고 ★ 파일로 옮긴 데이터 중에서 임시 테이블 만들어 삭제하는 구조인 거잖아.
   ★ 만일 네가 지우기 싫다면 ★ 나는 「파일을 지우지 마라」라고 얘기할 거야.
   ★ 파일이 원본이니까.  ★ 파일 압축이 가능하니까.  ★ 그렇게 정리하자**」
```

## ★ 뒤집힌 것

| | 옛것 (08-29 아침까지) | ★ **새것 (마스터 확정)** |
|---|---|---|
| 원본은 무엇인가 | `raw_response` 행 | ★ **원문 파일** |
| 지우지 않는 것 | `raw_response` (P3) | ★ **원문 파일** — ★ 압축해 남긴다 |
| 지우는 것 | ~~파일 (7일)~~ ★ 물린다 | ★ **`raw_response` 행** — 파일로 옮기고 수를 대조한 뒤 |
| 「7일」이 걸리는 곳 | 원문 파일 | ★ **임시 테이블·중간 산물** |

```
★★★★ ★ **내가 거꾸로 적고 있었다** — ★ 설계는 「원문 = 파일」인데(4장)
   ★ ★ 명령서에는 ★ 「파일은 7일 뒤 지우고 `raw_response` 는 안 지운다」라 적었다
   ★ ★ ★ 그것은 ★ **파일 설계를 통째로 뒤집는 말**이었다
★ ★ **까닭** — ★ `raw_response.body` 가 ★ **258.7MB** 다 (1장).  ★ 그것을 줄이려고 파일로 옮기는 것이다
   ★ ★ ★ 옮겨 놓고 ★ 행을 안 지우면 ★ **아무것도 안 줄어든다**

필수  ★ 원문 파일은 ★ **안 지운다**.  ★ 커지면 ★ **압축한다** (`.gz`)
필수  ★ `raw_response` 행은 ★ **파일로 옮기고 ★ 수를 대조한 뒤 지운다** (P3 무손실)
필수  ★ 「7일」은 ★ **임시 테이블·중간 산물**에만 건다
금지  ★ **원문 파일을 지우는 것**
금지  ★ 옮기기 전에 ★ `raw_response` 를 지우는 것
검산  ★ `S46-185` — ★ 원문 파일을 ★ **안 지운다**가 지켜지는가
```
