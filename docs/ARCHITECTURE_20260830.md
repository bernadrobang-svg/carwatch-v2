# 아키텍처 2판 — 파일 수집 · 적재 · 판정

```
version  SPEC-2026.08.29-r943
follows  docs/chapters/10-collect/00-intro.md · docs/chapters/11-store/a-key.md
sources  마스터 확정 08-29 · 개발측 물음 58개(`outputs/QUESTIONS_ARCH_20260829.md`)
         가이드 답(`outputs/ANSWERS_ARCH_20260829.md`) · 실측 08-29
checks   S46-151 · S46-166 · V2-01(★ 3걸음이 기다린다)
```

★ **1판(`ARCHITECTURE_20260829.md`)을 대신한다.** ★ 1판은 남긴다 — 무엇이 바뀌었는지 보려고.
★★ ★ **3걸음만 비어 있다** — ★ `V2-01` 을 받아야 채운다 (6장).

---

# 1. 왜 바꾸나 — ★ 오늘 잰 것이 까닭이다

| 잰 것 | 수 | 무엇을 말하나 |
|---|--:|---|
| A 등급이 엔카에만 | 엔카 **166** · 아홉 곳 **0** | ★ 견줄 수 없다 |
| C40 1위 근거 있는 축 | **635 / 910** | ★ 상세를 못 받아 275점이 빈다 |
| 같은 차 짝 | **0건** | ★ 「엔카에서 싸게」를 못 한다 |
| `raw_response` | 194,583행 · `body` **258.7MB** | ★ DB 가 커진다 |
| `outputs/check-tmp` | **12GB** | ★ 검사가 흘린 것 (08-29 에 지웠다) |
| 살아 있는데 죽인 매물 | ★ **74대** (K카 12 · 다섯 곳 61 · 렉서스 1) | ★ 목록에 없다고 죽이면 안 된다 |

```
★★ ★ **마지막 줄이 이 설계의 까닭이다** — ★ 「목록에 없다 = 팔렸다」가 ★ **틀렸다**
   ★ ★ 3걸음이 ★ 그것을 ★ **상세로 확인하고 죽이는 자리**다
```

---

# 2. 다섯 걸음

```
 1걸음 · 목록 받기      사이트 → 파일.  ★ DB 를 안 연다
        raw/{site}/list/{YYYY-MM-DD}/page-{묶음키}-{NNNN}.json
        ★ 엔카도 여기 들어온다 — 마스터 회선이 받아 준 것도 파일로
        ★ origin 을 파일 안에 적는다 (collector · browser · import)
                        ↓
 2걸음 · 목록 → 임시표  파일 → stage_{run_id}.db   ★ 통신 없다
        ★ 함께 outputs/stage/{run_id}.jsonl 을 뜬다 — ★ 가이드가 그것으로 검증한다
                        ↓
 3걸음 · ★ 무엇을 더 받을지 정한다        ← ★★ **비어 있다.  V2-01 을 기다린다**
                        ↓
 4걸음 · 상세 받기      「받을 것」만 → 파일
        raw/{site}/detail/{YYYY-MM-DD}/{앞2}/{source_id}.json
                        ↓
 5걸음 · 상세 → 임시표 → 맞대기 → 판정.  ★ 바뀐 것만 재판정
```

## ★ 파일 꼴 (물음 #6~#16 의 답)

```
한 파일  {"site","endpoint","source_id","url","http_code","status","fetched_at","body"}
누름     store/raw.pack_body(`CWZ1`) 를 그대로 쓴다 — ★ 새로 만들지 않는다
반쪽     `.part` 로 쓰고 os.replace.  ★ 2걸음이 `.part` 를 안 읽는다
다시 받음 덮어쓰지 않는다 — `{source_id}__{HHMMSS}.json` 으로 붙인다 (P3)
실패     status='error' 로 ★ 남긴다.  ★ 「없음으로 저장하지 않는다」는 core_listing 규칙이다
자리     config/deploy.json 의 work_dir 아래 raw/ — ★ 코드에 안 박는다
```

## ★ 임시표 (물음 #17~#21·#55 의 답)

```
꼴    stage_{run_id}.db  ★ 따로 둔 파일이다 — ★ 본 DB 와 잠금을 안 다툰다
칸    목록·상세가 주는 칸만.  ★ core_listing 을 늘리지 않는다
PII   ★ 안 들어간다 — split_pii 를 2걸음에서 먼저 부르고 plate_hash 만 담는다
자식  stage_record · stage_inspection 도 둔다 — ★ 「임시표만 버리면 된다」가 깨지지 않게
클렌징 ★ 회차가 끝나면 파일째 지운다.  ★ 다음 회차 시작 때 남은 것이 있으면 먼저 지운다
      ★ ★ 「살아 있나」는 시각이 아니라 ★ collect_run 표로 본다
```

## ★ 적재 로그 (물음 #36~#39 의 답)

```
표    load_log — 한 파일 = 한 줄
칸    file_path(열쇠) · site · endpoint · source_id
      file_bytes · file_hash(sha256 · ★ 압축 전 원문) · parse_version
      loaded_at(★ 적재한 날) · run_id
      rows_in · rows_new · rows_changed · rows_same · status
필수  넣기 전에 file_path 로 대본다 — ★ 이미 있으면 안 넣는다
필수  파일이 다시 받아졌으면 file_hash 가 다르다 — ★ 그때만 다시 넣는다
필수  파서가 바뀌면 parse_version 이 다르다 — ★ 그때도 다시 넣는다
금지  ★ 지우는 것.  ★ 한 줄이 작다 (24,717줄이 몇 MB 다)
★ core_listing.first_seen(매물 기준)과 ★ 섞지 않는다 — load_log 는 파일 기준이다
```

---

# 3. 상태 — ★ 사이트가 말한 대로

| 칸 | 무엇 | 누가 정하나 |
|---|---|---|
| `sales_status` | ★ **사이트가 준 값 그대로** (`resvYn=Y` · `CONTRACT` · `listed` …) | 사이트 |
| `status` | 우리 판정 (`new·active·gone·relisted·out_of_scope`) | 우리 |

```
★ 우리 말로 옮기는 것은 ★ dict_enum 이 한다.  ★ 화면은 mapped 를 본다
★ 실측 08-29 — CONTRACT 830 · RESERVED 98 · K카 resvYn=Y 118
★★ ★ 팔린 차·계약 중은 ★ /listings 조회에서 빠지고 ★ /sold 와 통계에는 나온다
   ★ ★ core_listing 에는 ★ 남는다 (P3)
```

---

# 4. ★★★ 목록에 없다고 죽이지 않는다 (08-29 에 74대를 죽였다)

```
★ 목록에서 사라진 것은 ★ **gone 후보**다.  ★ 바로 gone 이 아니다
★ ★ **상세를 눌러 확인한 뒤** 죽인다.  ★ 확인이 안 되면 ★ 안 죽인다

「살아 있음」의 근거 — ★ 사이트마다 다르다 [실측 08-29]
  K카      data.rvo.carCd 가 있다 (없는 매물도 200·3,256B 를 준다)
  렉서스    car_detail 이 있다 (200·12,585B) ↔ 없으면 200·71B
  헤이딜러  403 은 ★ 오류가 아니라 「판매중 아님」이다
  그 밖    그 사이트 parse_detail 이 값을 뽑는다
★★ ★ **낱말로 가르지 마라** — 보배 「판매완료 573」은 ★ 딜러 실적이다
★ 가드 「끝까지 받았나」는 ★ 「이 창구를 끝까지」이지 ★ 「이 창구가 전량인가」가 아니다
```

---

# 5. 지금 꼴과 무엇이 다른가

| | 지금 | 바꾼 뒤 |
|---|---|---|
| 원문 | `raw_response`(258.7MB) | ★ **파일** |
| 넣기 | 값이 같아도 다시 쓴다 | ★ **달라진 것만** |
| 임시표 | 없다 | ★ `stage_{run_id}.db` |
| 적재 로그 | 없다 | ★ `load_log` — 두 번 안 넣는다 |
| 상세 받기 | 전건 (KB 4,320 = 432회차) | ★ **새 것·바뀐 것만** |
| gone | 목록에 없으면 바로 | ★ **상세로 확인한 뒤** |
| 검증 | 가이드가 못 한다 | ★ `outputs/stage/{run_id}.jsonl` |

---

# 6. ★★★★★ 3걸음 — ★ **비어 있다.  `V2-01` 을 기다린다**

```
★ 3걸음이 할 일 — ★ 임시표 ↔ core_listing 을 대보고 ★ 「무엇을 더 받을지」 정한다
   임시표에 있고 DB 에 없다   → ★ 상세를 받는다
   둘 다 있고 값이 다르다     → ★ 상세를 다시 받는다
   둘 다 있고 값이 같다       → ★ 안 받는다      ← ★ 여기서 대부분이 걸러진다
   DB 에 있고 목록에 없다     → ★ 4장으로 (죽이지 않는다)

★★★ ★ **채우려면 이 수가 있어야 한다** —
   `V2-01`  ok 원문 수 == CORE 행 수
   ★ ★ 지금 ★ **안 맞는데 까닭을 모른다**.  ★ 모르는 채 쓰면 ★ 안 맞는 것을 굳힌다

★ 받을 것 (명령서 0d) —
  ① 사이트마다 ok 원문 몇 건 · CORE 행 몇 건
  ② 차가 나는 곳이 어디인가
  ③ 갈래 — ⓐ 원문 있는데 안 넣음 ⓑ ★ 원문 없이 CORE 만 ⓒ 한 원문이 여러 행 ⓓ 파서 None

★ 수를 받으면 채울 것 —
  ★ 「값이 다르다」의 잣대 (물음 #22 — 값·주행·연식·sales_status 넷만 본다)
  ★ 못 재는 사이트를 어떻게 가르나 (#23 — load_log.file_hash · 리본카는 lastmod)
  ★ ⓑ 가 있으면 ★ **그 행을 어떻게 할지** — ★ 근거 없는 행이라 마스터께 올린다
```

---

# 7. 차례

```
① ★ 개발측이 V2-01 을 잰다 (명령서 0d)
② ★ 가이드가 3걸음을 채우고 ★ 2판을 닫는다
③ ★ 사이트 하나씩 아홉 — 렉서스(74건)부터.  ★ 한꺼번에 안 간다
④ ★ 원문을 파일로 옮긴다.  ★★ raw_response 는 ★ **지우지 않는다** (감독 지시 08-29)
   ★ ★ 「안 준다」가 08-29 하루에 네 번 뒤집혔다.  ★ 안정된 뒤에 지운다
```

★ **끝났는지 재는 잣대** — ① 아홉 사이트에 A 등급 (지금 **0**) ② 같은 차 짝 (지금 **0**)
③ 전기차 A 이상 (지금 **13**). ★ **회차마다 이 세 수를 낸다.**
