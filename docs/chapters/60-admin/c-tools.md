## STEP 133 [규격] — 조회 전용 쿼리

```
version  SPEC-2026.08.29-r989
follows  `docs/chapters/30-score/f-table.md`
sources  실측 08-22
checks   S46-38 · S46-39
```


```
목적    관리자가 DB 를 직접 조회한다
원천    사용자 입력 SQL
입력    SQL 문자열
출력    결과 표 + 복사 버튼 · CSV·JSON 내보내기
값규칙  SELECT 만 허용한다.  그 외는 거부하고 사유를 낸다
근거    변경이 가능하면 RAW 무손실(P3)이 깨진다
       원문을 지우면 재파싱으로 복구가 안 된다
금지    문자열 필터로 판정하는 것.  주석·부분 문자열로 우회된다
검산    거부 목록의 쿼리가 전건 거부되는가
```

### 판정 방식 — 컴파일해서 본다

```
방식   sqlite3 가 SQL 을 컴파일하고, 바이트코드에 쓰기 연산이 있으면 거부
근거   문자열 필터는 주석 · 부분 문자열 · 다중 문장으로 우회된다
      파서가 아니라 실행 엔진이 판정하므로 우회할 여지가 없다
```

```
허용   SELECT · WITH · 서브쿼리 · JOIN · 집계
거부   INSERT · UPDATE · DELETE · DROP · ALTER · CREATE · REPLACE
      PRAGMA · ATTACH · 다중 문장
```

**거부 표본을 시험에 고정한다.**

```
SELECT 1; DELETE FROM …            다중 문장
-- 주석 뒤 DELETE FROM …            줄 주석 우회
/* c */ UPDATE account SET role=…  블록 주석 우회
```

```
필수   거부된 것도 QueryLog 에 남긴다.  무엇을 시도했는지가 기록이다
```

```
★ 「업데이트가 필요하면 개발 요청으로」 — 마스터 확정
  쿼리로 데이터를 고치지 않는다.  고칠 일이 있으면 그 자체가 결함이다
  거부 화면에 개발 요청 버튼을 둔다 (STEP 137)
```

### 제한

```
행 수      config.admin.query_row_limit
실행 시간   config.admin.query_timeout_sec
raw_* body  길이만 표시.  전문은 별도 버튼 (한 건씩)
감사       QueryLog 에 전건 기록.  거부된 것도 남긴다
```

```
필수   결과를 그대로 복사할 수 있게 한다 (탭 구분 · CSV · JSON)
★ 복사한 수치를 문서에 넣지 않는다 (0장 STEP 5.5)
  분석 근거로 쓰되, 지시서 본문에는 규칙만 남긴다
```

---

### ★ 개인정보 테이블은 조회도 막는다 — 08-14

```
금지   core_pii · core_dealer_pii 를 쿼리로 여는 것
근거   쓰기만 막고 있어 SELECT * FROM core_pii 가 통과했다 (실측 08-14)
      개인정보는 get_pii() 한 경로로만 본다.  그래야 누가 언제 봤는지 남는다
필수   EXPLAIN 결과의 OpenRead 대상 테이블을 본다
금지   SQL 문자열에서 테이블명을 찾는 것 — 별칭·주석으로 우회된다
필수   거부해도 query_log 에 남긴다
검산   V10-18  core_pii · core_dealer_pii 조회가 거부되는가
```

---


## STEP 134 [규격] — API 조회 · 저장

```
목적    지정한 URL 의 응답을 받아 저장하고, 내보낸다
원천    외부 API 응답
입력    URL · 헤더 (선택)
출력    ApiSnapshot + JSON 파일
값규칙  응답을 가공하지 않는다.  원문 그대로 저장한다
근거    STEP 25a 실측 요청을 웹에서 대신한다
       마스터가 브라우저로 확인해 PDF 를 만드는 단계를 줄인다
금지    저장한 응답을 raw_response 에 섞는 것.  별도 테이블이다
검산    저장한 JSON 을 다시 읽어 원문과 같은가
```

### 저장

```sql
CREATE TABLE admin_api_snapshot (
  snapshot_id   INTEGER PRIMARY KEY,
  account_id    TEXT NOT NULL,
  url           TEXT NOT NULL,
  http_code     INTEGER,
  content_type  TEXT,
  body          TEXT,
  note          TEXT,
  fetched_at    TEXT NOT NULL
);
```

```
용도   새 엔드포인트 탐색 · 응답 구조 변경 확인 · 다른 사이트 조사
내보내기  JSON 파일.  세션에 첨부해 분석 의뢰
경로 추출  저장된 응답의 경로 전수를 화면에 낸다 (2장 평탄화 규칙)
          → 매핑표 작성이 바로 시작된다
```

```
★ 이 기능이 STEP 25a 를 대체하지 않는다
  전량 실패 시 원인 판별은 여전히 STEP 25a 절차다
  이것은 「탐색」 도구다
```

---

## STEP 135 [규격] — 관리 도구

```
목적    보정 · 검증 · 표본 추출을 웹에서 실행한다
원천    기존 검증·보정 함수
입력    도구별 파라미터
출력    결과 표 + 내보내기
값규칙  도구는 읽기 또는 config 변경만 한다.  데이터를 고치지 않는다
근거    데이터 수정은 재파싱·재판정으로 한다 (STEP 50a)
금지    도구가 core_* 를 직접 UPDATE 하는 것
검산    도구 실행 후 V1~V9 가 여전히 통과하는가
```

| 도구 | 하는 일 | 대응 STEP |
|---|---|---|
| 검증 실행 | V1~V9 를 지금 돌린다 | 6장 |
| 계수 재산출 | 차종별 보정계수 | STEP 64 |
| 감가 곡선 산출 | 실매물 분포에서 | STEP 70 |
| 임계 제안 | 분포 분위수 → `warnings.json` 후보 | STEP 82d |
| 표본 추출 | 조건에 맞는 원문을 fixtures 형식으로 | STEP 6a |
| 경로 전수 | 엔드포인트별 경로 · 값 표본 | 2장 |
| 매핑 대조 | 원문 값 ↔ CORE 값 일치율 | V4-01 |
| 사전 재생성 | RAW → `dict_*` | STEP 42 |
| 등급 분포 | 현재 배점의 등급 분포 | V5-05 |

```
★ 도구 결과는 제안이다.  적용은 별도 버튼이다
  「계수를 산출했다」와 「계수를 바꿨다」는 다르다
  적용하면 ConfigChange 가 생기고 재계산이 큐에 들어간다
```

---

## STEP 136 [규격] — 문서 뷰어

```
목적    지시서 · 가이드를 웹에서 읽는다
원천    지시서 md · 부록 · 개정 이력
입력    없음
출력    화면
값규칙  읽기 전용.  웹에서 지시서를 고치지 않는다
근거    지시서는 세션에서 갱신된다.  두 경로로 고치면 갈라진다
금지    뷰어에서 편집 기능을 넣는 것
검산    표시된 STEP 번호가 실제 문서와 일치하는가
```

```
기능   STEP 번호로 이동 · 검색 · 목차
      개정 이력 표를 별도 화면으로
      현재 화면에서 관련 STEP 으로 바로 가기 (예: 배점 화면 → STEP 68)
★     각 관리 화면에 「근거 STEP」 링크를 단다
      관리자가 「왜 이렇게 되어 있나」를 그 자리에서 본다
```

---


---

## STEP 136a [규격] — 목록 반입 (`/admin/import`)

```
목적    수집이 막힌 단계의 결과를 밖에서 받아 넣는다
원천    사람이 붙여넣거나 올린 JSON · CSV
입력    붙여넣기 상자 또는 파일 · 사이트 · 차종 · 사유
출력    S4 결과와 같은 형태로 core_listing 에 반영 · 원문은 raw_response 로
값규칙  반입은 「수집」이다.  탐색이 아니다 — raw_response 에 넣는다
근거    실측 08-16.  AWS 서울 IP 에서 엔카 /search/ 가 407 로 막혔다
        /v1/readside/ 4종은 200 이라 상세는 서버가 직접 받는다
        「목록만 밖에서, 나머지는 서버가」가 가능한 구조다
```

```
필수   origin 을 'import' 로 남긴다.  'collector' 와 구분한다
필수   누가 · 언제 · 왜 넣었는지 audit 에 남긴다
필수   원문을 그대로 저장한다.  가공한 것만 넣지 않는다 (P3)
필수   반입한 매물에 그 뒤 단계(S5~S10)를 이어서 돌릴 수 있다
필수   화면에 「이 데이터는 반입분입니다」를 표시한다
금지   반입분을 수집분과 섞어 「우리가 받았다」로 보이게 하는 것
금지   반입 없이 목록이 비었는데 「매물 0건」만 내는 것
       — 왜 비었는지와 반입 화면 링크를 낸다
검산   V11-40  반입분의 origin 이 'import' 인가
       V11-41  반입 뒤 S5~S10 이 이어서 도는가
```

```
★ 받는 형식 네 가지
  ① 원문 JSON      엔카 목록 응답을 그대로.  파서가 처리한다
  ② 매물 ID 목록    한 줄에 하나.  최소 입력이다
  ③ CSV           source_id 등 열이 있는 것.  ②로 취급한다
  ④ facet 원문 JSON  ★ 08-16 추가.  S2 를 대신한다
  ①이 원칙이다.  ②③은 원문이 없으므로 「원문 없음」으로 표시한다
★ ④가 필요한 이유
  color_ext · color_int · fuel · trim 사전은 facet 에서만 나온다
  상세를 아무리 받아도 안 채워진다 (실측 08-16 · V4-25)

★ 붙여넣기 상한
  max_form_bytes 를 넘으면 파일 업로드로 안내한다
  「잘렸습니다」를 조용히 넘기지 않는다 (413)
```

```
★ 왜 별도 화면인가
  /admin/api 는 「탐색」이다 — 응답을 보고 구조를 파악하는 자리다
  STEP 134 가 「저장한 응답을 raw_response 에 섞지 마라」고 못 박았다
  반입은 반대다.  수집의 일부이므로 raw_response 에 들어가야 한다
  둘을 한 화면에 두면 「탐색인지 수집인지」가 흐려진다
```

---

## STEP 136b [규격] — 반입의 네 가지 결정 · 08-16

**개발측이 구현 중 물은 것을 정한다. 규격에 구멍이 있었다.**

### ① `core_listing` 을 어떻게 채우나

```
필수   CSV·JSON 이 준 값을 그대로 넣는다.  추정하지 않는다
필수   classify_source 를 'import' 로 남긴다
      classify_stage 는 'confirmed' 다 — 사람이 정한 것이다
필수   CSV 에 없는 칸은 NULL 로 둔다
      S6 이 상세를 파싱하면 채워진다
근거   지금 NULL 은 「없다」가 아니라 「아직 안 받았다」다 (3장 STEP 32)
금지   반입 시점에 fuel_match 등으로 거르는 것
      상세를 안 받았으니 거를 근거가 없다
```

```
★ CSV 6열은 원문 JSON 도 순수 ID 목록도 아니다
  ②(ID 목록) 으로 보고 「원문 없음」으로 표시한다 — 개발측 해석이 맞다
```

### ② 컬럼 제약

```
필수   반입이 채우지 못하는 칸은 nullable 이어야 한다
      NOT NULL 이면 반입 자체가 막힌다
확인   S4 가 채우는 칸과 S6 이 채우는 칸을 가른다
      S4 칸만 NOT NULL 이 될 수 있다
```

### ③ 사전이 비어 있는 문제

```
순서   반입 → S5(상세) → build_dict → S9(판정)
근거   사전은 원문에서 만든다.  상세를 받기 전엔 만들 수 없다
필수   반입 직후 S9 를 돌리지 않는다.  build_dict 를 사이에 둔다
검산   V4-25 는 S9 앞에서 본다 — 순서를 지키면 통과한다
```

### ④ ★ 반입을 S4 완료로 치는가 — 친다

```
필수   반입이 끝나면 audit_validation 에 S4 완료를 남긴다
      code    'STEP53-S4'
      passed  1
      actual  'import'          ← 여기에 반입임을 남긴다
      expected 'collector' 또는 'import'
근거   S4 는 「목록을 확보했다」는 단계다.  반입이 그것을 대신했다
      남기지 않으면 precheck('S5') 가 「선행 단계 미완료」로 막는다
필수   화면에는 「S4 · 반입(import)」 으로 표시한다
금지   actual 을 'collector' 로 남기는 것 — 「우리가 받았다」가 된다
검산   V11-41  반입 뒤 S5~S10 이 이어서 도는가
       V11-42  S4 완료 행의 actual 이 'import' 인가
```

```
★ 이것이 「반입분을 수집분과 섞지 않는다」와 어긋나지 않는 이유
  단계는 완료로 친다 — 목록은 실제로 확보됐다
  다만 어떻게 확보했는지를 actual 에 남긴다
  화면도 감사 기록도 「반입」이라고 말한다
```



---

## STEP 136c [규격] — 브라우저 수집 (`/admin/collect`) ★ 08-16

**전제가 바뀌었다. CLI 는 없다. 모든 작업은 화면에서 한다.**

```
목적    서버 IP 가 막힌 API 를 사용자 회선으로 부른다
원천    실측 08-16.  AWS 서울 IP 에서 /search/ 407 · 사용자 휴대폰 200
입력    조회 조건 (차종 · 페이지) · 사유
출력    엔카 응답 원문 → raw_response → S4~S10 자동 진행
값규칙  브라우저가 부르고, 서버가 받아 저장한다
근거    서버는 못 부르고 브라우저는 부를 수 있다
       그 차이를 메우는 것이 이 화면의 유일한 존재 이유다
```

### 흐름

```
① 화면에서 「조회」
     브라우저 JS 가 api.encar.com 을 직접 부른다
     ★ 서버가 아니라 사용자 회선이 부른다
② 응답이 화면에 그대로 뜬다
     사람이 눈으로 확인한다.  Count 몇 · 매물 몇 건

★★★★ 개정 829 — ★ **한 번 누르기(`auto=1`)를 더한다** (마스터 확정 37 · `UI_REVIEW` 22-1)
   ★ ★ 마스터는 ★ **휴대폰뿐**이고 ★ 여섯 단계를 매번 누르실 수 없다
   ★ ★ `auto=1` 이면 ★ **26종을 차례로 돌고 ★ 끝나면 「몇 건 들어왔다」만** 낸다
   ★ ★ 개발측이 「★ 22-1 이 136c 를 갈음한다고 읽었다」 한 것 — ★★ **반은 맞다**
      ★ ★ **갈음이 아니라 ★ 길이 둘이 된 것**이다

★★ ★ **둘 다 산다** —
   ★ ★ `auto=1` ★ **없이 들어오면 ★ 옛 흐름 그대로** (조회 → 눈으로 확인 → 저장)
   ★ ★ `auto=1` ★ **이면 ★ 한 번에 끝난다** (마스터용)
필수  ★ ★ **옛 길을 지워서는 안 된다** — ★ 「사람이 보고 저장한다」는 ★ 시험자·개발측이 쓴다
      ★ ★ 개발측이 ★ **「감추는 것이지 지우는 게 아닙니다.  ★ `auto=1` 을 빼면 그대로 다 나옵니다」**
      ★ ★ 라 한 것 — ★★ **옳다.  ★ 그대로 두어라**
필수  ★ ★ `auto=1` 로 들어온 것도 ★ **`raw_response` 에 그대로 남긴다** — ★ 나중에 눈으로 볼 수 있게
③ 「저장」
     받은 원문을 그대로 서버로 POST 한다
④ 서버가 raw_response 에 넣고 S4 부터 이어서 돈다
```

```
필수   ②를 건너뛰지 않는다.  사람이 보고 저장한다
       자동으로 저장하면 무엇이 들어갔는지 모른다
필수   원문을 가공하지 않고 그대로 보낸다 (P3)
필수   origin 을 'browser' 로 남긴다
       'collector'(서버가 받음) · 'import'(파일 반입) 와 구분한다
필수   호출 간격을 브라우저에서도 지킨다 (interval_sec)
필수   응답이 407 · 403 이면 그대로 화면에 낸다.  삼키지 않는다
금지   서버가 이 응답을 다시 검증하려고 엔카를 부르는 것 — 막혀 있다
검산   V11-43  브라우저 수집분의 origin 이 'browser' 인가
       V11-44  ② 확인 없이 저장되지 않는가
```

### CORS — 실측 결과 08-16

```
★ 성립한다.  엔카가 두 엔드포인트 모두 ACAO 를 준다
실측   Count = 148,592  (휴대폰 · KT 회선)
       /search/ 도 /v1/readside/ 도 브라우저에서 200
★ 407 은 CORS 가 아니라 AWS IP 차단이었다
  둘을 구분하지 않았으면 「막혔다」로 끝났을 것이다
```

```
필수   그래도 막혔을 때의 갈래를 남긴다
       엔카가 정책을 바꾸면 하루아침에 안 될 수 있다
필수   막히면 화면에 「브라우저가 직접 못 부릅니다.  붙여넣기로 하십시오」
       를 내고 STEP 136a 로 넘긴다
필수   JS 가 꺼져 있어도 「직접 열기」로 새 탭에서 열 수 있게 한다
```

### ★ 크기 — 08-16 실측

```
실측   목록 100건 응답 = 324,556 바이트 (317KB)
       max_form_bytes 64KB 로는 20건이 한계다
★ 「나눠서 보내라」를 사람 몫으로 두지 않는다
  붙여넣기(136a)는 사람이 하므로 413 안내가 맞다
  브라우저 수집(136c)은 JS 가 보낸다.  JS 가 알아서 나눈다
```

```
필수   화면이 한 번에 부르는 건수를 상한 안에서 정한다
       browser_collect_rows 를 config 에 둔다.  기본 20
필수   여러 쪽이 필요하면 JS 가 이어서 부르고 나눠 보낸다
       ① 조회 → ② 확인 → ③ 저장 을 쪽마다 반복하지 않는다
       한 번 확인하고 저장은 여러 번 간다
필수   진행을 화면에 낸다 — 「3/8쪽 저장했습니다」
필수   중간에 실패하면 어디까지 됐는지 남긴다.  처음부터 다시 하지 않는다
금지   사람에게 「나눠서 보내십시오」라고 하는 것
       ★ 브라우저가 하는 일을 사람에게 떠넘기지 않는다
검산   V11-47  브라우저 수집이 한 번에 max_form_bytes 를 넘기지 않는가
```

```
★ 다른 길 — 상한을 올린다
  max_form_bytes 를 1MB 로 올리면 300건이 한 번에 간다
  다만 그러면 붙여넣기 상자에 1MB 를 넣을 수 있게 된다
  1인 도구에 그만한 폼을 열어 둘 이유가 없다
필수   상한은 그대로 두고 JS 가 나눈다
```

### ★ 범위 — 차종별 · 전 차종 · 08-16

```
목적    사람이 차종을 하나씩 고르지 않게 한다
근거    마스터 지시 08-16.  차종 12종을 일일이 누르는 것은 도구가 할 일이다
```

```
필수   범위를 고른다
       □ 차종 하나        KOLEOS_HEV
       □ 여러 차종        고른 것만
       ● 전 차종          targets.json 의 status='active' 전부
필수   전 차종은 「높음」이다 (STEP 149l)
       예상 쪽수 · 예상 시간 · 예상 건수를 먼저 낸다
       확인 문구를 받는다 — 「all 을 입력하십시오」
필수   차종마다 목록과 facet 을 함께 받는다
       facet 이 없으면 그 차종의 사전이 안 채워진다 (V4-25)
필수   한 차종이 실패해도 나머지를 이어서 한다
       실패한 차종을 화면에 남기고 그것만 다시 할 수 있게 한다
필수   진행을 차종 단위로 낸다
       「KOLEOS_HEV 4/8쪽 · 전체 3/12차종」
필수   중단할 수 있다.  중단하면 어디까지 됐는지 남긴다
금지   전 차종을 확인 없이 시작하는 것
금지   한 차종이 실패했다고 전체를 멈추는 것
검산   V11-48  전 차종 수집에 확인 절차가 있는가
       V11-49  한 차종 실패가 나머지를 멈추지 않는가
```

```
★ 실측 규모 (차종 12 · 매물 200 가정)
  목록   2,400건 = 120쪽 × 20건 · 약 7MB
  시간   간격 0.3초 기준 약 1분 · 1.0초 기준 약 2분
  ★ 브라우저는 목록·facet 만 받는다.  상세(S5)는 서버가 받는다
    상세는 약 7,500 호출로 별개다 — 그건 /admin/run 이 한다
```

```
★ 호출 간격 — 브라우저는 두지 않는다
필수   브라우저 수집의 기본 간격은 0 이다
근거   마스터 실측 08-16.  사용자 회선은 막히지 않는다
       서버의 interval_sec 은 데이터센터 IP 를 위한 것이다
       사용자가 브라우저로 보는 것과 같은 속도면 막을 이유가 없다
필수   browser_interval_sec 을 config 에 둔다.  기본 0
       막히는 일이 생기면 그때 올린다.  코드를 안 고치고 대응한다
필수   막히면 화면에 그대로 낸다 (407 · 429 를 삼키지 않는다)
```

---

## STEP 136d [규격] — CLI 없이 돌아간다 ★ 08-16

```
전제    PC 가 없다.  터미널 작업은 없다.  모든 것은 화면에서 한다
근거    마스터 지시 08-16.  운영 환경이 휴대폰뿐이다
```

### 화면에 있어야 하는 것

| 하던 것 | 어디로 |
|---|---|
| `run.py collect` | `/admin/run` (있음) |
| `run.py migrate` | `/admin/config` 에 「스키마 갱신」 |
| `tools/build_dict.py` | `/admin/registry` 에 「사전 생성」 |
| `tools/sync_registry.py` | `/admin/registry` 에 「등록부 동기화」 |
| `tools/check_all.py` | `/admin/tools` (있음) |
| `tools/run_tests.py` | `/admin/tools` 에 「시험 실행」 |
| 목록 수집 | `/admin/collect` (STEP 136c) |
| 파일 반입 | `/admin/import` (STEP 136a) |

```
필수   위 전부가 화면에서 실행되고 결과를 화면에서 본다
필수   오래 걸리는 것은 진행 표시를 낸다 (STEP 132)
필수   실패하면 무엇이 왜 실패했는지 화면에 낸다.  로그를 보라고 하지 않는다
검산   V11-45  CLI 로만 되는 기능이 없는가
```

### 다만 — 부트스트랩은 예외다

```
최초 관리자 계정   run.py setup
근거   계정이 없으면 화면에 들어갈 수 없다.  닭과 달걀이다
필수   이것 하나만 CLI 로 둔다.  그 밖에는 없다
필수   화면에서 「최초 계정은 서버에서 만듭니다」를 안내한다
```

```
★ 왜 규격을 바꾸는가
  STEP 141 이 「1인 사용 도구다.  외부 공개가 목적이 아니다」라고 썼다
  그때는 PC 에서 로컬로 쓰는 것을 전제했다
  지금은 서버가 유일한 실행 환경이고 접근 수단은 브라우저뿐이다
  전제가 바뀌었으므로 규격도 바뀐다.  실물이 옳다는 근거가 있다 (0장)
```


---

## STEP 136e [규격] — 사전 확정 (`/admin/dict`) ★ 08-16

**`pending` 을 `confirmed` 로 올리는 자리가 없었다. 화면으로 만든다.**

```
목적    새로 관측된 값을 사람이 확인해 사전에 확정한다
근거    4장 STEP 40 「confirmed 는 사람이 검토 후」
       그 「사람이 검토」할 자리가 규격에 없었다 (실측 08-16)
       CLI 가 없으므로 화면이어야 한다 (STEP 136d)
입력    없음
출력    dict_enum.status 변경 · 감사 기록
```

### 화면

```
① 확정 대기 목록
   축 · 값 · 몇 건에서 나왔나 · 출처(facet · list) · 처음 본 시각
   ★ 「list」 출처는 따로 표시한다 — 전체 집합을 본 게 아니다 (개정 266)

② 값마다 고른다
   확정      사전에 넣는다.  판정에 쓴다
   보류      그대로 둔다
   폐기      retired.  다시 나와도 pending 으로 안 올린다

③ 묶어서 확정
   축 하나를 통째로 확정할 수 있다 — 41종을 하나씩 누르지 않는다
   ★ 다만 몇 건을 확정하는지 먼저 낸다

④ 확정하면 무엇이 달라지는지 미리 본다
   「이 값을 확정하면 매물 N건이 판정 가능해집니다」
```

```
필수   사유를 받는다.  누가 · 언제 · 왜 확정했는지 남긴다 (STEP 149k)
필수   미리보기 없이 확정 불가
필수   출처가 'list' 인 것은 그렇게 표시한다
       「facet 없이 목록에서 관측한 것입니다.  전체 집합이 아닙니다」
필수   facet 을 나중에 받으면 대조 결과를 이 화면에 낸다 (V3-38)
       facet 에 없는데 목록에 있던 값을 ★ 로 표시한다
필수   폐기(retired)한 값이 다시 관측되면 화면에 알린다
       「폐기한 값이 다시 나왔습니다 — 사이트가 바뀌었을 수 있습니다」
금지   확정을 자동으로 하는 것.  사람이 본다는 것이 이 단계의 전부다
금지   pending 인 채로 판정에 쓰는 것 (4장 STEP 40)
검산   V10-24  확정에 사유가 남는가
       V10-25  'list' 출처가 화면에 표시되는가
```

```
★ 왜 자동으로 안 하는가
  사전은 「같은 값을 같은 것으로 인정하는 규칙」이다 (4장)
  기계가 정하면 「흰색」과 「화이트」를 다른 것으로 두거나
  「진주색」을 흰색에 넣어 버린다
  그 판단은 사람이 한다.  그래서 pending 이 있는 것이다
```

```
★ 41종을 하나씩 누르는 것은 도구가 할 일이 아니다
  축 단위 묶음 확정을 둔다.  다만 몇 건인지 먼저 보여 준다
```


---

## STEP 136f [규격] — 진행 모니터 (`/admin/status`) ★ 08-16

**실행하는 곳과 지켜보는 곳은 다르다.**

```
목적    오래 도는 작업을 옆에서 지켜본다
근거    마스터 지시 08-16.  /admin/run 은 「시작하는 곳」이다
       거기서 지켜보면 실수로 다시 실행할 수 있다
       10,148 호출이 도는 1시간 동안 볼 화면이 따로 있어야 한다
입력    없음
출력    지금 무엇이 도는가 · 어디까지 왔는가
```

### 무엇을 내는가

```
① 지금 도는 것
   단계 (S5 · S6 · S9 · S10 · S7) · 요청/완료/실패
   경과 시간 · 남은 것 추정 · 마지막으로 처리한 매물

② 방금 끝난 것
   무엇이 · 언제 · 몇 건 · 실패 몇

③ 숫자가 어떻게 변했나
   raw_response · core_listing · result_score
   ★ 「받았다」와 「쓰였다」를 함께 낸다 (개정 268)

④ 실패
   무엇이 왜.  「로그를 보십시오」라고 하지 않는다
```

```
필수   스스로 갱신한다.  간격은 config 에 (status_poll_sec · 기본 5)
필수   갱신 간격과 마지막 갱신 시각을 화면에 낸다
필수   폴링이 실패해도 지금까지의 진행은 남는다
       「연결이 끊겼습니다 — 마지막 확인 HH:MM」
필수   「대기 중」과 「멈춘 것」을 가른다
       마지막 처리가 N분 전이면 「N분째 진척 없음」이라 적는다
필수   ★ 큐만 보지 않는다.  실제로 도는 것을 본다 (08-16 실측)
       실측 — 화면이 「도는 것이 없습니다」인데
              raw_response 가 54초에 152행 늘고 있었다
              큐를 안 거친 실행(진입점 직접)이 recalc_job 에 없어서다
필수   판정 근거를 셋 다 본다
       ① recalc_job 에 running 이 있는가
       ② raw_response 가 최근 N초에 늘었는가
       ③ 마지막 처리 시각이 N초 안인가
       ★ 하나라도 참이면 「도는 중」이다
필수   「도는 것이 없다」와 「마지막 처리 0분 전」이 같은 화면에 있으면 안 된다
       ★ 화면이 스스로 모순되면 사람이 아무것도 못 믿는다
금지   큐에 행이 없다는 이유로 「할 일이 없다」고 단정하는 것
필수   읽기 전용이다.  이 화면에 실행 단추를 두지 않는다
필수   JS 가 꺼져 있어도 새로고침하면 지금 상태가 보인다
       ★ 자동 갱신은 편의이지 유일한 길이 아니다
금지   실행 단추를 이 화면에 두는 것
       ★ 지켜보다 실수로 다시 시작하는 것을 막는다
금지   진행이 없는 동안 빈 화면
검산   V11-51  모니터 화면에 실행 단추가 없는가
       V11-52  마지막 갱신 시각이 표시되는가
       V11-53  「도는 것 없음」과 「최근 처리 있음」이 함께 나오지 않는가
```

```
★ /admin/run 과의 차이
  /admin/run     시작한다.  사유·범위를 고르고 누른다
  /admin/status  지켜본다.  아무것도 바꾸지 않는다
  둘을 한 화면에 두면 「보다가 또 누르는」 일이 생긴다
```

## STEP 137 [규격] — 개발 요청

```
목적    없는 기능 · 막힌 것을 기록해 세션에 전달한다
원천    관리자 입력 + 발생 맥락 자동 수집
입력    title · body · origin · context
출력    DevRequest 1행
값규칙  status 8종 밖의 값을 받지 않는다
근거    「지금 안 되는 것」이 기록되지 않으면 다음 세션이 모른다
금지    요청을 삭제하는 것.  상태 전이로만 관리한다
검산    내보낸 요청이 전건 exported_at 을 갖는가
```

### 상태 8종

```
draft        작성 중
requested    내보내기 완료.  세션에 전달됨
in_progress  세션에서 검토 중
applied      지시서 반영 + 구현 완료
not_applied  검토 후 반영하지 않기로.  사유 필수
misapplied   반영됐으나 의도와 다름.  재요청 대상
reopened     applied 였으나 다시 문제.  재개
```

```
전이   draft → requested → in_progress → applied · not_applied
      applied → misapplied → reopened → in_progress
      not_applied → reopened  (마스터가 다시 요청)
필수   applied · not_applied · misapplied 는 사유(direction)를 받는다
필수   applied 는 step_ref 를 받는다.  어느 STEP 에 반영됐는가
```

### 요청이 생기는 자리

```
성분 추가 화면    「함수 없음」인 필드 (STEP 129)
쿼리 화면        UPDATE 거부 시 (STEP 133)
등록부           blocked 항목의 해소 요청 (STEP 87)
화면 어디서나     「이 기능이 없다」 버튼
```

```
자동 수집 맥락   발생 화면 · 시도한 값 · 관련 STEP · 그때의 VersionStamp
근거           「무엇을 하려다 막혔는가」가 없으면 세션이 판단할 수 없다
```

### 내보내기

```
형식   md.  개정 이력과 같은 표 형태
내용   status 별로 묶어서.  requested 와 reopened 를 먼저
파일   dev_requests_{날짜}.md
```

```
★ 세션이 이 파일을 읽고 지시서를 고친다
  고친 뒤 step_ref 를 채워 돌려준다.  관리자가 applied 로 표시한다
반영 확인   지시서 개정 이력에 그 STEP 이 있는가로 대조한다
```

---

## STEP 138 [규격] — 관리자 화면

```
목적    관리 기능을 화면으로 배치한다
원천    STEP 126~137
입력    Account · 화면 파라미터
출력    화면
값규칙  변경 화면은 미리보기 → 사유 입력 → 저장 순서를 지킨다
근거    저장하면 버전이 올라가고 재계산이 돈다.  되돌리기 비용이 있다
금지    미리보기 없이 저장 버튼을 여는 것
검산    각 화면이 근거 STEP 링크를 갖는가
```

### 메뉴 3분류 — 잠금 단위와 일치시킨다

```
운영   실행 · 큐 · 감사        실행 중에는 조정 메뉴가 잠긴다 (STEP 132)
조정   배점 · 차종 · 등록부 · config   변경이 재계산을 유발한다
탐색   쿼리 · API · 도구 · 문서 · 요청  읽기 전용.  언제든 열려 있다
```

```
★ 잠금 단위가 메뉴 단위와 같아야 한다
  실행 중에 배점을 바꾸면 그 실행의 결과가 어느 버전인지 모르게 된다
  탐색은 잠그지 않는다.  읽기만 하므로 안전하다
```

| 분류 | 화면 | 내용 | STEP |
|---|---|---|---|
| — | `/admin` | 현황 · 큐 상태 · 미분류 · 대기 요청 | — |
| **운영** | `/admin/run` | 실행 지시 · 큐 | 132 |
| **운영** | `/admin/audit` | 감사 조회 — 설정 변경 · 쿼리 · 작업 | 138a |
| **조정** | `/admin/scoring` | 배점 조정 + 미리보기 | 128 · 129 |
| **조정** | `/admin/targets` | 차종 추가 · 수정 | 130 |
| **조정** | `/admin/registry` | 등록부 분류 | 131 |
| **조정** | `/admin/config` | `config` 전체 편집 + 이력 | 127 |
| ★ **계정** | ★ `/admin/users` | ★ 계정을 만들고 · 권한을 주고 · 잠근다 | ★ **136z** |
| **탐색** | `/admin/query` | 조회 쿼리 | 133 |
| **탐색** | `/admin/api` | API 조회 · 저장 | 134 |
| **운영** | `/admin/import` | 목록 반입 — JSON · ID 목록 · CSV | 136a · 136b |
| **운영** | `/admin/collect` | 브라우저 수집 — 사용자 회선으로 부른다 | 136c |
| **조정** | `/admin/dict` | 사전 확정 — pending → confirmed | 136e |
| **운영** | `/admin/status` | 진행 모니터 — 읽기 전용 | 136f |
| **탐색** | `/admin/tools` | 관리 도구 | 135 |
| **탐색** | `/admin/docs` | 문서 뷰어 | 136 |
| **탐색** | `/admin/requests` | 개발 요청 | 137 |

```
공통   모든 관리 화면에 「근거 STEP」 링크
      변경 화면에는 미리보기 → 저장 순서
      저장 시 사유 입력
```

---

## STEP 138a [규격] — 감사 조회 · 2차


```
목적    쌓인 이력을 보는 자리를 만든다
원천    config_change · query_log · recalc_job · audit_validation
입력    기간 · 계정 · 종류 필터
출력    화면 + 내보내기
값규칙  읽기 전용.  이력을 고치지 않는다
근거    쌓기만 하고 보는 자리가 없으면 이력이 없는 것과 같다
우선순위 2차.  착수를 막지 않는다.  1차는 이력을 쌓기만 해도 된다
       화면이 없어도 쿼리(STEP 133)로 볼 수 있다
금지    이력 삭제 버튼
검산    되돌리기 버튼이 config_change 에서만 나오는가
```

| 탭 | 내용 | 행동 |
|---|---|---|
| 설정 변경 | `config_change` — 누가·언제·무엇을·왜 | **되돌리기** (STEP 127) |
| 쿼리 | `query_log` — 거부된 것 포함 | 재실행 |
| 작업 | `recalc_job` — 큐·진행·실패 | 재시도 · 이어받기 |
| 검증 | `audit_validation` — V1~V10 이력 | 해당 실행 리포트로 |

```
★ 「왜 이렇게 됐나」의 출발점이 이 화면이다
  점수가 이상하면 → 설정 변경 이력 → 그 변경의 재계산 → 그때의 검증 결과
```

## STEP 139 [검증] — 13장 검증

| 코드 | 검사 | 등급 |
|---|---|---|
| V10-01 | `admin` 전용 함수를 `user` 로 호출 시 `PolicyError` | fatal |
| V10-02 | 화면 숨김만으로 권한을 대신하지 않음 (서버 검증 존재) | fatal |
| V10-03 | `run_query` 가 SELECT 외를 전건 거부 | fatal |
| V10-04 | `run_query` 판정이 AST 기반 (정규식 아님) | fatal |
| V10-05 | `config` 변경이 `ConfigChange` 없이 일어나지 않음 | fatal |
| V10-06 | 배점 저장 시 Σ components == total_points | fatal |
| V10-07 | 성분 추가가 선택 가능 목록 밖에서 일어나지 않음 | fatal |
| V10-08 | 관리 도구가 `core_*` 를 UPDATE 하지 않음 | fatal |
| V10-09 | `DevRequest` 가 삭제되지 않음 (상태 전이만) | fatal |
| V10-10 | 문서 뷰어에 편집 경로 없음 | fatal |
| V10-11 | 실행 중 `config` 변경이 잠김 | fatal |
| V10-12 | 배점 조정 후 0점 성분이 생기지 않음 | warn |
| V10-13 | **웹 경로에서 `site_response_shape` 재계산이 큐에 들어가지 않음** | 0 | fatal |
| V10-14 | `components.{axis}.{component}` 경로로 읽고 쓸 수 있음 | 통과 | fatal |
| V10-15 | 저장 전 `Σ(skipped 아닌 points) == total_points` 검사 | 통과 | fatal |
| V10-16 | `must_change_secret=1` 계정이 다른 화면에 접근 못 함 | 0 | fatal |
| V10-17 | `admin` 수가 0 이 되는 변경이 거부됨 | 0 | fatal |
| V10-18 | `core_pii` · `core_dealer_pii` 조회가 거부됨 | 0 | fatal |
| V10-19 | 중지·역할하향·비밀번호 변경 후 옛 세션이 anonymous | 0 | fatal |
| V10-20 | 연속 로그인 실패 상한을 넘으면 거부됨 | 통과 | fatal |
| V10-22 | `queued` 를 소비하는 코드가 있음 | 통과 | fatal |
| V10-23 | 오래된 `queued` 가 화면에 표시됨 | 통과 | warn |
| V10-24 | 사전 확정에 사유가 남음 | 통과 | fatal |
| V10-25 | `'list'` 출처가 화면에 표시됨 | 통과 | fatal |

---

## STEP 140 [목록] — 13장 미확정

| # | 항목 | 상태 |
|:--:|---|---|
| 1 | 인증 방식 | 1차는 단일 계정 · 비밀번호. **최초 계정만 부트스트랩** (STEP 126 · 136d). 그 뒤는 화면에서 만든다 |
| 1b | 비밀번호 해시 방식 | 표준 라이브러리 범위에서. 알고리즘 미정 |
| 2 | 권한 세분화 | 사이트가 커진 뒤. `role` 컬럼이 자리를 잡아 둠 |
| 3 | 스케줄 주기 | 첫 수집 후 소요 시간을 보고 정한다 |
| 4 | `query_row_limit` · `query_timeout_sec` | 첫 사용 후 조정 |
| 5 | 배점 조정 시 이전 `calc_version` 보존 기간 | 무제한이면 용량이 는다 |

---

**13장 종료 (STEP 126–140).**

---



---

## ★★ 나눌 수 없는 원문 — 08-17

**실측 — `facet` 이 190~217KB 인데 상한이 65,536 이라 못 보냈다.**

```
목록   SearchResults 배열 → 쪼갤 수 있다 (개정 263)
facet  하나의 JSON     → 쪼갤 수 없다
★ 개정 263 이 「JS 가 나눠 보낸다」로 했는데 나눌 수 없는 것이 있다
```

### 규격

```
필수   나눌 수 없는 원문은 조각으로 나눠 보내고 서버가 이어붙인다
       ★ 「내용을 나누는 것」이 아니라 「바이트를 나누는 것」이다
       ★ 원문은 그대로 복원된다 (P3)

보내는 쪽 (JS)
  전송키   한 번에 하나 · 그 원문을 가리키는 값
  조각     seq · total · 바이트 조각
  마지막   길이와 해시를 함께 보낸다

받는 쪽 (서버)
  조각을 순서대로 이어붙인다
  전부 오면 길이와 해시를 대조하고 저장한다
  하나라도 빠지면 저장하지 않는다.  「조각 3/8 이 안 왔습니다」

필수   조각 하나는 max_form_bytes 안이다
필수   중간에 끊기면 받은 조각을 버린다.  반쪽을 저장하지 않는다
필수   진행을 화면에 — 「facet 3/4 조각」
금지   상한을 올려 해결하는 것
       ★ 그러면 붙여넣기 상자에도 그만한 폼이 열린다 (개정 263)
검산   V11-98  이어붙인 원문의 길이와 해시가 보낸 것과 같은가
```

---

## ★★★ CSRF 토큰은 한 번 쓰고 버리지 않는다 — 08-17

**실측 — 전 차종 저장에서 첫 차종만 성공하고 나머지 6차종이 403.**

```
G70_20T/list       성공
G80_25T/list       502
나머지 6차종/list   403
```

```
★ CSRF 토큰이 1회용이면 이렇게 된다
  첫 POST 로 소모되고 그다음부터 거부된다
★ 브라우저 수집은 한 화면에서 수십 번 POST 한다
  1회용 토큰으로는 성립하지 않는다
```

### 규격

```
필수   한 화면에서 여러 번 보내는 경로는 토큰이 세션 단위다
       ★ 한 번 쓰고 버리지 않는다
필수   그래도 갱신이 필요하면 응답에 새 토큰을 실어 보낸다
       JS 가 그것으로 갈아 끼운다
필수   403 이 나면 왜인지 화면에 낸다
       「토큰이 만료됐습니다.  새로고침하십시오」
       ★ 지금은 그냥 「저장 403」이라 원인을 모른다
필수   502 도 마찬가지다 — 앞단이 응답을 못 받았음을 밝힌다
금지   여러 번 보내는 화면에 1회용 토큰을 쓰는 것
검산   V11-99  같은 화면에서 연속 POST 가 되는가
       ★ 실제로 5번 연속 보내 본다
```

```
★ 이것이 왜 안 잡혔나
  시험이 한 번만 POST 했다.  연속으로 안 해 봤다
필수   시험은 실제 사용 패턴대로 한다
       전 차종 수집은 16묶음이다.  시험도 16번 보내야 한다
```


---

## STEP 136g [규격] — 저장하면 나머지가 이어서 돈다 ★★★ 08-17

**마스터 지시 — 「수집은 자동으로 하나?」 → 「셋팅해줘」**

```
지금   마스터가 목록을 저장 → 사람이 「이어서 해라」 → 수집
바꿈   마스터가 목록을 저장 → 나머지가 스스로 돈다
```

### 무엇을 이어서 하나

```
목록 저장이 끝나면
  ① 목록 대조      새로 뜬 것 · 사라진 것 · 가격이 바뀐 것
  ② 상세 4종       새로 뜬 것 · 가격이 바뀐 것만
  ③ 새 5종        아직 안 받은 것
  ④ facet → 사전   pending 을 만든다.  확정은 사람이 한다
  ⑤ 파싱 · 판정
  ⑥ 검사 · 기록
```

```
필수   목록 저장이 끝난 시각에 큐에 넣는다 (STEP 132a)
필수   마스터는 화면을 닫아도 된다.  서버가 계속한다
필수   ★ ④ 는 pending 까지만.  확정은 사람이 한다 (STEP 136e)
필수   ⑤ 는 ②③ 이 끝난 뒤에 한다.  중간 상태로 판정하지 않는다
필수   진행을 /admin/status 에 낸다
필수   실패해도 다음 단계로 가지 않는다.  멈추고 사유를 남긴다
       ★ 상세가 반만 왔는데 판정하면 등급이 틀린다
금지   사람이 「이어서 해라」를 말해야 하는 것
검산   V10-26  목록 저장 후 큐에 작업이 들어가는가
       V10-27  중간 실패에서 다음 단계로 넘어가지 않는가
```

---

## ★★★ 무엇을 언제 다시 하는가 — 08-17 정리

**마스터 물음 — 「목록을 신규로 저장하면 자동으로 전체 재수집?**
**아니면 기존 목록에서 4시에 배치로 수집?」**

### 셋을 갈라야 한다

```
① 원문 재수집   엔카를 다시 부른다.  비싸다 (1만 호출 · 1시간)
② 재파싱       원문은 그대로.  파서만 다시 돈다.  싸다 (몇 분)
③ 재판정       원문·파싱 그대로.  배점만 다시.  가장 싸다 (몇 십 초)
★ 「재수집」이라는 말이 셋을 뭉치고 있다.  갈라서 부른다
```

### 무엇이 무엇을 필요로 하나

```
새 매물을 알려면        목록이 있어야 한다.  ★ 목록만이 안다
팔린 매물을 알려면      목록이 있어야 한다.  ★ 목록에서 사라지는 것이 신호다
가격 변동을 알려면      목록 또는 상세.  둘 다 가격을 준다
점검·이력·옵션을 알려면  상세가 있어야 한다
파서를 고쳤으면        ② 만.  다시 안 받는다
배점을 고쳤으면        ③ 만.  다시 안 받는다
```

### 그래서 이렇게 한다

```
목록을 저장하면 (마스터가 누를 때)
  대조     새로 뜬 것 · 사라진 것 · 가격이 바뀐 것
  상세     ★ 새로 뜬 것 · 가격이 바뀐 것만.  전건이 아니다
  새 5종   아직 안 받은 것만
  판정     그 뒤

매일 04:00 (자동)
  K카      목록 + 상세    ★ 전부 자동 (서버에서 200)
  엔카 상세  ★ 오래된 것만 — detail_refresh_days (기본 7)
           목록이 없어도 기존 ID 로 가격 변동을 잡는다
  새 5종    아직 안 받은 것
  재판정    전건
  검사

배점·파서를 고쳤을 때 (개발측이 판단)
  ★ 수집하지 않는다.  ② 또는 ③ 만 돈다
```

```
필수   「전체 재수집」은 사람이 명시할 때만 한다
       ★ 목록 저장이 전건 재수집을 부르지 않는다
       근거   3,470건 × 9종 = 3만 호출이다.  매일 할 일이 아니다
필수   상세를 다시 받는 기준을 config 로 — detail_refresh_days
필수   가격이 바뀐 매물은 기준일과 무관하게 다시 받는다
       ★ 값이 바뀌면 다른 것도 바뀌었을 수 있다
필수   화면에 무엇을 하는지 적는다
       「상세 128건 (새로 뜬 것 96 · 가격 바뀐 것 32)」
금지   「재수집」이라는 말만 쓰고 무엇을 하는지 안 밝히는 것
검산   V10-29  목록 저장이 전건 재수집을 부르지 않는가
       V10-30  재판정이 수집 없이 도는가
```

```
★ 지금 배점이 자주 바뀐다 (개정 292 · 306)
  그때마다 ③ 재판정만 돌리면 된다.  1분이면 끝난다
  ★ 그것을 몰라서 매번 다시 받으면 하루가 간다
```

---

## STEP 136h [규격] — 매일 스스로 돈다 ★★ 08-17

```
필수   하루 한 번 정해진 시각에 돈다
       시각은 config — collect_daily_at (기본 새벽 4시)
필수   서버에서 부를 수 있는 것만 자동으로 한다
       엔카 상세 4종 · 새 5종 · 재판정 · 검사
       K카 목록 · 상세      ★ K카는 서버에서 200 이다 (전부 자동)
필수   ★ 엔카 목록은 자동이 아니다.  407 이라 브라우저가 필요하다
       → 마스터를 기다린다
필수   systemd timer 를 쓴다.  cron 을 두지 않는다
       근거   systemd 가 이미 서비스를 돌린다.  둘을 섞지 않는다
필수   돌기 전에 이미 도는 것이 있으면 건너뛴다
       ★ 겹쳐 돌면 원문이 꼬인다
필수   결과를 outputs 에 남긴다
검산   V10-28  타이머가 겹쳐 돌지 않는가
```

---

## STEP 136i [규격] — 목록이 오래됐으면 말한다 ★ 08-17

```
필수   엔카 목록이 N일 이상 안 들어왔으면 화면에 낸다
       「엔카 목록이 3일째 갱신되지 않았습니다.
        브라우저 수집을 눌러 주십시오 (/admin/collect)」
필수   N 은 config — list_stale_days (기본 1)
필수   전 화면 머리말에 낸다.  「등록부에 미분류가 있습니다」와 같은 자리
필수   ★ 가격 변동은 목록에서 온다.  목록이 멈추면 변동이 멈춘다
       그것을 함께 적는다 — 「그동안의 가격 변동을 알 수 없습니다」
금지   조용히 옛 목록으로 판정하는 것
검산   V11-103  목록이 오래되면 화면에 나오는가
```


---

## ★★ 자동 수집 시각을 13:00 으로 — 08-18

**마스터 지시 — 「수집은 내가 해줘야 한다면 시간이 틀렸다. 오후 1시로 바꾸자」**

```
전   04:00
     ★ 엔카 목록은 407 로 서버가 못 받는다
     ★ 마스터가 /admin/collect 를 눌러야 목록이 들어온다
     ★ 새벽 4시에 알림이 떠도 마스터가 못 본다
후   13:00
     ★ 마스터가 화면을 보는 시간대다
```

```
필수 [마스터]   자동 수집은 13:00
                config — collect_daily_at (기본 "13:00")
필수 [마스터]   ★ 사람 손이 필요한 일은 사람이 깨어 있을 때 돈다
                ★ 이것을 규칙으로 둔다
필수           서버가 혼자 할 수 있는 것은 시간을 안 가린다
                파싱 · 판정 · 검사 · 점검
필수 [마스터]   13:00 에 목록이 오래됐으면 화면 맨 위에 낸다
                「목록이 N일째입니다 — 브라우저 수집을 눌러 주십시오」
필수           마스터가 누르면 그 뒤 단계가 이어서 돈다 (STEP 136g)
검산            V10-31  자동 수집이 13:00 인가
                V10-32  사람 손이 필요한 작업이 낮 시간대인가
```

### ★ 시간표

| 무엇 | 언제 | 누가 |
|---|---|---|
| 가벼운 점검 | 4시간마다 (02·06·10·14·18·22) | 서버 |
| **자동 수집 큐** | **13:00** | **서버 → 마스터에게 알림** |
| 일일 점검 | 23:00 | 서버 |
| 주간 일제 점검 | 금 02:00 | 서버 |

```
★ 13:00 에 큐를 넣고 마스터를 기다린다
★ 마스터가 /admin/collect 를 누르면 그때부터 이어서 돈다
```

---

## STEP 136z [규격] — ★ 사용자 (`/admin/users`) ★ 08-25

```
★★ 08-25 — ★ 살아 있는데 ★ 규격 표에 ★ 없었다.  ★ `test_admin` 이 ★ 세 회차째 막혀 있었다
★ ★ 가이드가 화면을 늘리면서 ★ 규격을 안 늘렸다 (오판 112)
```

| 경로 | 방식 | 권한 | 묶음 | 무엇 |
|---|---|---|---|---|
| ★ `/admin/users` | GET · POST | ★ **admin** | ★ **계정** | ★ 계정을 만들고 · 권한을 주고 · 잠근다 |

```
★ 권한 셋 — `anonymous` · `user` · `admin` (STEP 126)
필수  ★ ★ **관리 화면에 ★ 문을 단다** — ★ 지금 ★ 주소를 직접 쳐야 들어간다
      ★ ★ 시안 `v3_admin_시안.html` 의 ★ 「계정」 묶음이 그 자리다
필수  ★ 비밀번호를 ★ **화면에 내지 않는다** — ★ 바꾸기만 된다
필수  ★ 계정을 만들거나 권한을 바꾸면 ★ **감사에 남긴다**
검산  ★ `V11-158` — ★ 살아 있는 라우트에 ★ 문이 있는가
```
