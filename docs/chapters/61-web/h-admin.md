# 14장-8. 관리 화면

```
version  SPEC-2026.09.02-r1077
follows  `docs/chapters/30-score/f-table.md`
sources  실측 08-22
checks   S46-38 · S46-39
```


**마스터 지적 08-18 — 「제대로된 설계나 가이드 줬니?」**

```
★ 안 줬다.  61-web/ 은 매물 화면(목록·추천·상세·비교)만 정했다
★ 13장 관리 요구 172건에 대한 화면 설계가 없었다
★ 그래서 「화면 없음」이 115건으로 떴다 — 다만 그중 96건은 화면이 필요 없는 것이다
```

---

# 0. 먼저 — 「화면 없음」을 가른다

```
필수   요구를 셋으로 가른다
       화면 필요      「화면에 낸다」 · 「보여준다」 · 「누른다」 · 「알린다」
       해당 없음      금지 규칙 · 코드 규칙 · 저장 규칙 · 계층 규칙
                     ★ 화면 칸에 「해당 없음」이라 적는다
       미정          판단이 필요하면 「?」
검산   V11-123  「화면 없음」과 「해당 없음」이 갈려 있는가
```

```
★ 13장 실측 — 화면 없음 115건 중
  화면이 정말 필요한 것    19건
  화면이 필요 없는 것      96건   (금지·코드 규칙)
★ 96건을 「해당 없음」으로 바꾸면 남는 것은 19건이다
```

---

# 1. 관리 화면 목록 — 19개

| # | 경로 | 무엇을 하는가 | 누가 |
|--:|---|---|---|
| 1 | `/admin` | 첫 화면 · 상태 요약 · 링크 | 관리자 |
| 2 | `/admin/status` | 진행 모니터 (읽기 전용) | 관리자 |
| 3 | `/admin/collect` | 브라우저 수집 | **마스터** |
| 4 | `/admin/import` | 목록 반입 | 관리자 |
| 5 | `/admin/run` | 실행 지시 | 관리자 |
| 6 | `/admin/dict` | 사전 확정 | **마스터** |
| 7 | `/admin/registry` | 등록부 조정 | **마스터** |
| 8 | `/admin/targets` | 차종 조정 | **마스터** |
| 9 | `/admin/scoring` | 배점 조정 | 관리자 |
| 10 | `/admin/config` | 설정 조정 | 관리자 |
| 11 | `/admin/account` | 사용자 | 관리자 |
| 12 | `/admin/audit` | 감사 | 관리자 |
| 13 | `/admin/query` | 쿼리 탐색 | **마스터** |
| 14 | `/admin/api` | API 탐색 | 관리자 |
| 15 | `/admin/tools` | 도구 탐색 | 관리자 |
| 16 | `/admin/docs` | 문서 탐색 | 관리자 |
| 17 | `/admin/devreq` | 개발 요청 | 관리자 |
| 18 | `/reports` | 리포트 | 관리자 |
| 19 | `/notready` | 미판정 사유 | 관리자 |

```
필수   ★ 「마스터」 표시가 있는 다섯이 가장 중요하다
       ★ 마스터가 매일 손대는 화면이다
```

---

# 2. 공통 — 전 관리 화면

```
필수   무엇을 하는 화면인지 한 줄로 맨 위에
       「지금 도는 것을 지켜봅니다.  읽기 전용입니다」
필수   실패하면 무엇이 왜 실패했는지 화면에
       ★ 「로그를 보라」고 하지 않는다
필수   407 · 403 · 429 · 502 를 그대로 낸다.  삼키지 않는다
필수   되돌릴 수 없는 것은 미리보기를 먼저
       ★ 확정 · 저장 · 적용 앞에 「무엇이 바뀌나」를 낸다
필수   결과를 화면에서 본다.  파일을 내려받게 하지 않는다
필수   JS 가 꺼져도 새로고침하면 보인다
금지   실행 단추가 있는데 무엇이 도는지 안 보이는 것
검산   V11-124  관리 화면마다 머리말이 있는가
        V11-125  실패 사유가 화면에 나오는가
```

---

# 3. 화면별 — 마스터가 쓰는 다섯

## 3-1. `/admin/collect` 브라우저 수집

```
① 범위      차종 하나 · 여러 개 · 전 차종
            ★ 전 차종은 「높음」이라 적고 확인을 받는다
② 조회      브라우저가 부른다.  진행을 차종 단위로
            「G80_25T 3/8쪽 · KOLEOS_HEV 대기」
③ 확인      무엇이 들어가는지 보여준다.  건수 · 앞 500자
④ 저장      나눠 보낸다.  「facet 3/4 조각」
결과        「저장 395쪽 (7,661건) · 실패 0」
            ★ 실패가 있으면 어느 차종 · 왜인지
```

```
필수   막히면 갈래를 낸다 — 407(IP) · 403(권한) · CORS · 로그인 필요
필수   막혔을 때 「붙여넣기로 하십시오」로 이어진다
필수   중단해도 어디까지 됐는지 남는다
```

## 3-2. `/admin/dict` 사전 확정

```
목록      축별로 접는다.  「spec.color 12건 대기」
한 항목    값 · 관측 건수 · 출처(facet/목록) · 제안
확정       [확정] [폐기] [나중에] · 사유 입력
미리보기   ★ 확정하면 무엇이 바뀌나 — 「매물 340건의 판정이 바뀝니다」
```

```
필수   미리보기 없이 확정 불가
필수   폐기한 값이 다시 관측되면 알린다
금지   자동 확정
```

## 3-3. `/admin/registry` 등록부 조정

**★ 개정 367 — 사람에게 물을 때는 판단할 재료를 준다**

```
기본 화면   ★ 판정을 막는 것만 (32건)
           나머지는 접는다 — 「지금 안 봐도 되는 것 88건」
한 항목     경로 · ★ 값 분포 「true 180 · false 20」
           ★ 판정에 쓰이는가 「안 쓰임」 · 「warranty.site 에 쓸 수 있음」
           ★ 어디에 있었나 「detail 의 advertisement 아래 · 옆에 price」
           [쓴다] [안 쓴다] [나중에]
           ★ 안 정하면 무엇이 막히나
묶음        같은 뜻으로 보이는 것을 한 번에
           「advertisement.verification 계열 4개」
```

## 3-4. `/admin/targets` 차종 조정

```
목록      차종 · 상태 · 매물 수 · 마지막 수집
추가       ★ 드롭다운으로 — 국산/수입 → 제조사 → 모델그룹 → 모델
           ★ 자유 입력 금지 (개정 276)
           facet 을 확인한 뒤에만 active
수정       fuel_match · trim_include · 연식 범위
           ★ 고치면 몇 건이 바뀌는지 미리보기
```

## 3-5. `/admin/query` 쿼리 탐색

```
★ 마스터 — 「네가 쿼리를 줘서 내가 보려고 만든 창」
입력      붙여넣기 상자를 크게.  여러 줄
실행      결과를 표로.  많으면 앞부분 + 건수
복사      탭 구분 · CSV · JSON
저장      자주 쓰는 쿼리를 이름 붙여 저장
실패      왜인지 — 문법 · 금지 테이블 · 시간 초과
```

---

# 4. 화면별 — 그 밖

## 4-1. `/admin` 첫 화면

```
필수   지금 상태를 한 눈에
       마지막 수집 · 마지막 점검 · fatal 수 · 미해결 수
필수   할 일이 있으면 크게 — 「미분류 32건이 판정을 막고 있습니다」
필수   19개 화면으로 가는 링크를 갈래별로
```

## 4-2. `/admin/status` 진행 모니터

```
필수   스스로 갱신 (기본 5초) · 마지막 갱신 시각
필수   셋을 함께 — 큐 · 지금 도는 것 · 마지막 처리 시각
       ★ 「도는 것이 없다」와 「마지막 처리 0분 전」이 같은 화면에 있으면 안 된다
필수   queued 가 오래 남으면 알린다
필수   ★ 4시간 점검 결과 — 「10:00 · fatal 3 · 「—」 41 · 빈 칸 849」
필수   읽기 전용.  실행 단추를 두지 않는다
```

## 4-3. `/admin/run` 실행 지시

```
필수   무엇을 돌릴지 고른다 — 단계 · 범위
필수   ★ 화면은 판단 재료를 낸다.  「전면 재수집」 단추를 두지 않는다
필수   큐에 넣고 /admin/status 로 보낸다
필수   실행기가 없으면 큐에 넣지 않는다
```

## 4-4. `/admin/import` 목록 반입

```
필수   CSV · JSON 붙여넣기 또는 파일
필수   ★ 화면에 「이 데이터는 반입분입니다」
필수   무엇을 반입했는지 samples 에 남긴다
필수   반입 없이 「매물 0건」만 내지 않는다 — 무엇을 하면 되는지 낸다
```

## 4-5. `/admin/scoring` 배점 조정

```
필수   축별 배점을 표로.  ★ 합이 총점(5장-2a)인지 그 자리에서
필수   ★ 미리보기 — 「이 배점으로 바꾸면 S 12건 → 3건」
필수   적용하면 calc_version 을 올린다.  덮어쓰지 않는다
필수   변경 사유를 받는다
```

## 4-6. `/admin/config` 설정 조정

```
필수   config 파일별로 접는다
필수   키 · 지금 값 · 기본값 · 무엇에 쓰는가
필수   변경 사유를 받는다 · 이력을 낸다
금지   파일을 직접 편집하게 하는 것
```

## 4-7. `/admin/account` 사용자

```
필수   계정 목록 · 마지막 로그인 · 잠금 여부
필수   ★ 관리자가 다른 관리자의 잠금을 푼다
       ★ 마스터에게 PC 가 없다.  CLI 로 못 푼다
필수   비밀번호 길이·문자 제한 없음 (S36)
```

## 4-8. `/admin/audit` 감사

```
필수   누가 · 언제 · 무엇을 · 왜
필수   거부된 것도 낸다
금지   삭제 단추
```

## 4-9. `/reports` 리포트

```
필수   목록에서 누르면 팝업으로 내용 (개정 357)
필수   팝업 안에 「다운로드」 단추
필수   md 렌더 · csv 표 · json 트리 · 큰 파일은 앞부분만
```

## 4-10. `/notready` 미판정

```
필수   왜 판정을 못 했는지 사유별로
필수   ★ 「무엇을 하면 되나」를 순서대로
필수   진행분을 낸다 — 처음부터 다시가 아님을 알 수 있게
```

## 4-11. 탐색 넷 — `/admin/api` · `/tools` · `/docs` · `/devreq`

```
필수   목록 + 누르면 내용 (팝업 또는 펼침)
필수   검색 상자
금지   편집 기능 (문서·도구는 읽기 전용)
필수   개발 요청만 상태 전이 — applied · not_applied · misapplied + 사유
```

---

# 5. 검사

```
V11-123  「화면 없음」과 「해당 없음」이 갈려 있는가
V11-124  관리 화면마다 머리말이 있는가
V11-125  실패 사유가 화면에 나오는가
V11-126  되돌릴 수 없는 것 앞에 미리보기가 있는가
V11-127  19개 화면이 전부 있는가
```

---

```
★ 이 설계가 13장 요구 172건 중 화면이 필요한 것을 덮는다
★ 나머지 96건은 「해당 없음」이다 — 금지·코드·저장 규칙이다
```
