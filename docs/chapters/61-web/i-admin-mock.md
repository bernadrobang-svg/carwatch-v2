# 관리 화면 시안 — 좁은 폭 360 기준

```
version  SPEC-2026.08.29-r954
follows  `docs/chapters/30-score/f-table.md`
sources  실측 08-22
checks   S46-38 · S46-39
```


**★ 마스터 지적 — 「시안도 없이 말로 따우네」**
**★ 이 시안은 `[마스터]` 층이다 (S32). 마스터가 고치면 그것이 정본이다.**

```
그리는 법   글자로 자리와 차례를 보인다
           [단추]  (입력칸)  ▸접힘  ─선  ★강조
           → 누르면 무엇이 되는지
```

---

# 1. `/admin` 첫 화면

```
┌────────────────────────────────┐
│ CarWatch          마스터 [로그아웃] │
├────────────────────────────────┤
│ ★ 지금 할 일 2                    │
│ · 미분류 32건이 판정을 막습니다 →등록부 │
│ · 목록이 1일째 안 들어왔습니다 →수집   │
├────────────────────────────────┤
│ 마지막 수집   08-18 09:12 · 7,661건 │
│ 마지막 점검   10:00 · fatal 3       │
│ 판정         3,528건 · S 12 A 340   │
│ 미해결        결함 54 · 빈 칸 849    │
├────────────────────────────────┤
│ 매물  [목록] [추천] [시세] [관심]     │
│ 수집  [브라우저 수집] [반입] [실행]    │
│ 확정  [사전] [등록부] [차종]          │
│ 설정  [배점] [설정] [사용자]          │
│ 보기  [진행] [감사] [쿼리] [리포트]    │
└────────────────────────────────┘
```

```
★ 「지금 할 일」이 맨 위다.  없으면 「할 일이 없습니다」
★ 숫자 넷이 그다음.  어제와 견준 화살표를 붙인다 (↑↓)
```

---

# 2. `/admin/collect` 브라우저 수집

```
┌────────────────────────────────┐
│ 브라우저 수집                      │
│ 엔카가 서울 IP를 막습니다.          │
│ 마스터 회선으로 받습니다.            │
├────────────────────────────────┤
│ ① 범위                           │
│  (▼ 전 차종                    )  │
│  ★ 전 차종은 8차종·16묶음입니다      │
│  확인란 (all         ) 사유(     )│
│                      [① 조회]    │
├────────────────────────────────┤
│ ② 받은 것                        │
│  G80_25T   200 · 192KB  ✓        │
│  KOLEOS    200 · 188KB  ✓        │
│  …                              │
│  ★ 한 쪽이 커서 20건→1건으로 줄였습니다│
│  {"Count":402,"iNav":…           │
│                      [② 확인]    │
├────────────────────────────────┤
│ ③ 저장                           │
│  나눠서 보냅니다                    │
│                      [③ 저장]    │
├────────────────────────────────┤
│ 진행                             │
│  저장 395쪽 (7,661건)             │
│  묶음 16/16 · 끝났습니다            │
│  실패 0                          │
└────────────────────────────────┘
```

```
★ ①②③ 이 위에서 아래로.  건너뛸 수 없다
★ 막히면 그 자리에 — 「407 · 서울 IP 차단 · 붙여넣기로 하십시오 →반입」
★ 진행은 차종 단위로 늘어난다
```

---

# 3. `/admin/registry` 등록부 조정 ★ 개정 367

```
┌────────────────────────────────┐
│ 등록부 조정                        │
│ 판정을 막는 것만 냅니다.             │
├────────────────────────────────┤
│ ★ 판정을 막는 것 32건              │
│ ▸ 지금 안 봐도 되는 것 88건          │
├────────────────────────────────┤
│ ┌ advertisement.verification 계열 4│
│ │ 값   true 180 · false 20        │
│ │ 쓰임  warranty.site 에 쓸 수 있음  │
│ │ 위치  detail > advertisement      │
│ │      옆에 price · status         │
│ │ 안 정하면 → 판정이 막힙니다         │
│ │ [쓴다] [안 쓴다] [나중에]          │
│ │ 사유 (                        )  │
│ └────────────────────────────┘ │
│ ┌ view.meetgoCheckupAvailable      │
│ │ 값   true 12 · false 188         │
│ │ 쓰임  안 쓰임                     │
│ │ 안 정하면 → 막지 않습니다           │
│ │ [쓴다] [안 쓴다] [나중에]          │
│ └────────────────────────────┘ │
└────────────────────────────────┘
```

```
★ 값 분포가 맨 위다.  그것을 보고 판단한다
★ 같은 계열은 묶어 한 번에
★ 「안 정하면 무엇이 막히나」가 있어야 급한지 안다
```

---

# 4. `/admin/dict` 사전 확정

```
┌────────────────────────────────┐
│ 사전 확정                         │
│ 같은 값을 같은 것으로 묶습니다.       │
├────────────────────────────────┤
│ 대기 47건 · 확정 1,204건           │
├────────────────────────────────┤
│ ▾ spec.color  12건               │
│  ┌──────────────────────────┐ │
│  │ "진주색"      관측 34 · facet │ │
│  │ 제안 새 값으로 등록           │ │
│  │ [확정] [폐기] [나중에]        │ │
│  │ 사유 (                    ) │ │
│  │ ▸ 확정하면 340건의 판정이 바뀝니다│ │
│  └──────────────────────────┘ │
│  … 11건 더                       │
├────────────────────────────────┤
│ ▸ history.usage  8건             │
│ ▸ spec.fuel  3건                 │
└────────────────────────────────┘
```

```
★ 축별로 접는다.  한 번에 하나씩 편다
★ 미리보기(▸)를 펴야 [확정]이 눌린다
★ 폐기한 값이 다시 관측되면 맨 위에 「다시 나타났습니다」
```

---

# 5. `/admin/targets` 차종 조정

**★ 08-18 마스터 정정 — 「내가 볼보의 EX60을 추가하고 싶을 때 쓰는 거야.**
**완전히 새로운 차량을 추가할 때. 그래서 국내에 생산 또는 수입되는 차종이 다 보여야 돼」**

```
★ 지금 보는 11개를 고르는 화면이 아니다
★ 엔카에 있는 전 차종에서 아직 안 보는 것을 찾는 화면이다
★ 볼보 EX60 처럼 우리가 한 번도 안 본 차도 나와야 한다
```

## ① 전 차종 목록 — 엔카가 주는 그대로

```
┌────────────────────────────────┐
│ 차종 조정                         │
│ 국내에 팔리는 차를 전부 냅니다.       │
├────────────────────────────────┤
│ 찾기 (볼보              ) [찾기]  │
│  또는                            │
│  국산·수입 (▼ 수입          )     │
├────────────────────────────────┤
│ ▾ 볼보  14 모델                   │
│  XC60      매물 412 · ★ 안 봄     │
│  XC90      매물 187 · ★ 안 봄     │
│  EX30      매물  34 · ★ 안 봄     │
│  ★ EX60   매물  12 · ★ 안 봄     │
│  S60       매물  98 · ★ 안 봄     │
│  …                              │
├────────────────────────────────┤
│ ▸ 제네시스  9 모델  (3 보는 중)     │
│ ▸ 현대     22 모델  (0 보는 중)    │
│ ▸ 기아     19 모델  (1 보는 중)    │
│ ▸ 테슬라    5 모델  (1 보는 중)    │
│ ▸ 르노      8 모델  (1 보는 중)    │
│ …                               │
└────────────────────────────────┘
```

```
필수 [마스터]   ★ 엔카에 있는 전 차종을 낸다.  우리가 보는 것만 내지 않는다
필수 [원문]     제조사 → 모델그룹 → 모델을 엔카 facet 그대로
필수 [마스터]   모델마다 매물 수를 낸다 — 없으면 「매물 0」
필수 [마스터]   ★ 「보는 중」 · 「안 봄」을 표시한다
필수           이름으로 찾을 수 있게 — 「볼보」 · 「EX60」 · 「전기」
필수 [기술]     이 목록은 하루 한 번 새로 받는다.  매번 부르지 않는다
                ★ config — brand_list_refresh_days (기본 1)
금지 [마스터]   등록된 것만 내는 것
검산            V11-143  엔카 전 차종이 나오는가
                V11-145  「보는 중」과 「안 봄」이 갈리는가
```

## ② 고르면 — 무엇이 있는지 본다

```
┌────────────────────────────────┐
│ 볼보 EX60                        │
│ 매물 12건                         │
├────────────────────────────────┤
│ ▸ 엔카가 주는 것                   │
│  연료   전기 12                   │
│  트림   울트라 7 · 플러스 5         │
│  연식   2025 3 · 2026 9           │
│  가격   6,200 ~ 8,900만           │
│  ★ 12건이라 시세를 못 냅니다        │
│    (표본 5건 미만인 조건이 있습니다)  │
├────────────────────────────────┤
│ 무엇을 볼까요                      │
│  연료  ☑ 전기                     │
│  트림  ☑ 울트라  ☑ 플러스          │
│  연식  (        ) 이후            │
│  가격  (        ) ~ (        ) 만 │
│                    [몇 건인지 보기] │
├────────────────────────────────┤
│ ▸ 이 조건이면 12건입니다            │
│  이름 (EX60             )         │
│  ★ 겹치는 차종이 없습니다           │
│                      [등록]      │
└────────────────────────────────┘
```

```
필수 [마스터]   고르면 그 모델의 facet 을 받아 낸다
필수 [마스터]   ★ 매물이 적으면 미리 말한다 — 「12건이라 시세를 못 냅니다」
                ★ 등록은 되게 한다.  판단은 마스터가 한다
필수           조건을 안 걸면 그 모델 전부다.  비워 두면 된다
필수           이름은 제안하되 고칠 수 있게
검산            V11-144  facet 분포를 보고 고르는가
                V11-146  매물이 적으면 알리는가
```

## ③ 보고 있는 차종 — 맨 아래

```
┌────────────────────────────────┐
│ 보고 있는 차종 11                  │
├────────────────────────────────┤
│ G80_25T      1,253건 · 08-18     │
│  [고치기] [잠시 끄기]             │
├────────────────────────────────┤
│ ⚠ SPORTAGE_LPI  181건 · 08-15    │
│  ★ 3일째 안 들어왔습니다            │
└────────────────────────────────┘
```

```
필수   오래 안 들어온 차종에 ⚠
필수   [잠시 끄기] — 지우지 않는다
```

---

# 6. `/admin/status` 진행 모니터

```
┌────────────────────────────────┐
│ 진행 모니터        읽기 전용         │
│ 5초마다 갱신 · 마지막 10:02:15       │
├────────────────────────────────┤
│ 지금 도는 것                       │
│  S6 파싱  1,204 / 3,528           │
│  ████████░░░░░░  34%             │
│  시작 09:58 · 4분째                │
├────────────────────────────────┤
│ 큐                               │
│  대기 3 · 도는 중 1 · 끝 42         │
│  ⚠ queued 가 12분째 있습니다        │
├────────────────────────────────┤
│ 마지막 처리                        │
│  raw_response  0분 전             │
│  core_listing  0분 전             │
│  result_score  6시간 전  ⚠         │
├────────────────────────────────┤
│ 4시간 점검  10:00                  │
│  fatal 3 (↓3) · 「—」 41 (=)      │
│  빈 칸 849 (↓0)                   │
└────────────────────────────────┘
```

```
★ 셋을 한 화면에 — 도는 것 · 큐 · 마지막 처리
★ 「도는 것이 없다」와 「마지막 처리 0분 전」이 같이 있으면 안 된다
★ 실행 단추가 없다
```

---

# 7. `/admin/query` 쿼리 탐색

```
┌────────────────────────────────┐
│ 쿼리 탐색                         │
│ 읽기만 됩니다. PII 표는 안 열립니다.  │
├────────────────────────────────┤
│ ▸ 저장한 쿼리 6                    │
│   · 축별 미확인 건수                │
│   · 차종별 등급 분포                │
├────────────────────────────────┤
│ ┌────────────────────────────┐ │
│ │ SELECT target_key, COUNT(*)  │ │
│ │ FROM core_listing            │ │
│ │ GROUP BY 1 ORDER BY 2 DESC   │ │
│ │                              │ │
│ └────────────────────────────┘ │
│              [실행]  [저장]       │
├────────────────────────────────┤
│ 결과 11행 (앞 50행)                │
│  target_key      COUNT(*)        │
│  G80_25T           1,253         │
│  KOLEOS_HEV          237         │
│  …                              │
│      [탭 복사] [CSV] [JSON]       │
└────────────────────────────────┘
```

```
★ 입력 상자가 크다.  여러 줄 쿼리가 들어간다
★ 저장한 쿼리를 위에 접어 둔다
★ 실패하면 그 자리에 — 「금지 테이블 core_pii 를 열려 했습니다」
```

---

# 8. `/notready` 미판정

```
┌────────────────────────────────┐
│ 아직 판정하지 못했습니다             │
├────────────────────────────────┤
│ 사유                             │
│ · 등록부 미분류 32건 — V4-11 이 막음  │
│ · 차종이 안 붙은 매물 4,243건        │
│   → 판정 대상이 아닙니다             │
├────────────────────────────────┤
│ 무엇을 하면 되나                    │
│ 1. 등록부에서 32건을 정합니다 →등록부  │
│ 2. targets.json 의 fuel_match 를   │
│    보고 고치거나 그대로 둡니다 →차종   │
├────────────────────────────────┤
│ 진행분                            │
│  범위 안 3,528 · 판정 완료 3,528    │
│  ★ 처음부터 다시가 아닙니다          │
├────────────────────────────────┤
│ ▸ 차종이 안 붙은 매물 4,243건        │
└────────────────────────────────┘
```

```
★ 「무엇을 하면 되나」가 번호로.  누르면 그 화면으로
★ 진행분을 낸다 — 처음부터가 아님을 알게
```

---

# 나머지 11개 화면 — ★ `j-admin-mock2.md` 에 그렸다 (08-18)

```
필수   같은 방식으로 시안을 그린다
       /admin/import · /admin/run · /admin/scoring · /admin/config
       /admin/account · /admin/audit · /admin/api · /admin/tools
       /admin/docs · /admin/devreq · /reports · (그 밖)
필수   ★ 마스터가 이 일곱을 보시고 고치신 뒤에 나머지를 그린다
       ★ 방식이 맞는지 먼저 확인받는다
```

---

```
검산   V11-128  관리 화면마다 시안이 있는가
       V11-129  시안과 실물이 같은가 (V11-59 와 같은 방식)
★ 이 시안은 [마스터] 층이다.  마스터가 고치면 그것이 정본이다
```
