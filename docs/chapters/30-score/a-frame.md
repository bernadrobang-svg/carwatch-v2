## STEP 67 — 축 설계 원칙

```
version  SPEC-2026.09.01-r1033
follows  ★ 정본 — 배점의 원천은 `config/scoring.json`
sources  실측 08-22
checks   S46-38 · S46-39
```


### 축이 되는 조건

```
① 매물마다 값이 다르다        변별력
② 원문에 근거가 있다          추정 아님
③ 구매 판단에 영향을 준다      의미
④ 값의 좋고 나쁨이 명확하다    방향
```

**넷 중 하나라도 빠지면 축으로 만들지 않는다.** `display_only` 로 둔다 (8장 STEP 87).

| 예 | ① | ② | ③ | ④ | 판정 |
|---|:--:|:--:|:--:|:--:|---|
| 가격 | O | O | O | O | 축 |
| 사고 이력 | O | O | O | O | 축 |
| 점검 내부 항목(`inners`) | **X** | O | O | O | **`display_only`** — 대부분 「양호」 |
| 엔카 홈서비스 인증 | O | O | **X** | O | **`unused_by_policy`** — 배송 서비스지 차량 품질이 아니다 |
| 차주 인증 | O | O | **X** | O | `unused_by_policy` |

```
v1 오류   inners 를 축으로 만들려 했다.  전건 「양호」라 변별력이 0이다
         딜러가 팔 매물에 「불량」을 적지 않는다
```

### 변별력 판정은 8차종 통합에서 한다

```
차종 내부 값 종류 1   그 차종의 특성일 수 있다
예   콜레오스 주행거리 전건 만점 — 2024~25년식만 있어서다
금지  차종 내부만 보고 배점을 낮추는 것.  저주행 차종의 강점이 지워진다
```

---

## STEP 68 — 배점 구조

**v1 에서 마스터가 확정한 배점을 승계한다. 단 근거를 이 장에서 재확인한다.**
**★ 지금 총점은 `f-table` 5장-2a 가 정본이다 (개정 454).**

| 축 (Axis) | 배점 | 비중 | 하위 성분 (Component) | 근거 |
|---|---:|---:|---|---|
| `price` | 200 | 36% | — | STEP 70·71 |
| `warranty` | 100 | 18% | `general` 50 · `power` 50 | STEP 72 |
| `spec` | 90 | 16% | `hud` 20 · `sunroof` 20 · `svm` 10 · `scc` 10 · `bsd` 5 · `tinting` 5 | STEP 73~75 |
| `history` | 55 | 10% | `damage` 20 · `insurance` 15 · `rental` 20 | STEP 76~78 |
| `safety` | 40 | 7% | `diagnosis` 20 · `warranty_product` 20 | STEP 79 |
| `color` | 40 | 7% | — | STEP 80 |
| `mileage` | 30 | 5% | — | STEP 81 |
| **합** | ★ **5장-2a** | 100% | | |

### ★ Axis 와 Component 를 구분한다

```
Axis        점수 집계 · 화면 표시 단위.  7개
Component   판정 단위.  분모 제외는 Component 단위로 일어난다
```

```
Component 이름은 축 접두를 붙이지 않는다.  전체 키가 '{axis}.{component}' 다
  O  warranty.general · history.damage · safety.diagnosis · spec.hud
  X  warranty.warranty_general       ← 축 이름이 두 번 나온다
축이 하나뿐인 것은 축 이름이 곧 키다
  price · color · mileage

result_axis 에 저장하는 것   Component 단위 (총 17행)
                          axis 컬럼에 'spec.hud' 처럼 점 표기로 계층을 남긴다
집계                      Component → Axis → 총점
분모 제외                  Component 배점 단위로 뺀다
예                       모델Y hud = -1 → spec 분모가 90 → 70
```

**압류·저당은 `safety` 점수에 넣지 않는다.** E등급 절대조건이다 (STEP 82).

```
safety 점수     safety.diagnosis 20 + safety.warranty_product 20
absolute_fail   seizing · pledge          점수 아님.  후보에서 제외
```

### 배점 비중의 근거

```
가격 36%    구매 결정에서 가장 큰 변수다.  다른 축이 좋아도 예산을 넘으면 못 산다
보증 18%    잔여 보증은 향후 수리비 리스크를 직접 줄인다.  금액으로 환산 가능한 유일한 축
사양 16%    마스터 필수 스펙(HUD·선루프)이 여기 있다.  대체 불가 항목
이력 10%    사고는 E등급 절대조건으로도 걸리므로 배점은 보조 역할
안전 7%     진단·압류.  대부분 정상이라 변별력이 낮다
색상 7%     재판매성에 영향.  값 종류가 적어 변별력 제한
주행 5%     연식과 상관이 높아 독립 기여가 작다
```

### 배점 변경 — 정수 보정

```
축 총점을 바꾸면 성분을 비율로 재배분한다.  전부 정수다
1  각 성분 = round(기존 × 새 총점 ÷ 기존 총점)
2  잔여 = 새 총점 − Σ(반올림 결과)
3  잔여를 배점이 가장 큰 성분에 더한다.  같으면 이름 순 첫 번째
4  Σ components == total_points 를 저장 전에 검산한다
```

```
금지   소수점 배점.  47.5점은 비교를 어렵게 한다
금지   0 이 되는 성분을 만드는 것.  스킵과 다르다 (13장 STEP 128)
검산   check_spec ⑦ 이 Σ components == total_points 를 본다
```

**★ 미확정 — 이 비중은 v1 승계값이다.**
**8차종 수집 완료 후 축별 실제 변별력(값 종류 · 분산)을 측정해 재검토한다** (STEP 86).

```
재검토 기준   축의 표준편차가 배점 대비 지나치게 작으면 배점을 낮춘다
             v1 실측: 주행 30점이 콜레오스에서 전건 만점 → 기여 0
금지         근거 없이 숫자를 바꾸는 것.  측정 결과와 함께 바꾼다
```

---

## STEP 69 — 판정 엔진

```python
def put(v: Verdict, axis: str, value, prio: int, src: str,
        excluded: bool = False) -> None:
    if src in BANNED_SOURCES:
        raise ValidationError(f"banned source: {src}")
    if value is None and not excluded:
        return                                   # 값도 없고 제외도 아니면 기록하지 않는다
    cur = v.prios.get(axis)
    if cur is not None:
        if cur < prio:  return                   # 강한 근거가 이긴다
        if cur == prio and v.values.get(axis) != value:
            v.conflicts.append((axis, prio, value))
            return                               # 첫 값 유지
    v.values[axis], v.prios[axis], v.sources[axis] = value, prio, src
    if excluded: v.excluded.add(axis)
    else:        v.excluded.discard(axis)
```

### ★ `excluded` 는 판정의 결과다 — 채점이 추측하지 않는다

```

### ★ `conflicts` 를 버리지 않는다 — 08-14

```
필수   판정 후 v.conflicts 가 비어 있지 않으면 기록한다
저장   result_axis_conflict (listing_id · calc_version · axis · prio · value · source)
필수   화면 · L1 리포트에 「같은 근거 두 값」으로 낸다
근거   실측 08-14.  구조는 있는데 아무도 읽지 않았다.  조용히 사라진다
      같은 우선순위에서 다른 값이 나온 것은 규칙이 겹쳤다는 뜻이다.
      첫 값을 유지하는 것은 임시 조치이고, 겹친 규칙을 고쳐야 한다
금지   충돌을 무시하고 넘어가는 것 — v1 의 사고가 전부 그렇게 시작했다
검산   V3-35  conflicts 가 있는 매물이 기록되는가
      V3-36  conflicts 건수가 임계를 넘으면 warn (규칙이 겹쳤다는 신호)
```

```
★ 충돌이 나면 판정을 멈추지는 않는다
근거   첫 값 유지가 결정적이라 결과는 재현된다.
      다만 「왜 두 값이 나왔나」를 사람이 봐야 한다.  그래서 기록이다
```
금지   채점 단계에서 value is None 을 보고 분모를 줄이는 것 (STEP 83)
필수   판정기가 「이 차종에 없는 사양이다」를 알고 excluded=True 로 기록한다

put(v,"spec.hud",     -1,  1, "spec_table", excluded=True)   모델Y HUD
put(v,"spec.tinting", None,4, "unknown",    excluded=True)   언급 없음
put(v,"spec.sunroof",  0,  2, "installed")                   미장착.  제외 아님
```

### 우선순위 4단

```
prio 1   제조사 사양 · 사이트 코드값     SPEC_DEFAULT_ON · RANK_* · displacement
prio 2   매물 실측                     options.standard/choice · 점검 결과 · 이력
prio 3   전용 판정기                   classify_hud
prio 4   키워드 · 문자열                contents.text · 트림명
```

```
불변식   호출 순서를 뒤섞어도 결과가 같다 (0장 STEP 7-①)
검증     표본 100건에 셔플 시험 (6장 V3-11)
```

### 금지 근거

```python
BANNED_SOURCES = {
    "catalog_full_list",   # 모델 전체 옵션 목록 — 매물 장착이 아니다
    "facet_count",         # 집계 — 매물 단위 사실이 아니다
    "record_fuel",         # API 별 표기 상이 — 분류에 쓰면 안 된다
    "part_name_string",    # 부위명 문자열 — 표기 흔들림에 무너진다
}
```

**주석이 아니라 코드가 막는다.** v1 은 넷 다 실제로 사고를 냈다.

---



---

## ★★★ 분모는 고정이다 — 08-16

**마스터 지적 — 「못 찾은 것은 니 사정이지. 왜 평가를 왜곡하니」**

```
지금까지
  축을 못 보면 분모에서 뺐다
  → 못 볼수록 비율이 올라간다
  → 못 찾을수록 좋은 등급을 받는다
★ 우리가 못 받은 것을 그 매물의 장점으로 만들었다
```

```
실측 08-16
  330/350 = 94.3%  →  S
  541/910 = 59.5%  →  D
  같은 차가 분모에 따라 등급이 갈린다
```

### 규격

```
필수   분모는 ★ 만점 그대로다 (5장-2a).  예외 없다
필수   못 본 축은 0점이다
       ★ 「우리가 못 받았다」는 그 차의 사정이 아니라 우리 사정이다
         그것으로 평가를 바꾸지 않는다
필수   왜 0점인지는 화면에 낸다 — 사람이 판단할 재료다
       missing               우리가 못 받았다
       no_mention            원문이 말하지 않았다
       expected_unavailable  원래 없는 것이다
필수   「확인 못 한 것 N점」을 함께 낸다
       「만점 중 541점.  나머지는 확인하지 못했습니다」
       ★ 그러면 사람이 「정보가 부족한 차구나」를 안다
금지   분모를 줄이는 것.  어떤 사유로도
금지   excluded 를 분모 조정에 쓰는 것
       ★ excluded 는 「왜 0점인가」의 사유로만 남긴다
검산   V3-41  전 매물의 분모가 만점과 같은가
       ★ 하나라도 다르면 fatal
```

```
★ 이것이 뜻하는 것
  정보가 적은 매물은 낮은 점수를 받는다.  그것이 맞다
  「모르는 차」와 「좋은 차」는 다르다
  판매자가 정보를 안 주는 것도 그 매물의 성질이다
★ 등급이 전반적으로 내려간다.  그것도 맞다
  지금 등급이 부풀려져 있었을 뿐이다
필수   등급 경계(S · A · B …)를 다시 잡는다
       ★ 분모가 바뀌면 경계도 바뀌어야 한다.  실측으로 정한다
```

---

## ★ 차종 특성상 받을 수 없는 축 — 08-16 미확정

```
예   전기차에 엔진보증 · 디젤에 LPG 관련 축
```

```
★ 이것도 0점으로 하면 그 차종이 통째로 불리해진다
★ 그렇다고 분모를 줄이면 위 규격을 어긴다
미확정   배점 설계를 차종별로 둘 것인가
        아니면 전 차종 공통 축만 쓸 것인가
필수    정해질 때까지는 0점으로 둔다.  분모를 줄이지 않는다
```


---

## ★★★ 배점은 시장 기준을 따른다 — 08-16

**마스터 지적 — 「깡통에 HUD 만 있어도 만점 주겠더라」**

```
지금   취향 축(HUD · 선루프)이 큰 점수를 차지한다
시장   트림 · 옵션 전체가 값을 결정한다
★ 깡통 + HUD = 만점 이면 시장과 정반대다
  시장에서 깡통은 싸게 팔린다
```

### 축을 세 갈래로 나눈다

```
① 시장가치 축 — 이 차가 얼마짜리인가
   트림 등급 · 선택 옵션 합 · 연식 · 주행 · 신차가 대비 감가
   ★ 여기가 가장 커야 한다.  중고차는 「얼마짜리를 얼마에 사나」다

② 상태 축 — 이 차가 성한가
   사고 회수 · 자차 수리비 · 골격 · 보증 잔여 · 렌트 이력
   ★ 값을 깎는 요소다

③ 취향 축 — 내가 원하는가
   HUD · 선루프 · 색 · 특정 옵션
   ★ 가중치이지 만점 요소가 아니다
     사람마다 다르므로 사용자가 조정한다
```

```
필수   세 갈래의 배점 비중을 규격에 적는다
필수   취향 축만으로 높은 등급이 나오지 않게 한다
       ★ 「깡통에 HUD」가 「풀옵션에 HUD 없음」보다 높으면 안 된다
필수   트림이 높을수록 · 옵션이 많을수록 시장가치 축 점수가 높다
       ★ 지금은 그 반영이 없다
필수   취향 축은 사용자가 켜고 끈다
       ★ 「나는 선루프 필요 없다」를 반영할 수 있어야 한다
검산   V3-42  취향 축만으로 S·A 가 나온 건이 있는가
       V3-43  트림·옵션이 높은데 점수가 낮은 역전이 있는가
```

---

## ★ 차종별 가격 평가 기준 — 08-16 미확정

**마스터 지적 — 「차종별로 가격을 어떻게 우수하게 평가하는 기준도 없다」**

```
필수   차종마다 다른 것을 인정한다
       감가율          제네시스와 기아는 다르다
       인기 트림       그 차종에서 무엇이 잘 팔리나
       옵션 폭         제네시스는 옵션으로 값이 갈린다
필수   그것을 config 로 둔다.  코드에 박지 않는다
필수   자료가 없으면 그렇게 적는다
       「이 차종의 감가 자료가 없습니다 — 전체 평균으로 봅니다」
미확정   차종별 기준을 어떻게 얻을 것인가
        ① 우리 DB 의 실측 (3,470건)
        ② 외부 자료
        ★ ①로 시작한다.  표본이 쌓이면 정확해진다
```

---

## ★ `null` 은 원인을 역산한다 — 08-16

**마스터 지적 — 「null 이면 왜 빠졌는지 역산해서 원인을 분석해야지」**

```
필수   축이 NULL 일 때 원인을 자동으로 가른다
       ① 원문을 못 받았다        어느 엔드포인트가 실패했나 · 코드는
       ② 원문은 받았는데 필드가 없다   어느 필드를 찾았나
       ③ 필드는 있는데 값이 null 이다
       ④ 값은 있는데 사전에 없다     그 값이 무엇인가
       ⑤ 계산에 필요한 다른 값이 없다  무엇이 없나
필수   그 원인을 화면에 낸다.  「확인 못 함」으로만 두지 않는다
       「점검부를 받지 못했습니다 (404)」
       「점검부에 틴팅 항목이 없습니다」
       「카탈로그를 받지 못해 사양을 볼 수 없습니다」
필수   원인별로 몇 건인지 집계한다 — 관리 화면에
       ★ 「404 가 200건」이면 수집을 다시 하면 된다
         「원문에 항목이 없음이 3,000건」이면 그 축을 접어야 한다
금지   「확인 못 함」 하나로 뭉치는 것
검산   V3-44  NULL 축에 원인 코드가 붙어 있는가
```

```
★ 왜 중요한가
  원인을 알면 고칠 수 있다.  모르면 영원히 「확인 못 함」이다
  ★ 지금 excluded 1,017행이 있는데 무엇을 고쳐야 할지 모른다
```


---

# ~~배점 재설계 — 시장 기준 · 08-16~~ ★ 폐기 — 개정 329

```
★★ 이 절은 개정 329(부록 F)로 대체됐다.  읽지 마라
   개정 292 의 배점(값250·상태180·사양75·취향50)은 폐기다
   지금 유효한 배점은 docs/ref/F-scoring.md 하나뿐이다
근거   마스터 지적 08-17 — 「항목이 너무 한정적이다」
      17축 → 26축으로 다시 짰다
남긴 이유   시장 조사 근거(감가율·주행·사고·용도·색상)는 그대로 쓴다
          그 근거는 부록 F 에 옮겼다
```

---

# ★★★ 한 차종에서 만든 기준을 전 차종에 펴지 않는다 — 08-16

**마스터 통찰 — 「한 차종에 집착한 기준을 다른 차에 일관적으로 세워서 그런 듯」**

```
어떻게 그렇게 됐나
  v1 은 KOLEOS_HEV 하나만 수집했다
  그 차를 보며 축을 만들었다
    HUD · 선루프   그 차에서 마스터가 보던 옵션
    엔진보증             내연기관·하이브리드 전제
    틴팅                 그 차 점검부에서 봤던 항목
  → 「한 차를 잘 보는 기준」이 되었다
  → 그것을 8차종에 그대로 폈다
```

```
그래서 지금
  MODEL_Y (전기)      엔진보증 0점 — 엔진이 없다
  G80 (고급)          HUD 는 기본 장착이라 변별력이 없다
  SPORTAGE (신차급)   기준가가 없다 — 감가 곡선 밖이다
★ 축이 차종을 못 가린다
```

### 규격

```
필수   축은 「모든 차가 가질 수 있는 것」으로 정의한다
       엔진보증  →  동력계 보증 (엔진 · 모터 · 배터리)
       ★ 특정 동력원·특정 트림만 가질 수 있는 이름을 쓰지 않는다

필수   축을 전 차종에 펴기 전에 결측률을 본다
       차종마다 그 축이 몇 % 채워지는가
       ★ 한 차종에서 90% 채워지는데 다른 차종에서 0% 면
         그 축은 그 차종 전용이거나 이름이 잘못된 것이다

필수   변별력도 본다
       그 차종에서 전건이 같은 값이면 점수가 의미 없다
       ★ G80 은 HUD 가 기본이라 전건 「있음」이다
         그러면 그 축이 G80 안에서는 아무것도 안 가른다

필수   기준가는 끊기지 않게 잇는다
       1년 미만    신차가 대비
       1~N년      감가 곡선 · 실제 중앙값
       ★ 구간 사이에 빈 곳이 없어야 한다

금지   한 차종에서 뽑은 축을 검증 없이 전 차종에 적용하는 것
검산   V3-47  축별 차종 간 결측률 편차가 상한을 넘지 않는가
       V3-48  축이 차종 안에서 변별력이 있는가 (전건 동일값이 아닌가)
```

```
★ 이것이 v1 의 근본 한계였다
  한 차종을 깊이 본 것은 좋았다.  그것을 편 것이 문제였다
  v2 는 8차종 3,470건을 갖고 있다.  이제 검증할 수 있다
```


---

# ★★★ 엔카가 이미 주는 것 — 08-16 실물 화면 확인

**마스터가 엔카 상세·리포트·성능점검·보험이력·엔카진단 화면을 보여 주심.**
**우리가 계산하려던 것을 엔카가 이미 준다.**

## ① 매물 경쟁력 — 시세 판정

```
「157호6282 차량은 시세대비 가격이 적정해요」
2022년형 가솔린 2.5 터보 AWD · 4,010만원
막대   3,831만 ─── ● 적정 ─── 4,056만
```

```
★ 엔카가 시세 구간과 적정 여부를 직접 준다
필수   이 값을 받아 쓴다.  우리 계산과 함께 낸다
필수   둘이 다르면 그것을 낸다 — 「엔카 적정 · 우리 판단 비쌈」
       ★ 다른 것이 정보다.  숨기지 않는다
금지   엔카 값이 있는데 우리 계산만 내는 것
```

## ② 엔카진단 — 플랫폼이 직접 진단하고 보증

```
「엔카진단 결과 무사고 차량으로 판정되었습니다」
프레임 진단 6항목   프론트패널/인사이드 · 앞뒷휠하우스 · 필러/대쉬/플로어
                  사이드실/쿼터 · 리어패널/트렁크플로어 · 사이드멤버/루프/패키지트레이
외부패널 진단 5항목  후드 · 프론트휀더 · 앞문 · 뒷문 · 트렁크리드
「엔카확인 결과 가솔린 2.5 터보 AWD 등급으로 판정」   ← 트림을 엔카가 확인
```

```
★ 마스터 지시 — 「엔카가 차량 점검하고 보증한 것도 가중치를 높게 줘야 해」
필수   엔카진단 여부를 별도 축으로 둔다.  배점을 높게 준다
근거   딜러가 하는 말이 아니라 플랫폼이 책임지는 판정이다
       진단 없는 매물과 같이 볼 수 없다
필수   엔카확인 등급이 우리 트림 판정과 다르면 그것을 낸다
```

## ③ 엔카보증

```
성능보험        30일 / 2,000km · 약 60개 부품
엔카종합보증     ~180일 / 10,000km · 약 160개 부품 · 보상한도 내 무제한 수리
```

```
필수   엔카보증 유무·범위를 축으로 둔다
필수   제조사 보증과 따로 낸다
       ★ 제조사 보증이 끝났어도 엔카보증이 있으면 다르다
         실측 — 그랜저 GN7 「일반/차체 보증기간 종료」 · 엔카종합보증 180일
```

## ④ 용도 변경 — 렌트 이력이 여기 있다

```
성능점검   「용도변경  있음 / 렌트」
보험이력   「용도 변경이력  사용이력있음」
```

```
★ 우리가 advertisementType 으로만 보던 것이 성능점검에 명시돼 있다
필수   둘을 대조한다.  다르면 낸다
필수   「렌트」가 명시되면 그것을 그대로 낸다
```

## ⑤ 항목별 점검 리포트 — 엔카 매니저 직접 점검

```
기본정보   성능점검기록부 · 기타정비내역 · 차량 키 수량[2개] · 제조사 메뉴얼
          틴팅(앞 유리) · 리페어장비 · 소화기
탭        기본정보 · 휠타이어 · 등화장치 · 실내 · 외관 · 옵션
```

```
★ 마스터 물음 — 「왜 내 눈에는 틴팅과 차량 키 개수가 보일까」
  → 우리가 이 리포트를 안 받고 있다
필수   이 리포트를 받는다.  어느 엔드포인트인지 찾는다
필수   차량 키 개수 · 매뉴얼 · 소화기 · 리페어장비를 축으로 둔다
       ★ 「키가 1개」는 분실을 뜻한다.  실제 비용이 든다
```

## ⑥ 그 밖에 화면에 있는 것

```
누유 / 미세누유    0건 / 0건
유료 옵션 합계     890만원          ★ 엔카가 합계를 준다
특수 사고이력      전손 0 / 침수 0 / 도난 0
보증유형          보험사보증
원동기 형식        G4KR
주행거리 계기상태   양호
튜닝 · 리콜대상    없음 · 해당없음
타이어 트레드      3mm / 6mm       ★ 소모품 교체 비용을 뜻한다
```

## 규칙

```
필수   엔카가 주는 값을 우리가 다시 계산하지 않는다
       ★ 계산하더라도 엔카 값을 함께 내고 대조한다
필수   플랫폼이 책임지는 것(엔카진단 · 엔카보증)은 가중치를 높게 준다
필수   화면에 있는데 우리가 못 받은 것은 결함이다
       ★ 「원문에 없다」고 하기 전에 화면을 본다
검산   V1-22  엔카 화면 항목 중 우리가 안 받는 것이 있는가
       V3-51  엔카 시세 판정과 우리 판정이 크게 다른 건이 있는가
```


---

# ★★★ 「왜 싼가」를 말한다 — 08-17

**마스터 지적 — 「엔카 보증이 없는 것이 가격이 왜 싼지가 중요해.**
**1차로 엔카 보증이 없으니 불안하고, 이 케이스는 엔진성능을 판매자가 직접 올렸고 렌트카야」**

## 실측 — 같은 차를 우리와 엔카가 이렇게 봤다

```
매물   296가7288 · G80 (RG3) 가솔린 2.5 터보 AWD · 23/06식 · 34,145km · 2,990만

우리   등급 A · 89.9% · 818/910 · 시세차 −1,100만 · 「렌트 아님」
엔카   신차대비 45% · ★ 특이 사항 「렌트 이력」(빨간 글씨)
      성능점검 「판매자가 사진으로 등록했습니다」
      ★ 엔카진단 없음
      성능점검  사고이력 있음 · 단순수리 있음 · 용도변경 렌트
```

```
★ 싼 데는 이유가 있었다.  우리는 그것을 「좋은 것」으로 읽고 A 를 줬다
```

## 규격

```
필수   「싸다」를 말할 때 「왜 싼가」를 함께 낸다
       ★ 이유 없이 싼 차는 없다.  이유를 못 찾았으면 그렇게 적는다
필수   싼 이유 후보를 순서대로 확인해 낸다
       ① 플랫폼 진단·보증이 없다
       ② 성능점검을 판매자가 올렸다 (엔카직영 점검이 아니다)
       ③ 렌트·리스 이력
       ④ 사고 · 단순수리
       ⑤ 주행거리 · 연식
       ⑥ 인기 없는 색 · 트림
       ⑦ 자차 미가입 기간
필수   찾은 이유를 화면에 낸다 — 「싼 이유 셋을 찾았습니다」
필수   이유를 못 찾으면 그것도 낸다 — 「싼 이유를 찾지 못했습니다.  직접 확인하십시오」
       ★ 그것이 오히려 위험 신호다
금지   「시세차 −1,100만」만 내고 끝내는 것
검산   V3-52  「싸다」로 표시된 매물에 이유가 붙어 있는가
```

---

# ★★★ 점검의 출처를 가른다 — 08-17

```
엔카직영 점검   엔카가 직접 · 플랫폼이 책임진다
판매자 등록     판매자가 사진으로 올린 것 · 「모든 책임은 판매자에게 있습니다」
```

**실물 문구 — 「본 페이지는 … 해당 내용은 판매자가 직접 입력한 내용으로 모든 책임은 판매자에게 있습니다」**

```
필수   성능점검 출처를 축으로 둔다
       엔카직영 · 판매자 등록 · 없음
필수   엔카진단 유무를 축으로 둔다
필수   엔카보증 유무를 축으로 둔다
필수   셋을 묶어 「플랫폼 신뢰도」로 낸다
       높음   엔카진단 · 엔카보증 · 엔카직영 점검
       보통   엔카직영 점검만
       낮음   판매자 등록 점검만
       없음   점검 자체가 없음
필수   화면에 그대로 낸다 — 「점검을 판매자가 올렸습니다」
근거   마스터 지적 — 「엔카 보증이 없으니 불안하다」
       ★ 같은 값이라도 누가 보증하느냐가 다르다
검산   V3-53  점검 출처가 판정에 반영되는가
```

---

# ★ 신차가는 등급기준 + 선택옵션이다 — 08-17 실측 확인

```
엔카 화면
  신차가        6,547만원
  등급 기준      5,787만원
  선택옵션 2종   760만원
    2열 컴포트 패키지 II   250만원
    파퓰러 패키지         510만원
  현재 판매가    2,990만원 (45%)
  「신차가는 판매자가 입력한 등급+선택옵션 기준으로 산출되었습니다」
```

```
★ 우리 화면은 5,787만(등급 기준)만 냈다.  옵션 760만이 빠졌다
필수   신차가 = 등급기준 + 선택옵션 합
필수   셋을 다 낸다 — 「등급기준 5,787 + 옵션 760 = 6,547만」
필수   「판매자가 입력한 것」임을 밝힌다
검산   V11-92  신차가가 등급기준 + 옵션 합과 같은가
```

---

# ★ 렌트 이력을 놓쳤다 — 08-17

```
우리   「렌트 아님」
엔카   특이 사항 「렌트 이력」 (빨간 글씨)
      성능점검 용도변경 「렌트」 체크
```

```
★ 우리는 advertisementType 만 봤다.  그것은 「지금 리스 상품인가」다
  「과거에 렌트로 쓰였나」는 성능점검과 보험이력에 있다
필수   셋을 다 본다
       advertisementType          지금 리스 상품인가
       성능점검 용도변경            과거 용도
       보험이력 용도 변경이력        과거 용도
필수   하나라도 렌트면 「렌트 이력」이다
검산   V3-54  렌트 이력을 세 곳에서 대조하는가
```


---

# ~~배점 605 — 사이트 보증 축 신설 · 08-17~~ ★ 폐기 — 개정 329

```
★★ 이 절은 개정 329(부록 F)로 대체됐다.  읽지 마라
   ⑤ 사이트 보증 50(우수등급30·점검12·보증8)은 폐기다
   부록 F 에서 ⑤ 보증 30(제조사15·우수등급10·점검5)으로 바뀌었다
남긴 것   사이트마다 우수등급의 뜻이 다르다는 것은 그대로다
         엔카 진단+우수 · K카 직영 · 인증중고차 등록
         → sites.json 의 site_grade_rule
```

---

# ★★ 전기차는 SOH 가 주행거리에 해당한다 — 08-17

**개발측 실측 — 배터리 진단 30건 · SOH 91.1~96.8 · 중앙 94.4**

```json
{"soh": 93.9, "sohGrade": "SS", "gradeMessage": "아주 좋은 상태에요!"}
```

```
★ 파서가 「있다/없다」만 남기고 SOH 를 버리고 있었다
  「있다」만 남기면 그 값을 버리는 것이다
```

## 개정 293 의 답이다

```
개정 293   「전기차가 엔진보증으로 손해를 본다」
답        전기차에는 그 자리에 다른 것이 있다
```

```
~~필수   ① 값 250 의 「주행 대비 60」을 동력원별로 다르게 본다~~
~~       전기차은 주행 30 + SOH 30~~     ★ 폐기 — 개정 380

★★ 개정 380 마스터 확정 — SOH 는 **축이 아니라 가점**이다
   「없다고 감점하지 않는다」
   ★ 실측 — 전기차 778건 중 SOH 가 있는 것은 33건뿐이다.
     축으로 만들면 745건이 0점을 받는다.  분모가 왜곡된다

필수 [마스터]  SOH 는 **가점** (최대 +30).  ★ 분모를 늘리지 않는다
필수          SOH 가점표
              97% 이상   +30
              94~97      +24
              90~94      +18
              85~90      +10
              85 미만      0
              ★ 실측 중앙 94.4 를 기준으로 잡았다.  표본이 늘면 다시 잰다
~~필수   SOH 를 못 받았으면 0점이다 (개정 289)~~  ★ 폐기 — 개정 380
필수 [마스터]  ★ SOH 를 못 받았으면 **가점이 없을 뿐이다**.  감점도 0점 축도 아니다
              사유를 낸다 — 「배터리 진단이 없습니다」
필수   화면에 그대로 낸다 — 「배터리 SOH 93.9% (SS)」
       ★ 등급 문자도 함께.  사이트가 매긴 것이다
금지   SOH 를 「있다/없다」로 뭉개는 것
금지   ★ SOH 를 축으로 만드는 것.  ★ 분모가 늘어나면 실패다
검산   V3-58  ★ SOH 가 **가점**으로 더해지는가 (축이 아니다 · ★ 분모 그대로)
              ★ SOH 가 **없는** 전기차 표본으로 되살린다.  있는 것만 보면 통과한다
       V3-59  내연기관에 SOH 가점이 붙지 않는가
```

```
★ 이것이 「축이 차종을 못 가린다」의 올바른 해법이다
  축을 빼는 것이 아니라 그 자리에 맞는 것을 넣는다
필수   다른 축에도 같은 눈으로 본다
       엔진보증  →  동력계 보증 (엔진 · 모터 · 배터리)
       배기량    →  출력
```


---

# ~~축별 계산식 — 605점 전문 · 08-17~~ ★ 폐기 — 개정 329

```
★★ 이 절은 개정 329(부록 F)로 대체됐다.  읽지 마라
   개정 326 의 17축 계산식은 폐기다
   지금 유효한 것은 docs/ref/F-scoring.md 의 26축 점수표다
근거   마스터 지적 08-17 — 「항목별 점수 테이블도 없고 배점 룰도 없다」
```

---

# ★★★ 지금 유효한 배점 — 부록 F 하나뿐이다

```
docs/ref/F-scoring.md      26축 · 605점 · 등급 기준 555
★ 배점을 볼 때 이 파일만 본다
★ 이 장의 다른 배점표는 전부 폐기다 (개정 292 · 306 · 326)
필수   배점을 고칠 때 부록 F 만 고친다.  여기저기 적지 않는다
검산   V3-69  배점표가 부록 F 하나뿐인가
```
