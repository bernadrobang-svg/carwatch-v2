## STEP 76 — 이력 · 사고 20점

```
version  SPEC-2026.08.29-r967
follows  ★ 정본 — 배점의 원천은 `config/scoring.json`
sources  실측 08-22
checks   S46-38 · S46-39
```


### 목적

```
차량 자체가 얼마나 손상됐는가.  보험 처리 여부가 아니라 물리적 손상을 본다.
```

### 근거 우선순위

```
1  점검부 outers[].attributes       RANK_* 로 골격/외판 직접 판정
2  record accidents[]               보험 처리 이력
3  record myAccidentCnt             집계값
금지 부위명 문자열 매칭              표기 흔들림에 무너진다
```

### `attributes` 는 부위 분류 코드다 — 실측

| 값 | 분류 | 부위 | 건수 |
|---|---|---|---:|
| `RANK_ONE` | 외판 1랭크 | 휀더 · 도어 · 후드 · 트렁크 리드 · 라디에이터 서포트 | 915 |
| `RANK_TWO` | 외판 2랭크 | 쿼터 패널 · 사이드실 패널 · 루프 패널 | 194 |
| `RANK_A` | **골격 A** | 리어 패널 · 트렁크 플로어 · 인사이드 패널 · 프론트 패널 | 126 |
| `RANK_B` | **골격 B** | 사이드 멤버 · 휠하우스 · 필러 패널 | 51 |
| `RANK_C` | **골격 C** | 플로어 패널 | 1 |

```
v1 실사고   부위 사전이 「프론트펜더」, 원문이 「프론트 휀더(우)」
           가장 흔한 부위 344건이 미분류.  사고 20점이 한 번도 작동하지 않았다
→ 엔카는 처음부터 코드값으로 주고 있었다.  사전이 필요 없는 문제였다
```

### 배점 — 외판 교환 판수

| 판수 (`RANK_ONE`+`RANK_TWO` 중 「교환(교체)」) | 점수 |
|:---:|---:|
| 0 | 20 |
| 1 | 15 |
| 2 | 10 |
| 3 | 5 |
| 4 이상 | 0 |

```
적용     statusTypes 에 「교환(교체)」인 것만
미적용   「판금/용접」 단독  →  감점 없음
근거     도색·판금은 색상 보정으로도 발생한다.  사고와 구분되지 않는다
        교환은 부품을 갈았다는 뜻이라 손상이 확실하다
골격     RANK_A/B/C 는 판수와 무관하게 E등급 (STEP 82)
```

### `statusTypes` 실측 4종

```
교환(교체)  861   감점 대상
판금/용접   425   감점 없음
용접,절단    14   ★ 미확정 — 구조 개입이라 판금과 성격이 다르다 (STEP 86)
손상          1   ★ 미확정
```

### 검산

```
분포 특성   외판 교환 0판이 다수를 차지한다.  이 축은 나머지를 가른다
경고       0판이 95% 를 넘으면 판정이 안 붙는 것을 의심한다
          v1 은 전건 0판이었다 (insp_outer_json 전건 NULL)
```

**표본 시험** (0장 STEP 6a)

```
inspection_outer_swap   교환 5판 · 골격 없음   →  사고 20점 = 0
inspection_outer_paint  판금/용접만            →  감점 없음 (20점)
inspection_frame        RANK_B 1 · 외판 4판    →  골격.  E등급 (STEP 82)
inspection_clean        outers 0판             →  20점
```

---

## STEP 77 [판정] — 이력 · 보험 15점

```
목적    보험 처리 금액과 건수로 손상 규모를 본다
원천    record accidents[] · myAccidentCost · accidentCnt
값규칙  이력 미확보 시 NULL + excluded.  0 원으로 채우지 않는다
근거    금액이 0원이어도 사고 이력 자체가 감점 요인이라 건수 상한을 함께 건다
금지    type 3 을 감점에 넣는 것.  insuranceBenefit 을 쓰는 것
검산    type 1+2 합 == myAccidentCost 인가.  어긋나면 파싱을 의심한다
```

### 목적

```
보험 처리 금액으로 손상 규모를 본다.  건수 상한과 함께 쓴다.
```

### `accidents[].type` — 산술로 확정

**아래 표는 사양 확정의 유일한 근거다. 관측치가 아니라 검산이다.**
**같은 검산을 새 데이터에서 다시 하면 같은 결론이 나온다.**

| 건 | type | part+labor+painting |
|---|:---:|---:|
| 2026-02-03 | 2 | 16,703,000 |
| 2025-10-22 | 2 | 1,779,050 |
| 2025-10-14 | 2 | 1,084,340 |
| 2025-02-22 | 1 | 14,096,329 |
| 2025-02-22 | **3** | **5,855,775** |

```
type 1+2 합 = 33,662,719  =  myAccidentCost      (4건 = myAccidentCnt)
type 3      =  5,855,775  =  otherAccidentCost   (1건 = otherAccidentCnt)
→ 원 단위까지 일치.  우연이 아니다
```

| type | 의미 | 감점 |
|:---:|---|:---:|
| 1 · 2 | 내 차 피해 | **대상** |
| **3** | **타 차 가해** | **제외** |

```
근거   type 3 은 상대 차 수리비다.  이 차의 손상이 아니다
결과   myAccidentCost 를 그대로 쓰면 된다.  별도 합산 로직이 필요 없다
금지   insuranceBenefit 사용.  세 항목 합과 다르다
v1 오류 「1·3 합산」.  2 를 빼먹었고 3 을 잘못 넣었다
```

**표본 시험** — `record_with_accident` 는 `type` 3 만 1건이다.

```
myAccidentCnt 0 · myAccidentCost 0 · otherAccidentCnt 1
→ 보험 15점 만점.  건수 상한도 걸지 않는다
type 3 을 감점에 넣으면 이 표본이 깨진다
```

### ★ 표본 한계를 명시한다

```
근거 표본   정상 record 가 극소수다 (대부분 점검부로 오염)
관측       type 2 · 3 은 다건.  type 1 은 위 검산 1건 외 추가 관측 없음
표기       「27건 표본에서 관측」이다.  「흔하다」가 아니다 (0장 STEP 5.2a)
확정       이력 재수집 후 전수로 다시 확인한다
```

### 배점 — 금액 곡선 × 건수 상한

**두 단계다. 금액으로 점수를 내고, 건수로 천장을 씌운다.**

```
1  금액 비율 = myAccidentCost ÷ price_origin_won
2  비율 → 점수 (아래 곡선)
3  건수 상한으로 클리핑
4  min(금액 점수, 건수 상한)
```

### 금액 곡선

| 수리비 ÷ 신차가 | 점수 |
|---|---:|
| 0 (무사고) | 15 |
| ~2% | 13 |
| ~4% | 11 |
| ~7% | 8 |
| ~10% | 5 |
| ~15% | 2 |
| 15% 초과 | 0 |

```
근거   구간 경계를 실매물 분위수에 맞춘다
      p25 · p50 · p75 · p90 근처에 경계를 두면 점수가 고르게 퍼진다
      한쪽에 몰리면 이 축이 순위를 못 가른다 (V3-04)
설정   config.scoring.axis_rules.history.insurance_cost_curve
       초기값은 첫 수집 분포에서 산출한다.  본문에 숫자를 박지 않는다
```

```
★ 금액이 0 인데 사고 건수가 있는 경우가 있다
  type 3 만 있는 매물이다 (표본 record_with_accident)
  → 금액 15점 만점.  건수 상한도 적용하지 않는다.  내 차 피해가 아니다
필수   건수는 myAccidentCnt 를 쓴다.  accidentCnt 가 아니다
```

| `accidentCnt` | 상한 |
|:---:|---:|
| 0 | 15 |
| 1 | 12 |
| 2 | 9 |
| 3 이상 | 6 |

```
근거   금액이 0원이어도 사고 이력 자체가 감점 요인이다
      경미 사고 다건과 중대 사고 1건을 금액만으로 비교할 수 없다
검산   42436127 (사고 5건) → 금액 감점 이전에 이미 6점 상한
```

---

## STEP 78 [판정] — 이력 · 렌트 20점

```
목적    렌터카 이력은 사용 강도와 정비 이력의 불확실성을 뜻한다
원천    점검부 usageChangeTypes · record carNo · notJoinDate · carInfoUse
값규칙  판정 근거가 하나도 없으면 「불명」 10점.  NULL · excluded 가 아니다
근거    수집 실패를 렌트 이력으로 단정하지 않는다.  0 으로 두면 실패가 감점이 된다
금지    SellType 을 1순위로 쓰는 것.  현재 판매 형태지 과거 이력이 아니다
검산    렌트 비율이 차종별 SellType 분포와 크게 어긋나면 판정 오류
```

### 목적

```
렌터카 이력은 사용 강도와 정비 이력의 불확실성을 뜻한다.
```

### 근거 우선순위

```
1  점검부 master.detail.usageChangeTypes   ★ 용도 변경 이력.  직접 근거
2  core_record.plate_use_char             허·하·호 = 렌터카 (원본은 core_pii · 3장 STEP 35)
3  record notJoinDate1~5                  보험 미가입 구간 = 렌트사 통합보험 신호
4  record carInfoUse1s / Use2s            용도 이력 코드
5  목록 SellType · LeaseType              현재 판매 형태.  참고만
```

### ★ 정정 — `usageChangeTypes` 는 실재한다 (2026-08-09 재실측)

```
v1·v2 초판 서술   「존재하지 않는 필드다」  →  틀렸다
사실             record 에는 없다.  점검부 master.detail 에 있다
```

**관측된 값**

```
[]                                용도 변경 없음
[{"code":"1","title":"렌트"}]        ★ 렌트 이력
[{"code":"3","title":"영업용"}]       영업용
키 없음
```

**렌트 판정의 가장 직접적인 근거를 스스로 차단하고 있었다.**

```
교훈   「존재하지 않는다」는 그 필드를 찾은 엔드포인트에서만 참이다
      다른 엔드포인트를 확인하지 않고 전역 금지로 선언하면 안 된다
필수   금지 근거 등록 전 전 엔드포인트에서 경로를 검색한다
```

```
SellType 을 1순위로 쓰지 않는다.
「현재 렌트로 판매」와 「과거 렌트 이력」은 다르다.
렌트 출신인데 일반으로 파는 매물을 놓친다.
```

| 판정 | 점수 |
|---|---:|
| 비렌트 | 20 |
| 렌트 이력 | 0 |
| 불명 | 10 |

```
근거   불명 10 — 이력 API 미확보 매물을 렌트로 단정할 수 없다
      0 으로 두면 수집 실패가 감점이 된다
검산   렌트 비율이 차종별 SellType 분포와 크게 어긋나면 판정 오류
실측   모델Y 렌트·리스 32.1% · G80 8.0% · 콜레오스 7.2%
       모델Y 는 구조적으로 이 축에서 불리하다.  차종 특성이지 결함이 아니다
```

**표본 시험** — `inspection_clean` 은 `outers` 0판인데 `usageChangeTypes` 에 렌트다.
**사고 20점 만점 · 렌트 0점.** 두 축이 독립임을 이 표본이 고정한다.

**`LeaseType` 실측 4값** — 렌트승계 · 운용리스 · 장기렌트 · 금융리스.
`SellType` 보다 성격을 직접 지시한다. **일부만 적으면 나머지가 통과한다** (4장 STEP 43).

---



---

## ★★★ 자차보험 미가입 기간 — 08-16

**마스터 물음 — 「자차보험을 들지 않은 차와 든 차를 구분 가능해?」**

```
★ 이것이 「무사고」 판정의 가장 큰 함정이다
```

### 조사한 것

```
「자차 미가입 기간이 있거나 사고 이후 자비로 수리하는 경우,
 보험 이력에 남지 않아 구매자는 이를 모를 수밖에 없다」

「이 차량은 5년간 자차보험에 미가입되어 있었기 때문에
 이 기간 동안 어떤 사고가 났는지는 전혀 알 수 없습니다」

「보험이력 없음이 곧 완전 무사고를 뜻하지는 않습니다」
```

```
★ 카히스토리 이력에 「자차 가입 기간」이 나온다
  그러므로 구분할 수 있다
```

### 「내차 피해 0원」은 두 가지 뜻이다

```
① 자차에 가입한 상태로 사고가 없었다        ←  진짜 무사고
② 자차 미가입이라 기록이 안 남았다          ←  알 수 없음
★ 지금은 둘을 구분하지 않는다.  ②를 「무사고」로 읽고 있다
```

### 규격

```
필수   자차 가입 기간을 원문에서 확인한다
필수   보유 기간 중 자차 미가입 기간이 있으면 그것을 낸다
       「자차 미가입 5년 — 그 기간의 사고는 알 수 없습니다」
필수   「무사고」를 세 갈래로 낸다
       무사고        자차 가입 전 기간 · 이력 없음
       사고 N회      이력에 있음
       확인 불가      자차 미가입 기간이 있음        ← ? 로 낸다
       ★ A-4 의 O · - · ? 가 여기에도 적용된다
필수   미가입 기간이 길수록 점수를 낮춘다.  「무사고」로 만점을 주지 않는다
       기준   미가입 0%       사고 이력대로
             미가입 30% 미만  감점
             미가입 30% 이상  「확인 불가」 — 그 축을 ? 로
금지   자차 미가입 차를 「무사고」로 내는 것
       ★ 그것은 사실이 아니다.  「기록이 없다」일 뿐이다
검산   V3-49  자차 미가입 기간이 있는 매물이 「무사고」로 나오지 않는가
```

```
★ 왜 이것이 중요한가
  「무사고 · 자차 미가입 5년」은 「사고를 숨긴 차」일 수 있다
  실제로 그런 사례가 보고된다 — 렌터카로 쓰인 기간
★ 그리고 이 도구의 목적과 직결된다
  값이 싼 이유가 「자차 미가입 기간이 길어서」일 수 있다
  그것을 모르면 「싸고 무사고」로 보인다
```

---

## ★ 성능기록부와 보험이력을 대조한다 — 08-16

```
조사한 것
  「성능 기록부에는 흔적이 없는데 보험 이력에는 수리 비용이 있었다」
  「보험으로 수리한 비용은 있지만 성능 기록부에는 이력이 없다면」
```

```
필수   둘이 어긋나면 그것을 낸다
       성능부 무사고 · 보험 수리비 있음  →  「성능부와 보험이력이 다릅니다」
필수   어긋난 매물을 목록에서 표시한다
       ★ 사람이 그것만 따로 볼 수 있게
검산   V3-50  성능부와 보험이력이 어긋난 건을 세는가
```

```
★ 우리는 둘 다 갖고 있다 — inspection 과 record
  대조는 우리만 할 수 있는 일이다.  사람은 눈으로 못 한다
```
