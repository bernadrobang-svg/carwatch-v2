# 0장. 개발 표준

```
version  SPEC-2026.08.29-r952
follows  `docs/chapters/30-score/f-table.md`
sources  실측 08-24
checks   S46-38 · S46-39
```


## 실행 환경 — 08-16

```
필수   Python 3.11 이상
근거   zip(strict=True) 는 3.10+ · match 문은 3.10+
      3.9 에서는 TypeError: zip() takes no keyword arguments 로 죽는다
      실측 08-16.  Amazon Linux 2023 기본이 3.9.25 라 5파일이 한꺼번에 터졌다
필수   외부 의존이 없다.  pip install 없이 표준 라이브러리만 쓴다
필수   실행·시험·검사를 전부 같은 인터프리터로 돌린다
      python3 가 3.9 인 환경에서는 python3.11 을 명시한다
금지   버전이 낮다는 이유로 코드를 내리는 것
      zip(strict=True) 는 규격이다 (부록 E · S22 — B905 fatal)
검산   S23  실행 인터프리터가 3.11 이상인가
```

```
★ 확인
  python3 -V        시스템 기본 — 3.9 일 수 있다
  python3.11 -V     이것이 앱이 쓰는 것이다
  systemd 서비스도 /usr/bin/python3.11 을 쓴다
```


## 도구와 시험 — 08-14 등재

**소스에 있는데 규격에 없던 것을 적었습니다. 지우지 않고 등재합니다.**

### `tools/` — 13종

| 이름 | 무엇 | 성격 |
|---|---|---|
| `check_spec.py` | 지시서 자체 검사 13종 + `S17`~`S21` | 필수 |
| `check_src.py` | 소스 ↔ 지시서 대조 `S1`~`S16` | 필수 |
| `check_screens.py` | 시안 ↔ 템플릿 대조 | 필수 |
| `check_all.py` | 실측 DB 로 `V1`~`V11` 전 차수 | 필수 |
| `run_tests.py` | 시험 15종 실행 | 필수 |
| `migrate.py` | DDL 변경을 기존 DB 에 반영 | 필수 |
| `sync_registry.py` | RAW 경로 전수 → `meta_field_usage` | 필수 |
| `build_dict.py` | RAW 에서 사전 생성 | 필수 |
| `classify_fields.py` | 등록부 `usage` 분류 보조 | 보조 |
| `inspect_dict.py` | 사전 조회 | 보조 |
| `inspect_facet.py` | facet 축 확인 | 보조 |
| `inspect_requests.py` | 요청 기록 조회 | 보조 |
| `menu.py` | `run.bat` 이 부르는 메뉴 | 보조 |
| `setup_check.py` | 환경 점검 | 보조 |

```
필수   「필수」는 checkall 에 들어간다.  「보조」는 사람이 필요할 때 부른다
금지   보조 도구가 DB 를 고치는 것.  읽기만 한다
★ menu.py 에만 있는 명령을 만들지 않는다.  run.py 가 전 명령을 받는다 (V1-13)
```

### `tests/` — 15종

| 이름 | 무엇 |
|---|---|
| `test_invariants.py` | 0장 불변식 6개 (`S21`) |
| `test_collect.py` | 수집 · 봉투 · 재시도 |
| `test_pipeline.py` | 단계 순서 · 중단 · 재개 |
| `test_store.py` | 적재 · 대리키 · 불변 필드 |
| `test_dict.py` | 사전 · 완전 일치 |
| `test_registry.py` | 등록부 |
| `test_score.py` | 판정 · 채점 · 등급 |
| `test_report.py` | 리포트 · 파일 출력 |
| `test_watch.py` | 추적 · 알림 |
| `test_admin.py` | 관리자 · 권한 |
| `test_web.py` | 화면 · 라우팅 · 게이트 |
| `test_screens.py` | 시안 대조 |
| `test_crosssite.py` | 다중 사이트 |
| `test_run.py` | 진입점 · CLI |
| `test_fixtures.py` | 표본 무결성 |

```
필수   장을 착수하면 그 장의 시험을 함께 만든다
금지   시험을 마지막에 몰아 붙이는 것
표본   tests/fixtures/ 15개 — 상세 4종 · 점검부 4종 · 이력 2종 · 진단 · 카탈로그
      EXPECTED.json 이 기대값이다.  NOTES.json 에 표본의 성격을 적는다
```

### ★ 시험은 격리된 상태에서 돈다 — 08-16

```
금지   운영 DB(carwatch.db)를 복사해 시험을 도는 것
근거   실측 08-16.  test_integration.py:129 가 운영 DB 를 복사한다
      거기에 status='queued' 인 recalc_job 이 1건 남아 있어
      관리 화면이 전부 409 로 잠기고 test_spec_ui 가 9건 무더기로 실패했다
      코드는 하나도 안 바뀌었는데 결과가 달라졌다
필수   시험은 스키마만 만든 빈 DB 에서 시작한다
필수   필요한 데이터는 시험이 스스로 넣는다.  fixtures 를 쓴다
필수   같은 코드면 언제 돌려도 같은 결과가 나와야 한다
       ★ 이것이 안 되면 「고쳤다」와 「환경이 바뀌었다」를 못 가른다
검산   S24  시험이 운영 DB 를 읽지 않는가
```

```
★ 왜 이것이 중요한가
  v1 은 「어제는 됐는데 오늘은 안 된다」를 여러 번 겪었다
  원인이 코드인지 데이터인지 가릴 수 없으면 고칠 수 없다
  시험은 그 판단의 유일한 기준이므로 흔들리면 안 된다
```

```
★ 실전을 피하라는 것이 아니다 — 자리를 나누라는 것이다

  시험 (tools/run_tests.py)
    빈 DB · fixtures · 격리
    묻는 것   「코드가 맞나」
    조건      같은 코드면 언제 돌려도 같은 결과

  검증 (tools/check_all.py)
    실측 DB · 실제 API 호출
    묻는 것   「데이터가 맞나」
    조건      실물이어야 한다.  꾸며 낸 것으로는 못 본다
```

### ★★ 화면이 본체다 — 08-16

**마스터 진단 — 「CLI 있다고 개무시하니 웹이 개판이 됐다. v2 의 패착은 CLI 때문이다」**

```
무슨 일이 있었나
  CLI 가 있으니 「기능은 CLI 로 되니까 화면은 나중에」가 됐다
  화면은 「있기만 하면」 통과했다
  시안 255종 중 58종만 반영해도 검사가 안 잡았다
  시세가 표가 되고 링크가 반만 걸리고 툴팁이 4곳이었다
★ 개발측이 게을러서가 아니다
  「CLI 로 되니 화면이 못해도 일이 굴러갔다」가 원인이다
```

```
필수   화면이 본체다.  진입점은 화면이 부르는 도구다
필수   기능이 완성됐다는 판단은 화면에서 한다
       CLI 로 되는 것은 완성이 아니다
필수   화면을 못 만들면 그 기능은 미완이다.  「CLI 로 됩니다」로 넘기지 않는다
필수   검사는 「돌아가는가」가 아니라 「쓸 수 있는가」를 본다
       ★ 절이 있는가가 아니라 시안대로 보이는가
       ★ 링크가 걸렸는가 · 값이 몇 개 보이는가 · 몇 초 걸리는가
금지   진입점에만 있는 기능
금지   「화면은 나중에」
검산   V11-45  CLI 로만 되는 기능이 없는가 (부트스트랩 제외)
       S27     기능마다 화면이 있는가
```

```
★ v1 이 왜 화면이 나았나
  v1 은 화면밖에 없었다.  거기서 안 되면 아무것도 안 됐다
  그래서 화면에 전부 넣었다 — 22열 · 미리보기 · 단추 · 드롭다운
★ v2 는 CLI 가 있어서 화면이 「보조」가 됐다
  그것이 패착이다
```

### ★ 형상 관리 — 08-16

```
필수   소스는 git 저장소다.  모든 변경을 커밋한다
필수   GitHub 원격에 push 한다 — 서버가 죽어도 남고, 가이드가 읽는다
필수   커밋 메시지 첫머리에 규격 코드나 STEP 번호를 쓴다
       예) "V11-34: 화면 쿼리 21→17. 중복 집계 제거"
필수   작업 기록(outputs/)에 커밋 해시를 적는다
금지   여러 작업을 한 커밋에 섞는 것
금지   .bak 파일로 백업하는 것 — git 이 그 일을 한다
금지   carwatch.db · secrets/ 를 커밋하는 것
되돌릴 때   git revert.  git reset 은 쓰지 않는다
검산   S25  커밋되지 않은 변경이 남아 있지 않은가
```

```
★ 버전 번호
필수   소스가 바뀌면 올린다.  문서·표본만 바뀌면 올리지 않는다
필수   outputs/ 파일명의 버전은 「그 작업 시점의 소스 버전」이다
```

### ★ 작업 기록 — 08-16

```
필수   모든 작업의 처리와 결과를 outputs/ 에 남긴다
필수   파일명은 YYYYMMDD_HHMM_v버전_제목.md
필수   여섯 절을 담는다
       ① 무엇을 하려 했나 (지시 원문)
       ② 무엇을 했나 (파일 · 줄 번호)
       ③ 왜 그렇게 했나 (규격 STEP 번호)
       ④ 1차 결과 (고치기 전 숫자)
       ⑤ 조치 후 결과 (고친 뒤 숫자)
       ⑥ 못 한 것 · 막힌 것 · 판단이 필요한 것
금지   「고쳤습니다」만 적는 것.  숫자가 없으면 기록이 아니다
금지   1차 결과를 빼고 조치 후만 적는 것
필수   check_all · run_tests 의 실제 출력을 붙인다
검산   S26  작업마다 outputs/ 기록이 있는가
```

```
★ 실측으로만 보이는 것이 있다
  · 원문에 문서에 없던 필드가 오는가
  · 응답 구조가 바뀌었는가
  · 사전에 없는 값이 몇 개 나오는가
  · 등급 분포가 실제로 어떻게 나오는가
근거   v1 은 문서만 보고 만들다 344건 미분류를 냈다
      실호출을 할 수 있는 환경이면 반드시 한다.  그것이 이점이다
필수   fatal 이 실측 데이터 부재로 나는 것인지 코드 결함인지 가른다
      V4-25 · V3-30 은 사전이 비어서다 — 수집하면 사라진다
      그것을 코드로 우회하지 않는다
```


---


## ★ 지시서는 개발측이 고치지 않는다 — 08-15

```
필수   docs/ 를 고치는 것은 가이드다.  개발측은 읽기만 한다
필수   정정이 필요하면 근거와 함께 요청한다.  직접 고치지 않는다
근거   실측 08-15.  개발측 문서가 176 STEP 이었다.  정본은 205 다
      양쪽이 다른 문서를 보면 「지시서대로 했다」가 서로 다른 뜻이 된다
금지   개발측이 STEP 을 지우거나 합치는 것
금지   가이드가 소스를 직접 고치는 것.  반대도 같다
★ 문서를 받으면 diff 를 먼저 보십시오.  받은 것과 다르면 그때 알립니다
```

## ★ 규격서가 정본이다

```
필수   실물이 규격과 다르면 실물을 고친다
예외   실물이 옳다는 근거가 실측일 때만 규격을 바꾼다
      예 — 옵션 코드가 3자리다 (API 응답으로 확인)
금지   「구현이 어렵다」 · 「migrate 가 막힌다」 를 근거로 규격을 낮추는 것
      그것은 구현 사정이지 사실이 아니다
금지   선택지를 만들어 타협하는 것.  규격이 이미 정하고 있다
```

```
★ 08-14 에 실제로 3건이 그렇게 낮춰졌다
  max_form_bytes 64KB → 1MB      「실물이 1MB 라서」
  run_id NOT NULL → nullable     「migrate 가 막혀서」
  rows_per_page 200 → 300        「실물이 300 이라서」
  셋 다 되돌렸다 (개정 224~226)
```


## STEP 1 — 3티어 구조

```
Presentation   화면 · 리포트 · 진입점
               ★ 08-16 — 사람이 쓰는 것은 화면뿐이다 (STEP 136d)
Business       수집 · 파싱 · 분석 · 검증 · 채점
Data           RAW 저장소 · CORE 저장소 · 사전
```

**계층 간 호출은 한 방향이다.**

```
Presentation  →  Business  →  Data
```

| 금지 | 이유 |
|---|---|
| Presentation 이 Data 를 직접 조회 | 화면을 바꾸면 SQL 이 바뀐다 |
| Data 가 Business 를 호출 | 순환. 테스트 불가 |
| Business 안에서 화면 문자열 생성 | 언어 이식 시 전부 다시 쓴다 |

**계층 간 전달은 DTO 로만 한다.** DB Row 객체를 상위 계층으로 넘기지 않는다.

```python
# 금지
def score(row): return row["origin_price"] * 0.87

# 필수
@dataclass(frozen=True)
class ListingSnapshot:
    listing_id: str
    origin_price_won: int | None
    ...
def score(snap: ListingSnapshot) -> AxisResult: ...
```

**이유** — v1 은 352컬럼 Row 를 그대로 함수에 넘겼다.
어떤 컬럼이 판정에 쓰이는지 시그니처만 봐서는 알 수 없었고, 그래서 「저장 O · 판정 미사용」이
12건 발생해도 아무도 몰랐다. **DTO 는 사용 필드를 강제로 선언하게 만든다.**

## STEP 2 — 언어 이식성 규칙

**1차는 Python 로컬. 온라인 서비스 전환 시 Java 등으로 이식한다.**
**이식 비용을 줄이는 것이 아니라, 이식이 가능하도록 만드는 것이 목표다.**

| 규칙 | 내용 | 이유 |
|---|---|---|
| ORM 금지 | SQL 은 `sql/` 디렉터리의 `.sql` 파일 | ORM 은 언어에 종속된다 |
| 표준 라이브러리 우선 | 외부 의존은 HTTP · JSON 파서로 제한 | 대체 라이브러리를 찾기 쉽다 |
| 순수 함수 우선 | 분석·채점은 입력만으로 출력이 결정 | 언어 무관하게 그대로 옮겨진다 |
| 부작용 격리 | I/O · 시각 · 난수는 주입 | 테스트·이식 양쪽에 필요 |
| 타입 명시 | 전 함수 시그니처에 타입 | 정적 언어 이식의 전제 |
| 설정 외부화 | 배점 · 임계값 · URL 은 `config/*.json` | 코드 수정 없이 정책 변경 |
| 예외 표준화 | 도메인 예외 5종 고정 | 언어별 예외 체계 차이 흡수 |

```python
# 부작용 주입 — 시각·난수·HTTP
class Clock(Protocol):
    def now(self) -> datetime: ...
class Fetcher(Protocol):
    def get(self, url: str, headers: dict) -> Response: ...
```

**분석 계층은 `Clock` · `Fetcher` 를 모른다.** 스냅샷과 설정만 받는다.

## STEP 3 — 도메인 예외 5종

```
CollectError      수집 실패 (네트워크 · 4xx · 5xx)
FormatError       응답 형식 불일치 (라벨↔내용)
ParseError        원문 → 필드 변환 실패
ValidationError   검증 단계 위반
PolicyError       설정·정책 모순 (배점 합 불일치 등)
```

**이 5종 외의 예외를 밖으로 던지지 않는다.** 하위 예외는 감싸서 올린다.
**예외에는 반드시 `listing_id` · `endpoint` · `step` 을 담는다.** v1 은 「포기」만 찍혀 원인을 몰랐다.

## STEP 4 — 명명 규칙

### 접미사를 고정한다

**같은 뜻에 다른 접미사를 쓰면 나중에 어느 것이 정본인지 모른다.**

| 접미사 | 뜻 | 예 |
|---|---|---|
| `_won` | 원 단위 금액 | `price_current_won` · `down_payment_won` |
| `_km` | 주행거리 | `mileage_km` · `warranty_body_km` |
| `_month` · `_days` · `_years` | 기간 | `warranty_body_month` · `relist_window_days` |
| `_cnt` | 개수 | `accident_my_cnt` · `owner_change_cnt` |
| `_rate` · `_ratio` · `_pct` | 비율 | `relist_rate` · `min_denominator_ratio` |
| `_json` | 직렬화 배열·객체 | `accidents_json` · `options_choice_json` |
| `_at` | 시각 (datetime) | `fetched_at` · `decided_at` |
| `_date` | 날짜 (date) | `first_registration_date` |
| `_id` · `_key` | 식별자 | `listing_id` · `vehicle_id` |
| `_source` · `_prio` | 판정 근거 | `spec_source` · `spec_prio` |
| `_status` | 상태 | `record_status` · `row_status` |

### `_cnt` 와 `_count` — 뜻이 다르다

```
_cnt     원문이 준 집계값.  사이트가 세어서 준 것
         accident_my_cnt · owner_change_cnt · plate_change_cnt · total_loss_cnt

_count   우리가 센 값.  수집·집계 과정에서 산출
         listing_count · site_count · axis_count · unclassified_count
         view_cnt · subscribe_cnt · seizing_cnt · pledge_cnt
```

```
★ 접미사가 출처를 드러낸다
  _cnt 는 사이트 값이라 우리가 검증할 수 없다.  원문 그대로 쓴다
  _count 는 우리 산출이라 재계산으로 검증된다 (V2-01 · V1-01)
판별   매핑표에 원천 경로가 있는가
      있다  →  _cnt      seizing_cnt ← condition.seizing.seizingCount
      없다  →  _count    listing_count (우리가 센 것)
필수   새 컬럼을 만들 때 어느 쪽인지 판단해 접미사를 고른다
금지   같은 값에 둘을 섞는 것
검증   V4-18  매핑표에 원천이 있는 컬럼이 _count 로 끝나는가
```

**예외 — `facet_count`**

```
BANNED_SOURCES 의 문자열이다.  컬럼이 아니다
「facet 의 Count 를 근거로 쓰지 말라」는 뜻이라 원문 용어를 그대로 쓴다
```

```
금지   시각에 _date 를 쓰는 것              →  시각은 _at
승계   v1 의 added_date 는 added_at 으로 옮긴다 (11장 STEP 119a)
```


```
계층      collect_*   parse_*   analyze_*   score_*   validate_*   report_*
테이블    raw_*  (원문)   core_*  (정규)   dict_*  (사전)   result_*  (판정)
컬럼      {도메인}_{항목}_{단위}      예: price_current_won · warranty_body_month
근거      {axis}_source · {axis}_prio
상태      *_status  (ok / empty / not_found / error / not_requested)
```

**금지**

```
사이트 고유 명칭을 CORE 컬럼명에 쓰는 것
  금지   insp_outer_json · encar_diagnosis
  필수   inspection_panel_json · site_diagnosis_grade
```

**이유** — 2차 사이트를 붙일 때 컬럼명이 엔카 전용이면 CORE 를 쓸 수 없다.
**사이트 고유값은 `site_*` 접두 또는 어댑터 내부에만 둔다.**

## STEP 5 — 정의서 규격 · 착수 Gate

**「정의서 없는 장은 착수하지 않는다」고 선언했으나 양식이 없었다. 여기서 확정한다.**

### 5.1 스텝 정의서 양식

**모든 STEP 은 착수 전 이 양식으로 제출한다. 항목이 비면 착수 금지다.**

```
STEP 번호 · 제목
목적          한 줄.  무엇을 사실로 인정하려는가
원천          RAW 의 어느 엔드포인트 · 어느 경로
              「없음」이면 그 스텝은 성립하지 않는다
입력          타입 · 의미 · NULL 허용
출력          타입 · 의미
사용 필드      DTO 의 어느 필드.  연결표와 대조된다
근거          왜 이 로직·이 숫자인가.  출처 명시
              추정이면 「추정」이라 쓰고 검증 계획을 적는다
금지          쓰면 안 되는 입력과 이유
NULL 규칙     값 없음 · 수집 실패 · 구조적 부재를 어떻게 구분하는가
실패 규칙      예외 종류 · 재시도 여부 · 중단 조건
검산          기대 분포 · 경계값 · 실측 표본
```

### 5.1a STEP 유형 — 필수 항목이 다르다

**전 STEP 에 9항목을 요구하면 원칙 선언 스텝에서 실패가 쏟아진다.**
**그러면 검사를 끄게 된다** (V4-13 과 같은 함정).

| 유형 | 예 | 필수 항목 |
|---|---|---|
| **원칙** | 3티어 · 명명 규칙 · 금지 근거 | 목적 · 근거 · **금지** |
| **규격** | 구조체 · 계약 · 스키마 형식 | 목적 · 입력 · 출력 · 제약 |
| **판정** | 축별 분석 · 채점 | **9항목 전부** |
| **수집** | 엔드포인트 · 요청 정책 | 목적 · 원천 · 실패 규칙 · 검산 |
| **검증** | V1~V9 | 목적 · 검사 항목 · 등급 |
| **화면** | 각 화면 | 목적 · 표시 항목 · 금지 |
| **목록** | 미확정 · 승계 · 테이블 목록 | 목적 · 항목 |

```
헤딩에 유형을 표기한다   ## STEP 70 [판정] — 가격 · 기대가와 감가 곡선
검사                   유형별 필수 항목만 본다
누락                   그 STEP 을 목록으로 출력.  fatal 은 [판정] 유형에만
```

**「9항목 전부」는 판정 스텝에만 적용된다. 거기가 틀리면 점수가 틀리기 때문이다.**

### 5.2 착수 Gate — 6개 전부 통과해야 코드를 쓴다

```
[ ] 원문 근거    RAW 에서 해당 경로를 실제로 확인했다
[ ] 필드 존재    표본 300건 이상에서 출현율을 측정했다
                희귀·조건부 필드는 그 조건 모집단 전수로 확인했다
[ ] 값 종류      실제 관측된 값 전량을 나열했다
                「0건 관측」을 「존재하지 않음」으로 해석하지 않았다
[ ] NULL 규칙    3종(없음·실패·부재)이 구분돼 있다
[ ] 금지 근거    BANNED_SOURCES 와 충돌하지 않는다
[ ] 검산 기준    통과·실패를 판정할 수치가 있다
```

**「원문 근거」가 미통과인 스텝은 실측 대기로 표시하고 다음 스텝으로 넘어간다.**
**추정으로 채우지 않는다.** v1 은 감가곡선·부위 사전·`accidents[].type` 을 추정으로 채웠고
셋 다 틀렸다.

### 5.2a 표본 규모 — 필드 성격에 따라 다르다

| 필드 성격 | 표본 | 예 |
|---|---|---|
| 일반 (전건 출현) | 300건 이상 | `category.originPrice` · `spec.mileage` |
| **조건부** | **그 조건 모집단 전수** | 사고 매물의 `accidents[]` · 리스 매물의 `LeaseType` |
| **희귀값** | **전수 + 미관측 명시** | `RANK_C` (1건) · `손상` (1건) · `가솔린+LPG` (1건) |

```
금지   300건에서 안 나왔다는 이유로 「없는 값」으로 확정
필수   「표본 N건에서 미관측」이라고 쓴다.  「존재하지 않음」과 구분한다
검증   새 값이 나타나면 사전 생성이 실패한다 (3장 STEP 36)
```

**v1 실사고** — `accidents[].type` 을 표본 몇 건으로 보고 「1·3」이라 단정했다.
실제로는 `1·2·3` 세 값이었고, `2` 가 가장 흔했다. **감점 규칙이 통째로 틀렸다.**

### 5.2b 문서 자체 점검 — 부록 A

**「정의서 없는 장은 착수하지 않는다」를 선언했으므로 문서 자체를 점검한다.**

```
도구   tools/check_spec.py <문서.md>      종료코드 0 통과 · 1 실패
검사   13종.  항목과 판정 방식은 부록 A · 규격은 부록 E
착수   전 STEP 은 이 검사를 통과한 문서를 정본으로 삼는다
```

**검사를 새로 만들 때는 부록 A 머리의 「두 방향의 실패」를 먼저 읽는다.**

### 5.3 함수 정의 규격

**모든 함수는 착수 전에 아래를 문서에 먼저 적는다. 코드가 먼저 나오지 않는다.**

```
이름
목적          한 줄
입력          타입 · 의미 · NULL 허용 여부
출력          타입 · 의미
사용 필드      DTO 의 어느 필드를 읽는가        ← 연결표(1장)와 대조된다
근거          왜 이 로직인가.  숫자면 그 숫자의 출처
금지          쓰면 안 되는 입력과 이유
예외          어떤 조건에서 무엇을 던지는가
검산          기대 분포 · 경계값
```

**「근거」가 비어 있으면 그 함수는 착수 금지다.**
v1 의 감가곡선 87/69/55% 는 출처가 없었고, 가격 200점 전체가 그 위에 얹혀 있었다.

### 5.4 RAW → 축 추적 사슬

**2장 이후 모든 필드는 이 사슬이 끊기지 않아야 한다.**

```
RAW 원문 경로
  ↓  실제 응답에서 확인 (표본 300건+)
관측 값 종류 · 출현율
  ↓  변환 명시 (×10,000 · ym · date10 · json)
CORE 필드
  ↓  연결표 (어느 파일이 읽는가)
사용 축   또는   사용 불가 사유
```

**사슬이 끊긴 필드는 CORE 에 넣지 않는다.**
**개발자가 API 를 보고 임의로 필드를 추가하거나 값을 추론해 채우는 것을 금지한다.**

## STEP 5.5 — 수치의 자리 ★ 신설

**새 프로그램은 빈 DB 로 시작한다. 이 문서의 관측 수치는 그 시점에 하나도 재현되지 않는다.**
**그런데 규칙을 만들려면 근거가 필요하다. 그래서 수치를 세 갈래로 나눈다.**

| 구분 | 성격 | 새 프로그램에서 | 자리 |
|---|---|---|---|
| **사양** | 열거값 · 필드 존재 · 구조 | **그대로 성립한다** | **본문** |
| **관측** | 건수 · 비율 · 확보율 · 분포 | **재현되지 않는다** | **적지 않는다** |
| **경계** | 임계값 · 범위 | 관측에서 유도됐다 | **유도식만 본문 · 값은 `config`** |

### 판별 질문

```
그 수치가 없으면 규칙을 못 만드는가        →  사양.  본문에 남긴다
아니면 규칙이 맞았는지 확인만 하는가        →  관측.  적지 않는다
```

### ★ 관측치를 부록으로 빼지 않는다 — 아예 적지 않는다

```
검토했던 방식   부록에 관측치를 모으고 본문은 참조만 한다
문제           「부록 수치가 맞는지 어떻게 확인하나」가 따라붙는다
              스냅샷 식별자 · 대조 절차 · 검사 규칙이 줄줄이 붙는다
              그런데 그 수치가 맞든 틀리든 판정식 · 배점 · 스키마는 바뀌지 않는다

예   record 정상 원문이 27건이든 42건이든
    결론은 「이력 원문은 재수집이 선행된다」 하나뿐이다
    숫자를 맞춰봐야 결론이 같다.  대조할 이유가 없다
```

**근거가 필요하면 v1 DB 를 직접 열면 된다. 문서가 그것을 대신하지 않는다.**

### 예

```
사양   LeaseType 은 4값이다 (렌트승계·운용리스·장기렌트·금융리스)
      usageChangeTypes 는 record 가 아니라 점검부 master.detail 에 있다
      축 식별 키는 (Name, Type='Aspect') 다
      → 이걸 지우면 다음 사람이 다시 추측한다.  v1 이 무너진 지점이다

관측   매물 건수 · record 오염률 · 확보율 · 잔가율
      → 규칙이 맞았는지 확인한 근거일 뿐이다.  적지 않는다

경계   「보정계수 1.2 초과면 의심」  →  config.depreciation.coefficient_sane_range
      「최소 분모 60%」            →  config.scoring.min_denominator_ratio
      → 본문에는 유도식과 config 키 이름만 쓴다
```

### ★ 예외 — v1 사고 기록은 본문에 남긴다

```
「휀더/펜더로 344건 미분류」
「dealer_shop 에 지역이 들어감」
「판매글 키워드가 실장착을 이겨 선루프 84건 오판」
```

**이것은 관측치지만 금지 조항의 이유다. 이유를 지우면 다음 사람이 금지를 풀어버린다.**

### ★ 관측치 표를 문서에 두지 않는 이유

```
v1 사고   작업지시서 부록에 카탈로그 34건 표를 넣었다
         개발자가 그 표를 옵션 판정 근거로 썼다
         표에 없는 옵션은 판정이 안 됐고, 표의 코드가 다른 차종에 적용됐다
         부록이 스펙이 됐다
```

**표가 있으면 읽힌다. 「참고용」이라 적어도 읽힌다.**

```
필수   이 문서만으로 구현할 수 있어야 한다
      v1 DB 를 봐야 구현되면 본문이 부실한 것이다
검증   V4-13  매직 넘버 금지.  임계값은 config 참조여야 한다
```

### 관측치의 진짜 자리

```
audit_validation      실행마다 새로 생긴다.  날짜 · 표본 · 쿼리가 함께 남는다
coefficient_history   계수 변동 이력
meta_field_usage      필드 사용 구분
watch_track           관심 매물 시계열
```

**문서에 숫자를 흩뿌리지 않는다. 테이블이 정본이다.**
**문서에 숫자를 흩뿌리지 않는다. 실행 결과가 정본이다.**

## STEP 6 — 설정과 코드의 분리

```
config/
  sites.json         사이트 목록 · status (active · planned · paused)
  endpoints.json     URL · 헤더 · 타임아웃 · 재시도 · 요청 간격
  targets.json       차종 정의 · 분류 조건
  scoring.json       축 · 배점 · 등급컷 · 분모 최소치
  depreciation.json  감가 곡선 · 차종별 보정계수     ← 첫 수집 후 생성
  finance.json       선납 · 할부 개월 · 이율 · 세율 · 부대비용
  warnings.json      경고 임계값
  dealer_trust.json  딜러 지표 가중치
  labels.json        화면 문구 (AXIS · GRADE · VALUE)
  dictionaries/      코드·열거값 사전               ← RAW 에서 생성
sql/
  ddl/               테이블 정의                    ← 정본
  queries/           조회
```

### config 키 전량 — 파일별

**본문에 등장하는 키는 전부 여기 있어야 한다. 없으면 그 키는 오타이거나 미정의다.**

| 파일 | 키 | 용도 |
|---|---|---|
| `scoring.json` | `total_points` | 총점 |
| | `components` | Component 별 배점 |
| | `grade_cuts` | 등급컷 비율 |
| | `min_denominator_ratio` | 분모 최소 비율 |
| | `price_curve` | 가격 구간 배점 |
| | `peer_year_window` · `peer_mileage_window` · `peer_min_sample` | 유사군 (7장 82e) |
| | **`axis_rules`** | **축별 임계·상수 (7장)** — 아래 |
| | ↳ `warranty.full_months` · `expire_penalty` · `km_per_month` | 보증 (STEP 72) |
| | ↳ `history.damage_by_swap` · `damage_by_status` · `insurance_cost_curve` · `insurance_cap_by_count` · `rental` | 이력 (STEP 76~78) |
| | ↳ `safety.warranty_product_na_targets` | 안전 (STEP 79) |
| | ↳ `color.grade_points` · `gate_open` | 색상 (STEP 80) |
| | ↳ `mileage.full_km` · `zero_km` | 주행 (STEP 81) |
| | ↳ `absolute_fail.repair_cost_ratio` · `frame_ranks` · `outer_ranks` | E등급 (STEP 82) |
| | **`validation`** | **검증 임계 (6장)** — 아래 |
| | ↳ `sample_limit` | 위반 사례 보존 수 (STEP 66) |
| | ↳ `price_median_min_won` | V2-05 단위 검사 |
| | ↳ `mapping_ratio` (A·B·C) | V4-01 등급별 일치율 |
| | ↳ `grade_dominant_ratio` | V5-05 쏠림 경고 |
| | ↳ `shuffle_sample` | V3-11 표본 수 |
| | ↳ `min_value_kinds` | V3-03·04 변별력 |
| | ↳ `day_gap_total_limit` · `day_gap_new_ratio` | V2 전일 GAP (STEP 57) |
| | **`report`** | 리포트 (9장) |
| | ↳ `top_n` | L2 상위 N건 |
| | ↳ `market_quantiles` | `/market` 분위수 (STEP 100) |
| | **`admin`** | 관리자 (13장) |
| | ↳ `query_row_limit` · `query_timeout_sec` | 조회 쿼리 제한 (STEP 133) |
| | ↳ `schedule` | 자동 실행 주기 (STEP 132) |
| | ↳ `session_hours` | 세션 유효 시간 (STEP 126) |
| | ↳ `hash_rounds` | 비밀번호 해시 라운드 (STEP 126) |
| | `components.{key}.skipped` | 성분 스킵 (13장 STEP 128). `{"points": N, "skipped": true}` |
| `depreciation.json` | `curve` | 경과년 → 잔가율 |
| | `coefficient` | 차종별 보정계수 |
| | `coefficient_change_limit` | 계수 변동 상한 |
| | `coefficient_min_sample` | 보정 최소 표본 |
| | `coefficient_max_iter` | 수렴 반복 상한 |
| | `coefficient_sane_range` | 계수 정상 범위 |
| | `curve_min_sample` | **곡선 산출 최소 표본.** 미달이면 곡선 미산출 (STEP 70) |
| `finance.json` | `down_payment_won` | 선납금 (취득 부대비용 포함) |
| | `loan_months` · `loan_rate_annual` · `repay_method` | 할부 조건 |
| | `tax_acquisition_rate` | 취득세율 |
| | `bond_table` | 공채 |
| | `fee_stamp` · `fee_transfer` · `fee_delivery` · `fee_tinting` | 부대비용 |
| | `ev_tax_exempt` | 전기차 감면 대상 차종 |
| | `hold_months` | 총소유비용 산정 기간 |
| `warnings.json` | `price_anomaly_sigma` · `price_low_pct` | 저가 경고 |
| | `relist_count` · `relist_window_days` | 재등록 경고 |
| | `dealer_trust_floor` | 딜러 신뢰도 하한 |
| `field_usage.json` | `seed` | `endpoint:json_path` → `usage` · `reason` · `unblock_condition` |
| | `default` | 시드에 없는 경로의 초기 `usage` (= `unclassified`) |
| | `ghost_miss_limit` | 유령 경로 전환 기준 (연속 미관측 횟수) |
| `field_usage.suggested.json` | (생성물) | `sync_registry` 가 낸 분류 후보. 사람이 검토 후 정본으로 옮긴다 |
| `dealer_trust.json` | `weights` | 지표별 가중치 |
| | `min_sample` | 표본 충분 기준 |
| `web.json` | `host` · `port` | 바인딩 (14장 STEP 141) |
| | `session_cookie` · `static_max_age_sec` · `flash_max` | 화면 |
| | `signup_policy` | 가입 정책 open·approval·closed (14장 STEP 149n) |
| | `max_queries_per_request` | 요청당 쿼리 상한 기본 20 (`V11-34`) |
| `checks.json` | `spec_glob` · `ref_glob` · `chapters` · `self_exclude` · `screens_dir` | 검사 대상 (부록 E) |
| `admin.json` | `login_fail_limit` | 연속 실패 상한 기본 10 (13장 · `V10-20`) |
| | `login_lock_sec` | 잠금 시간 기본 300 |
| | `min_secret_length` | 비밀번호 최소 길이 기본 8 |
| `scoring.json` | `validation.min_salvaged_fields` | 파싱 구제 후 최소 필드 수 기본 2 (`V2-28`) |
| | `validation.conflict_warn` | 축 충돌 경고선 기본 10 (`V3-36`) |
| `endpoints.json` | `interval_sec` · `timeout_sec` · `retry_max` | 요청 정책 |
| | `fail_streak_limit` | **연속 실패 중단 기준** (5장 STEP 52) |
| | `base_url` · `paths` · `headers` | 엔드포인트 (2장 STEP 17) |
| `sites.json` | `{site_code}` → `label` · `status` · `order` · `multi_instance` | 사이트 목록 (8장 STEP 89) |
| `targets.json` | `{target_key}` → `label` · `site_query` · `fuel_match` · `trim_include` · `trim_exclude` | 차종 정의 |
| | `SPEC_DEFAULT_ON` · `SPEC_DEFAULT_OFF` | 차종별 기본 장착 (7장 STEP 74) |
| `known_issues.json` | `{target_key}` → `[{level, text}]` | 차종별 알려진 결함 3단계 (14장 STEP 149c). **사람이 넣는다** |
| `labels.json` | `AXIS_LABELS` · `GRADE_LABELS` · `VALUE_LABELS` · `STATUS_LABELS` | 화면 문구 (10장 STEP 105) |
| `dictionaries/` | `tint_keywords` 외 | **생성물.** RAW 에서 만든다 (4장 STEP 42) |
| `dictionaries/fixed_enums.json` | `panel_rank` · `panel_status` · `accident_type` · `condition_flag` | **`halt` 축 고정 집합.** 생성물이 아니다 (4장 STEP 41) |

### config 파일 예시 — 부록 B

**전 12종의 형태·중첩·초기값이 부록 B 에 있다.**

```
필수   새 config 키를 만들면 부록 B 예시도 함께 고친다
      check_spec ⑨ 가 본문 표와 부록 예시를 대조한다
정본   값이 갈리면 본문 STEP 표를 따른다.  예시는 발췌다
```

### 본문 참조 형식

```
필수   config.{파일}.{키}       config.scoring.min_denominator_ratio
      또는 `config/{파일}.json` 의 `{키}`
금지   config.{키}             어느 파일인지 알 수 없다
```

**표의 「파일」 열이 빈 행은 바로 위 행과 같은 파일이다.**

```
검증   V4-14  본문의 config 키가 이 표에 전건 있는가
      V4-15  이 표의 키가 실제 config 파일에 있는가
      V4-16  본문 참조가 config.{파일}.{키} 형식인가
```

**배점을 바꾸는 데 코드를 고치면 안 된다.** 정책 변경과 로직 변경을 분리한다.
**사전은 손으로 적지 않는다.** `tools/build_dict.py` 가 RAW 에서 생성하고, 변경분은 리뷰한다.

### 6.1 무엇을 빼고 무엇을 남기는가 — 경계

**「하드코딩 제거 = 전부 JSON」이 아니다.** JSON 을 해석하는 코드가 또 하나의 하드코딩이 된다.

| 종류 | 위치 | 판별 기준 | 예 |
|---|---|---|---|
| **정책값** | `config/*.json` | 마스터가 바꿀 수 있는 숫자 | 배점 · 등급컷 · 임계값 · 요청 간격 |
| **사이트 설정** | `adapters/{site}/config` | 사이트 바뀌면 달라지는 것 | URL · 헤더 · 쿼리 문법 · `required_keys` |
| **도메인 규칙** | **코드** | 바뀌면 로직이 바뀌는 것 | 우선순위 4단 · `-1` 의 의미 · 분모 축소 |
| **데이터 사전** | `config/dictionaries/` **생성물** | RAW 에서 뽑는 것 | 부위명 · 옵션명 · 연료 · 색상 · `RANK_*` |

```
판별 질문   이 값을 바꾸면 「정책이 바뀌는가」 「로직이 바뀌는가」
정책        config
로직        코드.  JSON 으로 빼지 않는다
```

**금지** — 조건식·분기·우선순위를 JSON 에 넣는 것.

```json
// 금지.  이것은 설정이 아니라 코드다
{"rule": "if fuel in ['가솔린+전기'] and displacement == 1499 then include"}
```

**v1 의 실제 문제는 config 부족이 아니었다.** `SPEC_DEFAULT_ON` 같은 도메인 규칙이
코드 다섯 군데에 흩어져 서로 덮어쓴 것이다. **흩어짐이 문제지 위치가 문제가 아니었다.**

## STEP 6a — 실물 표본 (fixtures) ★

**모의 응답으로만 시험하면 「내가 만든 구조」를 검증하게 된다.**
**v1 사고는 전부 「원문이 내 예상과 달랐다」에서 나왔다.**

```
위치   tests/fixtures/
동봉   BJY_v2_실물표본.tar.gz — v1 원문에서 추출한 12건
구성   응답 원문 12 + EXPECTED.json + NOTES.json
```

### 표본 12건 — 부록 C

**어느 표본이 무엇을 고정하는지, `EXPECTED.json` 사용법, 표본을 늘리는 기준이 부록 C 에 있다.**

```
필수   새 표본은 v1 원문 또는 실수집 원문에서 뽑는다.  손으로 만들지 않는다
금지   모의 응답을 fixtures 에 넣는 것
      모의는 tests/ 안에서 경계 조건용으로만 쓴다
필수   변환 결과는 EXPECTED 의 _core 값과 직접 비교한다
      시험이 변환 로직을 다시 구현하면 둘 다 틀려도 통과한다
```

---

## STEP 7 — 테스트 계층

```
단위     순수 함수 (분석 · 채점).  입력→출력만
계약     어댑터가 인터페이스를 지키는가
회귀     RAW 고정 표본 → 판정 결과 스냅샷 비교
불변식   아래 6개는 항상 참이어야 한다
```

**불변식 6개**

```
① put() 호출 순서를 뒤섞어도 결과가 같다
② 금지 근거(카탈로그 전체목록 · facet Count)가 판정에 들어가면 실패
③ 전 축의 {axis}_source 가 전건 NULL 이면 실패
④ 원문 라벨과 내용 형식이 어긋나면 저장 거부
⑤ 배점 합계가 config 의 총점과 일치
⑥ 사전 미분류 0건
```

```
★ 불변식은 6개가 다 돌아야 한다 — 08-14
실측   test_invariants.py 가 ①②⑤ 만 구현했다
      「③④⑥ 은 2~4장 착수 후 추가한다」고 적어 두고 착수 후에 안 넣었다
필수   장을 착수하면 그 장이 여는 불변식을 그 자리에서 넣는다
금지   「나중에」.  시험이 「전부 통과」로 나오는데 절반만 본다
검산   S21  불변식 6개가 시험에 전건 있는가
```

### 7.1 분모 시험 6종 — 필수

**「값 없음 · 수집 실패 · 구조적 부재」가 섞이면 점수가 부풀려진다.**
**분모가 줄어들수록 같은 획득 점수가 높은 비율이 되기 때문이다.**

| # | 상황 | 기대 |
|:--:|---|---|
| A | 전 축 정상 | 정상 점수 |
| B | 한 축 수집 실패 | 그 축 분모 제외 · 점수 산출 |
| C | 한 축 구조적 부재(`-1`) | 그 축 분모 제외 · 점수 산출 |
| D | **전 축 수집 실패** | **점수 생성 금지** |
| E | **금지 근거만 존재** | **점수 생성 금지** |
| F | **분모가 최소 기준 미만** | **등급 생성 금지** |

```
최소 분모   config.scoring.min_denominator_ratio
근거       분모의 절반이 빠진 매물과 전부 있는 매물을 같은 등급표로 비교할 수 없다
          초기값은 config 에 두고, 첫 수집 결과를 보고 조정한다
표시       분모 미달 매물은 '평가 불가' 로 표시한다.  낮은 등급이 아니다
```

**D·E·F 가 핵심이다.** 「모든 축이 빠졌는데 평균 점수가 높게 나오는 것」을 반드시 차단한다.
v1 은 사양 축에서 이것이 실제로 일어났다 — `[]` 가 `NULL` 로 저장돼 분모에서 빠졌다.

```
★★ 08-16 개정 289 로 이 절이 바뀐다 — B · C · F 폐기
개발측 지적으로 충돌이 드러났다.  가이드 잘못이다

폐기   B  한 축 수집 실패  → 그 축 분모 제외
폐기   C  구조적 부재      → 그 축 분모 제외
폐기   F  분모 최소 기준 미만 → 등급 생성 금지
폐기   config.scoring.min_denominator_ratio

근거   마스터 지적 — 「못 찾은 것은 니 사정이지.  왜 평가를 왜곡하니」
      분모를 줄이면 못 찾을수록 비율이 올라간다
      이 절은 「분모가 줄면 부풀려진다」를 알면서도
      「그러니 줄이되 최소치를 두자」로 갔다.  그것이 절반만 맞았다

남는다  A  전 축 정상          정상 점수
       D  전 축 수집 실패      점수 생성 금지
       E  금지 근거만 존재      점수 생성 금지
새로   G  분모는 늘 만점이다     ★ 5장-2a 총점.  예외 없다
       H  못 본 축은 0점       사유(missing · no_mention · expected_unavailable)를 화면에
       I  확인율을 함께 낸다    「만점 중 573점을 확인했습니다 (63%)」
       ★ 「등급 생성 금지」는 확인율로 판단한다.  분모로 하지 않는다
검산   V3-41  전 매물의 분모가 만점과 같은가
```


## STEP 8 — 온라인 전환 대비

**1차는 로컬 배치다. 그러나 아래를 지키면 서비스 전환이 재작성이 아니라 배치 교체가 된다.**

| 항목 | 1차 (로컬) | 전환 시 |
|---|---|---|
| 실행 | 화면 · 타이머 | 스케줄러 · 큐 |
| 저장소 | SQLite | PostgreSQL 등 |
| 상태 | 파일 | 외부 상태 저장소 |
| 동시성 | 단일 프로세스 | 워커 다중화 |

```
필수   ① SQL 은 표준 문법 우선.  SQLite 고유 문법 최소화
      ② 모든 배치는 멱등.  같은 입력에 같은 결과
      ③ 처리 단위에 진행 상태를 남긴다 (재개 가능)
      ④ 전역 가변 상태 금지        ← v1 의 last_raw 사고 원인
      ⑤ 시각은 주입.  now() 직접 호출 금지
```

**④는 v1 에서 실제로 터진 것이다.** `last_raw` 라는 공유 변수 하나가 원문 2,183건을
잘못된 라벨로 저장했고, 이력 축이 통째로 죽었다. **워커를 늘리면 같은 사고가 확률적으로 재현된다.**

---



---

## ★★★ 가이드 작업 규칙 — S28 · 08-17

**마스터 지적 — 「너도 이런 대충하는데」**

**가이드가 규격을 만드는 방식에도 규칙이 필요하다. 어기면 검사가 잡는다.**

### 규칙

```
필수   새 규격은 표로 준다.  말로 주지 않는다
       ★ 배점은 표로 주면서 화면은 말로 줬다 (개정 332 에서 고침)
       ★ 「자리 · 크기 · 구간 · 점수」가 있어야 구현이 갈리지 않는다

필수   같은 주제는 정본 한 파일에만 적는다
       배점   ref/F-scoring.md
       화면   ref/G-screens.md
       검사   ref/A-check.md · CHECKS.md
       config ref/B-config.md
       ★ 본문에서는 파일 이름으로 가리킨다.  값을 다시 적지 않는다

필수   규격을 바꾸면 앞의 것을 폐기 표시한다
       본문에   「~~제목~~ ★ 폐기 — 개정 N」
       버전에   guide/00_버전.md 의 폐기 표에 한 줄
       이력에   「개정 N 을 폐기한다」
       ★ 조용히 덮어쓰지 않는다

필수   매 검토마다 스크린샷을 받아 부록 F·G 로 항목 대조한다
       ★ 「좋아 보인다」 금지.  「G 의 몇 번 위반」으로만 적는다
       ★ 스크린샷을 안 봤으면 검토가 아니다

필수   숫자를 말할 때 근거를 함께 적는다
       파일 · 줄 또는 실측 명령
       ★ 짐작을 사실처럼 적지 않는다

필수   개정마다 이력에 한 줄 — 날짜 · 장 · 무엇 · 근거
필수   버전을 갱신한다 — guide/00_버전.md
```

### 검사 — `tools/check_spec.py` 에 넣는다

```
S28-1   같은 배점 숫자가 정본 밖에 있는가
        ★ 총점·축 점수가 f-table.md 밖에 나오면 실패
S28-2   화면 크기·줄 수가 G-screens.md 밖에 있는가
S28-3   「폐기 — 개정 N」 표시와 00_버전.md 폐기 표가 일치하는가
S28-4   이력의 마지막 개정 번호와 00_버전.md 버전이 같은가
S28-5   본문이 참조하는 개정 번호가 이력에 실재하는가
S28-6   개정 번호가 건너뛰거나 겹치지 않는가
S28-7   정본 파일이 서로 모순되지 않는가
        ★ F 의 축 목록과 CHECKS.md 의 검사가 이어지는가
```

```
필수   ★ 이 검사는 개발측이 만들고 개발측이 돌린다
       근거   가이드가 자기를 검사하면 같은 맹점을 넘긴다
             배점표를 세 군데 만든 것이 가이드다 (개정 331)
필수   check_all 에 넣는다.  따로 돌리지 않는다
필수   실패하면 가이드에게 알린다 — outputs 기록에
       ★ 개발측이 고치지 않는다 (규칙 2).  가이드가 고친다
검산   S28  가이드 작업 규칙이 지켜지는가
```

```
★ 왜 개발측이 검사하나
  가이드는 지시서를 고칠 수 있고 개발측은 못 고친다 (규칙 2)
  그러니 「고치는 쪽」과 「검사하는 쪽」이 갈린다
  ★ 그것이 검사의 뜻이다
```


---

## ★★★ 정기 점검 — S29 · 08-17

**마스터 지시 — 「작업이 끝나거나 하루 마무리 시점에 일제 점검하게.**
**금요일 새벽 2시부터 너희들이 서로 일괄로 점검하게」**

### 네 주기

| 주기 | 언제 | 누가 | 무엇 | 시간 |
|---|---|---|---|---|
| **가벼운 점검** | **4시간마다** | 개발측 | 깨진 것만 | **1분 안** |
| **작업 점검** | 작업이 끝날 때 | 개발측 | 그 작업이 규격대로인가 | — |
| **일일 점검** | 매일 23:00 | 개발측 | 오늘 것 전부 · 숫자 | 몇 분 |
| **주간 일제 점검** | **금요일 02:00** | **서로** | 전수 · 규격 ↔ 구현 | 오래 |

```
★ 마스터 지시 08-17 — 「4시간 간격으로 하자」
★ 무거운 것을 자주 돌리면 서버가 그것만 한다.  갈라서 돈다
```

---

### ⓪ 가벼운 점검 — 4시간마다 · 02·06·10·14·18·22시

```
필수   1분 안에 끝나는 것만 한다
       check_all · run_tests
       화면 29개 렌더 — 「—」 · 계산식 · 템플릿 문법 개수
       DB 행 수 — raw_response · core_listing · result_score
필수   ★ 어긋난 것이 있을 때만 기록한다
       ★ 늘 기록하면 하루 6개가 쌓여 아무도 안 본다
필수   무엇이 늘고 줄었는지 낸다
       「fatal 4 → 6」 · 「「—」 21 → 41」
       ★ 이것이 신호다.  숫자 자체보다 변화가 중요하다
필수   /admin/status 에 마지막 시각과 결과를 낸다
       「마지막 점검 10:00 · fatal 4 · 「—」 41 · 미해결 12」
필수   수집이 도는 중이면 건너뛴다
       ★ 원문이 들어오는 중에 세면 숫자가 흔들린다
금지   재시작하는 것 (개정 308)
금지   고치는 것 — 재는 것만 한다
       ★ 고치면 마스터의 작업을 끊는다

★★ 08-18 정정 — 재기만 하면 아무도 안 고친다

```
마스터 지적   「4시간마다 돌라고 했으면 잔여 작업이 없어야 하는데
             클로드 코드는 작업하지 않는다」
★ 점검은 도는데 고치는 사람이 없었다.  가이드 설계 문제다
```

```
필수   점검이 fatal 을 찾으면 그 자리에서 고친다
       ★ 「재기만 한다」는 폐기한다
필수   다만 고쳐도 되는 것과 물어야 하는 것을 가른다
       고친다      화면 깨짐 · 검사 실패 · 「—」 · 계산식 노출
                 ★ 되돌릴 수 있고 규격이 정해져 있는 것
       묻는다      규격이 없는 것 · DB 를 지우는 것 · 배점을 바꾸는 것
                 ★ outputs 에 적고 다음 점검까지 기다린다
필수   고친 것을 기록에 남긴다 — 무엇을 왜
필수   수집이 도는 중이면 고치지 않는다.  다음 차례로 미룬다
       ★ 재시작이 마스터의 작업을 끊는다 (개정 308)
★★ 08-18 마스터 확정 — 상한 없이 전부

필수 [마스터]   찾은 fatal 을 전부 고친다.  상한을 두지 않는다
필수 [기술]     다만 하나씩 고치고 하나씩 커밋한다
                ★ 무엇이 깨졌는지 알 수 있게
필수 [기술]     고치다 새 fatal 이 나면 멈추고 기록한다
검산   S29-4  점검이 찾은 fatal 이 다음 점검까지 남아 있지 않은가
```
검산   S29-0  4시간 점검이 하루 6번 도는가
```

```
★ 왜 4시간인가
  1시간이면 하루 24번이다.  기록이 쌓여 안 보게 된다
  하루 한 번이면 아침에 깨진 것을 저녁에야 안다
  ★ 4시간이면 밤에 깨져도 아침에 안다
```

---

### ① 작업 점검 — 작업이 끝날 때

```
필수   check_all 을 돌린다.  fatal 이 늘었으면 고치고 끝낸다
필수   화면을 고쳤으면 스크린샷을 다시 찍고 부록 G 로 대조한다
       ★ 「G 의 몇 번을 고쳤고 몇 번이 남았다」로 적는다
필수   배점을 고쳤으면 표본 3건을 손으로 계산해 대조한다
필수   outputs 기록에 남긴다 — 규칙 6
필수   ★ 작업 기록에 지시서 버전을 적는다 — SPEC-2026.08.17-r333
금지   「고쳤습니다」만 적는 것.  숫자를 적는다
```

---

### ② 일일 점검 — 매일 23:00 · 자동

```
필수   check_all 전수 · fatal 목록
필수   run_tests 전수
필수   숫자 요약
       수집   목록 · 상세 · 새 엔드포인트 · 실패
       판정   건수 · 등급 분포 · 확인율 분포
       사전   pending 몇 · confirmed 몇
       화면   전 화면 렌더 · 응답 시간 · 크기
필수   어제와 견준다.  늘고 준 것을 낸다
       ★ 「fatal 4 → 6」 같은 것이 신호다
필수   미해결 목록 — inbox 미처리 · 검사 실패 · 마스터 지적 중 안 된 것
필수   outputs/daily/YYYY-MM-DD.md 로 남긴다
필수   /admin/status 에 마지막 일일 점검 시각을 낸다
```

---

### ③ 주간 일제 점검 — 금요일 02:00 · ★ 서로 점검

```
★ 「서로」가 핵심이다.  각자 자기를 검사하면 같은 맹점을 넘긴다
```

#### 개발측 → 가이드 (규격을 검사한다)

```
S28-1~7   정본 밖 숫자 · 폐기 표시 · 개정 번호 · 정본 모순
필수      규격끼리 충돌하는 곳을 목록으로
필수      본문이 참조하는 STEP·검사 번호가 실재하는가
필수      「미확정」으로 남은 것 목록 — 몇 주째인지 함께
          ★ 두 주 넘으면 그것도 결함이다
필수      결과를 outputs/weekly/ 에 쓴다
★ 고치지 않는다.  가이드에게 넘긴다 (규칙 2)
```

#### 가이드 → 개발측 (구현을 검사한다)

```
필수   전 화면 스크린샷을 받아 부록 G 로 항목 대조
       ★ 「좋아 보인다」 금지.  「G 의 몇 번 위반」으로
필수   부록 F 로 배점 대조 — 축 수 · 배점 합 · 구간 경계
필수   v1 대조 (V11-68 · V11-69) 결과 확인
필수   지난 주 지적 중 안 고쳐진 것 목록
필수   검사 227개 중 자료 없어 못 도는 것 목록
       ★ 「자료 없음」이 오래가면 수집을 고쳐야 한다는 뜻이다
```

#### 함께

```
필수   ★ 마스터 지적 대장을 훑는다
       언제 무엇을 지적했고 지금 어떤가
       ★ 두 주 넘게 안 고쳐진 것은 이유를 적는다
필수   폐기 후보 — 안 쓰는 규격 · 안 도는 검사 · 안 쓰는 config
필수   다음 주 우선순위 세 개를 정한다
필수   결과를 한 파일로 — outputs/weekly/YYYY-MM-DD_주간점검.md
필수   /admin 에 링크를 낸다.  마스터가 아침에 본다
```

```
필수   systemd timer 로 돈다.  02:00 에 개발측 몫이 먼저 돈다
       ★ 04:00 수집(STEP 136h)보다 앞이다.  결과를 보고 수집한다
필수   타이머가 서비스를 재시작하지 않는다 (개정 308)
검산   S29-1  일일 점검이 매일 남는가
       S29-2  주간 점검이 금요일마다 남는가
       S29-3  지난 주 지적 중 안 고쳐진 것이 목록에 있는가
```

```
★ 왜 금요일 새벽인가
  주말 전에 무엇이 밀렸는지 본다
  02:00 은 사람이 안 쓸 때다 — 마스터의 화면을 끊지 않는다
```


---

## ★★★ 문서를 어디에 두나 — 08-18

**마스터 지적 — 「부록은 검사·체크 로직을 넣고 기준은 본문에 넣는데**
**또 부록을 잔뜩 늘리는 중이지? 원문이랑 부록이랑 이격이 나잖아」**

```
★ 가이드가 자기 원칙을 어겼다.  부록 F(배점) · 부록 G(화면)를 만들었다
★ 그 결과 본문(61-web.md)과 부록 G 가 같은 것을 두 번 적었다
```

### 어디에 무엇을

```
본문 chapters/    ★ 기준 · 규격 · STEP · 표
                 배점표 · 화면 배치 · 절 순서 — 전부 본문이다
부록 ref/         ★ 참조표만
                 A 검사 목록 · B config 키 · C 표본 · D 갱신 · E 원문 구조
                 ★ 「본문을 읽다가 찾아보는 것」만 부록이다
guide/           개요 · 시작 · 이력 · 버전 · 구성
```

```
필수   기준을 부록에 두지 않는다
필수   부록에는 목록·표만 둔다.  「왜」를 적지 않는다
       ★ 「왜」는 본문에 있다
금지   본문이 크다고 부록으로 옮기는 것
검산   S28-8  부록에 「필수·금지·근거」가 있는가
       ★ 있으면 그것은 기준이다.  본문으로 옮겨야 한다
```

### 파일이 크면 쪼갠다

```
필수   한 파일이 800줄을 넘으면 폴더로 쪼갠다
       chapters/61-web.md  →  chapters/61-web/a-common.md · b-list.md · …
       ★ 이미 그렇게 한 장이 있다 — 10-collect · 11-store · 20-verify · 30-score · 60-admin
필수   쪼갤 때 이름은 「a- b- c-」 순서로.  읽는 차례가 그것이다
필수   원래 파일에는 색인만 남긴다
금지   ★ 파일이 크다고 부록으로 옮기는 것
       ★ 그러면 본문과 부록이 같은 것을 두 번 적게 된다
검산   S28-9  800줄 넘는 본문 파일이 있는가
```

```
★ 08-18 에 옮긴 것
  부록 F 배점표   →  chapters/30-score/f-table.md
  부록 G 화면     →  chapters/61-web/  (7파일로 쪼갬)
  ★ 부록 F·G 는 폐기.  안내만 남겼다
```


---

## ★★ 색인 넷을 기계로 갱신한다 — 08-18

**마스터 물음 — 「가이드의 인덱스, 그리고 코드와 DB는 인덱스 정리 되었니?」**

```
실측 08-18
  docs/INDEX.md    ★ 새 파일(61-web/ · f-table)이 없었다 — 손으로 안 고쳤다
  docs/SOURCE.md   ★ 61-web/ 을 모른다
  docs/CHECKS.md   ★ 검사 표가 비어 있다
  docs/SCHEMA.md   ★ 없었다 — DB 표 43개를 아무도 안 세었다
```

```
필수   색인 넷을 기계로 만든다.  손으로 적지 않는다
       INDEX.md    지시서 파일 목록 · 줄 수 · 800줄 넘는 것 표시
       SOURCE.md   소스 파일 · 줄 수 · 무엇이 어디에
       CHECKS.md   검사 코드 → 규격 위치 · 소스 파일
       SCHEMA.md   DB 표 · 열 수 · 무엇
필수   파일이 늘거나 줄면 색인이 함께 바뀐다
       ★ 4시간 점검이 갱신한다 (S29-0)
필수   색인과 실제가 다르면 실패다
검산   S28-10  SCHEMA.md 의 표가 DDL 과 같은가
       S28-11  INDEX.md 의 파일이 실제와 같은가
       S28-12  SOURCE.md 의 파일이 실제와 같은가
       S28-13  CHECKS.md 의 검사가 코드와 같은가
```

```
★ 왜 기계로 하나
  손으로 적으면 오늘처럼 빠진다
  ★ 내가 부록 F·G 를 본문으로 옮기고도 INDEX 를 안 고쳤다
```


---

## ★★★ 검사를 정리한다 — S30 · 08-18

**마스터 물음 — 「검사가 너무 많은데 필요한 것과 중복되는 것, 불필요한 것 정리가 가능할까」**

```
★ 지금은 셀 수가 없다.  docs/CHECKS.md 가 비어 있다
★ 몇 개인지 · 어디 있는지 · 규격과 맞는지 아무도 모른다
```

### 먼저 `CHECKS.md` 를 기계로 만든다

```
필수   validate/ 를 훑어 Check(...) 를 뽑는다
필수   docs/ 를 훑어 「검산 V?-nn」 · 「S nn」 을 뽑는다
필수   둘을 잇는다
       | 코드 | 무엇 | 규격 위치 | 소스 파일 | 등급 | 마지막 통과 | 마지막 실패 |
필수   ★ 마지막 통과·실패 시각을 함께 낸다
       ★ 한 번도 안 돈 검사가 드러난다
검산   S28-13  CHECKS.md 의 검사가 코드와 같은가
```

### 다섯 갈래로 가른다

```
① 중복        같은 것을 두 검사가 본다
              예 — V11-78(세로 떨어짐) · V11-109(칸 겹침)
② 죽은 검사    통과도 실패도 한 적이 없다
              ★ 자료가 없어 안 도는 것.  안 도는 검사는 없는 것과 같다
③ 못 잡는 검사  있는데 결함이 지나갔다
              ★ V11-78 이 있는데 08-18 세로 떨어짐을 못 잡았다
④ 규격 없는 검사 코드에 있는데 docs 에 근거가 없다
⑤ 검사 없는 규격 docs 에 검산을 적었는데 코드에 없다
              ★ 가장 위험하다.  지킨다고 믿고 안 지킨다
```

### 정리 규칙

```
필수   중복이면 「더 넓은 것」을 남기고 좁은 것을 지운다
       ★ V11-78 을 다섯 폭으로 넓히면 V11-109 가 흡수된다
필수   ★ 「못 잡은 검사」는 지우지 않는다.  고친다
       ★ 지우면 그 결함이 영영 안 잡힌다
필수   ② 죽은 검사는 왜 안 도는지 적는다
       자료가 없어서면   → 수집을 고쳐야 한다는 뜻이다.  지우지 않는다
       조건이 안 맞아서면 → 조건을 고친다
       정말 쓸모없으면   → 지운다
필수   ⑤ 를 가장 먼저 만든다.  규격이 빈말이 되고 있다
필수   지울 때 왜 지우는지 이력에 적는다
금지   숫자를 줄이려고 지우는 것
       ★ 「검사 300개」가 목표가 아니다
검산   S30-1  CHECKS.md 에 마지막 통과·실패 시각이 있는가
       S30-2  한 번도 안 돈 검사가 목록에 있는가
       S30-3  규격에 있는데 코드에 없는 검사가 0 인가
```

```
★ 누가 하나
  세는 것 · 갈래로 나누는 것   개발측
  지울지 판단하는 것           ★ 가이드 — 규격이 가이드 것이다
  ★ 개발측이 규격을 지우지 않는다 (규칙 2)
```

---

## ★★ 테스터 — S31 · 08-18

**마스터 물음 — 「테스터가 검사 역할을 대신하는 거야? 개발이 하는 거니?」**

```
검사 (validate/)   기계가 매번 자동으로 잰다.  개발측이 만들고 돌린다
테스터             ★ 검사가 못 잡는 것을 찾는다
가이드             규격을 쓰고 정본과 대조한다
★ 테스터가 검사를 대신하지 않는다.  검사를 늘릴 재료를 만든다
```

```
흐름   테스터가 찾는다 → 가이드가 규격으로 → 개발측이 검사로 → 기계가 잡는다
```

### 테스터가 하는 것

```
① 판정 재현    매물 하나를 원문에서 손으로 계산해 화면과 대조
              ★ 개발측 코드를 안 쓴다.  독립으로 계산한다
② 데이터 정합성 같은 값이 두 곳에서 다른가 · 말이 안 되는 값이 있는가
              주행 0km 인데 3년식 · 신차가보다 비싼 중고차
              사고 0회인데 수리비 300만
③ 검사의 검사  ★ 일부러 결함을 넣어 검사가 잡는지 본다
              「—」를 하나 넣고 V11-106 이 잡는가
              배점 합을 604 로 만들고 V3-56 이 잡는가
              ★ 이것은 테스터만 할 수 있다.  개발측이 자기 검사를 시험하면 무디다
④ 화면 사용성  써 보고 「몇 번 눌러야 하나」를 센다
⑤ 원문 대조    사이트 화면에 있는데 우리에게 없는 것
```

### 테스터 규칙

```
필수   소스를 보지 않는다 — web/ · report/ · analyze/ · validate/
       ★ 「왜 그런지」를 알면 눈이 흐려진다
필수   보는 것   docs/chapters/61-web/ · 30-score/f-table.md
                outputs/render/ · outputs/shot/ · DB
필수   ★ 고치지 않는다.  적기만 한다
필수   ★ 「고치는 법」을 제안하지 않는다
       ★ 제안하면 개발측이 그것만 하고 원인을 안 찾는다
필수   재현 절차를 적는다 — 「이 매물 · 이 화면 · 이 순서」
필수   outputs/test/YYYY-MM-DD_HHMM.md 에 쓴다
필수   별도 세션으로 돈다 — tmux new -s tester
       ★ 같은 세션이면 자기가 만든 것을 자기가 본다
검산   S31-1  테스터 기록이 남는가
       S31-2  테스터가 소스를 안 보는가
```

```
★ 테스터의 한계
  같은 모델이라 같은 맹점을 가진다
  잡힌다    깨진 화면 · 안 되는 링크 · 「—」 · 말 안 되는 값 · 계산 불일치
  못 잡는다  시장 논리 · 배점이 현실과 맞는가
  ★ 그것은 마스터만 안다
```


---

## ★★★ 규격에 출처를 붙인다 — S32 · 08-18

**마스터 지적 — 「규격은 네가 정하는 요구사항이잖아.**
**그중에 내가 요구하는 것도 있을 거고 로직도 있을 건데. 구분이 어렵나?」**

```
★ 지금 다섯 층이 섞여 있다.  표시가 없으니 전부 같은 무게로 보인다
```

### 다섯 층

| 표시 | 무엇 | 누가 바꾸나 | 안 지켜지면 |
|---|---|---|---|
| **`[마스터]`** | 마스터가 정한 것 | **마스터만** | **★ 최우선 결함** |
| `[시안]` | **v1 화면 · `ref/screens/`** | **마스터만** | **★ 최우선 결함** |
| `[원문]` | 사이트가 주는 것 | 못 바꾼다 | 사실과 어긋남 |
| `[조사]` | 출처가 있는 것 | 다시 조사 | 출처를 댄다 |
| **`[판단]`** | **가이드가 정한 것** | 가이드 | ★ 가장 틀리기 쉽다 |
| `[기술]` | 기술 제약 | 마스터 승인 | 0장 원칙 |

```
필수   「필수·금지」 줄마다 표시를 붙인다
       필수 [마스터]   ★ 총점은 5장-2a 가 정한다
       필수 [조사]     감가율 1년 12% · 3년 33%  (승용차 평균 33.6%)
       필수 [판단]     ★ 옵션 기준값은 그 트림의 P90
                      근거 — 최대는 이상치.  ★ 조사 없음
       필수 [원문]     신차가 = 등급기준 + 선택옵션가 합
       금지 [기술]     JS 로 폭을 재는 것

필수   ★ [판단] 에는 근거를 반드시 적는다
       근거가 없으면 「★ 근거 없음 — 가이드가 그냥 정함」이라 적는다
       ★ 그것이 마스터가 먼저 볼 목록이다
필수   ★ [마스터] · [시안] 은 가이드가 바꾸지 않는다
       고쳐야 할 것 같으면 마스터께 여쭌다
필수   테스터·검사가 결함을 찾으면 어느 층인지 함께 적는다
검산   S32-1  「필수·금지」 줄에 표시가 있는가
       S32-2  [판단] 인데 근거가 없는 것 목록
       S32-3  ★ [마스터]·[시안] 이 안 지켜진 것 — 최우선 결함으로 센다
```

### ★ 시안은 `[마스터]` 다

```
★ 08-16 사고 — 시안 CSS 255종 중 58종만 반영됐다 (95% 누락)
  ★ 마스터가 「시세가 표다」라 하실 때까지 아무도 몰랐다
  ★ 가이드가 시안을 「참고」처럼 다뤘다.  그것이 잘못이다

필수 [마스터]   ref/screens/ 의 시안은 마스터 요구사항이다
                v1 원본(ref/v1-ui/)도 같다
필수           시안과 다르면 그것이 결함이다.  「더 낫다」는 이유가 안 된다
필수           바꾸려면 마스터께 여쭌다
검산           V11-59 · V11-60 · V11-68 · V11-69
               ★ 이 넷의 실패는 [마스터] 층 결함이다.  가장 먼저 고친다
```

```
★ 왜 층을 나누나
  테스터가 「이상하다」 할 때
    [원문]·[마스터]·[시안] 과 다르면  →  개발측이 고친다
    ★ [판단] 이 이상하면              →  가이드가 다시 본다
    ★ [마스터] 가 이상하면            →  마스터께 여쭌다
  ★ 지금은 전부 「규격 결함」으로 뭉쳐진다
```


---

## ★★★ 요구사항 대장 — S33 · 08-18

**마스터 지시 — 「가이드에 요구사항 섹션을 따로 구분하고,**
**규격과 검사도 정비하고 테스터의 역할도 명확하게 정리하자.**
**내 요구사항도 중복이나 오류가 있으니 내가 보고 정리해도 좋고」**

```
★ 지금 마스터 요구사항이 개정 346건에 흩어져 있다
★ 중복·모순을 아무도 못 본다
```

### ★ 셋을 가른다 — 08-18 마스터 지적

**마스터 — 「테스트 지적과 요구사항을 구분을 못하네」**

| 문서 | 무엇 | 닫히나 |
|---|---|:--:|
| `guide/01_요구사항.md` | **새로 만들라** — 「605로 올리자」 「4시간마다」 | **✗ 계속 지킨다** |
| `guide/02_결함대장.md` | **잘못됐다** — 「HUD 0 은 뭐야」 「차트가 안 보여」 | ○ 고치면 닫힘 |
| `chapters/00-standard.md` | **일하는 방식이 틀렸다** — 「부록에 기준 넣지 마라」 | 규칙이 됨 |

```
필수   셋을 한 표에 넣지 않는다
       ★ 고쳐진 것과 계속 지킬 것이 안 갈린다
필수   결함이 재발하면 검사로 만든다.  대장에 「재발 N회」를 적는다
필수   원칙 지적은 S 검사로 만든다 (S28~S33 이 그렇게 나왔다)
```

### `docs/guide/01_요구사항.md`

```
필수   개정 이력에서 마스터 지시·지적만 뽑아 한 곳에
필수   갈래별로 — 판정·배점 · 화면 · 수집·데이터 · 운영·점검 · 문서·규격
필수   한 줄에   개정 번호 · 날짜 · 마스터 말씀 그대로 · 무엇으로 만들었나 · 상태
필수   ★ 마스터 말씀은 원문 그대로 인용한다.  요약하지 않는다
필수   상태를 넷으로 — ○ 지켜짐 · ★★ 일부 · ★★★ 안 지켜짐 · ? 확인 필요

필수   ★ 가이드가 이 문서를 임의로 고치지 않는다 ([마스터] 층 · S32)
       ★ 마스터가 지우거나 합치면 가이드가 규격에 반영한다
필수   새 지시가 오면 대장에 한 줄 더한다
필수   개발측이 상태를 갱신한다 — 구현·검증 결과로
       ★ 상태는 사실이다.  가이드가 짐작으로 적지 않는다
검산   S33-1  이력의 마스터 지시가 대장에 전부 있는가
       S33-2  대장의 상태가 검사 결과와 맞는가
       S33-3  ★ ★★★ 인 것이 몇 건인가 — 그것이 밀린 목록이다
```

```
★ 왜 마스터가 정리하나
  중복인지 모순인지는 요구한 사람만 안다
  예 — 「사이트 보증 50점」(개정 306) 과 26축의 「보증 30」(개정 329)
       ★ 가이드가 줄인 것이다.  마스터가 판단할 일이다
```


---

## ★★★ 추적표 — S34 · 08-18

**마스터 지시 — 「각종 챕터나 작업 단위별로 요구사항을 역으로 뽑아내고,**
**그걸 기반으로 규격이 충실하게 만들어졌는지 체크하고, 결함과 개선사항을 구분하고,**
**해당 요구사항과 개선사항·가이드·소스/화면 메뉴·테스트 결과·결함·조치완료를 한 세트로」**

### 번호 체계

```
R-nnn   요구사항   마스터가 요구한 것 · 규격에서 역으로 뽑은 것
I-nnn   개선사항   지금 되긴 하는데 더 나아야 하는 것
D-nnn   결함       규격·원문과 다른 것.  고치면 닫힌다
★ 셋을 가른다
  요구  없으면 안 되는 것        ★ 안 닫힌다
  개선  있는데 불편한 것          ★ 닫힌다.  다만 급하지 않다
  결함  있어야 할 것이 틀린 것     ★ 닫힌다.  급하다
```

### 한 세트 — `docs/trace/{장}.md`

```
| R | 요구사항 | 출처 | 규격 | 소스 | 화면·메뉴 | 검사 | 테스트 | 결함 | 상태 |
```

```
R        R-005
요구사항   한 줄로.  「무엇이 되어야 하는가」
출처      [마스터] 개정 306 · [시안] · [원문] · [조사] · [판단]   (S32)
규격      STEP 149n · f-table 4-2      ★ 없으면 「규격 없음」
소스      analyze/axis/option.py       ★ 없으면 「미구현」
화면·메뉴  /listings 옵션 열            ★ 화면이 없으면 미완이다 (S27)
검사      V11-100                      ★ 없으면 「검사 없음」
테스트     outputs/test/… 2026-08-18    ★ 테스터가 확인한 날
결함      D-012                        ★ 열려 있는 결함
상태      ○ 완료 · ◐ 진행 · ✗ 미착수 · ! 결함
```

### 규칙

```
필수   한 줄이 비면 그것이 결함이다
       규격 없음    → 가이드가 쓴다
       소스 없음    → 개발측이 만든다
       화면 없음    → ★ 미완이다 (S27)
       검사 없음    → 개발측이 만든다
       테스트 없음   → 테스터가 확인한다
필수   ★ 요구사항을 규격에서 역으로 뽑는다
       ★ 규격에 있는데 요구가 없으면 「왜 있는지 모르는 규격」이다
필수   장마다 한 파일 — docs/trace/00-standard.md · 05-score.md · 14-web.md …
필수   새 요구·개선·결함이 오면 그 장의 표에 한 줄 더한다
검산   S34-1  표의 규격이 실재하는가
       S34-2  표의 소스·검사가 실재하는가
       S34-3  ★ 빈 칸이 몇 개인가 — 그것이 할 일 목록이다
       S34-4  규격에 있는데 표에 없는 것 (왜 있는지 모르는 규격)
```

---

## ★★ 누가 무엇을 하나 — S35 · 08-18

**마스터 지시 — 「너랑 개발자·테스터·내가 각각 역할하게 관리하고」**

| 칸 | 채우는 이 | 누가 승인 |
|---|---|---|
| **요구사항** | 마스터 · 가이드(역추출) | **마스터** |
| **출처** | 가이드 | — |
| **규격** | **가이드** | 마스터 (`[마스터]`·`[시안]` 층) |
| **소스** | 개발측 | — |
| **화면·메뉴** | 개발측 | 마스터 (보고 판단) |
| **검사** | 개발측 | 가이드 (규격과 맞는지) |
| **테스트** | **테스터** | — |
| **결함** | 테스터 · 마스터 | — |
| **상태** | 개발측 (사실로) | 가이드 (검증) |

```
필수   자기 칸만 채운다
금지   ★ 가이드가 「상태」를 짐작으로 적는 것
       ★ 상태는 사실이다.  개발측이 돌려 본 결과다
금지   ★ 개발측이 「요구사항」을 지우는 것
금지   ★ 테스터가 「고치는 법」을 적는 것
필수   막히면 자기 칸에 「막힘 — 왜」를 적고 다음으로 간다
필수   ★ 주간 점검(금 02:00)에 추적표 빈 칸을 함께 센다 (S29)
검산   S35-1  자기 칸이 아닌 곳을 고친 흔적이 있는가
```

```
★ 흐름
  마스터가 요구한다        →  가이드가 규격으로
  가이드가 규격을 쓴다      →  개발측이 소스·화면·검사로
  개발측이 만든다          →  테스터가 확인한다
  테스터가 결함을 찾는다     →  가이드가 규격을 다시 본다
  ★ 한 바퀴가 돈 것이 「조치 완료」다
```


---

## ★★ 보안은 정식 서비스 전에 한다 — S36 · 08-18

**마스터 확정 — 「그냥 제한 없애. 정식 서비스할 때 강화해.**
**보안은 제일 마지막에 해. 미리 설레발 치지 말자」**

```
지금   마스터 한 사람이 쓴다.  공개돼 있지 않다
정식   사람이 늘고 밖에서 접근한다
★ 지금 강화하면 마스터만 불편하고 얻는 것이 없다
```

```
필수 [마스터]   비밀번호 길이·문자 종류 제한을 두지 않는다
필수 [마스터]   보안 강화는 「정식 서비스 착수」 항목에 모아 둔다
                ★ 지금 하지 않는다
필수 [기술]     다만 아래는 지금도 지킨다 — 되돌릴 수 없는 것이라서
                비밀번호 해시를 로그에 안 남긴다
                개인정보 테이블을 쿼리로 안 연다
                HTTPS 를 쓴다
                쿠키에 Secure · HttpOnly
                ★ 이것들은 나중에 붙이면 이미 샌 뒤다
금지 [마스터]   지금 단계에서 보안을 이유로 기능을 막는 것
검산            S36-1  「정식 서비스 착수」 목록이 있는가
```

### ★★ 08-19 — 가이드가 S36 을 어겼다

**마스터 지적 — 「보안, 개인정보 보호는 모든게 마무리 되고 진행할거니 집착하지 않기를 바래」**

```
★ 무엇을 어겼나
  묻지 않으신 보안 경고를 두 번 냈다
  마스터가 하시려던 일을 멈추게 하고 그 자리에 보안 이야기를 놓았다
★ S36 이 이미 규격에 있었다.  가이드가 제 규격을 안 지켰다
```

```
필수 [마스터]   보안 이야기를 먼저 꺼내지 않는다.  물으시면 답한다
필수 [마스터]   위 「지금도 지키는 넷」에 걸리는 것만 그 자리에서 한 줄
                ★ 그 넷 밖은 outputs/LATER.md 로 보낸다.  대화에 올리지 않는다
필수           같은 경고를 두 번 하지 않는다.  한 번 적었으면 끝이다
금지 [마스터]   지시서 · 순서 · 명령서에 보안 항목을 끼워 넣는 것
                ★ 순서를 흐리는 것이 지금 단계에서 가장 큰 손해다
검산            S36-2  ★ outputs/ORDER_* 에 보안 항목이 있는가 (있으면 실패)
                       예외 — 위 「지금도 지키는 넷」
```

```
★ 왜 이것이 규격이 되나
  보안은 「옳은 말」이라 반박이 없다.  그래서 조용히 순서를 밀어낸다
  ★ 옳은 말이라는 것이 지금 할 이유가 되지 않는다
```

```
★ 정식 서비스 착수 목록 — 그때 한다
  비밀번호 규칙 강화 · 2단계 인증 · 접근 로그 감사
  요율 제한 (rate limit) · 세션 만료 정책 · 계정 잠금 정책
  개인정보 보관 기간 · 삭제 요청 처리
```

---

## ★ 마스터께 여쭐 것과 가이드가 정할 것 — 08-18

**마스터 반응 — 「이게 뭐야? 내가 알아야 해?」 (확보율 질문에)**

```
★ 기술 세부를 마스터께 여쭀다.  가이드가 정할 일이었다
```

```
여쭌다      값의 뜻이 바뀌는 것 — 배점 · 등급 경계 · 무엇을 보여줄까
            거래·시장 지식이 필요한 것 — 「전액 현금이 뭔가」
            우선순위 · 무엇을 먼저 할까
            ★ 마스터가 쓰는 화면의 모양

가이드가 정한다  계산 방식 · 자료 구조 · 파일 배치 · 검사 설계
                ★ 정하고 근거를 적는다.  틀리면 마스터가 지적하신다
금지            기술 세부를 여쭤 마스터의 시간을 쓰는 것
```


---

## ★★★ 이 도구는 사는 사람을 위한 것이다 — S37 · 08-18

**마스터 지적 — 「내가 엔카랑 직거래를 하기 위한 용도지 판매 대행 용도가 아닌데 뭘까?」**

```
★ 가이드가 「계약 4단계」를 넣었다.  판매를 대행하는 쪽이 쓰는 것이다
★ 쓰는 사람을 잘못 잡으면 기능이 통째로 헛것이 된다
```

### 쓰는 사람

```
마스터    차를 사려는 한 사람
          엔카·K카에서 매물을 보고 딜러에게 직접 연락해 산다
          ★ 파는 사람이 아니다.  대행하지 않는다.  중개하지 않는다
```

### 그래서 있어야 할 것

```
있다   좋은 매물을 찾는다        판정 · 추천 · 시세
       왜 그런지 안다           근거 · 왜 싼가 · 확인 못 한 것
       담아 두고 지켜본다       관심 · 가격 변동 · 사라짐
       실제로 얼마인지 안다     사이트별 구매비용 총액
       연락하고 보러 간다       딜러 정보 · 진행 메모

없다   ★ 판매 대행 절차 · 계약서 · 수수료 · 정산
       ★ 여러 사용자 · 권한 등급 · 고객 관리
       ★ 매물 등록 · 판매 관리
```

```
필수 [마스터]   새 기능을 넣기 전에 「마스터가 차를 사는 데 쓰는가」를 묻는다
필수 [마스터]   아니면 넣지 않는다.  「나중에 쓸 수도 있다」로 넣지 않는다
금지 [마스터]   파는 쪽·대행하는 쪽의 기능
검산            S37-1  판매·대행 개념이 규격에 남아 있는가
```

```
★ 왜 이것이 규격이 되나
  「계약 4단계」처럼 조용히 섞여 든다
  ★ 가이드가 일반적인 중고차 시스템을 떠올리면 그렇게 된다
```


---

## ★★★ 「완료」의 뜻 — S38 · 08-18

**마스터 지적 — 「항상 이래 너희들은 참내」**

```
실측 08-18 08:40
  요구 865건 · 상태 ○ 538건 (62%)
  ★ 그런데 소스 칸은 16건 (1%) 만 찼다
  ★ 「됐다」고 하고 「어디에 있는지」는 안 적었다
  ★ 순서 2 를 「추적표 상태를 사실로」라 커밋했는데 상태만 고쳤다
```

### 규격

```
필수   ○ 가 되려면 네 칸이 다 차야 한다
       규격 · 소스 · 화면 · 검사
       ★ 하나라도 비면 ○ 가 아니다
필수   비는 칸에 따라 상태가 정해진다
       소스 빔        →  ✗   「어디에 있는지 모르는데 됐다」는 성립하지 않는다
       화면 빔        →  ◐   기능은 있으나 못 쓴다 (S27)
       검사 빔        →  ◐   지킨다고 믿을 근거가 없다
       테스트 빔       →  ○ 는 되되 「확인 안 됨」을 함께 적는다
                          ★ 테스터가 아직 안 돌았을 뿐이다
필수   ★ 화면이 필요 없는 R 은 화면 칸에 「해당 없음」이라 적는다
       ★ 비워 두지 않는다.  빈 것과 없는 것은 다르다
금지   상태만 고치고 칸을 안 채우는 것
검산   S38-1  ○ 인데 소스·화면·검사 중 빈 칸이 있는가
       S38-2  칸별 채움률을 낸다 — 규격 · 소스 · 화면 · 검사 · 테스트
```

### ★★★ 08-21 — 화면은 **배포까지가 완료**다

**마스터 실측 — 화면 다섯을 만들었는데 마스터 화면에는 하나도 안 보였다**

```
저장소  outputs/render/listings.html   8칸 · 등급 A · ★ 네 묶음 막대 있음
실제   {base_url}          ★ 옛 카드형 · 등급 없음 · 막대 없음
                                     ★ 메뉴 27개 (규격은 3개)
★ 커밋만 하고 서버에 안 올렸다
★ outputs/render/ 는 렌더 결과를 파일로 떨군 것이지 서버가 아니다
```

```
★★ 검사도 같이 속았다
   V11-157 「상단 메뉴가 넷을 넘는가」가 통과했는데 ★ 실제 화면은 27개다
   ★ 검사가 렌더 파일을 읽었기 때문이다
   → 검사가 통과해도 마스터가 못 보면 ★ 안 된 것이다
```

```
필수   ★ 화면 칸은 「배포된 화면」으로만 채운다
       렌더 파일 · 템플릿 · 스크린샷은 ★ 근거가 아니다
필수   ★ 배포한 뒤 실제 주소로 열어 확인한다.  스크린샷을 그때 찍는다
필수   화면을 고쳤으면 ★ 작업기록에 배포 시각과 확인한 주소를 적는다
금지   ★ 「렌더 파일이 맞다」를 완료 근거로 삼는 것
금지   ★ 화면 검사를 outputs/render/ 로만 도는 것
검산   S38-6  ★ 신설 — 화면 검사가 배포된 주소를 보는가 (렌더 파일만 보면 실패)
       S38-7  ★ 신설 — 화면을 고친 커밋에 배포 시각이 적혀 있는가
```

```
★ 왜 이것이 규격이 되나
  ★ 이 도구는 마스터가 화면으로 차를 고르는 것이 목적이다
  ★ 화면이 안 보이면 만든 것이 아니다.  파일이 맞은 것뿐이다
```

### ★★★ 08-19 — 상태는 사람이 적지 않는다. 계산값이다

**가이드 실측 — `✗` 46건 중 진짜 미착수가 0건이었다**

```
소스 칸  46건 전부 채워져 있다.  「미구현」·「미확인」이 하나도 없다
검사 칸  44건이 「검사 없음」
S38 규칙으로 다시 매기면  ◐ 45 · ○ 1.  ✗ 는 하나도 없다
```

```
★ 왜 어긋났나
  trace_fill.py 가 채우는 것은 소스 · 화면 · 검사 세 칸뿐이다 (:26)
  상태 칸은 사람이 썼다
  ★ 개정 372·397 로 소스 칸이 275 + 151건 채워졌는데 상태가 안 따라왔다
  ★ 「✗ 46건」은 소스가 비어 있던 시절의 흔적이다
★ 같은 뿌리가 「?」 16건에서도 나왔다 — 마스터가 답하셨는데 추적표가 안 따라왔다
```

**상태를 세 칸에서 유도한다. 사람이 손으로 적지 않는다.**

| 소스 | 화면 | 검사 | 상태 |
|---|---|---|:--:|
| 있음 | 참 또는 「해당 없음」 | 있음 | **○** |
| 있음 | 하나라도 빔 | | **◐** |
| 미구현 · 미확인 | | | **✗** |

```
필수   trace_fill.py 가 상태 칸을 함께 쓴다
필수   ★ 「!」 와 「?」 는 사람이 적는 것이다.  도구가 덮지 않는다
       ! = 결함이다 → guide/02_결함대장.md 로도 간다
       ? = 확인 필요.  답이 나오면 사람이 지운다
필수   화면 칸 「—」(층이 화면이 아님)는 「해당 없음」과 같이 본다 (S39)
금지   상태를 손으로 적는 것.  ★ 세 칸을 채우면 상태는 따라온다
검산   S38-4  ★ 상태가 세 칸에서 유도한 값과 다른 행이 있는가
       S38-5  「!」·「?」 가 도구 실행 뒤에도 남아 있는가
```

```
★ 왜 이것이 규격이 되나
  사람이 적는 칸과 기계가 적는 칸이 섞이면 늘 한쪽만 갱신된다
  ★ 두 번 같은 방식으로 어긋났다.  세 번째를 막는다
```

### 4시간 점검이 매번 센다

```
필수   S34-3 을 4시간 점검에 넣는다
       「소스 빔 849 · 화면 빔 748 · 검사 빔 678 · 테스트 빔 862」
필수   ★ 어제와 견준다.  줄지 않으면 그것이 화면에 남는다
필수   /admin/status 에 낸다
```

```
★ 왜 이것이 필요한가
  「했다」를 잴 방법이 없으면 「했다」가 늘어난다
  ★ 숫자가 나빠져도 사실에 가까운 편이 낫다
```

---

## ★ 작업 끝냈다고 말하는 법 — 08-18

```
필수   커밋 제목이 「순서 N — 무엇」이면 그 순서의 전 항목을 끝낸 것이다
       일부만 했으면 「순서 N 일부 — 무엇만」이라 적는다
필수   기록에 숫자를 적는다
       「소스 칸 16 → 620 (72%)」
필수   못 한 것을 함께 적는다
       「화면 칸은 못 채웠습니다 — 라우팅 표가 없어서」
금지   「했습니다」만 적는 것 (기존 R-543a)
금지   ★ 일부만 하고 순서 번호로 커밋하는 것
검산   S38-3  「순서 N」 커밋 뒤 그 순서의 항목이 전부 끝났는가
```


---

## ★★★ 서로 점검한다 — S43 · 08-19

**마스터 지시 — 「개발측과 너와의 상호 조화가 중요하니 서로 점검하는 체계를 공고히 하자」**

```
★ 08-19 에 양쪽이 다 어긋났다
  가이드   낱말 검색으로 「미착수」라 단정했다.  세 건이 이미 끝나 있었다
  개발측   v170·v171 이 SPEC 버전을 안 적었다.  v172 는 r398 인데 지금은 r407 이다
  공통     개발측이 v172 §8 로 셋을 여쭀는데 가이드가 답하지 않았다
```

### ① 버전 손잡기 — 매번 맞춘다

```
개발측 필수   작업기록 머리에 SPEC 버전을 적는다.  ★ 빠뜨리지 않는다
가이드 필수   그 버전과 지금 버전을 대조해 「못 본 개정」을 목록으로 낸다
             예 — 「v172 는 r398 이다.  개정 400~407 을 아직 못 봤다」
필수         명령서 첫 줄은 늘 `git pull` 이다
검산         S43-1  작업기록마다 SPEC 버전이 있는가
             S43-2  ★ 개발측이 본 버전과 지금 버전의 차이를 가이드가 냈는가
```

### ② 「했다」를 확인하는 법 — 가이드

```
필수   ★ 함수 본문을 열어 읽는다.  낱말 검색 결과로 판단하지 않는다
       실패 08-19 — `res.text()` 를 찾고 「grab() 에서 쓰는 것」이라 단정했다.
                    같은 낱말이 두 군데 있었고 postChunks 안은 안 열었다
필수   파일 존재는 목록이 아니라 직접 조회로 확인한다
       실패 08-19 — tree 목록에 없다고 했는데 다시 부르니 있었다
금지   「없다」를 한 번의 검색으로 단정하는 것
검산   S43-3  ★ 가이드가 「미착수」라 적은 것 중 실제로는 되어 있는 것이 있는가
```

### ③ 「했다」를 확인하는 법 — 개발측

```
필수   실패하는 표본으로 되살린다 (S42-2).  ★ 통과만 보고 「된다」 하지 않는다
필수   원인을 고쳤다고 말하기 전에 사본으로 끝까지 돌린다
       실패 08-18 — 「S4 를 고쳤다」고 보고했는데 원인이 다른 데 있었다 (v172 §6)
필수   숫자를 적는다.  「검사 없음 289 → 270」
```

### ④ 서로 묻고 답하는 자리

| 방향 | 어디 | 규칙 |
|---|---|---|
| 가이드 → 개발측 | `outputs/ORDER_2026MMDD_rNNN.md` | ★ 늘 하나다.  순서는 그 안에 적는다 (원칙 2) |
| 개발측 → 가이드 | 작업기록 「여쭐 것」 절 | **★ 가이드가 다음 차례에 반드시 답한다** |
| 답한 것 | `outputs/` | 어느 기록의 몇 절에 답하는지 적는다 |

```
필수   ★ 미답으로 넘어가지 않는다.  답을 못 하면 「아직 못 정했다」라고 적는다
필수   개발측 지적으로 규격이 바뀌면 이력에 「개발측 지적」이라 표시한다
       ★ 지시서가 틀렸다는 기록이다.  그것이 다음을 막는다
검산   S43-4  개발측 기록의 「여쭐 것」 중 답이 없는 것이 있는가
```

### ⑤ 틀렸을 때

```
필수   양쪽 다 오판을 기록에 남긴다.  ★ 지우지 않는다
필수   「왜 그렇게 판단했나」를 함께 적는다 — 방법이 틀린 것이지 사람이 틀린 것이 아니다
금지   조용히 고치는 것
```

```
★ 왜 이것이 규격이 되나
  둘 다 상대가 한 일을 직접 못 본다.  글로만 본다
  ★ 글을 대충 읽으면 「안 했다」거나 「했다」가 둘 다 거짓이 된다
```

---

## ★★★ 검사를 어디에 만드나 — S42 · 08-19

**실측 08-19 — 검사가 없는 요구가 289건이다**

| 층 | 검사 없음 |
|---|--:|
| 운영 | 67 |
| 화면 | 47 |
| 판정 | 41 |
| 저장 | 40 |
| 수집 | 38 |
| 검사 | 17 |
| 사전 | 9 |
| 복합 | 30 |

```
번호는 정해졌는데 코드가 없는 것   32건 (고유 검사 15개)
번호조차 없는 것                257건
출처가 [마스터] 인 것            189건  ★ 마스터가 정한 값이 검사 없이 있다
```

### ★ 289건을 다 만들지 않는다

```
★ 검사 자체가 이미 세 번 거짓을 냈다
  V3-22    「빠진 것 중에 경고가 있나」를 세어 거짓 경보
  S34-3    옛 형식(12칸)을 세어 「온전한 행이 없다」
  V11-134  주석·딴 문장에 걸려 두 번 거짓 통과
★ 검사를 늘리면 거짓도 늘어난다.  골라 만든다
```

### 만드는 기준 — 다섯 중 하나에 걸릴 때

```
① 조용히 틀린다     틀려도 화면이 정상으로 보인다
                   예 — ⑤ 하위 합 65 가 갈래 표기 50 과 어긋났는데 아무도 몰랐다
② 같은 값이 두 곳 이상  어긋나면 아무도 모른다
                   예 — 사진 크기가 61-web.md 와 a-common.md 둘에 있었다
③ 되돌릴 수 없다    원문이 사라진다 · 잘못된 등급이 나간다 · 망에 요청이 나간다
④ 이미 한 번 어긋났다  이력에 개정 번호가 있는 것.  ★ 재발을 막는다
⑤ 사람이 못 잰다    632건 대조 · 백분율 · 전수 합계
```

### 만들지 않는 것

```
✗ 눈으로 한 번 보면 아는 것    → S41 로 가이드가 스크린샷에서 본다
✗ 금지 조항의 서술            → 개정 50·53.  정적 검사는 문자열 상수만 본다.
                              서술까지 잡으면 문서화할수록 붉어진다
✗ 값이 하나뿐이고 한 곳에만 있는 것
```

### 검사를 만들 때 반드시

```
필수   ★ 「실패하는 표본」으로 되살려 확인한다
       ★ 통과만 보고 「된다」 하지 않는다.  V11-134 가 그래서 두 번 거짓 통과했다
필수   실패했을 때 무엇을 하라는지 함께 낸다 — Check.action (개정 47)
필수   정적 검사는 문자열 상수만 본다 (개정 53)
금지   「코드에 있다」로 통과시키는 것.  ★ 실제로 부른다 (V11-99)
검산   S42-1  4시간 점검이 「검사 없음」 건수를 매번 내는가
       S42-2  ★ 새로 만든 검사마다 실패 표본으로 되살린 기록이 있는가
```

### 순서

```
1차   번호는 정해졌는데 코드가 없는 15개.  ★ 규격이 이미 「필요하다」고 정했다
      그중 V9-01·03·04·05·08 다섯은 K카 대기다 → 실질 10개 · 요구 19건
2차   ④ 이미 한 번 어긋난 것
3차   ② 두 곳 이상에 있는 값 — 배점 · 크기 · 상수
그 밖  ★ 기준 다섯에 안 걸리면 만들지 않는다.  「검사 없음」으로 남긴다
      ★ 남기는 것도 판단이다.  빈 칸으로 두는 것과 다르다
```

---

## ★★★ 화면 확인을 누가 하나 — S41 · 08-19

**마스터 확정 — 「클로드 AI 를 AWS 에 에이전트 추가해서 테스트 업무를 시킬 거야.**
**지금은 없어. 그걸 감안해서 너랑 나랑 역할을 정해.**
**AI 가 보지 못하는 것은 내가 해야 하고. 시안과 대조해서 만든 것 점검하는 것은 너 몫이고」**

```
★ 테스터 자리는 비어 있다.  AWS 에이전트가 들어올 때까지 둘이 나눈다
★ 가르는 기준은 하나 — 「AI 가 볼 수 있는가」
```

### 가이드가 본다 — 시안·정본과 대조

| 무엇 | 어떻게 |
|---|---|
| 시안과 실물이 같은가 | `ref/screens/v2_*_시안.html` ↔ 렌더 결과 |
| 잴 값이 맞는가 | 사진 88px · 카드 8줄 이내 · 색 12 · 글꼴 2 |
| 금지가 지켜지나 | 「—」 안 쓴다 · 계산식 안 낸다 · 라벨이 경로가 아니다 |
| 다섯 폭에서 안 깨지나 | 360 · 640 · 900 · 1100 · 1400 |
| 있어야 할 절·칸이 있나 | 상세 11절 · 목록 열 · 머리말 |

```
필수   스크린샷을 받아 view 로 실제로 본다.  ★ 렌더 HTML 만 grep 하지 않는다
필수   「G-3 위반」처럼 항목 번호로 적는다.  숫자를 낸다
금지   「좋아 보인다」 · 「대체로 맞습니다」
검산   V11-129  시안과 실물이 같은가
```

### 마스터가 본다 — AI 가 못 보는 것

| 무엇 | 왜 AI 가 못 보나 |
|---|---|
| **사진이 실제로 뜨는가** | 스크린샷이 `file://` 이라 외부 이미지(`ci.encar.com`)가 안 뜬다 |
| **눌렀을 때 무엇이 되는가** | 스크린샷은 정지 화면이다. 가이드는 브라우저를 못 쓴다 |
| **실기기 감각** | 손가락으로 눌리는가 · 스크롤이 걸리는가 · 느린가 |
| **정보의 양과 순서** | 「이만큼이면 충분한가」 · 「이 순서가 맞는가」 |
| **말이 통하는가** | 문구를 보고 무엇을 하라는지 아시겠는가 |
| **새 자료가 들어왔을 때** | 그때의 화면은 미리 못 본다 |

```
필수   한 화면씩 몰아서 드린다.  ★ 마스터 확정 08-19
       화면 하나에 ① 가이드가 잰 것(숫자) ② 여쭐 것(단추)
필수   여쭐 것은 잴 값이 없는 것만.  이미 정하신 값은 가이드가 대조한다
금지   여러 화면을 섞어 드리는 것
검산   S41-1  여쭐 항목에 정본의 잴 값이 있는가 (있으면 가이드 몫이다 — 실패)
```

### 테스터가 들어오면 — AWS 에이전트

```
넘길 것   눌렀을 때의 흐름 · 되돌아가기 · 빈 결과 · 실패 안내 · 느림
남는 것   ★ 사진이 실제로 뜨는가 · 실기기 감각 · 정보의 양과 순서 · 말이 통하는가
          → 이것은 그때도 마스터 몫이다
필수      에이전트가 들어오면 이 절을 고친다.  ★ 그 전에 미리 나누지 않는다
```

```
★ 왜 이것이 규격이 되나
  「화면 확인」을 뭉뚱그리면 전부 마스터께 간다
  ★ 마스터 시간이 가장 비싸다.  기계로 잴 수 있으면 마스터께 가지 않는다
```

---

## ★★★ 추적표에 「층」 칸을 둔다 — S39 · 08-18

**마스터 지적 — 「다른 요구들은 어디에 재배치 되어야 하는 거 아니니?**
**로직에 반영하거나 수집 또는 리포트일 텐데」**

```
★ 「화면 없음 96건」을 「해당 없음」으로 치우려 했다.  그것이 잘못이다
★ 화면이 아니면 어딘가 다른 층에 있다.  어디인지를 적어야 한다
```

### 층 여덟

| 층 | 무엇 | 어디 |
|---|---|---|
| **수집** | 원문을 받아 그대로 둔다 | `collect/` · `adapters/` |
| **저장** | 원문을 표로 · 이력을 남긴다 | `store/` · `sql/ddl/` |
| **파싱** | 원문에서 값을 뽑는다 | `parse/` |
| **사전** | 같은 값을 같은 것으로 | `store/dict.py` |
| **판정** | 축 · 점수 · 등급 | `analyze/` · `score/` |
| **화면** | 사람이 보고 누른다 | `web/` · `report/screens/` |
| **검사** | 지켜지는지 잰다 | `validate/` · `tools/` |
| **운영** | 큐 · 설정 · 계정 · 감사 | `store/adminops.py` · `deploy/` |

```
필수   R 하나마다 층을 적는다
필수   ★ 「화면」 칸은 층이 「화면」인 R 에만 요구한다
       ★ 다른 층이면 화면 칸에 「—」 를 적는다.  「화면 없음」이 아니다
필수   층이 둘 이상이면 둘 다 적는다 — 「수집·화면」
       예   「407 을 그대로 화면에 낸다」 → 수집이 잡고 화면이 낸다
필수   층을 못 정하면 「?」 로 두고 가이드가 판단한다
금지   층을 안 적고 「해당 없음」이라 치우는 것
검산   S39-1  R 마다 층이 적혀 있는가
       S39-2  층이 「화면」이 아닌데 「화면 없음」이라 적힌 것이 있는가
```

```
★ 왜 이것이 필요한가
  「화면 없음 354건」이 실은 「수집 40 · 저장 60 · 판정 80 · 화면 60 …」이다
  ★ 층을 알면 누가 무엇을 해야 하는지가 나온다
  ★ 「해당 없음」으로 치우면 그 요구가 영영 사라진다
```

---

## ★★ 관리 화면도 시안을 그린다 — 08-18

**마스터 지적 — 「시안도 없이 말로 따우네」**

```
매물 화면   ref/screens/ 에 시안이 있다 (마스터가 v1 에서 뽑은 것)
관리 화면   ★ 없다.  말로만 적었다
★ 말로 적으면 사람마다 다르게 만든다
```

```
필수   관리 화면 19개에 시안을 붙인다
필수   좁은 폭(360) 기준으로 자리를 그린다
       ★ 글자로 그려도 된다.  자리와 차례가 보이면 된다
필수   무엇을 누르면 무엇이 되는지 화살표로
필수   ★ 시안은 [마스터] 층이다 (S32).  마스터가 고치면 그것이 정본이다
검산   V11-128  관리 화면마다 시안이 있는가
```


---

## ★★★ 비화면 요건을 층으로 재배치하고 개발 여부를 다시 잰다 — S40 · 08-18

**마스터 지시 — 「비화면 요건에 대해서는 재배치하고 개발 여부 재점검하게 하고」**

```
★ 「화면 없음」으로 몰아 둔 것을 층으로 돌려놓고 그 층에서 다시 잰다
★ 지금은 「화면이 없다」로만 적혀 있어 무엇을 해야 하는지 모른다
```

### 절차

```
1  R 마다 층을 정한다 (S39 — 여덟 층)
   수집 · 저장 · 파싱 · 사전 · 판정 · 화면 · 검사 · 운영

2  층별로 소스를 다시 찾는다
   ★ 층을 알면 어느 디렉터리를 볼지 정해진다
   수집 → collect/ · adapters/
   저장 → store/ · sql/ddl/
   파싱 → parse/
   사전 → store/dict.py
   판정 → analyze/ · score/
   화면 → web/ · report/screens/
   검사 → validate/ · tools/
   운영 → store/adminops.py · deploy/

3  ★ 그 층에서 실제로 도는지 확인한다
   코드가 있다  ≠  돈다
   ★ 08-18 실측 — 「자동화 고쳤다」가 한 번도 안 돌았다
   필수   실행 흔적을 본다 — 로그 · DB 행 · 검사 통과
   필수   흔적이 없으면 ✗ 다.  코드가 있어도

4  상태를 다시 매긴다 (S38)
   소스 있음 + 돈 흔적 있음      →  ○
   소스 있음 + 돈 흔적 없음      →  ◐
   소스 없음                    →  ✗
   ★ 「금지」 항목은 「어기지 않았는가」를 검사로 잰다
      검사가 없으면 ◐ 다

5  층별 요약을 낸다
   「수집 R 60 — ○ 20 · ◐ 30 · ✗ 10」
   ★ 이것이 「어느 층이 밀렸나」다
```

```
필수   ★ 「금지」 요건도 개발 여부를 잰다
       금지는 「안 하는 것」이라 코드가 없다.  검사가 그 자리를 대신한다
       예   「금지 파일을 직접 편집」  →  V?-nn 이 그것을 잡는가
       ★ 검사가 없으면 그 금지는 지켜지는지 아무도 모른다
필수   층별 담당을 적는다 — 누가 그 층을 고치나
금지   「화면 없음」으로 남겨 두는 것
검산   S40-1  R 마다 층이 있고 그 층에서 확인됐는가
       S40-2  「금지」 요건에 검사가 붙어 있는가
       S40-3  층별 요약이 있는가
```

---

## ★★ 시안을 그리면 지금 화면과 대조한다 — 08-18

**마스터 지적 — 「그래서 지금 화면과 뭐가 차이나는데」**

```
★ 시안만 그리고 대조를 안 했다.  그러면 무엇을 고쳐야 하는지 모른다
```

```
필수   시안을 그리면 그 자리에서 지금 화면과 대조한다
필수   차이를 번호 붙여 표로 — 「A-1 표가 세로로 깨짐」
필수   ★ 스크린샷을 받아 실제로 본다
금지   시안만 올리고 끝내는 것
검산   V11-131  시안마다 대조표가 있는가
```
