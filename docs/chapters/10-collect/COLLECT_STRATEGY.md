

```
version  SPEC-2026.09.02-r1062
follows  `config/targets.json`
sources  개정 726 · 실측 08-25
checks   S46-38 · S46-39
```


---

# 6. ★★★ K카 · 리본카 — ★ 골라 받는 길을 찾았나 (마스터 지시 08-24 · 개정 712)

```
★★ 마스터 — 「★ **케이카 리본카는 ★ 골라서 받아 주는 방법 찾아**」
★ ★ 가이드가 ★ 다시 두드렸다.  ★ 결과를 ★ 그대로 적는다
```

## 6-1. ★ K카 — ★ **길이 없다.  ★ 그러나 문제가 아니다**

| 무엇을 두드렸나 | 결과 |
|---|---|
| `modelNm` · `mnuftrNm` · `modelGrpNm` · `carNm` | ★ **전부 무시** — 526 그대로 |
| `searchWord` · `keyword` · `modelCd` | ★ 무시 |
| `fuelNm` · `fuelCd` · `grdNm` · `sellDcd` · `prc` · `milg` · `yridnw` | ★ 무시 |
| 이웃 경로 `count` · `search` · `model` · `filter` · `category` | ★ **404** |
| `POST /bc/stockCar/list` | ★ **405** |

```
★★★ ★ **파라미터를 ★ 열셋 두드렸고 ★ 이웃 경로 다섯을 두드렸다.  ★ 다 안 된다**
   ★ ★ 서버가 ★ `pageSize` 하나만 본다
★★ ★ **그런데 ★ 전량이 ★ 526건이다.  ★ 좁힐 까닭이 없다**
   ★ ★ 한 번에 ★ 1초에 받는다.  ★ 우리 쪽에서 거르면 된다
필수  ★ ★ **K카는 ★ 「좁힐 수 없다」로 ★ 확정한다.**  ★ 더 두드리지 마라
```

## 6-2. ★ 리본카 — ★ **길이 없다.  ★ 1,038건이라 역시 문제가 아니다**

| 무엇을 두드렸나 | 결과 |
|---|---|
| 사이트맵 | ★ **매물번호만** 준다 — ★ 차종·연료가 없다 |
| 목록 화면 `?carType=` · `?maker=` | ★ **200 이나 매물 0** — ★ 껍데기다 (JS 렌더) |
| 목록 API `SB1001L.rb` | ★★ **robots `Disallow`** — ★ 두드리지 않는다 |
| `blog/sitemap.xml` | ★ 매물 아님 |

```
★★★ ★ **사이트맵이 ★ 매물번호만 주므로 ★ 상세를 열기 전에는 ★ 차종을 모른다**
   ★ ★ 좁히려면 ★ 상세 1,038건을 ★ 다 열어야 한다 — ★ 그것이 ★ 「다 받는 것」이다
★★ ★ **그러나 ★ 값이 있다** — ★ 우리 대상이 ★ 약 100건(10%)이다
필수  ★ ★ **상세를 다 열되 ★ 차종이 안 맞으면 ★ 그 자리에서 버린다**
      ★ ★ `core_listing` 에 ★ **안 넣는다** — ★ 미분류가 안 쌓인다
      ★ ★ 지금은 ★ 1,036건을 ★ **다 넣어** ★ 미분류를 만들고 있다
```

## 6-3. ★★★ 그러므로 — ★ **셋째 방식이 있다**

```
★ 지금까지 ★ 둘로만 봤다 —
   ⓐ ★ 받을 때 좁힌다 (사이트가 되면)
   ⓑ ★ 다 받아 ★ 저장하고 ★ 화면에서 거른다  ← ★ 지금 이것이다
★★★ ★ **ⓒ ★ 다 받되 ★ 저장할 때 거른다**
   ★ ★ 상세를 열어 ★ 차종이 안 맞으면 ★ **`core_listing` 에 안 넣는다**
   ★ ★ `raw_response` 에는 ★ 남는다 — ★ 나중에 갈래를 넓히면 ★ 다시 판다

| | 받는 양 | 저장 | 미분류 |
|---|---|---|---|
| ⓐ 받을 때 좁힘 | 적다 | 우리 것만 | ★ 없다 |
| ⓑ 다 받아 다 저장 | 많다 | 다 | ★★ **쌓인다** |
| ★ **ⓒ 다 받고 ★ 걸러 저장** | 많다 | ★ 우리 것만 | ★ **없다** |

필수  ★ ★ **K카·리본카·렉서스는 ★ ⓒ 로 간다**
      ★ ★ 받는 것은 어쩔 수 없으나 ★ **저장을 거르면 ★ 미분류가 안 쌓인다**
필수  ★ `raw_response` 는 ★ 남긴다 — ★ 갈래를 넓히면 ★ 다시 판다
검산  ★ `S46-60` — ★ K카·리본카·렉서스에서 온 매물 중 ★ 우리 대상 아닌 것이
      ★ `core_listing` 에 있으면 ★ 실패
```

---

# 7. ★★★ 사이트별 차종 코드 — ★ **채움을 세는 법** (개정 732 · 08-25)

```
★★ ★ 가이드가 ★ 「★ 26종 중 몇」으로 세어 ★ **다 비어 보였다** — ★ 잘못 세었다
   ★ ★ 기아 1/20 · 렉서스 0/20 · BMW 0/20 로 보였으나
   ★ ★ **그 사이트는 ★ 자기 브랜드만 판다**
```

## ★ 브랜드 전문 사이트 — ★ **자기 것만 센다**

| 사이트 | ★ 파는 브랜드 | 우리 대상 | 채움 |
|---|---|--:|:--:|
| 기아 CPO | 기아 | ★ **1종** (스포티지) | ★ **1/1** |
| 현대 인증 | 현대 · 제네시스 | ★ **7종** | ★ **7/7** |
| 볼보 셀렉트 | 볼보 | ★ **5종** | ★ **5/5** |
| 렉서스 인증 | 렉서스 | ★ **2종** | ★ **2/2** |
| BMW BPS | BMW | ★ **1종** (X3) | ★ **1/1** |

## ★ 여러 브랜드 사이트 — ★ ★ 26종을 다 센다

| 사이트 | 채움 | 빠진 것 |
|---|:--:|---|
| 엔카 | ★ **20/20** | — |
| KB차차차 | 19/20 | ★ `EX60` — ★ **KB 에 없다** |
| 보배드림 | 19/20 | 〃 |
| 헤이딜러 | 15/20 | ★ 모델Y · GLC · Q5 · NX · EX60 — ★ **마스터 주소가 필요하다** |

```
★★★ ★ **`EX60` 은 ★ 어느 사이트에도 없다** — ★ 신형이라 ★ 중고가 안 나왔다
   ★ ★ **빠뜨린 것이 아니다.**  ★ 나오면 잡힌다
★ ★ 헤이딜러 다섯은 ★ **해시라 지어낼 수 없다** — ★ 마스터께서 주소를 주셔야 한다
필수  ★ ★ **채움을 셀 때 ★ 「그 사이트가 파는 것」으로 센다** — ★ ★ 26종으로 세지 마라
검산  ★ `S46-64` — ★ 브랜드 전문 사이트가 ★ 자기 브랜드 차종을 ★ 다 가졌는가
```
