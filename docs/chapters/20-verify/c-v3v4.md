## STEP 58 — V2 GAP 보정

```
version  SPEC-2026.08.29-r933
follows  `docs/chapters/30-score/f-table.md`
sources  실측 08-22
checks   S46-38 · S46-39
```


**`anomaly` 는 자동으로 덮어쓰지 않는다. 원인을 분류한 뒤 조치한다.**

| 원인 | 판별 | 보정 |
|---|---|---|
| ① 파싱 오류 | 같은 원문 재파싱 시 이전 값 재현 | 파서 수정 → 재파싱 |
| ② 원문 수정 | 원문이 실제로 바뀜 | 새 값 수용 + 이력 기록 |
| ③ 매물 교체 | `source_id` 재사용 · `vin`/`plate` 불일치 | 별도 매물로 분리 |
| ④ 스키마 변경 | 여러 매물에서 동시 발생 | 수집 중단 · 매핑 갱신 |

```
판별 순서
  1  이전 원문과 현재 원문을 비교          같으면 ①
  2  동시 발생 건수를 본다                다수면 ④
  3  vin · plate 를 대조                 불일치면 ③
  4  나머지                              ②
기록   core_listing_change 에 change_kind='anomaly' + cause
금지   원인 분류 없이 새 값으로 덮어쓰는 것
```

### 보정 후 재검증

```
보정을 적용한 뒤 V2 를 다시 돌린다.
anomaly 가 0 이 되거나, 남은 것이 전부 설명돼야 통과다.
```

---

## STEP 59 — V3 로직 검증

**판정이 「작동은 하는데 의미가 없는」 상태를 잡는다.**

| 코드 | 검사 | 기대 | 등급 |
|---|---|---|---|
| V3-01 | `result_axis.source` 전건 NOT NULL | 위반 0 | fatal |
| V3-02 | `result_axis.prio` 전건 NOT NULL | 위반 0 | fatal |
| V3-03 | 축별 `source` 값 종류 ≥ 2 | 참 | warn |
| V3-04 | **축별 값 종류 ≥ 2** | 참 | warn |
| V3-05 | 금지 근거가 `source` 에 등장하지 않음 | 0 | fatal |
| V3-06 | `put()` 충돌 기록 검토 | 설명됨 | warn |
| V3-07 | 축별 `-1` 비율 | 차종 사양과 일치 | warn |
| V3-08 | 사전 `pending` 이 판정에 쓰이지 않음 | 0 | fatal |
| V3-09 | 축별 `excluded` 비율이 차종 사양과 일치 | 통과 | warn |

### V3-03 · V3-04 — 변별력

```
값 종류 1   그 축은 순위에 기여하지 않는다
           결함일 수도, 그 차종의 실제 특성일 수도 있다
```

| 상황 | 판정 |
|---|---|
| 전 차종에서 값 종류 1 | **결함 유력.** 판정 경로 확인 |
| 특정 차종만 값 종류 1 | 그 차종 특성 가능. 8차종 통합 분포로 재판정 |

```
예   콜레오스 주행거리 전건 만점 → 2024~25년식만 있어서다.  결함 아님
    accident_type 전 차종 unknown → 결함이다
```

**차종 내부 변별력이 0이어도 차종 간 비교에서는 기능할 수 있다.**
**차종 내부만 보고 배점을 낮추지 않는다.**

### V3-03 `source` 종류

```
한 축의 source 가 전건 동일하면 우선순위 구조가 작동하지 않는 것이다
예   전건 'keyword'  →  1·2순위 경로가 한 번도 이긴 적이 없다
```

---

## STEP 59a — V3 경고 · 딜러 검증

| 코드 | 검사 | 등급 |
|---|---|---|
| V3-20 | `trust_score` 가 총점에 합산되지 않음 | fatal |
| V3-21 | 경고(`listing_warning`)가 총점에 합산되지 않음 | fatal |
| V3-22 | **경고로 매물이 목록에서 제외되지 않음** | fatal |
| V3-23 | **경고로 등급·추천 순위가 바뀌지 않음** | fatal |
| V3-24 | `acknowledged` 가 신호 감지 자체를 멈추지 않음 | fatal |
| V3-25 | 소멸한 경고가 삭제되지 않고 `resolved_at` 으로 남음 | fatal |
| V3-26 | `sample_sufficient=False` 딜러의 점수가 확정 표시되지 않음 | fatal |
| V3-27 | 모든 경고에 `evidence` 존재 | fatal |
| V3-28 | `PeerGroup` 이 확장 단계를 표시 | fatal |
| V3-29 | 배점 변경 시 `calc_version` 이 증가 | fatal |
| V3-30 | `halt` 축의 사전이 비어 있지 않음 (S9 선행) | fatal |
| V3-37 | 목록 관측분의 `source` 가 `'list'` 임 | code | fatal |
| V3-40 | 핵심 축이 excluded 인데 등급이 매겨진 건이 없음 | data | fatal |
| V3-41 | 전 매물의 분모가 만점과 같음 | data | fatal |
| V3-42 | 취향 축만으로 S·A 가 나온 건이 없음 | data | fatal |
| V3-43 | 트림·옵션이 높은데 점수가 낮은 역전이 없음 | data | fatal |
| V3-44 | NULL 축에 원인 코드가 붙어 있음 | data | fatal |
| V3-45 | 배점 합이 총점과 같음 | code | fatal |
| V3-46 | 등급이 505 기준으로 매겨짐 | code | fatal |
| V3-47 | 축별 차종 간 결측률 편차가 상한 이내 | data | fatal |
| V3-48 | 축이 차종 안에서 변별력이 있음 | data | warn |
| V3-49 | 자차 미가입 기간이 있는 매물이 「무사고」로 나오지 않음 | data | fatal |
| V3-50 | 성능부와 보험이력이 어긋난 건을 셈 | data | warn |
| V3-60 | 등급 상한이 적용됨 | code | fatal |
| V3-61 | 상한으로 내려간 매물에 사유가 붙어 있음 | code | fatal |
| V3-70 | 제조사 보증을 일반·동력계로 따로 내는가 | code | fatal |
| V3-71 | 보증 잔여가 기간·거리 중 낮은 쪽인가 | code | fatal |
| V9-10 | 사이트 보증 규칙이 sites.json 대로인가 | code | fatal |
| V3-38 | facet 수신 후 목록 관측분과 대조함 | code | warn |
| V3-31 | 딜러 NULL 매물에 `dealer_untrusted` 경고가 없음 | fatal |
| V3-32 | `seizing` null 매물이 「저당 없음」으로 판정되지 않음 | fatal |
| V3-34 | 판정 항목 수 == `resultCode IS NOT NULL` 인 `items` 수 | code | fatal |
| V3-35 | `conflicts` 가 있는 매물이 기록됨 | code | fatal |
| V3-36 | `conflicts` 건수가 임계 미만 | external | warn |
| V3-35 |  가 있는 매물이 기록됨 | code | fatal |
| V3-36 |  건수가 임계 미만 | external | warn |

**V3-20~29 는 STEP 59a 다.** V3-01~11(STEP 59·60)과 번호가 겹치지 않는다.

---

## STEP 60 — V3 판정 재현 검증

```
같은 CORE + 같은 dict_version 으로 다시 판정하면 같은 Verdict 가 나와야 한다.
```

| 코드 | 검사 | 등급 |
|---|---|---|
| V3-10 | 재판정 결과가 이전과 동일 | fatal |
| V3-11 | `put()` 호출 순서 셔플 후에도 동일 (표본 100건) | fatal |

```
다르면   비결정 요소가 섞였다.  시각 · 난수 · 전역 상태 · dict 순회 순서
근거     0장 STEP 8 · 불변식 ①
```

---

## STEP 61 — V4 매핑 검증

**이름이 아니라 값으로 검증한다.**

```
방법   원문 경로의 값 ↔ CORE 컬럼의 값을 매물 단위로 대조
변환   ×10,000 · ym · date10 · json 을 자동 감지해 표기
```

### ★ 일치율 기준은 필드 중요도별로 다르다

| 등급 | 필드 | 기준 | 미달 시 |
|---|---|---|---|
| **A** | 식별키 · 점수 근거 · E등급 근거 | **100%** | fatal |
| B | 분류 근거 · 사전 원천 | **99%** | fatal |
| C | 화면 표시 전용 | 80% | warn + 원인 기록 |

```
A 예   listing_id · source_id · vin · plate_no · price · origin_price
      warranty_* · options_* · inspection_panel_json
      flood · total_loss · airbag · seizing · pledge
근거   돈과 등급을 결정하는 필드가 80% 만 맞으면 20% 는 틀린 점수를 받는다
      「원문 근거 없이 확정하지 않는다」는 이 문서의 원칙과도 어긋난다
```

**80% 를 전 필드에 일괄 적용하지 않는다.**

| 코드 | 검사 | 기대 | 등급 |
|---|---|---|---|
| V4-01 | 매핑표 전 항목이 **등급별 기준** 충족 (A 100% · B 99% · C 80%) | 전건 | fatal |
| V4-02 | **미매핑 경로 목록** | 없음 또는 사유 기재 | warn |
| V4-03 | **오매핑 탐지** — 다른 경로와 더 높은 일치율 | 없음 | fatal |
| V4-04 | 매핑표에 없는 CORE 컬럼 | 없음 | warn |
| V4-05 | 원문 경로 수 변동 | 안정 | warn |
| V4-06 | **RAW 경로 ↔ 등록부 대조** — RAW 에 있는데 미등록 | 0 | fatal |
| V4-06b | **역방향** — 등록부에 있는데 RAW 에 없는 유령 경로 | 0 | warn |
| V4-07 | `usage='in_use'` 인데 `core_column` NULL | 0 | fatal |
| V4-08 | `usage='blocked'` 인데 `unblock_condition` NULL | 0 | fatal |
| V4-09 | `usage='deferred'` 인데 `use_when` NULL | 0 | fatal |
| V4-10 | `usage='display_only'` 인데 `core_column` NULL | 0 | fatal |
| **V4-11** | **판정에 쓰는 축의 경로가 `unclassified`** | 0 | **fatal** |
| V4-11b | 판정에 안 쓰는 경로가 `unclassified` | 0 | **warn** |
| V4-12 | facet 필수 축 집합 존재 (`REQUIRED_FACET_AXES`) | 전건 | fatal |
| **V4-13** | **매직 넘버 없음** — 임계값·경계값이 `config` 참조인가 | 0 | **fatal** |
| V4-14 | 본문의 `config` 키가 STEP 6 표에 전건 있는가 | 0 | fatal |
| V4-15 | 표의 키가 실제 `config` 파일에 있는가 | 0 | fatal |
| V4-16 | 본문 참조가 `config.{파일}.{키}` 형식인가 | 0 | warn |
| **V4-17** | **코드의 수치 상수가 전건 상수표에 있는가** | 0 | **fatal** |
| V4-18 | 상수표 이름과 `config` 키가 같은 뜻으로 겹치지 않는가 | 0 | warn |
| V4-19 | 성격(`kind`)이 없는 `Check` 가 없는가 | 0 | fatal |
| V4-20 | `dict_option_code` 에 문장(공백·한글)이 없는가 | 0 | fatal |
| V4-21 | 같은 이름의 함수가 두 모듈에 없는가 | 0 | warn |
| V4-22 | 역방향 · 순환 `import` 가 없는가 | 0 | fatal |
| V4-23 | 모듈 최상위에 I/O · 부작용이 없는가 | 0 | fatal |
| V4-24 | 축 함수가 `target_config` 에서 매물 값을 읽지 않음 | code | fatal |
| V4-25 | 판정에 쓰는 축의 사전이 비어 있지 않음 | 0 | fatal |

### ★ 검사기 자신을 검사 대상에서 제외한다 — 네 번째 사례

```
V6-03 · V6-06 · V7-03   금지 조항 서술이 잡혔다        →  문자열 상수만 본다
V10-05 · S7 · S14       검사기의 거부 표본이 잡혔다     →  자기 제외
```

```
사례   v10_admin.py 가 「DELETE FROM raw_response 를 거부하는가」를 시험한다
      그 문자열이 소스에 있어야 하는데, V10-05 가 그것을 「금지된 쓰기」로 잡는다
      기대값 20 이 S14 매직 넘버로 잡힌다
```

```
필수   검사 대상에서 검사기 자신을 뺀다
      tools/check_spec.py · tools/check_src.py · validate/v*_*.py
근거   거부 표본이 검사에 잡히면 그 검사를 만들 수 없다
       검사기는 「금지된 것」을 문자열로 들고 있어야 판정할 수 있다
금지   제외 대신 표본을 우회 표기로 바꾸는 것
      (예: "DEL"+"ETE").  읽는 사람이 무엇을 시험하는지 모른다
```

```
★ 자기 제외는 구멍이다.  좁게 연다
  제외 대상은 파일 목록으로 고정한다.  디렉터리 통째로 빼지 않는다
  검사기 자신의 로직 결함은 시험으로 잡는다 (거부 표본 8종 · 허용 4종)
```

### ★ 정적 검사는 문자열 상수만 본다 — 세 번 겪은 함정

```
V6-03   화면이 raw_* 를 조회하지 않는다        →  금지 조항 서술이 잡혔다
V6-06   gone 을 「판매됨」으로 쓰지 않는다       →  금지 조항 서술이 잡혔다
V7-03   sold_price 컬럼을 만들지 않는다        →  금지 조항 서술이 잡혔다
```

**금지를 문서화할수록 검사가 붉어진다. 그러면 검사를 끄게 된다.**

```
필수   AST 로 문자열 리터럴만 뽑는다.  주석 · docstring 은 대상이 아니다
필수   SQL 인지 화면 문구인지 나눈다.  용도가 다르면 다른 검사다
금지   소스 파일을 정규식으로 훑는 것
근거   V4-13 과 같은 함정이다.  정상 코드에서 실패가 나면 사람이 검사를 끈다

★ 새 「금지」 검사를 만들 때마다 이 절을 확인한다
  「이 검사가 금지 조항 자체를 잡는가」를 먼저 묻는다
```

### V4-13 — 문서 수치와 대조하지 않는다

```
폐기한 방식   문서의 관측 수치를 추출해 소스에서 문자열 검색
문제         배점 200 과 관측치 200 을 구분할 방법이 없다
            39 · 62 · 4 · 5 · 0 · 1 · -1 도 마찬가지다
            정상 코드에서 fatal 이 쏟아지고, 결국 검사를 끄게 된다
            v1 에서 검사를 끈 이유가 대개 그것이었다
```

**검색 방향을 뒤집는다. 소스에서 숫자를 뽑아 근거를 묻는다.**

```
① 소스의 숫자 리터럴을 추출한다
② 아래 중 하나가 아니면 실패

   허용   config 키 참조                coefficient_sane_range 등
         AxisSpec.max_points          배점.  config/scoring.json 에서 온다
         열거 집합의 길이               len(REQUIRED_FACET_AXES)
         값 상수 0 · 1 · -1 · None     축 값 정의 (7장 STEP 83)
         인덱스 · 슬라이스 · HTTP 코드

   금지   임계값 · 경계값 · 비율 · 표본 수를 코드에 직접
         if coefficient > 1.2          ✗
         if denominator < total * 0.6  ✗
         if sample < 30                ✗
```

```
효과   문서를 고쳐도 검사 목록을 안 고쳐도 된다
      config 로 가기로 한 다른 조항(0장 STEP 6)과 결이 같다
```

**진짜 방어는 검사가 아니라 이 문장이다.**

```
「이 문서만으로 구현할 수 있어야 한다.
  v1 DB 를 봐야 구현되면 본문이 부실한 것이다.」

검사는 위반을 잡지만, 이 기준은 위반이 생길 자리를 없앤다
```

### V4-03 오매핑 탐지 — v1 실사고

```
dealer_shop   ←  OfficeCityState   일치율 100%
              딜러 상사명 컬럼에 지역이 들어가 있었다
              이름으로는 절대 안 보인다.  값 대조로만 잡힌다
```

```
탐지   컬럼 C 가 선언된 경로 P 보다 다른 경로 Q 와 더 높은 일치율을 보이면 오매핑
조치   fatal.  매핑표와 파서를 함께 고친다
```

### V4-05 경로 수 변동

```
어제 123경로 → 오늘 140경로     사이트가 필드를 추가했다
어제 123경로 → 오늘 110경로     사이트가 필드를 뺐다.  파싱이 깨질 수 있다
조치   신규 경로는 8장 미사용 목록에 등록.  삭제 경로는 영향 분석
```

---

## STEP 62 — V4 보정

| 상황 | 보정 |
|---|---|
| 미매핑 경로 | 매핑표에 사유와 함께 등록. 필요하면 CORE 컬럼 추가 후 재파싱 |
| 오매핑 | 파서 수정 → 재파싱 → V4 재실행 |
| 매핑표에 없는 컬럼 | 컬럼을 없애거나 매핑표에 등록. 「나중에 쓸 것 같아서」는 사유가 아니다 |
| 일치율 하락 | 사이트 응답 변경 의심. 원문 표본 비교 |

```
원칙   보정은 재파싱으로 한다.  재수집이 아니다.  원문은 그대로다
```

---

