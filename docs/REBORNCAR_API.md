# 리본카 API · 매핑 규격

`SPEC-2026.08.23-r532` · 2026-08-23 · **마스터가 주소를 주셨다**

```
★ 가이드가 ★ `reboncar.com` 으로 두드려 ★ 도메인 없음을 받고 「주소가 틀렸다」로 적었다 (개정 526)
★★ ★ 바른 주소는 ★ `reborncar.co.kr` 이다 — ★ `n` 이 하나 더 있다
★ ★ 오토플러스가 운영하는 ★ 직영 중고차다 — ★ K카 직영 · 기아 CPO 와 같은 갈래
```

---

# 1. 경로

| 무엇 | 경로 | 실측 |
|---|---|---|
| **목록** | `GET www.reborncar.co.kr/m/smartbuy/SB1001.rb` | 200 · 145KB |
| **상세** | `GET www.reborncar.co.kr/m/smartbuy/SB1002/{매물번호}` | 200 · ★ **335KB** |

```
헤더   모바일 UA 만 있으면 된다.  ★ 인증·토큰·쿠키 ★ 없다
인코딩  UTF-8
매물번호  ★ `C26012110086` 꼴 (영문 1 + 숫자 11)
robots  광고봇을 명시적으로 허용한다 — ★ 금지 경로는 ★ 더 봐야 한다 (5장)
```

---

# 2. ★ 상세가 주는 것 (표본 `C26012110086` · 제네시스 G80 3.5 AWD)

| `core_*` 칼럼 | 리본카 | 표본 값 |
|---|---|---|
| `source_id` | 매물번호 | C26012110086 |
| `plate_hash` | 차량번호 (해시해서) | 351주3870 |
| `year_month` | 연식 | 2022년 07월 → `202207` |
| `mileage_km` | 주행거리 | 78,454 |
| `color_ext_raw` | 색상 | 검정색 |
| `displacement_cc` | 배기량 | 3,470 |
| `fuel_raw` | 연료 | 가솔린 |
| — 승차인원 | 승차인원 | 5인승 |
| **`price_origin_won`** | ★★ **신차출고가** | ★ **7,300만원** |
| `price_current_won` | 차량가격 | 3,490만원 |
| — 신차 대비 | ★ **「가성비 — 신차가격대비 48%」** | ★ **비율까지 준다** |
| `state.accident` 사고 | ★ **차량 이력 — 사고여부** | **무사고** |
| `state.special` 특수 | ★ **침수여부** | **없음** |
| **`history.use` 용도** | ★ **용도변경** | ★ **있음** |
| `options_choice_json` | ★ **차량 옵션** (이름) | HUD · AVM · SCC · 통풍시트 · LDW · VDC · ABSD … |
| — 선택 옵션 | ★ **추가된 선택 옵션** | 뒷자석 듀얼모니터 · 컴포트 패키지 · 시그니처 디자인 셀렉션 |
| `warranty.site` 사이트 검증 | ★ **직영 · 8일 안심환불 · 연장보증** | 직영 |
| — 냄새등급 | ★ **냄새등급** | 양호 |
| — 부대비용 | 관리비용(매도비) 44만 · 성능책임보험료 7.5만 | 원 단위 |

```
★★★ ★ 신차출고가를 ★ 금액으로 주고 ★ 「신차가격대비 48%」까지 준다
   → ★ 신차가를 주는 곳이 ★ 엔카 · 헤이딜러 · KB(비율) 에 이어 ★ 넷째다
   ★ `value.origin` 75점이 여기서 산다
★★ ★ 「용도변경 있음」을 ★ 명시한다 — ★ 우리 `history.use` 축과 바로 맞는다
★ 옵션이 ★ 이름으로 온다 · ★ 기본옵션과 ★ 선택옵션을 ★ 갈라 준다
   → ★ `taste.option` · ★ 신차가 매칭에 둘 다 쓸 수 있다
★ 부대비용을 ★ 금액으로 준다 — ★ 마스터가 「총비용」을 보고 싶어 하시는 자리다
```

---

# 3. ★ 조심할 것

```
① ★ 「무사고」가 ★ 사이트 판정인지 ★ 판매자 글인지 ★ 아직 모른다
   ★ 「차량 이력 — 사고여부 > 무사고」 꼴이라 ★ 사이트 판정으로 보이지만
   ★ 보배드림에서 ★ 판매자 글에 속았다 (개정 527).  ★ 표본을 늘려 확인한다
② ★ 부위별 판금·교환이 ★ 없다 — ★ 「성능·상태 점검기록부 조회」는 ★ 링크다
   ★ 골격 · 외판 · 누유는 ★ NULL 이다.  ★ 0 이 아니다
③ ★ 차대번호(VIN)를 ★ 못 찾았다 — ★ 상세 본문에 없다
④ ★ 소유자 변경 · 압류 · 저당이 ★ 없다
⑤ ★ 「연장보증 서비스」는 ★ 유료 선택이다 (미선택 시 0원) —
   ★ 사이트 검증 축에 ★ 자동으로 주지 마라.  ★ 붙은 매물만
```

---

# 4. 우리 규격에서의 자리

```
★ 오토플러스 ★ 직영이다 — ★ K카 직영 · 기아 CPO 와 같은 갈래
★ f-table 「사이트 검증 36점」에 ★ 직영으로 넣는다 —
   ★ 다만 개정 520 대로 ★ 「실제로 받았는가」를 함께 본다
★ 채워지는 축 — 연식 · 주행 · 예산 · 색상 · 옵션 · HUD · ★ 신차가 · ★ 용도 · 사고(확인 뒤)
★ 못 채우는 축 — 골격 · 외판 · 누유 · 수리비 · 소유자변경 · 압류저당 · VIN
```

---

# 5. ★ 아직 모르는 것 — 지어내지 마라

```
· ★★ 매물번호를 ★ 목록에서 못 뽑는다 (개정 532 · ★ 세 번 시도)
    화면 문구는 ★ 「2,026대」로 나온다
    ★ HTML 에 ★ `SB1002/{번호}` 링크가 ★ 0건이다 — ★ JS 가 그린다
    ★ 브라우저로 열어 ★ 스크롤을 네 번 해도 ★ 목록 XHR 이 안 뜬다
      (잡히는 것은 `api/v1/notice/getNoticeList.rb` 공지뿐이다)
    ★ 경로 후보 여덟을 눌러 봤는데 ★ 전부 200 인데 ★ 빈 응답이다 —
      ★ 없는 경로도 200 을 주므로 ★ 200 을 「있다」로 읽으면 안 된다
    ★ 3차 시도 (개정 532) —
      ⓐ ★ 상세 API 는 ★ 잡았다 (아래 「상세 API」)
      ⓑ ★ 페이지 JS 안의 `/api/v1/**.rb` 경로 ★ 14개를 캤다 —
         ★ 배너 · 최근검색 · 공지 · 마이페이지뿐이고 ★ 매물 목록이 없다
      ⓒ ★ `rebornCarLpData.rb` · `myCarListLpData.rb` 를 눌렀는데 ★ 404 다
    ★★ ★ 목록은 ★ 서버가 그려 내려주는 것으로 보인다 (JS 가 부르는 것이 아니다)
    ★ 지어내지 않는다.  ★ 마스터께 ★ 목록에서 매물 몇 개의 주소를 청한다

## ★ 상세 API — 이것은 잡았다 (개정 532)

```
POST /api/v1/car/carOption.rb              productId={번호}
POST /api/v1/car/getCarAqiInfo.rb          productId={번호}&isVipData=true
POST /api/v1/carpart/getEssentialList.rb   productId={번호}&tCarType=1&warrantyType=3&…
POST /api/v1/carpart/getRecommendList.rb   〃
POST /api/v1/carpart/getConvenienceList.rb 〃
POST /api/v1/car/leaRatio.rb               month=48&classCode=HA&domestic=1

★ `productId` 가 ★ 매물번호다 (`C26012110086`)
★★ ★ 서버에서 직접 부르면 ★ 200 인데 ★ 0B 다 — ★ 세션이 필요하다
   ★ 브라우저 안에서 부르면 ★ 200 에 값이 온다
★★ ★ 그리고 ★ 없는 경로도 ★ 서버에서는 200 인데 ★ 브라우저 안에서는 ★ 404 로 갈린다
   → ★ 경로를 확인할 때는 ★ 반드시 ★ 브라우저 안에서 부른다.  ★ 서버 200 을 믿지 마라
· `robots.txt` 의 ★ 금지 경로 — ★ 광고봇 허용만 읽었다.  ★ 전문을 다시 본다
· 「무사고」가 ★ 사이트 판정인가 ★ 판매자 글인가 (3장 ①)
· 성능점검 기록부를 ★ 링크로 받을 수 있는지
★ 표본이 ★ 1건이다.  ★ 20건 이상으로 늘려 다시 대조해야 한다 (오판대장 모양 ④)
```
