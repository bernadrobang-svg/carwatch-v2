# 리본카 API · 매핑 규격

```
version  SPEC-2026.08.31-r1018
follows  `f-table.md` · `MULTISITE_MAPPING.md`
sources  개정 866 · 실측 08-29
checks   S46-5 · S46-31
```
★ 이 문서는 ★ **그 사이트가 무엇을 주는가**만 적는다.  ★ 판정은 ★ `f-table` 이 한다 (가이드역할 ㉺)


`SPEC-2026.08.23-r554` · 2026-08-23 · **마스터가 주소를 두 번 주셨다**

```
★★ 표본 ★ 상세 25건 (사이트맵 앞 25) · 목록 ★ 전수 1,066건
★ 개정 533 은 ★ 표본 1건이었고 ★ 「목록을 못 뚫었다(네 번 시도)」로 멈춰 있었다
★★★ ★ 둘 다 풀렸다 — ★ 아래 1a장(목록) · 2a장(UA 함정)
```

```
★ 가이드가 ★ `reboncar.com` 으로 두드려 ★ 도메인 없음을 받고 「주소가 틀렸다」로 적었다 (개정 526)
★★ ★ 바른 주소는 ★ `reborncar.co.kr` 이다 — ★ `n` 이 하나 더 있다
★ ★ 오토플러스가 운영하는 ★ 직영 중고차다 — ★ K카 직영 · 기아 CPO 와 같은 갈래
```

---

# 1. 경로

| 무엇 | 경로 | 실측 |
|---|---|---|
| **목록** | ★ `GET www.reborncar.co.kr/sitemap-ext.xml` — **1a장** | 200 · ★ **1,066건** |
| ~~목록 화면~~ | ~~`/m/smartbuy/SB1001.rb`~~ ★ **껍데기다 — 매물 0개** | 200 · 145KB |
| **상세** | `GET www.reborncar.co.kr/m/smartbuy/SB1002/{매물번호}` | 200 · ★ **335KB** |

```
헤더   모바일 UA 만 있으면 된다.  ★ 인증·토큰·쿠키 ★ 없다
인코딩  UTF-8
매물번호  ★ `C26012110086` 꼴 (영문 1 + 숫자 11)
robots  광고봇을 명시적으로 허용한다 — ★ 금지 경로는 ★ 더 봐야 한다 (5장)
```

---

# 1a. ★★★ 목록 — ★ 사이트맵이 전량을 준다 (밀린일 #59 끝)

```
★★ 개정 530·533 이 ★ 네 번 시도하고 「목록에서 매물번호를 못 뽑는다」로 적었다
★ 까닭 — ★ 목록 화면(`SB1001.rb`)은 ★ 껍데기다.  ★ 매물을 ★ JS 가 그린다
   실측 — 145KB 를 받아도 ★ `C\d{11}` 코드가 ★ 0개다.  ★ `bsNos` 를 바꿔도 그대로다
★★ ★ 그런데 ★ robots.txt 가 ★ 스스로 사이트맵을 가리키고 있었다
```

| 무엇 | 주소 | 실측 |
|---|---|---|
| **매물 전량** | `GET https://www.reborncar.co.kr/sitemap-ext.xml` | 200 · 417KB · `<loc>` **2,694** |
| 그중 매물 | `…/smartbuy/SB1002/{코드}` | ★ **1,066건** (중복 0) |
| 그 밖 | `…/rent/RT1002/{코드}` 등 렌트·구독 | 1,628건 — ★ 우리 대상이 아니다 |

```
코드 꼴   ★ `C26081900017` — ★ 영문 C + 숫자 11 · ★ 1,066건 ★ 전부 12자
덤       ★ `<lastmod>` 를 함께 준다 (2,686개) — ★ 언제 올라온 매물인지 알 수 있다
필수     ★ 목록은 ★ 사이트맵으로 받는다.  ★ 목록 화면을 긁지 않는다
금지     ★ `/smartbuy/SB1001L.rb*` · `/smartbuy/SB1002L.rb*` — ★ robots 가 막았다 (검색창·팝업)
         ★ `SB1002/{코드}` 는 ★ 막지 않았다.  ★ 이것이 우리가 쓰는 경로다
```

---

# 2a. ★★★ UA 함정 — ★ 데스크톱으로 받으면 ★ 값이 거짓이다

```
★★★ ★ 같은 매물을 ★ 두 UA 로 받으면 ★ 값이 ★ 뒤집힌다 (실측 `C26072810098`)
```

| 받는 법 | 크기 | 신차출고가 | 용도변경 |
|---|--:|---|---|
| 데스크톱 UA | 382KB | `6350` | ★ **없음** |
| ★ **모바일 UA** | 316KB | ★ **7,110만원** | ★ **있음** |

```
★★ 데스크톱 응답의 그 블록은 ★ 껍데기다 —
   ★ 25건을 데스크톱으로 받았더니 ★ 신차출고가가 ★ 25건 전부 `6350` 이었다
   ★ 사고여부 25/25 「무사고」 · 용도변경 25/25 「없음」 · 신차가격대비 25/25 `0`
   ★★ ★ 그대로 믿었으면 ★ 「값이 안 갈린다 → 판정값이 아니다」로 ★ 규격을 썼을 것이다
   ★ 실제로는 ★ 사고가 갈리고(무사고 21 · 단순수리 4) · ★ 용도가 갈린다(있음 13 · 없음 12)
필수  ★ 모바일 UA 로 받는다 — `Mozilla/5.0 (iPhone; CPU iPhone OS 17_0 …) … Mobile/15E148`
필수  ★ 경로도 ★ `/m/smartbuy/SB1002/{코드}` 를 쓴다
필수  ★ 라벨이 ★ 두 꼴이다 — ★ 껍데기는 「신차 출고가」(띄어쓰기) · ★ 실값은 「신차출고가」
      ★ 「신차가격대비 >」(꺾쇠)는 껍데기 · ★ 「신차가격대비」가 실값이다
      ★ ★ 꺾쇠·띄어쓰기가 붙은 라벨을 읽으면 ★ 전건이 같은 값이 된다
검산  S46-3 ★ 신설 — 리본카 매물의 `history.use` 가 ★ 전건 같은 값이면 실패
      (★ 실측 25건에서 52%/48% 로 갈린다.  ★ 전건 같으면 ★ 껍데기를 읽은 것이다)
```

---

# 2b. ★★ 상세가 주는 것 — ★ 표본 25건 · ★ 값이 갈리는가

| 칸 | 채움 | 갈리나 | 실측 |
|---|--:|---|---|
| 주행거리 | 25/25 | ★ 갈린다 | 25가지 |
| 배기량 | 25/25 | ★ 갈린다 | 15가지 |
| 연료 | 25/25 | ★ 갈린다 | 5가지 |
| 색상 | 25/25 | ★ 갈린다 | 4가지 |
| 승차인원 | 25/25 | ★ 갈린다 | 5·7·9인승 |
| 차량번호 | 25/25 | ★ 갈린다 | 25가지 — ★ 해시해서 넣는다 |
| **신차출고가** | 25/25 | ★ 갈린다 | 25가지 · 4,420만~1억 7,060만 |
| 차량가격 | 25/25 | ★ 갈린다 | 25가지 |
| **신차가격대비** | 25/25 | ★ 갈린다 | 17가지 · **44~56%** |
| **사고여부** | 25/25 | ★ **갈린다** | 무사고 **21** · 단순수리 **4** |
| **용도변경** | 25/25 | ★★ **갈린다** | 있음 **13** · 없음 **12** |
| 냄새등급 | 25/25 | ★ 갈린다 | 양호 · 보통 |
| 침수여부 | 25/25 | ☓ 안 갈린다 | 없음 25 |
| 안심환불 | 25/25 | ☓ 안 갈린다 | 8일 25 — ★ 사이트 보장(②) |

```
★★ ★ 「용도변경 있음」이 ★ 25건 중 ★ 13건(52%) 이다 —
   ★ `history.use` 는 ★ 감점이 큰 축이다.  ★ 리본카 매물 ★ 절반이 걸린다
   ★ 이것을 껍데기로 읽어 「전건 없음」으로 넣으면 ★ 절반이 ★ 부당하게 만점을 받는다
★ 「침수 없음 25/25」 「안심환불 8일 25/25」는 ★ 안 갈린다 —
   ★ 「판정값이 아니다」이지 ★ 「없다」가 아니다.  ★ 안심환불은 ★ 사이트 보장으로 쓴다
★ 표본이 25건이다.  ★ 침수는 ★ 더 받아 보면 갈릴 수 있다 — ★ 「못 봤다」로 적는다
```

## ★ 아직 못 찾은 것

```
★ 목록을 ★ 조건으로 좁히는 법 — ★ 못 찾았다 (★ 「없다」가 아니다)
  ★ 두드린 것 — `bsNos` (마스터가 주신 값 · 다른 값 · 없는 값) → ★ 셋 다 응답이 같다
  ★ 화면 JS 안의 API 열둘을 봤으나 ★ 목록을 주는 것이 없다
★ 지금 방법 — ★ 사이트맵으로 ★ 1,066건 코드를 받고 ★ 상세에서 차종을 가른다
  ★ 상한 — ★ 1,066건.  ★ 렌트·구독 1,628건은 ★ 받지 않는다
★ 그러므로 ★ 명령서 3-0 「좁혀 부른다」를 ★ 리본카는 ★ 아직 못 지킨다.  ★ 상한으로 지킨다
```

---

# 1b. ★★★ 08-24 재측 — ★ 차종 좁히기와 ★ 건수 (개정 697)

```
★ 마스터 지시 24-1 — 「★ 못 한다·없다로 단정한 자리를 다시 두드려라」
★ ★ 규격이 ★ 「목록 좁히기」를 ★ 안 본 채로 있었다
```

## ★ 사이트맵 — ★ 전량 ★ **1,038건** (실측 08-24)

```
GET  https://www.reborncar.co.kr/sitemap-ext.xml   → 200 · 411,749B
     ★ 주소 ★ 2,661개 중 ★ **매물 1,038건** (`/smartbuy/SB1002/{C…}` 꼴)
     ★ 나머지 — 렌트 73 · 시스템 페이지
★ ★ 규격이 적은 ★ 1,066 은 ★ 08-23 값이다.  ★ 매물은 날마다 바뀐다
★ ★ 한 번 ★ **503** 이 났으나 ★ 다시 두드리니 200 — ★ 재시도로 살린다
```

## ★★ robots — ★ **사이트맵을 스스로 가리킨다**

```
`Sitemap: https://www.reborncar.co.kr/sitemap-ext.xml`   ← ★ robots.txt 에 적혀 있다
★★ ★ **사이트가 ★ 「여기를 읽어라」고 ★ 스스로 알려 준다**
★ ★ `Disallow` 는 ★ 로그인·결제·마이페이지·문의 같은 ★ 사람 전용 화면뿐이다
   ★ ★ `/smartbuy/SB1002/…`(상세)는 ★ **막혀 있지 않다**
```

## ★★★ 차종 좁히기 — ★ **없다.  ★ 우리 쪽에서 거른다**

```
★ 사이트맵은 ★ 매물번호만 준다 — ★ 차종·연료가 없다
★ ★ 목록 화면(`SB1001.rb`)은 ★ 껍데기다 (개정 530·533 · 그대로다)
★ ★ 그리고 ★ `Disallow: /m/smartbuy/SB1001L.rb*` — ★ **목록 API 는 robots 가 막는다**
필수  ★ **1,038건을 다 받아 ★ 우리 쪽에서 거른다** (★ K카와 같다)
필수  ★ 상세는 ★ `https://www.reborncar.co.kr/smartbuy/SB1002/{매물번호}` — ★ 200 · 약 318KB
      ★ ★ `?carNo=` 꼴은 ★ **404 다.  ★ 사이트맵 주소 그대로 써라**
```

## ★ 표본 60건으로 잰 것 (실측 08-24)

| | |
|---|---|
| 제조사 | 기아 **24** · 현대 14 · 제네시스 5 · 르노코리아 4 · 벤츠 4 · 쉐보레 3 · KG 2 · BMW 2 |
| ★ **우리 대상** | ★ **6 / 60 (10%)** — G80 4 · GV70 1 · GLC 1 |
| ★ 전량 추정 | ★ **약 100건** (1,038 × 10%) |

```
★★ ★ **표본 60건이다.  ★ 전수가 아니다** — ★ 개발측이 ★ 전량을 받아 ★ 다시 센다
필수  ★ 「1,038 중 우리 대상 몇 건」을 ★ 재서 이 표를 갈아라
★ ★ 100건쯤이면 ★ 보배(11)·K카(14)보다 ★ 크다 — ★ 값이 있다
```

---


# 2. ★ 상세가 주는 것 (표본 1건 · 개정 533) (표본 `C26012110086` · 제네시스 G80 3.5 AWD)

| `core_*` 칼럼 | 리본카 | 표본 값 |
|---|---|---|
| `source_id` | 매물번호 | C26012110086 |
| `plate_hash` | 차량번호 (해시해서) | 351주3870 |
| `year_month` | 연식 | 2022년 07월 → `202207` |
| `mileage_km` | 주행거리 | 78,454 |
| `color_ext_raw` | 색상 | 검정색 |
| `displacement_cc` | 배기량 | 3,470 |
| `fuel_raw` | 연료 | 가솔린 |
| — 승차인원 | 승차인원 | 5인승 |
| **`price_origin_won`** | ★★ **신차출고가** | ★ **7,300만원** |
| `price_current_won` | 차량가격 | 3,490만원 |
| — 신차 대비 | ★ **「가성비 — 신차가격대비 48%」** | ★ **비율까지 준다** |
| `state.accident` 사고 | ★ **차량 이력 — 사고여부** | **무사고** |
| `state.special` 특수 | ★ **침수여부** | **없음** |
| **`history.use` 용도** | ★ **용도변경** | ★ **있음** |
| `options_choice_json` | ★ **차량 옵션** (이름) | HUD · AVM · SCC · 통풍시트 · LDW · VDC · ABSD … |
| — 선택 옵션 | ★ **추가된 선택 옵션** | 뒷자석 듀얼모니터 · 컴포트 패키지 · 시그니처 디자인 셀렉션 |
| `warranty.site` 사이트 검증 | ★ **직영 · 8일 안심환불 · 연장보증** | 직영 |
| — 냄새등급 | ★ **냄새등급** | 양호 |
| — 부대비용 | 관리비용(매도비) 44만 · 성능책임보험료 7.5만 | 원 단위 |

```
★★★ ★ 신차출고가를 ★ 금액으로 주고 ★ 「신차가격대비 48%」까지 준다
   → ★ 신차가를 주는 곳이 ★ 엔카 · 헤이딜러 · KB(비율) 에 이어 ★ 넷째다
   ★ `value.origin` 75점이 여기서 산다
★★ ★ 「용도변경 있음」을 ★ 명시한다 — ★ 우리 `history.use` 축과 바로 맞는다
★ 옵션이 ★ 이름으로 온다 · ★ 기본옵션과 ★ 선택옵션을 ★ 갈라 준다
   → ★ `taste.option` · ★ 신차가 매칭에 둘 다 쓸 수 있다
★ 부대비용을 ★ 금액으로 준다 — ★ 마스터가 「총비용」을 보고 싶어 하시는 자리다
```

---

# 3. ★ 조심할 것

```
① ★★ 「사고여부」는 ★ 사이트 판정이 맞다 (개정 533 확정)
   실측 3건 — ★ 「무사고」 2 · ★ 「단순수리」 1
   ★ 판매자 글이면 ★ 「단순수리」라고 쓸 리 없다.  ★ 사이트가 판정해 낸 값이다
   필수  ★ `state.accident` 에 쓸 수 있다.  ★ `dict_enum` 으로 단계를 받는다
   ★ 보배드림의 「무사고」(판매자 제목 문구)와 ★ 다르다 — ★ 사이트마다 가른다
② ★ 부위별 판금·교환이 ★ 없다 — ★ 「성능·상태 점검기록부 조회」는 ★ 링크다
   ★ 골격 · 외판 · 누유는 ★ NULL 이다.  ★ 0 이 아니다
③ ★ 차대번호(VIN)를 ★ 못 찾았다 — ★ 상세 본문에 없다
④ ★ 소유자 변경 · 압류 · 저당이 ★ 없다
⑤ ★ 「연장보증 서비스」는 ★ 유료 선택이다 (미선택 시 0원) —
   ★ 사이트 검증 축에 ★ 자동으로 주지 마라.  ★ 붙은 매물만
```

---

# 3b. ★ 표본 3건 실측 (개정 533 · 마스터가 주신 주소)

| 매물 | 신차출고가 | 신차 대비 | 사고여부 | 침수 | 용도변경 | 냄새등급 | VIN |
|---|--:|--:|---|---|---|---|:--:|
| `C26012110086` | 7,300만 | **48%** | 무사고 | 없음 | 있음 | 양호 | ✘ |
| `C26070700077` | 8,809만 | **52%** | ★ **단순수리** | 없음 | 있음 | 양호 | ✘ |
| `C26070900013` | 9,884만 | **61%** | 무사고 | 없음 | 있음 | 양호 | ✘ |

```
★ 신차출고가 · 신차 대비 % · 사고여부 · 침수 · 용도변경 · 냄새등급 ★ 3/3 온다
★★ ★ 「용도변경 있음」이 ★ 3/3 이다 — ★ 직영이라 ★ 회사가 한 번 소유해서일 수 있다
   ★ 현대 인증의 「소유자 변경 있음」이 30/30 이었던 것과 ★ 같은 꼴이다 (개정 485)
   → ★ 변별력이 없을 수 있다.  ★ 표본을 더 늘려 확인한다
★ VIN 은 ★ 3/3 없다 — ★ 이 사이트는 VIN 을 안 준다
★ 표본이 ★ 3건이다.  ★ 목록을 못 뚫어 더 못 늘렸다 (5장)
```

---

# 4. 우리 규격에서의 자리

```
★ 오토플러스 ★ 직영이다 — ★ K카 직영 · 기아 CPO 와 같은 갈래
★ f-table 「사이트 검증 36점」에 ★ 직영으로 넣는다 —
   ★ 다만 개정 520 대로 ★ 「실제로 받았는가」를 함께 본다
★ 채워지는 축 — 연식 · 주행 · 예산 · 색상 · 옵션 · HUD · ★ 신차가 · ★ 용도 · 사고(확인 뒤)
★ 못 채우는 축 — 골격 · 외판 · 누유 · 수리비 · 소유자변경 · 압류저당 · VIN
```

---

# 5. ★ 아직 모르는 것 — 지어내지 마라

```
· ★★ 매물번호를 ★ 목록에서 못 뽑는다 (개정 532 · ★ 세 번 시도)
    화면 문구는 ★ 「2,026대」로 나온다
    ★ HTML 에 ★ `SB1002/{번호}` 링크가 ★ 0건이다 — ★ JS 가 그린다
    ★ 브라우저로 열어 ★ 스크롤을 네 번 해도 ★ 목록 XHR 이 안 뜬다
      (잡히는 것은 `api/v1/notice/getNoticeList.rb` 공지뿐이다)
    ★ 경로 후보 여덟을 눌러 봤는데 ★ 전부 200 인데 ★ 빈 응답이다 —
      ★ 없는 경로도 200 을 주므로 ★ 200 을 「있다」로 읽으면 안 된다
    ★ 3차 시도 (개정 532) —
      ⓐ ★ 상세 API 는 ★ 잡았다 (아래 「상세 API」)
      ⓑ ★ 페이지 JS 안의 `/api/v1/**.rb` 경로 ★ 14개를 캤다 —
         ★ 배너 · 최근검색 · 공지 · 마이페이지뿐이고 ★ 매물 목록이 없다
      ⓒ ★ `rebornCarLpData.rb` · `myCarListLpData.rb` 를 눌렀는데 ★ 404 다
    ★ 4차 시도 (개정 533 · 마스터가 주소 여섯을 주셨다) —
      ⓐ ★ `SB1001L.rb` 가 ★ 목록 화면이었다 — ★ 그런데 ★ HTML 에 매물 링크가 0건이다
      ⓑ ★ `?bsNos=20906`(지점)으로 좁혀도 ★ 0건
      ⓒ ★ 페이지 스크립트가 ★ `m.reborncar.co.kr` 로 옮기라고 적어 두어 ★ 그리로도 열었다 — ★ 0건
    ★★ ★ 목록은 ★ 서버가 그려 내려주는 것으로 보인다 (JS 가 부르는 것이 아니다)
    ★ 지어내지 않는다.  ★ 마스터께 ★ 목록에서 매물 몇 개의 주소를 청한다

## ★ 상세 API — 이것은 잡았다 (개정 532)

```
POST /api/v1/car/carOption.rb              productId={번호}
POST /api/v1/car/getCarAqiInfo.rb          productId={번호}&isVipData=true
POST /api/v1/carpart/getEssentialList.rb   productId={번호}&tCarType=1&warrantyType=3&…
POST /api/v1/carpart/getRecommendList.rb   〃
POST /api/v1/carpart/getConvenienceList.rb 〃
POST /api/v1/car/leaRatio.rb               month=48&classCode=HA&domestic=1

★ `productId` 가 ★ 매물번호다 (`C26012110086`)
★★ ★ 서버에서 직접 부르면 ★ 200 인데 ★ 0B 다 — ★ 세션이 필요하다
   ★ 브라우저 안에서 부르면 ★ 200 에 값이 온다
★★ ★ 그리고 ★ 없는 경로도 ★ 서버에서는 200 인데 ★ 브라우저 안에서는 ★ 404 로 갈린다
   → ★ 경로를 확인할 때는 ★ 반드시 ★ 브라우저 안에서 부른다.  ★ 서버 200 을 믿지 마라
· `robots.txt` 의 ★ 금지 경로 — ★ 광고봇 허용만 읽었다.  ★ 전문을 다시 본다
· ~~「무사고」가 판정인가~~ ★ 판정이 맞다 (3장 ① · 개정 533)
· 성능점검 기록부를 ★ 링크로 받을 수 있는지
★ 표본이 ★ 1건이다.  ★ 20건 이상으로 늘려 다시 대조해야 한다 (오판대장 모양 ④)
```

```
★★ 칼럼 단위 매핑은 ★ `docs/MULTISITE_MAPPING.md` ① 이 정본이다 —
   `price_origin_won` · `price_current_won` · `plate_hash` · `year_month` ·
   `core_record.use_business` 가 ★ `core_listing`·`core_record` 칼럼이다
★ ★ 그릇(`sql/ddl/02_core.sql`)을 열고 썼다 (오판 #44)
```
---

# 1c. ★★★ 마스터가 주소를 주셨다 — ★ `m.` 과 `bsNos` (개정 713 · 08-24)

```
★★ 마스터 주소 다섯 —
   `m.reborncar.co.kr/m/smartbuy/SB1001L.rb`
   `m.reborncar.co.kr/m/smartbuy/SB1001.rb?bsNos=22422,22423`
   `m.reborncar.co.kr/m/smartbuy/SB1001.rb?bsNos=22659`
   `m.reborncar.co.kr/m/smartbuy/SB1002/{매물번호}`
```

## ★★ 얻은 것 — ★ **차종을 좁히는 이름**

```
★★★ ★ **`?bsNos={코드}` 가 ★ 차종을 좁히는 파라미터다**
   ★ ★ 쉼표로 ★ 여럿 — `bsNos=22422,22423`
   ★ ★ 실측한 코드 — ★ `22422` · `22423` · `22659` (★ 마스터가 주신 셋)
★ ★ 그리고 ★ 호스트가 ★ **`m.reborncar.co.kr`** 다 — ★ 가이드는 ★ `www` 만 두드렸다
```

## ★★★ 못 얻은 것 — ★ 서버 HTML 에 ★ **매물이 없다**

| 두드린 것 | 결과 |
|---|---|
| `m/smartbuy/SB1001.rb?bsNos=22659` | 200 · 144,968B · ★ **매물번호 0** · 「0대」 |
| `?bsNos=22422,22423` · 조건 없음 | 〃 — ★ **셋이 크기만 12B 다르다** |
| `m/smartbuy/SB1001L.rb` | 200 · 133,545B · ★ 매물 0 — ★ **필터 화면**이다 (`filter.js` 실측) |
| `/api/v1/…` 여섯 | ★ **404** · `POST` ★ **404** |
| 스크립트 17개 훑기 | ★ `/api/v1/auth/csrf.rb` 만 나옴 |

```
★★ ★ **화면이 ★ JS 로 그린다.**  ★ 서버 HTML 에는 ★ 매물이 없다
   ★ ★ 마스터 화면에는 보이나 ★ 서버에서 부르면 ★ 「0대」다
★ ★ `SB1001L.rb` 은 ★ **목록 API 가 아니라 ★ 필터 고르는 화면**이다
   ★ ★ `filter.js` 가 ★ `locationFilterPage()` 로 ★ 그 주소를 만든다 — ★ 사람이 누르는 자리다
```

## ★ 그러므로 — ★ 아직 ★ **골라 받지 못한다**

```
필수  ★ ★ **`bsNos` 코드표를 모아야 한다** — ★ 지금 셋뿐이다
      ★ ★ 마스터께서 ★ 차종마다 눌러 주소를 주시면 ★ 나온다 (★ K카·헤이딜러와 같다)
필수  ★ ★ **그리고 ★ JS 가 부르는 XHR 를 찾아야 한다**
      ★ ★ 개발측이 ★ 브라우저 개발자도구 ★ 네트워크 칸을 열어 —
        ① 그 주소 ② 요청 꼴(GET/POST) ③ 헤더에 토큰이 있는가
      ★ ★ 헤이딜러가 ★ 그렇게 열렸다 (토큰 두 걸음)
★★ ★ **그때까지는 ★ 사이트맵 1,038건을 ★ 다 받고 ★ 저장할 때 거른다** (ⓒ 방식)
   ★ ★ `COLLECT_STRATEGY.md` 6장 그대로
```

---

# ★★ 08-29 재측 — ★ 사이트맵 [실측 08-29 · 가이드]

```
`GET /sitemap-ext.xml` → ★ 420,860B · `<loc>` **2,718개** · ★ 매물 **1,083건** (중복 0)
★ 08-24 의 1,036건에서 ★ 47건 늘었다 — ★ 재고는 날마다 바뀐다.  ★ 규격에 박지 않는다
★ 화면 저장은 ★ **30건**이다 (배포 실측 08-29)

★★ ★ 리본카는 ★ **목록에 차종이 없다** — ★ 상세를 받아야 갈린다 (1b장).
   ★ ★ 그래서 ★ 1,083건을 ★ 건당 1초로 도는 것이 ★ 병목이다 (약 18분)
★ `targets.json` 에 ★ 리본카 질의가 ★ **한 종도 없다** — ★ 그것이 맞다.
  ★ ★ 사이트맵이 전량이고 ★ 우리가 이름으로 거르는 구조다 (1b장)
★★ ★ 못 잰 것 — ★ 우리 차종이 그중 몇 건인지는 ★ 상세를 다 받아야 안다.
   ★ ★ 가이드는 DB 를 못 연다 — ★ 개발측이 센다
```

## ★★ 좁힐 길 — ★ 사이트맵의 나머지도 봤다 [실측 08-29]

```
`<loc>` 2,717개를 갈랐다 —
  `/smartbuy/SB1002/{코드}`  ★ **1,082** ← ★ 매물이다 (수집기 `codes()` 가 세는 것)
  `/review/RV…`             ★ 1,526 ← ★ 후기다.  ★ 좁히는 데 못 쓴다
  `/rent/RT1002/{코드}`      ★ **82** ← ★ **렌트다.  ★ 매물이 아니다**
  `/system/RS…` 8 · `/event/EV…` 3 · 그 밖 16

★★ ★ 세는 법을 조심하라 — ★ 내가 `C\d{11}` 무늬로 세니 ★ **1,164** 가 나왔다.
   ★ ★ 그 차 82건이 ★ `/rent/` 다.  ★ **수집기 쪽(경로로 가르는 것)이 맞다**
★ ★ 규격 1b·1c 가 이미 적어 둔 대로 ★ `bsNos`·XHR·`m.` 은 ★ 다 0건이다
★★ ★ 그러므로 ★ **좁힐 길이 없다** — ★ 상세 1,082건을 받아야 차종이 갈린다

★ 전기차가 몇 대인지 ★ **가이드는 못 잰다** — ★ 상세를 다 받는 것은 개발측 몫이다 (ⓜ)
```

★ 파서 — `parse/reborncar/mapping.py` · ★ 상세 표본 10건에서 ★ `price_origin_won` 10/10 (개정 885)

---

# ★★★★ 08-29 — ★ **레이어 마흔을 다 열었다** (마스터 물음 「리본카는?」)

```
★ 앞서 나는 ★ `#layer-insurance` ★ **하나만 보고** 「보험료 계산이다」로 넘겼다 — ★ 성겼다
★ ★ 다시 세니 ★ **레이어가 40개**다.  ★ 그중 우리 축에 닿는 것 —
   `layer-insure-record`   ★ 보험사고 이력 (수리비·미가입기간 설명)
   `layer-special-record`  ★ 특수 사고 (전손·도난·침수)
   `layer-state`           ★ 성능·상태점검책임보험료
   `layer-tire-state`      ★ 타이어 트레드
   `layer-battery-state`   ★★ **EV 배터리 진단 (SOH·셀 밸런스·셀 전압)**
   `layer-guarantee` · `layer-care` · `layer-careplus`  ★ 보증
```

## ★ 다만 — ★ **그 레이어는 다 「안내문」이다** [실측 08-29 · 표본 4~5건]

| 잰 것 | 결과 |
|---|---|
| 타이어 트레드 「4.9mm」 | ★ **5건 전건 같다** — ★ 안내표의 보기다 |
| 「사고 N건」·「내 차 피해」·「소유자 변경」 | ★ **표본 4건 전건 안 나온다** |
| 성능·상태점검책임보험료 | ★ **갈린다** (140,470 · 51,810 · 107,690 · 11,800 · 114,950) |
| `/api/v1/` | ★ **22개** — ★ 보험·성능 창구는 ★ **없다** (옵션·배너·채팅뿐) |

```
★★★ ★ 곧 ★ **리본카는 「자리」는 있는데 「값」이 상세 HTML 에 안 온다**
   ★ ★ ★ **JS 가 그리는 것으로 보인다** — ★ 다만 ★ **어느 창구가 채우는지 못 찾았다**
   ★ ★ ★ **「없다」로 적지 않는다** — ★ 여기까지가 내가 잰 것이다
★ ★ **`layer-battery-state`(EV 배터리 SOH)는 ★ 우리에게 큰 자리다** —
   ★ ★ 마스터께서 전기차를 보시는데 ★ SOH 는 ★ **지금 어느 사이트에서도 못 받고 있다**
```
