# 미확정

```
version  SPEC-2026.08.29-r948
follows  ★ 정본 — 가이드 문서
sources  실측 08-24
checks   S46-38 · S46-39
```


`SPEC-2026.08.29-r948`


**「지금 정할 수 없는 것」만 여기 둔다. 「내가 안 쓴 것」은 미확정이 아니다.**

```
필수   미확정을 발견하면 이 표에 행을 추가한다.  장 안에만 적지 않는다
근거   흩어져 있으면 「무엇이 안 정해졌나」를 물을 때 답할 수 없다
금지   규격을 안 쓴 것을 미확정으로 적는 것
```

---

## A. 데이터가 없어서 못 정한다 — 수집하면 풀린다

| # | 항목 | 왜 못 정하나 | 해소 조건 | 그때까지 |
|:--:|---|---|---|---|
| 1 | `encarDiagnosis` 1 · 2 의 뜻 | 둘 다 404 · `diagnosisCar=True` | 엔카 화면에서 그 매물이 어떻게 보이는지 확인 | 호출 안 함 |
| 1b | 진단 부위 목록이 8개가 전부인가 | 표본 1건 | 진단 수집 후 (484건) | 처음 보는 `name` 은 사전 미검토 |
| 1c | `resultCode` 열거값 | `NORMAL` · `REPLACEMENT` 2종만 관측 | 진단 수집 후 | 처음 보는 값은 사전 미검토 |
| 2 | `record` 정상 응답 경로 | 일부만 받아 봤다 | 이력 재수집 | 받은 것만 파싱 |
| 3 | `warnings.json` 임계 | 분포를 봐야 정해진다 | 전 차종 수집 후 | `null` — 경고 안 냄 |
| 4 | `dealer_trust` 가중치 | 지표 분포 미확인 | 전 차종 수집 후 | `null` — 산출 안 함 |
| 5 | 감가 곡선 · 계수 | 표본 부족 | 차종당 100건 이상 | 계수 `1.000` · 「미보정」 배지 |
| 6 | `curve_min_sample` 실효값 | 연차별 표본 미확인 | 전 차종 수집 후 | 100 |
| 7 | `User-Agent` | 실측 필요 | 전량 401/403 시 STEP 25a | 일반 모바일 UA |
| 8 | `recallFullFillTypes` | 관측 42건뿐 | 전 차종 수집 후 | `deferred` |
| 9 | `coout` · `hcout` | 무엇인지 모른다 | 전 차종 분포 | `deferred` |
| 10 | `BadgeGroup` 축 사용 | 트림이 아니다 | 쓸 곳이 생기면 | 등록부 `unclassified` |
| 11 | 병행수입 분리 | 표본 없음 | 병행수입 매물이 모이면 | 정식수입과 같은 그룹 |
| 12 | `domestic=false` + `importType=NONE` 46건 | 근거 없음 | 실측 확인 | 정식수입으로 봄 |

---

## ★ A2. 08-29 설계로 새로 연 것 — ★ 다섯 (아키텍처 9-3 과 같은 표다)

| # | 항목 | 왜 못 정하나 | 누가 정하나 | 그때까지 |
|:--:|---|---|---|---|
| ~~20~~ | 사이트별 판매 상태 낱말 | ★ **닫았다 08-29** — K카 `resvYn` Y 118/487 · 헤이딜러 `sale_status=listed` · KB 는 안 준다.  다섯은 파일이 쌓이면 센다 | — | `11-store/a-key` 08-29 절 |
| ~~21~~ | `raw_response` 가 몇 MB 인가 | ★ **닫았다 08-29** — **194,583행 · `body` 258.7MB**(zlib 뒤 · 상세 179.6·점검 24.2·목록 14.8) · DB 전체 **655MB** [개발측 실측] | — | — |
| ★ 22 | 파일 보관 기간 | 하루치가 얼마인지 모른다 — ★ **파일 꼴이 안 정해져 개발측도 못 쟀다**(물음 #7·#8) | ★ **마스터** | 안 지운다 (P3) |
| ★ 23 | 성능점검 여섯 축을 ⑥(80%)로 볼지 | 파서가 안 뽑아 못 갈랐다 | ★ **마스터** | 지금 0점 |
| ~~24~~ | 전기차 누유 축 | ★ **닫았다 08-29** — 마스터 「난 전기차 누유는 **제외**라고 했는데」.  ★ **갈래 ⑦ 「그 차에 원래 없다 → 만점」** 신설 (★ 08-29 재확정 — 마스터 「전기차라 누유가 없으니 만점 줘야지」 · 오판 203).  ★ 내가 이미 정해진 것을 다시 여쭈었다 (오판 202) | — | `f-table` 갈래 ⑦ |

```
★ 이 표는 ★ `docs/ARCHITECTURE_20260829.md` 9-3 과 ★ 같은 것이다 — ★ 거기가 뜻을 적고 ★ 여기가 목록이다
★ ★ 20·21 은 재면 풀린다.  ★ 22·23·24 는 ★ 마스터께서 정하신다
```

---

## B. 지금 필요가 없어서 안 정한다 — 필요해지면 정한다

| # | 항목 | 왜 안 정하나 | 해소 조건 | 지금은 |
|:--:|---|---|---|---|
| 12b | 진단으로 안전 축 세분화 | 484건 수집 전 | 진단 수집 후 | `diagnosis_car` 0/1 로 20점 |
| 13 | 권한 세분화 | 1인 사용 | 사용자가 늘면 | `role` 3단 |
| 14 | 알림 외부 채널 | 1차 범위 밖 | 2차 (11장 STEP 120a) | 화면 배지만 |
| 15 | 화면 캐시 | 필요성 미확인 | 느려지면 | 없음 |
| 16 | 템플릿 엔진 범위 | 4구문으로 충분한지 미검증 | 화면 3개 만든 뒤 | 자체 구현 |
| 17 | 동시 접속 | 1인 사용 | 필요해지면 | 단일 프로세스 |
| 18 | `outputs/` 보관 정책 | ★ **용량 문제가 났다** (마스터 08-29 「공간 낭비가 심해」) | ★ **A2-22 로 옮겼다** — 개발측이 MB 를 잰 뒤 마스터께서 정하신다 | 지우지 않음 |
| 19 | 배포 대상 | 아직 로컬 | 온라인 서비스 시 | 집 PC · `127.0.0.1` |

---

## 원칙

```
미확정 축은 excluded 로 둔다.  0 점이 아니다
분모에서 빠지고, 채워지면 즉시 살아난다
```

```
필수   해소되면 이 표에서 지우고 개정 이력에 남긴다
★ 「자료는 있는데 내가 안 정한 것」은 여기 두지 않는다
  실제로 2건이 그렇게 섞여 있었다 — 「용접,절단」 감점 · repair_cost_ratio
  둘 다 판단 재료가 이미 있었다.  개정 148·149 로 확정했다
```

---

## 해소된 것 — 기록

| 항목 | 언제 | 어떻게 |
|---|---|---|
| 「용접,절단」 감점 | 08-11 | 교환과 같게 센다. 전건 `RANK_TWO` (개정 148) |
| `repair_cost_ratio` | 08-11 | p90 = 0.139. 「상위 10%」가 정책 (개정 149) |
| `facet` 의 `Badge` | 08-10 | facet 은 `BadgeGroup`. 트림은 목록에서 (개정 104) |
| 등급컷 기준 | 08-12 | ★ 비율 판정. 절대컷은 분모가 줄면 S 가 구조적으로 불가능하다 (개정 128) |
| **`diagnosis` 경로·조건** | **08-14** | **경로는 맞음. `encarDiagnosis == 0` 만 200. 1·2 는 404 (개정 163)** |
| `core_diagnosis` 스키마 | 08-14 | 원문 확보 후 DDL 2종 작성. 실측 97건 · 776부위 검산 (개정 164) |

---

# ★★★ 확정된 것 — ★ 마스터가 정하신 것 (개정 669 · 한 자리에 모았다)

```
★ 08-24 — ★ 마스터 확정이 ★ 명령서 23곳에 흩어져 있었다
★ ★ 여기가 정본이다.  ★ 명령서는 ★ 가리키기만 한다 (가이드역할 ⓒ)
```

| # | 무엇 | 언제 |
|:--:|---|---|
| **1** | 상단 메뉴는 ★ **넷** — 매물 · 관심 · 현황 · 관리 | 08-23 (427·551) |
| **2** | ★ **엔카에서도 받는다.  ★ 모든 사이트에서 받는다** — ★ 407 은 회선 제약이지 차종 제약이 아니다 | 08-24 |
| **3** | ★ **한 사이트 전담으로 묶지 마라** — 겹쳐 받고 ★ `cno`(차량번호)로 가른다 | 08-24 |
| **4** | 등록 차종 ★ **18종** — 국산 10 ＋ 수입 8 (X3·GLC·Q5·NX·RX·XC60·S60·EX60) | 08-24 |
| **5** | ★ GV70 가솔린은 ★ **넣지 않는다** | 08-24 |
| **6** | 화면 — ★ **실물 사진 30장 금지 · 딜러 자유 서술 · 지도 · 전화 단추 금지** | 08-23 |
| **7** | ★ 그러나 ★ **「한 장도 금지」가 아니다** — 목록 160×90 · 상세 320×180 (눌러서 다음 · 1/10) | 08-24 |
| **8** | ★ **딜러는 상세에 남긴다.**  목록 카드에는 두지 않는다 | 08-24 |
| **9** | 0 을 ★ **`OK` · `×` · `—`** 로 — ★ `×`(없다)와 `—`(모른다)를 가른다 | 08-24 |
| **10** | 깡통은 ★ **옵션가 100만원 이하** | 08-24 |
| **11** | 총 구매비용은 ★ **상세에서만** | 08-24 |
| **12** | 가격대는 ★ **최소~최대** (예산 아님) | 08-24 |
| **13** | 필터는 ★ **기본(조밀)＋자세한 조건** · ★ 매물·관심·현황·미판정 넷에 · ★ 조건이 화면을 넘어가도 남는다 | 08-24 |
| **14** | 재판정이 안 돌면 ★ **네 시간마다 채운다** | 08-24 |
| **15** | 볼보 보증 ★ **5년 / 10만km** | 08-23 |
| **16** | 절 차례는 ★ **바꾸지 않는다** — 실제 상세의 열 절 (V11-159) | 08-24 |

```
| **17** | ★★ **내린 화면 여섯을 제자리로** — 비교→관심 · 시세→현황 · 딜러→카드 · 후보→매물 흡수 · 리포트·미판정만 관리 | 08-24 |
| **18** | ★ **엔카 robots — 지금대로 받는다** (마스터 회선으로만 · 우회 금지) | 08-24 |
| **19** | ★ **중복 매물 값은 둘 다 보여 준다** (점수는 낮은 쪽으로 계산) | 08-24 |
| **20** | ★ **제원은 복합연비·승차정원 둘만 · 상세와 비교에만** (금지 열 항목은 그대로) | 08-24 |
| **21** | ★ **차종 미정을 「범위 밖」과 「미분류」로 가른다** — 미분류는 마스터께 묻는 자리 | 08-24 |
| **22** | ★ **현대 옛 922건을 버린다** — 차종을 좁혀 받은 것만 남긴다 | 08-24 |

```
| **23** | ★★ **갈래를 안 넓힌다 — 제외한다** (GV70 가솔린 · 그랜저 가솔린 · 스포티지 가솔린 · G80 3.5T 등 **3,995건**) | 08-24 |

```
| **24** | ★★ **볼보 XC40 · V60 크로스컨트리를 대상에 더한다 — ★ 전 사이트 대상** (차종 18 → **20종**) | 08-24 |

```
| **25** | ★★★ **같은 차가 여러 사이트에 있으면 ★ 다 보여 준다** — ★ 합치지 않는다 | 08-24 |
| **26** | ★★ **새 화면 「추적」** — 중복 매물만 모아 보여 준다 (★ 관리도 괜찮다고 하셨다) | 08-24 |
| **27** | ★ **목록에는 낮은 점수** · ★ **상세에 「중복매물」 절** | 08-24 |

```
| **28** | ★★ **목록은 한 쪽에 ★ 30장** (★ 카드에서 빼는 것은 없다) | 08-26 |

```
| **29** | ★★★ **시안 `v4m_*` 여덟 장이 정본** — 모바일 기준 · 관심을 본으로 (08-26) | 08-26 |

```
| **30** | ★★ **수입은 ★ 2019년 기준** — ★ 열 종 공통 `year_range 201900..` | 08-27 |

```
| **31** | ★★ **미분류 381건 중 ★ ES300h 만 받는다** — ★ 나머지는 접는다 (차종 20 → **21종**) | 08-27 |

```
| **32** | ★★★ **21종이 아니면 ★ 수집 제외** — ★ 미분류에 넣지 않는다 | 08-28 |

```
| **33** | ★★★ **여덟 곳이 ★ 근거 없는 축에 ★ 배점의 80% 를 받는다** — 볼보·렉서스·현대·기아·BMW·K카·★ **헤이딜러**·★ **리본카** · ★ 안 주는 곳은 **엔카·KB·보배** (08-28) | 08-28 |

```
| **34** | ★★★★ **개발측이 「왜 안 되는지」를 적으면 ★ 그 문장을 그대로 옮겨 적고 답한다** (`05` ⓧ · `S46-107`) | 08-29 |

```
| **35** | ★★★★★ **회차 보고 첫 줄은 「안 한 것」 표다** — 무엇·왜·며칠째·누가 답하나 (`05` ⓨ · `S46-109`) | 08-29 |

```
| **36** | ★★★ **딜러 정직도는 ★ 910 에 안 넣는다** — ★ 셈은 하고 ★ `/dealers/{id}` 에서만 본다 (08-29) | 08-29 |

```
| **37** | ★★★★★ **마스터가 누르시는 품을 줄이는 것이 규격이다** — 휴대폰뿐 · 엔카는 마스터 회선뿐 (`UI_REVIEW` 22장) | 08-29 |

```
필수  ★ 새 확정이 나오면 ★ **여기 먼저** 적고 ★ 명령서는 가리킨다
필수  ★ 확정을 어기려면 ★ 마스터께 여쭌다 — ★ 가이드가 뒤집지 않는다
```

## ★ 42. 팔린 차·계약 중·「잘 팔렸나」·전기차 누유 (08-29)

```
마스터 원문 —
「31번, 목록에는 있으나 화면 조회 목록에는 안 보여야 돼.  다만 판매완료 목록과 통계에는 나와야 되고」
「33번은 뭐지.  잘 팔릴 차량에 대해 평가를 너희가 나에게 분석 자료를 준다는 의미이겠지」
「35번, 사실상 내가 못 사는 것이니 목록에는 안 보여야지.  다만 판매완료 목록에는 나와야지.  통계도」
「49, 50이 내가 왜?  난 전기차 누유는 제외라고 했는데」

31  팔린 차 — DB 에 남고 · /listings 에 안 나오고 · /sold 와 통계에는 나온다
33  「잘 팔렸나」는 사이트 실적이 아니라 ★ 마스터께 드리는 분석 자료다
    — 「이 차를 이 값에 사면 나중에 잘 팔리겠나」에 답한다
35  계약 중도 31 과 똑같이 — 사실상 못 사는 차다 (CONTRACT 830 · K카 resvYn Y 118)
49  성능점검 여섯 축 — ★ 마스터 몫이 아니다.  수를 재서 가이드가 정한다
50  전기차 누유 — ★ 이미 정해져 있었다.  제외 = ★ **만점** (f-table 갈래 ⑦)
    ★ 마스터 재확정 — 「왜 너 마음대로 분모를 손대니?  전기차라 누유가 없으니 만점 줘야지」
    ★ 분모는 910 그대로다.  가이드가 분모를 건드리지 않는다
```

## ★ 41. 원문은 파일에 · `raw_response` 를 없앤다 · 달라진 것만 적재 (08-29)

```
마스터 원문 — 「이 로우데이터 DB 자체를, 테이블 자체를 없애버리고 그 파일로 다 들어오게 하게.
  일단 다 만든 다음에 적재가 필요한 데이터만 목록과 비교해서 실제 목록에 신규 추가된 것이나
  가격만 변경된 것을 업데이트하는 구조로 가면 되겠지?  이미 등록된 것들은 삽입을 할 필요가 없으니까.
  더 중요한 거는 목록을 받아서 비교해서, 목록에 없는데 DB 에 있던 것은 상태를 변경해야 돼.
  각 사이트마다 계약 중, 판매 완료, 삭제되는 것들이 있어.  그 상태 정보에 체크를 해야 돼.
  지금 핵심은 DB 가 너무 커져 있고 파일도 너무 쓰고 있어서 공간 낭비가 심해.
  이런 로우 데이터들은 DB 에 탑재 말고 파일로 하고, 그 파일들은 수시로 잘 됐는지
  계속 점검할 수 있는 체계가 돼야 돼」

정본  docs/chapters/11-store/a-key.md 08-29 절 · 10-collect/00-intro.md 08-29 절
① raw_response 테이블을 없앤다.  원문은 파일에만.  ★ 옮기고 대조한 뒤에 지운다
② 넣기는 「달라진 것만」 — 새 것·바뀐 칸만.  같으면 아무것도 안 한다
③ 상태는 sales_status 에 사이트 말 그대로 — 계약 중·판매 완료·삭제를 gone 으로 뭉치지 않는다
④ 파일이 잘 쌓였는지 수시로 점검하는 화면을 만든다
```

## ★ 40. K카·리본카는 직영 보증이 있다 · 견주기의 뜻 (08-29)

```
마스터 원문 — 「케이카 사이트 보정은 원래 K카가 지금 보증이 있어서 그거는 K카가
  보증이 있다고 판단하는 게 맞을 거야.  그걸 리본카랑 K카에 같은 의미라고 봐주면 될 거 같아」

마스터 원문 — 「나는 솔직히 같은 차를 산다면 K카나 헤이딜러나 그 BMW나 리본카에서
  먼저 사고 싶고 그리고 같은 차에 대해서 앵카에서 싸게 살 수 있는 걸 찾고 싶은 것뿐이야.
  그런 관점에서 비교를 하는 것일 뿐이니까」

① K카·리본카 — 사이트 보증을 직영 보증으로 채운다 (둘을 같은 의미로)
② 견주기의 목적 둘 — ㉡ 인증에서 먼저 고르고 · 같은 차를 엔카에서 더 싸게 찾는다
③ 그러므로 같은 차 짝짓기가 심장이다.  지금 짝지어진 차가 0건이다
```

## ★ 39. 사이트를 두 갈래로 — ㉠ 검증 필요 · ㉡ 인증 기본 (08-29)

```
마스터 원문 — 「인증 중고차는 사이트 인증이 기본이기 때문에 … 앵커랑 KB차차차가
  인정이 필요하고 … 보배드림도 인정이 필요하겠네.  하지만 다른 것들은 인정할 필요 없겠지.
  그리고 하나 더 리본카는 하고 있는 거야.  리본카에 대해서도 확인이 필요해」

㉠ 검증 필요  엔카 · KB차차차 · 보배드림 · 헤이딜러(가이드 판단)
㉡ 인증 기본  현대인증 · 기아CPO · 렉서스 · 볼보셀렉트 · BMW BPS
              ＋ K카 직영(가이드 판단) ＋ 리본카(오토플러스 직영 · 규격 23·269행 확인)
★ K카·헤이딜러·리본카는 가이드 판단이다 — 다르면 마스터께서 한 줄 주신다
```

## ★ 38. 갈래 ⑥ — 사이트가 원래 안 주는 축은 **만점의 80%** (08-29)

```
마스터 원문 — 「보배에 신차가를 안 준다고 하는 것은 보배에서 진짜 신차가를 못 주더라도
  다른 사이트에 신차가로 대체가 가능하잖아.  그리고 BMW나 중고차 인증 사이트에서
  성능점검을 안 줄 수도 있고 그런 차들은 가격에 다 반영되기 때문에 가격만 보더라도
  충분하다고 판단해.  따라서 중고차 인증 사이트에서 그런 점수가 없다는 거에 대해서는
  그냥 일단은 80점을 줘도 된다고 판단해.  따라서 그 사이트에서 원래 안 주는 것들을
  나에게 보여주고, 그거에 대한 점수를 먼저 정해 달라」

정본  docs/chapters/30-score/f-table.md 「갈래 ⑥」
①  대체 가능한 축(신차가·트림)은 갈래 ③ — 다른 사이트 표에서 끌어온다
②  사이트가 원래 안 주는 것은 0점이 아니라 **만점의 80%** · 화면에 밝힌다
③  어느 사이트의 어느 축이 ⑥ 인지는 **가이드가 표로 올리고 마스터가 정하신다**
```
