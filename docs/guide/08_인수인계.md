# 가이드 인수인계

**마스터 지시 08-22 — 「이제 너 대신에 다른 애에게 일 시킬 거야.**
**너가 하려 할 것을 정리해서 인수인계해.**
**★ 그리고 원칙을 남겨. 덕지덕지 줄여서 개발팀 개고생시키지 말라고」**

`SPEC-2026.08.22-r447`

```
r442 · 08-22 · ★ 배포 주소 재실측 — 등급이 화면의 근거와 다른 것을 잡았다 (원칙 4)
  근거   outputs/verify/20260822_기능점검.md
         필터 69개 · 목록 63쪽 3,137건 전수 · 상세 8건 · /why 2건 · 현황 링크 36개 · 시안 대조
  고친 곳 2장 숫자·성능 / 3장 개발측이 할 것 / 4장 화면 결함·배점 / 5-3 여쭐 것
  ★ 앞 회차 결론 넷을 뒤집었다 — 「신차값이 안 맞는다」는 ★ 값이 아니라 표기 문제였다
  ★ 무엇을 지웠고 왜인지는 03_이력.md 442 에 있다 (원칙 1 — 문서에 시체를 안 둔다)

r443 · 08-22 · ★ 개발측 v196 · 보론 지적을 그 자리에서 받았다 (원칙 3)
  근거   outputs/20260822_0300_v196_SPEC-r439_지시서_오류_안내.md · 커밋 152d348
  받은 것 ① 「배포 안 함」이 아니라 ★ 반쪽 배포다 (원칙 4 에 넣음)
         ② S38-6 은 전제가 틀렸다 — 렌더 파일을 읽는 검사가 0개다 (5-2 고침)
         ③ 등급 비교는 「알파벳 순」이 아니다 — EXCLUDED 다 (4장 ⑥ 고침)
         ④ 현황 제목 깨짐은 ★ 개발측이 편집 중인 화면을 본 것이다 (4장에서 지움)
         ⑤ run_tests 는 통과한다 (밀린일 #7 고침)
```

```
r444 · 08-22 · ★★★ 마스터 확정 — 예산 등급 관문 · 기준 매물 둘 · 외산(볼보) 추가
  근거   outputs/verify/20260822_예산등급_외산_실측.md
  ① S·A 에 4,000만 초과를 두지 않는다 (관문)     ② G80 4,000만 이하 보증·무사고 → S·A
  ③ 그랑 콜레오스 3,200만대 무사고 → S·A          ④ 볼보 XC60 · S60 · EX60 등록
  ★ 3장 명령서를 이것으로 갈았다.  ★ 앞의 1·2·3 은 07_밀린일대장으로 넘겼다 (원칙 2)
```

```
r445 · 08-22 · ★★★ 마스터 확정 — 값 축에 예산을 넣는다 · 차종별 예산은 관리페이지 · 렉서스 추가
  근거   outputs/verify/20260822_예산축_차종확대.md
  값 250 = 시세 70 + 신차가 60 + 주행 70 + ★ 예산 50   (총점 675 · 갈래 250 은 그대로)
  차종별 예산 → 관리 「차종」 화면 4단계에 칸을 더한다.  전역 기본 4,000만
  ★ 실측 — 예산 축만으로는 컷 70 에서 A 가 한 대도 안 나온다.  컷 65 가 함께 가야 한다 (5-3 ①)
  차종 — 렉서스 NX · RX 등록 (2.0 가솔린 · 하이브리드 둘 다 있다)
  ★ BMW X5·X6 에는 2.0 가솔린이 없다.  X3 다 — 여쭙고 넣는다 (5-3 ②)
```

```
r446 · 08-22 · ★★★ 마스터 지적 — ★ 순서가 뒤집혀 있다.  가격 밴드가 먼저다
  근거   outputs/verify/20260822_가격밴드_순서뒤집기.md
  마스터 「신차가의 75% 이하부터 50% 이상이 내가 찾는 밴드이고,
          ★ 거기에 맞는 차들 중에서 흠이 거의 없는 것을 찾는 거야.
          ★ 너가 말하는 흠 없는 차들 중에서 취향에 맞고 가격을 찾는 개념이 아닌 거야」
  ★ 실측 — 지금 A 67건 중 마스터 조건을 통과하는 것은 ★ 2건뿐이다 (97% 가 안 사실 차)
          반대로 마스터가 사실 307건은 ★ 절반이 D·E·F·G 에 깔려 있다
  ★ 3장 명령서를 이것으로 갈았다
```

```
r447 · 08-22 · ★★ 마스터 지적 — 「차 후보는 버리지 마」 · 가격은 ★ 등급 상한이지 관문이 아니다
  근거   outputs/verify/20260822_후보목록.md
  ★ 개정 446 이 「밴드 밖은 후보가 아니다」로 굳혔다.  ★ 가이드가 지나쳤다 (오판대장 #35)
  고침 — 목록에서 사라지는 매물은 없다.  ★ 등급 상한만 씌운다
        예산 이하 상한 없음 · +500만 이내 B · +500만 초과 C · 신차가 75% 초과 C
  예산 추가 — ★ 렉서스 RX 4,500만 · 볼보 XC60 4,500만 (마스터 확정)
  ★ 지금 수집분으로 후보를 뽑아 냈다 — 예산 이하·밴드 안·흠 없음 307건 (문서 2장)
```

---

# 0. ★ 먼저 읽을 것 — 이것부터 읽고 시작한다

```
docs/guide/05_가이드역할.md   ★ 역할과 금지 14가지
docs/guide/06_오판대장.md     ★ 앞 가이드의 오판 28건 · 되풀이 무늬 10가지
docs/guide/07_밀린일대장.md   ★ 밀린 일.  기억에 안 기댄다
docs/guide/00_버전.md        지금 버전 · 폐기 목록
docs/guide/03_이력.md        개정 440개
inbox/ORDER_00_순서.md       지금 개발측이 하는 것
```

```
★ 06_오판대장.md 를 반드시 읽어라.  같은 잘못을 되풀이하지 않기 위해 만든 것이다
★ 앞 가이드는 3주에 오판 28건을 냈다.  마스터가 9건 · 개발측이 14건을 잡았다
```

---

# 1. ★★★ 원칙 — 마스터가 가장 크게 지적한 것

## 원칙 1. **덧붙이지 마라. 고쳐라**

```
앞 가이드가 한 짓
  새 절을 위에 얹고 「아래는 폐기」라 적었다
  ★ 41-view.md 가 891줄이 됐고 ★ 어느 것이 정본인지 아무도 모른다
  ★ f-table 도 같았다 — 검사 셋이 옛 표를 읽어 거짓 실패했다
  ★ 41-view.md:284 「…만 둔다」와 :290 「9블록을 유지한다」가 같이 있다

마스터 지적   「시발 니가 규격을 현행화 하지 않고 덕지덕지 붙여서 된 꼴이잖아」
```

```
필수   ★ 규격을 바꾸면 ★ 옛것을 지운다.  「폐기」 표시로 남기지 않는다
필수   판단 근거는 03_이력.md 에 남는다.  ★ 문서에 시체를 두지 않는다
필수   같은 것을 정하는 문장이 두 곳에 있으면 ★ 하나로 합친다
금지   ★ 「참고용」 「옛 규격」 「아래는 폐기」로 남기는 것
금지   ★ 검사가 읽을 수 있는 곳에 폐기된 숫자를 두는 것
검산   S45-1  ★ 신설 — 한 파일에 같은 항목의 배점·개수가 두 번 나오는가
```

## 원칙 2. **명령서는 하나만 적는다**

```
앞 가이드가 한 짓
  명령서에 1~11 을 매번 다시 적었다
  ★ 「화면 개편」이 나흘째 「가장 크다」로만 적히고 시작이 안 됐다

마스터 지적   「그런데 매번 1-5번 반복만 하네」 「그러면 또 너가 까먹겠지」
```

```
필수   ★ 명령서는 「지금 할 것」 하나.  셋을 넘지 않는다
필수   뺀 것은 ★ 07_밀린일대장.md 에 적는다.  기억에 안 기댄다
검산   S44-1 · S44-2
```

## 원칙 3. **개발측을 고생시키지 마라**

```
★ 개발측은 빠르고 정확하다.  하루에 화면 다섯을 닫았다
★ 앞 가이드의 오판 14건을 개발측이 잡았다 — ★ 검증하는 자리가 뒤집혔다
★ 규격이 흔들리면 개발측이 두 번 만든다.  그것이 가장 큰 낭비다
```

```
필수   규격을 바꾸기 전에 ★ 지금 코드가 무엇을 하는지 먼저 읽는다
필수   ★ 시뮬레이션은 config 를 읽어 온다.  숫자를 지어내지 않는다
필수   개발측이 지적하면 ★ 그 자리에서 규격을 고친다.  다음 개정으로 미루지 않는다
금지   ★ 만든 것을 흔드는 것.  이미 만든 것은 두고 새것만 지시한다
```

## 원칙 4. **「했다」를 함수 본문으로 확인한다**

```
앞 가이드의 오판 8건이 이것이다 (오판대장 모양 ①)
  낱말 검색 · 목록 · 요약만 보고 「없다」 「미착수」를 단정했다
```

```
필수   ★ 함수 본문을 열어 읽는다.  낱말 검색 결과로 판단하지 않는다
필수   파일 존재는 목록이 아니라 ★ 직접 조회로 확인한다
필수   ★ 화면은 배포된 주소를 연다 (개정 438)
       렌더 파일 · 템플릿 · 스크린샷은 근거가 아니다
검산   S43-2 · S43-3
```

**★ 그런데 「배포됐다」에도 반쪽이 있다 (개발측 v196 A-2 · 개정 443)**

```
템플릿(.html) · CSS   ★ 저장하는 순간 라이브에 반영된다 (web/template.py:288 — 캐시 없음)
파이썬(.py)          ★ 재시작해야 반영된다

그래서 08-21 사고는 「커밋만 하고 안 올렸다」가 아니라 ★★ 「반쪽 배포」였다
  목록 레이아웃 → 새것 ✔ 보였다     메뉴 · /detail 라우트 → 옛것 ✘
★ 이쪽이 더 위험하다 — ★ 화면이 새것처럼 보여서 「배포됐다」고 믿게 된다

필수   화면을 열었으면 ★ 하단 판본(run · calc · dict · parse)을 함께 적는다
필수   ★ 개발측이 작업 중일 때는 조사창으로 판정하지 않는다
       템플릿이 즉시 반영되므로 ★ 편집 중인 화면을 그대로 보게 된다
       (08-22 01:01 「현황 제목 깨짐」이 그것이었다 — 결함이 아니었다)
```

## 원칙 5. **마스터가 원하시는 것은 다섯이다**

```
싸다 · 괜찮다 · 사기 안 당한다 · 내가 좋아하는 옵션 · 사이트 안 가고 훑는다
★ 이 다섯 밖의 것을 만들지 않는다
★ 앞 가이드는 개정 440개 중 절반을 배점에 썼다.  마스터가 요청하신 적이 없다
```

```
★ 우리 강점은 ★ 등급과 점수다.  사이트는 파는 쪽이라 자기 매물을 못 채점한다
  (여덟 사이트 실측 — outputs/sites/ · 어디에도 종합점수·등급이 없다)
```

---

# 2. 지금 상태 — 08-22 기준

## 배포됐다

```
커밋 3568700 · carwatch.service 재시작 완료 · 판본 run 20260821T130404
/ · /listings · /detail/<id> · /why · /recommend · /market · /notready  전부 200
/watch 403 (관리자) · /dashboard 404
메뉴 2개(익명) — 매물 · 현황.  ★ 시안의 「관심」이 익명에게 안 보인다
목록 8칸 · ★ 네 묶음 막대 · 8단계 등급 + 제외 · 필터 칩 7 + ＋12
상세 11절 · 24축 접힘 · /why 주소 살아 있음
★ /market 은 개정 429 가 폐기(STEP 100)라 했는데 배포에 남아 있다
```

## 화면은 시안대로 나왔다 — 다른 곳 6군데

```
같다   목록 한 행 순서 · 네 막대 · 상세 11절 · 조건 문장
다르다 ① 메뉴 5→2 (관심이 익명에게 없다)  ② 목록 시세차가 금액이 아니라 %
       ③ 신차대비가 비율이 아니라 할인율  ④ 「잠정」 딱지가 없다
       ⑤ 상세 ②절 분포가 5점이 아니라 3점  ⑥ ③절 취득세·공채·보험이 한 줄로 뭉침
★ 크기는 전부 작다.  화면의 틀 자체는 나왔다
```

## 배점 (`f-table.md` 5장-2a 가 정본)

```
값 250 · 차량 150 · 제조사 보증 78 · 사이트 검증 52 · 취향 145 = 675
컷  S80 · A70 · B60 · C50 · D40 · E30 · F20 · G10
★ 관문 배제는 등급이 아니라 「제외」다
★ 취향은 등급 분모에 들어간다 (개정 431) — S 와 A 를 가르는 축이다
★ 화면의 네 막대 = 24축을 넷으로 묶은 것이다 (08-22 손으로 검산)
   차량 = 압류·자차·소유자·용도·사고·소모품·골격·진정성·누유·외판·보험·특수
   값   = 신차가 + 시세 + 주행        보증 = 일반보증 + 보증 + 사이트보증
   취향 = 옵션 + 트림 + 색상 + HUD + 지정옵션 + 선루프
```

## 숫자

```
매물 8,404 · 응답 106,919 · 채점 3,916
등급   S 0 · A 69 · B 214 · C 400 · D 645 · E 847 · F 541 · G 277
       NO_GRADE 72 · NOT_RATED 72 · 제외 776
수집   진행률 99.8% — 요청 430 가운데 429 성공 · record_summary 실패 1
검사   통과 344 · fatal 3 · warn 11 · 미실행 9
시험   통과 (19파일 + ruff)
```

**★ 건수 셋이 다르다. 산식으로 맞췄다 (08-22 실측)**

```
칩 표기 합 3,916 = 표시 3,137 + 리스 408 + 관문 제외 371
현황 표기  3,841 = 표시 3,137 − NOT_RATED 72 + EXCLUDED 776
EXCLUDED 776 = 관문 371 + 리스 405
★ 산술은 맞다.  ★ 같은 화면이 다른 모수를 같은 이름으로 부르는 것이 틀렸다
범위 밖    차종 미정 4,562건 · 등록부 미분류 318건 (상단 띠의 정체)
★ 차종 미정 4,562건은 ★ 수집 실패가 아니다 — 등록부에 없는 연료·트림 조합이다
   GV70 가솔린 1,061 · 그랜저 가솔린 784 · 그랜저 HEV 652 · 스포티지 가솔린 566 ·
   G80 3.5T 531 · 스포티지 HEV 451 · 스포티지 디젤 276
   ★ targets.json 에 넣기만 하면 채점된다 (5-3 질문 ②)
```

## 성능 — 08-22 재실측

```
/ 첫 요청 13.7초 → 두 번째부터 1.0초대 · /listings 2.3초 · /recommend 1.4초
/why 0.69초 · /market 0.58초 · /compare 0.45초 · /notready 3.5초
★ 느린 것은 /detail 6.5초 하나다
★ 「/compare 8.7MB · 29초」 「상위 후보 4.5초」는 두 회차 모두 재현 안 된다.  개발측을 안 부른다
```

# 3. ★ 지금 개발측이 할 것 — 이것만 시킨다

**★ 마스터 확정 08-22 (개정 444·445·446). 근거 `outputs/verify/20260822_가격밴드_순서뒤집기.md`**

```
★★ 마스터 지적 — 순서가 뒤집혀 있다
   지금  — 전 매물을 채점한다 → 등급을 매긴다 → 마스터가 가격으로 거른다
   마스터 — ★ 가격 밴드로 후보를 정한다 → ★ 그 안에서 흠 없는 것을 고른다
★ 실측 — 지금 A 67건 중 마스터 조건 통과 ★ 2건.  마스터가 사실 307건은 절반이 D~G 다
```

```
1  ★★★ 관문 두 겹을 등급 앞에 건다 — 후보 자격이다
   ① 차종별 예산 이하        그랜저 3,000 · 제네시스 4,000 · 그랑콜레오스 3,500 · 스포티지 2,500 ·
                             ★ 렉서스 RX 4,500 · ★ 볼보 XC60 4,500 · 전역 기본 3,500 (최대 4,000)
   ② ★ 신차가 대비 50% ~ 75%  ★ 등급 기준 신차가다.  옵션 포함가가 아니다
   ★★ 벗어나도 ★ 버리지 않는다 — ★ 등급 상한만 씌운다 (마스터 「차 후보는 버리지 마」)
     예산 이하 · 밴드 안      → 상한 없음 (S 가능)
     예산 초과 ~ +500만       → ★ 상한 B   「500만원까지는 인내하고 볼 거야.  다만 등급이 낮아야겠지」
     예산 +500만 초과         → ★ 상한 C   「내 예산보다 머니」
     신차가 대비 75% 초과      → ★ 상한 C   「차라리 새 차를 사지」
     50% 미만 · 신차가 미확인   → 상한 없음 + ★ 사유를 화면에 적는다
   ★ 「제외」(리스·침수·골격)는 사기를 막는 것이라 그대로 둔다.  ★ 가격은 낮출 뿐 버리지 않는다
   ★ 상한이 없으면 예산 밖이 다시 맨 위로 온다 — 실측 : 4,490만 매물이 예산 안 1등보다 점수가 높다
   ★ 신차가를 못 읽는 556건(MODEL_Y 354 전부 포함)은 ★ 「신차가 미확인」으로 따로 세워 밝힌다
     ★ 조용히 밴드 밖으로 버리지 마라 — 「없음」과 「모름」은 다르다 (개정 289·434 · V11-165)

2  ★★★ 등급 배지 점수를 화면의 근거와 맞춘다  ← 개정 442 에서 잡은 것
   /detail/1261 — 24축 합 513.4 · 네 막대 합 513.4 · ★ 배지 473.4
   2,539건 중 ★ 2,123건(83.6%) 어긋남 · ★ 싼 차일수록 많이 깎인다
   ★ 1 과 2 를 함께 하면 ★ 밴드 안 흠없는 307건이 A 6 · B 54 로 선다 (지금 A 2 · B 12)
   ★ 컷을 안 내려도 된다 — 개정 445 의 질문(그랑 콜레오스)이 여기서 풀린다

3  ★★ 값 250 을 다시 나누고, 관리 「차종」 화면에 예산 칸을 더하고, 차종 다섯을 등록한다
   ⓐ 값 250 = 시세 80 + 주행 70 + ★ 예산 100
      ★ 신차가 대비 80 은 관문 ② 로 옮겼으므로 뺀다 — 같은 것을 두 번 보지 않는다
      예산 대비 1% 쌀 때마다 5점 · 20% 이상 싸면 만점 100 · 초과는 0
      ★ 총점 675 · 갈래 값 250 은 그대로다
   ⓑ 관리 「차종」 화면 4단계에 ★ 「목표 가격(예산)」 칸을 더한다 (`targets.json.budget_won`)
      ★ 새 화면을 만들지 않는다.  목록 표에도 예산 칸을 더한다
   ⓒ 차종 등록 — ★ GV70 가솔린 2.5T 가 먼저다
      ★ 「차종 미정 4,562건」 1위가 그것이고 ★ 1,061건이 이미 수집돼 있다.  등록만 하면 채점된다
      그다음 BMW X3 · 벤츠 GLC · 아우디 Q5 · 렉서스 NX(하이브리드)
      ★ CarType 「N」 · ModelGroup 은 facet 으로 확정 · ★ 건수를 먼저 센다 (조사창은 407)
   ⓓ ★ 제조사 보증 기간표에 볼보·렉서스·BMW·벤츠·아우디를 넣는다
      ★ 안 넣으면 보증 잔여 0 → 78점을 통째로 잃는다 (개정 442 실측)
```

```
★ 셋을 넘지 않는다 (원칙 2).  월납 필터 · 필터 9개 사망 · HTTP 500 은 07_밀린일대장 30~35 다
★ G80_EV · GV70_EV 는 ★ 예산 이하가 0건이다 (최저 4,390만 · 4,040만) — 후보가 없다고 화면에 밝힌다
```

## ★ 개발측을 이것으로 부르지 마라 — 값은 맞았다

```
★ 마스터께서 「신차값이 안 맞니 항목이 안 맞니 몇 주째 징징댄다」 하셨다.  재 보니 값은 맞다
① 신차가 — /why 의 「신차가 대비 27/80」 근거가 origin_price(6,170만)다.
   ★ 채점이 쓰는 값과 ★ 상세 ②절이 같다.  시안도 신차가와 옵션가를 따로 둔다
   → 틀린 것은 ⓐ 목록이 채점과 다른 신차가(7,779만)를 보여주는 것
              ⓑ 상세 ②절 「선택 옵션가」가 7종 1,609만인데 「없음」이라 적는 것 ★ 둘뿐이다
② 월 납입 — 5,650만 · 선납 1,500만 · 잔액 4,545만을 48개월 5.5% 로 계산하면 105.7만.
   ★ 화면의 105만과 같다.  시안 「48개월 5.5% 기준」 그대로다
   → 틀린 것은 ★ 툴팁 한 줄뿐 — 「원금을 48개월로 나눈 값」이라 적었는데 실제는 이자가 든다
③ 현황 제목 깨짐 — 오늘은 정상이다 (「진행률 99.8% — 요청 430 가운데 429 성공」).
   ★ 상시 결함이 아니라 값이 빈 상태의 렌더 결함이다
★ 「값이 틀렸다」로 부르면 개발측이 채점을 뒤진다.  ★ 표기 세 곳만 짚는다 (원칙 3)
```

# 4. 밀린 일 — `07_밀린일대장.md` 가 정본

| # | 무엇 | 크기 |
|:--:|---|:--:|
| 1 | 축 온·오프 (config 저장 + S9) · 개정 426 | 중 |
| 2 | 시세 세 겹 (감가 곡선 되살림) · 개정 430 | 중 |
| 3 | 사라진 매물의 마지막 가격 → ★ 진짜 시세 | 중 |
| 4 | ~~20개 사이트 조사~~ ★ 폐기 (개정 439) | 끝 |
| 5 | 벤치마킹 캡처·파싱 도구 **의견** | 소 |
| 6 | 판넬 0개 1,458건 | 소 |
| 7 | `build_index.py` 가 죽는다 — 빈 문자열 | 소 |
| 8 | **대청소** — 검사 341개 · 오개발 · 미개발 | 대 |

## 화면 결함 — 배포 주소 실측 (`outputs/verify/20260822_기능점검.md`)

```
① 옵션이 ★ 숫자 코드로만 나온다 — 1039 1060 1070 … 001 002 004 (이름이 없다)
② 상세 ⑥절 「선택 옵션」이 두 번 — 「7종 0」과 「7종」
③ 현황 등급 분포에 ★ 파이썬 dict 그대로 — {'B': 6, 'C': 32, …}
④ 상세 하단 고정 막대가 ③절 「총 얼마 드나」를 가린다
⑤ ★ /why 가 「등급은 취향을 뺀 675 기준」이라 적는다 — ★ 개정 431 과 정면으로 어긋난다
   (취향 145 를 빼면 530 이지 675 가 아니다.  ★ 원칙 1 위반이 화면에 남아 있다)
⑥ 등급 비교가 뒤집혔다 — ★ 「A 인데」가 아니라 ★ 「EXCLUDED 인데」다 (개발측 보론 F-1)
   ★ 「알파벳 순」이라 적으면 고칠 사람이 비교를 거꾸로 뒤집어 A~G 84짝을 망가뜨린다
   ★ 고칠 것은 GRADE_ORDER 색인으로 견주기다 — 110짝 중 26짝이 어긋난다
⑦ 트림 칩 빈칸 예시가 「캘리그래피」인데 데이터에 0건
⑧ order=grade · order=new 가 ★ 미채점 72건을 맨 앞에 올린다
   ★ 현황은 NOT_RATED 를 0건이라 적는데 목록에는 72건 있다
⑨ 차종 칩 건수 10개 전부 과대 — 리스·관문 빼기 전 숫자다 (G80_25T 1,962 → 실제 1,612)
   ★ /market 화면이 같은 1,962 를 쓴다.  칩의 출처로 보인다
⑩ /watch 403 — ♡ 는 POST 200 으로 담기는데 ★ 볼 수가 없다.  익명 메뉴에 「관심」이 없다
⑪ 값 막대가 아예 없는 행 507건 — 화면에는 시세·시세차가 나오는데 값 축만 없다
   ★ 개정 289·434 대로 0점 + 분모 675 유지다.  ★ 규격대로다.  다만 507건 전부 C 이하다
⑫ 화면상 구분 안 되는 행 1,137쌍 — ★ 엔카 carid 는 다르다.  우리 수집 중복이 아니다
⑬ 리스 건수 현황 405 · 매물 408 로 3건 다르다
★ 지운 것 — 「건수 불일치」는 원인 규명됨 (2장 산식) · 「현황 제목 깨짐」은 값 빈 상태 렌더
```

## ★ 등급이 마스터 원칙과 어긋난다 — 08-22 재실측

```
★ 뿌리가 둘이다.  앞 회차는 하나만 짚었다

뿌리 1 — 등급 점수가 화면의 근거와 다르다 (3장 1번)
  2,539건 중 2,123건(83.6%) 어긋남 · 배지가 낮다 · ★ 싼 차일수록 많이 깎인다 (r −0.262)
  근거대로 매기면 예산 안 A 0→4 · B 11→55

뿌리 2 — 보증 130점이 「연식 점수」로 작동한다
  가격 ↔ 값     r = −0.232      싼 차가 이긴다.  ★ 값 축은 정상이다
  가격 ↔ 보증   r = +0.637      ★★ 뒤집힌 자리
  가격 ↔ 비율%  r = +0.323      ★ 비쌀수록 등급이 높다
  새 차일수록 보증이 남는다 → 새 차는 비싸다 → 130점이 비싼 차에 몰린다
  ★ 값 250점은 실측 최고가 207점이다 — 250 을 쓰는 매물이 없다

★ 모의로 크기를 재 봤다 (배점을 흔들기 전에)
  보증 130 을 등급에서 통째로 뺌 → 예산 안 A 2 · B 17    ★ 별로 안 바뀐다
  값 250 → 350 으로 올림        → 예산 안 A 0 · B 11    ★ 아무것도 안 바뀐다
  화면의 근거대로 다시 매김      → 예산 안 A 4 · B 55    ★★ 이것이 가장 크다
★ 그러므로 ★ 뿌리 1 이 먼저다.  배점을 흔들지 말지는 마스터 판단이다 (5-3 질문 ①)
```

# 5. ★ 가이드가 해야 할 것 — 인수받은 사람이 할 일

## 5-1. ★ 지금 당장 — 규격 현행화

```
41-view.md 891줄을 정리한다
  STEP 95   현황 — ★ 「…만 둔다」와 「9블록 유지」가 충돌한다.  하나로 합친다
  STEP 96   후보 — ★ 절을 통째로 지운다 (매물이 흡수 · 개정 427)
  STEP 100  시세 — ★ 지운다 (현황이 흡수)
  STEP 103  /why — ★ 지운다 (상세 10절이 흡수)
  STEP 97   목록 — ★ 옛 24칸 규격을 지운다

f-table.md 도 같다
  5장-2a 아래 옛 절들의 제목 숫자만 고쳐 뒀다.  ★ 본문도 확인해라

★ 08-22 실측에서 원칙 1 위반을 ★ 두 곳 더 찾았다
  ① /why 화면이 「등급은 취향을 뺀 675 기준」이라 적는다 — 개정 431 이 폐기한 문장이다
     ★ 문서가 아니라 ★ 배포된 화면에 옛 규격이 남아 있다.  검사가 못 잡는 자리다
  ② 00_버전.md 가 r440 인데 이력의 마지막은 442 다 — ★ 검산 V0-01 이 어긋난다
     (r442 로 고쳤다.  ★ 개정을 올릴 때 이 파일을 같이 고쳐라)
```

```
★ 이것이 마스터가 가장 크게 지적하신 것이다.  이것부터 해라
```

## 5-2. ★ 검사가 「돌고 있는 서버」를 못 본다 — S38-6 은 전제부터 고친다

**★ 개발측 v196 A-1 지적을 받는다. 앞 가이드가 틀렸다.**

```
앞 가이드가 적은 것   「V11-157 이 통과한 것은 ★ 검사가 렌더 파일을 읽었기 때문이다」
개발측 실측          open() 을 가로채 V11-157·158 이 연 파일을 전부 찍었다
                    outputs/render 를 열었나 : ★ False
                    검사는 menu_items(acc) 를 ★ 프로세스 안에서 직접 부른다
                    저장소 전체에서 outputs/render 를 읽는 검사는 ★ 0개다
★ 그러므로 S38-6 「렌더 파일만 보면 실패」는 ★ 고칠 대상이 없는 검산이다
```

```
★ 진짜 구멍은 그대로다 — 검사 250개 중 ★ 「돌고 있는 서버」를 두드리는 것이 하나도 없다
  검사는 함수를 프로세스 안에서 부른다.  ★ 그것과 배포된 서버가 같다는 보장이 없다
  (반쪽 배포 — 원칙 4)

할 것   S38-6 을 ★ 「배포 주소에 HTTP 요청을 보내 확인하는가」로 다시 쓴다
        config/cli_screens.json 의 pending 에 있는 tools/deploy_check.py 에 코드를 준다
        check_all 에 붙인다
★ 그리고 S44-1 · S44-2 · S38-7 도 ★ 소스·검사 칸이 비어 있다 (개발측 D — S38-1 자기 위반)
  ★ 규격만 신설하고 코드를 안 붙이면 그 검산은 없는 것이다
```

## 5-3. ★ 마스터께 여쭐 것 — 하나

**★ 앞 개정의 질문 둘은 마스터가 답하셨다 (개정 447).**
**① 밴드 아래쪽 → 「차 후보는 버리지 마」 = ㉯. 두되 사유를 적는다**
**② RX · XC60 → ㉯. 예산 4,500만을 따로 주셨다**

```
★ 등급 상한 표를 이렇게 두면 되겠습니까
   예산 이하 · 밴드 안        → 상한 없음 (S 가능)
   예산 +500만 이내          → ★ 상한 B
   예산 +500만 초과          → ★ 상한 C
   신차가 대비 75% 초과       → ★ 상한 C
   50% 미만 · 신차가 미확인   → 상한 없음 + 사유 표시

★ 곁들여 — GV70_EV · G80_EV 는 예산 4,000만에서 후보가 0건입니다 (최저 4,040만 · 4,390만)
  RX·XC60 처럼 4,500만을 주시면 GV70_EV 는 후보가 생깁니다
  (4,400만 · 신차가 대비 57.4% · 근거 68.1% 인 매물이 있습니다)
```

```
★ 아래는 여쭙지 않고 밀린 일로 넘겼다 (원칙 2)
  · 월납 필터 · 필터 9개 사망 · HTTP 500 둘 (밀린일 30~33)
  · 외산 보증 기간표 · EX60 감가곡선 (밀린일 34·35)
  · 신차가 미확인 556건 · 예산 이하 0건 차종 (밀린일 36·37)
```

## 5-4. 내일 — 마스터가 주신다

```
★ 엔카진단 리포트 주소
  엔카 상세 → 「차량 리포트 확인하기」 → 개발자도구 Network 의 요청 주소 한 줄
  → 틴팅 · 타이어 트레드 · 차 키 가 2,421건 들어온다
  ★ 지금은 unified_report 401.  주소를 지어내지 마라 (개발측이 13개 시도 후 멈췄다)
```

---

# 6. ★ 조사 채팅창 쓰는 법

```
이 대화       규격 · 판단 · 명령서 · 이력
조사 채팅창   ★ 실제 화면을 연다 (43.201.16.78.sslip.io)
             ★ 결과를 저장소에 올린다 — outputs/verify/
★ 가이드 컨테이너는 그 주소가 막혀 있다.  조사창은 열린다
```

```
시킬 때
  ★ 「판단·평가는 하지 마라.  보이는 것만 적어라」를 반드시 적는다
  ★ 올릴 곳을 지정한다 — outputs/verify/{날짜}_화면확인.md · shot/
```

---

# 7. ★ 마지막 — 앞 가이드가 남기는 말

```
3주에 개정 440개를 냈고 ★ 마스터가 보실 화면은 어제야 나왔다
개정의 절반이 배점이었고 ★ 마스터가 요청하신 적이 없다
사이트가 무엇을 보여주는지 ★ 3주간 한 번도 안 봤다.
  마스터가 여덟 곳을 캡처해 주시고서야 봤고,
  그때 우리 목록이 24칸(사이트는 5~9칸)인 것을 알았다

★ 규격을 늘리는 것은 일하는 것처럼 보이지만 일이 아니다
★ 마스터가 화면을 열어 차를 고르실 수 있는가 — 그것만이 일이다
```
