# 밀린 일 대장

**★ 명령서에서 뺀 것을 여기 남긴다. 가이드가 잊어도 파일이 남는다.**

`SPEC-2026.08.22-r498`

```
왜 만드나   명령서에 1~11 을 매번 다시 적으니 ★ 5번 화면이 나흘째 안 시작됐다
           앞의 1~4 가 계속 새로 생겨 뒤로 밀린다
           → 명령서는 ★ 지금 할 것 하나만 적고, 나머지는 여기 남긴다
근거       마스터 지적 08-21 「그런데 매번 1-5번 반복만 하네」
          「그러면 또 너가 까먹겠지」
★ 가이드는 잊는다 (오판대장 모양 ⑥).  기억에 기대지 않는다
```

---

# 0. ★★★ 지금 당장 — ★ 가이드가 할 것 셋 (마스터 지시 08-22)

```
마스터 지시 08-22 — 「그 지시를 그놈에게 시키게 해」
★ 검토에서 나온 것 중 ★ 개발측이 아니라 ★ 가이드가 고쳐야 할 것만 여기 적는다
★ 새 지시서 파일을 만들지 않았다 — ★ 만들면 명령서가 넷이 된다 (원칙 2)
```

## 0-1. ★★ `inbox/` 의 옛 명령서 여섯을 지운다 — ★ 이것이 제일 크다

| 파일 | 머리 버전 | 지금 |
|---|---|---|
| ★ **`inbox/ORDER_00_순서.md`** | ★ **`SPEC-2026.08.21-r434`** | r497 — ★ **개정 63개 전** |
| `inbox/ORDER_unknown_split.md` | `r434` | 〃 |
| `inbox/ORDER_review424.md` | `r424` | 〃 |
| `inbox/ORDER_panels.md` | `r418` | 〃 |
| `inbox/ORDER_sites20.md` | 버전 없음 | ★ 개정 439 로 **폐기된 「20개 사이트 조사」** |
| `inbox/ORDER_value_lease_filter.md` | 버전 없음 | 〃 |

```
★★ `ORDER_00_순서.md` 는 「★ 다음 순서」라고 적혀 있다 — ★ 개발측이 여는 파일이다
   내용이 「등급 컷 8단계 · 취향 145 재배분 · 화면 전면 개편」 — ★ 675·850 시대 지시다
★★ 그리고 ★ 인수인계 0장 121행이 새 가이드에게
   「inbox/ORDER_00_순서.md ← ★ 지금 개발측이 하는 것」이라 ★ 읽으라고 한다
   ★ 새로 오는 가이드가 ★ 어제 것을 오늘 것으로 안다

할 것
  ① `inbox/ORDER_*.md` ★ 여섯을 지운다.  ★ 「폐기」 표시로 남기지 않는다 (원칙 1)
  ② 살릴 내용이 있으면 ★ 이 대장(2장)이나 ★ 지금 명령서에 ★ 옮긴 뒤 지운다
  ③ 인수인계 0장 121행의 ★ 안내 줄을 고친다
     `inbox/ORDER_00_순서.md  지금 개발측이 하는 것`
     → ★ `outputs/ORDER_2026MMDD_rNNN.md  지금 개발측이 하는 것 (★ 늘 하나다)`
검산  ★ S44-2 를 ★ `inbox/` 까지 세게 고친다 — ★ 0-3
```

## 0-2. ★ 이 대장 1장 「지금 하는 것」을 지운다

```
지금 여기에  「★★ 화면 전면 개편 — 41-view STEP 97·97a · 진행」
실제 명령서  `outputs/ORDER_20260822_r493.md` — ① 재산정 ② 차종 확대 ③ 다른 사이트

★★ 「지금 할 것」을 ★ 두 곳이 다르게 선언한다.  ★ 원칙 2 위반이다
★ 이 대장은 ★ 「밀린 것」만 적는 곳이다.  ★ 「지금 할 것」은 ★ 명령서 하나가 정한다
할 것  1장을 지우고 ★ 화면 전면 개편을 ★ 2장(밀린 것)으로 내린다
```

## 0-3. ★★ 검사 S44-2 가 ★ `inbox/` 를 안 센다 — ★ 지키는 척한다

```
`validate/v0_guide.py:91`
    got = sorted((ROOT / "outputs").glob("ORDER_*.md"))
    if len(got) > 1: ...

★ `outputs/` 만 센다.  ★ 지금 거기 하나뿐이라 ★ 통과한다
★★ 그런데 ★ `inbox/` 에 ★ 여섯이 있다.  ★ 그중 하나는 「다음 순서」를 선언한다
★ 검사가 ★ 원칙 2 를 ★ 지키는 척하고 있다 — ★ 사각을 남긴 채 통과한다

할 것  ★ `inbox/` 와 `outputs/` 를 ★ 함께 센다.  ★ 합쳐 ★ 하나여야 한다
       ★ 고친 뒤 ★ 돌려서 ★ 실패가 나는지 본다 (원칙 4 — 「했다」를 본문으로 확인)
```

```
★ 검사 다섯(S43-2·S43-3·S44-1·S44-2·S45-1)은 ★ 개정 496 에서 ★ 진짜로 구현됐다.
  ★ 가이드가 독립으로 다시 돌려 확인했다 — ★ S45-1 은 f-table 12개 절을 ★ 실제로 잡는다
★ 남은 것은 ★ S44-2 의 사각 하나다
```

---

# 1. 지금 하는 것

| 무엇 | 지시 | 상태 |
|---|---|:--:|
| **★★ 화면 전면 개편** | `41-view` STEP 97·97a · 시안 | **진행** |

```
★ 이것이 끝날 때까지 아래를 명령서에 넣지 않는다
```

---

# 2. 밀린 것 — 화면 다음

| # | 무엇 | 지시서 · 개정 | 크기 | 왜 밀렸나 |
|:--:|---|---|:--:|---|
| 1 | **축 온·오프** (config 저장 + S9) | 개정 426 · 마스터 확정 「B」 | 중 | |
| 2 | **시세 세 겹** (감가 곡선 되살림) | 개정 430 | 중 | ★ 갈래 ④ 450건을 이게 푼다 |
| 3 | **사라진 매물의 마지막 가격** | — | 중 | ★ 진짜 시세의 재료 |
| ~~4~~ | ~~20개 사이트 조사~~ | ★ **폐기 — 개정 439** · 마스터와 여덟 곳을 이미 뜯었다 | — | **끝** |
| 5 | 벤치마킹 캡처·파싱 도구 **의견** | `ORDER_review424` §3-4 | 소 | |
| 6 | 판넬 0개 1,458건 | `ORDER_panels.md` | 소 | `diagnosis` 3,302건 미요청 |
| ~~7~~ | ~~`run_tests` 실패 1건~~ | ★ **끝 — 08-22 실측 통과** (19파일 + ruff · 개발측 v196 C-1) | — | **끝** |
| 8 | **대청소** — 검사 341개 · 오개발 · 미개발 | 마스터 지시 08-21 | **대** | 「소스가 누더기」 |
| 9 | ★★ **「확인 안 됨」을 셋으로 가른다** — ① 딜러가 안 올림 ② 원문에 있는데 우리가 못 찾음 ③ 우리가 안 받음 | ★ **`inbox/ORDER_unknown_split.md` 에서 옮김** (개정 434 · 08-22 삭제) | **대** | ★ **24축 전부** · 원문을 직접 열어 가른다(S43-②) · ②는 원문 조각을, ③은 까닭을 함께 낸다 · **「모르겠습니다」도 답이다**. ★ **미확인 30.4% 중 얼마가 우리 부실인지 모르는 채로 컷을 내리면 우리 부실을 컷으로 덮는 것이 된다** |
| 10 | **틴팅 리포트** | ★ 마스터가 주소를 주신다 | 소 | ★ `ORDER_00_순서.md` 에서 옮김 |

---

# 2a. ★ 화면 검수에서 나온 결함 — 현황이 끝나면 지적한다

**가이드가 `outputs/render/detail_listing_id.html` 을 열어 실측 · 08-21**

| # | 어디 | 무엇 | 근거 |
|:--:|---|---|---|
| 1 | 상세 11절 | **등급 비교가 뒤집혔다** | ★ **「EXCLUDED 인데」다** (개발측 보론 F-1). A~G 끼리 84짝은 **지금 맞다** — 110짝 중 26짝만 어긋난다 |
| 2 | 상세 11절 | **「왜 나은지」가 등급으로 돌아간다** | 네 건 중 셋이 「등급이 높습니다」뿐 — ★ 등급을 등급으로 설명한다 |

```
★ 1 은 ★ 「알파벳 순」이 아니다 (08-22 정정).  고칠 것은 ★ GRADE_ORDER 색인 비교다
   ★ 거꾸로 뒤집으면 지금 맞는 84짝이 망가진다 — ★ 틀린 진단을 개발측에 보내지 마라
★ 2 는 규격이 「왜 나은지 한 줄」인데 순환이다
   좋은 예 — 「1,214만 쌉니다」 · 「주행이 적습니다」
   나쁜 예 — 「등급이 C 로 높습니다」
★ 지금 끊으면 마지막 화면(현황)이 밀린다.  ★ 현황이 끝난 뒤 지적한다
```

---

# 3. ★ 내일 — 마스터가 주신다

```
틴팅 리포트 주소   ★ 마스터가 엔카 상세에서 「차량 리포트 확인하기」를 누르고
                 개발자도구 Network 의 요청 주소 한 줄
영향             state.consumable 3,841건 (7점) · 틴팅 · 타이어 트레드 · 차 키
지금 상태         unified_report 401 · reportExists=true 2,421건
                 ★ tire_tread_mm 은 죽은 필드다 (읽는 곳만 있고 쓰는 곳이 없다)
금지             ★ 주소를 지어내는 것.  개발측이 13개 시도 후 멈춘 것이 옳다
```

---

# 4. ★ 화면 안에서 함께 고칠 것

```
「모름」을 「없음」으로 내는 것 (개정 435)
  report/screens/build.py:150 이 value 로 가른다 → ★ source 로
  ★ 15,709건이 지금 「없음」으로 나온다
  O 있음 · · 없음 · ★ ? 모름 — 셋을 다른 기호로
  V3-95 · V3-96
★ 따로 하지 않는다.  화면 고치는 김에 같이 한다
```

---

# 5. ★ 고칠 것 — 갈래 ②③④ (개정 435)

| | 갈래 | 축 | 건수 | 회복 | 무엇 | 언제 |
|--:|:--:|---|--:|--:|---|---|
| 1 | ③ | `state.consumable` | 3,841 | ≤26,887 | 리포트 주소 | ★ 내일 |
| 2 | ④ | `value.market` | 450 | 16,200 | 시세 세 겹 | 밀린 것 #2 |
| 3 | ② | `state.integrity` | 675 | 675 | ★ `boardStateType` 을 읽는다 | 작다 |
| 4 | ② | `history.not_join` | 7 | 91 | `openData` 를 읽는다 | 작다 |

```
★ 3·4 는 파서 한 줄이다.  화면 중에 짬이 나면 끼워도 된다
★ 「계기판 교환은 원문에 없다」 주석이 키를 잘못 본 것이다 —
  mileageStateType 은 800건 전부 null · ★ boardStateType 은 757/800 에 값
```

---

# 5-2. ★ 명령서에서 뺀 것 — 08-22 실측분 (원칙 2)

**근거 `outputs/verify/20260822_기능점검.md`. 명령서에는 1·2·3 만 올렸다.**

| # | 무엇 | 크기 | 왜 뺐나 |
|:--:|---|:--:|---|
| 1 | `/watch` 403 — ♡ 는 POST 200 으로 담긴다 | 소 | 담기기는 한다 |
| 2 | 차종 칩 건수 10개 과대 (리스·관문 전 숫자) | 소 | 표기만 틀리다 |
| 3 | 상세 ②절 「선택 옵션가」가 7종 1,609만인데 **「없음」**이라 적는다 | 소 | ★ **신차가는 안 틀렸다** — `/why` 의 `origin_price` 가 상세와 같다. **목록의 7,779만이 채점과 다른 값**이다 |
| 4 | 현황 NOT_RATED 0건 표기 (실제 72건) | 소 | |
| 5 | 리스 건수 405 / 408 불일치 | 소 | |
| 6 | 옵션이 숫자 코드로만 (1039 · 001 …) | 중 | ★ **원인 규명됨 (개정 487)** — 이름은 `dict_model_option`(1,474행)에 **이미 있고** 화면이 `dict_option_code.display` 만 읽는데 **거기엔 코드가 그대로** 들어가 있다. `build_dict.py:209·306` 이 `display=value` 로 넣는다. ★ **화면이 두 표를 합쳐 읽으면 오늘 고쳐진다** |
| 7 | 상세 ⑥절 「선택 옵션」 두 번 표기 | 소 | |
| 8 | 현황 등급 분포에 파이썬 dict 그대로 | 소 | |
| ~~9~~ | ~~현황 제목 깨짐~~ | ★ **결함이 아니었다** — 08-22 01:01 은 **개발측이 현황을 편집 중이던 화면**이다. 템플릿이 즉시 반영된다 (v196 A-2 · 보론 J-④) | — | **끝** |
| 10 | 상세 하단 고정 막대가 ③절을 가린다 | 소 | |
| 11 | `/detail` 5.1~6.5초 | 중 | 나머지는 1초 안쪽. ★ `/` 첫 요청만 13.7초 (두 번째부터 1.0초) |
| 12 | ＋조건 「사이트」 드롭다운 빔 · 트림 예시 「캘리그래피」 0건 | 소 | ★ `site=` 값 자체는 살아 있다 (`site=xxx` → 0건). **고를 목록만 안 채운다** |
| 13 | 값 막대 없는 행 507건 (시세 미산출) | 중 | 밀린 일 #2 와 같은 뿌리 |
| 14 | 화면상 구분 안 되는 행 1,137쌍 (carid 는 다름) | 중 | 우리 결함이 아니다 |
| 15 | ★ `/why` 가 「등급은 **취향을 뺀 675 기준**」이라 적는다 | 소 | ★ **개정 431 이 폐기한 문장이 배포 화면에 남아 있다** (원칙 1 위반 · 취향을 빼면 530 이다) |
| 16 | 목록 신차가 표기(7,779만)가 **채점 기준(6,170만)과 다르다** | 소 | 위 3번과 한 뿌리 |
| 17 | 총 구매비용 툴팁 「원금을 48개월로 나눈 값」 | 소 | ★ **실제 계산은 48개월 5.5%** — 숫자는 시안대로 맞다. **문구만 고친다** |
| 18 | 상세 ③절 취득세·공채·보험이 「이전등록비」 한 줄로 뭉침 | 소 | 시안은 줄줄이 낸다 |
| 19 | 상세 ②절 분포가 3점(최저·중앙·최고) | 소 | 시안은 5점(+25% · 75%) |
| 20 | 목록 시세차가 금액이 아니라 % · 신차대비 부호가 시안과 다름 | 소 | 시안 대조 08-22 |
| 21 | 목록에 「**잠정**」 딱지가 없다 | 소 | 시안은 상세 미완을 잠정으로 낸다 |
| 22 | `/market` 이 배포에 남아 있다 (개정 429 폐기 대상) | 소 | ★ 차종 칩 과대 건수 1,962 와 같은 모수를 쓴다 |
| 23 | ★ 현황 「3,841건 · 전 차종」이 **뺀 건수를 안 밝힌다** | 소 | ★ 개발측 보론 J-⑤ — 건수를 맞출 일이 아니라 **한 줄 붙일 일**이다 (`V11-165`) |
| 24 | `V11-157` 본문 「셋」 vs 검산 「넷을 넘는가」 | 소 | ★ 메뉴를 넷으로 늘려도 통과한다 (v196 B-1) |
| 25 | `tools/build_index.py` 가 죽는다 — 빈 문자열 | 소 | ★ `CHECKS.md` 색인이 35줄 어긋났는데 **고칠 도구가 없다** (v196 C-4) |
| 26 | 밀린일 §5 갈래 **② 표가 낡았다** — ② 는 전 축 **0건** | 소 | 개정 437 이 정정했는데 표를 안 고쳤다 (v196 C-2) |
| 27 | `value.market` 회복 **16,200 vs 45,000** — 어느 쪽이 정본인가 | 소 | 36 의 근거를 못 찾았다 (v196 C-3) |
| 28 | `S44-1`·`S44-2`·`S38-7` 이 **소스·검사 0건** | 중 | ★ `S38-1` 자기 위반 (v196 D) |
| 29 | `inbox/ORDER_00_순서.md` 에 항목 **10개 + 틴팅** | 소 | ★ `S44-1` 위반 (v196 E-1) · 판본도 r434 로 낡음 |
| 30 | ★ **월납 필터가 가격 필터로 환산된다** — `monthly_max` ×75 = `price_max` | 중 | ★ 개정 444 로 명령서에서 뺐다. 화면에 「월 40만」인 18건이 빠진다 |
| 31 | ★ **필터 9개 사망** — `option_min`·`year_from` 무시 · `price_max` 단위 · 죽은 링크 4 | 중 | ★ 개정 444 로 명령서에서 뺐다 (r442 3장) |
| 32 | ★ **HTTP 500 둘** — `?region=` · `?honesty_min=` | 소 | 〃 |
| 33 | `order=grade`·`order=new` 가 미채점 72건을 맨 앞에 올린다 | 소 | 〃 |
| 34 | ★ **볼보 제조사 보증 기간표**(5년/10만km) 를 넣지 않으면 78점을 통째로 잃는다 | 중 | ★ 개정 444 · 외산 등록과 함께 본다 |
| 35 | ★ **EX60 은 감가 곡선 표본이 없다** — 값 축이 빈다 (지금 507건과 같은 꼴) | 소 | ★ 개정 444 |
| 36 | ★ **신차가를 못 읽는 매물 556건** — MODEL_Y 354건은 전부 | 중 | ★ 개정 446 · **밴드 관문을 걸 수가 없다.** 「신차가 미확인」으로 따로 세워 밝힌다 |
| ~~37~~ | ~~`G80_EV`·`GV70_EV` 예산 이하 0건~~ | ★ **끝 — 개정 448 로 제네시스 EV 예산이 4,500만이 되어 GV70_EV 12건 · G80_EV 2건 · GV60 50건이 후보가 됐다** | — | **끝** |

```
★ 「/compare 8.7MB · 29초」는 ★ 밀린 일이 아니라 지운 것이다 — 재현되지 않는다
★ 「상위 후보 6쿼리 4.5초」도 재현 안 된다 — /recommend 0.85초 · 08-22 재실측 1.41초
★ ★ 「등급 배지와 화면 근거 불일치」는 ★ 밀린 일이 아니라 ★ 명령서 1번이다 (인수인계 3장)
```

---

# 6. 적는 규칙

```
필수   ★ 명령서에서 뺀 것은 반드시 여기 적는다
필수   ★ 명령서는 「지금 할 것」 하나만 적는다.  1~11 을 다시 적지 않는다
필수   끝나면 지우지 말고 ★ 「끝」으로 표시하고 날짜를 적는다
필수   ★ 새 일이 생기면 여기에 먼저 적고, 지금 것을 밀지 말지 판단한다
금지   ★ 가이드 기억에 기대는 것.  「나중에 하자」를 말로만 남기는 것
검산   S44-1  ★ 신설 — 명령서에 적힌 항목이 셋을 넘는가 (넘으면 실패)
       S44-2  ★ 신설 — 밀린 일 대장에 없는 것이 명령서에 있는가
```

| ~~38~~ | ~~f-table 479행 이후가 675 판~~ ★ **절 제목·갈래 머리는 910 으로 갈았다 (개정 492).  ★ 본문 예시 숫자만 675 로 남았고 「방법은 같다」를 못 박았다** | 소 | ★ 같은 파일에 두 규격이 있다. 배점 숫자·분모·사이트검증 단계표가 **둘**이다. **개정 489 로 경계는 못 박았으나 본문은 910 으로 안 옮겼다** |
| 39 | 검사 **V3-90 이 「등급 분모 675 고정」** | 중 | ★ 배포는 `/why` 「/910」이다. **개정 454 에서 검사를 함께 안 고쳤다** |
| 40 | ★ `/why` 가 「등급은 **취향을 뺀 910 기준**」이라 적는다 | 중 | ★ 개정 431 과 어긋난다. **인수인계 4장 ⑤ 가 「지운다」고 적어 둔 그 문장인데 숫자만 910 으로 바뀌었다** |
| ~~41~~ | ~~한 사유로 두 번 깎는다~~ ★ **끝 — 개정 491 감점 상한 확정** · — 골격 26/43 + 판금 −54 · 용도 0/22 + 렌트 −67 · 자차미가입 2/18 + −54 · 사이트보증 36 만점인데 판매자등록 −27 | **대** | ★ **합 −202 = 총점의 22%.** f-table 감점 절이 **「축과 겹치는가」를 한 줄도 안 적었다 — 규격 구멍이다** |
| 42 | ★ `dict_option_code.display` 에 **코드가 그대로** 들어가 있다 | 중 | ★ 이름은 `dict_model_option`(1,474행)에 있다. **화면이 두 표를 합쳐 읽으면 된다** (개정 488) |
| 43 | ★ `f-table` 본문 ★ 산문 안의 옛 숫자 | 소 | ★ 제목·표는 다 910 으로 갈았다(개정 495). ★ 산문 예시만 남았다 |
| 44 | ★★ `config/scoring.json` 에 ★ `state.year`·`value.budget` 이 **없다** | **대** | ★ 검사 **S43-2** 가 잡는다. ★ 넣기 전까지 ★ 연식 80 · 예산 95 가 채워지지 않는다 |
| 45 | ★ 목록에 ★ **사이트 출처가 안 나온다** | **대** | ★ 배포 실측 — `site` 드롭다운이 「전체」뿐 · 행에 꼬리표 없음 · 목록 SQL 에 `site` 없음. ★ 다른 사이트가 붙는 순간 구분 못 한다 |
| 46 | ★ 다사이트 목록 화면 ★ 시안이 없다 | 중 | ★ 시안 24개는 ★ 엔카 하나일 때 만든 것이다 |
