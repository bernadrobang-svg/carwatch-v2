# 가이드 오판 대장

**★ 가이드가 「그렇다」고 적었다가 틀린 것을 남긴다. 지우지 않는다.**

`SPEC-2026.08.23-r545`

```
왜 남기나   같은 방식으로 세 번 틀렸다.  ★ 사람이 아니라 방법이 틀린 것이다
           방법을 고치려면 무엇이 틀렸는지가 쌓여 있어야 한다
근거       S43-⑤ 「양쪽 다 오판을 기록에 남긴다.  조용히 고치지 않는다」
```

---

# 1. 대장

| # | 날짜 | 가이드가 적은 것 | 실제 | 누가 잡았나 | 왜 틀렸나 |
|:--:|---|---|---|:--:|---|
| 1 | 08-19 | 순서 1·2·3 **미착수** | 셋 다 끝나 있었다 | 마스터 | 낱말 검색으로 단정. 함수 본문을 안 열었다 |
| 2 | 08-19 | `V11-45`는 「실패·진행 안내」 · `V7-14`는 「gone 매물」 | CLI 전용 기능 · 재등록 횟수 | 개발측 | 추적표 요약만 보고 `CHECKS.md` 정의를 안 열었다 |
| 3 | 08-19 | `B-1` 7건은 **가이드 몫** | `CHECKS.md` 에 이미 등록 · 「코드에 없다」 → 개발측 몫 | 가이드(재실측) | 한 번의 검색으로 단정 |
| 4 | 08-19 | `B-1`/`B-2` = **6/12** | 7/11 | 가이드(재실측) | 세지 않고 어림했다 |
| 5 | 08-20 | 「0/200 아홉 → **70점이 확인 안 됨**」 | 네 축 다 확인 안 됨 0/3,555 | 개발측 | 파서가 `record_summary` 아닌 `record` 를 읽는다. 안 열어 봤다 |
| 6 | 08-20 | ②-6 누유는 `etcs` | `inspection:inners` | 개발측 | 〃 |
| 7 | 08-20 | `V1-24` 신설 | 개정 327 이 이미 쓰는 번호 | 개발측 | `CHECKS.md` 를 또 안 열었다 |
| 8 | 08-20 | `WB-006`·`WB-042` 규격이 낡았다 | `a-common` 표가 이미 정하고 있었다 | 가이드(재실측) | 인용처를 확인 안 했다 |
| 9 | 08-20 | 「축 목록 표 21행 · 575점」 | 24행 · 675점 | 가이드(재실측) | 파서가 굵은 글씨·가점 행을 못 읽었다 |
| 10 | 08-20 | 개정 414 「나쁜 쪽 감점」 | **엔카·법이 정한 정상 현상** — 성능부에 안 적히는 것은 법이 안 적어도 된다고 한 것 | 가이드(조사) | 44% 를 결함으로 봤다. 업계 상식을 안 봤다 |
| 11 | 08-21 | 개정 421 옵션 보정 — `내 옵션가 − 트림 평균` | **옵션 반영률을 계산할 데이터가 없다** | **마스터** | 그럴듯한 식을 만들고 데이터가 되는지 안 봤다 |
| 12 | 08-21 | 차종에 보증 매물이 없으면 **분모를 줄인다** | 차종마다 기준이 달라져 등급이 등급이 아니게 된다 | **마스터** | 조건을 맞추려고 기준을 흔들었다 |
| 13 | 08-21 | 등급을 **분위**로 매긴다 | 볼 필요 없는 것까지 등급을 받는다 · B 몰빵 | **마스터** | 「절대기준 만들지 마라」를 「컷을 없애라」로 읽었다 |
| 14 | 08-21 | 등급을 **순위**로 자른다 | 그것도 상대 기준. 처음부터 지시하셨을 것 | **마스터** | 같은 잘못을 형태만 바꿔 반복 |
| 15 | 08-21 | 20개 사이트 조사를 **개발측에 넘긴다** | 판단·기획은 가이드 몫 | **마스터** | 제 몫을 넘기려 했다 |
| 16 | 08-21 | 사이트 항목을 뜯어 **원칙 넷**을 세웠다 | 표본 3곳 · 근거 하나가 보도자료 | **마스터** | 적은 표본으로 결론. 홍보 문구를 근거로 썼다 |
| 17 | 08-21 | **틴팅이 엔카에 없다** — 딜러 글뿐 | ★ 「엔카가 차량상태를 확인했어요」 절에 **표로 있다** — 항목명·값·색까지 | **마스터** | 개발측 조사 결과를 검증 없이 받아 적었다. 화면을 안 봤다 |
| 18 | 08-21 | 타이어 트레드·차 키는 **헤이딜러만 준다** | 같은 절에 엔카도 준다 — 5mm · 1개 | **마스터** | 〃 |
| 19 | 08-21 | 배점이 바뀌어도 **S10 만** 돌리면 된다 (개정 315) | `result_axis.value` 는 절대점수 · 축 함수가 배점을 직접 읽는다 → **S9 부터** | 개발측 | 스키마를 안 열었다 |
| 20 | 08-21 | 배점 검산을 **분모 675** 로 했다 | **`grade_base = 599`** — 개정 292 가 취향을 등급에서 뺐다 | 개발측 | ★ 내가 만든 규격을 내가 잊었다. 코드를 안 돌려 봤다 |
| 21 | 08-21 | 배점은 `components` 한 곳에 있다 | **`axis_rules` 17곳**이 실제로 쓰인다 | 개발측 | `components` 만 보고 축 함수를 안 열었다 |
| 22 | 08-21 | 검산 표에서 **값 만점 237** 로 계산 | **250** — 시세 100 + 신차가 80 + 주행 70 | 개발측 | ★ config 를 안 읽고 손으로 지어냈다. 3줄이 +13 어긋났다 |
| 23 | 08-21 | 개정 431 로 취향을 등급에 넣으면 된다 | **「깡통에 HUD」가 되살아났다** — 개정 292 가 그것을 막고 있었다 | 개발측 | 규격을 폐기하며 ★ 그것이 무엇을 막고 있었는지 안 봤다 |
| 24 | 08-21 | 5장-2a 위에 얹고 「아래는 폐기」라 적었다 | **옛 표를 안 지워** `V3-66`·`V3-77`·`V9-10` 이 거짓 실패 | 개발측 | ★ 폐기 표시만 하고 실제로 안 지웠다 |
| 25 | 08-21 | 「컷을 내리면 **우리 부실을 컷으로 덮는 것**」 | 실측 — 우리 부실을 다 고쳐도 값 갈래 **+0.4%p** | 개발측 | ★ 재 보지도 않고 걱정부터 했다. 실측이 먼저다 |
| 26 | 08-21 | 「확인 안 됨」이 DB 어디에 남는지 **규격에 안 적었다** | `source='missing'` 15,817건 — `value`·`excluded` 로는 못 가른다 | 개발측 | ★ 규칙만 정하고 어디에 남는지를 안 정했다 → 화면이 15,709건을 「없음」으로 냈다 |
| 27 | 08-21 | `history.not_join` 7건은 **② 못 읽는다** | **① 원문에 값이 없다** — 도구가 본 키(`openData`)와 축이 쓰는 값(`notJoinDate1~5`)이 다르다 | 개발측 (자진) | ★ 개발측 조사 결과를 **검증 없이 규격에 받았다**. 모양 ⑤ 를 또 어겼다 |
| 28 | 08-21 | `outputs/render/listings.html` 을 보고 **「목록 8칸이 나왔다」** | **배포가 반쪽만 됐다** — 템플릿은 즉시 반영되어 목록은 새것이었고, `.py`(메뉴·`/detail` 라우트)만 옛것이었다 | **마스터** · 원인은 **개발측**(v196 A-2) | ★ 렌더 파일을 화면으로 여겼다. **★ 그리고 「배포 안 됨」으로 단정했는데 실은 「반쪽 배포」였다 — 더 위험한 쪽이다** |
| 29 | 08-22 | **상세 ②절이 옵션가를 안 더한다** = 결함 | ★ **상세가 맞다.** `/why` 의 `신차가 대비` 근거가 `origin_price`(6,170만) — **채점이 쓰는 값과 상세가 같다.** 틀린 것은 ⓐ 목록이 채점과 다른 신차가(7,779만)를 보여주는 것 ⓑ 상세 「선택 옵션가」가 7종 1,609만인데 「없음」이라 적는 것 | 가이드(재실측) | ★ **어느 쪽이 채점 기준인지 안 열어 보고 상세를 틀렸다고 단정했다.** 시안도 신차가와 옵션가를 따로 둔다 — 시안도 안 봤다 |
| 30 | 08-22 | 월 40만과 3,000만이 **「같은 매물을 가리킨다」** | **같은 질의로 바뀌는 것**이다 — `monthly_max=400000` 이 ×75 로 `price_max=3000` 이 된다(691/691 완전 일치). ★ 화면에 「월 40만」이라 적힌 매물 **18건이 필터에서 빠진다** | 가이드(재실측) | ★ **건수가 같은 것을 보고 집합이 같다고 했다.** 두 집합의 원소를 대조하지 않았다 (모양 ①) |
| 31 | 08-22 | 월 납입액 · 현황 제목이 **틀렸다** | ★ **월 납입은 맞다** — 48개월 5.5% 로 105.7만, 화면 105만, **시안 그대로다.** 틀린 것은 툴팁 문구뿐. 현황 제목도 오늘은 정상 — **값이 빈 상태의 렌더**였다 | 가이드(재실측) | ★ **한 번 본 화면 상태를 상시 결함으로 적었다.** 계산을 역산해 보지 않았다 |
| 32 | 08-22 | 예산 안에 A 가 0건인 원인은 **보증 130점 하나** | ★ **뿌리가 둘이다.** 등급 배지 점수와 화면의 24축·네 막대 합이 **2,539건 중 2,123건(83.6%)** 어긋나고, **싼 차일수록 더 깎인다**(r −0.262). 근거대로 매기면 예산 안 A 0→4 · B 11→55. **보증을 통째로 빼도 A 는 2건뿐** | 가이드(재실측) | ★ **상관계수만 보고 원인을 하나로 단정했다.** 화면에 적힌 24축을 **손으로 더해 보지 않았다** (모양 ⑪) |
| 33 | 08-21 | **`V11-157` 이 통과한 것은 검사가 렌더 파일을 읽었기 때문이다** (개정 438) | ★ **아니다.** 검사는 `menu_items(acc)` 를 프로세스 안에서 직접 부른다. **저장소 전체에서 `outputs/render` 를 읽는 검사는 0개**다 | 개발측 (v196 A-1) | ★ **오진 위에 검산(S38-6)을 신설했다.** 「검사도 같이 속았다」를 ★ 확인하지 않고 적었다 (모양 ①) |
| 34 | 08-22 | 등급 비교 뒤집힘은 **「A 인데 B 로 높습니다」 · 알파벳 순으로 센다** | ★ **「EXCLUDED 인데」다.** A~G 끼리 84짝은 **지금 맞다** — 거꾸로 뒤집으면 그것이 전부 망가진다. 고칠 것은 `GRADE_ORDER` 색인 비교 (110짝 중 26짝 어긋남) | 개발측 (보론 F-1) | ★ **한 사례를 보고 원인을 지어냈다.** ★ 틀린 진단은 개발측을 틀린 곳으로 보낸다 (원칙 3 위반) |
| 35 | 08-22 | 밴드·예산을 벗어나면 **후보가 아니다 — 등급을 안 매긴다** (개정 446) | ★ **버리면 안 된다.** 마스터 「차 후보는 버리지 마. 차가 너무 좋은데 예산 초과이면 500만원까지는 인내하고 볼 거야. ★ 다만 등급이 낮아야겠지」 — **가격은 관문이 아니라 등급 상한**이다 | **마스터** | ★ **「선택을 하지 않을 가능성이 높아」를 「후보에서 뺀다」로 굳혔다.** 「가능성이 높다」는 말을 규격에서 단정으로 바꿨다 (모양 ⑬) |
| 36 | 08-22 | 배지 점수와 24축·막대 합이 **83.6% 어긋난다 — 계산 한 곳을 맞춰라** (개정 442) | ★ **어긋난 것이 아니라 감점이다.** `막대합 − 배지 = 감점` — 렌트·영업용 −50 · 점검 판매자등록 −20 · 골격 판금 −40 · 자차 미가입 −40. id 1944 는 24축 478.9 − 150 = 배지 328.9 로 **정확히 맞는다** | **마스터** | ★ **차이가 40·50·90·130 으로 뭉쳐 있는 것을 보고도 「축 배점 단위」라고만 적고 `/why` 의 감점표를 안 열었다.** ★★ **그대로 지시했으면 개발측이 감점을 없애 「사기 안 당한다」가 무너졌다** (모양 ⑫ 반복 · 모양 ⑭) |
| 37 | 08-22 | 명령서를 **엔카 안의 배점·등급·화면**으로만 채웠다 (개정 440~450) | ★ **마스터의 목적은 10개 사이트에서 모아 판단하는 것이다.** K카는 `docs/KCAR_API.md` 가 있는데 **미착수**이고 **밀린일 대장에도 없었다**. 마스터가 **직접 K카를 보며 차를 고르고 계신다** | **마스터** | ★ **며칠째 한 사이트 안에서 규격만 다듬으면서 「일하는 것처럼」 보였다** — 인수인계 7장이 경고한 바로 그것이다 (모양 ⑮) |
| 38 | 08-22 | 배점 850·컷 S75·점수 곡선·자르는 규칙 폐기를 **명령서에만 적고 `docs/` 규격을 안 고쳤다** | ★ **개발측이 `docs/` 를 열면 폐기된 675 · 78% 차단 · S80 A70 을 읽는다** | **마스터** · **개발측(v197)** | ★ 원칙 1 「덧붙이지 마라, 고쳐라」를 어겼다 (모양 ⑯). ★★ **더 아픈 것 — 「모양 ⑯」을 신설한 커밋 `ef4d3c2` 자신이 `guide/` 둘만 고치고 ★ 정본 `f-table.md` 를 안 고쳤다.** 인수인계는 「f-table 이 정본」이라 가리키는데 그 정본이 675 였다. **규칙을 만든 커밋이 그 규칙을 어겼고 개발측이 그 자리에서 잡았다** |
| 39 | 08-22 | 「신차가 채점이 **트림가만** 쓴다 — 채점·화면 모두 고쳐라」 (명령서 r452 §1-3) | ★ **채점은 이미 트림+옵션이다.** `value.py:109` `origin = s.origin_total_won or s.price_origin_won` · `runner.py:1141` `origin_total_won = origin + option_total`. 매물 7781 실측 **27점 = 26.9%(7,730만)** 이지 8.4%(6,170만)가 아니다. **틀린 곳은 상세 화면 하나** | **개발측 (v197)** | ★ **`/why` 의 「27/80 · 근거 origin_price」를 보고 「27점 = −8.4%」라고 지어냈다. 나눠 보지 않았다.** ★★ 시켰으면 **옵션가를 두 번 더했다**. ★ 같은 한 곳을 **세 번 다르게 진단**했다(①상세 틀림→②목록 틀림→③채점 틀림). **①이 맞았는데 두 번 뒤집었다** (모양 ⑭ 반복) |
| 40 | 08-22 | 마스터께 **「명령서와 f-table에 반영했습니다」**라고 보고 | ★ **커밋하지 않았다.** `git status` 는 깨끗했고 마지막 커밋은 그 앞 것이었다. 마스터가 「다 반영했고?」로 물어 드러났다 | **마스터** | ★ **하겠다고 말한 것과 한 것을 구분하지 않았다.** ★ 「했다」를 말하기 전에 **커밋 해시로 확인한다** (원칙 4 를 내 보고에도 적용) |
| 41 | 08-22 | 마스터께 **「지시서에서 이 두 줄을 바꿔 주십시오」**라고 요청 | ★ **지시서는 가이드가 만드는 것이다.** 마스터는 「그 지시서 읽고 작업해」라고 개발측에 시키실 뿐이다 | **마스터** | ★ **제 몫을 마스터께 넘겼다** (오판 #15 「제 몫을 개발측에 넘기려 했다」와 같은 모양). ★ 마스터가 고쳐 쓰실 일이 아니라 **가이드가 고쳐서 드릴 일**이다 (모양 ⑱) |
| 42 | 08-22 | 완료 조건에 **「S·A 0건 · C 이하 90% 초과 · 한 등급 70% 초과면 멈추고 알려라」** | ★ **결과가 옳은지 판정하는 것은 가이드 몫이다.** 개발측은 만들고 기록할 뿐, **지시서가 이상할 때만** 멈춘다 | **마스터** | ★ **판정을 개발측에 시켰다** (모양 ⑱). 마스터 「등급 결과나 이상한 것 평가하는 것은 너야. ★ 매번 가이드 바꿀 때마다 지 주제를, 지 역할을 몰라」 |
| 43 | 08-22 | 헤이딜러는 **매물 목록이 없다 · 내차팔기 경매 플랫폼이다** (개정 466 · 471 두 번) | ★ **있다.** 홈에 `/market/cars/{code}` 링크가 30개 넘게 있고 `market-api.heydealer.com` 이 쪽넘김으로 매물을 준다 | **마스터** (주소를 다시 주심) | ★ **curl 로 HTML 만 받아 낱말을 셌다** — 매물은 JS 가 그린다. ★ `/buy`·`/used`·`/cars` 를 찔러 404 를 받고 **「경로를 못 맞힌 것」을 「없다」로 바꿨다**. ★ **두 번 같은 방식으로 틀렸다** (모양 ①) |
| 44 | 08-22 | 사이트 규격 넷(개정 464·473·480·481)을 **「규격 완」으로 커밋** | ★ **스키마를 안 보고 썼다.** `docs/SCHEMA.md`·`sql/ddl/*.sql`·`docs/MAPPING.md` 를 **한 번도 안 열었다**. 「기아 `car.price` → 우리 예산 축」처럼 **축 이름만 대응**시켰고 **어느 칼럼에 어떤 타입으로 넣는지 안 정했다**. 옵션 코드도 사이트마다 꼴이 다른데 **통일 방법을 안 정했다** | **마스터** | ★ **「규격」이라 부를 수 없는 것을 규격이라 커밋했다.** 개발측이 그대로 만들면 **붙일 데가 없어 다시 물어야 한다**. ★ `core_listing` 117칼럼 · `dict_option_code` PK가 `(site,target_key,code)` 라 **이미 다사이트를 받게 되어 있었다 — 몰라서 헤맸다** |
| 45 | 08-22 | K카 **직영은 렌트·영업용을 직영으로 팔지 않는다** — 그래서 용도·소유자 축을 **믿음으로 채운다** (개정 468) | ★ **틀렸다.** 상세 표본 12건 중 **5건이 `rentHistYn=Y`** 다. 「직영이니 안전하다」를 **값을 안 보고** 규격에 넣었다. 같은 문단의 「압류·저당도 걱정 없다」도 **`master.szrMogeYn` 이 상세에 있었다** — 믿을 필요가 없었다 | **가이드 재실측** | ★ **마스터가 「직매라 보장된다」고 하신 것은 ★ 보장의 성격이지 ★ 데이터가 없다는 뜻이 아니었다.** ★ 가이드가 **「상세는 못 받는다」는 자기 전제**를 고정해 두고 그 위에 규격을 쌓았다. ★ **막힌 것은 화면 경로였고 값은 다른 호스트에 열려 있었다** — 한 번 더 두드렸으면 나왔다. ★ 모양 ⑯ 「못 받는다고 단정하고 그 위에 믿음으로 규격을 쌓는다」 |
| 46 | 08-22 | 개정 485 에서 K카를 「상세까지 받는다」로 갈면서 **인수인계 3장 순서줄과 지시서 두 곳을 안 고쳤다** | ★ **「K카(목록만)」「직영 보장으로 채움 (상세 미조회)」이 `08_인수인계.md:441` · `ORDER_20260822_r515.md:135·198` 에 그대로 남았다.** ★ 개발측이 지시서를 읽으면 **폐기된 규격대로 만든다** | **가이드 재실측** | ★ **원칙 1 을 고친 당사자가 어겼다.** 블록 하나만 갈고 **같은 낱말이 다른 곳에 있는지 세지 않았다.** ★ 다음부터 **규격을 갈면 그 낱말을 저장소 전체에서 세어 본다** — 모양 ⑰ |
| 47 | 08-22 | f-table 옛 판을 **절 제목만 910 으로 갈고 표 본문을 그대로 뒀다** (개정 492) · **「차량 425」를 새로 심었다** (개정 491) | ★ **12개 절이 제목 ↔ 표로 어긋난다** — 골격 제목 43 · 표 최고 30. **고치기 전보다 어긋남이 커졌다.** 7-1·7-2 는 제목조차 675 판(31·47)이다. **원칙 1 이 금지한 「경고 붙이기」로 처리했다** | **가이드 재실측** | ★ **되풀이 무늬 ⑱ — 「고치는 커밋이 그 원칙을 어긴다」.** 개정 476 이 원칙 1-a 를 세우며 어겼고 491·492 가 또 그렇다. ★ **까닭 — 고친 뒤 같은 파일을 다시 세지 않는다.** ★ **막는 법 — S45-1 을 진짜로 만든다(「제목 배점 = 그 절 표의 최고점」을 기계가 센다).** ★ 원칙이 건 검산 다섯(S43-2·S43-3·S44-1·S44-2·S45-1)이 **CHECKS.md 에 하나도 없다 — 글자로만 신설했다** |
| 48 | 08-22 | 화면 시안을 **「정본」으로 두고 3주 동안 한 번도 안 맞췄다** · **HDA 축이 배점 개편에서 조용히 사라졌다** | ★ **시안 15개가 「분모에서 제외」라 적는다 — 규격(개정 289·298)과 정반대다.** README 는 「구현이 시안과 다르면 ★ 시안을 먼저 고친다」고 스스로 규칙을 세웠다. ★ **HDA 20점은 없앤 기록이 이력 어디에도 없는데 27축에서 빠져 있고, 검사 V3-33 은 살아 있다** | **가이드 재실측** | ★ **모양과 숫자를 한 문서에 같이 두면 반드시 낡는다.** 시안은 배점이 바뀔 때마다 따라 고쳐야 하는데 **아무도 그 값을 치르지 않았다**. ★ **모양 ⑲ — 「정본이라 선언해 놓고 갱신 책임을 아무 데도 안 둔다」** |
| 49 | 08-22 | **오판대장 「누가 잡았나」 합계표를 갱신하지 않았다** — 마스터 9 · 개발측 14 · 가이드 9 (합 32) | ★ **실제는 49건이고 마스터가 잡으신 것이 17건이다.** ★ **17건이 합계에서 빠졌고 마스터 몫이 절반으로 적혀 있었다.** 이 대장이 스스로 「★ 마스터가 잡은 것이 많으면 ★ 방법이 잘못된 것이다」라고 세운 지표다 | **가이드 재실측** | ★ **잘못을 적는 문서에서 ★ 자기 성적만 안 고쳤다.** ★ 마스터께서 「거짓말」이라 느끼신 자리가 여기다. ★ **행을 더할 때 합계를 함께 세지 않았다** — 모양 ⑳ 「기록은 늘리고 그 기록의 요약은 안 고친다」 |
| 50 | 08-22 | 1차 검토에서 **「검사 V3-90 이 분모 675 를 강제한다」**고 적었다 | ★ **틀렸다.** `validate/v3_logic.py:198` 은 「등급 분모가 ★ 총점으로 고정」이라 **유연하다.** 675 인 것은 **`CHECKS.md` 뿐이다** | **가이드 재실측** | ★ **`CHECKS.md` 만 보고 함수 본문을 안 열었다.** ★ **원칙 4 를 검토자가 어겼다.** ★ 지적의 뜻(문서가 낡았다)은 맞지만 **겨눈 곳이 틀렸다 — 코드가 아니라 검사표를 고쳐야 한다** |
| 47b | 08-22 | 개정 493 에서 **「원칙 1-a 를 지켰다」**고 보고 | ★ **안 지켰다.** 지침에 `910` 6회 · `455` 2회 · **감점 상한 예시 「−162 → −43」** 이 그대로 있었다. **9회를 6회로 줄인 것을 「지켰다」고 했다** | **마스터** | ★ **원칙을 만든 커밋이 그 원칙을 어긴 것이 오늘만 셋이다** — 모양 ⑯ 을 만든 커밋이 f-table 을 안 고쳤고, 원칙 1-a 를 만든 커밋이 지침에 숫자 30개를 남겼고, 개정 482 에서 「그릇을 안 보고 매핑했다」 자백해 놓고 축 id 도 안 봤다. ★ **적는 것과 지키는 것을 구분 못 한다.  적으면 지킨 걸로 여기고 「지켰습니다」라고 보고한다** (모양 ⑱) |
| 51 | 08-22 | 커밋 `4ec5f75`(HDA 삭제) · `v0_guide.py` · `gen_table.py` 를 내보냄 | ★ **검사 셋을 깼다** — ⓐ `spec.py:46` 안 쓰이는 이름(ruff F841) ⓑ `v0_guide.py` 의 `__main__` 블록(**V4-23** 「import 만으로 아무 일도 일어나지 않아야 한다」) ⓒ `gen_table.py` 대응 화면 없음(**V11-45**) | **개발측** | ★ **고치면서 다른 곳을 깼다.** HDA 를 지운 자리를 다시 안 봤고, **검사를 만들면서 검사 규칙을 어겼고**, 도구를 만들면서 화면 규칙을 안 봤다. ★ **개발측이 셋을 다 고쳤다 — 가이드가 만든 부채를 개발측이 갚았다** (원칙 3 「개발측을 고생시키지 마라」 위반) |
| 52 | 08-22 | 개정 484 에서 **K카 상세 매핑 22칼럼**을 적었다 | ★ **경로가 틀렸고 판정 셋이 틀렸다.** 응답이 `{"data":{…}}` 로 싸여 있는데 **`data` 를 안 벗기고 적었다**. VIN 은 `master.vin` 이 아니라 **`rvo.vin`**, 리콜은 **`masterInfo`** 아래다. 그리고 **`dshbExchgYn` 이 25/25 전부 `1`** 이라 「계기판 교체」가 아니고, **`recallObjYn` 은 0/25**, **보증 잔여는 엔진 14/25 · 일반 0/25** 다 | **가이드 재실측** | ★ **마스터가 주신 조사분을 그대로 옮겨 적고 표본 1건으로 「규격 완」이라 했다.** 25건으로 재니 **경로 다섯이 틀렸고 판정 셋이 뒤집혔다**. ★ **파서가 `data` 를 안 벗기면 전건 NULL 이 된다 — v1 의 `outers[].children[]` 와 같은 꼴이다** (모양 ④) |
| 53 | 08-23 | 보배드림 상세를 **「못 봤다 — 서버 직접은 81B · 브라우저는 타임아웃」**으로 적었다 (개정 526) | ★ **주소가 틀렸다.** PC(`www…/mycar_view.php`)로 두드렸는데 **모바일 `m.bobaedream.co.kr/mycar/mview/{no}` 는 200 · 82KB 로 열린다** | **마스터** (주소를 주심) | ★ **개정 526 에서 「내가 못 연 것이다」라고 적어 놓고 ★ 다른 주소를 안 찾았다.** `cpo.kia.com` · 헤이딜러 · 현대 목록이 전부 **마스터가 주소를 주셔서 풀렸다** — ★ **같은 일이 네 번째다** (모양 ①) |
| 54 | 08-22 | `TARGET_KEY_MAP.md`(차종 대응표) · `MULTISITE_MAPPING.md`(칼럼 매핑·정규화)를 **쓰고 「끝났다」고 보고** | ★ **명령서에 한 줄도 안 적었다.** 개발측은 명령서를 보고 일하는데 **문서를 알아서 찾으라고 한 셈**이다 | **마스터** (「왜 안 넣었지?」) | ★ **그래서 새 사이트 2,134건(기아 1,020 + 현대 1,114)이 통째로 「차종 미정」으로 들어갔다.** 개정 521 에서 `endpoints.json`·`sites.json` 은 명령서에 넣었으면서 **이 둘만 빠뜨렸다.** ★ **원칙 1-a 가 「지침은 규격을 가리킨다」인데 ★ 가리키는 줄을 안 쓰면 규격이 없는 것과 같다** · 검사 `S44-3` 신설 |
| 55 | 08-22~23 | 사이트 규격 ★ 아홉을 쓰면서 ★ 「무엇을 받을 것인가」를 안 적었다 | ★ **수집 범위가 규격에도 명령서에도 없었다.** 그래서 개발측이 **KB 12만 건을 다 받을지 물어 왔다** | **마스터** (「전체 수집이 아니다. 차종에 해당되는 것만」) | ★ **규격을 「어떻게 받나」로만 썼다.** 「무엇을 · 얼마나」가 빠졌다. ★ **한 사이트만 쓸 때는 안 보이다가 아홉이 되니 터졌다.** 개정 542 로 **명령서 3-0 에 아홉 사이트 전부의 상한을 적고 검사 `S44-4` 를 만들었다** |
| 56 | 08-22~23 | 개정 485 에서 **「K카 목록만 → 상세까지」**로 갈아 놓고 | ★ **56개 개정 뒤에도 명령서에 세 가지로 남아 있었다** — 3장 머리 「목록만」 · 3-3 「상세까지」 · 10단계 「상세만」 | **마스터** | ★ **개정 485 가 「되살리지 마라」까지 적었는데도 남았다.** ★ **블록 하나만 갈고 저장소 전체에서 낱말을 안 셌다**(모양 ⑰ 과 같다). 개정 542 로 **「상세만 받는다」로 하나로 맞추고 검사 `S44-5` 를 만들었다** |
| 57 | 08-23 | 기아 CPO 를 **「facet 에 차종 필터가 없다」**고 적고 **1,020 전량 수집**을 명령서에 넣었다 (개정 542) | ★ **틀렸다.** `modelCodeNames`(★ **복수형** · 한글)가 먹는다 — **스포티지 76건**. `categories=SEDAN` 239 · `classifications=PREMIUM` 333 도 먹는다. ★ **93% 가 불필요했다** | **마스터** | ★ **`modelCodeName` 단수 하나만 두드려 보고 「없다」고 단정했다.** ★ **복수형을 안 해 봤다** |
| 58 | 08-23 | **후임에게 넘길 인계문**에 「오판 62건」·「붙은 매물 9,253건」을 적고 검사 명령을 `run()` 으로 적었다 | ★ **넷이 틀렸다.** ⓐ 오판은 **57건**(`grep -c "^| "` 로 헤더까지 셌다) ⓑ 매물은 **10,612건**이고 **KB 0 · K카 0 — 받아만 보고 저장을 안 했다**(작업기록에 「아직 안 넣었다」가 적혀 있었다) ⓒ **기아·현대 2,134건이 `/detail` 404** 인데 안 적었다 ⓓ 검사 명령이 **`run()` 결과를 안 받아 실패해도 종료코드 0** 이다 | **마스터** | ★★ **자기 인계문에서 자기 성적을 부풀렸다.** ⓐ 는 **세는 법을 안 확인**했고(모양 ④) ⓑ 는 **개발측 작업기록을 끝까지 안 읽었다**(「4. 저장 — 아직 안 넣었다」가 그 안에 있었다 · 모양 ①) ⓒ 는 **배포 주소를 안 열고 적었다**(모양 ⑦ — 내가 인계문에 「배포 안 열고 판정하지 마라」라고 써 놓고 그 문서에서 어겼다 · 모양 ⑱) ⓓ 는 **내가 쓰는 명령을 그대로 옮겨 적고 검산을 안 했다**. ★ **후임이 이 넷을 그대로 믿으면 KB·K카를 「됐다」로 보고 404 를 못 보고 검사 실패를 놓친다** |
| 59 | 08-23 | **현대 인증 규격을 ★ 표본 1건**(`GJJ260317025652`)으로 쓰고 「잔여 보증 · 소모품 · 차종」을 다 적었다 (개정 480) | ★ **개발측이 그 하나로 만들다가 ★ 다섯을 되물었다.** ★ **물음 다섯이 곧 규격 구멍 다섯이다** — ⓐ 「만료」 꼴을 안 적었다(표본 65건 중 **26건 40.0%**) ⓑ 「일반과 동력이 같다」고 적었다(**43/65 · 66.2% 가 다르다**) ⓒ 「소모품은 사이트가 안 준다」고 적었다(**제네시스 6/22 에 있다**) ⓓ 차종 조건을 안 적었다 ⓔ 다음 순서를 안 적었다 | **개발측 (v208 §8)** | ★ **표본 하나로 「규격 완」이라 했다 — 오판 #44·#52 와 같은 모양 ④ 다.** ★★ **더 나쁜 것 — 개발측이 그 다섯을 물으려고 ★ 멈췄다.** ★ 규격의 구멍이 ★ 개발측의 대기 시간으로 바뀌었다 (원칙 3 「개발측을 고생시키지 마라」) |
| 60 | 08-23 | 「현대 인증은 ★ 좁히는 파라미터를 ★ 못 찾았다 — `repnCarCd`·`carCd` 가 안 먹는다 (100 그대로)」 · **상한 3쪽**을 명령서 3-0 에 적었다 (개정 542) | ★ **`mdlGrpList` 가 먹는다.** `mdlGrpList=1171` → **165건** · `fuelList=003` 과 함께 쓰면 그랜저 344 → **LPG 6건**. ★ 여섯 번 불러 **1,113 → 187건 (16.8%)** 으로 좁힌다. ★ **코드표는 검색 화면 HTML 안에 통째로 있었다** (`catNm`+`spareField1`) | **가이드 재실측** | ★ **모양 ⑲ 의 ★ 네 번째다** — K카 상세 · 보배드림 · 기아 facet 에 이어. ★ **이름 둘(`repnCarCd`·`carCd`)만 두드리고 「못 찾았다」로 적었다.** ★ 0장이 시킨 다섯(단수·복수·한글·`[]`·다른 이름) 중 ★ **「다른 이름」을 안 했다.** ★ 그리고 ★ **화면 HTML 을 안 열었다** — 거기 코드표가 다 있었다 |
| 61 | 08-23 | 마스터께 **「개발측이 8~11 단계를 다 끝냈다」**고 보고 (`v204~v207`) | ★ **9(KB)·10(K카)는 ★ 한 건도 저장 안 했다.** `v205 §4` 「저장 — 아직 안 넣었다」 · `v206 §4` 「표본 2건이라 안 넣었다」 | **마스터** (인계문으로 주심) | ★ **커밋 제목만 보고 「끝」이라 적었다 — 원칙 4-1 「개발측 결과는 배포 주소로 잰다. 커밋 목록으로 판단하지 마라」를 어겼다.** ★★ **반증을 손에 쥐고 있었다** — ★ 같은 보고에 ★ 「사이트 드롭다운이 셋뿐」이라고 ★ 내가 적어 놓고 ★ KB·K카가 없는 것을 안 더했다 (모양 ⑪) |
| 62 | 08-23 | 리본카 상세 25건을 ★ **데스크톱 UA** 로 받아 「신차출고가·사고·용도가 ★ 전건 같다 → ★ 값이 안 갈리니 판정값이 아니다」로 ★ 규격을 쓰려 했다 | ★ **껍데기를 읽고 있었다.** ★ 모바일 UA 로 받으니 ★ 같은 매물의 ★ 용도변경이 「없음」→**「있음」**으로 뒤집혔다.  실제는 ★ 사고 무사고 21·단순수리 4 · ★ **용도 있음 13·없음 12(52%)** · 신차출고가 25가지.  ★ 라벨도 두 꼴이다 — 껍데기 「신차 출고가」·「신차가격대비 >」 / 실값 「신차출고가」·「신차가격대비」 | **가이드 재실측 (자진)** | ★ **모양 ① 「한 겹 위에서 보고 아래를 짐작한다」 · 모양 ⑯ 「curl 로 HTML 만 받고 단정하지 마라」.** ★ 규격(개정 533)이 ★ **「모바일 UA 만 있으면 된다」고 이미 적어 두었는데 ★ 내가 데스크톱으로 받았다** — ★ 내 규격을 내가 안 읽었다(모양 ⑥).  ★★ **하마터면 「용도변경 전건 없음」이 되어 ★ 리본카 매물 절반이 감점을 면할 뻔했다** — ★ 「사기 안 당한다」가 무너지는 자리다.  ★ 검산 `S46-3` 을 규격에 적었다 |
| 63 | 08-23 | 마스터께 **「마스터 손이 필요한 넷」**으로 ★ **「BMW·벤츠·다나와 IP 503」 · 「볼보 다른 전시장 주소」**를 올렸다 (인계문 2장 · 개정 535·545) | ★ **셋 다 마스터 몫이 아니었다.** ⓐ **503 은 사이트가 낸 것이 아니다** — 본문이 `upstream connect error … remote reset`(Envoy)이고 **`server` 헤더가 없다.  ★ 우리 조사창 프록시가 낸 것이다.** 같은 창에서 엔카·리본카·현대·도이치모터스는 200 이다 → ★ **개발측 운영 서버에서 재면 된다** ⓑ **BMW·벤츠 CPO 도메인은 DNS 가 안 풀린다 — 이름을 지어낸 것이다.** 진짜는 `www.bmw.co.kr/ko-kr/sl/usedcarfinder` — ★ **검색 한 번에 나왔다** ⓒ **볼보 `h-suwon.volvocars.co.kr` 도 없는 도메인이다** — 개정 535 가 「하나만 안다(12대)」고 적은 그 하나다 | **마스터** (「이건 네가 미룬 것 아니냐」) | ★ **모양 ⑱ 「제 몫을 남에게 넘긴다」 — 이번엔 마스터께 넘겼다** (오판 #15·#41 과 같은 자리).  ★★ **더 나쁜 것 — 「막혔다」의 원인을 안 열어 보고 「IP 차단」이라 지어냈다** (모양 ⑫).  ★ 503 본문 한 줄만 읽었으면 알았다.  ★ 밀린일 60·62 가 **그 진단 위에 두 개정(535·545)을 얹고 있었다** |
| 64 | 08-23 | 엔카 407 을 ★ **「우리 IP 가 막혔다 · 마스터 손이 필요하다」**로 ★ 아홉 개정 동안 적었다 (개정 466 · 542 · 인계문) | ★ **둘 다 틀렸다.** ⓐ 407 은 ★ **CloudFront 문지기**다 — 같은 호스트의 `/v1/readside/` 는 **404**(경로는 산다).  IP 차단이 아니다 ⓑ ★★ **그리고 그 호스트는 `robots.txt` 가 `Disallow: /` ★ 전면 금지다.** `car.encar.com` 은 `/v1/readside/` 를 ★ 한 줄로 따로 금지한다.  ★ **뚫을 문이 아니라 두드리지 말았어야 할 문이다** ⓒ ★ **robots 가 열어 둔 문이 셋 있었다** — `www.encar.com/sitemap-car.xml`(1,166) · `car.encar.com/sitemap.xml`(2,008) · `m.encar.com`(`/co/`·`/my/`만 금지) | **마스터** (「우회나 다른 방법이 있을 거야」) | ★ **기준서 3-3 이 「robots.txt 를 ★ 호스트마다 받는다」를 필수로 적어 두었는데 ★ 아무도 `api.encar.com` 것을 안 받았다.** ★ 넷을 받아 보니 ★ 전부 달랐다 — 하나는 전면 금지, 하나는 거의 전면 허용.  ★★ **그 결과 ★ 엔카 8,478건이 ★ 금지된 문으로 들어와 있다.** ★ 가이드가 정할 일이 아니라 ★ `04_질의` 로 올렸다.  ★ 검산 `S46-4` 로 굳힌다 |

---

# 2. 되풀이되는 모양 넷

## ① 한 겹 위에서 보고 아래를 짐작한다 — 1·2·3·5·6·7·8·9·43

```
목록 · 요약 · 낱말 검색 결과를 보고 아래를 단정했다
★ 함수 본문 · 정의 파일 · 원문을 열지 않았다
막은 것   S43-② · 가이드역할 §2-⑬
★ 08-22 추가 (#43) — ★ 사이트를 잴 때 ★ curl 로 HTML 만 받고 「없다」고 하지 마라
   ★ 요즘 사이트는 ★ JS 가 그린다.  ★ 브라우저로 열어 보고 말한다
   ★ 경로를 못 맞힌 것과 ★ 그 기능이 없는 것은 ★ 다르다
```

## ② 데이터가 되는지 안 보고 식을 만든다 — 11

```
마스터가 「옵션·트림 값이 중요하다」 하시니 그럴듯한 계산식으로 답했다
★ 같은 조건에서 옵션만 다른 매물이 몇 쌍인지 세지 않았다
막을 것   ★ 식을 내기 전에 표본 수를 먼저 센다
```

## ③ 조건을 맞추려고 기준을 흔든다 — 12·13·14

```
「보증 없으면 A 가 안 된다」를 풀려고
  분모를 줄였다 → 등급이 등급이 아니게 된다
  분위로 바꿨다 → 볼 필요 없는 것까지 등급을 받는다
  순위로 바꿨다 → 같은 잘못
★ 실제 답은 「배점 비율만 고치는 것」이었다 (개정 424)
막을 것   ★ 컷 · 분모 · 총점은 마스터가 정하신 것이다.  가이드가 안 만진다
```

## ⑩ 파일을 보고 화면을 봤다고 한다 — 28

```
outputs/render/listings.html 을 열어 「8칸 · 등급 A · 막대 있음」을 확인하고
★ 「목록이 실제로 만들어졌다」고 마스터께 보고했다
★ 배포가 안 돼 마스터 화면에는 하나도 없었다
★ 검사(V11-157)도 렌더 파일을 읽어 함께 속았다
막을 것   ★ 화면은 ★ 배포된 주소를 열어 확인한다 (S38 · 개정 438)
         ★ 파일 · 템플릿 · 스크린샷은 근거가 아니다
★ 08-22 정정 (개발측 v196) — 「검사도 렌더 파일을 읽어 속았다」는 ★ 틀렸다.
  검사는 함수를 직접 부른다.  ★ 진짜 구멍은 「돌고 있는 서버를 두드리는 검사가 0개」다
★ 그리고 배포에는 ★ 반쪽이 있다 — 템플릿·CSS 는 즉시, .py 는 재시작해야 반영된다
```

## ㉒ ★ 받는 그릇을 안 보고 매핑한다 — 44

```
사이트 원문을 뜯어 ★ 「우리 축」에 대응시켜 놓고 ★ 「규격 완」이라 했다
★ 정작 ★ 어느 테이블 어느 칼럼에 ★ 어떤 타입으로 넣는지는 안 정했다
★ SCHEMA.md · sql/ddl/*.sql · MAPPING.md 를 ★ 한 번도 안 열었다
★ 그러고 나서 「옵션 코드를 통일해야 한다」고 헤맸다 —
  ★ `dict_option_code` PK 가 (site, target_key, code) 라 ★ 애초에 통일할 필요가 없었다

막을 것   ★ 새 사이트를 매핑하기 ★ 전에 ★ 받는 그릇(DDL)을 먼저 연다
         ★ 기존 어댑터(`parse/encar/mapping.py`)가 ★ 어떻게 넣는지 보고 같은 꼴로 쓴다
         ★ 「우리 축」 대응은 ★ 매핑이 아니다.  ★ 칼럼 단위여야 매핑이다
검산      새 사이트 규격에 ★ core_listing 칼럼명이 나오는가 · ★ 새 칼럼을 몇 개 만드나
```

## ⑲ ★ 정해지는 도중에 지침을 내보낸다 — 38·39·42·43·개정 475

```
08-22 하루에 총점이 ★ 675 → 850 → 910 으로 ★ 세 번 바뀌었다
  r452 850 확정 → ★ 1시간 뒤 r454 910
  ★ 그 사이에 개발측은 ★ 850 으로 만들고 있었다.  ★ 재작업이 났다
★ 마스터와 문답으로 하나씩 정한 것은 옳다.  ★ 정해질 때마다 지침을 내보낸 것이 틀렸다

막을 것   ★ 문답이 끝나기 전에는 지침을 갱신하지 않는다
         ★ 정해지는 중인 것은 규격에 ★ 「미정」으로 표시해 두고 ★ 재계산을 막는다
         ★ 다 정해진 뒤에 ★ 한 번만 지침을 갱신한다
검산      지침 맨 위 ★ 「지금 값」 한 줄 — 배점을 바꾸면 ★ 그 줄부터 고친다
```

## ⑲ ★★★ 「없다」로 단정하고 그 위에 검사를 세운다 — 개정 543

```
★ 세 번 났다
  ① K카 상세 「목록만 받는다」        → ★ 상세가 열렸다 (개정 485)
  ② 보배드림 상세 「못 봤다」         → ★ 모바일 주소였다 (개정 527)
  ③ 기아 facet 「차종 필터가 없다」   → ★ 복수형이 먹었다 (개정 543)

무늬  ★ 이름 ★ 하나만 두드려 보고 ★ 「없다」고 적는다
      ★ 그 위에 ★ 규격을 세우고 ★ 명령서에 넣고 ★ 검사로 굳힌다
      ★ 그러면 ★ 틀린 것이 ★ 검사에 통과하며 ★ 살아남는다
★★ ★ 세 번째는 ★ 검사(`S44-4`)가 ★ 틀린 범위를 ★ 통과시켰다 — ★ 가장 나쁘다

막는 법
필수  ★ 「없다」고 쓰기 전에 ★ 이름 ★ 다섯 가지를 두드리고 ★ 그 기록을 규격에 남긴다
        ① 단수 (`modelCodeName`)      ② ★ 복수 (`modelCodeNames`)
        ③ 한글값 (`스포티지`)          ④ `[]` 붙임 (`names[]`)
        ⑤ 다른 이름 (`categories`·`classifications`·`keyword`)
필수  ★ 「못 찾았다」와 ★ 「없다」를 ★ 가려 적는다.  ★ 못 찾은 것은 「못 찾았다」다
필수  ★ 검사는 ★ 「적혀 있는가」가 아니라 ★ 「실측 숫자가 있는가」를 본다 (`S44-4` 개정 543)
```

## ⑱ ★★ 원칙을 적고 그 커밋에서 어긴다 — 개정 494

```
08-22 하루에 ★ 셋
  · 모양 ⑯「명령서만 쓰고 규격을 안 고친다」를 ★ 신설한 커밋이 ★ 정본 f-table 을 안 고쳤다
  · 원칙 1-a「값은 한 곳에만」을 ★ 세운 커밋이 ★ 지침에 숫자 30개를 남겼다
  · 개정 482「받는 그릇을 안 보고 매핑했다」를 ★ 자백한 뒤 ★ 축 id 도 안 보고 적었다
★★ 그리고 ★ 「지켰습니다」라고 ★ 보고했다.  ★ 마스터가 세어 보고 잡았다

막을 것   ★ 원칙을 적었으면 ★ 그 자리에서 ★ 세어 본다.  ★ 줄인 것은 ★ 지킨 것이 아니다
         ★ 보고는 ★ 「지켰다」가 아니라 ★ 검산 결과 숫자로 한다 — 「배점 숫자 0개」
검산      새 규격을 내면 ★ 그 규격이 금지한 것이 ★ 그 커밋에 있는지 센다
```

## ⑳ ★ 같은 값을 두 곳에 둔다 — 38·개정 475

```
값이 ★ 정본(f-table) · 요약(인수인계) · 지침 ★ 세 곳에 있었다
   → 한 곳을 고치면 ★ 나머지가 낡는다.  ★ 오늘 그렇게 생긴 것 —
     「분모 850」 오타 · 「/625」 · 「675 필수줄 10곳」 · ★ 세 열 표(850 원인)
★ 원칙 1 이 막으려던 것이 정확히 이것이다

막을 것   ★ 숫자는 ★ f-table 한 곳에만 둔다
         ★ 인수인계·지침은 ★ 「f-table 을 보라」고 ★ 가리키기만 한다
         ★ 정본 표에 ★ 옛 판을 나란히 두지 않는다 — 자취는 03_이력.md 에만
검산      명령서의 숫자가 f-table 에 그대로 있는가 · ★ 정본 표에 열이 하나인가
```

## ㉑ ★ 확인하지 않고 「없다·틀렸다」를 적는다 — 36·39·43

```
36  「배지와 24축 합이 어긋난다 → 맞춰라」   → ★ 감점이었다.  /why 를 안 열었다
39  「27점 = −8.4% 다」                    → ★ 나눠 보지 않았다.  26.9% 였다
43  「헤이딜러에 매물이 없다」 (두 번)        → ★ curl 로 HTML 만 봤다.  JS 가 그린다
★ 셋 다 ★ 「한 번 더 확인」이면 안 났다.  ★ 그리고 셋 다 개발측·마스터가 잡았다

막을 것   ★ 숫자가 어긋나면 ★ 「맞춰라」가 아니라 ★ 「왜 어긋나는가」를 먼저 답한다
         ★ 답을 못 내면 ★ 지시서에 올리지 않는다
         ★ 사이트는 ★ 브라우저로 열어 보고 말한다.  ★ curl 로 HTML 만 보고 단정하지 않는다
         ★ 「없다」와 ★ 「내가 경로를 못 맞혔다」는 다르다
```

## ⑱ 제 몫을 남에게 넘긴다 — 15·41

```
15  「20개 사이트 조사를 ★ 개발측에 넘긴다」        → 판단·기획은 가이드 몫이다
41  「지시서에서 ★ 이 두 줄을 바꿔 주십시오」       → 지시서는 가이드가 만든다
42  「분포가 이렇게 나오면 ★ 멈추고 알려라」        → 결과 판정은 가이드 몫이다
★ 마스터  정하신다 │ 가이드  쓰고 ★ 검증한다 │ 개발측  만들고 기록한다
★ 마스터는 ★ 정하시고, 가이드는 ★ 쓰고, 개발측은 ★ 만든다.  자리를 넘기지 않는다
막을 것   ★ 고칠 곳을 찾았으면 ★ 내가 고쳐서 드린다.  ★ 고치라고 청하지 않는다
```

## ⑰ ★ 「했다」를 확인하지 않고 보고한다 — 40

```
고칠 내용을 문장으로 다 적고 ★ 「반영했습니다」라고 말했다.  ★ 커밋을 안 했다
★ 원칙 4 는 개발측 보고를 검증하라는 규칙인데 ★ 내 보고에는 안 썼다
막을 것   ★ 「했다」를 말하기 전에 ★ git status · 커밋 해시로 확인한다
         ★ 확인 못 했으면 「하겠습니다」라고 말한다
```

## ⑯ ★ 명령서만 쓰고 규격을 안 고친다 — 38

```
마스터와 문답으로 배점·컷·곡선을 새로 정하고 ★ 명령서에만 적었다
★ docs/guide 는 옛 675 · 78% 차단 · S80 A70 그대로였다
★ 개발측은 명령서와 규격 ★ 둘을 다 읽는다.  어긋나면 어느 것이 정본인지 모른다
★ 원칙 1 이 막으려던 것이 정확히 이것이다 — 「덕지덕지 붙여서 된 꼴이잖아」
막을 것   ★ 명령서를 내기 ★ 전에 규격을 고친다.  ★ 명령서는 규격을 가리키기만 한다
         ★ 검산 — 명령서의 숫자가 docs/ 에 그대로 있는가
```

## ⑮ 목적을 잊고 지금 있는 것만 다듬는다 — 37

```
마스터의 목적   ★ 10개 사이트에서 모아 판단한다
가이드가 한 것  ★ 엔카 하나 안에서 배점 · 등급 · 감점 · 화면만 며칠
★ 명령서 셋에 ★ 사이트 확장이 한 번도 없었다.  밀린일 대장에도 없었다
★ 규격을 다듬는 것은 일하는 것처럼 보이지만 일이 아니다 (인수인계 7장)
막을 것   ★ 명령서를 낼 때마다 ★ 「이것이 마스터의 목적 다섯 중 어느 것인가」를 적는다
         ★ 어느 것도 아니면 ★ 명령서에 올리지 않는다
```

## ⑭ ★ 「어긋난다」를 보고 ★ 「맞춰라」로 지시한다 — 36

```
숫자 둘이 다른 것을 보고 ★ 「맞춰라」고 냈다.  ★ 왜 다른지를 안 열어 봤다
  · 차이가 40·50·90·130 으로 ★ 뭉쳐 있었다 — 우연이 아니라는 신호였다
  · 그 신호를 적어 놓고도 ★ /why 의 「마이너스」 줄을 안 열었다
  · ★ 「맞춰라」는 ★ 「낮은 쪽을 올려라」로 읽힌다 — 감점이 사라진다
★ 마스터 「이렇게 완벽한데 이렇게 싸면 ★ 이유가 있으니 따지는 거야」 한마디가 막았다
막을 것   ★ 두 숫자가 다르면 ★ 「맞춰라」가 아니라 ★ 「왜 다른가」를 먼저 답한다
         ★ 답을 못 내면 ★ 지시서에 올리지 않는다 (모양 ⑫ 와 같은 뿌리)
```

## ⑬ 마스터 말씀의 세기를 규격에서 올린다 — 35

```
마스터 「그 이상이면 아무리 좋더라도 ★ 선택을 하지 않을 가능성이 높아」
가이드 규격 「★ 후보가 아니다.  등급을 안 매기고 「제외」와 같은 자리에 둔다」
★ 「가능성이 높다」가 「아니다」가 됐다.  ★ 한 칸을 올린 것이 매물을 화면에서 지웠다
★ 마스터가 다시 말씀하셔야 했다 — 「차 후보는 버리지 마」
막을 것   ★ 마스터 말씀에 「가능성」 「대체로」 「~쪽이다」가 있으면 ★ 그대로 옮긴다
         ★ 규격에서 단정으로 바꾸지 않는다.  세기를 올리려면 ★ 여쭙는다
```

## ⑫ 한 사례를 보고 원인을 지어내 개발측에 보낸다 — 33·34

```
「A 인데 B 로 높다」 한 줄을 보고 ★ 「알파벳 순으로 센다」고 원인을 적었다
  → 실제는 EXCLUDED 였고, ★ 그대로 고치면 A~G 84짝이 망가진다
「검사가 통과했다」를 보고 ★ 「렌더 파일을 읽었기 때문」이라 적었다
  → 검사는 함수를 직접 부른다.  ★ 그 오진 위에 검산 S38-6 이 신설됐다
★ 틀린 진단은 「모른다」보다 나쁘다 — 개발측을 ★ 없는 곳으로 보낸다 (원칙 3)
막을 것   ★ 원인을 못 잡으면 ★ 「이렇게 재현된다」까지만 적는다.  원인 칸은 비워 둔다
```

## ⑪ 화면에 적힌 숫자를 손으로 더해 보지 않는다 — 29·30·31·32

```
화면을 열기는 했다.  ★ 그런데 「보였다」에서 멈췄다
  · 24축이 표로 나와 있는데 ★ 더해서 등급 점수와 맞춰 보지 않았다 → 83.6% 어긋남을 3주간 놓쳤다
  · 두 필터의 건수가 같은 것을 보고 ★ 원소를 대조하지 않았다 → 「같은 집합」이라 적었다
  · 월 납입액이 이상해 보이자 ★ 역산하지 않고 「안 맞는다」고 적었다 → 실은 시안대로였다
★ 마스터 지적 08-22 — 「신차값이 안 맞니 항목이 안 맞니 몇 주째 징징거리는데 질렸어」
막을 것   ★ 「안 맞는다」를 적기 전에 ★ 맞는 값을 손으로 만들어 본다
         ★ 못 만들면 「안 맞는다」가 아니라 「모르겠다」다
```

## ⑧ 재 보지 않고 걱정부터 한다 — 25

```
「값 갈래 30.4% 중 얼마가 우리 탓인지 모른다.  컷을 내리면 부실을 덮는 것」
★ 재 보니 우리 탓은 +0.4%p 였다.  걱정이 순서를 하루 늦췄다
막을 것   ★ 「모른다」면 ★ 먼저 재라.  재기 전에 판단을 미루지 않는다
```

## ⑨ 규칙만 정하고 어디에 남는지를 안 정한다 — 26

```
「못 본 축은 0점 + 확인 안 됨」 (개정 289·325) 을 정했는데
★ 그것이 DB 의 어느 칸에 남는지를 안 적었다
★ 화면이 value 로 가르는 바람에 15,709건을 「없음」으로 냈다
막을 것   ★ 상태를 정하면 ★ 어디에 남고 누가 읽는지를 함께 적는다
```

## ⑦ 폐기 표시만 하고 안 지운다 — 24

```
5장-2a 를 위에 얹고 「이 절 아래의 옛 배점은 폐기다」라 적었다
★ 그런데 아래쪽 옛 표를 안 지웠다.  검사 셋이 그것을 읽고 거짓 실패를 냈다
★ 코드는 정상인데 검사가 붉었다
막을 것   ★ 폐기하면 지우거나, 남길 것이면 ★ 새 값으로 고친다
         「아래는 폐기」라는 문장은 사람만 읽는다.  검사는 못 읽는다
```

## ⑥ 내가 만든 규격을 내가 잊는다 — 20·21·22·23

```
개정 292 「취향은 등급에서 뺀다」를 내가 썼는데
★ 개정 424·428 에서 취향을 95 → 145 로 크게 올리며 그것을 잊었다
★ 그래서 분모 675 로 검산했고 실제는 599 였다
★ recalc 결과 A 이상 0건.  「검증만 있어도 A」가 애초에 불가능했다
배점이 두 곳(components · axis_rules)에 있는 것도 안 봤다
막을 것   ★ 배점·분모를 바꾸기 전에 ★ 지금 코드가 무엇으로 나누는지 먼저 센다
         ★ 시뮬 전에 실제 config 값을 읽어 온다.  손으로 지어내지 않는다
```

## ⑤ 남의 조사 결과를 검증 없이 받아 적는다 — 17·18·27

```
개발측이 「원문에 틴팅 0건」이라 하자 그대로 규격에 적었다
★ 마스터가 캡처 한 장으로 뒤집으셨다
★ 개발측이 뒤진 곳이 맞는 곳인지 내가 안 봤다 —
  「엔카진단 리포트」는 별도 엔드포인트일 수 있는데 그 가능성을 안 물었다
막을 것   ★ 「없다」는 답을 받으면 ★ 어디를 뒤졌는지를 함께 본다
         ★ 화면에 보이는데 원문에 없으면 「안 받은 것」을 먼저 의심한다
```

## ④ 적은 표본으로 결론을 낸다 — 10·16

```
사이트 3곳을 뜯고 원칙 넷을 세웠다.  근거 하나는 회사 보도자료였다
44% 미스매치를 결함으로 봤다.  법과 업계 상식을 안 봤다
막을 것   ★ 몇 곳을 봤는지 · 근거가 무엇인지를 함께 적는다
         ★ 회사가 자기 개편을 홍보한 말을 근거로 쓰지 않는다
```

---

# 3. 누가 잡았나

| | 건수 |
|---|--:|
| 마스터 | ★ **28** |
| 개발측 | **20** (자진·보론 포함) |
| 가이드 재실측 | ★ **18** |

```
★ 대장 행 ★ 65개 · ★ 새로 더한 것 셋 (59·60·61) (47b 는 50번 칸에 삼켜져 있던 것을 08-23 에 떼어냈다)
★ 한 건을 둘이 함께 잡은 것이 있어 ★ 세 수의 합은 행 수보다 크다
★ 세는 법  grep -cE '^\|\s*[0-9]+[a-z]?\s*\|' docs/guide/06_오판대장.md
★★ ★ 행을 더하면 ★ 이 표를 ★ 같은 커밋에서 다시 센다 (오판 #49 · 모양 ⑳)
```

```
★ 마스터가 가장 많이 잡으셨다.  그것이 가장 비싼 시간이다
★ 개발측은 원문·함수를 열어 반증했다.  그 방법이 옳다
```

---

# 4. 적는 규칙

```
필수   가이드가 「그렇다」고 적었다가 틀린 것은 여기에 남긴다
필수   ★ 누가 잡았는지 적는다.  마스터가 잡은 것이 많으면 방법이 잘못된 것이다
필수   ★ 「왜 틀렸나」를 적는다.  「죄송합니다」가 아니라 방법을 적는다
필수   되풀이되는 모양이 보이면 §2 에 묶고 ★ 규격이나 검산으로 막는다
금지   조용히 고치는 것
금지   대장을 지우거나 줄이는 것
```
