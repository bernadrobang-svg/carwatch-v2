# 마스터께 여쭐 것 — 2026-08-18

**답하는 법 — 번호와 알파벳만. 「1-A · 2-C · 3-B …」**
**설명이 필요한 것만 덧붙여 주십시오.**

---

# ★ 먼저 — 큰 것 셋

## 1. 사이트 보증 배점

```
개정 306   마스터  「사이트 보증 50점을 넣고 총점을 605로」
개정 329   가이드가 24축을 다시 짜며 ⑤ 를 30 으로 줄임
           ★ 안 여쭙고 줄였습니다
```

```
A   50 으로 되돌린다 (마스터 지시대로)
B   30 으로 둔다 (지금)
C   다른 값 — 몇 점?
```

## 2. 급한 순서

```
지금 열린 결함 50건
  14장 화면 19 · 5장 판정 8 · 2장 수집 6 · 13장 관리 4 · 그 밖 13
```

```
A   화면 → 판정 → 수집        (가이드 판단)
B   판정 → 수집 → 화면        등급을 먼저 믿을 수 있게
C   수집 → 판정 → 화면        축이 0점인 것부터 (SOH·타이어·카탈로그)
D   다른 순서 — 적어 주십시오
```

## 3. 규격끼리 충돌 (7장 R-998·999)

```
7장   「사이트가 다른 매물을 같은 등급표로 비교하지 않는다.
        분모가 같은 매물끼리만 비교한다」
개정 289  「★ 분모는 늘 총점이다.  줄이지 않는다」
★ 둘이 어긋납니다
```

```
A   7장 규격을 지운다 (개정 289 가 이김)
B   개정 289 를 고친다
C   둘 다 남기되 뜻을 다시 쓴다 — 가이드가 정리
```

---

# 값 · 비용 표시

## 4. 「전액 현금」 판정 (9장 R-732·733)

```
지금   표시가 + 부대비용 ≤ 선납금 이면 「전액 현금」
      부대비용 > 선납금 이면 부족액을 표시
★ 가이드가 정한 계산입니다.  실제 거래와 맞는지 모릅니다
```

```
A   그대로 둔다
B   지운다 — 안 쓴다
C   고친다 — 어떻게?
```

## 5. 「추정」 표시 (9장 R-731 · 8장 R-874)

```
지금   월 납입 · 이전등록비에 「추정」을 붙인다
★ 법적으로 필요한지 확인 안 했습니다
```

```
A   그대로 둔다
B   뺀다 — 번거롭다
```

## 6. 예상 구매비용 총액 (9장 R-734 · 결함 D-702)

```
K카는 「차량가 + 이전등록 + 보증 + 기타 = 37,706,000」을 냅니다
우리는 차량가만 냅니다
```

```
A   총액을 낸다 (K카처럼)
B   차량가만 낸다 — 추정이 틀리면 오히려 해롭다
C   총액을 내되 「추정」을 크게 붙인다
```

---

# 관심 · 절차

## 7. 계약 4단계 (10장 R-863)

```
지금   후보 확정 → … → 계약  네 단계로 나누고 시각·메모를 남긴다
★ 가이드가 넷으로 나눴습니다.  실제 거래 절차와 맞는지 모릅니다
```

```
A   그대로 둔다
B   지운다 — 그때 가서 정한다
C   단계를 다시 정한다 — 몇 단계로?
```

## 8. 같은 차를 묶는가 (10장 R-815)

```
같은 vehicle_id 가 여러 번 등록되면 (내렸다 다시 올림)
```

```
A   묶어서 한 줄로 — 지금
B   따로 낸다 — 값이 바뀌었을 수 있다
C   묶되 「N번 재등록」을 표시
```

---

# 화면 · 조작

## 9. 목록 한 쪽에 몇 건 (9장 R-757)

```
지금   config 가 정한다.  URL 로 못 바꾼다
★ 상한만 두면 URL 로 받아도 됩니다
```

```
A   그대로 — config 만
B   URL 로 받되 상한을 둔다 (예: 200)
```

## 10. 리포트 파일 (8장 R-891 · 9장 R-781)

```
지금   파일명 규격을 정하고 화면에서 파싱해 목록을 낸다
★ 가이드가 정했습니다.  쓰시는지 모릅니다
```

```
A   쓴다 — 그대로 둔다
B   안 쓴다 — 규격을 지운다
C   화면에서 바로 내려받게만 한다
```

---

# 계정 · 운영

## 11. 비밀번호 규칙 (13장 R-628a)

```
지금   8자 이상 · 임시 비밀번호와 다름
★ 통념일 뿐 근거가 없습니다.  1인 시스템입니다
```

```
A   그대로
B   길이 제한도 없앤다
C   더 강하게 — 어떻게?
```

## 12. 쿼리 결과 복사 (13장 R-682a)

```
지금   탭 구분 · CSV · JSON 으로 복사할 수 있게 한다
★ 쓰시는지 확인 안 했습니다
```

```
A   쓴다 — 그대로
B   안 쓴다 — 지운다
```

## 13. 점검이 한 번에 고치는 양 (0장 R-611b)

```
지금   4시간 점검이 fatal 을 찾으면 한 번에 3건까지 고친다
★ 「3」에 근거가 없습니다
```

```
A   3건
B   더 많이 — 몇 건?
C   상한 없이 전부
```

## 14. 소스 버전 올리는 기준 (0장 R-537a)

```
지금   소스가 바뀌면 버전을 올린다.  문서·표본만 바뀌면 안 올린다
```

```
A   그대로
B   문서가 바뀌어도 올린다
```

---

# 확보율

## 15. 확보율을 무엇으로 (3장 R-372)

```
원문 기준   받은 엔드포인트 수 ÷ 불러야 할 수
컬럼 기준   채워진 컬럼 수 ÷ 전체 컬럼 수
★ 둘이 다릅니다
```

```
A   둘 다 낸다
B   원문 기준만
C   컬럼 기준만
```

## 16. 이름이 빈 계정 (3장 R-350)

```
지금   display_name 이 비면 login_name 을 넣는다
```

```
A   그대로
B   빈 채로 두고 화면에서 「이름 없음」
```

---

# 마지막

## 17. 이 문서 형식

```
A   좋다 — 앞으로도 이렇게
B   더 짧게 — 선택지만
C   더 자세히 — 왜 물어보는지 배경을 더
```

---

```
답하실 때
  「1-A · 2-C · 3-A · 4-B · …」
  ★ 판단이 어려우면 「모르겠다」라고 하셔도 됩니다.  제가 다시 조사하겠습니다
  ★ 나중에 정할 것은 「보류」라 하시면 미확정으로 남기겠습니다
```

---

# ★★★ 08-23 · 엔카 robots — ★ 마스터만 정하실 수 있는 것

```
★ 가이드가 잰 것 (근거 `docs/ENCAR_ROBOTS.md`)
  · `api.encar.com/robots.txt` = ★ `Disallow: /`  — ★ 전면 금지
  · `car.encar.com/robots.txt` = ★ `Disallow: /v1/readside/` — ★ 우리 상세 경로
  · 407 은 ★ CloudFront 문지기다.  ★ IP 차단이 아니다
  · ★ 지금 엔카 매물 ★ 8,478건이 ★ 그 문으로 들어와 있다
  · ★ 우리 기준서 3-3 은 ★ 「금지된 경로는 두드리지 않는다」를 ★ 필수로 적어 두었다
★ 가이드는 ★ 우회를 만들지 않았습니다.  ★ 그리고 ★ 스스로 멈추지도 않았습니다
```

**★ 어떻게 할까요**

| | 고르실 것 | 뜻 |
|:--:|---|---|
| **A** | ★ 지금대로 간다 | 엔카를 `api.encar.com` 으로 계속 받는다. ★ robots 는 법이 아니라 규범이다 |
| **B** | ★ 열려 있는 문으로 옮긴다 | `www.encar.com/sitemap-car.xml` · `car.encar.com/sitemap.xml` · `m.encar.com` ★ 셋 다 robots 가 허용. ★ **다시 만드는 일이 크다** |
| **C** | ★ 엔카를 ★ 멈추고 ★ 다른 아홉 사이트로 간다 | 기아·현대·KB·K카·헤이딜러·보배·리본카·렉서스·볼보 |
| **D** | ★ 우선 ★ 새 사이트를 붙이고 ★ 엔카는 나중에 정한다 | ★ 가이드 추천 — ★ 목적 ⑤ 에 가장 가깝다 |

```
★ 가이드 판단 — ★ **D** 입니다.  ★ 다르면 말씀해 주십시오
   까닭 ①  ★ 지금 급한 것은 ★ 관리 메뉴와 ★ 2,134건 채점이다 (명령서 1·2단계)
   까닭 ②  ★ B 는 ★ 목록·상세·파서를 ★ 다시 만드는 일이다.  ★ 재 보지 않고 시작하면 안 된다
   까닭 ③  ★ 엔카는 ★ 이미 8,478건이 들어와 있어 ★ 당장 화면이 비지 않는다
★ 검산 `S46-4` 를 만들어 ★ 앞으로는 ★ 기계가 이것을 잡게 하겠습니다 —
  ★ `endpoints.json` 의 모든 `base_url` 에 대해 robots 를 받아 대조
```
