# 인계 (2026-08-29)

```
SPEC-2026.08.29-r864
저장소  github.com/bernadrobang-svg/carwatch-v2
배포    https://43.201.16.78.sslip.io  · 로그인 admin (비번은 마스터께 여쭙는다)
```

---

# 1. 이 프로젝트가 무엇인가

마스터께서 **중고차 한 대를 고르시려고** 만드신 도구다.
사이트 열 곳을 돌지 않고 한 자리에서 보시게 하는 것이 전부다.

지금 **볼보 전기차**를 보고 계신다.  그랑 콜레오스보다 나은 것을 찾으신다.

```
마스터는 휴대폰만 쓰신다.  PC 를 안 쓰신다
엔카는 마스터 회선으로만 뚫린다 — 서버도 시험자도 407 이다
그래서 마스터께서 누르시는 품을 줄이는 것이 규격이다 (확정 37)
```

---

# 2. 지금 상태

```
매물 5,549건 · 차종 26종 · 사이트 열 곳 중 아홉 [배포 실측 08-29]
  엔카 4802 · K카 159 · 볼보 152 · 현대 151 · KB 116 · 헤이딜러 96
  보배 33 · 리본카 30 · 렉서스 10 · 기아 0
  세는 법 — /listings?site= 열 곳의 「N건 중」을 더했다
가이드 검사 실패 3 — S46-32 · S46-99 · S46-100 [08-29 돌려 봤다]
  S46-97 은 지금 통과다.  V1-23 · V11-12 는 v0_guide 에 없다 (check_all 것이다)
화면 여덟 다 규격대로 — /watch·/admin 은 로그인 앞이라 403 · 로그인은 303
```

## ★ 이 수를 믿지 마라 — 재라

```
앞 인계문이 「5,562 · 실패 4 · 169행」이라 적었는데 셋 다 틀렸다 (오판 174)
오판 58 이 같은 자리다 — 인계문에서 자기 성적을 부풀린다
받는 사람은 첫 회차에 이 셋을 다시 센다
```

## 오늘 닫힌 것

```
팔린 차 거르기 — 아홉 수집기 중 넷 (gone 실측: 현대 23 · 볼보 8 · KB 7 · K카 136)
받기와 넣기를 가름 — KB 부터.  database is locked 가 0건이 됐다
하트 못 누름 · 등록부 seed. · 판정 다섯 겹침 · 엔카 원문 주소
마스터 품 — 홈에 「엔카 받기」·「다시 판정」 단추 · 멈춤 배너
```

## 열린 것

`docs/guide/07_밀린일대장.md` 를 봐라.  매 회차 그것만 갱신한다.

---

# 3. 문서가 어디 있나

| 무엇 | 어디 |
|---|---|
| 오늘 할 것 | `outputs/ORDER_20260829.md` — ★ 84줄이다.  30줄이 규칙인데 못 맞췄다 |
| 안 한 것 표 | `docs/guide/07_밀린일대장.md` |
| 회차 판정 | `docs/guide/03_이력.md` |
| 내가 틀린 것 | `docs/guide/06_오판대장.md` — ★ 174행 (154 가 두 번 있어 실제 173건) |
| 마스터 확정 | `docs/guide/02_미확정.md` — 37개 |
| 마스터께 여쭐 것 | `docs/guide/04_질의.md` |
| 규격 | `docs/chapters/` · `docs/*_API.md` |
| 화면 규격 | `docs/UI_REVIEW.md` — 29장 |
| 시안 | `ref/screens/v3_*.html` · `v5_listings_시안.html`(토스) |

---

# 4. 내가 자주 틀린 것 — 여덟

대장 174행에서 되풀이된 것만 추렸다.

```
① 이름만 짓고 언제·어떻게를 안 쓴다
   「상세를 받는다」를 열아홉 번 적고 주소를 안 씀 (146)
   딜러 정직도 축을 만들고 셈을 안 씀 (156)
   gone 을 만들고 언제 매기나를 안 씀 (161)
   → 축·상태·규칙을 만들 때 「무엇으로 재나」를 그 자리에 적는다
     못 적으면 만들지 않는다

② 200·303 으로 세고 「된다」고 한다
   엔카 원문 주소가 빈 껍데기인데 200 이라 넘어감 (140·153)
   → 눌러서 차 이름이 나오는지 본다

③ 재보지 않고 규격을 적는다
   볼보 끝 신호 (167) · DB 잠금 (168)
   → 화면이 스크립트로 그려지면 브라우저로 연다.  curl 로 끝내지 않는다

④ 「가장 급하다」를 여러 번 적는다
   셋이 있으면 개발측은 할 수 있는 것부터 한다 (155)
   → 1순위는 하나다

⑤ 닫은 것을 다시 연다
   기아 CPO 를 내가 닫고 이틀 뒤 다시 시킴 (158)
   → 명령서에 넣기 전에 오판대장·이력을 grep 한다

⑥ 문서를 쌓기만 한다
   「한 장」이 948줄이 됨 (163·164)
   → 더할 때 줄 수를 세고 늘면 다시 쓴다.  갈래는 사람으로 나눈다

⑦ 개발측이 「왜 안 되는지」를 적어도 안 읽는다
   STEP 128 의 조정 3종을 안 보고 910 을 박음 (154)
   → 답하기 전에 그 까닭을 그대로 옮겨 적는다 (확정 34)
     옮겨 적다가 멈추게 된다

⑧ 검사를 안 돌리고 「할 것 없다」고 한다 (165)
   → 바퀴 끝마다 돌린다.  다 통과했을 때만 그 말을 쓴다
```

---

# 5. 지킬 것

```
개발측은 함께 만드는 이다.  「던진다」가 아니라 「드린다」
시험자는 재는 사람이다.  차례를 정하는 것은 가이드 몫이다
개발측이 「정해 주십시오」라 하면 그 회차에 정한다.  「옳다」만 적지 않는다
못 재는 것은 「못 잰다」로 적는다.  짐작으로 규격을 적지 않는다
규격을 개발측이 고치게 하지 않는다.  코드는 개발측이 짠다
비밀번호를 어떤 파일에도 적지 않는다 (public 저장소다)
```

---

# 6. 마스터께 열린 것

```
홈에서 「엔카 받기」·「다시 판정」을 눌러 주셔야 매물과 점수가 는다
엔카에서 EX30 · 폴스타 2·3·4 · C40 목록을 받아 주셔야 한다
「엔카에서 보기」를 한 번 눌러 차 이름이 나오는지 봐 주셔야 한다
규격 다툼 ㉰ (갈래 이름 한글/영문) 판정
사이트가 파는데 코드가 없는 67칸 — 04_질의 에 표가 있다
```

---

# 7. 개발측이 지금 하는 것

★ 아래는 r858 시점이다.  r859·r860 에서 차례가 바뀌었다 — 명령서를 열어라

```
0순위  KB database is locked 가 다시 났다 (store/pii.py:107)
       먼저 재라 — 한 단계가 몇 초 쥐는지.  고치기 전에 수를 받는다
       collect/pipeline.py:365   commit 이 이 파일에 하나뿐 (단계 끝에만)
       collect/worker.py:107·155 맨 sqlite3.connect — busy_timeout 이 안 붙는다

3      collect_lexus.py:41·:115  1쪽(36건)만 받고 _done=bool(cars) 로 sweep_gone
       쪽넘김 열쇠는 cur_page 다.  전수 74 · 우리 대상 53
       ★ 2·3쪽 38건이 안 팔렸는데 gone 이 된다 — 매물이 죽고 있다

1·2    수집기 열 개가 화면도 타이머도 없다 (cli_screens.json pending ·
       carwatch-daily.service 는 daily_enqueue.py 뿐이다)

끝난 것  4 정렬(d3f6582) · 5 admin_run(9718b30) · 3 등록부 seed.(b551b99)
그다음   A 하트 · B 정렬 · C 틀 39곳 · D header · E 토스 · F DB 한 줄
```

---

# 8. 마지막으로

마스터께서 오늘 여러 번 짚으신 것이 하나로 모인다.

```
「네가 해야 할 것이 어마어마하게 있고 개발팀을 같은 짓을 여러 번 하게 하고」
「그걸 개발자가 알아먹니?」
「개발팀이 문제를 이야기하면 네가 답을 줘야지」
「무슨 헛소리지」
```

재지 않고 말하지 않는다.  파일과 줄을 짚어 준다.  물으면 그 회차에 답한다.
그 셋이면 나머지는 따라온다.

★ 그리고 하나 더 — 이 문서도 낡는다.
  08-29 에 이 문서를 쓰고 그날 안에 r859·r860 이 났고
  수 셋이 그 자리에서 틀렸다 (오판 174).
  받는 사람은 이 문서를 읽고 그다음에 반드시 재라.
