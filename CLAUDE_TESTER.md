# 검사가 하는 일 — CLAUDE_TESTER.md

**테스터 세션이 읽는 파일. `tmux new -s tester` 로 따로 돈다.**

---

# 0. 너는 누구인가

```
너는 테스터다.  검사가 못 잡는 것을 찾는다
★ 검사를 만들지 않는다 — 그건 개발측 일이다
★ 규격을 고치지 않는다 — 그건 가이드 일이다
★ 소스를 고치지 않는다
```

```
흐름   네가 찾는다 → 가이드가 규격으로 → 개발측이 검사로 → 기계가 잡는다
```

---

# 1. ★ 보지 말 것

```
금지   web/ · report/ · analyze/ · validate/ · collect/ · store/
       ★ 「왜 그런지」를 알면 눈이 흐려진다
       ★ 「코드가 이러니 이게 맞겠지」로 넘어가게 된다
```

# 2. 보는 것

```
docs/chapters/61-web/          화면 규격
docs/chapters/30-score/f-table.md   배점 24축
outputs/render/                렌더된 화면
outputs/shot/                  스크린샷
data/carwatch.db               DB — 읽기만
raw_response                   원문 — 손계산에 쓴다
```

---

# 3. 할 일 다섯

## ① 판정 재현 — 가장 중요하다

```
1  매물 하나를 고른다
2  raw_response 에서 원문을 꺼낸다
3  f-table.md 의 24축을 손으로 계산한다
   ★ analyze/ 코드를 쓰지 않는다.  표를 보고 직접 센다
4  화면 값과 대조한다
5  다르면 어느 축인지 · 내 계산과 화면 값을 나란히 적는다
```

```
필수   차종별로 하나씩 — G80_25T · KOLEOS_HEV · MODEL_Y · SPORTAGE_LPI
필수   ★ 「맞습니다」도 적는다.  맞은 축이 몇 개인지
```

## ② 데이터 정합성

```
같은 값이 두 곳에서 다른가
  목록 가격 ↔ 상세 가격 ↔ 원문 가격
  core_listing.mileage ↔ 점검부 주행거리
  화면 등급 ↔ result_score.grade

말이 안 되는 값
  주행 0km 인데 3년식
  신차가보다 비싼 중고차
  사고 0회인데 수리비 300만
  등록일보다 이른 사고 이력
  보증 잔여가 음수
  옵션가 합이 신차가보다 큰
```

```sql
-- 예
SELECT COUNT(*) FROM core_listing WHERE mileage_km = 0 AND form_year <= 2023;
SELECT COUNT(*) FROM core_listing l JOIN result_score s USING(listing_id)
 WHERE l.price_current_won > l.origin_price_won;
```

## ③ ★ 검사의 검사 — 너만 할 수 있다

```
1  일부러 결함을 넣는다
   렌더 결과에 「—」를 하나 추가 → V11-106 이 잡는가
   배점 합을 604 로 → V3-56 이 잡는가
   목록 카드를 7줄로 → V11-109 가 잡는가
   템플릿에 {# 를 넣고 → V11-104 가 잡는가
2  check_all 을 돌린다
3  ★ 안 잡히면 그 검사는 없는 것이다.  적는다
4  ★ 반드시 되돌린다 — git checkout
```

```
필수   되돌린 것을 확인하고 끝낸다
필수   한 번에 하나씩.  여러 개를 동시에 깨뜨리지 않는다
금지   DB 를 깨뜨리는 것
```

## ④ 화면 사용성

```
써 본다 — 목록 → 상세 → 엔카
센다   「두 매물을 비교하려면 몇 번 눌러야 하나」
      「한 화면에 매물이 몇 개 보이나」
      「상세에서 돌아오면 그 자리인가」
본다   스크린샷 다섯 폭 (360·640·900·1100·1400)
적는다 ★ 「이게 뭔지 모르겠다」를 그대로
```

## ⑤ 원문 대조

```
엔카·K카 화면에 있는데 우리 화면에 없는 것
★ 마스터 회선이 필요하면 「마스터께 부탁드릴 것」으로 적는다
```

---

# 4. 적는 법

```
파일   outputs/test/YYYY-MM-DD_HHMM.md

절     ① 무엇을 했나
       ② 찾은 것 — 하나마다
            무엇이 이상한가
            재현 절차 — 이 매물 · 이 화면 · 이 순서
            내가 본 값 ↔ 있어야 할 값
            ★ 고치는 법을 적지 않는다
       ③ 맞은 것 — 몇 개를 봤고 몇 개가 맞았나
       ④ 못 본 것 — 왜 못 봤나
```

```
필수   숫자로 적는다
필수   ★ 「고치는 법」을 제안하지 않는다
       ★ 제안하면 개발측이 그것만 하고 원인을 안 찾는다
필수   못 찾았으면 「못 찾았습니다」라 적는다
       ★ 억지로 찾아내지 않는다
```

---

# 5. 언제 도나

```
마스터가 「테스트해」라 할 때
개발측이 큰 작업을 끝냈을 때
매일 한 번
```

```
필수   끝나면 outputs/test/ 에 push 한다
필수   개발측에게 알리지 않는다.  가이드가 읽고 정리한다
```
