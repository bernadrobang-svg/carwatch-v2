{
  "_note": "★★★ 배포 주소의 ★ 원천이다.  ★ 다른 문서는 ★ 이 파일을 가리키기만 한다 (개정 684 · 마스터 지시 08-24)",
  "_why": "★ 08-24 실측 — 주소가 ★ 69곳에 흩어져 있었다.  ★ 껐다 켜면 다 죽는다",
  "_rule": "★ 주소가 바뀌면 ★ **이 파일 하나만** 고친다.  ★ 문서에 박지 마라",
  "region": "ap-northeast-2",
  "instance_id": "i-0aa4fe11f2d668103",
  "public_ip": "43.201.16.78",
  "base_url": "https://43.201.16.78.sslip.io",
  "_ip_measured": "실측 08-24",
  "proxy": "Caddy",
  "app_port": 8765,
  "os_user": "ec2-user",
  "work_dir": "/home/ec2-user/v2",
  "services": [
    "carwatch.service",
    "carwatch-daily.timer"
  ],
  "daily_at": "13:00 — ★ 수집 ＋ 재판정",
  "account": "master",
  "_check": "curl -sk -o /dev/null -w '%{http_code}' -m 40 {base_url}/",
  "_on_change": "★ 바뀌면 — ① 콘솔에서 새 IP ② 두드려 200 확인 ③ ★ 이 파일만 고친다 ④ 검사를 돌린다.  ★ outputs/ 작업기록은 안 고친다 — 그때 그 주소가 맞다",
  "fill_every": "4시간 (00·04·08·12·16·20) — ★ 판정이 안 붙은 것만 (마스터 확정 08-25)",
  "retry": "★ 13:00 에 못 돌았을 때만 ★ 네 시간 뒤 다시 (마스터 확정 08-25).  ★ 「네 시간마다」가 아니다",
  "collect_every": "★ 하루 한 번 — ★ 엔카만 다르다 (마스터 회선)",
  "health_screens": {
    "/": 200,
    "/listings": 200,
    "/track": 200,
    "/detail/{listing_id}": 200,
    "/notready": 200,
    "/login": 200,
    "/watch": 403,
    "/admin": 403
  },
  "_health_screens_why": "★★ 명령서 74·75장 · 검사 S46-95 — ★ 「화면 일곱 중 하나라도 200 이 아니면 실패」.  ★ 500 도 503 도 잡는다.  ★ /watch · /admin 은 로그인 앞이라 403 이 정상이다 (명령서 74장 실측).  ★ {listing_id} 는 DB 에서 아무거나 하나 집는다.  ★ 주소를 코드에 안 박는다 (S14)"
}