# CarWatch v2 — 작업 규칙

이 파일은 세션이 새로 시작돼도 규칙이 유지되도록 남긴 것이다.
**작업을 시작하기 전에 이 파일을 먼저 읽는다.**

이 프로젝트 전체의 설계 목표는 **「선언과 실제의 괴리」를 막는 것**이다.
v1 이 「저장했다」고 선언했으나 전건 NULL 이었던 사고(`docs/guide/00_개요.md`)의
재발 방지가 목표다. 검증 없는 완료 보고가 이 프로젝트에서 가장 큰 실패 모드다.

---

## 규칙 1. `docs/` 가 유일한 정본이다

코드가 규격과 다르면 **코드를 고친다.** 문서를 코드에 맞추지 않는다.

## 규칙 2. `docs/` 를 개발측이 고치지 않는다

정정이 필요하면 문서를 건드리지 말고 **사용자에게 말한다.**
규격 자체가 비어 있거나 서로 어긋나면 그것을 보고한다 — 임의로 메우지 않는다.
근거: `docs/guide/03_이력.md` 개정 238 「지시서는 개발측이 고치지 않는다」

## 규칙 3. 고친 뒤 반드시 두 도구를 돌린다

```
python3.11 tools/check_all.py
python3.11 tools/run_tests.py
```

## 규칙 4. 「고쳤습니다」라고 하기 전에 실제로 돌려서 확인한다

출력(통과 / fatal / warn 수, 실패한 test 파일명)을 **그대로** 보고한다.

## 규칙 5. 문서 수신

가이드가 주는 문서는 `~/v2/inbox/` 에 들어온다.
「inbox 확인해라」라고 하면

1. `inbox/` 의 파일을 전부 읽는다
2. `*.tar.gz` 가 있으면 풀어 `docs/` 에 반영한다
3. 반영한 뒤 `inbox/` 에서 지운다
4. 무엇을 읽고 무엇을 반영했는지 보고한다
5. 지금 작업과 어긋나면 멈추고 물어본다

## 규칙 6. 작업 기록

모든 작업의 처리와 결과를 `~/v2/outputs/` 에 문서로 남긴다.

```
파일명   YYYYMMDD_HHMM_v버전_제목.md
예       20260816_0130_v133_반입_S5수집.md
         20260816_0210_v134_V11-34_수정.md
```

버전은 지금 소스의 버전이다. 고쳐서 버전이 오르면 새 번호를 쓴다.

문서에 반드시 담을 것

1. 무엇을 하려 했나 (지시 원문)
2. 무엇을 했나 (고친 파일 · 줄 번호)
3. 왜 그렇게 했나 (규격 STEP 번호)
4. 1차 결과 (고치기 전 숫자)
5. 조치 후 결과 (고친 뒤 숫자)
6. 못 한 것 · 막힌 것 · 판단이 필요한 것

```
금지   「고쳤습니다」만 적는 것.  숫자가 없으면 기록이 아니다
금지   1차 결과를 빼고 조치 후만 적는 것
필수   check_all · run_tests 를 돌린 실제 출력을 붙인다
```

## 규칙 8. 형상 관리

`~/v2` 는 git 저장소이고 GitHub 에 push 한다.
`https://github.com/bernadrobang-svg/carwatch-v2` (public · main)

### 형상 관리 — 자동

**작업이 끝나면 물어보지 말고 바로 커밋하고 push 한다.
자격 증명은 저장돼 있다. 인증을 묻지 않는다.**

```
git add -A
git commit -m "규격코드: 무엇을 왜"
git push
```

```
필수  커밋 메시지 첫머리에 규격 코드나 STEP 번호
필수  작업 하나에 커밋 하나.  섞지 않는다
필수  outputs/ 기록을 남긴 뒤 함께 커밋한다
필수  push 까지 끝내야 작업이 끝난 것이다
      가이드가 GitHub 을 읽는다 (개정 257 · S25)
금지  「커밋할까요?」를 묻는 것.  그냥 해라
금지  push 를 미루는 것
```

```
작업 시작 전   git status 로 깨끗한지 본다.  아니면 먼저 물어본다
작업 후        git add -A
              git commit -m "규격코드: 무엇을 왜"
              git push
```

```
필수   커밋 메시지 첫머리에 규격 코드나 STEP 번호
      예) "V11-34: 화면 쿼리 21→17. 중복 집계 제거"
필수   outputs/ 의 작업 기록에 커밋 해시를 적는다
필수   작업이 끝나면 반드시 push 한다 — 가이드가 그걸 읽는다
금지   여러 작업을 한 커밋에 섞는 것
금지   .bak 파일로 백업하는 것 — git 이 그 일을 한다
되돌릴 때  git revert <해시>   ★ git reset 은 쓰지 않는다
```

---

## 환경 — 반드시 `python3.11` 로 돌린다

이 장비의 기본 `python3` 는 **3.9.25** 다. 코드가 쓰는 `zip(..., strict=True)` 는
3.10+ 이므로 3.9 로 돌리면 `TypeError: zip() takes no keyword arguments` 로 죽는다.

실측 (2026-08-16, 같은 소스):

| 실행 | `check_all` 통과 | `run_tests` |
|---|--:|---|
| `python3` (3.9) | 144 | test_screens · test_store · test_web · test_spec_ui 등 무더기 실패, ruff 「검사기가 없다」 |
| `python3.11` | 172 | 실패 3파일 |

실패를 보면 **먼저 어느 인터프리터로 돌렸는지 확인한다.**
`zip() takes no keyword arguments` 가 보이면 코드 문제가 아니라 3.9 로 돌린 것이다.
근거: `docs/guide/03_이력.md` 개정 242 (0장 · 부록 E 에 Python 3.11 이상 명시)

## 알아 둘 것 — 시험이 실제 `carwatch.db` 를 복사해서 돈다

`tests/test_integration.py:129` 가 `shutil.copy(ROOT/carwatch.db, ...)` 를 한다.
`tests/test_spec_ui.py` 도 그 `start_server` 를 쓴다.

그래서 **실제 DB 에 남은 상태가 시험 결과를 바꾼다.** 예: `recalc_job` 에
`queued` 행이 하나 남으면(`store/admin.py:83` `JOB_OPEN = ("queued","running")`)
관리 화면이 전부 409 로 잠기고 `test_spec_ui` 가 5건 더 실패하며
`test_spec_ui.py:930` 에서 `TypeError` 로 죽는다.

시험 결과가 갑자기 나빠지면 **코드를 의심하기 전에 `carwatch.db` 상태를 본다.**
