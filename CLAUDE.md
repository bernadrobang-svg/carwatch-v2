# CarWatch v2 — 작업 규칙

> ★ 배포 주소는 ★ `config/deploy.json` 이 정본이다 (★ 개정 684).  ★ 문서에 박지 않는다
> ★ `{base_url}` · `{public_ip}` 는 ★ 그 파일의 값이다


이 파일은 세션이 새로 시작돼도 규칙이 유지되도록 남긴 것이다.
**작업을 시작하기 전에 이 파일을 먼저 읽는다.**

이 프로젝트 전체의 설계 목표는 **「선언과 실제의 괴리」를 막는 것**이다.
v1 이 「저장했다」고 선언했으나 전건 NULL 이었던 사고(`docs/guide/00_개요.md`)의
재발 방지가 목표다. 검증 없는 완료 보고가 이 프로젝트에서 가장 큰 실패 모드다.

---

## 규칙 1. `docs/` 가 유일한 정본이다

코드가 규격과 다르면 **코드를 고친다.** 문서를 코드에 맞추지 않는다.

## 규칙 2. `docs/` 를 개발측이 고치지 않는다

정정이 필요하면 문서를 건드리지 말고 **사용자에게 말한다.**
규격 자체가 비어 있거나 서로 어긋나면 그것을 보고한다 — 임의로 메우지 않는다.
근거: `docs/guide/03_이력.md` 개정 238 「지시서는 개발측이 고치지 않는다」

## 규칙 3. 고친 뒤 반드시 두 도구를 돌린다

```
python3.11 tools/check_all.py
python3.11 tools/run_tests.py
```

## 규칙 4. 「고쳤습니다」라고 하기 전에 실제로 돌려서 확인한다

출력(통과 / fatal / warn 수, 실패한 test 파일명)을 **그대로** 보고한다.

## 규칙 5. 문서 수신

가이드가 주는 문서는 `~/v2/inbox/` 에 들어온다.
「inbox 확인해라」라고 하면

1. `inbox/` 의 파일을 전부 읽는다
2. `*.tar.gz` 가 있으면 풀어 `docs/` 에 반영한다
3. 반영한 뒤 `inbox/` 에서 지운다
4. 무엇을 읽고 무엇을 반영했는지 보고한다
5. 지금 작업과 어긋나면 멈추고 물어본다

## 규칙 6. 작업 기록

모든 작업의 처리와 결과를 `~/v2/outputs/` 에 문서로 남긴다.

```
파일명   YYYYMMDD_HHMM_v버전_제목.md
예       20260816_0130_v133_반입_S5수집.md
         20260816_0210_v134_V11-34_수정.md
```

버전은 지금 소스의 버전이다. 고쳐서 버전이 오르면 새 번호를 쓴다.

문서에 반드시 담을 것

1. 무엇을 하려 했나 (지시 원문)
2. 무엇을 했나 (고친 파일 · 줄 번호)
3. 왜 그렇게 했나 (규격 STEP 번호)
4. 1차 결과 (고치기 전 숫자)
5. 조치 후 결과 (고친 뒤 숫자)
6. 못 한 것 · 막힌 것 · 판단이 필요한 것

```
금지   「고쳤습니다」만 적는 것.  숫자가 없으면 기록이 아니다
금지   1차 결과를 빼고 조치 후만 적는 것
필수   check_all · run_tests 를 돌린 실제 출력을 붙인다
```

## 규칙 8. 형상 관리

`~/v2` 는 git 저장소이고 GitHub 에 push 한다.
`https://github.com/bernadrobang-svg/carwatch-v2` (public · main)

### 형상 관리 — 자동

**작업이 끝나면 물어보지 말고 바로 커밋하고 push 한다.
자격 증명은 저장돼 있다. 인증을 묻지 않는다.**

```
git add -A
git commit -m "규격코드: 무엇을 왜"
git push
```

```
필수  커밋 메시지 첫머리에 규격 코드나 STEP 번호
필수  작업 하나에 커밋 하나.  섞지 않는다
필수  outputs/ 기록을 남긴 뒤 함께 커밋한다
필수  push 까지 끝내야 작업이 끝난 것이다
      가이드가 GitHub 을 읽는다 (개정 257 · S25)
금지  「커밋할까요?」를 묻는 것.  그냥 해라
금지  push 를 미루는 것
```

```
작업 시작 전   git status 로 깨끗한지 본다.  아니면 먼저 물어본다
작업 후        git add -A
              git commit -m "규격코드: 무엇을 왜"
              git push
```

```
필수   커밋 메시지 첫머리에 규격 코드나 STEP 번호
      예) "V11-34: 화면 쿼리 21→17. 중복 집계 제거"
필수   outputs/ 의 작업 기록에 커밋 해시를 적는다
필수   작업이 끝나면 반드시 push 한다 — 가이드가 그걸 읽는다
금지   여러 작업을 한 커밋에 섞는 것
금지   .bak 파일로 백업하는 것 — git 이 그 일을 한다
되돌릴 때  git revert <해시>   ★ git reset 은 쓰지 않는다
```

## 규칙 9. 가이드가 GitHub 에 직접 push 한다

**2026-08-16 부터.  마스터를 안 거친다.**

```
전       가이드 → 파일 → 마스터 → SFTP → inbox/
지금부터  가이드 → GitHub push → 나는 git pull
```

```
필수   작업을 시작하기 전에 항상 git pull 한다
필수   inbox/ 에 새 파일이 있으면 규칙 5 대로 처리한다
      ① 읽는다 ② docs/ 는 이미 반영돼 있다 (tar 를 풀 필요가 없다)
      ③ 처리 후 지운다 ④ 무엇을 읽었는지 보고한다
      ⑤ 지금 작업과 어긋나면 멈추고 묻는다
필수   push 하기 전에 git pull --rebase 한다
      docs/ 는 가이드만, 그 밖은 나만 쓰므로 실제 충돌은 드물다
★     docs/ 는 여전히 내가 고치지 않는다 (규칙 2).  읽기만 한다
      정정이 필요하면 보고한다
```

## 규칙 10. 지시서 읽기

`docs/` 는 53파일 · 734KB 다. **통째로 읽지 않는다.**

```
필수   작업을 시작하면 docs/INDEX.md 를 먼저 본다 (2KB)
필수   고칠 소스가 정해져 있으면 docs/MAPPING.md 로 챕터를 찾는다 (3KB)
      예   score/ 를 고친다  →  30-score/
필수   그 챕터만 연다.  다른 것을 열지 않는다
필수   특정 규칙은 grep 으로 그 줄만 본다
      grep -rn "V11-79" docs/
      grep -n "개정 289" docs/guide/03_이력.md
금지   docs/ 전체를 읽는 것
금지   guide/03_이력.md 를 통째로 읽는 것 (58KB · 개정 292건)
```

## 규칙 11. 소스와 검사도 색인으로 찾는다

```
docs/CHECKS.md   검사 250개 — 코드 ↔ 규격 ↔ 소스 파일·줄
docs/SOURCE.md   소스 112파일 — 줄 수 · 무엇 · 큰 파일의 최상위 정의 위치
```

```
필수   「V11-79 를 고쳐라」 → CHECKS.md 로 파일·줄을 짚고 그 부분만 읽는다
필수   둘은 기계가 만든다 — python3.11 tools/build_index.py
필수   검사를 더하거나 고치면 다시 돌린다.  S28 이 낡은 색인을 잡는다
금지   CHECKS.md · SOURCE.md 를 손으로 고치는 것
★     이 둘은 개발측이 쓰는 유일한 docs/ 파일이다 (가이드 지시).
      나머지 docs/ 는 규칙 2 대로 읽기만 한다
```

---

## 환경 — 반드시 `python3.11` 로 돌린다

이 장비의 기본 `python3` 는 **3.9.25** 다. 코드가 쓰는 `zip(..., strict=True)` 는
3.10+ 이므로 3.9 로 돌리면 `TypeError: zip() takes no keyword arguments` 로 죽는다.

실측 (2026-08-16, 같은 소스):

| 실행 | `check_all` 통과 | `run_tests` |
|---|--:|---|
| `python3` (3.9) | 144 | test_screens · test_store · test_web · test_spec_ui 등 무더기 실패, ruff 「검사기가 없다」 |
| `python3.11` | 172 | 실패 3파일 |

실패를 보면 **먼저 어느 인터프리터로 돌렸는지 확인한다.**
`zip() takes no keyword arguments` 가 보이면 코드 문제가 아니라 3.9 로 돌린 것이다.
근거: `docs/guide/03_이력.md` 개정 242 (0장 · 부록 E 에 Python 3.11 이상 명시)

## ★ 8765 를 뺏지 않는다 — 마스터가 보는 화면이다

**2026-08-16 부터 앞단에 Caddy 가 있다.  주소는 `{base_url}` 다.**

```
브라우저 ──https──▶ Caddy :443 ──http──▶ CarWatch 127.0.0.1:8765
자세한 것   deploy/README.md
```

`carwatch.service` 가 `--host 127.0.0.1 --port 8765` 로 돈다. **그것이 마스터가 보는 화면이다.**

```
금지   python3.11 run.py web 을 8765 로 띄우는 것
      → 서비스가 포트를 못 잡아 재시작 루프에 빠진다
      → 마스터 브라우저에 「사이트에 접근할 수 없음」이 뜬다
      실측 2026-08-16.  내 개발 서버가 127.0.0.1:8765 를 쥐고 있어
      밖에서 접속이 통째로 막혀 있었다
```

### ★★ 재시작이 마스터의 작업을 끊는다 — 2026-08-17 실측

```
마스터가 09:00~09:05 에 전 차종 수집을 눌렀다
나는 그 5분 사이에 carwatch 를 네 번 재시작했다
결과   첫 묶음만 성공 · 나머지 7개가 403 · 하나는 502
원인   CSRF 토큰이 서버 메모리에 있어 재시작마다 전부 무효가 됐다
```

```
고쳤다  토큰을 세션 키에서 만든다 (개정 308).  재시작에 안 죽는다
필수   그래도 재시작 전에 마스터가 쓰고 있는지 본다
      sudo journalctl -u carwatch --since "-10min" | grep POST
금지   몇 분 사이에 거듭 재시작하는 것.  마지막에 한 번만 한다
```

```
확인   systemctl is-active carwatch caddy    둘 다 active 여야 한다
      ss -tln | grep 8765                   127.0.0.1:8765 여야 한다
      curl -sI {base_url}/listings
고친 뒤  sudo systemctl restart carwatch   ★ 코드를 고쳤으면 반드시 재시작
시험용   포트를 따로 쓴다 — run.py web --port 8799
```

## 알아 둘 것 — 시험은 씨앗 DB 로 돈다 (S24 · 개정 246)

`tests/seed.py` 의 `build_seed_db()` 가 `StubEncar` 로 S0~S10 을 돌려
판정까지 끝난 DB 를 만든다. **운영 `carwatch.db` 를 복사하지 않는다.**

전에는 `test_integration.py` 가 운영 DB 를 복사했고, 거기 남은 `queued`
`recalc_job` 하나 때문에 관리 화면이 409 로 잠겨 `test_spec_ui` 가
9건 무더기로 실패했다 — 코드는 하나도 안 바뀌었는데 결과가 달라졌다.
`check_src` 의 `S24` 가 그 재발을 막는다.

그래도 시험 결과가 갑자기 달라지면 **씨앗이 무엇을 넣는지부터 본다**
(`python3.11 tests/seed.py` 가 건수를 찍는다).
