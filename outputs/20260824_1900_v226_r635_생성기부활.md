# r635 — 생성기가 죽어 있었다 · 검사 셋

`SPEC-2026.08.24-r635` · 명령서 29장
2026-08-24 19:00 · v226

---

## 1. ★★★ ① 생성기가 194행에서 죽어 있었다

### 1-1. ★ 까닭 — ★ `say` 가 ★ 세 파일에서 뜻이 다르다

```
IndexError: string index out of range   (tools/build_index.py:194 · head[0])

★ `_checks_in_code()` 가 ★ `say(...)` 를 ★ 검사로 센다
★ ★ `say` 가 ★ 세 파일에 있다 —
   tools/check_src.py     · say(코드, 제목, …)  → ★ 검사 한 줄이다
   tools/check_screens.py · 〃                   → ★ 검사 한 줄이다
   report/screens/build.py:2495 · say(tone, head, …) → ★ ★ 화면 문구다
★ ★ 화면 쪽 `say("", …)` 의 ★ 첫 인자가 ★ 빈 문자열이라
   ★ 검사 코드로 읽히고 ★ `head[0]` 에서 터진다
```

### 1-2. ★ ⓐ 를 골랐다 — ★ 까닭

```
★ 고른 것 — ⓐ `_checks_in_code()` 가 ★ `tools/` 의 `say` 만 읽는다
★ 까닭 —
  ① ★ 이 함수의 ★ 제 뜻이 ★ 처음부터 그것이었다.
     ★ 함수 설명이 ★ 「validate/ 의 Check(...) 와 ★ **tools/** 의 say(...) 를 뽑는다」다.
     ★ `report/screens/` 를 읽은 것은 ★ 애초에 뜻과 다르다
  ② ★ ⓑ(빈 이름 건너뛰기)는 ★ 조용히 넘긴다 —
     ★ 화면 쪽 `say("warn", …)` 은 ★ 이름이 비지 않아 ★ 여전히 「warn」이라는
     ★ 검사로 세어진다.  ★ 증상만 가리고 ★ 뿌리는 남는다
★ ★ 그래도 ★ 다음에 또 나면 ★ 조용하지 않게 했다 —
   ★ 빈 코드가 오면 ★ 「`say(...)` 를 검사로 잘못 센 것이다 · SAY_DIRS 를 보라」로 죽는다
```

### 1-3. ★ 되살린 뒤 — ★ 생성기가 맞춘 것

```
검사 393개 → docs/CHECKS.md
소스 160개 → docs/SOURCE.md
DB 표 44개 → docs/SCHEMA.md
지시서 105파일 → docs/INDEX.md

바뀐 줄 — CHECKS.md 950 · SOURCE.md 318 · INDEX.md 125
★ 손으로 고치지 않았다.  ★ 생성기가 맞췄다 (명령서 지시 그대로)
```

### 1-4. ★★ 되살리자 ★ 결함이 ★ 둘 더 드러났다

**ⓐ 생성기가 ★ 결정적이지 않았다**

```
★ `--check` 를 만들자 ★ 「생성물이 낡았다」가 ★ 늘 떴다
★ 재 보니 ★ 줄 차례가 ★ 실행마다 바뀐다 — V1-08 · V2-10b · V4-06 …
★ 까닭 — 정렬 키가 ★ `(글자, 숫자, 숫자)` 인데 ★ 동률이 있다.
   ★ `sorted` 는 안정 정렬이라 ★ 동률은 들어온 차례를 지키는데,
   ★ 들어오는 것이 ★ `set` 이라 ★ 해시 시드마다 차례가 다르다
조치  ★ 키 끝에 ★ 코드 자체를 둔다 — ★ 키가 완전해진다
확인  ★ 세 번 연속 ★ 종료코드 0
```

**ⓑ `S43-2c`(HDA 금지)가 ★ 자기 이름에 걸렸다**

```
★ 생성기가 살아나 ★ `docs/SOURCE.md` 가 ★ 코드의 함수 이름을 실었다
★ ★ 그 안에 ★ 이 검사의 이름 ★ `s43_2c_no_hda` 가 있다
★ ★ 검사는 ★ `HDA|hda` 를 훑으므로 ★ 제 이름에 제가 걸렸다
조치  ★ 생성물 넷을 ★ 훑기에서 뺀다 — ★ 생성물은 ★ 코드를 비추는 거울이다.
      ★ 진짜 HDA 가 코드에 있으면 ★ 그 코드 파일에서 잡힌다
```

---

## 2. ★ ② S46-32 — ★ 생성물이 낡았으면 실패

```
★ `tools/build_index.py --check` 를 만들었다 —
  ★ 생성기를 돌려 ★ 결과가 파일과 같은지 본다.  ★ 파일은 ★ 되돌린다 (안 고친다)
★ 보는 것 넷 — docs/CHECKS.md · SOURCE.md · SCHEMA.md · INDEX.md
```

★ **일부러 깨서 잡히는 것을 보고 되돌렸다** (원칙 6) —

```
  깨기 전            → ★ 「생성물 4개가 최신이다」
  ① INDEX 한 줄 고침 → ★ 「생성물이 낡았다 — docs/CHECKS.md · docs/INDEX.md」
  ★ 되돌린 뒤        → ★ 「생성물 4개가 최신이다」
```

★ 명령서 말대로다 — ★ **이 검사가 있었으면 ★ 생성기가 죽은 날 바로 잡혔다.**

---

## 3. ★ ③ S46-31 — ★ 이름 짝 표를 두었다

```
SITE_NAME_PAIRS = { "hyundai_certified": "hyundai_cert" }
NOT_A_SITE = (multisite_mapping · dedup_cross_site · server_survival ·
              target_key_map · ui_review · encar_robots)
★ `dealer_site` 는 ★ `docs/*_API.md` 가 없어 ★ 애초에 안 세어진다
지금 — ★ 통과 「규격이 있는 사이트 전부가 config 에 있다 (사이트 11개)」
```

★ 짝 표가 없었으면 ★ `HYUNDAI_CERTIFIED_API.md` 하나로 ★ 늘 빨간불이었다.

---

## 4. ★ ④ S46-24 — ★ 이미 WARN 이다

```
★ 지난 회차(r634)에 ★ fatal → warn 으로 낮췄다.  ★ 이번에 확인만 했다
```

---

## 5. 검사

```
v0_guide   ★ 종료코드 0 — ★ fatal 0 · warn 2
           warn — S46-24(facet 미확인 8종) · S46-30(trace/INDEX.md)
           S46-31 ★ 통과 · S46-32 ★ 통과
run_tests  (이어 적는다 — 도는 중)
```

## 6. ★ 알릴 것

```
ⓐ ★ 커밋을 ★ 한 단계 한 번으로 묶었다 — ★ 이 회차는 ★ 커밋 하나다 (지시 그대로)
ⓑ ★ 앞 회차에 남긴 것 — ★ v3 CSS 14곳(판단 대기) · ★ L-3 색 158(판단 대기) ·
   ★ result_axis 가 새 사이트에 안 들어가는 것 · ★ KB 2,935B 셋째 꼴
```

---

## 7. ★★★ 생성기를 되살리자 ★ 더 큰 것이 드러났다 — ★ 씨앗이 죽어 있었다

### 7-1. ★ 증상

```
run_tests → ★ 12파일 실패
   test_admin_flow · test_collect · test_dict · test_endtoend · test_fixtures ·
   test_integration · test_invariants · test_report · test_run · test_screens ·
   test_spec_ui · test_web
공통 뿌리 — ★ 「S5: 선행 조건 미충족: ★ active 매물 0건」
★ `tests/seed.py` 가 ★ 씨앗 DB 를 못 만든다 → ★ 그 위의 시험이 ★ 줄줄이 무너진다
```

### 7-2. ★ 뿌리 — ★ `collect_group` 이 ★ 18종 ★ 전부 「multi」다

```
★ 명령서 21-2 는 ★ 「★ 수입 8종 전부 ★ multi 로 바꿨다」라 적었다
★ ★ 그런데 ★ 실측하니 ★ 국산 10종도 ★ 「multi」다 —
   G80_25T · G80_EV · G70_25T · G70_20T · GV70_EV · GV60 ·
   KOLEOS_HEV · SPORTAGE_LPI · GRANDEUR_LPG · MODEL_Y
★ ★ 21-2 가 적은 것보다 ★ 넓게 적용됐다
```

★ **그래서 무슨 일이 났나 — 둘이다.**

```
① `tests/seed.py:66` 이 ★ `collect_group == "encar:G80"` 으로 고른다
   → ★ 빈 목록 → ★ 매물이 하나도 안 들어와 ★ S5 가 멈춘다

② ★ `classify(targets, "multi", …)` 는 ★ 열여덟을 ★ 한 후보군으로 본다
   → ★ 실측 「후보 8종 · 배기량 2497」 · 「후보 11종 · 배기량 180」
   → ★ G80_25T 와 G70_25T 는 ★ 가솔린 · 2,400~2,600 으로 ★ 조건이 같다.
     ★ 갈라낼 길이 없어 ★ 아무것도 안 붙는다
```

### 7-3. ★ 고친 것 셋

```
ⓐ `collect/runner.py` — ★ `classify_in_group()` 신설
   ★ 그 묶음의 차종만 후보로 둔다.  ★ `CollectGroup` 이 제 차종을 안다
   ★ 한 묶음은 ★ 쿼리가 같은 것들이라 ★ 좁혀도 잃는 것이 없다
   ★ ★ 이것이 없으면 ★ 내가 만든 묶음 이름(`multi:G80`)과
     ★ config 값(`multi`)이 안 맞아 ★ 후보가 0 이 된다

ⓑ `tests/seed.py` · `tests/test_run.py` — ★ 차종 키로 고른다
   ★ `collect_group` 으로 고르면 ★ config 가 바뀔 때마다 ★ 씨앗이 죽는다

ⓒ `config/targets.json` — ★ 국산 10종을 ★ `encar:*` 로 되돌렸다
   ★ 수입 8종은 ★ `multi` 그대로다 (21-2 가 시킨 그것)
```

### 7-4. ★ 되돌리면 되는지 ★ 재서 증명했다

```
국산 10종을 encar:* 로 되돌린 뒤 —
   tests/test_fixtures.py → ★ 통과   (전 「G80 2497 → 후보 8종」 실패)
   tests/test_dict.py     → ★ 통과   (전 「collect_group 미정의: encar:KOLEOS」)
   tests/seed.py          → ★ 산다   result_score 12 · result_axis 312 ·
                                     등급 [C 2 · D 3 · E 1 · EXCLUDED 6]
차종 18종 · collect_group ★ 16개 ★ 그대로다
수입 분류도 ★ 그대로다 — KB X3 → X3_IMPORT · KB GLC → GLC_IMPORT · K카 G80 → G80_25T
```

```
★★ ★ 여쭐 것 — ★ 국산 10종을 ★ `encar:*` 로 되돌린 것이 맞습니까.
   ★ 21-2 가 ★ 「수입 8종」이라 적어 ★ 그대로 맞췄습니다.
   ★ ★ 만약 ★ 열여덟 전부를 ★ 「multi」로 두실 뜻이었다면 ★ 말씀해 주십시오 —
     ★ 그때는 ★ 분류를 ★ 다른 얼개로 바꿔야 합니다.
     ★ 지금 얼개로는 ★ G80 과 G70 을 ★ 갈라낼 수 없습니다 (연료·배기량이 같다)
```

## 8. ★ 이 회차의 커밋 — ★ 하나다 (지시 그대로)
