# KB 는 20종만 — 15,836 → **15,825건** · 이미 20종만 받고 있었다

```
버전     소스 v258 · 지시서 SPEC-2026.08.26-r757
때       2026-08-26 20:00
지시     ★★★ KB 전량 수집을 멈춰라.  내가 잘못 시켰다 (명령서 60장 · 오판 130)
         ★ targets.json 의 site_query.kbchachacha 로 20종만 받아라
         ★ 「15,836 → 몇 건」으로 적어라
         ★ 이미 들어온 것 중 20종이 아닌 것은 out_of_scope 로 표시해라
앞 작업  b9a99ec
```

---

## 1. 멈췄다

```
pkill -f collect_kbchachacha      ★ 멈췄다
★ 그때까지 받은 것 — ★ 매물 295건 (상세 256건)
```

---

## 2. ★★★ 「15,836 → 몇 건」 — ★ **15,825건**

`targets.json` 의 `site_query.kbchachacha` 로 좁혀 **목록만** 다시 세었다
(`--narrow --dry` · 봇 차단 0건).

| 차종 | maker · class | 쪽 | 매물 |
|---|---|--:|--:|
| `GRANDEUR_LPG` | 101 · 1101 그랜저 | 203 | **6,917** |
| `SPORTAGE_LPI` | 102 · 1151 스포티지 | 77 | **2,595** |
| `G80_25T`·`G80_EV` | 189 · 2684 G80 | 80 | **2,547** |
| `GV70_EV` | 189 · 2771 | 27 | 752 |
| `G70_25T`·`G70_20T` | 189 · 2698 | 25 | 695 |
| `GLC_IMPORT` | 108 · 2646 | 23 | 638 |
| `X3_IMPORT` | 107 · 1910 | 15 | 420 |
| `MODEL_Y` | 190 · 2774 | 9 | 214 |
| `Q5_IMPORT` | 109 · 1960 | 9 | 214 |
| `XC60_IMPORT` | 117 · 2237 | 8 | 191 |
| `XC40_IMPORT` | 117 · 2708 | 5 | 126 |
| `S60_IMPORT` | 117 · 2229 | 5 | 113 |
| `KOLEOS_HEV` | 105 · 2862 | 5 | 104 |
| `V60CC_IMPORT` | 117 · 2235 | 4 | 101 |
| `NX_HEV` | 133 · 2563 | 4 | 92 |
| `RX_IMPORT` | 133 · 2132 | 3 | 55 |
| `GV60` | 189 · 2805 | 3 | 51 |
| **합** | 17묶음 | **505** | ★ **15,825** |

```
★★★ ★ **15,836 → 15,825.  ★ 거의 안 줄었다**
```

---

## 3. ★★★ 까닭 — ★ **이미 20종만 받고 있었다**

★ 실측했다.  `--narrow` 는 **처음부터** `targets.json` 의 17묶음만 부른다.

```
저장된 KB 매물 295건의 차종
  G80  130 · GLC 93 · X3 33      ← ★ 다 우리 20종이다
  차명을 아직 못 읽은 것 39       ← 상세를 못 받았다
★★★ ★ 쏘렌토 · 카니발 · K8 · 싼타페 · 모하비 · 모닝 — ★ **0건.  ★ 한 건도 없다**
★ ★ 17묶음에 ★ 그 차종의 `classCode` 가 ★ 아예 없다
```

★★ **줄지 않는 진짜 까닭** — KB 의 `classCode` 는 **차종(모델) 단위**다.
우리 20종 가운데 셋이 대형 판매 차종이라 그것만으로 76%다.

```
그랜저 6,917 ＋ 스포티지 2,595 ＋ G80 2,547 = 12,059  (15,825 의 ★ 76%)
```

★ 그 6,917건에는 **가솔린 그랜저도 다 들어 있다** — 우리 것은 `GRANDEUR_LPG` 뿐인데도다.
★ 「보지도 않을 것」이 **차종 밖이 아니라 차종 안**에 있었다.

---

## 4. ★★★ 더 좁힐 수 있다 — ★ 실측했다 (`carCode`)

★ KB 가 **세대(`carCode`) 표**를 준다.

```
GET /public/search/carName.json?makerCode=101&classCode=1101   → 200
  3301 디 올 뉴 그랜저          2022~현재
  3302 디 올 뉴 그랜저 하이브리드  2022~현재
  3048 더 뉴 그랜저             2019~2022
  3049 더 뉴 그랜저 하이브리드     2019~2022
  2852 그랜저IG · 2879 IG 하이브리드 · 1411 HG · 1486 하이브리드 …  (15세대)
```

★★ 그리고 **목록이 그 코드로 실제로 걸린다.**

```
list.empty?…&carCode=3301   쪽1 40건 · ★ 기본 쪽1과 겹치는 것 ★ 5건뿐
list.empty?…&carCode=3302   쪽1 40건 · 겹치는 것 2건
list.empty?…&carCode=9999   ★ 0건        ← ★ 모르는 코드는 아무것도 안 준다
★ 곧 ★ 걸러지는 것이 맞다.  ★ 흉내가 아니다
```

★ 수집기와 **같은 멈춤 규칙**으로 세었다.

| 세대 | 건수 |
|---|--:|
| 그랜저 **전체** (1101) | 6,917 |
| 　디 올 뉴 그랜저 3301 | **507** |
| 　디올뉴 하이브리드 3302 | **220** |
| 　더 뉴 그랜저 3048 | **1,879** |
| 　★ 위 셋 합 | **2,606** (전체의 38%) |

```
★★ 그랜저 하나만 ★ 세대로 좁혀도 ★ 6,917 → 2,606 이다 (★ -4,311)
★ 세대를 더 줄이면 (2022~ 두 개만) ★ 727건까지 내려간다
```

### ★ 여쭐 것 — ★ 세대 코드는 규격이 정한다

```
★ ★ 내가 targets.json 에 ★ **안 넣었다** — ★ 규칙 2 · 금지 6
★ ★ 「어느 세대가 우리 GRANDEUR_LPG 인가」는 ★ 마스터·가이드가 정하실 것이다
여쭙는다  ⓐ site_query 에 `carCode` 를 더할까 — ★ 그러면 15,825 가 크게 준다
         ⓑ 지금대로 차종 단위로 받고 ★ 상세를 열어 거를까 (3-2)
            ★ 그러면 ★ 상세를 15,825건 다 받아야 한다 — ★ 엿새 그대로다
★ ★ 실측만 적었다.  ★ 지어 넣지 않았다
```

---

## 5. ★★★ 이미 들어온 것 — ★ **접을 것이 0건이었다.  ★ 대신 도구가 틀렸다**

```
python3.11 tools/fold_out_of_scope.py --dry
★ 접을 것 — ★ 우리 차종이 아닌 것 ★ **0건**
```

★★ 그런데 **돌렸으면 우리 차 120건이 접힐 뻔했다.**

```
옛 규칙   target_key IS NULL 이면 ★ 다 접는다
실측     KB 의 G80 130 · GLC 93 · X3 33 이 ★ 다 target_key 가 없다
        ★ 우리 차종인데 ★ 갈래(연료·트림)를 아직 못 붙인 것이다
★★★ ★ 그것을 접으면 ★ **마스터께서 보실 차가 사라진다**
```

★ 「모르는 차」와 「우리 차가 아닌 차」는 **다르다** (`UI_REVIEW` 9a 가 이미 그렇게 갈랐다).
★ 도구만 그것을 안 지키고 있었다.

```
고쳤다  tools/fold_out_of_scope.py  _split() — ★ 세 갈래로 가른다
  ① 우리 차종이 아니다        → ★ 접는다      (known_model_of 가 None)
  ② 우리 차종인데 갈래만 없다  → ★ 안 접는다   (여쭐 것이다)
  ③ 차명을 아직 못 읽었다     → ★ 안 접는다   (「모른다」지 「아니다」가 아니다)
```

★ 그리고 **옛 규칙으로 잘못 접힌 39건을 되돌렸다** (`--repair`).

```
★ 상태 — 전 → 후
   active           4836 →   4836
   gone              135 →    135
   new               120 →    159      ← ★ 39건이 돌아왔다
   out_of_scope     8415 →   8376
★ 지우지 않았다.  ★ 접었을 뿐이고 ★ 되돌린 것이다
```

---

## 6. 그 밖에 한 것

```
tools/collect_kbchachacha.py
  ★ 「전체 164,490 중」 → 「우리 차종만 받는다 — 17묶음 (명령서 60장)」
  ★ 「가려 받지 마라 = 전량」 주석을 ★ 60장 뜻으로 고쳤다
  ★ --pages · --count 는 조사용이라고 적어 두었다
validate/v0_guide.py  ★ S46-77 신설 — 「KB 는 우리 20종만 받는가」
  ① 좁히는 조건을 targets.json 에서 읽는가
  ② 저장하는 길(--narrow)이 그 조건을 쓰는가
  ③ 차종 코드를 코드에 박지 않았는가 (금지 6)
```

---

## 6b. ★★★ 원문 보관이 낸 결함 — ★ V4 차수가 통째로 죽어 있었다

★ 이번 회차 `check_all` 에서 잡혔다.

```
V4  검사가 예외로 죽었다: JSONDecodeError: Expecting value: line 16 column 1
★ 통과 381 → 363 · warn 12 → 8   ← ★ V4 의 검사 스물일곱이 통째로 안 돌았다
```

★★ **내가 어제 켠 원문 보관 때문이다.**
`raw_response` 에 이제 **엔카 말고도** 들어오는데,
엔카 원문(JSON)의 키를 짚는 자리들이 **사이트로 안 좁혀 있었다.**

```
validate/v4_mapping.py:497   endpoint='detail' 전건을 json.loads → ★ KB 의 HTML 에서 죽는다
validate/v1_collect.py:780   V1-25 「JSON 이 아니다」 — ★ 볼보·BMW 목록(HTML)을 거짓 실패로 잡는다
validate/v1_collect.py:856   V1-21 — 엔카 봉투(SearchResults)만 뜻이 있다
collect/runner.py:682        ★ S6 파서 — ★ json.loads(body) 에 try 가 없다
                             ★ 지금은 그 매물에 target_key 가 없어 ★ **우연히** 안 걸린다
```

```
고쳤다  넷 다 ★ `site='encar'` (S6 은 `r.site=?` 로 어댑터 사이트) 로 좁혔다
        ★ V4 27검사 · 실패 5 로 ★ 다시 돈다
검사    ★ S46-78 신설 — 「엔카 전용 경로가 좁혀 있는가」
        ★ 세 자리를 글자로 짚는다.  ★ 자리가 사라지면 그것도 알린다
```

★★ 「원문을 한 표에 모은다」는 규격이다 (`collect_kia_cpo.py` 8행 「엔카와 같은 표에 넣는다」).
★ 그러면 **읽는 쪽이 좁혀야 한다** — 그것을 어제 같이 안 했다.  **내 잘못이다.**

---

## 7. 결과

```
python3.11 tools/run_tests.py
  test_admin         통과
  test_admin_flow    통과
  test_collect       통과
  test_crosssite     통과
  test_dict          통과
  test_endtoend      통과
  test_fixtures      통과
  test_integration   통과
  test_invariants    통과
  test_pipeline      통과
  test_registry      통과
  test_report        통과
  test_run           통과
  test_score         통과
  test_screens       통과
  test_spec_ui       통과
  test_store         통과
  test_watch         통과
  test_web           통과
  ruff               통과  (F821,F811,B905,F841,DTZ005,F401)

  임시 DB 62개 치움 (검사가 디스크를 채우지 않는다)

결과: 통과
```

```
python3.11 tools/check_all.py
DB carwatch.db · run 20260825T021516
통과 384 · fatal 5 · warn 12 · 환경 차이 2 · 미실행 8

■ FATAL — 고쳐야 한다
  S46-36    폐기된 요구가 규격에 안 살아 있는가 — ★ 폐기된 요구가 규격에 살아 있다 2곳 — 요구 121 → KBCHACHACHA_API.md:364 · 요구 121 → KBCHACHACHA_A
  V1-08     동일 코드 실패율 100% 인 엔드포인트 없음 — 2건 (예: detail: 328건 전량 실패, inspection: 182건 전량 실패)
  V1-21     받아 두고 안 펼쳐진 원문이 없음 — 182
  V1-23     필요한 조합 대비 받은 카탈로그 비율 — 127조합 · 78.9% · 카탈로그 없는 매물 633건 · 사이트가 안 줌 empty 21조합 · 매물 586건 (예: 8245694202607
  V2-01     ok 원문 수 == CORE 행 수 — 5774

■ warn — 진행은 된다
  S46-24    facet 미확인 차종이 없는가 — ★ facet 미확인 47종 — G80_25T.bobaedream · G80_25T.kbchachacha · G80_EV.bobaedream · G80
  V1-07     매물별 엔드포인트 4종 상태 존재 — 4242
  V4-06     RAW 경로가 등록부에 있는가 — 3
  V4-06b    등록부에 있는데 RAW 에 없는 유령 경로 — 48
  V4-11b    판정에 안 쓰는 미분류 경로 — 319
  V4-21     같은 이름의 공개 함수가 두 모듈에 없음 — 7
  V3-39     이론가와 실제 중앙값의 차가 상한 안 — 최대 33.2%
  V3-47     축별 차종 간 결측률 편차가 상한 안 — 3축
  V3-03     축별 source 값 종류 >= 2 — 1건 (예: taste.fitting)
  V3-04     축별 값 종류 >= 2 — 2건 (예: state.consumable, taste.fitting)
  V3-91     가이드 검산 일곱 줄이 표대로 나옴 — 7줄 다름
  V3-96     value IS NULL 과 source 모름 건수 차 — source 모름 33989 · 차 +32,797

■ 환경 차이 — 결함이 아니다
  V1-05     실행 시작 시각 기준이라 DB 만으로는 재현되지 않는다
  V4-01     HMAC 키가 다르면 plate_hash 가 전건 불일치한다

■ 미실행 — 이번 실행에서 그 단계를 안 돌았다
  V1-20     이번 실행에 카탈로그를 안 받았다
  V4-29     판정을 막는 미분류가 없다 — 잴 것이 없다
  V4-30     판정을 막는 것이 없다
  V3-68     부록 F 에 축 목록이 없다
  V3-64     규격에서 경계를 못 읽었다
  V9-09     비율이 같은 매물 짝이 없다 — 잴 것이 없다
  V10-24    사전 확정 이력이 없다
  V11-121   두 사이트에 같이 올라온 차가 없다 — 나란히 낼 것이 없다

★ `S43-3` 이 사라졌다 — ★ 가이드께서 `00_버전.md` 를 고치셨다 (r757).
★ 남은 fatal 다섯 — ★ `S46-36` 하나가 규격 쪽이고 ★ 넷은 원문 쪽이다
```

---

## 8. 못 한 것 · 판단이 필요한 것

```
★ 여쭐 것 — 4장의 `carCode`(세대) 좁히기 ⓐ/ⓑ
★ 앞 회차에서 넘어온 것
   00_버전.md:18 (r749)  ★ **가이드께서 고치셨다 — S43-3 통과**
   STEP 149j ↔ v4m_admin_시안  ★ 아직 답을 못 받았다
   ★ S46-36 — ★ 자리가 :142 에서 ★ **:364 · :367 로 옮겨 갔다** (2곳)
     ```
     364  ★ 마스터 08-25 — 「★ 내가 왜 KB는 ★ **수입만** 받으라고 했지?
     365     ★ 내가 다 받으라고 했다.
     366     ★ **전체가 빠진 것 없이** 전체를 받으라고 했는데 ★ 가려 받겠다고」
     ```
     ★ 이것은 ★ **마스터 말씀을 인용한 줄**이다 — ★ 살아 있는 지시가 아니다.
     ★ 그런데 ★ 08-26 정정(60장)으로 ★ 그 요구 자체가 폐기됐으니 ★ 검사가 옳게 잡는다
     ★ 여쭙는다 — ★ 인용 줄에 무엇을 적어 「죽은 줄」로 표시할까
       (지금 표시는 틀렸다·폐기·취소·물렸다·아니다·안 준다·✘·정정 여덟뿐이다)
★ KB 수집은 ★ 멈춰 둔다 — ★ 세대 좁히기를 정하신 뒤에 다시 건다
   ★ 지금 다시 걸면 ★ 15,825건 상세를 받는다 (하루 2,880건 · 엿새)
① 3-2 걸러 저장 다시 받기 · ② 3-3 되돌리기(끝) · ③ 멀티 매핑 · ④ 견주기
⑤ v4m — 상세·현황·관리·미판정·비교는 아직 표·목록 꼴이 남아 있다
```
