# `excluded` 축 분해 · `/why` 판정 근거 확인

작업 시각 2026-08-16 17:05~17:10 · 소스 v134 · 실행 `python3.11`
커밋 `1b2838c`(개정 271 본작업) · `<이번 커밋>`

---

## 1. 무엇을 하려 했나 (지시 원문)

> ```
> 1  NULL 원인 확정 (숫자로)     2  V2-31 · V2-32
> 3  KOLEOS_HEV 완주            4  숫자 보고
>    사전에 없는 값 몇 종 · 등록부 미분류 몇 건 · 매핑 실패 몇 건
>    판정된 매물 몇 건 · 등급 분포 · ★ excluded 축 몇 개 · 왜
> 5  /listings 에 실제 매물이 뜨는지 · /why 에 판정 근거가 나오는지
> ```

**1·2·3 은 이미 끝나 push 돼 있다** (커밋 `1b2838c`). 가이드가 그 push 전에 쓴 지시로 보인다.
이 기록은 **아직 안 낸 두 가지** — `excluded` 축 분해와 `/why` 확인이다.

## 2. 무엇을 했나

새로 고친 코드는 없다. 이미 만들어진 판정 결과를 **세어서** 냈다.
`/why` 는 실제로 렌더해 절과 행 수를 확인했다.

## 3. 왜 그렇게 했나 (규격)

- 개정 271 — 「54% 는 그 자체가 결함 신호다」 · 「짐작으로 고치지 말고 숫자로 확정하라」
- 10장 STEP 97 · `V11-22` — `excluded` 축은 0점이 아니라 **제외**다. 왜 제외인지 화면이 말한다
- 14장 개정 256 — `/why` 9절 순서

## 4. 1차 결과 (앞서 보고한 숫자)

```
core_listing 7,661 · 범위 안 3,472 · 차종 미정 4,189
판정 240건 · 등급 C 90 · B 63 · E 35 · NOT_RATED 30 · D 20
매핑 실패 0 · 사전 미분류 18종 · 등록부 미분류 91건
check_all 통과 196 · fatal 4
```

**`excluded` 는 「몇 개인지」만 알고 「왜」를 안 냈다.**

## 5. 조치 후 결과

### ★ `excluded` 축 — 몇 개 · 왜

```
축 전체 4,046행 · excluded 1,017행 (25%)
```

| 축 | excluded/전체 | 사유 |
|---|---|---|
| `spec.tinting` | **227/238** | `no_mention` — 원문이 틴팅을 언급조차 안 한다 |
| `spec.hda` | **225/238** | `catalog_missing` 195 · `missing` 30 — 카탈로그를 안 받았다 |
| `history.damage` | 126/238 | `missing` — 이력 원문이 없다 (record 404) |
| `history.insurance` | 106/238 | `missing` — 같음 |
| `safety.diagnosis` | 31/238 | `missing` — 진단이 없는 매물 |
| `safety.warranty_product` | 31/238 | `missing` |
| `color` | 31/238 | `missing` |
| `price` | 30/238 | `expected_unavailable` — 감가 곡선이 없는 구간 |
| `spec.*` (bsd·hud·scc·svm·sunroof) | 각 30/238 | `missing` — 카탈로그 미수신 |
| `warranty.general` · `warranty.power` | 각 30/238 | `missing` |

사유별 합계:

```
missing               565   원문이 없다 (record·inspection 404 · 카탈로그 미수신)
no_mention            227   원문은 있는데 그 항목을 언급 안 함 (틴팅)
catalog_missing       195   카탈로그를 안 받아 사양 축을 못 봄
expected_unavailable   30   감가 곡선 구간 밖 — 없는 것이 정상
```

**★ 셋을 가른 것이 핵심이다.** `missing`(못 받음) · `no_mention`(받았는데 말이 없음) ·
`expected_unavailable`(원래 없는 것)은 뜻이 다르다. v1 은 이걸 전부 0점으로 눌렀다.

### 등급이 그렇게 나온 이유

```
NOT_RATED 30건   분모가 전건 50.0 (만점 555)  → 분모 부족으로 등급을 안 매김
E 35건 사유      리스·렌트 상품 18 · 골격 손상 6 · 계약중·판매완료 6
                수리비 10% 초과 5 · 저당 3
```

### `/why` — 판정 근거가 나온다

```
GET /why/3 → 200
  무엇을 조회했는가 · 축별 판정 · 엔카 진단 · 확인 못 한 것 · 비용 ·
  주요 옵션 · 참고 자료   ← 7절 전부 있음
  축 표 30행 · excluded 표시(—) 17곳
```

### `/listings` — 매물이 뜬다

```
/          200 · /listings 200 (매물 200건) · /recommend 200
/market · /dealers · /notready 200 · 외부 IP 200
```

## 6. 못 한 것 · 막힌 것 · 판단이 필요한 것

### ① `spec.hda` 225건이 `catalog_missing` 이다 — 카탈로그를 안 받았다

```
raw_response catalog 21건 · 매물 238건
```

카탈로그는 S7 이 모델당 1회 받는데(STEP 21c), 이번 실행에서 21건만 받았다.
`spec.hda`·`spec.hud` 등 사양 축이 그래서 통째로 excluded 다.
**S7 을 더 돌리면 분모가 늘어 등급이 올라간다.** 다음 작업 후보다.

### ② `spec.tinting` 227건은 `no_mention` — 이건 정상이다

원문에 틴팅 언급이 없다. 「없다」가 아니라 「모른다」로 둔 것이 맞다 (A-4).

### ③ 남은 fatal 4건

```
V1-16  이번 run_id 밖의 행을 보지 않음 — 1
V2-18  parse_rule 재처리 후 전 봉투가 현재 parse_version — 30
V4-11  unclassified 존재 — 27
V4-20  dict_option_code 에 문장(공백·한글)이 없음 — 10
```

### ④ 대기 중 둘

facet 수신(`/admin/collect` 「facet 만」) · 큐 A/B 결정(`V10-22`·`V10-23`).
