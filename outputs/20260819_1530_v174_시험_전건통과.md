# 순서 5 — `run_tests` 실패 2건을 닫았다 · 전건 통과

2026-08-19 15:30 · v174 · **SPEC-2026.08.19-r409**

`inbox/ORDER_00_순서.md` 5번

---

## 1. 1차 결과 (고치기 전)

```
python3.11 tools/run_tests.py
결과: 실패 — test_endtoend.py, test_web.py
```

---

## 2. `test_web` — 화면이 아닌 것을 화면으로 셌다

```
errors.ValidationError: 없는 리포트입니다:  [step=STEP 91b]
  /reports/{name} 은 파일을 준다.  <h1> 도 시안도 없다
```

```
고침   web/routes.py 의 NON_SCREEN_VIEWS 를 시험이 본다
      ★ 시험이 따로 목록을 들면 갈린다 — 그것이 V11-30 의 뜻이다
★ 세는 자리도 같은 기준으로 고쳤다.  안 그러면 「30/31」로 영원히 하나 모자란다
```

---

## 3. `test_endtoend` — 셋이 겹쳐 있었다

### ① ★★ 화면과 검사가 다른 것을 세고 있었다

```
_blocking_paths   103건   ← 미분류 화면이 쓰던 것
parser_paths      32건    ← V4-11 · 목록이 쓰는 것
겹치는 것         22건
```

★ **규격이 바로 이것을 경고합니다** —
「검사와 화면이 다른 것을 세면 「32건」과 화면의 수가 어긋난다」

```
고침   V4-11 이 정본이다.  화면도 그것을 본다
      ★ store 는 parse·tools 를 못 부른다 (V4-22) —
        report/screens/admin.py::blocking_set() 이 이어 준다
```

### ② 파서 경로 지식이 `tools/` 에 있었다

```
store → tools 는 역방향이다 (V4-22).  store 는 contracts·errors 만 부른다
고침   parse/encar/paths.py 를 새로 만들었다 — 파서 지식은 parse 의 것이다
      tools · validate · report 가 그것을 읽는다.  두 벌 두지 않는다
```

### ③ ★ 시험 자료의 분류를 사람에게 미루고 있었다

```
V4-11 이 씨앗 DB 에서 16건 걸렸다
★ 그 16건은 스텁 원문이 만든 것이다.  마스터가 정할 일이 아니다
★ 시험이 사람의 판단을 기다리면 이 시험은 영영 실패한다
```

```
고침   시험이 만든 자료는 시험이 정한다 (S24)
      파서가 읽는 경로 → unused_by_policy
      ★ in_use 로 두면 core_column 을 요구한다 (V4-07 이 잡았다)
      ★ 판정을 안 막는 미분류 1건은 남긴다 — 분류 화면을 재려면 대상이 있어야 한다
```

### ④ `V4-29` 가 「다 분류한 DB」에서 늘 실패했다

```
전   막는 것이 0 이면 「판정을 막는 것을 표시하지 않는다」로 잡았다
후   막는 것이 없으면 「잴 것이 없다」 — 해당 없음
★ 검사는 「할 수 있는가」를 봐야 한다.  「한 적이 있는가」가 아니다
```

---

## 4. ★ 만들다 낸 결함 둘 — 검사가 잡았다

| 무엇 | 잡은 것 |
|---|---|
| `web` 이 `tools` 를 불렀다 | **V4-22** 역방향 import |
| 미분류 화면이 한 쪽에 **52쿼리** | **V11-34** 상한 20 |

```
52쿼리   blocking_set 이 blocking_rows 를 불러 엔드포인트마다 원문을 훑었다.
        「막는가」만 알면 되는데 관측까지 셌다
고침     blocking_keys() — 셈 없이 경로만 가린다.  52 → 20
```

---

## 5. 조치 후 결과 — ★ 실제 출력

```
python3.11 tools/run_tests.py
  임시 DB 62개 치움 (검사가 디스크를 채우지 않는다)
결과: 통과                      ← ★ 전건 통과

python3.11 tools/check_all.py
통과 300 · fatal 3 · warn 7 · 환경 차이 1 · 미실행 5
■ FATAL   V1-11 · V4-11 · V3-38   ← 셋 다 가이드 몫 (지시 §3)

python3.11 tools/check_src.py
S7 · S14 · S15 · S24 전부 통과
```

| | 전 | 후 |
|---|--:|--:|
| `run_tests` | 실패 2 | **통과** |
| `check_all` 통과 | 300 | 300 |
| fatal | 3 | 3 |

---

## 6. 못 한 것 · 여쭐 것

### ① 운영 DB 의 `V4-11` 32건은 그대로입니다

```
★ 시험 DB 만 시험이 정했습니다.  운영은 마스터가 정합니다 (지시 §3)
★ 목록과 판단 재료는 다 냈습니다 —
  outputs/20260819_registry32_목록.md · /admin/registry
```

### ② 「막는 것」의 정의가 하나로 모였습니다 — 확인해 주십시오

```
정본   parser_paths() — 「파서가 실제로 그 경로를 읽는가」
버림   _blocking_paths() — 형제 경로가 분류돼 있으면 막는다는 옛 셈 (103건)
★ 옛 셈은 아직 코드에 남아 있습니다 (부르는 곳이 없습니다).
  지워도 될지 정해 주십시오 — 되돌릴 수 없는 것이라 그대로 뒀습니다
```
