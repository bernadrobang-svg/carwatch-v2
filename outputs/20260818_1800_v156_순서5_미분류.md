# 순서 5 — 미분류 349건 · 판정이 막혀 있다

2026-08-18 18:00 · v156 · **SPEC-2026.08.18-r365**

`inbox/ORDER_00_순서.md` **5번**

---

## 1. 무엇을 하려 했나 (지시 원문)

```
1  ★ 판정을 막는 것만 막는다 (V4-11 을 좁힌다)
   ★ 349개 전부가 막을 이유가 없다.  이것부터 하면 판정이 풀린다
2  원인별로 가른다 — 새 필드 · 이름만 다름 · 늘 빈 것 · 정말 새것
3  이름만 다름 · 늘 빈 것은 자동 제안
4  엔드포인트별로 낸다
```

★ 2·4 는 어제 했습니다 (`v147`).  **1·3 이 남아 있었습니다.**

---

## 2. 1차 결과

```
check_all   통과 268 · fatal 7
V4-11       판정을 막는 미분류 100건
V4-11b      판정에 안 쓰는 미분류 249건
```

---

## 3. ★★ 5-1 — 100건 중 68건은 막을 이유가 없었습니다

### 원인 — 잎 이름만 견주고 있었습니다

```python
if bare in used or bare.split(".")[-1] in used or …
```

`used` 는 파서가 읽는 경로입니다.  파서는 `Badge` 를 읽는데,
**`MultiView.Badge` 가 잎 이름 `Badge` 로 걸려** 함께 막고 있었습니다.

```
list 24건이 전부 MultiView.* 였습니다
  MultiView.AdType · MultiView.Badge · MultiView.BadgeDetail
  MultiView.BuyType · MultiView.Color …
★ 파서가 읽는 것은 「Badge」지 「MultiView.Badge」가 아닙니다
```

### 고침 — 전체 경로로 견줍니다

```
V4-11   판정을 막는 미분류   100 → 32
V4-11b  판정에 안 쓰는 미분류 249 → 317
```

남은 32건은 **파서가 실제로 그 경로를 읽는 것**입니다.

| 엔드포인트 | 건수 |
|---|--:|
| record_summary | 17 |
| inspection_summary | 8 |
| inspection | 2 |
| facet | 2 |
| list · ev_battery · platform_check | 각 1 |

★ `record_summary` · `inspection_summary` 가 25건입니다 —
**어제 순서 3에서 처음 펼친 요약 원문**들입니다.  펼치니 파서가 읽는
경로가 드러났고, 그것이 아직 미분류입니다.

---

## 4. 5-3 자동 제안 — 파일로 냅니다

```
python3.11 tools/classify_unclassified.py --suggest
제안 → config/field_usage.suggested.json
★ 정본에 자동으로 넣지 않았습니다.  사람이 확인한 뒤
  config/field_usage.json 으로 옮깁니다 (STEP 87)
```

```
제안 229건
  ② 이름만 다름   68건 → 이미 분류된 같은 이름을 짚어 줍니다
  ③ 늘 비어 있음 161건 → suggested_usage: "not_provided"
  ④ 새로운 것    120건 → ★ 넣지 않았습니다.  사람이 봐야 합니다
```

예

```json
"inspection_summary:inspName": {
  "kind": "② 이름만 다름",
  "reason": "unused_by_policy — 「inspection:master.detail.inspName」과
             같은 것으로 보입니다",
  "observed": 200, "of": 200
}
```

---

## 5. 조치 후 결과 — ★ 실제 출력

```
python3.11 tools/check_all.py
통과 268 · fatal 7 · warn 7 · 환경 차이 1 · 미실행 2
  V4-11     unclassified 존재 — 32
  V4-11b    판정에 안 쓰는 미분류 경로 — 317

python3.11 tools/run_tests.py
결과: 실패 — test_endtoend.py
```

| | 전 | 후 |
|---|--:|--:|
| check_all 통과 | 268 | **268** |
| fatal | 7 | **7** |
| **판정을 막는 미분류** | **100** | **32** |
| 제안이 있는 것 | 화면에만 | **파일로 229건** |

---

## 6. 못 한 것 · 판단이 필요한 것

### ① 32건이 아직 판정을 막습니다

`V4-11` 은 여전히 fatal 입니다.  남은 32건은 **파서가 실제로 읽는 경로**라
막는 것이 맞습니다.  마스터가 `/admin/registry` 에서 분류해야 풀립니다.

★ **25건이 어제 처음 펼친 요약 원문에서 나왔습니다** —
받아 놓고 안 펼치면 미분류도 안 드러납니다.  펼쳐야 보입니다.

### ② ③ 「늘 비어 있음」 161건을 자동으로 넣을까요

제안 파일까지만 만들었습니다 (STEP 87 「자동 분류를 그대로 정본에 넣지
않는다」).  넣으면 미분류가 **349 → 188** 로 줍니다.
표본 200건에서 전건 비어 있는 것이지 전건은 아닙니다.

### ③ 그대로 남은 fatal 7

```
V1-16 · V1-21(마스터 수집 73쪽) · V2-01 · V2-18 · V4-11(32) · V3-38 · V11-77
```
