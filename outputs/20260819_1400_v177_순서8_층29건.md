# 순서 8 — 층 29건 반영 · `trace_fill` 재실행 · `S39-1` · `S39-2`

`SPEC-2026.08.19-r409` · 2026-08-19 14:00 · v177

---

## 1. 무엇을 하려 했나 (지시 원문)

```
8   층 29건 반영 후 trace_fill 재실행 — ★ 유지 6건은 미구현으로 둔다
```

근거 `inbox/ANSWER_guide5.md` §2 (개정 397) · `SPEC-2026.08.19-r398`

```
바뀐 것 21건 · 유지 6건 (진짜 미구현) · RULES.md 로 뺀 것 2건
필수   trace_fill.py 를 다시 돌린다
       ★ 유지 6건은 층이 아니라 소스가 없는 것이다 — 「미구현」으로 두고 개발 대상에 넣는다
금지   유지 6건을 층을 바꿔 억지로 찾히게 만드는 것
검산   S39-1
```

---

## 2. 1차 결과 (고치기 전 숫자)

```
check_all   통과 301 · fatal 3 · warn 7 · 환경 차이 1 · 미실행 5
run_tests   결과: 통과
check_src   S39-1 · S39-2   ★ 없음

추적표 632행
  상태   ○ 40 · ◐ 528 · ✗ 0 · ! 44 · ? 19 · D-500b 1
  소스   확인 ✓ 114 · 추정 ~ 503 · 없음 15 · 미구현 0
  층     29건이 개정 397 이전 그대로 (WT-019 판정 · AD-007 수집 · …)
```

---

## 3. ★ 먼저 — 개정 397 표의 합이 안 맞습니다

지시에 「바뀐 것 21 · 유지 6 · `RULES.md` 2」라 적혀 있습니다.
표를 한 줄씩 세면 **전 ≠ 후 23줄 · 「(유지)」 4줄 · `RULES.md` 2줄 = 29** 입니다.

```
「(유지)」로 표시된 것   WB-001 · VW-010 · MS-023 · CO-036   — 넷
지시가 말한 유지        여섯
```

**표를 정본으로 삼아 줄 그대로 반영했습니다** (규칙 1). 21/6 이 맞다면
어느 둘이 더 「유지」인지 짚어 주십시오 — 제가 고르지 않았습니다 (규칙 2).

---

## 4. 무엇을 했나 · 왜 그렇게 했나

### ① 층 판단을 `config/trace_layers.json` 에 옮겨 적었습니다

```
layers    23건   가이드가 정한 새 층
unbuilt    4건   「유지 — 진짜 미구현」.  소스 칸을 「미구현」으로 되돌린다
to_rules   2건   RULES.md 로 옮긴다
```

★ **층은 가이드의 칸입니다** (S35). 개발측이 값을 짓지 않으므로
코드가 아니라 **config 에 옮겨 적고**, 검사(S35-1)가 「그 표에 있는 값으로만
바뀌었나」를 잽니다. 다른 값으로 바꾸면 그대로 걸립니다.

### ② `relayer()` — 층을 옮기고 「유지」는 미구현으로 되돌린다 (`trace_fill.py:530`)

```
필수   유지 4건은 다시 찾지 않는다 (redo 에서 뺀다)
      ★ 「층을 바꿔 억지로 찾히게 만드는 것」이 금지다.
        찾으면 「미구현인데 ○」가 된다
```

### ③ ★ 층을 옮겼으면 소스도 그 층에서 다시 찾는다

처음엔 층만 바꿨더니 이렇게 남았습니다.

```
SC-051  층 [화면]  ←  소스 ~ analyze/axis/site.py    ★ 층을 옮긴 뜻이 없다
CO-003  층 [수집·화면] ← 소스 ~ adapters/kcar.py
```

```
고침   dec.layers 에 있고 hints 에 없으면 다시 찾는다
후    SC-051 → report/screens/build.py::_axis_state
      CO-003 → web/views.py::admin_collect
```

### ④ ★ `trace_hints.json` 의 「확인했다」 아홉 건이 틀렸습니다

층이 바뀌자 `✓` 로 적힌 자리가 층 밖으로 나갔습니다.
**「사람이 확인했다」가 거짓이 된 것이므로 바로잡았습니다.**

| R | 새 층 | 전 (내 hints) | 후 |
|---|---|---|---|
| VF-009 | 검사 | `config/scoring.json::…min_value_kinds` | **`validate/base.py::Check`** ← 가이드가 짚어 준 자리 |
| WB-065 | 화면 | `store/core.py::option_diff` | **`web/templates/compare.html`** ← 〃 |
| AD-092 | 저장 | `config/web.json::max_form_bytes` | **`store/chunk.py::put`** ← 〃 |
| OP-136 | 운영 | `web/server.py` | **`✓` 를 뺐다** — 기계가 그 층에서 다시 찾는다 |
| VW-015 | 화면 | `analyze/peer.py::stage_conditions` | 〃 |
| VW-020 | 저장 | `report/screens/build.py::axis_chips` | 〃 |
| MS-027 | 화면 | `analyze/axes.py:100` | 〃 |
| AD-067 | 운영 | `web/templates/admin_collect.html` | 〃 |
| AD-085 | 운영 | `validate/v10_admin.py::_automation_checks` | 〃 |
| VW-010 · MS-023 | 화면 | (경로) | **뺐다 — 가이드가 「진짜 미구현」이라 판단** |

★ **결과 「층 밖 소스」가 0 건입니다.**

### ⑤ ★ `✓` 표시만 붙고 자리는 옛 값이 남는 결함을 잡았습니다

`restate` 가 표시(`✓`)만 붙이고 경로는 안 바꿨습니다. hints 를 고쳐도
표는 옛 자리를 그대로 들고 있었습니다 — **`✓` 가 거짓말이 됩니다.**
hints 가 있으면 **자리도 그것으로 바꿉니다.**

### ⑥ ★ 「질문 행」 13건 — 도구가 손대지 않게 했습니다

각 트레이스 파일 끝에 **이미 있는 R 번호를 다시 쓴 「여쭐 것」 행**이
13건 있습니다 (`AD-119`·`RP-015`·`ST-027` …). 전부 상태가 `?` 입니다.
hints 를 R 로 걸면 그 질문 행까지 채워졌습니다.

```
고침   상태가 유도값(○·◐·✗)이 아니면 상태만이 아니라 소스도 안 건드린다
★     AD-119 는 그래서 그대로입니다 — 지시대로 「되돌리지 않은 것이 맞다」
```

### ⑦ `RULES.md` 로 둘을 옮겼습니다

```
ST-026  미확정이 해소되면 본문도 고친다   → RULES.md  「추적표에서 옮겨 온 것 — 개정 397」
OP-024  이 문서만으로 구현할 수 있어야 한다 → 〃
★ 지우지 않았습니다.  R 번호를 「무엇」 앞에 남겨 어디서 왔는지가 끊기지 않습니다
```

### ⑧ `S39-1` · `S39-2` 를 만들었습니다 (`check_src.py:963`)

★ 규격(`00-standard.md:2064`)의 정의대로 만들었습니다.

```
S39-1  R 마다 층이 적혀 있는가
S39-2  층이 「화면」이 아닌데 「화면 없음」이라 적힌 것이 있는가 (「—」여야 한다)
★ 「!」·「?」 행의 빈 층은 미착수로 낸다 — 층은 가이드 칸이라 내가 못 채운다 (규칙 2)
```

★ **지시의 검산 문구(「층이 안 맞는 것이 0 인가」)와 규격의 문구
(「R 마다 층이 적혀 있는가」)가 다릅니다.** 규칙 1 대로 `docs/` 를 따랐습니다.

### ⑨ ★ 곁가지 — `S35-1` 이 이번 작업을 통째로 위반으로 잡았습니다

```
✗ OP-136 — 남의 칸을 고쳤다 (2번): 「`[저장]`」 → 「`[운영]`」   … 8건
```

```
고침   층 칸이 config/trace_layers.json 의 값과 같으면 「옮긴 것」으로 본다
★     다른 값으로 바꾸면 그대로 걸립니다.  검사를 무르게 하지 않았습니다
```

---

## 5. ★ 검사가 진짜로 잡는지 되살려 봤다

| 되살린 결함 | 결과 |
|---|---|
| 보통 행(`SC-051`)의 층을 지웠다 | **S39-1 실패** 「SC-051 — 층이 없다」 |
| 질문 행(`RG-010`)의 빈 층 | **미착수** 「가이드 몫 — 여쭐 행이다」 ★ 실패가 아니다 |
| 층을 가이드 표에 없는 값으로 바꿨다 | S35-1 이 그대로 문다 (표와 다르면 걸린다) |
| `trace_fill --write` 로 되돌린 뒤 | S39-1 · S35-1 통과 |

---

## 6. 조치 후 결과 — ★ 실제 출력

```
python3.11 tools/trace_fill.py --write

쓰기 — 미확인 12건 중 찾음 10 · 못 찾음 2
RULES.md 로 옮김 2건 — OP-024 · ST-026
상태 다시 매김 11건 · ★ 「!」 44 · 「?」 19 은 사람 몫이라 두었습니다

| 소스 칸 | 전 | 후 |
|---|--:|--:|
| 미구현 | 0 | 6 |
| 없음 | 15 | 15 |
| 추정 ~ | 503 | 503 |
| 확인 ✓ | 114 | 106 |
```

```
python3.11 tools/check_src.py

S38-4 상태가 세 칸에서 유도한 값과 같음     통과  통과 1 · 미착수 0 · 실패 0
S38-5 「!」·「?」 가 도구 실행 뒤에도 남음    통과  통과 64 · 미착수 0 · 실패 0
S39-1 R 마다 층이 적혀 있음             부분  통과 629 · 미착수 1 · 실패 0
S39-2 화면 층이 아닌데 「화면 없음」이 아님   통과  통과 630 · 미착수 0 · 실패 0
S35-1 자기 칸만 고침                  통과  통과 1 · 미착수 0 · 실패 0
S34-3 추적표 빈 칸을 센다               통과  통과 630 · 미착수 0 · 실패 0
결과: 실패 — S13(20) / S16(68) / S25(1) / S26(21) / S28(2)
```

```
python3.11 tools/check_all.py
통과 301 · fatal 3 · warn 7 · 환경 차이 1 · 미실행 5

python3.11 tools/run_tests.py
결과: 통과
```

| | 순서 7 후 | **순서 8 후** |
|---|--:|--:|
| 추적표 행 | 632 | **630** (2건 `RULES.md` 로) |
| ○ | 40 | **32** |
| ◐ | 528 | **528** |
| ✗ | 0 | **6** |
| 확인 `✓` | 114 | **106** |
| 추정 `~` | 503 | **503** |
| 미구현 | 0 | **6** |
| 층 밖 소스 | 6 | **0** |
| check_src S39 | 없음 | **S39-1 · S39-2** |

### ★ 「미구현 6건」이 개발 대상입니다

| R | 층 | 요구 |
|---|---|---|
| WB-001 | 화면 | 가변형 반응형 — 값을 버리지 않는다 |
| WB-015 | 화면 | v1 22열 · 축을 열로 |
| VW-010 | 화면 | 세 값을 한 셀에 |
| VW-020 | 저장 | 축 칩은 한 번에 (`IN`) |
| MS-023 | 화면 | 사이트별로 거를 수 있게 |
| CO-036 | 화면 | 다중 사이트 — 화면에 출처를 낸다 |

★ 가이드가 「유지」로 표시한 넷(`WB-001`·`VW-010`·`MS-023`·`CO-036`)에
**둘이 더 붙었습니다** — `WB-015`(판정·화면 → 화면) 과 `VW-020`(판정 → 저장)
은 새 층에서 소스를 못 찾았습니다. **공교롭게 21/6 의 6 과 수가 맞습니다.**
3절의 셈이 이것을 뜻하는지 확인해 주십시오.

---

## 7. 고친 파일

```
config/trace_layers.json   ★ 새로 만듦 — 개정 397 판단 29건
config/trace_hints.json    114 → 106.  ④ 표대로 셋을 고치고 여덟을 뺌
tools/trace_fill.py        :505 _layers()  :530 relayer()
                           :560 restate — hints 가 자리의 정본 · KEEP 행 불가침
                           :600 move_to_rules()
                           :640 fill_file — 층이 바뀌면 다시 찾는다
tools/check_src.py         :963 S39-1 · S39-2 신설
                           :1050 S35-1 — 가이드가 정한 층은 「옮긴 것」
docs/trace/*.md            도구가 다시 씀 (13파일 + RULES.md + INDEX.md)
docs/CHECKS.md 외          build_index.py 가 다시 씀
```

---

## 8. 못 한 것 · 막힌 것 · 여쭐 것

### 여쭐 것 ①  개정 397 표의 셈이 안 맞습니다 (3절)

「21 · 6 · 2」인데 표를 세면 「23 · 4 · 2」입니다.
표대로 반영했고, 결과적으로 **미구현이 6건**이 됐습니다.

### 여쭐 것 ②  질문 행 13건이 R 번호를 겹쳐 씁니다

`AD-119`·`AD-120`·`RP-015`·`RP-016`·`ST-027`·`ST-028`·`VW-026`~`VW-029`·
`WT-028`·`WT-029`·`OP-130`·`OP-131`·`RG-010` 이 **본문에 이미 있는 R 번호로**
파일 끝에 한 번 더 나옵니다. 도구가 R 로 짚으면 두 행이 다 걸립니다.
지금은 「상태가 `?` 면 안 건드린다」로 피했습니다.
**다른 번호를 주시거나 질문 표를 따로 빼 주시면** 더 안전합니다.

### 여쭐 것 ③  `RG-010` 은 층이 비어 있습니다

「분모가 같은 매물끼리만 비교」 — **개정 289 로 분모가 늘 555 라 충돌한다**는
질문 행입니다. 층 칸이 가이드 몫이라 못 채웠습니다 (S39-1 미착수 1건).

### 여쭐 것 ④  `S39` 검산 문구가 지시와 규격에서 다릅니다 (⑧)

### 못 한 것

없습니다. 순서 8 은 닫혔습니다. **다음은 순서 9** — 「기타 색상」입니다.

### 그대로 남은 것

```
check_all fatal 3   V1-11 · V4-11(가이드 몫) · V3-38(순서 9 에서 닫는다)
check_src           S13(20) · S16(68) · S26(21) · S28(2)
```

---

## 9. 커밋

```
S39-1·S39-2: 층 29건을 개정 397 대로 옮기고 trace_fill 재실행 (순서 8)
해시  537aad2
```
