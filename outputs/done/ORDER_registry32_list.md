# ★★★ 개정 390 — 등록부 32건 · **목록부터** 내십시오

**마스터 지시 — 「실제값으로 너랑 나랑 판단해야지」**
**★ `inbox/ORDER_registry32_targets.md` (08-18) 을 이 문서가 대신한다**

`SPEC-2026.08.19-r393`

---

# 1. 가이드 잘못이 먼저다

```
★ 앞 지시서는 값 분포 「형식」만 적고
  「32건이 무엇인지 목록을 먼저 내라」를 안 적었다.  예시 5개만 적었다
★ 그래서 산출물 outputs/20260818_registry32.md 가 나오지 않았고
  개발측 보고 2건이 「V4-11 미분류 32건 — 마스터 몫」으로 되돌렸다
  outputs/20260818_2230_v161 · 20260819_0200_v165
★ 지금 저장소 어디에도 32건의 경로가 없다.  DB 와 화면에만 있다
  ★ 목록이 없으니 가이드도 마스터도 아무것도 못 정한다
```

---

# 2. 1단계 — 목록 32줄 (오늘)

```
python3.11 tools/classify_unclassified.py --blocking --list
→ outputs/20260819_registry32_목록.md
```

**한 줄에 이것만.  값 분포는 2단계다.**

```
| 엔드포인트 | 경로 | 관측 | 파서가 읽는 곳 |
|---|---|--:|---|
| record_summary | record_summary:accidentCnt | 200/200 | score/axes/accident.py:41 |
```

```
필수   32건 전부.  건너뛰지 않는다
필수   ★ 「파서가 읽는 곳」을 파일·줄로 적는다
       ★ 이 32건은 「파서가 실제로 그 경로를 읽는 것」이라 막는 것이다 (v156)
         정말 읽는지 줄로 보여야 한다
필수   많이 관측된 순
필수   알려진 구성과 맞는지 확인한다
       record_summary 17 · inspection_summary 8 · inspection 2
       facet 2 · list · ev_battery · platform_check 각 1
금지   값 분포를 기다리느라 목록을 늦추는 것
검산   V4-30  목록 파일이 있고 줄 수가 32 인가
```

---

# 3. 2단계 — 값 분포 (목록 낸 다음)

```
python3.11 tools/classify_unclassified.py --blocking --values
→ outputs/20260819_registry32_값.md
```

```
detail:advertisement.verification
  관측 200/200
  값    true 187 · false 13
  예시  {"verification": true, "reportExists": true, "price": 29900000}
  위치  detail > advertisement · 옆에 price · status
```

```
필수   ★ 값 분포가 핵심이다.  true/false 인지 · 숫자인지 · 문자인지
필수   예시 원문은 그 필드 앞뒤 두 줄
필수   전건 null 이면 「전건 null」이라 적는다 — 빈칸으로 두지 않는다
```

---

# 4. 그다음은 가이드가 한다

```
1  가이드가 32건을 본다.  값을 보고 「쓴다 / 안 쓴다」를 정한다
2  정말 마스터 판단이 필요한 것만 남긴다 — 다섯 개 안팎
3  마스터께 뜻으로 여쭙는다
   「엔카 검증 표시 — 200건 중 187건이 true.  보증 점수에 넣을까요」
★  마스터는 「차 사는 데 쓸모있나」만 판단하신다.  필드 이름을 보실 일이 아니다
```

---

# 5. 화면은 개정 367 대로

```
한 항목에 다섯   ① 값 분포 ② 판정에 쓰이는가 ③ 원문에서 어디에 있었나
                ④ 고를 것을 단추로 ⑤ 안 정하면 무엇이 막히나
금지            경로만 내고 「사람이 봐야 합니다」라 하는 것
검산            V4-28 · V4-29
```
