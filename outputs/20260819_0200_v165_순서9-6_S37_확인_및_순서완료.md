# 순서 9-6 — S37 파는 쪽 개념 확인 · **순서 8~9 완료**

2026-08-19 02:00 · v165 · **SPEC-2026.08.18-r365**

`inbox/ORDER_00_순서.md` **9. 남은 것 — S37 판매·대행 개념이 코드에 남아 있는지**

---

## 1. 무엇을 하려 했나 (지시 원문)

```
S37 — 판매·대행 개념이 코드에 남아 있는지
```

---

## 2. 결과 — 통과입니다

```
python3.11 tools/check_src.py

S37-1 파는 쪽 개념이 안 남아 있음   통과   통과 1 · 미착수 0 · 실패 0
```

찾는 말

```
「계약서」 「수수료」 「정산」 「판매 대행」 「매물 등록」 …
```

---

## 3. ★ 다만 — 이 세션에서 두 번 되살아났습니다

**검사가 「지금 없다」를 확인하는 것만이 아니라 실제로 막았습니다.**

| 언제 | 무엇 | 어디 |
|---|---|---|
| 9-3 (구매비용) | 「이전등록 **대행수수료**」 | `web/templates/why.html` |
| 9-5 (진행 메모) | 「**계약서** 작성」 · 「**판매 대행** 용도」 | `validate/v7_watch.py` |

★ 둘 다 **새로 쓴 글**입니다.  개정 362 로 걷어낸 말을
**같은 날 두 번 다시 썼습니다.**

```
9-5 의 것은 마스터 말을 그대로 인용한 것이었습니다 —
「내가 엔카랑 직거래를 하기 위한 용도지 판매 대행 용도가 아닌데 뭘까?」
인용이라도 코드에 남으면 다음 사람이 그 말을 개념으로 읽습니다.
뜻을 살려 다시 썼습니다
```

**S37-1 은 「한 번 지우면 끝」이 아니라 계속 도는 검사여야 합니다.**  그렇게 돕니다.

---

## 4. ★ 순서 8~9 를 끝냈습니다

| # | 무엇 | 결과 | 커밋 |
|---|---|---|---|
| 8 | K카 | 어댑터·경로 확보 · **목록 막힘** | `eb9045b` |
| 9-1 | `test_endtoend` 실패 | 고침 (지렛대가 안 듣는 자리) | `413f751` |
| 9-2 | fatal 6 이 무엇인지 | **6 → 2** · 넷이 검사 오류였다 | `2146a3e` |
| 9-3 | 사이트별 구매비용 (353) | `V11-120`·`V11-121` 신설 | `7414c63` |
| 9-4 | 리포트 팝업 (357) | `V11-122` 신설 · `/reports` | `fea76b3` |
| 9-5 | 4단계 → 메모 셋 (362) | `V7-15` 신설 · `watch_note` | `d9ae89b` |
| 9-6 | S37 확인 | 통과 · 두 번 막았다 | 이 기록 |

### 숫자

```
                   시작(08-18 21:00)   지금
check_all 통과            274           280
fatal                       6             3
warn                        7             7
검사 차수                    9             9
새 검사                      —   V11-120 · V11-121 · V11-122 · V7-15
run_tests 실패 파일          1             2
```

★ **fatal 이 6 → 2 로 내려갔다가 3 으로 올라왔습니다.**  이유를 적습니다.

```
내려간 것   V1-16 · V2-01 · V2-18 · V3-38 · V11-77   ← 다섯을 고쳤다
올라온 것   V11-80   ★ 통과하던 것이 거짓이었다 (죽은 CSS 를 읽고 있었다)
          V11-12   ★ /reports 두 줄이 라우팅 표에 없다
```

**둘 다 「새 결함」이 아니라 「드러난 것」입니다.**

---

## 5. ★★ 남은 셋 — 전부 사람이 정해야 합니다

### ① `V4-11` 미분류 32건 — 마스터 몫 (STEP 87)

```
python3.11 tools/classify_unclassified.py --suggest
  → config/field_usage.suggested.json
★ 사람이 확인한 뒤 config/field_usage.json 으로 옮깁니다.
  자동으로 정본이 되지 않습니다
```

### ② `V11-80` — 규격끼리 어긋납니다 (규칙 2)

```
R-206 (trace/14-web.md:17)     「사진 최소 128px — 좁아도 안 줄인다」 [마스터] 281
a-common.md:17 정본             목록 96×72 · 88×66 · 80×60
개정 342                        STEP 149o-3 의 크기 규정은 「폐기」
```

셋 중 하나를 정해 주십시오 — **9-2 기록 11절**에 적었습니다.

### ③ `V11-12` — 라우팅 표에 `/reports` 가 없습니다 (규칙 2)

```
| `/reports` | GET | `view_reports` | **user** | — | 08-18 신설 |
| `/reports/{name}` | GET | `view_report_download` | **user** | — | 08-18 신설 |
```

**개발측이 `docs/` 를 고치지 않습니다.**  두 줄을 넣어 주십시오.
`test_web` 의 「Route 수 == 지시서 표 행 수」도 같은 것을 봅니다.

---

## 6. ★ 막힘 — K카 목록 (순서 8)

```
mapi.kcar.com 을 찾았고 상세는 74KB JSON 을 받습니다
목록 경로만 못 찾았습니다 — 여섯 경로 전부 404
번들 호출이 params(searchCondition) 이라 키 이름을 모르면 못 부릅니다
```

```
① 마스터가 K카 목록 화면에서 브라우저 수집을 눌러 주십니다  ← 가장 확실합니다
② 번들을 더 파서 키 이름을 찾습니다 (2.7MB 짜리가 있습니다)
③ 매물 ID 를 다른 곳에서 얻습니다
```

**정해 주시면 그대로 하겠습니다.**

---

## 7. ★ 따로 보고 — 자동화 큐가 42건 실패로 멈춰 있습니다

```
recalc_job   failed 42 · 그 밖은 0
마지막        2026-08-18T04:55  「S5: 선행 조건 미충족: 선행 단계 미완료: S4」
```

```
원인   S4 가 옛 봉투를 순서 없이 다시 펼치다 STEP 29 에 걸려 멈춘 것입니다
고쳤습니다   08-18 · fetched_at 순서 + 낡은 봉투 건너뛰기
그런데      고친 뒤로 새 작업이 큐에 안 들어왔습니다.
           04:55 이후 수집이 없어서입니다
```

★ **failed 42 는 옛 기록입니다.  지금 고장이 아닙니다.**
다음 수집이 들어오면 실제로 도는지 봐야 합니다 — 그때 확인하겠습니다.

---

## 8. 서비스 상태 — 고친 뒤 재시작했습니다

```
sudo journalctl -u carwatch --since "-20min" | grep POST   → 0건
recalc_job 도는 것                                        → 없음
★ 마스터 작업을 끊지 않는 것을 확인하고 한 번만 재시작했습니다

systemctl is-active carwatch caddy    active · active
ss -tln | grep 8765                   127.0.0.1:8765

curl https://43.201.16.78.sslip.io
  /listings   200
  /why/1427   200
  /reports    403   ← 로그인이 필요합니다.  맞습니다
  /watch      403   ← 맞습니다
```

★ `HEAD` 는 501 입니다 — 이 서버가 원래 GET 만 받습니다.  이번 변경과 무관합니다.

---

## 9. 이번에 배운 것

```
① 검사가 「없는 결함을 있다」고 하면 놓친 것보다 나쁘다
   V1-16 · V2-01 · V2-18 · V3-38 — 넷 다 그랬다.
   여덟 달치 빨간불이 전부 검사의 잘못이었다

② 통과하는 검사가 더 위험할 수 있다
   V11-80 은 아무 데도 안 붙은 CSS 를 읽고 「128px 이다」라고 했다.
   화면은 96px 이었다.  V11-77 을 고치다 우연히 드러났다

③ 되살림 시험은 검사를 만든 다음이 아니라 검사의 일부다
   V7-15 는 되살린 결함 앞에서 통째로 죽었다.
   죽는 검사는 없는 검사와 같다

④ 시험에 수를 박으면 시험이 규격을 막는다
   이 세션에서만 네 번 (V10-20 · test_score · test_store · test_spec_ui)
```
