# 가이드 점검 VERIFY_0817_0900 — G-1b · G-4 · G-3 · G-2b

2026-08-18 02:30 · v142

---

## 1. 무엇을 하려 했나 (지시 원문)

`inbox/VERIFY_0817_0900.md` §7 순서

```
1  ★★ G-1b 추천 사진 — 목록은 되는데 추천만 안 됩니다
2  ★ G-4 「—」 41곳 · V11-106 검사
3  G-3 계산식 10곳 — 목록에서 빼고 상세로
4  G-2b 카드 6줄로 묶기
5  부록 F 24축 · 손계산 24축 전부
```

**★ 1·2 를 먼저 하십시오.  「—」는 검사가 없으면 계속 늘어납니다.**

이 기록은 **1 · 2 · 3 · 4** 다.  5(24축 손계산)는 못 했다 — 6절에 적는다.

---

## 2. 1차 결과 (고치기 전 숫자)

```
check_all   통과 250 · fatal 6 · warn 7
run_tests   실패 1 — test_endtoend.py
「—」        값 칸(셀 전체가 줄표) 77곳
목록 카드    13줄 (가이드 실측) · 값이 서로 겹쳐 읽을 수 없었다
추천 카드    사진 빈 회색 상자 · 축 넷 없음 · 옵션 없음
```

---

## 3. 무엇을 했나 · 왜 (규격 STEP)

### G-1b  추천 사진이 안 나옴 — **원인이 둘이었다**

가이드가 준 세 갈래(①템플릿 ②CSS ③build)는 전부 아니었다.

| # | 원인 | 근거 |
|---|---|---|
| ① | `.cand-rows` 가 목록 카드 격자를 물려받아 사진 칸이 눌렸다 | `app.css:1726` |
| ② | **`loading="lazy"`** — 스크린샷은 한 장으로 찍는데 화면 밖 사진은 안 받아진다 | `tools/render_screens.py:193` |

②가 진짜였다.  추천은 머리말(깔때기 상자)이 길어 카드가 목록보다
**아래에** 있다.  그래서 **같은 CSS 인데 목록만 나왔다.**

```
고침   app.css       .cand-rows 전용 격자 (8줄)
      render_screens.py  스크린샷 사본에서만 loading="lazy" 를 뗀다
      ★ 실서비스는 그대로 lazy 다 — 스크롤하면 나온다
```

추천 카드에 **축 넷과 옵션 줄이 아예 없었다** (부록 G 2절 6줄·7줄).

```
고침   web/templates/recommend.html:41,76   축 넷 + 옵션 칸 추가
      web/views.py:517,533                 axis_heads 를 추천에도 넘긴다
      recommend.html:64                    트림 칸에 class="trim" (없어서 CSS 가 안 걸렸다)
```

### G-4  「—」 77곳 → 0곳 · **V11-106 신설**

가이드는 41곳이라 했다.  전 화면을 세니 **77곳**이었다
(셀 전체가 줄표인 것만.  문장 속 줄표는 글이지 값이 아니다).

뿌리는 **`web/template.py` 의 표시 필터 6개**였다.

```
고침   web/template.py:41   NO_VALUE = "없습니다" 상수
      f_won · f_km · f_pct · f_date · f_num · f_signwon  전부
      ★ 0 은 여기 안 온다.  0 은 「0」으로 찍힌다 (None·"" 만 걸린다)
      web/views.py:947     「—」 → 「설정을 읽지 못했습니다」
```

템플릿 리터럴 9곳

| 파일 | 전 | 후 |
|---|---|---|
| dealers.html:35 | — | 없습니다 |
| dealers.html:37,42,43 | — | 확인 못 함 |
| dealers.html:38 | — | 표본 부족 |
| admin_targets.html:71 | — | 없습니다 |
| why.html:93 | — | 해당 없음 |
| compare.html:23 | — | 없습니다 |
| admin_collect.html:73 | — | 아직 부르지 않았습니다 |

```
신설   V11-106  값 자리에 「—」가 없음 (fatal)
      validate/v11_web.py:1510
      ★ 렌더 결과를 본다 — 템플릿만 보면 필터가 만드는 줄표를 못 본다
      ★ 셀 전체가 줄표인 것만 잡는다 — 문장 속 줄표는 글이다
```

### G-3  계산식

세어 보니 **목록에는 이미 없었다** (앞 작업에서 `title=` 로 옮겼다).
남은 것은 「1/353쪽」 하나였는데 앞의 「3,528건 중 10건」과 어투가 어긋났다.

```
고침   listings.html:3 · dealers.html:1
      「1/353쪽」 → 「353쪽 중 1쪽」
```

「489/555」는 상세(`/why`)에 17곳 있다 — 부록 G 대로다.

### G-2b  카드 6줄

부록 G `## 좁음 (<640)` 도면을 그대로 따랐다.  **도면에 이름표가 없다.**

```
1  [사진]  A  G80_25T                    ♡
2   80×60  2024-11   7,591km
3  가솔린 2.5 터보 AWD · (세부등급 없음)
4  4,550만   시세 −14.0%   신차 −26.3%
5  무사고  골격 이상 없음  렌트 아님  3년 3개월 · 92천km
6  없음        근거   엔카에서 보기 ↗
```

★ 도면의 2줄·3줄이 서로 바뀌었다.  실제 트림이 도면 예시
(「가솔린 2.0T 4WD · 시그니처」)보다 길어 사진 오른쪽 8칸(195px)에
넣으면 두 줄로 넘쳤다.  **줄여 감추지 않고**(개정 278) 셋째 줄을
통째로 줬다.  값은 하나도 안 빠졌다.

---

## 4. ★ 배치를 여섯 번 고쳐도 안 낫던 진짜 원인 — 우선순위

「6줄로 묶었는데 글자가 서로 겹친다」가 계속 반복됐다.
**배치 문제가 아니었다.**  브라우저에 직접 물어보고서야 나왔다.

| # | 진짜 원인 | 증상 |
|---|---|---|
| ① | `.rows td.ax[data-label="용도"] { span 6 }` 이 더 좁아 이겼다 | 5줄이 15칸이 되어 **격자가 통째로 어긋났다** — 칸 폭이 10.1px 아홉 · 30.3px 셋으로 갈렸다 |
| ② | `body.s-listings td { white-space:nowrap }` (0,1,2) 가 `.rows td` (0,1,1) 보다 좁아 이겼다 | 카드가 화면 밖으로 밀려 ♡ 와 보증이 잘렸다 |
| ③ | `.rows td[data-label="제조사 보증"] { min-width:9em }` 이 전역이었다 | 격자 칸 69px 을 94px 로 벌렸다 |

```
셋 다 「선택자 특정도(specificity)」다.  눈으로 CSS 를 읽어서는 안 보인다.
★ 스크린샷을 보고 배치를 고치는 짓을 여섯 번 했다.
  브라우저에 getBoundingClientRect 를 물어본 뒤 한 번에 끝났다.
```

측정 방법 (재현용) — `outputs/render/*.html` 옆에 CSS·사진을 두고
계측 스크립트를 붙여 firefox 로 찍는다.  칸마다 x·폭·grid-area 가 나온다.

---

## 5. 조치 후 결과 — ★ 실제 출력

```
python3.11 tools/check_all.py

통과 251 · fatal 6 · warn 7 · 환경 차이 1 · 미실행 1
  V1-16     이번 run_id 밖의 행을 보지 않음 — 2
  V2-01     ok 원문 수 == CORE 행 수 — 3470
  V2-18     parse_rule 재처리 후 전 봉투가 현재 parse_version — 30
  V4-11     unclassified 존재 — 100
  V3-38     facet 수신 후 목록 관측분과 대조함 — 남은 목록 관측 41
  V11-77    시안의 시각 요소가 렌더 결과에 나옴 — 6/7 · 자료 없어 못 본 것 1
```

```
python3.11 tools/run_tests.py

결과: 실패 — test_endtoend.py
```

| | 전 | 후 |
|---|--:|--:|
| check_all 통과 | 250 | **251** |
| fatal | 6 | **6** |
| 시험 실패 | 1 | **1** |
| 값 칸 「—」 | 77 | **0** |
| 목록 카드 줄 수 | 13 | **6** |
| 추천 카드 줄 수 | — | **8** |
| 검사 수 | 277 | **278** |

`test_endtoend` 는 그대로다 — 위 fatal 6 을 그대로 보고할 뿐이다.

시험 둘을 부록 G 에 맞춰 고쳤다 (개발측 시험이다.  docs 가 아니다).

```
tests/test_web.py:224      「None 은 —」 → 「None 은 없습니다」 + 「0 은 0」
tests/test_spec_ui.py:48   A-1  목록은 비율 % · 원점수/분모는 상세 (부록 G)
                           ★ 전에는 목록 본문에서 「\d+/\d+」를 찾았는데
                             그게 「1/353쪽」쪽수에 걸려 우연히 통과하고 있었다
```

---

## 6. ★ 판단이 필요한 것 — 규칙 2 (docs 는 안 고쳤습니다)

### ① 부록 G 「좁음 도면」과 V11-70 이 부딪힙니다

부록 G `## 좁음 (<640)` 도면에 **없는 칸**이 일곱입니다.

```
순위 · 비율 · 월 · 상태 · 딜러 · 경과 · 사이트
```

V11-70 은 「좁은 폭에서 값이 사라지지 않음」(STEP 149o-2 · 개정 278)이라
`display:none` 을 전부 fatal 로 잡습니다.  **도면을 따르면 V11-70 이 깨집니다.**

```
한 것   부록 G 를 따랐습니다 (규칙 1 · 개정 332 가 더 뒤).
       V11-70 을 「config 에 못박은 칸만 접을 수 있다」로 좁혔습니다.
       config/web.json  narrow_folded_labels · narrow_folded_parts
       ★ 몰래 늘어나면 그대로 fatal 입니다
물어봄  이 일곱을 좁은 폭에서 빼는 것이 맞습니까.
       특히 「비율」은 정렬 기준인데 도면에 없습니다.
```

### ② 부록 G 도면의 2줄·3줄을 바꿨습니다

실제 트림이 도면 예시보다 길어 그대로는 7줄이 됩니다 (4절).
값은 하나도 안 뺐고 순서만 바꿨습니다.  괜찮습니까.

### ③ 축 이름을 CSS 에 적었습니다

「제조사 보증」은 값이 길어(3년 3개월 · 92천km) 칸을 다섯 줘야 합니다.
축 이름은 `config/labels.json` 이 정하는데 CSS 에 적어야 폭을 줄 수 있습니다.
이름이 바뀌면 균등(span 3)으로 돌아갑니다 — 깨지지는 않지만 두 줄이 됩니다.

### ④ 못 한 것 — 부록 F 24축 손계산

`VERIFY_0817_0900.md` §7 의 5번입니다.  지금까지 6축만 대조했습니다.
남은 18축(외판 20 · 특수사고 15 · 누유 10 · 소모품 10 · 진정성 5 ·
소유자변경 15 · 압류저당 10 등)은 다음 작업으로 남깁니다.

### ⑤ 그대로 남은 fatal 6

```
V1-16   run_id 밖의 행 2
V2-01   ok 원문 3470 vs CORE 행 수
V2-18   parse_rule 재처리 후 전 봉투 30
V4-11   미분류 100  ← ★ 마스터가 /admin/registry 에서 분류해야 합니다
V3-38   facet 수신 후 남은 목록 관측 41
V11-77  시안 시각 요소 6/7 (자료 없어 못 본 것 1)
```

---

## 7. 고친 파일

```
web/static/app.css              카드 격자 재작성 (중간 2줄 · 좁음 6줄/8줄)
web/template.py                 NO_VALUE · 필터 6개
web/views.py                    axis_heads → 추천 · 「—」 1곳
web/templates/recommend.html    축 넷 · 옵션 · class="trim"
web/templates/listings.html     쪽 표기
web/templates/dealers.html      「—」 5곳 · 쪽 표기
web/templates/admin_targets.html · why.html · compare.html · admin_collect.html
validate/v11_web.py             V11-106 신설 · V11-70 좁힘
config/web.json                 narrow_folded_labels · narrow_folded_parts
tools/render_screens.py         lazy 제거 · 사진 실패 보고 · SHOT_IMAGE_MAX 40→120
tests/test_web.py · tests/test_spec_ui.py
```

커밋 해시 — 아래 절

---

## 8. 이어서 — 부록 G 가 이름 지은 검사 셋 신설 (V11-108 · 109 · 110)

`docs/CHECKS.md` 가 「규격이 요구했는데 코드에 없다」로 셋을 짚고 있었다.
줄 수를 지키는 검사다 — **없으면 또 늘어난다** (가이드 08-17 지적과 같은 이유).

```
V11-108  좁은 폭에서 한 화면에 매물 2개 이상인가   (G-screens.md:51)
V11-109  카드가 부록 G 줄 수 상한을 안 넘는가      (G-screens.md:171)
V11-110  상세 절 순서가 부록 G 와 같은가          (G-screens.md:238)
```

### V11-109 — ★ CSS 를 세지 않는다.  칸을 실제로 놓아 본다

처음엔 CSS 의 `grid-area` 만 셌다.  **오늘의 버그를 못 잡았다.**
축 넷이 반복문 하나로 적혀 있어 CSS 만 세면 **한 칸**으로 세어진다.

```
고침   템플릿에서 칸을 편다 (축은 axis_heads 로 실제 수만큼)
      선택자를 칸마다 맞춰 보고 가장 좁은 규칙을 고른다
      격자에 하나씩 놓아 보고 겹침·넘침을 센다
```

**검사가 진짜로 잡는지 버그를 되살려 확인했다**

| 되살린 버그 | V11-109 |
|---|---|
| 축 「제조사 보증」을 span 9 로 | 실패 — `rows 5줄 — 칸 9 개가 안 들어간다` |
| 트림을 7줄로 | 실패 — `rows 7줄 — 부록 G 상한 6줄` |
| 차종을 사진 위로 (겹침) | 실패 — `rows 1줄 3칸 겹침` |
| 되돌린 뒤 | 통과 |

★ 도중에 검사 자체의 결함도 나왔다 — **주석 안에 `{ }` 를 쓰면**
규칙 쪼개기가 깨져 축 규칙 넷이 통째로 사라졌다.  주석을 먼저 뗀다.

### V11-110 — 상세 절 순서.  ★ 순서를 코드에 박지 않는다

`docs/ref/G-screens.md` 3장의 ①~⑩ 을 읽어 온다.  **두 곳이 어긋나 있었다.**

| 부록 G | 고치기 전 | 고친 뒤 |
|---|---|---|
| ⑥ 마이너스 | 값 절 안에 「뺀 것」으로 섞여 있었다 | `<h2>마이너스</h2>` 로 세워 축별 판정 뒤로 |
| ⑧ 옵션 → ⑨ 비용 | 비용 → 주요 옵션 (뒤바뀜) | 주요 옵션 → 비용 |

지금 순서 (렌더 실측)

```
감가 곡선 → 왜 싼가 → 무엇을 조회했는가 → 축별 판정 → 마이너스
→ 엔카 진단 → 확인 못 한 것 → 왜 이 등급인가 → 주요 옵션 → 비용 → 참고 자료
```

★ 부록 G 에 없는 절(감가 곡선 · 엔카 진단 · 왜 이 등급인가)은 그대로 뒀다.
검사는 **부록 G 절끼리의 앞뒤**만 본다.

### 시험 C-10 도 같은 곳을 읽게 했다

`tests/test_spec_ui.py` 의 C-10 이 **개정 256 순서를 박아 두고** 있었다.
부록 G(개정 332)가 더 뒤다 — 시험이 옛 순서를 지키고 있었던 것이다.

```
고침   tests/test_spec_ui.py:214
      순서를 박지 않고 validate.v11_web._why_order_spec() 을 쓴다
      ★ V11-110 과 정본이 하나다 — 어긋날 수가 없다
```

### 8절 조치 후 숫자

```
python3.11 tools/check_all.py
통과 254 · fatal 6 · warn 7 · 환경 차이 1 · 미실행 1

python3.11 tools/run_tests.py
결과: 실패 — test_endtoend.py
```

| | 5절 시점 | 8절 뒤 |
|---|--:|--:|
| check_all 통과 | 251 | **254** |
| fatal | 6 | **6** |
| 시험 실패 | 1 | **1** |
| 검사 수 | 278 | **281** |
| 규격이 요구했는데 코드에 없는 검사 | 36 | **33** |

`test_endtoend` 는 그대로다 — fatal 6 을 그대로 보고할 뿐이다.

### 8절에서 새로 물어볼 것

부록 G ② 값 절에서 **「뺀 것」 칩을 빼 ⑥ 마이너스 절로 옮겼습니다.**
값 절 맨 위에서 「−80점」을 바로 보던 것을 아래로 내린 셈입니다.
부록 G ⑥ 이 별도 절이라 그렇게 했는데, 값 옆에도 남겨 둘까요.
