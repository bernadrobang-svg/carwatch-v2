# v287 — 원문을 눌렀다.  DB 1.26GB → 528MB

```
지시   마스터 08-28 「do it」 — v285 6항 · v286 7항의 「raw_response 774MB 압축」
장비   t4g.small · vCPU 2 · 램 1,841MB
```

## 1. 무엇을 하려 했나

`raw_response` 가 DB 의 77% 였다.  DB 가 램에 안 들어가서
**차가울 때 10초 · 더울 때 0.7초**로 14배 갈렸다 (v285 2-①).
원문을 눌러 **DB 를 램에 들어가게** 한다.

## 2. 규격을 어기지 않는가 — 먼저 확인했다

```
chapters/01-arch.md:17     L2 RawStore  원문 무손실 저장.  삭제 금지
chapters/13-pipeline.md:401 raw_*  추가만.  덮어쓰지 않는다
```

```
★ 압축은 ★ 무손실이다 — ★ 글자 하나 안 바뀐다.  ★ 지우지도 않는다
★ 그래도 ★ 말로 끝내지 않았다 — ★ 아래 6항에서 ★ 186,547행 전건을
  ★ 백업과 ★ 글자 단위로 대조했다
```

## 3. 어떻게 했나

### 저장 꼴 — `body` 칸에 압축 바이트를 그대로 넣는다

```
★ 칸을 새로 만들지 않았다.  ★ 그래야 `body IS NOT NULL` 을 쓰는 여섯 자리가
  그대로 산다 (store/core.py 2 · validate/v0_guide.py 2 · tools/fill_photos.py ·
  sql/ddl/01_raw.sql 의 부분 색인)
★ 가른다 — 글자(str)면 옛 꼴 · 바이트(BLOB)면 눌린 것.  앞에 표식 `CWZ1` 을 둔다
★ 섞여 있어도 된다 — 옮기는 중에도 읽는 쪽이 안 깨진다
```

### 코드 — 새 함수 둘 · 옮긴 자리 스물넷

```
store/raw.py    pack_body(text)   저장할 꼴로 만든다
                raw_body(value)   원문 글자로 되돌린다
config/web.json raw_body_compress · _min_bytes(1024) · _level(6) · _why
```

| 쓰는 자리 (5) | `store/raw.py` save_raw · save_site_raw · save_import_raw · save_browser_raw · `tools/repair_facet_chunks.py` |
|---|---|
| **읽는 자리 (19)** | `store/core.py` 4 · `collect/runner.py` 2 · `report/screens/admin.py` 2 · `validate/` 8 · `tools/` 8 · `tests/test_run.py` 1 |

```
★ 전수로 찾았다 — 기계로 훑어 「raw_response 를 치면서 body 를 다루는 자리」
  44개를 뽑고, 남은 11개가 DDL 색인·주석·시험 INSERT·소스 대조임을 하나씩 확인했다
★★ 처음 grep 은 한 줄만 봐서 ★ 넷을 놓쳤다 —
   report/screens/admin.py 2 (화면!) · collect/runner.py(파싱!) · validate/v4_mapping.py.
   ★ 여러 줄로 다시 훑어 잡았다
```

### ★ 안 누른 것 — 까닭이 있다

```
① 1KB 미만 (86,156건)  — 눌러 봤자 머리글이 커진다
② origin='browser' 인데 response_meta 가 JSON 이 아닌 55건
   ★ V11-47 이 「한 번에 보낸 바이트」를 재는데 그 값을 meta 에 못 넣는 행이라
     LENGTH(body) 로 재야 한다.  누르면 그 길이가 거짓이 된다
   ★ 왜 JSON 이 아닌가 — tools/repair_facet_chunks.py 가 JSON 뒤에
     「| 조각이다 …」를 이어붙인다.  ★ 이건 따로 볼 것으로 6항에 적는다
```

### `V11-47` — 검사를 함께 고쳤다

```
전   SELECT MAX(LENGTH(body)) …
     ★ 누르면 「압축된 크기」를 재게 되어 ★ 상한을 넘겼는데도 통과한다
후   response_meta.bytes 를 쓴다 (save_browser_raw 가 넣는다).
     json_valid 로 감싸고, 물러설 때도 typeof(body)='text' 인 것만 잰다 —
     ★ 눌린 길이는 절대 안 쓴다
```

## 4. 이관 — `tools/compress_raw.py` (새 파일)

```
★ 한 줄마다 ★ 되돌려 대조한 뒤에만 쓴다.  다르면 안 건드리고 세어서 보고한다
★ config 의 db_batch_commit_rows(100) 마다 커밋 — 잠금 창을 자른다
★ 중간에 끊겨도 다시 돌리면 이어서 한다 (아직 글자인 줄만 고른다)
★ --dry-run 이 있다.  먼저 그것으로 돌렸다
```

```
건조 실행  본 것 143,915 · 눌림 57,759 · 그대로 86,156 · ★ 안 맞음 0 · 118초
실제       본 것 143,915 · 눌림 57,759 · 그대로 86,156 · ★ 안 맞음 0 · 135초
           브라우저 meta.bytes 채움 4,509
           본문 675MB → 167MB  (4.0배)
★ 이관하는 135초 동안 ★ 사이트는 계속 돌았다 (읽기는 WAL · 쓰기는 100줄마다 끊었다)
```

## 5. 되찾기

```
이관 뒤 빈 쪽   120,944쪽 = 472MB
VACUUM INTO     10초  ★ 사이트는 계속 돌았다
                1.01 GB → 0.52 GB
검증            integrity ok · foreign_key ok · 표 46/46 · 인덱스 49/49 · 전 표 행 수 일치
바꿔치기        서비스를 멈추고 다시 대조 → 유실 없음 확인 후 교체.  ★ 멈춘 시간 몇 초
```

## 6. ★★★ 무손실 증명 — 전건 대조

```
백업            carwatch.db.before-compress (압축 전 · VACUUM INTO 로 뜬 1.01GB)
대조            186,547행 전건 · raw_body(지금 body) == 백업 body
결과            ★ 내용이 다른 행 0건 · id 가 어긋난 행 0건 · 행 수 186,547 = 186,547
걸린 시간       11초
```

## 7. 1차 결과 (고치기 전)

```
check_all  통과 398 · fatal 9 · warn 12 · 환경 차이 1 · 미실행 8
  V1-21 16 · V2-01 10604 · V4-06 3 · V4-06b 48 · V4-11b 319 · V1-07 8190 · V3-96 59204
run_tests  실패 1파일 test_endtoend · ruff 통과
DB         1.01 GB (v285·v286 뒤) · 처음은 1.26 GB
```

## 8. 조치 후 결과

```
check_all  통과 398 · fatal 9 · warn 12 · 환경 차이 1 · 미실행 8
  V1-21 16 · V2-01 10604 · V4-06 3 · V4-06b 48 · V4-11b 319 · V1-07 8190 · V3-96 59204
  ★★ 원문을 읽는 검사의 숫자가 ★ 한 자리도 안 달라졌다 —
     ★ 이것이 「읽는 자리를 하나도 안 놓쳤다」의 증거다
run_tests  실패 1파일 test_endtoend · ruff 통과   ★ 그대로
DB         ★ 528 MB
```

캐시를 비우고 화면마다 새 연결로 (관리자)

| 화면 | 처음 | v286 뒤 | ★ 지금 |
|---|--:|--:|--:|
| `/` | 11.23초 | 0.61초 | **0.76초** |
| `/admin/registry` | 18.91초 | 0.61초 | **0.13초** |
| `/admin/import` | — | — | 0.06초 |
| `/admin/collect` | — | — | 0.04초 |

배포 실측 (`{base_url}` · HTTPS)

| `/` | `/listings` | `/detail/1839` | `/track` | `/market` | `/notready` | `/compare` |
|--:|--:|--:|--:|--:|--:|--:|
| 0.68초 | 0.57초 | 1.13초 | 0.20초 | 0.11초 | 0.14초 | 0.05초 |

## 9. 전체 — 오늘 한 것을 합치면

| | 아침 | 지금 |
|---|--:|--:|
| DB | 1.26 GB | ★ **528 MB** (42%) |
| `/detail` | 8.64초 | 1.13초 |
| `/` (차가운 관리자) | 11.23초 | 0.76초 |
| `/admin/registry` | 18.91초 | 0.13초 |
| V11-76 | 4곳 실패 | ★ 0곳 통과 |

★ DB 528MB 는 램 1,841MB 에 넉넉히 들어간다 — ★ 「차가울 때 10초」의 뿌리가 사라졌다.

## 10. 못 한 것 · 판단이 필요한 것

```
★ tools/repair_facet_chunks.py 가 response_meta(JSON) 뒤에 글을 이어붙여
  JSON 을 깨뜨린다 (55건).  ★ 이번 작업의 원인은 아니라 안 고쳤다 —
  고치려면 meta 에 칸을 하나 두는 것이 맞다.  ★ 가이드 판단을 청한다
★ carwatch.db.before-compress (1.1GB) 를 남겨 뒀다.  ★ 며칠 안정되면 지운다
  (디스크 여유 6.2GB)
★ test_endtoend 는 V11-12 · V11-45 로 실패한다 — 내가 손대기 전부터 있던 것이다
★ 새로 넣는 원문은 이제 자동으로 눌린다.  끄려면 config raw_body_compress=false —
  이미 눌린 것은 그대로 읽힌다
```
