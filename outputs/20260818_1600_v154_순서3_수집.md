# 순서 3 — 수집 · 축 70점이 죽어 있다

2026-08-18 16:00 · v154 · **SPEC-2026.08.18-r365**

`inbox/ORDER_00_순서.md` **3번**

---

## 1. 무엇을 하려 했나 (지시 원문)

```
1  카탈로그 조합을 센다 — not_called 가 있으면 지금 받는다   → 옵션 20점
2  타이어 트레드 — 로그인 리포트 (unified-report)          → 소모품 10점
3  전기차 배터리 SOH — ev-battery                          → 값 30점
4  엔카진단 — clean-encar                                 → 사이트 보증 10점
★ 넷을 받으면 70점이 살아난다
```

---

## 2. ★★ 셋이 살아났습니다 — 원인은 「안 받은 것」이 아니라 「안 펼친 것」

```
raw_response 에 이미 쌓여 있었습니다
  platform_check   8,120건  (clean-encar)
  ev_battery       7,404건
  inspection_summary 3,999건 · record_summary 4,718건 · sellingpoint 7,295건
그런데 core_listing 은 전건 NULL 이었습니다
```

### 원인 — `S6` 이 네 엔드포인트만 읽고 있었습니다

```python
WHERE r.endpoint IN ('detail','inspection','record','diagnosis')
```

`EXTRA_PARSERS` 다섯(`record_summary` · `platform_check` ·
`inspection_summary` · `ev_battery` · `sellingpoint`)이 **조회에 없어**
파서가 있는데도 한 번도 안 돌았습니다 (개정 296·297 로 늘어난 것들).

```
고침   collect/runner.py s6 — EXTRA_PARSERS 를 조회에 넣는다
```

### 결과

| 칸 | 전 | 후 |
|---|--:|--:|
| `platform_verified` | 0 | **3,470** |
| `inspector_name` | 0 | **1,845** |
| `selling_point` | 0 | **1,462** |
| `ev_battery_soh` | 0 | **23** |

---

## 3. ★ 두 번째 원인 — `site_flags` 가 `site_*` 만 담았습니다

`⑤ 사이트 보증`의 근거는 이름이 `site_` 로 시작하지 않습니다.

```
platform_verified · warranty_deemed · warranty_extend · sell_type
```

`store/core.py` 의 `site_flags` 는 `k.startswith("site_")` 만 담아
**축이 전건 0점**이었습니다.  `SITE_WARRANTY_FIELDS` 를 더해 고쳤습니다.

### 사이트 보증 근거 — 재판정 뒤

```
엔카검증            1,802건   10점
엔카검증+엔카보증    1,627건   20점
엔카검증+엔카보증++     41건   40점
엔카보증                7건   10점
no_warranty            14건    0점
missing                37건   확인 안 됨
```

★ **3,477 / 3,528 이 점수를 받습니다.**  전에는 0건이었습니다.

---

## 4. 등급 분포 — 세 번 바뀌었습니다

| 등급 | 605점 (아침) | 675점 (순서 1) | 675점 + 근거 (지금) |
|---|--:|--:|--:|
| S | 0 | 0 | **0** |
| A | 14 | 0 | **1** |
| B | 228 | 32 | **188** |
| C | 318 | 266 | **296** |
| D | 2,296 | 2,558 | **2,371** |
| E | 672 | 672 | 672 |

---

## 5. 3-1 카탈로그 · 3-2 타이어

### 3-1 카탈로그 — ★ `not_called` 가 0 입니다

```
필요 조합 148 · 받은 것 126 · 85.1%
  empty      21조합 · 매물 505    ← 엔카가 200 [] 를 준다
  not_found   1조합 · 매물  23
  not_called  0조합              ★ 지금 받을 것이 없습니다
카탈로그 없는 매물 528
```

지시서는 「not_called 가 있으면 지금 받는다」인데 **0 입니다.**
남은 22조합은 **엔카가 안 주는 것**입니다 (08-18 확인 · 대표를 셋까지 바꿔도 같음).

### 3-2 타이어 트레드 — ★ 못 했습니다

```
/v2/verification/{id}/unified-report/free   →  401 Unauthorized
```

**로그인이 필요합니다.**  서버에서 못 부릅니다 —
`/admin/collect` 로 마스터가 브라우저에서 받아야 합니다.
소모품 축 10점은 그때까지 전건 「확인 안 됨」입니다.

### 3-3 배터리 SOH — ★ 23건만

`ev_battery` 원문 400건을 열어 보니 **세 칸이 전부 null** 입니다.

```
ensolRawInfo · jatoBatteryInfo · encarComputedInfo  — 전건 null
```

`docs/ENCAR_API.md` 도 「지금 전부 null」이라 적혀 있습니다.
파싱 뒤 `ev_battery_soh` 가 23건 생겼습니다 — 그것이 가진 전부입니다.

---

## 6. 조치 후 결과 — ★ 실제 출력

```
python3.11 tools/verify_axes.py
★ warranty.site — 사이트 보증 (항목 합) (부록 F 5-0)
    3   입력 10.0000 → 손 10 · 저장 10  ○  (엔카검증)
결과: 손계산과 맞습니다

python3.11 tools/check_all.py
통과 266 · fatal 7 · warn 7 · 환경 차이 1 · 미실행 2

python3.11 tools/run_tests.py
결과: 실패 — test_endtoend.py
```

| | 전 | 후 |
|---|--:|--:|
| check_all 통과 | 266 | **266** |
| fatal | 7 | **7** |
| 시험 실패 | 1 | **1** |
| 사이트 보증을 받는 매물 | 0 | **3,477** |
| 파싱된 요약 원문 | 0 | **약 3만 건** |

---

## 7. 못 한 것 · 판단이 필요한 것

### ① 「엔카보증 10」·「엔카보증++ 30」은 아직 추정입니다

```
엔카검증   10 ← platform_verified (clean-encar cleaned)  ★ 근거가 분명합니다
엔카보증   10 ← warranty_deemed=1   (1,634건)            ★ 추정
엔카보증++ 30 ← warranty_extend=1   (41건)               ★ 추정
```

규격은 화면 글자로 적었는데(「성능보험 30일/2,000km」) 우리 칸에는
그 글자가 없어 불 켜짐으로 이었습니다.  **확인해 주십시오.**

### ② 타이어 트레드는 마스터가 눌러야 합니다

`unified-report` 가 401 입니다.  `/admin/collect` 로 받아 주십시오.

### ③ 배터리는 엔카가 안 줍니다

`ev_battery` 세 칸이 전부 null 입니다.  23건은 다른 경로에서 온 것입니다.

### ④ V1-21 은 그대로입니다 (순서 1 · 7절)

마스터가 12:37 에 보낸 목록 73쪽이 아직 안 펼쳐집니다.
`S4` 가 옛 봉투를 다시 읽어 불변 가드에 걸립니다.
