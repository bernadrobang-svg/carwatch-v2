# v4m 관심 — 모바일 기준으로 다시 짰다

```
버전     소스 v252 · 지시서 SPEC-2026.08.26-r749
때       2026-08-26 06:00
지시     ★ 화면 모바일 기준으로 다시 (v4m 시안)
근거     ref/screens/v4m_watch_시안.html (마스터 확정 08-25)
```

---

## 1. 무엇을 하려 했나 (지시 원문)

가이드가 마지막에 남긴 목록의 끝줄이다.

```
남은 것 — KB 상세 전량(수입만이 아니다) · 3-2 걸러 저장 · 3-4 XHR /
멀티 매핑 · 견주기 · ★ 화면 모바일 기준으로 다시 (v4m 시안)
```

`git pull` 로 들어온 시안은 **한 장**이다 — `ref/screens/v4m_watch_시안.html`
(11,807B).  나머지 일곱 화면의 v4m 시안은 아직 없다.

시안이 「지켜야 하는 것」에 잴 수 있게 적어 두었다.

```
★★★ 모바일이 기준이다.  넓어지면 카드가 「옆으로 펴진다」
★★ 카드를 옆으로 붙이지 않는다 — 한 줄에 늘 하나
   340~639   사진 104×78 (340 아래 88×66) · 메뉴 아래 탭 · 단추 아래
   640~1023  사진 180×135 · 메뉴 위 · 단추가 오른쪽으로 · 판정 한 줄
   1024~1180 사진 240×180 · 글씨 키움 · 곁말을 옆에 편다
★★★ 세 폭이 담는 정보는 똑같다 — 자리만 바뀐다
★★★ 좁아진다고 정보를 빼지 않는다 — 글자를 줄이거나 감추지 마라
★★ 한 화면에 한 가지 일만 — 여기는 지켜보는 곳이다
★ 카드 맨 앞에 담은 뒤 무엇이 바뀌었나 — 그것이 이 화면의 이유다
★★★ 나머지는 목록 카드와 똑같이 담는다 —
   축 넷 · 등급 · 차종 · 트림 전체 · 연식 · 주행 · 외장/내장색 ·
   딜러 · 사이트 · 게시 상태 · 값 · 전 값 · 시세차 · 판정 다섯 ·
   순위 · 점수 · 원문 문
★ 팔린 차도 지우지 않는다.  얼마에 팔렸는지가 다음 판단에 쓰인다
★ 단추는 44px 이상 · 한 줄에 둘까지
```

---

## 2. 무엇을 했나 (고친 파일 · 줄)

### ① 관심을 표에서 카드로 — `web/templates/watch.html`

전에는 열세 칸짜리 `<table class="rows">` 였다.
좁히면 `td::before` 로 이름표를 붙여 카드처럼 보이게 하던 것이다.
**표를 좁힌 것이지 카드가 아니었다.**

```
지웠다   <table class="rows"> … </table>  (21~97행)
넣었다   <div class="kcards"> … 카드 … </div>  (21~137행)
```

카드 한 장의 차례는 시안 그대로다.

```
kchg     ↓ 60만 내렸다 / ↑ 61만 올랐다 / ✕ 팔렸다 · 8월 23일 / — 담을 때와 같다
kthumb   사진 (없으면 「사진 없음」)
kax      89 차량 · 80 값 · 59 보증 · 73 취향   + 막대 넷
kname    [S] KOLEOS_HEV                      ♥
ktrim    1.5 E-TECH 에스프리 알핀 2WD · 세부등급 없음
kspec    2024-12 · 55,763km · 쥐색 / 검정색 계열
kspec    점보모터스 · 엔카 · 게시중  (+ N번 재등록)
kprice   2,990만  3,050만(취소선)      ← 팔렸으면 「…에 팔림」
kspec    시세보다 400만 싸다
kj       사고 OK · 골격 OK · 용도 OK · 제조사보증 OK · 사이트보증 OK
ksince   1위 · 742.1 / 910 · 8월 17일부터 · 7일째 (+ 목표가)
ksince   가격 추이 (spark)
kacts    상세 ▸ · 엔카 ↗ · 목표가 · 관심 빼기   (팔렸으면 「비슷한 차 보기」)
```

### ② 세 폭 — `web/static/app.css` 2248~2345행

```
기본            모바일.  사진 104×78 · 단추 두 칸씩 아래
@media ≥640     사진 180×135 · kcardline 을 flex 로 — 단추가 오른쪽으로
@media ≥1024    사진 240×180 · 글씨 키움
@media ≤359     사진 88×66.  ★ 글자는 그대로 둔다
```

★ `display:none` 을 한 줄도 쓰지 않았다.  **자리만 바꾼다.**

### ③ 상단 메뉴를 손가락 폭에서 아래 탭으로 — `app.css` 2347~2362행

```
@media (max-width:639px)
  .nav       position:fixed · bottom:0 · 다섯 칸이 폭을 나눠 갖는다
  .nav-item  min-height:44px
  main       padding-bottom:72px   ← 탭이 마지막 카드를 안 가린다
```

★ 메뉴 글자를 지우지 않았다 — 자리만 아래로 옮겼다.
★ `base.html` 은 안 건드렸다.  CSS 만으로 한다.

### ④ 화면이 계산하지 않게 — `report/screens/views.py` · `build.py`

틀은 `==` 도 `>` 도 못 한다 (V11-104).  판단은 파이썬에서 한다.

```
views.py  WatchRow  + chg_cls · chg_text · gap_cls · gap_text
build.py  _man() _mmdd() _chg() _gap()   (2394~2450행)
```

```
_gap(None)  →  ("dim", "표본이 모자라 시세를 못 만든다")
            ★ 금지 — 0 으로 내는 것.  「시세와 같다」로 보인다
```

### ⑤ 검사 둘

```
S46-67   시안 이름 충돌 검사가 v3_ 만 훑고 있었다 → v4m_ 도 훑는다
         ★ 이 넓힘이 곧바로 내 이름 둘을 잡았다 (아래 4장)
S46-68   ★ 새로 만들었다 — 「관심이 모바일 기준 카드인가」
         ① 표가 아니라 카드다  ② 카드 맨 앞이 「담은 뒤 바뀐 것」이다
         ③ 폭 세 구간이 app.css 에 있다  ④ 좁은 폭에 display:none 이 없다
         ⑤ 시안이 적은 스무 가지를 다 담는다  ⑥ 단추 44px 이상
```

---

## 3. 왜 그렇게 했나

| 한 것 | 근거 |
|---|---|
| 표를 버리고 카드 | v4m 「모바일이 기준이다」 · 표를 좁히면 값이 이름표를 잃는다 |
| 카드 맨 앞이 변화 | v4m 「그것이 이 화면의 이유다」 · 명령서 1-7 ① |
| `display:none` 금지 | v4m 「좁아진다고 정보를 빼지 않는다」 |
| 단추 44px | v4m 「단추는 44px 이상 · 한 줄에 둘까지」 |
| 이름을 `k*` 로 | 마스터 확정 ㉮ — 시안이 이름을 먼저 갖는다 (S46-67) |
| `chg_text` 를 파이썬에서 | V11-104 — 틀은 비교를 못 한다 |

---

## 4. 1차 결과 (고치기 전 숫자)

v4m 판을 넣고 **처음** 돌린 것이다.

```
run_tests   실패 — test_endtoend.py
            V11-60  시안 CSS 를 다시 만들지 않음: 다름
            V11-104 템플릿 문법이 화면에 새지 않음: 1
            V11-106 값 자리에 「—」가 없음: 163
check_all   fatal 에 V11-106 163 · V11-76 5곳
```

세 가지가 다 **내가 낸 것**이다.

```
V11-60   내 .wcard · .wthumb 가 v3_watch·v3_detail·v3_compare 시안이
         먼저 쓰던 이름이었다.  S46-67 을 v4m 까지 넓히자마자 잡혔다
V11-104  ★ 내 잘못이다 — track.html 에 {% if t.order == 'gap' %} 를 썼다.
         엔진은 == 를 모른다.  화면에 글자 그대로 찍히고 있었다
V11-106  ★ 내 잘못이다 — track.html 「빠진 축」 칸이 162곳에서 「—」였다.
         detail 11절 사고 칸이 1곳.  줄표는 「못 받은 것」인지
         「없는 것」인지를 감춘다 (부록 G-4)
```

★ V11-104 · V11-106 은 **v4m 작업이 낸 것이 아니다.**
전에 만든 `/track` 이 낸 것인데 `outputs/render/` 가 낡아 안 보이고 있었다.
이번에 `tools/render_screens.py` 를 다시 돌려서 드러났다.

---

## 5. 조치 후 결과

```
V11-60   이름 스물하나를 w* → k* 로 바꿨다 (app.css · watch.html · v0_guide)
         → 시안 이름 177개가 app.css 와 안 겹친다 (충돌 0)
V11-104  TrackView.orders 를 build 가 만든다 → 샌 곳 0
V11-106  track 「빠진 축 없음」 · detail 「확인 못 함」 → 줄표 0
```

```
python3.11 tools/run_tests.py
  test_admin 통과 · test_admin_flow 통과 · test_collect 통과
  test_crosssite 통과 · test_dict 통과 · test_endtoend 통과
  test_fixtures 통과 · test_integration 통과 · test_invariants 통과
  test_pipeline 통과 · test_registry 통과 · test_report 통과
  test_run 통과 · test_score 통과 · test_screens 통과
  test_spec_ui 통과 · test_store 통과 · test_watch 통과 · test_web 통과
  ruff 통과  (F821,F811,B905,F841,DTZ005,F401)
결과: 통과
```

```
python3.11 tools/check_all.py
(6장에 그대로 붙였다)
```

---

## 6. check_all 그대로

```
DB carwatch.db · run 20260824T225341
통과 350 · fatal 5 · warn 11 · 환경 차이 2 · 미실행 8

■ FATAL — 고쳐야 한다
  V1-08     동일 코드 실패율 100% 인 엔드포인트 없음 — 2건 (예: detail: 328건 전량 실패, inspection: 182건 전량 실패)
  V1-21     받아 두고 안 펼쳐진 원문이 없음 — 182
  V1-23     필요한 조합 대비 받은 카탈로그 비율 — 127조합 · 78.9% · 카탈로그 없는 매물 633건 · 사이트가 안 줌 empty 21조합 · 매물 586건 (예: 8238608202207
  V2-01     ok 원문 수 == CORE 행 수 — 5734
  V11-76    화면 크기·시간이 상한 안 — 2곳

■ warn — 진행은 된다
  V1-07     매물별 엔드포인트 4종 상태 존재 — 4242
  V4-06     RAW 경로가 등록부에 있는가 — 3
  V4-06b    등록부에 있는데 RAW 에 없는 유령 경로 — 48
  V4-11b    판정에 안 쓰는 미분류 경로 — 319
  V4-21     같은 이름의 공개 함수가 두 모듈에 없음 — 7
  V3-39     이론가와 실제 중앙값의 차가 상한 안 — 최대 33.2%
  V3-47     축별 차종 간 결측률 편차가 상한 안 — 3축
  V3-03     축별 source 값 종류 >= 2 — 1건 (예: taste.fitting)
  V3-04     축별 값 종류 >= 2 — 2건 (예: state.consumable, taste.fitting)
  V3-91     가이드 검산 일곱 줄이 표대로 나옴 — 7줄 다름
  V3-96     value IS NULL 과 source 모름 건수 차 — source 모름 33989 · 차 +32,797

■ 환경 차이 — 결함이 아니다
  V1-05     실행 시작 시각 기준이라 DB 만으로는 재현되지 않는다
  V4-01     HMAC 키가 다르면 plate_hash 가 전건 불일치한다

■ 미실행 — 이번 실행에서 그 단계를 안 돌았다
  V1-20     이번 실행에 카탈로그를 안 받았다
  V4-29     판정을 막는 미분류가 없다 — 잴 것이 없다
  V4-30     판정을 막는 것이 없다
  V3-68     부록 F 에 축 목록이 없다
  V3-64     규격에서 경계를 못 읽었다
  V9-09     비율이 같은 매물 짝이 없다 — 잴 것이 없다
  V10-24    사전 확정 이력이 없다
  V11-121   두 사이트에 같이 올라온 차가 없다 — 나란히 낼 것이 없다
```

---

## 7. 못 한 것 · 막힌 것 · 판단이 필요한 것

### ★ 가이드에 여쭐 것 ① — 시안과 개정 427 이 어긋난다

```
v4m 시안   「★ 비교 고르기를 두지 않는다 (비교 화면이 따로 있다)」
개정 427   「비교를 관심이 흡수했다」 — /compare 로 들어가는 문이 관심뿐이다
V11-166    watch.html 에 action="/compare" 가 없으면 실패
/compare   ids 가 없으면 「관심에서 고르십시오 — 카드의 네모를 누른 뒤」
```

★ 둘 다 지킬 수는 없다.  **규칙 2 대로 규격을 고치지 않았다.**
지금은 이렇게 두었다 — **카드에서는 네모를 뺐고**, 목록 아래
「견주기」 한 절로 모았다.  카드는 시안대로다.

```
판단이 필요하다  ⓐ 카드에도 네모를 되살릴까
                ⓑ 지금대로 아래 절에 둘까
                ⓒ 관심에서 비교를 아주 뺄까 (그러면 /compare 로 가는 문이 없다)
```

### ★ 가이드에 여쭐 것 ② — v4m 시안이 한 장뿐이다

`ref/screens/` 에 `v4m_watch_시안.html` 하나만 있다.
목록 · 상세 · 추적 · 현황 · 관리 · 미판정 · 견주기 일곱은 아직 v3 판이다.
★ 관심만 카드고 나머지는 표라 **화면끼리 결이 다르다.**

```
여쭙는다   일곱 장의 v4m 시안을 받을 수 있나
          아니면 이 관심 카드를 본으로 삼아 내가 옮겨도 되나
★ 지어내지 않았다 — 시안이 없는 화면은 손대지 않았다
```

### ★ 남은 fatal — V11-76 화면 크기·시간 4곳

```
/              38KB · 15.84초   ★ 이것이 제일 크다
/listings     306KB ·  0.80초   상한 300KB 를 6KB 넘겼다
/notready      41KB ·  3.37초
/admin/import   7KB ·  8.21초   상한 2.0초
```

★ 6장 check_all 은 「2곳」으로 찍혔다.  같은 코드인데 수가 다르다 —
★ 지금 KB 수집이 돌아 DB 가 바쁘고, 시간 재기가 그 영향을 받는다.
★ 두 번 따로 재니 둘 다 「4곳」이었다.  **크기(306KB)는 흔들리지 않는다.**

★ 이번 작업이 낸 것이 아니다 (전 회차에도 있었다 — 6곳 → 5곳 → 4곳).
★ 다음 바퀴에서 이것만 따로 잡는다.

### ★ V1-08 · V1-21 · V1-23 · V2-01 은 지금 수집이 도는 중이라 그렇다

```
KB차차차 상세를 48바퀴로 받는 중이다 (pid 190118)
받아만 두고 아직 안 펼친 원문이 그만큼 쌓여 있다
★ 코드 결함이 아니다.  수집이 끝나고 S1~S11 을 돌리면 없어진다
★ 지금 KB 매물 235건 — 15,711 건 중
```

### 못 한 것

```
① KB 상세 전량 — 도는 중 (하루 2,880건 · 엿새)
② 낮에 봇 차단 재측정 — 새벽 값만 있다
③ 3-2 걸러 저장 — 코드는 넣었으나 다시 받는 것이 남았다
④ 멀티 매핑 (45-2) · 견주기 (45-3)
⑤ 나머지 일곱 화면의 v4m — 시안을 기다린다
```
