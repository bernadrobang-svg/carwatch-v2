# 순서 9-1 — `test_endtoend.py` 실패 1건

2026-08-18 22:00 · v160 · **SPEC-2026.08.18-r365**

`inbox/ORDER_00_순서.md` **9. 남은 것 — `test_endtoend.py` 실패 1건**

---

## 1. 무엇을 하려 했나 (지시 원문)

```
test_endtoend.py 실패 1건
```

---

## 2. 1차 결과 (고치기 전 숫자)

`run_tests` 는 「실패 — test_endtoend.py」 한 줄만 냅니다.
그 파일을 직접 돌려 보니 **안에서 셋이 실패**하고 있었습니다.

```
python3.11 tests/test_endtoend.py

FAIL  중단 없이 끝난다      V1-16 · V4-11 · V11-77
FAIL  fatal 실패가 없다     V1-16 · V4-11 · V11-77
FAIL  ★ config 를 고치면 판정 결과가 달라진다  {'D':21,'E':24} → {'D':21,'E':24}
결과: 실패 3
```

★ 앞의 둘은 fatal 그 자체입니다 — **순서 9-2 에서 다룹니다.**
여기서는 셋째만 봅니다.

---

## 3. ★ 지렛대가 안 듣는 자리에 걸려 있었습니다

이 시험은 **config 파일을 진짜로 고쳐 다시 돌립니다.**
「파일 → 판정」 경로가 살아 있는지 보는 것이라 값진 시험입니다.

```python
# 전
raw["grade_cuts"] = {g: min(0.99, v + 0.20) for g, v in raw["grade_cuts"].items()}
check("★ config 를 고치면 판정 결과가 달라진다", before != after)
```

### 왜 안 움직였나

`score/grade.py:grade_of` 는 이렇습니다.

```python
for g, cut in cutoffs(policy):
    if ratio >= cut:
        return g
return GRADE_D          # ★ 어느 컷에도 못 닿으면 D 다
```

```
씨앗 매물   D 21 · E 24
E 는 절대조건 탈락이다 (30-score/e-etc.md · STEP 82) — 컷과 무관하다
D 는 「어느 컷에도 못 닿음」이다 — 바닥이다

컷을 0.20 올리면   S 0.9→0.99 · A 0.8→0.99 · B 0.7→0.9 · C 0.6→0.8 · D 0.5→0.7
바닥에 있는 21건은 0.7 에도 못 닿는다 → 여전히 D
```

★ **이미 바닥인 무리를 더 밑으로 밀 수는 없습니다.**
시험은 「config 가 안 먹힌다」고 했지만 config 는 멀쩡했습니다.
**「없는 결함을 있다고」 한 것입니다.**

---

## 4. 무엇을 했나

`tests/test_endtoend.py:148` — **지금 분포가 어느 쪽으로 밀지 정하게** 했습니다.

```python
tops = {g for g, _c in cutoffs(ScoringPolicy(raw))[:1]}
rated = {g for g in before if g not in (NOT_RATED, GRADE_E)}
if rated - tops:
    # 바닥 쪽에 몰려 있다 — 컷을 내리면 등급이 오른다
    raw["grade_cuts"] = {g: round(max(0.01, v - 0.45), 2) for g, v in ...}
else:
    # 이미 꼭대기다 — 컷을 올리면 등급이 내린다
    raw["grade_cuts"] = {g: min(0.99, v + 0.20) for g, v in ...}
```

```
왜   검사는 「움직일 수 있는가」를 봐야 한다.  「움직인 적이 있는가」가 아니다
     ★ V10-26 에서 똑같은 것을 배웠다 (08-18 기록).  같은 잘못을 시험에서 또 했다
```

---

## 5. 조치 후 결과 — ★ 실제 출력

```
python3.11 tests/test_endtoend.py

전   결과: 실패 3 — 중단 없이 끝난다 / fatal 실패가 없다 /
              ★ config 를 고치면 판정 결과가 달라진다
후   결과: 실패 2 — 중단 없이 끝난다 / fatal 실패가 없다
```

| | 전 | 후 |
|---|--:|--:|
| test_endtoend 안의 실패 | 3 | **2** |
| 그중 코드 결함 | 1 | **0** |
| 남은 둘 | fatal 자체 | fatal 자체 → 9-2 |

---

## 6. 못 한 것 · 판단이 필요한 것

### ① 남은 실패 둘은 이 시험의 결함이 아닙니다

`중단 없이 끝난다` · `fatal 실패가 없다` 는 check_all 의 fatal 을 그대로
비춥니다.  **fatal 이 0 이 되어야 이 둘이 통과합니다** — 순서 9-2 입니다.

### ② 씨앗 매물이 전부 바닥인 것 자체는 그대로입니다

```
씨앗   D 21 · E 24 — S·A·B·C 가 하나도 없다
왜     StubEncar 가 주는 값이 빈약해 242/675 (35.9%) 밖에 안 나온다
```

시험은 이제 어느 분포에서도 움직이므로 막히지 않습니다.
다만 **등급 경계 근처의 판정은 씨앗으로 못 봅니다.**
경계를 보는 씨앗을 따로 넣을지 정해 주시면 만들겠습니다.
