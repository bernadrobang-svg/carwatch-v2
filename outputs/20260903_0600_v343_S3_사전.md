# v343 — 2부 S3 사전 · ★ 죽어 있던 도구를 살렸다

명령서 2부 S3 — 「★ 미분류 **333건**」 → 끝났다는 뜻 「pending 0」

## 1차 결과

```
meta_field_usage  미분류  ★ 330건   (facet 190 · ev_battery 68 · list 24 …)
dict_enum         pending ★ 193건   (target 184 · part 9)
```

## ★ 찾은 것 — ★ **등록부를 펴는 도구가 죽어 있었다**

```
$ python3.11 tools/sync_registry.py
ModuleNotFoundError: No module named 'contracts'
```

★ 다른 `tools/*.py` 는 다 ★ 뿌리를 `sys.path` 에 넣는다 — ★ **이 파일만 빠져 있었다.**
★ ★ 그래서 ★ 「파일 → 표」로 펴는 길이 ★ **아예 막혀 있었다.**
★ 고쳤다. 이제 돈다 —

```
$ python3.11 tools/sync_registry.py carwatch.db
  record       50경로  ·  sellingpoint 33경로  ·  record_summary 31경로 …
  unclassified 325건 — 분류 전에는 V4-11 이 fatal 이다
```

★ 다만 ★ **수는 안 줄었다** (330 → 330) — ★ 파일(`config/field_usage.json`)에
★ ★ 그 경로들이 ★ **아예 없기 때문**이다. ★ 펴 줄 것이 없다.

## ★ 왜 여기서 멈추나 — ★ 규격이 자동 확정을 금지한다

```python
# store/dictionary.py:572  confirm_enum
"""pending → confirmed.  ★ 사람이 원문을 보고 확정한다 (STEP 45).

금지   ★ 전건 일괄 확정을 ★ **코드가 자동으로 하는 것**.
      그러면 pending 이 있는 이유가 사라진다
"""
```

★ 그래서 ★ **코드로 닫지 않았다.** ★ `/notready` 에서 사람이 하는 일이다.

## 재 둔 것 — ★ 사람이 볼 때 쓰라고

`dict_enum` `target` pending **184건**을 ★ `targets.json` 과 맞대 봤다 —

```
우리 차종 이름과 같은 것    ★ **8건**  — G80 · 그랜저 · 스포티지 · GV70 · 모델 Y ·
                                      G70 · 그랑 콜레오스 · GV60
우리가 안 받는 차           ★ 176건  — 싼타페 · 팰리세이드 · 아이오닉5 · 쏘나타 ·
                                      투싼 · G90 · 코나 · 아이오닉6 …
```

## 여쭐 것 — ★ S3

```
★ `dict_enum.target` pending 184건을 ★ 어떻게 닫을지 못 정하겠다
★ ★ 규격이 ★ **코드의 일괄 확정을 금지**하므로 ★ 제가 못 닫는다

★ 안 ①  ★ 8건(우리 차종)만 ★ 사람이 확정한다.  ★ 176건은 ★ 그대로 둔다
        ★ ★ 그러면 ★ `assert_no_unknown` 이 ★ **target 축 판정을 계속 멈춘다**
★ 안 ②  ★ 184건을 ★ 다 확정한다 — ★ 「이 값을 안다」는 뜻이지
        ★ ★ 「이 차를 받는다」는 뜻이 아니다 (받는 차종은 `targets.json` 이 정한다)
        ★ ★ ★ 176건은 ★ 우리가 안 받는 차라 ★ 판정에 안 들어온다
★ ★ **제 생각은 안 ②**입니다 — ★ 사전은 「본 값을 적는 곳」이지
   ★ ★ 「고를 값을 적는 곳」이 아닙니다.  ★ 다만 ★ **사람이 눌러야** 합니다.
★ ★ ★ 이 회차에서는 ★ **아무것도 확정하지 않고 넘어갔다** (규격 금지 · STEP 45)

★ `meta_field_usage` 미분류 330건도 같은 자리다 — ★ `/admin/registry` 에서
  ★ ★ 사람이 고른다.  ★ 저장은 이제 ★ **표까지 고친다** (v337 · 1-8)
```

## 이 회차에 바뀐 것

| | 앞 | 뒤 |
|---|---|---|
| `tools/sync_registry.py` | ★ **못 돈다** (ModuleNotFoundError) | **돈다** |
| `meta_field_usage` 미분류 | 330 | 330 (사람이 할 일 · 위 참고) |
| `dict_enum` pending | 193 | 193 (〃) |
