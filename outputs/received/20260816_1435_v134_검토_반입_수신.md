# 검토 — 반입 · S5 실수집 · CORS · S24

**2026-08-16 · GitHub 커밋 12개 · 작업 기록 5건을 읽었습니다**

---

# 1. ★ 실전 수집이 됐습니다

```
반입    CSV 203건 → core_listing 203 · raw_response 1
S5      637 요청 · ok 400 · not_found 237 · error 0 · 거부 0
```

| 엔드포인트 | ok | not_found |
|---|--:|--:|
| detail | **171** | 32 |
| inspection | **94** | 109 |
| record | **109** | 94 |
| diagnosis | **26** | 2 |

```
detail 32건 404 — 08-10 목록이라 6일 사이 내려간 매물
```

**v1 이 문서만 보고 만들다 실패한 자리를, 실측으로 지났습니다.**

## 특히 잘한 것

```
expected 637 = 203×4 − 진단 미호출 175
  진단은 encarDiagnosis == 0 인 매물만 부른다 (STEP 21b)
  812 는 상한이지 호출 수가 아니다
```

**제가 「812 호출」이라 한 것을 정정했습니다. 맞습니다.**

```
web ← contracts·errors·store·score·report 에 parse 가 없다
→ report.screens.admin.parse_import_text() 로 잇는다 (STEP 15a)
```

**계층을 어기지 않고 우회했습니다.**

```
⑥ 문구를 「자동으로 돕니다」가 아니라
  「큐에 넣었습니다 — 실행기가 가져가면 시작합니다」로 적었다 (V11-33 정신)
```

**화면이 거짓말하지 않게 했습니다.**

---

# 2. 지적 4건 — 전부 맞습니다. 규격에 반영했습니다

## ① 메뉴 표에 `/admin/import` 가 없다 — 개정 258

```
V11-12 fatal 1 + test_web · test_admin · test_endtoend 3건의 원인
```

**13장 메뉴 표에 두 행을 넣었습니다.**

```
| **운영** | `/admin/import`  | 목록 반입 — JSON · ID 목록 · CSV | 136a · 136b |
| **운영** | `/admin/collect` | 브라우저 수집 — 사용자 회선으로 부른다 | 136c |
```

**14장 라우팅 표는 개정 253 에 이미 들어가 있었습니다.**

## ② ★ `S3` 가 `S1`·`S2` 를 요구한다 — 개정 259

```
PRECEDES["S3"] = ("S1","S2")     ← 둘 다 /search/ = 407
S4 만 열어 두니 반입 경로에서 영원히 막힌다
```

**정확한 지적입니다. 반입이 `S1`·`S2` 도 대신할 수 있게 했습니다.**

```
필수   code='STEP53-S1' · passed=1 · actual='import'  (S4 와 같은 방식)
필수   무엇을 반입했는지 samples 에 남긴다
금지   반입 없이 S1·S2 를 통과로 치는 것
검산   V11-46
```

## ③ ★ `V4-25` 는 facet 축이라 반입만으로 안 풀린다 — 개정 260

```
AXIS_SOURCES
  fuel · color_ext · color_int  ← facet   ★ 407
  trim                         ← list 봉투 ★ CSV 에 Badge 원문 없음
```

**제가 `STEP 136b ③` 에서 「순서를 지키면 통과한다」고 쓴 것이 틀렸습니다.**

**반입 형식에 facet 을 넣었습니다.**

```
① 원문 JSON  ② ID 목록  ③ CSV  ④ facet 원문 JSON  ← 신설
```

## ④ ★ 큐를 가져가는 실행기가 없다 — 개정 261 · `STEP 132a`

```
run.py 가 recalc_job 을 한 번도 읽지 않는다
넣기만 하고 아무도 안 가져가 관리 화면이 두 번 잠겼다
```

**규격으로 못 박았습니다.**

```
필수   큐에 넣은 것은 누군가 가져간다
필수   실행기가 없으면 큐에 넣지 않는다 — 바로 돌리거나 거절한다
필수   화면 문구가 사실이어야 한다
금지   큐가 밀렸다고 관리 화면 전체가 잠기는 것 — 조정만 잠근다
검산   V10-22 · V10-23
```

```
★ 선택지
  A. 실행기를 만든다 (systemd 타이머)
  B. 큐를 안 쓴다 — 화면이 바로 돌리고 진행을 표시
  ★ B 를 권합니다.  1인 도구에 큐는 과합니다
```

---

# 3. ★ 아직 안 된 것 — CORS 결과

```
web/static/cors_test.html   만들어졌습니다
결과                        아직 없습니다
```

**마스터가 휴대폰에서 열어 주셔야 합니다.**

```
http://43.201.16.78:8765/static/cors_test.html
```

**이 결과로 `/admin/collect` 설계가 갈립니다.**

---

# 4. 규칙 위반 1건 — 스스로 밝혔습니다

```
⑥ 커밋 하나를 섞었다 (규칙 8 위반)
```

**스스로 적은 것이 맞습니다. 다음부터 나누면 됩니다.**

---

# 5. 지금 상태

```
check_all    통과 174 · fatal 4 (V4-25 · V3-30 · V3-32 · V11-12)
check_src    통과
run_tests    실패 6파일 (S24 미완 · 큐 잠금 · 관리자 2명 때문)
core_listing 206 · raw_response 639
```

```
V11-40 · V11-41 · V11-42   실물 DB 에서 전부 통과
```

**`V11-12` 는 개정 258 로 사라집니다.**

---

# 6. 다음 지시

```
inbox 확인해라. spec_258_261.tar.gz 하나에 개정 251~261 이 다 있다.
앞 tar 들을 덮어라 — 상위 집합이다.

지적 4건 전부 맞았다. 규격에 반영했다.

할 일 (순서대로)
1) 개정 258 반영 후 V11-12 가 사라지는지 확인
   test_web · test_admin · test_endtoend 도 함께 볼 것

2) 개정 259 대로 S1 · S2 반입 완료를 구현
   /admin/import 가 목록을 받으면 S1 도 함께 연다
   V11-46 을 validate 에 구현

3) 개정 260 대로 facet 형식(④)을 /admin/import 에 추가
   받으면 S2 완료를 남긴다
   그 뒤 S3(build_dict) 가 도는지 확인 — V4-25 가 사라져야 한다
   ★ facet 원문은 마스터가 휴대폰에서 받아 준다. 화면만 준비해라

4) 개정 261 — 큐를 어떻게 할지 먼저 보고해라
   A(실행기 신설) 와 B(큐 폐기) 중 어느 쪽이 나은지
   너 판단을 근거와 함께. 고르기 전에 만들지 마라

5) S24 를 끝내라. 지금 시험 6건이 상태 오염으로 실패한다

CORS 결과는 마스터가 알려줄 때까지 /admin/collect 를 만들지 마라.

작업마다 outputs/ 기록 남기고 커밋을 나눠서 push 해라.
```
