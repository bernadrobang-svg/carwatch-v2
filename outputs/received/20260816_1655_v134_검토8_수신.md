# 검토 8 — fatal 전멸 · 범위 밖 54% · 2026-08-16

---

# 1. ★ `fatal` 이 전부 사라졌습니다

```
check_all   통과 182 → 194
V4-25 · V3-30 · V3-32   전부 사라짐
```

**오늘 아침 fatal 4 로 시작해 0 이 됐습니다.**

---

# 2. ★ `V2-30` 을 제가 쓴 것보다 넓게 만들었습니다

```
「row_status 만 보면 다음에 다른 칸이 빠졌을 때 또 못 잡는다」
→ 표의 NOT NULL 칸을 기계로 뽑아 파서 다섯이 각각 내는지 본다
→ 일부러 빼서 실제로 잡히는 것을 확인
```

**제 규격은 「전 파서가 `row_status` 를 내는가」였습니다.**
**그건 이번 결함만 막습니다. 다음 칸이 빠지면 또 못 잡습니다.**

**「표에서 기계로 뽑는다」가 맞습니다.**

---

# 3. ★ 실물이 또 결함을 냈습니다 — 그리고 더 큰 것을 드러냈습니다

```
대시보드 500
target_key 가 NULL 인 매물 4,188건을 sorted() 가 str 과 비교하려다 죽음
```

**500 은 고쳤습니다. 다만 숫자가 문제입니다.**

```
전체        7,661건
NULL        4,188건 (54%)
범위 안      3,473건
```

**★ 차종 8종을 골라 받았는데 절반 이상이 안 붙었습니다.**

## 가능한 원인 셋

```
① 목록 쿼리가 차종을 안 걸었다
② 응답에 다른 차종이 섞여 왔다
③ 분류 규칙이 안 맞다 — 모델명은 맞는데 사전이 안 맞음
```

**③이면 큰 문제입니다. 8종을 받았는데 반이 미분류라는 뜻입니다.**

## 규격 — 개정 271

```
필수   저장은 한다 (P3)
필수   target_key NULL 은 판정 대상이 아니다
필수   화면에서 갈라 낸다 — 「범위 안 N건 · 차종 미정 N건」
필수   ★ 왜 안 붙었는지 알 수 있어야 한다
       모델명·배지 원문을 화면에서 볼 수 있게
금지   NULL 을 조용히 버리는 것
금지   NULL 을 임의의 차종에 넣는 것
검산   V2-31 · V2-32
```

```
★ 54% 가 안 붙는 것은 그 자체가 결함 신호다
  「미정으로 두면 된다」로 넘기지 않는다
```

---

# 4. 다음 지시

```
inbox 확인해라. 개정 271 이 들어왔다.

★ fatal 전멸 잘했다. V2-30 을 내가 쓴 것보다 넓게 만든 것도 좋다.
  「표에서 기계로 뽑는다」가 맞다. 내 규격은 이번 결함만 막았을 것이다.

★ 그런데 4,188건(54%)이 target_key NULL 인 것이 문제다.
  차종 8종을 골라 받았는데 절반 이상이 안 붙었다.

1) 먼저 원인을 가려라. 셋 중 무엇인가
   ① 목록 쿼리가 차종을 안 걸었나
      /admin/collect 가 보낸 URL 을 확인해라
   ② 응답에 다른 차종이 섞였나
      NULL 매물의 Manufacturer · ModelGroup 을 세어 봐라
   ③ 분류 규칙이 안 맞나
      NULL 매물 상위 20종의 모델명을 내라
   ★ 숫자로 답해라. 셋 중 어느 것인지 확정한 뒤 고쳐라

2) V2-31 · V2-32 를 구현해라
   NULL 이 판정에 안 들어가는가 · 모델명이 화면에 보이는가

3) 그 다음 한 차종만 S5 → S6 → S9 → S10 끝까지
   KOLEOS_HEV 로. 이미 상세 400건이 있다

4) 파싱 뒤 숫자
   사전에 없는 값 몇 · 미분류 몇 · 매핑 실패 몇 · 판정 몇 · 등급 분포
```
