# V11-34 수정 — 화면이 요청당 쿼리 상한을 넘지 않음

작업 시각 2026-08-16 01:18 · 소스 v134 · 실행 `python3.11`
커밋 — 마스터가 `a1089e5` 「v134 기준선」에 함께 담음 (전용 커밋 없음)
★ 이 파일은 한 번 유실됐다. 커밋되지 않은 상태로 사라져 14:10 에 복원했다 (S25 가 잡는 사고다)

---

## 1. 무엇을 하려 했나 (지시 원문)

> 「V11-34 부터 고쳐」

남은 fatal 4건 중 `V11-34` 를 먼저 잡기로 했다. `test_endtoend` 까지 같이 막고 있었다.

## 2. 무엇을 했나 (고친 파일 · 줄)

### ① `report/screens/build.py` — 같은 집계를 두 번 돌던 것

`view_dashboard` 가 `_grade_counts()` 를 두 번 불렀다 — `grade_counts` 용, `grade_rows` 용.
완전히 같은 SQL 이다.

```
SELECT grade, COUNT(*) FROM result_score WHERE calc_version=? GROUP BY grade
```

- `build.py:547-548` — 반환문 앞에서 한 번만 계산하는 지역 변수 추가
- `build.py:558-560` — 두 자리가 그 변수를 쓰도록 변경

### ② `store/core.py` — COUNT 4개를 한 번에

`state_counts()` 가 `COUNT(*)` 4개를 각각 돌았다 (`core.py:490` 한 자리에서 4회).

- `core.py:494-508` — 서브셀렉트 하나로 묶고, 표가 없을 때만 하나씩 세는 갈래를 남겼다

동작이 같은 것을 실측으로 확인했다.

```
state_counts(carwatch.db) = {'listings': 3, 'unclassified': 48, 'scores': 3, 'not_rated': 0}
state_counts(:memory:)    = {'listings': 0, 'unclassified': 0, 'scores': 0, 'not_rated': 0}
```

## 3. 왜 그렇게 했나 (규격)

| 근거 | 내용 |
|---|---|
| `docs/chapters/61-web.md:137` | `max_queries_per_request` 기본 `20` |
| `docs/chapters/61-web.md` 검사 표 | `V11-34` 화면이 요청당 쿼리 상한을 넘지 않음 · **fatal** |
| `docs/chapters/00-standard.md` 부록 B | config 키 등재 |
| `docs/guide/03_이력.md` 개정 200 | 목록 N+1 금지. 200행 × 5축 = 1,200 쿼리였던 사고 |

규격이 요구하는 것은 「상한을 넘지 않는다」이지 「쿼리를 줄여라」가 아니다.
그래서 **의미가 같은 중복만** 없앴다. 화면이 내는 값은 하나도 바뀌지 않는다.

## 4. 1차 결과 (고치기 전 숫자)

```
V11-34 passed=False  worst=21  bad=['/ 한 쪽에 21 쿼리']   (상한 20)
```

`/` 대시보드 21쿼리를 전부 찍어 12번과 13번이 같은 문장임을 확인했다.

```
python3.11 tools/check_all.py
통과 171 · fatal 4 · warn 6 · 환경 차이 0 · 미실행 3
  V4-25 · V3-30 · V3-32 · V11-34(21)

python3.11 tools/run_tests.py
  test_endtoend  실패
      FAIL  중단 없이 끝난다  ['S11: V11-34 화면이 요청당 쿼리 상한을 넘지 않음: 21']
      FAIL  fatal 실패가 없다  ['V11-34', 'STEP53-S11']
결과: 실패 — test_admin_flow.py, test_endtoend.py, test_integration.py, test_spec_ui.py
```

## 5. 조치 후 결과

```
① 중복 집계 제거 후   passed=True  worst=20  bad=[]
② COUNT 4개 병합 후   passed=True  worst=17  bad=[]
```

①만으로도 통과지만 상한에 딱 붙어 여유가 0이라 ②를 더했다. 21 → 20 → **17**.

```
python3.11 tools/check_all.py
통과 172 · fatal 3 · warn 6 · 환경 차이 0 · 미실행 3
  V4-25 · V3-30 · V3-32

python3.11 tools/run_tests.py
  test_endtoend      통과
  ruff               통과  (F821,F811,B905,F841,DTZ005,F401)
결과: 실패 — test_admin_flow.py, test_integration.py, test_spec_ui.py
```

| 지표 | 고치기 전 | 고친 뒤 |
|---|--:|--:|
| `/` 한 쪽 쿼리 수 | 21 | **17** |
| `check_all` 통과 | 171 | **172** |
| `check_all` fatal | 4 | **3** |
| `run_tests` 실패 파일 | 4 | **3** |

## 6. 못 한 것 · 막힌 것 · 판단이 필요한 것

- **남은 fatal 3건은 손대지 않았다** — `V4-25` · `V3-30` · `V3-32`. 셋 다 사전이 비어서 나고,
  사전은 원문에서 만든다. 상세 수집(S5) 없이는 못 채운다.
- **상한까지의 여유** — 17/20 이다. 화면에 조회를 3개 더 붙이면 다시 fatal 이 된다.
  `store/core.py` `current_versions()` 도 같은 방식으로 묶으면 14 가 된다. 필요해지면 그때 한다.
- **이 기록이 유실됐다** — 커밋하지 않은 채 두어 사라졌다. 개정 257 `S25` 가 막으려는 것이
  정확히 이 사고다. 이후로는 작업마다 커밋한다.
