# S6 열한 사이트 파싱 · 적재 DB 실전 — ★ 본 DB 는 0.459 GiB 그대로

```
지시문   SPEC-2026.09.03-r1124 · 5절 S6 · 0-1~0-4
날짜     2026-09-04 02:05 (KST)
```

## 1. 무엇을 하려 했나

```
지시문 S6 — 「★ `parsed_at` — ★ 열한 사이트.  ★★ 뿌리는 ★ `run.py:113` 이
            ★ `EncarAdapter` 로 못 박혀 ★ 판이 ★ **엔카만 안다**는 것이었다 —
            ★ `--site` 로 고른다 | 끝 = 100%」
```

## 2. 1차 결과 — ★ 뿌리는 **이미 고쳐져 있었다**

`run.py:108` 에 ★ `ADAPTERS` 표가 있고 ★ `_adapter_for(site)` 가 ★ 없으면
`GenericAdapter` 로 떨어진다. ★ **코드 일은 앞 회차가 끝냈다.**
★ 남은 것은 ★ **자료**였다 — ★ `parsed_at` 을 잰다.

```
사이트별 parsed_at (active · 고치기 전)
  kbchachacha    234 / 4,264   ★ **5.5%**
  encar        1,484 / 1,484   100.0%
  hyundai_cert   209 /   209   100.0%
  volvo_selekt   148 /   148   100.0%
  heydealer       92 /   110    83.6%
  reborncar       85 /    86    98.8%
  lexus_certified 49 /    49   100.0%
  revolt          47 /    47   100.0%
  kcar            28 /    29    96.6%
  bmw_bps          3 /     3   100.0%
```

## 3. 무엇을 했나

### 3-1. 열한 사이트를 ★ 적재 DB 를 거쳐 넣었다 (0-1·0-2·0-4 실전)

| 사이트 | 넣음 | 적재 DB |
|---|--:|--:|
| reborncar | 2,262 | ★ **102.0 MiB** |
| hyundai_cert | 1,372 | 48.5 MiB |
| kia_cpo | 1,351 | 4.9 MiB |
| revolt | 964 | 0.0 MiB |
| volvo_selekt | 799 | 5.7 MiB |
| kbchachacha | 760 | 24.1 MiB |
| kcar | 431 | 9.3 MiB |
| heydealer | 404 | 0.0 MiB |
| lexus_certified | 350 | 0.0 MiB |
| bmw_bps | 282 | 14.6 MiB |
| bobaedream | 225 | 3.7 MiB |

```
★ 열한 판을 다 돌렸는데 ★ **본 DB 는 0.456 → 0.459 GiB** 다.
★ ★ `raw_response` 의 ★ **몸통 있는 행은 0** 그대로다.
★ ★ ★ 전 같으면 ★ 리본카 한 판만으로 ★ 본 DB 가 ★ **102 MiB** 부풀었다 —
  ★ ★ ★ ★ 그것이 ★ 09-03 에 ★ DB 를 1.08 GiB 로 만들고 ★ 서버를 세 번 죽인 길이다
★ 적재 DB 는 ★ 판마다 ★ 다 치웠다 (`tmp/` 가 비어 있다)
```

### 3-2. ★ KB 수집기가 ★ 원문을 전건 RAM 에 쌓던 것을 막았다

```
tools/collect_kbchachacha.py:440   rows = []  ← 원문 파일을 **다** 모은 뒤 돌았다
실측 09-03   원문 하나가 평균 **488 KB**
             KB 상세 파일 760개 = **371 MB** · 리본카 1,158개 = **565 MB**
             장비 RAM = **1,841 MB**
고침         생성기(`_stream()`)로 바꿨다 — ★ 한 번에 ★ **한 개**만 메모리에 있다
```

★ 다른 수집기(`collect_revolt` · `collect_lexus` · `collect_bmw` · `collect_kia_cpo`)의
같은 자리는 ★ **파싱 결과(작은 dict)** 를 모으는 것이라 ★ 위험하지 않다 — ★ 안 건드렸다.

## 4. 조치 후 결과

```
본 DB                       ★ 0.459 GiB · ★ 몸통 있는 행 **0**
`tmp/`                     ★ 비었다 (적재 DB 를 판마다 치웠다)
디스크                      13G 남았다
KB 말고 열 사이트            ★ 83.6 ~ 100% (헤이딜러 83.6 · K카 96.6 · 리본카 98.8 · 나머지 100)
kbchachacha                ★ 5.5% 그대로 — ★ **상세를 아직 못 받았다** (S1 의 일이다)
```

## 5. 못 한 것 · 다음

```
① ★ KB 상세 — ★ `python3.11 tools/collect_kbchachacha.py --narrow --detail 12
   --bursts 40 --interval 30` 을 ★ **배경으로 띄웠다** (2026-09-04 02:03 시작).
   ★ 실측 근거 — ★ 쉼 없이 21건째 캡차 · 5분 쉬면 12/12 · 누적이라 묶음을 늘려도 안 된다.
   ★ ★ 한 판이 ★ 약 **3.9시간**이다 (472건).  ★ 끝나면 회차에 수를 적는다
② 헤이딜러 83.6% · K카 96.6% — ★ 원문 파일이 없는 매물이다.  ★ 다음 회차
```
