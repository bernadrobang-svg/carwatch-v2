# v336 — 명령서 11~18 · 순위·사진·실패 원문·제원 27종

명령서 `outputs/ORDER_20260829.md` (r1085 — 회차 중 **9줄 → 18줄**로 늘었다)

## 0. 회차 처음 숫자

```
통과 494 · fatal 14 · warn 14 · 환경 차이 2 · 미실행 10
결과: 실패 — test_endtoend.py, ruff
```

---

## 1. 명령서 17 — `by_year` 를 코드가 읽는다

```
| 17 | config/dimensions.json MODEL_Y | ★★ 연식으로 갈랐다 (가이드 09-02) —
     ★ 너희는 값을 재지 마라.  ★ `by_year` 를 코드가 읽게 하라:
     ★ `~2024-12` 4,751 · `2025-01~` 4,790(주니퍼).
     ★ 우리 매물 827건이 450 대 377 로 반씩 갈린다 — ★ 한 값이면 39mm 틀린다 |
```

고친 곳 둘 —

| 파일 | 무엇 |
|---|---|
| `collect/runner.py:1338` `_dimensions()` | `value` 만 뽑던 것을 **묶음째** 넘긴다 |
| `analyze/axis/taste.py` `_length_mm()` (신설) | 매물 연식으로 **고른다** |

경계를 실제로 쟀다 —

```
2022-06 → 4,751     2024-12 → 4,751     2025-01 → 4,790     2026-03 → 4,790
(연식 모름) → 4,751 (물러섬)             단일값 G80_25T → 5,005 (그대로 돈다)
```

재판정 뒤 —

```
모델Y taste.size 점수 갈림   26점 450건  ·  30점 377건
```

★ **가이드가 센 「450 대 377」과 정확히 같다.**

## 2. 명령서 18 — 제원 12종 중 7종

`tools/fetch_dimensions.py` 를 고쳤다. **내 도구가 거짓을 냈다** —

```
실측 09-02  「Specifications」를 무조건 누르게 해 뒀더니 ★ 위키 **각주**가 걸려
           ★ BMW 보도자료 쪽으로 넘어가 ★ 엉뚱한 글을 읽었다
           (`BMW_X3_(G01)` 이 「Login Contact World Wide Corporate Brands…」)
고침       ★ **먼저 읽고, 못 찾을 때만 누른다** · 주소가 바뀌면 되돌아온다
더함       ★ 세대 머리말을 뽑는다 — 「Third generation (G01; 2017)」
           ★ 갈림 해가 없으면 `by_year` 를 못 적는다
```

| | 앞 | 뒤 |
|---|--:|--:|
| 제원이 있는 차종 | 20 | **27** |
| 크기 축이 점수를 준 매물 | 6,326 | **10,213** |

넣은 7종 — `X3_IMPORT`(4단 세대) · `GLC_IMPORT` · `XC60_IMPORT` · `NX_HEV` ·
`S60_IMPORT`(3단) · `GRANDEUR_LPG` · `POLESTAR3_EV`.

★ **안 넣은 5종과 까닭** (지어내지 않았다)

```
RX_IMPORT    세대 5개인데 전장 4개 — 짝을 못 지었다
ES_HEV       XZ10 이 「4,960–4,975」 범위다 — 어느 값인지 못 갈랐다
V60CC_IMPORT 크로스컨트리는 몸통이 V60 과 다르다.  그 값을 못 찾았다
G80_EV       전동화 G80 의 전장을 못 찾았다 (RG3 와 같다고 짐작하지 않는다)
EX60_IMPORT  아직 안 나온 차라 쪽이 없다 (`S46-92` 도 같은 것을 짚는다)
```

`GRANDEUR_LPG` 는 **2015년식 앞을 비웠다** — 세대 7개에 전장 6개라 HG 에 짝을
못 지었다. 그 매물은 0점이 아니라 **「못 쟀다」**가 된다 (금지 12).

## 3. 명령서 11 — 순위 번호 (`S46-242`)

```
마스터 09-01 — 「★ 왜 추천에 목록 번호가 없고」
```

시안 `lst-rank`·`rc-rank` 를 목록·추천에 넣었다.
★ 순위를 못 매긴 것(제외·등급 없음)은 **「—」**다 — 없는 번호를 짓지 않는다.

★ 추천 행(`RecommendRow`)엔 `rank` 칸이 아예 없었다. 더했다 —
이 화면은 `OFFSET` 이 없어 **1부터**다 (쪽 넘김이 생기면 그때 고친다).

## 4. 명령서 11·14 — 크기·내장색 축이 화면에 (`S46-98`)

```
S46-98 앞   /listings 에 없다 9 — 문이·사이트는·색상·아직·없으면·쟀습니다·전장·주소를
S46-98 뒤   /listings 에 없다 6 — 문이·사이트는·아직·없으면·쟀습니다·주소를
```

세 가지를 고쳤다 —

| | 무엇 |
|---|---|
| `config/labels.json` | 「색상」·「내장색」·「크기」 → **「색상 (외장)」·「색상 (내장)」·「크기 (전장)」** (시안 넉 장이 그렇게 쓴다) |
| `report/screens/build.py` `AXIS_LABEL` | **코드에 박혀 있던 넷**을 `labels.json` 에서 읽게 (`S14`). 축이 둘 늘자 이름을 못 찾아 화면에서 **조용히 사라졌다** |
| `AxisPoint` 신설 · `lst-axes` 줄 | 시안 「색상 (외장) 25/25 · 크기 (전장) 9/31」. `AxisChip` 은 **O·-·?** 만 내지 **몇 점인지를 못 낸다** |

```
확인   /listings   크기 (전장) 30/30행 · 색상 (내장) 30/30행
      /recommend  크기 (전장) 60 · 색상 (내장) 60   (앞 0)
```

## 5. 명령서 14 — 말없이 비우지 않는다

```
{% if !r.encar_url %}그 사이트는 주소를 아직 못 쟀습니다{% endif %}
```

```
확인   /listings?site=kcar   12행 중 12행이 그 말을 낸다 (K카는 robots 가 막는다)
```

★ **물음 20** — `S46-98` 이 기본 `/listings` 쪽만 본다. 그 쪽에는 링크 없는 행이
없어 「사이트는·주소를·아직·쟀습니다」가 여전히 「없다」로 잡힙니다.
★ 「문이」·「없으면」은 시안의 **지시문**(「원문 문이 없으면 … 를 낸다」)이라
화면에 낼 글이 아닙니다. `sian_word_skip` 에 넣을까요, 시안 글월을 고치실까요?

## 6. 명령서 13 — 볼보 주소 두 칸 (`S46-94`)

`_source_url()` 이 `source_id` 한 칸만 넣었다 → **두 칸**으로 늘렸다.
`site_model` 을 쿼리 **맨 뒤**에 붙였다 — 가운데 넣으면 뒤 번호가 다 밀린다.

```
실측   volvo_selekt site_model = `xc40` · `v60-cross-country`
막았다  틀이 바라는 칸이 비면 `None` — `…/None/b4-awd-…` 같은 깨진 주소를 안 만든다
```

## 7. 명령서 15 — 실패 응답이 사라졌다 (`S46-244`)

```
규격 5장 STEP 53-⑤ — 「★ 실패 응답도 원문이다 · 삭제 금지」
```

**뿌리를 찾았다** —

```
audit_request  detail 288건 (287 error)   ↔   raw_response  detail 3건
raw_response_reject                            0행 (통째로 비어 있다)
```

```
S5 의 차례가 이랬다
    streak.observe(res)
    if streak.tripped: break      ← ★ 여기서 끊는다
    save_raw(...)                 ← ★ **한 번도 못 온다**
★ 이 `break` 는 **안쪽(kind) 고리만** 끊는다 — 바깥 매물 고리는 계속 돈다.
★ 그래서 한 번 걸린 뒤 **모든 매물**이 두드리기만 하고 원문을 안 남겼다 — 285건
```

★ **먼저 남기고 나서 끊게** 바꿨다 — S3(목록) · S5(상세) · S6a(facet) · 카탈로그 **넷 다**.
★ facet 은 `ok` 가 아니면 `continue` 로 **그냥 버리고 있었다** — 그것도 남긴다
(`axis_count=None` = 「못 읽었다」 · `0` = 「받았는데 축이 없다」).

## 8. 덤으로 — S6a 가 죽어 있었다

```
JSONDecodeError: Expecting value: line 16 column 1   (tools/build_dict.py:103)
```

`raw_response` 의 `status='ok'` 를 **전부 JSON 이라고 여겼다.**
리본카·BMW BPS·보배드림·KB·현대인증·볼보는 **HTML 쪽**을 준다 —
그것이 그 사이트의 **바른 원문**이다 (실측 **2,766건**).
건너뛰되 몇 건인지 낸다. **S6a 가 살아났다.**

## 9. 명령서 12 — 사진이 하나도 안 보인다 (`S46-243`)

★ 명령서는 「밑주소가 엔카 하나뿐이라」고 봤으나 **까닭이 달랐다** —

```
엔카      [{"location": "/carpicture04/…", "ordering": …}]   ← 묶음 꼴
K카·차차차·리본카·볼보·렉서스
         ["https://img.kcar.com/…", …]                        ← ★ **온전한 주소 글월**
★ `isinstance(p, dict)` 에서 ★ 다섯 사이트가 통째로 버려졌다
```

| | 앞 | 뒤 |
|---|--:|--:|
| 사진이 나오는 매물 | 15,464 | **16,292** |

```
확인   /listings?site=kcar        <img> 25장   (앞 0)
      /listings?site=kbchachacha  78장        (앞 0)
```

★ `photo_url()` 이 꼴을 **따로 풀고 있었다** — `photo_urls()` 하나로 모았다.
같은 일을 두 군데 적으면 한 군데만 고치는 일이 생긴다 (`S14`).

## 10. 명령서 16 — 「판을 돌려야 나옵니다」

수집 뒤 화면이 아무 말도 안 했다. 이제 **수로** 낸다 —

```
「… 저장했습니다 · ★ 아직 판정 안 된 매물 10,715건 — 판을 돌려야 화면에 나옵니다」
```

## 11. 차례 ② — 단추·글자 겹침 (300·360·600·900px)

```
앞   진짜 겹침 4 — ['1 / 그랑 콜레오스 1.5 E-', '♡ / 그랑 콜레오스 1.5 E-']  (네 폭 모두)
뒤   ★ 300px 0 · 360px 0 · 600px 0 · 900px 0   ★ **네 폭 모두 0**
```

다섯 번 짚어 닿았다. **헛짚은 것까지 그대로 적는다** —

```
① `float:left`               → 겹침 4.  띄운 것은 **줄 상자만** 민다.
                                옆 덩이의 상자 자체는 그 밑으로 들어간다
② `.pickmid{overflow:hidden}` → 4→3.  그러나 ★ **사진 밑 183px 이 비었다** —
                                명령서 ① 이 막은 바로 그것이다.  되돌렸다
③ 순위를 ★ **이름 줄 안**으로  → 300·360·600px 0 · 900px 3
④ 하트 몫 `padding-right:48px` → 하트 겹침이 닫혔다 (하트는 `absolute` 라 자리를 안 먹는다)
⑤ ★ **재서 찾았다** — ★ 두 뿌리가 더 있었다 (CDP 로 이긴 규칙을 뽑았다)
```

★ ⑤ 에서 **내가 앞 회차에 「막았다」고 적은 것이 안 막혀 있었다** —

```
뿌리 ㉮  `.row a:not(.btn)…{padding:14px 0}` 이 ★ `.pickname a{padding:0}` 보다
        ★ **더 세서** 이겼다.  ★ 이름 상자가 48px 이 되어 제원 줄과 겹쳤다
        → `:not(.pickname *):not(.name *)` 로 뺐다
뿌리 ㉯  순위 꾸밈을 ★ **두 군데** 적었다.  앞의 `float:left` 판이 남아 있어
        ★ `display` 가 `block` 으로 셈됐다 (`float` 이 덩이로 바꾼다).
        ★ 실측 — `getComputedStyle(.rc-rank).display === 'block'` 이었다
        → 앞 판을 지웠다.  ★ 같은 것을 두 군데 적으면 한 군데만 고치게 된다 (`S14`)
```

★ 실측 (900px · 재서 확인) —

```
앞   SPAN.rc-rank  652~669   A(이름) 664~712   → ★ 664~669 이 겹친다
뒤   SPAN.rc-rank  652~669   A(이름) 705~725   SPAN.dim(2년) 730~747  → ★ 안 겹친다
```

## 12. 차례 ④ — 삭제된 차량

이미 지켜지고 있었다. **재서 확인했다** —

```
/listings   계약·예약·판매완료 딱지  0건
/sold       30건이 그대로 남는다
DB          CONTRACT 1,113 · RESERVED 99 · gone 730
```

## 13. 내가 낸 fatal 을 내가 닫았다

축 이름을 바꾸고 화면에 줄을 더하니 **fatal 이 넷 늘었다.** 그대로 적는다.

| 검사 | 무엇을 잘못했나 | 어떻게 닫았나 |
|---|---|---|
| `V11-01` | 「판정 안 된 매물 수」 SQL 을 `web/views.py` 에 적었다 — **web 은 SQL 을 안 쓴다** | `store/core.py` 의 `unscored_count()` 로 옮겼다 |
| `S46-67` | `app.css` 에 `.rc-axes` 를 지었다 — **시안이 그 이름을 이미 쓴다** (마스터 확정 ㉮) | 뺐다. 추천은 이미 `.pickaxes` 로 축을 내고 있었다 — **내가 겹쳐 만든 것이다** |
| `V11-60` | 위와 같은 뿌리 (시안 값과 다름) | 〃 |
| `V11-76` | `/listings` 가 상한을 넘었다 | **찬 캐시 탓이었다** — 아래 잼 |

```
/listings   첫 호출 36.98초  →  두 번째 1.01초  →  세 번째 0.83초
★ 첫 호출이 ★ 검사(`check_all`)와 겹쳐 돌아 그렇다.  ★ 색인은 6개 다 있다
   (`ix_listing_target`·`vehicle`·`vin`·`parsed`·`plate` ＋ autoindex)
```

## 13b. 물음 22 — `S46-100` (시안 낱말 차례)

```
S46-100  /detail/3 4번째부터 다르다 — 시안 「제조사 보증」 ↔ 화면 「색상 (외장)」
```

★ **축 이름을 시안에 맞추자 드러난 것**이다. 앞에는 화면이 「크기」·「색상」이라
불러 **견줄 낱말 자체가 아니었다.** 이제 같은 이름이라 차례가 견줘진다.

```
시안   … 사고 · 골격 · 용도 · 제조사 보증 · 사이트 보증 … 색상 (외장) · 크기 (전장) · 트림
화면   트림 · 크기 (전장) · 사고 · 골격 · …          ← ★ 취향 축이 판정 앞에 있다
```

★ 화면의 절 차례는 `detail.html:15` 에 적힌 **규격 차례**다 —
「판정 → 값 → 총액 → 관문 → 모르는 것 → **취향** → 상태 → 추이 → 딜러 → 대신 볼 것」.
★ 시안은 **보증을 취향 앞**에 둔다. **규격 절 차례와 시안이 어긋난다.**
★ 규칙 2 대로 제가 고르지 않고 **여쭙니다** — 어느 쪽이 정본입니까?

## 14. 회차 끝 숫자

```
통과 499 · fatal 11 · warn 13 · 환경 차이 2 · 미실행 9

결과: 실패 — test_endtoend.py
```

★ 시험은 **하나**만 실패한다 — `test_endtoend.py` 는 **남은 fatal 을 그대로 따라가는**
시험이라 fatal 이 0 이 될 때 같이 닫힌다. `ruff` 는 이번 회차에 닫았다
(가이드가 새로 낸 `S46-241` 코드에 안 쓰는 `import os` 가 있었다).

| | 회차 처음 | 회차 끝 |
|---|--:|--:|
| `check_all` 통과 | 494 | **499** |
| fatal | 14 | **11** |
| ruff | 실패 | **통과** |
| 제원이 있는 차종 | 20 | **27** |
| 크기 축이 점수를 준 매물 | 6,326 | **10,213** |
| 사진이 나오는 매물 | 15,464 | **16,292** |
| 실패 응답이 남는가 (S5 detail) | 3 / 288 | **288 / 288** (고침 뒤 새 판부터) |
| 진짜 글자 겹침 (300·360·600·900px) | 4 | **0** |
| 화면에 크기·내장색 축 | 없음 | 목록 30/30 · 추천 60/60 |
| 화면에 순위 번호 | 없음 | 목록 30 · 추천 60 |

남은 fatal 11 중 **내 몫은 둘**이다 — `S46-98`(물음 20) · `S46-100`(물음 22).
나머지 아홉은 가이드 몫이다 (`S46-152`·`S46-207`·`V1-27`·`V1-08`·`V1-23`·
`V2-01`·`V4-11`·`V3-77`·`V11-45`).

## 14. 못 한 것 · 판단이 필요한 것

| | 무엇 |
|---|---|
| **물음 17** (계속) | 엔카 수집(S3)을 돌릴까요? `S46-234` 는 다음 수집까지 fatal 로 남습니다 |
| **물음 18** (계속) | 제 `build_index.py` 가 가이드 `CHECKS.md` 를 502줄 깎습니다. `S46-32` 가 다시 만들기를 요구해 다시 만들어 뒀습니다 |
| **물음 20** | `S46-98` 의 남은 여섯 낱말 (위 5절) |
| **물음 21** | 900px 겹침 3 (위 11절) |
| **물음 19** 해결 | 가이드가 `by_year` 로 답을 줬습니다 — 그대로 넣었습니다 |
| 명령서 1~9 | 앞 회차에 고쳤습니다. `S46-241`(배포에서 재판정)은 지금 통과입니다 |
