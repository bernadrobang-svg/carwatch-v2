# 4-8 링크·사진을 꽂았다 · 4-1·4-9 올려서 쟀다 · 0번·2번 다시 쟀다

```
지시문   SPEC-2026.09.04-r1141 · 0 · 2 · 4-1 · 4-8 · 4-9 · 5
날짜     2026-09-04 20:10 (KST)
무엇으로 쟀나   ★ **크로미움으로 배포를 열어** 쟀다
```

## 1. 4-1 — 가이드 CSS·HTML 을 올렸다 (14·15·16차)

★ 받은 것 — `6696a6b1` 막대 높이 · `08928f98` 거르개 칩 · `40247b72` 사진 흐름 ·
`5c1d4424` 링크·사진 값.

| 잰 때 | 가려진 글자 | 상자 겹침 |
|---|--:|--:|
| 앞 회차 | 8 | 17 |
| ★ 이번 (14·15·16차) | ★ **7** | 18 |

```
/listings   390px  가려짐 1 · 겹침  6
/listings   900px  가려짐 0 · 겹침 12
/recommend  390px  가려짐 0 · 겹침  0
/recommend  900px  가려짐 6 · 겹침  0   ← ★ 겹침이 1 → 0 이 됐다
/track      390·900px  가려짐 0 · 겹침 0
```

## 2. ★ 4-9 — BMW 화면 (따로 쟀다)

```
/listings?site=bmw_bps  390px  잎  91 · ★ 가려진 글자 **30** · ★ 상자 겹침 **74**
/listings?site=bmw_bps  900px  잎 147 · ★ 가려진 글자 **37** · ★ 상자 겹침 **96**
                              합 ★ **67** · ★ **170**
캡처   outputs/shots/bmw/  (2장)
잰 수  outputs/hidden_text_bmw.json
```

★ **가이드께서 재신 수(가려짐 24 · 겹침 73)와 다르다.** ★ 재는 곳이 다르다 —
★ 나는 ★ **배포**(참 매물 · 두 폭)로 재고, ★ 가이드는 다른 자리로 재신 듯하다.
★ ★ **BMW 는 다른 화면보다 훨씬 나쁘다** — ★ 전체가 7·18 인데 ★ BMW 만 67·170 이다.

### ★ 내가 낸 흠 하나 — 고쳤다

```
★ BMW 만 따로 재려고 돌렸더니 ★ 그 판이 ★ **`outputs/hidden_text.json` 을 덮었다** —
  ★ ★ `S46-271` 이 읽는 ★ **정본 파일**이다.  ★ 검사가 ★ 「전체」 대신 ★ BMW 수를 읽는다.
고침  `hidden_text_report(..., report=)` — ★ 곁가지는 ★ 다른 곳에 적는다
      ★ 정본을 다시 돌려 ★ 7·18 로 되돌려 놓았다
```

## 3. ★★ 4-8 — 리볼트·볼보셀렉트 링크·사진

★ 가이드 실측 09-04 — 「★ 링크·사진은 ★ **없는 게 아니라 ★ 안 낸 것**이었다」.
★ 값은 ★ `config/endpoints.json` 에 ★ 넣어 두셨다 (`web_url` · `photo_field`).

### 1차

```
리볼트        active 47 · ★ 사진 **0** · ★ 원문 링크 **없다**
볼보셀렉트     active 148 · 사진 148 · ★ 원문 링크 **없다**
`site_detail_url`  revolt `None` · volvo_selekt `None`
```

### 무엇을 했나

| 파일 | 무엇 |
|---|---|
| `parse/revolt/mapping.py` | ★ 사진을 꽂는다 — 칸 이름은 ★ `photo_field` 가 정본 (`S14`) |
| `report/screens/build.py` `_site_detail_urls` | ★ `endpoints.json` 의 ★ **`web_url`** 도 읽는다 |

```
★ `web.json` 이 ★ **먼저**다 — ★ 거기 값이 있으면 안 덮는다.  ★ 두 벌로 안 적는다
★ `{detail_path}` 는 ★ 그 사이트의 `paths.detail` 로 편다 —
  ★ 볼보 `/kr/vehicles/volvo/{model}/{source_id}` 의 `{model}` 이 ★ 우리 `site_model` 이다
  ★ ★ 그러면 ★ `_source_url` 의 ★ **두 칸 틀**이 그대로 받는다 (09-02 에 이미 늘려 둔 자리)
★ K카는 ★ 여전히 `None` 이다 — ★ robots 가 막는다.  ★ 지어내지 않는다
```

### 조치 후 (배포 브라우저 실측)

```
리볼트      ★ `img` **29장** — https://prnd-car-purchase.s3…/media/cars/…
           ★ 링크 **https://www.revolt.kr/cars/Vym27On6**
볼보셀렉트   ★ `img` **61장** — https://selekt.volvocars.co.kr/picserver1/…
           ★ 링크 **https://selekt.volvocars.co.kr/kr/vehicles/volvo/s60/b5-ultimate-bright-m1bbhak**
빈 사진 상자 0 · 까닭 없음 0
DB          리볼트 사진 ★ **0 → 47/47** · 볼보 148/148
```

★ **바로잡을 것** — ★ 나는 앞서 ★ 「리볼트 `photo_list_json` 에 글자 `None` 이 들어 있다」고
읽었는데 ★ **내 출력 서식 탓**이었다 (`str(None)`). ★ 칸은 그냥 **비어 있었다** —
★ ★ 파서가 사진을 ★ **아예 안 읽고 있었다**.

## 4. 0번 — DB 를 줄인다 ★ **닫혔다**

| 무엇 | 지시문 | ★ 지금 |
|---|--:|--:|
| `raw_response` 행 | 372,571 | ★ **205,088** |
| ★ 몸통 있는 행 | (425 MB) | ★ **0** |
| DB | 1.08 GiB | ★ **0.479 GiB** |
| `tmp/` 적재 DB | — | ★ **0개** (판마다 치운다) |

```
★ 이번에도 ★ 몸통이 ★ **635행** 다시 자랐다 (카탈로그 받기·리볼트 넣기가 남긴 것) —
  ★ ★ 파일로 내보내고 지웠다 (★ 못 내보낸 행 0).  ★ VACUUM 은 0 MiB (이미 촘촘하다)
검산  `S46-265` ★ 통과
```

## 5. 2번 — 철학 ①② ★ **닫혔다**

```
철학 ①  `S46-266` ★ 통과 — ★ S5 가 `detail_status` 로 거른다 (`if resume:` 를 없앴다)
        ★ 이미 상세를 받은 것 ★ **7,123건** — ★ 지시문의 16,437 보다 적다.
        ★ ★ 4-4 로 ★ **407 8,948건을 `error` 로 돌렸기** 때문이다 —
        ★ ★ ★ `error` 는 ★ **다시 받을 것**이다.  ★ 줄어든 것이 맞다
철학 ②  `S46-267` ★ 통과
        ★ 엔카 `active` ＋ `CONTRACT` ★ **553 → 0**
        ★ 엔카 `gone` ★ **29 → 1,384** · 전체 `gone` 3,076
```

## 6. 5번 — S1 · S3 · S6

```
S3  ★ 닫혔다 — `target_key_of` 가 `trim_contains`·`year_from` 을 읽고
    ★ `target_map` 이 `targets.json` 의 `site_query` 도 읽는다.  `part` pending 0
S6  ★ 닫혔다 — ★ 열한 사이트를 적재 DB 로 넣었다.  ★ KB 말고 83.6~100%
S1  ★★ **막혔다** — ★ 40묶음 · 약 120회를 두드려 ★ **새 상세 0건** (앞 회차에 적었다)
    ★ 규격의 「5분이면 12/12」가 ★ 이제 안 통한다 — ★ 첫 요청부터 막힌다
```

## 7. 규칙 3 의 두 도구

```
python3.11 tools/run_tests.py
  실패 1파일 — test_endtoend.py
    FAIL  fatal 실패가 없다  ['V1-27', 'V4-28', 'V3-77', 'V11-34']
  ★ 이 회차 앞과 ★ **같다**
```

## 8. 여쭐 것

```
① ★ **BMW 화면이 67·170** 이다 — ★ 다른 화면(7·18)보다 훨씬 나쁘다.
   ★ 가이드께서 재신 24·73 과 달라 ★ **재는 곳을 맞춰야** 한다.
   ★ 내가 쓴 자를 그대로 쓰실 수 있다 —
     `python3 -c "from tools.browser_diff import hidden_text_report as r; \
      r('<base_url>', paths=('/listings?site=bmw_bps',), report='outputs/hidden_text_bmw.json')"`
② ★ 엔카 상세를 ★ 마스터 회선(`/admin/collect`)으로 채울지 — ★ 407 이라 ★ 「다」 대조가 안 된다
③ ★ KB — ★ 쉬는 참을 늘릴지 · 마스터 회선으로 옮길지
④ `check_all` 전체 — ★ 타이머를 세울 권한이 막혀 있다
```
