# 순서 10 — 부족액 표시 제거 · 쿼리 「그대로 복사」 · `V11-151`·`V11-152`·`V10-37`

`SPEC-2026.08.19-r409` · 2026-08-19 17:00 · v179

---

## 1. 무엇을 하려 했나 (지시 원문)

```
10  부족액 표시 제거 · 쿼리 결과 「그대로 복사」 단추 (탭 구분)
    V11-151 · V11-152 · V10-37
```

근거 `docs/chapters/41-view.md:278` (개정 400) · `inbox/ANSWER_q16.md` §1 (개정 401)

```
개정 400   ★ 1,500만은 「차값에 더하는 선납」이 아니라 총액 상한이다
          ★ 화면은 「전액 현금」인가 아닌가 둘뿐이다.  부족액을 내지 않는다
          ★ 1,500만은 일괄로 바꾸는 기준값 — config.finance.cash_limit 하나
금지       부족액 · 모자란 금액 · 「380만 부족」 같은 표시
검산       V11-151 부족액 문구가 화면에 있는가 (있으면 실패)
          V11-152 cash_limit 이 config 한 곳에서만 읽히는가

개정 401   필수 쿼리 탐색 결과에 「그대로 복사」 단추
          필수 ★ 붙여넣어 바로 쓸 수 있는 형태 — 탭 구분 (표에 붙는다)
          필수 내려받기(CSV)와 함께 둔다.  대신하지 않는다
검산       V10-37 결과 표 위에 복사 단추가 있는가
```

---

## 2. 1차 결과 (고치기 전 숫자)

```
check_all   통과 303 · fatal 2 · warn 7 · 환경 차이 1 · 미실행 5
run_tests   결과: 통과
V11-151 · V11-152 · V10-37   ★ 셋 다 없음

부족액      report/finance.py:114   shortfall = max(0, cost - down)
           report/views.py:73      shortfall_won
           report/exports/export.py:65  「★ 선납 부족 {…} — 부대비용이 선납금을 넘는다」
cash_limit  ★ 키 자체가 없음.  config/finance.json 은 down_payment_won
복사 단추   ★ 없음.  admin_query.html 에 내려받기도 복사도 없다
```

---

## 3. 무엇을 했나 · 왜 그렇게 했나

### ① 부족액을 계산에서도 화면에서도 뺐습니다

| 파일 · 줄 | 전 | 후 |
|---|---|---|
| `report/views.py:73` | `shortfall_won: int` | **칸을 없앰** |
| `report/finance.py:114` | `shortfall = max(0, cost - down)` | **없앰** |
| `report/exports/export.py:65` | 「★ 선납 부족 …」 두 줄 | **없앰** |
| `report/finance.py` 문서줄 | 「선납 부족 → 부족액 표시」 | 「★ 개정 400 — 부족액을 내지 않는다」 |

★ **계산 자체는 안 바뀝니다.** `vehicle_down = max(0, cash_limit − cost)` ·
`principal = 표시가 − vehicle_down` 그대로입니다. 없앤 것은 **내보이던 값**입니다.

### ② `cash_limit` — 읽는 자리를 하나로 (`report/finance.py:95`)

```python
def cash_limit(fin: dict) -> int:
    """현금 상한 (개정 400).  ★ config 를 읽는 자리는 여기 하나다."""
    return int(fin["cash_limit"])
```

부르던 두 곳(`build_finance` · `price_for_monthly`)이 이 함수를 씁니다.

### ③ ★ 규격 두 장이 이름을 달리 씁니다 — 한쪽을 안 지웠습니다

```
docs/chapters/41-view.md:287     config.finance.cash_limit        ← 개정 400
docs/chapters/40-report.md:432   down_payment_won · loan_months · …
docs/chapters/40-report.md:464   전액 현금  표시가 + 부대비용 ≤ down_payment_won
docs/chapters/40-report.md:467   선납 부족  부대비용 > down_payment_won   ★ 폐기된 것이 남아 있다
docs/chapters/00-standard.md:675 finance.json | down_payment_won | 선납금
docs/ref/B-config.md:115         "down_payment_won": 15000000
```

`cash_limit` 만 두었더니 **`S5 config 키 (V4-15)` 가 실패**했습니다 —
40-report 가 요구하는 키가 없어졌기 때문입니다.

```
한 것   config/finance.json 에 두 이름을 다 둔다.  값은 같다
       코드가 읽는 것은 cash_limit 하나다 (V11-152 통과)
       ★ 두 값이 갈리면 V11-152 가 잡는다 — 검사로 묶어 뒀다
왜     규칙 2.  규격이 서로 어긋나면 개발측이 한쪽을 지우지 않는다.  보고한다
```

### ④ 쿼리 결과 「그대로 복사」 (`web/templates/admin_query.html:9`)

```html
<p class="qtools">
  <button type="button" class="copy" data-copy="qtsv">그대로 복사</button>
  <span class="muted">탭 구분 — 표에 그대로 붙습니다</span>
</p>
<textarea id="qtsv" class="tsv" readonly rows="2" …>{{ r.tsv }}</textarea>
```

★ **클립보드가 막힌 환경에서도 쓸 수 있게 `textarea` 를 남겼습니다.**
단추가 안 먹으면 「아래 칸을 복사하십시오」로 바뀝니다.

### ⑤ `QueryResult.tsv` (`store/adminops.py:75`)

```
★ 탭 구분이다.  쉼표로 나누면 값 안의 쉼표에서 칸이 밀린다
★ 값 안의 탭·줄바꿈은 빈칸으로 바꾼다.  칸이 밀리는 것보다 낫다
NULL 은 빈 칸으로 낸다
```

### ⑥ 검사 셋

```
V11-151  부족액 문구가 화면에 없음        FATAL
        ★ 템플릿과 렌더 결과를 다 본다 — 템플릿에서 지우고 코드가 그리면 그대로다
        찾는 말   부족액 · 모자란 금액 · 「만 부족」 · 선납 부족
V11-152  cash_limit 을 한 곳에서만 읽음    FATAL
        ★ 옛 이름 down_payment_won 의 값이 갈리는지도 함께 잰다 (③)
V10-37   결과 표 위에 복사 단추가 있음     FATAL
        ★ `<h2>결과</h2>` 와 `<table>` 사이만 본다 — 「위에」가 규격이다
        ★ tsv 가 정말 탭 구분인지도 잰다
```

---

## 4. ★ 검사가 진짜로 잡는지 되살려 봤다

| 되살린 결함 | 결과 |
|---|---|
| 화면에 「380만 부족액」을 도로 넣었다 | **V11-151 실패** 「admin_query.html — 「부족액」가 있다」 |
| `cash_limit` 을 다른 파일에서도 읽게 했다 | **V11-152 실패** — 「report/finance.py — 1곳 / report/exports/export.py — 1곳」 |
| 옛 이름 값을 9 로 갈라놓았다 | **V11-152 실패** 「down_payment_won=9 이 cash_limit=15000000 과 다르다」 |
| 복사 단추를 「실행」으로 바꿨다 | **V10-37 실패** 「결과 표 위에 복사 단추가 없다」 |
| 되돌린 뒤 | 셋 다 통과 |

### ★ 첫 V10-37 은 거짓 통과했습니다

단추를 떼도 통과했습니다 — 안내 문구 「복사할 결과 (탭 구분)」에
「복사」라는 글자가 있었기 때문입니다.

```
고침   <button …>…복사 로 바꿨다.  ★ 누를 수 있는 것이어야 한다
```

**이것을 되살리기로 잡았습니다.** 안 해 봤으면 그대로 나갔을 검사입니다.

---

## 5. 조치 후 결과 — ★ 실제 출력

```
python3.11 tools/check_all.py

통과 306 · fatal 2 · warn 7 · 환경 차이 1 · 미실행 5

■ FATAL — 고쳐야 한다
  V1-11     예외로 종료된 실행이 없음 — 0단계
  V4-11     unclassified 존재 — 32
```

```
python3.11 tools/run_tests.py
결과: 통과

python3.11 tools/check_src.py
결과: 실패 — S13(20) / S16(64) / S25(1) / S26(21)
```

```
sudo systemctl restart carwatch   ★ 재시작 전 최근 15분 POST 0건 — 마스터 작업 중 아님
curl https://43.201.16.78.sslip.io/listings   200
```

| | 전 | 후 |
|---|--:|--:|
| check_all 통과 | 303 | **306** |
| fatal | 2 | **2** |
| run_tests | 통과 | **통과** |
| S16 남은 검사 | 67 | **64** |
| 부족액 문구 | 코드·리포트에 있음 | **0** |
| `cash_limit` 읽는 자리 | — | **1곳** |
| 복사 단추 | 없음 | **있음 (탭 구분)** |

---

## 6. 고친 파일

```
config/finance.json           cash_limit 신설 · down_payment_won 은 옛 이름으로 남김
report/finance.py             cash_limit() 신설 · shortfall 제거
report/views.py               FinanceView.shortfall_won 제거
report/exports/export.py      「선납 부족」 줄 제거
store/adminops.py             QueryResult.tsv · query_id
web/templates/admin_query.html  「그대로 복사」 단추 · textarea · script
web/static/app.css            .qtools · .copy · textarea.tsv  (★ 토큰만 쓴다)
validate/v11_web.py           V11-151 · V11-152 신설 · SHORTFALL_WORDS · RENDER
validate/v10_admin.py         V10-37 신설
tests/test_report.py          「선납 부족」 시험을 「부족액 칸이 없다」로
docs/CHECKS.md 외             build_index.py 가 다시 씀
```

---

## 7. 못 한 것 · 막힌 것 · 여쭐 것

### ★ 못 한 것 ①  「내려받기」를 못 넣었습니다 — 라우팅 표가 필요합니다

개정 401 은 「내려받기(CSV)와 함께 둔다. 대신하지 않는다」라 합니다.
**지금 쿼리 화면에는 내려받기가 아예 없습니다** (없앤 것이 아니라 원래 없었습니다).

```
넣으려면   GET /admin/query.tsv 같은 경로가 하나 필요합니다
막힌 것    라우팅 표는 docs/chapters/61-web.md 에 있고 V11-12 가 그것과 대조합니다.
          ★ 제가 표에 줄을 넣을 수 없습니다 (규칙 2) —
            넣으면 「규격에 없는 경로」로 V11-12 가 fatal 을 냅니다
드린 것    복사 단추는 넣었습니다.  ★ 「대신하지 않는다」를 어긴 상태입니다
청함       라우팅 표에 한 줄을 넣어 주십시오 — 그날 바로 붙이겠습니다
          예)  /admin/query.tsv  GET  view_query_tsv  admin  —  구현
```

### 여쭐 것 ②  규격 두 장이 어긋납니다 (3절 ③)

```
41-view.md  (개정 400)   cash_limit — 총액 상한 · 부족액 폐기
40-report.md :464 · :467  down_payment_won · 「선납 부족」이 그대로 있다
00-standard.md :675       down_payment_won | 선납금 (취득 부대비용 포함)
docs/ref/B-config.md :115 "down_payment_won": 15000000
```

★ **40-report 의 「선납 부족」 절은 개정 400 이 폐기한 것입니다.**
그 장이 안 고쳐져 있어 개발측이 지우지 않았습니다. 정정해 주십시오.

### 여쭐 것 ③  `V11-151` 은 낱말로 잡습니다

「부족액 · 모자란 금액 · 만 부족 · 선납 부족」 넷을 봅니다.
**다른 말로 쓰면 못 잡습니다** (예 「1,740만이 더 필요합니다」).
40-report.md:704 에는 아직 「넘으면 부족액을 낸다 — 「1,740만원 부족」」이
살아 있습니다 — 그것도 폐기 대상인지 확인이 필요합니다.

### 못 한 것 (그 밖)

없습니다. **다음은 순서 11** — 검사 1차 10개입니다. ★ 따로 커밋합니다.

### 그대로 남은 것

```
check_all fatal 2   V1-11 (다음 전체 실행이 끝나면 사라진다) · V4-11 (가이드 몫)
check_src           S13(20) · S16(64) · S26(21)
```

---

## 8. 커밋

```
V11-151·V11-152·V10-37: 부족액을 없애고 쿼리 결과 복사 단추를 넣었다 (순서 10)
해시  5fcb59f
```
