# 전 차종 완주 — 판정 240건 → 3,471건

작업 시각 2026-08-16 17:34~18:32 (약 58분) · 소스 v134 · 실행 `python3.11`
커밋 `de2ea20`(재개) · `e3e6063`·`35c4ef1`(개정 273) · `0a41bc8`(WAL) · `<이번 커밋>`

---

## 1. 무엇을 하려 했나 (지시 원문)

> ```
> 1  중단·재개를 먼저 확인한다.  실제로 중단해 보고 재개해 보십시오
> 2  전 차종 S5 → S6 → S9 → S10 + S7
> 3  끝나면 숫자 — 판정 · 등급 분포 · excluded 사유별
>    ★ 카탈로그를 늘린 뒤 spec 축 excluded 가 얼마나 줄었는가
>    사전 미분류 · 등록부 미분류 · 매핑 실패 · check_all fatal
>    raw_response · core_listing · result_score 행 수
> 4  /listings 에 3,000건 넘게 뜨는지 · /why 에 근거가 나오는지
> ```

## 2. 무엇을 했나

### ① 중단·재개 — 확인했더니 **안 되고 있었다** (커밋 `de2ea20`)

```
S5 를 100/948 에서 Ctrl+C
재개점 기록됨      ResumePoint(step='S5', source_id='41804825', endpoint='inspection')
다시 돌림          39554653 (첫 매물) 부터 시작   ★
```

`resume_point` 는 기록만 되고 아무도 쓰지 않았다. **13,888 호출짜리가 중간에 끊기면
전부 다시 던지는 상태였다.**

`--resume` 을 명시했을 때만 `should_refetch` 를 본다 —
`ok`·`empty`·`not_found` 는 이미 답을 받은 것이라 안 던지고 `expected` 에서도 뺀다.
**기본은 그대로 전건 재요청이다** (skip_done 을 기본으로 두면 v1 처럼 208건 중 76건만 받는다).

실측: `--resume` 재실행 → `expected 0 · 정상 종료`.

### ② 전 차종 실행

`python3.11 run.py collect --from S5 --resume` · tmux `carwatch:collect`

### ③ 도중에 개정 273 을 반영했다 (수집을 멈추지 않고)

마스터 실측 — 터미널은 `S5 2,052/13,888` 인데 화면은 「도는 것이 없습니다」였다.
`status_view` 가 `recalc_job` 만 봐서 큐를 안 거친 실행을 「할 일 없음」으로 단정했다.
셋을 보게 고치고(큐 · 최근 창의 요청 수 · 마지막 처리 시각) 진행 숫자를 화면에 냈다.
`V11-53` 구현.

곁다리로 `carwatch.db-shm`·`-wal` 이 추적되던 것도 뺐다 — `.gitignore` 의
`carwatch.db.*` 가 점이라 붙임표를 못 걸러 **커밋마다 4MB 가 따라붙고 있었다.**

## 3. 왜 그렇게 했나 (규격)

- 5장 STEP 52 — 재개점. 「중간에 끊기면 처음부터 다시 하지 않는다」
- 2장 STEP 25 · S5 금지 조항 — skip_done 류 플래그를 기본으로 두지 않는다
- 개정 273 · `STEP 136f` — 「큐만 보지 않는다. 실제로 도는 것을 본다」 · 검산 `V11-53`
- STEP 21b — 진단은 `encarDiagnosis == 0` 인 매물만 부른다 (expected 상한과 실제의 차)

## 4. 1차 결과 (작업 전 숫자)

```
판정             240건 (KOLEOS_HEV 만)
등급             C 90 · B 63 · E 35 · NOT_RATED 30 · D 20
raw_response   1,794 · core_listing 7,661 · result_score 238
카탈로그          21건
excluded        1,017/4,046 (25%)
  spec.hda      225/238 (95%)  ← catalog_missing
  spec.hud 등    각 30/238 (13%)
사전 미분류 18종 · 등록부 미분류 91건 · 매핑 실패 0
check_all      통과 200 · fatal 4
```

## 5. 조치 후 결과

### 단계 보고 — S5~S10 전부 정상

```
단계        expected requested   ok empty  n/f error 거부  상태
S5            10156     10156 7567     0 2589     0    0  정상
S6             8544      8544 8544     0    0     0    0  정상
S7              126       126  104    21    1     0    0  정상
S8             1474      1474 1474     0    0     0    0  정상
S9            58990     58990 58990     0    0     0    0  정상
S10            3470      3470 3470     0    0     0    0  정상
S11             215       215  206     0    0     9    0  중단
```

**1만 요청에 `error 0 · 거부 0`.** `not_found` 2,589 는 점검·이력이 없는 매물이다.

### 판정 · 등급

| 등급 | 전 | 후 |
|---|--:|--:|
| **S** | 0 | **7** |
| **A** | 0 | **31** |
| B | 63 | 416 |
| C | 90 | 655 |
| D | 20 | 1,602 |
| E | 35 | 654 |
| NOT_RATED | 30 | 106 |
| **합** | **238** | **3,471** |

**표본 240건에서는 S·A 가 한 건도 없었다.** 3,471건에서 38건이 드러났다.

### 표 행 수

```
raw_response  1,794 → 12,209
core_listing  7,661 → 7,661   (목록은 이미 다 펼쳐져 있었다)
result_score    238 → 3,471
result_axis   4,046 → 59,007
```

원문(ok): `detail 3,648 · record 2,379 · inspection 2,034 · diagnosis 486 · list 393 · catalog 125`

### ★ 카탈로그 효과 — 이번의 핵심

카탈로그 **21건 → 147건** (ok 125).

| 축 | 전 | 후 |
|---|--:|--:|
| `spec.hda` | **95%** excluded | **24%** |
| `spec.hud` · `bsd` · `scc` · `svm` · `sunroof` | 각 **13%** | 각 **1%** |
| `spec.tinting` | 95% | 95% (`no_mention` — 원문에 언급 없음) |

**`spec.hda` 가 95%→24%.** 나머지 사양 축은 사실상 전건 판정된다.
`tinting` 만 그대로인데, 그건 카탈로그 문제가 아니라 원문이 말을 안 하는 것이라
「모른다」로 두는 것이 맞다 (A-4).

### `excluded` 사유별

```
전  1,017/4,046 (25%)        후  8,311/59,007 (14%)

no_mention                3,328   받았는데 언급이 없다 (틴팅)
missing                   3,192   원문을 못 받았다 (점검·이력 404)
catalog_missing             802   카탈로그가 아직 없는 모델
product_absent              769   그 상품이 없다
coefficient_out_of_range    177   계수 구간 밖
expected_unavailable         43   원래 없는 것이 정상
```

### 미분류 · 실패

| 항목 | 전 | 후 |
|---|--:|--:|
| **매핑 실패** | 0 | **0** |
| **형식 거부** | 0 | **0** |
| 사전 미분류 | 18종 | 35종 |
| 등록부 미분류 | 91건 | 110건 |

**1만 요청 · 8,544건 파싱에 매핑 실패 0건.**

### `check_all`

```
통과 200 → 203 · fatal 4 · warn 5

V1-16  이번 run_id 밖의 행을 보지 않음 — 1
V2-18  parse_rule 재처리 후 전 봉투가 현재 parse_version — 30
V4-11  unclassified 존재 — 32
V4-20  dict_option_code 에 문장(공백·한글)이 없음 — 176  ← 카탈로그가 늘며 커졌다
```

### 화면

```
/ 200 · /listings 200 (한 쪽 200건) · /market · /dealers · /notready 200
```

## 6. 못 한 것 · 막힌 것 · 판단이 필요한 것

### ① 남은 fatal 4건 — `V4-20` 이 카탈로그와 함께 커졌다

```
V4-20  dict_option_code 에 문장(공백·한글)이 없음 — 10 → 176
```

카탈로그를 7배 받았더니 옵션 코드 사전에 **문장이 섞여 들어온 것이 176건**이다.
`option_name` 자리에 코드가 아니라 설명문이 들어간 것으로 보인다.
**이것부터 잡는 것이 맞다** — 사양 축 판정의 근거가 그 사전이다.

### ② `V1-16` · `V2-18` 은 재처리 경계 문제다

`--from S5 --resume` 으로 돌려 옛 봉투가 섞였다. 규격상 무엇이 맞는지 확인이 필요하다.

### ③ 새 config 키 5개가 부록 B 미등재다

```
browser_collect_rows · browser_interval_sec · worker_poll_sec
status_poll_sec · progress_refresh_sec · live_window_sec
```

규칙 2 대로 `docs/` 를 안 고쳤다. **등재를 부탁한다.**

### ④ `/market` · `/dealers` 가 매물 링크 0

화면은 200 이나 `/why/` 링크가 안 보인다. 3,471건이 들어온 지금
「의미 있는 수치를 내는지」(검토 11 5절) 확인이 필요하다. 다음 작업 후보다.

### ⑤ 내 감시 도구의 오탐

`pgrep -f "run.py collect"` 가 **자기 자신의 명령줄**을 잡아 「도는 중」으로 보였다.
`/admin/status` 는 DB 활동으로 판정하므로 같은 함정이 없다 — 화면 쪽은 무사하다.
