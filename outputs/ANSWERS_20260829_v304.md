# 개발측에 드리는 답 — 「마스터」로 적힌 여섯 (v303·v304)

```
version  SPEC-2026.09.02-r1082
follows  outputs/ORDER_20260829.md · `docs/ARCHITECTURE_20260830.md`
sources  개발 회차 v303·v304 · 마스터 확정 08-29
```

★ **여섯 다 「마스터」로 적혀 기다리고 있었다.**
★ ★ 넷은 **내가 이미 답한 것**인데 **명령서에만 있어 안 닿았다** — ★ **내 잘못이다** (오판 198 무늬).
★ ★ 이제 **한 장에 모았다.**  ★ 회차 표의 「누가 답해야 하나」에서 **지워라.**

---

| # | 개발측이 적은 것 | ★ 답 | 어디에 |
|:--:|---|---|---|
| 1 | `outputs/check-tmp` 12GB — 지워도 됩니까 | ★ **지워라.** 검사 임시물이라 P3 에도 「구조를 손대지 마라」에도 안 걸린다. `_sweep_temp` 이 그 자리도 치우게 고쳐라 | 명령서 0순위 |
| 2 | `test_web` — `sold.html` 이 없다 | ★ **만들어라.** 라우팅 표에 **`/sold` · `view_sold` 를 넣었다**(`61-web.md`). 「구조를 손대지 마라」는 **수집·적재**를 가리킨 말이고 **화면은 아니다** — 내 말이 성겼다 | `UI_REVIEW` 30장 · 시안 |
| 3 | 신차가를 ③(우리 표)으로 끌어올까요 (볼보 149·보배 227·BMW 8) | ★★ **08-29 정정 — 현대 인증은 원문에 있다** [실측 상세 표본 8건 전건 · 「신차 가격 대비 26,200,000」처럼 **매물마다 다르다**]. 그러니 **③ 으로 끌어올 표를 현대 인증에서 만들 수 있다**. 볼보·보배·BMW 는 그대로 ★ **끌어와라.** 마스터 확정 08-29 — 「**다른 사이트에 신차가로 대체가 가능하잖아**」. 열쇠 넷(제조사·모델·트림·연식) · **매칭 안 되면 0점＋미확인** · 화면에 밝힌다 | `f-table` 갈래 ③ · `02_미확정` 38 |
| 4 | 볼보 빈 15건 — `target_key` 가 없어 `gone` 이 안 된다 | ★ **`sweep_gone_groups` 를 고쳐라.** `target_key` 가 **없는 행도** 목록에 없으면 `gone` 이다. 지금 꼴이면 **값이 없는 행은 영영 안 죽는다** — 오판 161 과 같은 자리 | 아래 ★ |
| 5 | `target_key` 를 누가 붙이나 | ★ **수집기가 붙인다.** `target_by_rules` 를 `hyundai_cert`·`kia_cpo` 와 **같은 함수·같은 인자**로 | `TARGET_KEY_MAP` 08-29 절 |
| 6 | 조각 여섯 속 글을 어디에 | ★ **`config/screen_intro.json` 으로 옮긴다**(①안). **한 글자도 고치지 말고 옮기기만** | `UI_REVIEW` 31장 |

---

## ★ #4 를 더 적는다 — **마스터 몫이 아니다**

```
★ 개발측 — 「15건이 다 `status='new'` · `target_key` 없음 · 값·주행·연식 전부 `None`.
   ★ `sweep_gone_groups` 가 `target_key` 있는 행만 보므로 ★ 영영 `gone` 이 안 됩니다」

★ ★ **옳게 짚었다.  ★ 그리고 이것은 규칙 문제가 아니라 결함이다**
   ★ 「목록에 없으면 죽는다」는 ★ **차종을 아는 것과 상관없다**
   ★ ★ `target_key` 가 없는 행은 ★ 「우리가 이름을 못 붙인 것」이지 ★ 「없는 매물」이 아니다
★ 고치는 법 — ★ `sweep_gone_groups` 가 ★ **그 사이트의 이번 회차 목록에 없는 모든 행**을 본다
   ★ 필수 ★ 「끝까지 받았나」 가드는 ★ **그대로 둔다** (오판 161)
★ ★ 값이 없는 행은 ★ 이름을 못 붙이는 것이 맞다 — ★ **죽이는 것과는 다른 이야기다**
```

---

## ★★ 개발측이 잘한 것 — 그대로 적는다

```
★ 볼보 신차가를 ★ 「사이트가 안 준다」로 규격에 안 적고 ★ **「잰 것만 적었다」** (오판 189 를 지켰다)
★ 「값이 없는 행은 이름도 못 붙습니다」를 ★ **지어 고치지 않고 물었다**
★ 「한 회차가 20~30분이라 타이머에 맡겼다」 — ★ **몫을 넘긴 것이 아니라 크기를 적었다**
★ ★ 다만 ★ 볼보 신차가에 ★ **증거 파일**을 남겨 달라 — `docs/evidence/` (`S46-161`)
```

---

## ★★★★ 08-29 덧붙임 — ★ **현대 인증이 신차가를 준다** (가이드가 두드렸다)

```
★ 앞서 `HEYDEALER_API:349` 가 ★ 「기아 CPO · 현대 인증 · KB · K카 ★ 넷 다 안 준다」고 적었다.  ★ **틀렸다**
★ 실측 08-29 — ★ 현대 인증 상세 ★ **표본 8건 전건**에 ★ 「신차 가격 대비 {값}」이 있고 ★ **값이 서로 다르다**
   26,200,000 · 28,600,000 · 38,300,000 · 20,600,000 · 24,300,000 …
★ ★ **안내문이 아니다** (오판 192 잣대로 확인했다)
★ ★ K카는 ★ **안 준다** [상세 표본 8건 · `newCar*` 칸 0/8]
★ ★ 기아 CPO·KB 는 ★ **아직 안 두드렸다** — ★ 「안 준다」로 적지 않았다

★ 할 것 — ① `parse/hyundai_cert` 가 ★ `price_origin_won` 을 ★ 뽑는다
          ② ★ 그 값이 ★ ③(우리 표)의 ★ **밑감**이 된다 — ★ 볼보·보배·BMW 를 그것으로 채운다
★ 낱말 — `(신차\s*가격|신차\s*가|출고\s*가)[^0-9]{0,12}([\d,]{4,})`
```
