# 순서 9-2 — `check_all` fatal 6 이 무엇인가 · 넷을 고쳤다

2026-08-18 22:30 · v161 · **SPEC-2026.08.18-r365**

`inbox/ORDER_00_순서.md` **9. 남은 것 — `check_all` fatal 6 — 무엇인지**

---

## 1. 무엇을 하려 했나 (지시 원문)

```
check_all fatal 6 — 무엇인지
```

---

## 2. 1차 결과 (고치기 전 숫자)

```
python3.11 tools/check_all.py
통과 274 · fatal 6 · warn 7 · 환경 차이 1 · 미실행 2

  V1-16     이번 run_id 밖의 행을 보지 않음 — 2
  V2-01     ok 원문 수 == CORE 행 수 — 3470
  V2-18     parse_rule 재처리 후 전 봉투가 현재 parse_version — 30
  V4-11     unclassified 존재 — 32
  V3-38     facet 수신 후 목록 관측분과 대조함 — 남은 목록 관측 41
  V11-77    시안의 시각 요소가 렌더 결과에 나옴 — 6/7
```

---

## 3. ★★★ 여섯 중 넷이 「검사가 틀린 것」이었습니다

**데이터에는 아무 문제가 없었습니다.**  검사가 엉뚱한 것을 보고 있었습니다.

| 검사 | 무엇을 봤나 | 봐야 했던 것 |
|---|---|---|
| V1-16 | 조회에 `run_id` 가 있는가 | **누적 검사인가 아닌가** |
| V2-01 | 지금까지 한 번이라도 ok | **매물마다 마지막 봉투** |
| V2-18 | 전 행이 같은 판인가 | **펼칠 본문이 있는 행만** |
| V3-38 | `source_endpoint` 라벨 | **값이 facet 에 있는가** |

★ 넷 다 **「없는 결함을 있다고」** 한 것입니다.
놓친 것보다 나쁩니다 — 고칠 것이 없는데 여덟 달을 빨간불로 서 있었습니다.

---

## 4. `V1-16` — 누적 검사에 「이번 실행」 규칙을 걸고 있었습니다

### 무엇이 걸렸나

```
v1_collect.py: raw_response 에 run_id 조건이 없다   ← V1-25 · V1-21
```

```
V1-25  ok 로 저장된 원문이 온전한가 (조각 55개 사고 · 개정 307)
V1-21  받아 두고 안 펼쳐진 봉투가 없는가 (개정 268)
```

★ **둘 다 어제 것에서 찾아야 하는 검사입니다.**
조각으로 저장된 원문은 실행이 끝나도 DB 에 남습니다.
`run_id` 를 걸면 「목록을 안 받은 실행」에서 늘 통과가 됩니다.

### 규격이 요구한 것

`docs/chapters/20-verify/b-v1v2.md:74` 「★ `V1-16` 의 대상」

```
필수   검사마다 「이번 실행분인가 누적인가」를 Check 에 적는다
금지   전 검사에 run_id 를 일괄로 거는 것.  누적 검사가 무의미해진다
```

**적을 자리가 없었습니다.**  `Check` 에 그 칸을 만들었습니다.

```
validate/base.py:71    cumulative: bool = False   ← 기본은 「이번 실행분」
validate/v1_collect.py V1-21 · V1-25 에 cumulative=True + 왜 그런지
```

### ★ 겸사겸사 — 검사가 인자 이름에 속고 있었습니다

`run_id` 를 **함수 전체**에서 찾고 있었습니다.
`def _foo(conn, run_id)` 처럼 인자 이름만 있어도 통과였습니다.

```
고침   붙어 있는 문자열 리터럴을 tokenize 로 묶어 그 조회문만 본다
      (_sql_groups)
```

### 되살려 확인했습니다

| 되살린 결함 | V1-16 |
|---|---|
| ① `cumulative=True` 를 지움 | 실패 — `raw_response 에 run_id 조건이 없다 (V1-25)` |
| ② 진짜 「이번 실행」 검사에서 `WHERE run_id = ?` 를 지움 | 실패 — `… (V1-14)` |
| 되돌린 뒤 | 통과 |

★ ②는 **고치기 전에는 안 걸리던 것**입니다.  검사가 그만큼 촘촘해졌습니다.

---

## 5. `V2-01` — 누적 원문과 현재 상태를 견주고 있었습니다

```
ok raw detail distinct listing_id  3,494
core detail_status='ok'            3,470
차이                                  24
```

24건을 하나씩 봤습니다.

```
매물 11   08-16T08:32 detail ok    →   08-17T03:08 detail not_found
매물 34   08-16T07:30 detail ok    →   08-17T03:09 detail not_found
…
★ 24건 전부 마지막 호출이 not_found 다
```

**팔려서 내려간 차입니다.**  `detail_status='not_found'` 가 맞는 값이고
08-16 의 ok 원문도 버리면 안 되는 사실입니다 (P3 무손실).

```
고침   매물마다 마지막 detail 봉투로 견준다 (ROW_NUMBER OVER PARTITION BY)
더함   ok 로 받았는데 core_listing 에 행이 아예 없는 매물 — 0건
      ★ v1 의 사고가 이 모양이었다.  그것을 따로 센다
결과   3,470 == 3,470
```

---

## 6. `V2-18` — 「옛 판」과 「펼친 적 없음」을 섞고 있었습니다

```
parse_version 'p1'   7,779행
parse_version ''        30행   ← 이 30 이 fatal 이었다
```

30건을 봤습니다.

```
전건   status='active' · detail_status='not_found'
      ok 인 detail 봉투가 있는 것  0건
      detail 봉투가 아예 없는 것   0건
```

**detail 이 404 라 펼칠 본문이 없는 행입니다.**  비어 있는 것이 맞는 값입니다.

```
고침   parse_version <> '' 인 행 중 판이 다른 것  → 진짜 「옛 판이 남음」
      parse_version = '' 인데 detail_status='ok'  → 진짜 결함 (0건)
      parse_version = '' 이고 본문이 없음         → actual 에 적기만 한다
```

### ★ 덤 — `MAX(parse_version)` 도 고쳤습니다

```
전   cur = SELECT MAX(parse_version) FROM core_listing
```

`store/core.py:725` 가 **이미 겪고 적어 둔 함정**입니다.

```
「'c10' < 'c9' 도 같은 함정이다.  현재 판은 「가장 최근에 쓰인 것」이다」
```

같은 파일에 경고가 적혀 있는데 검사는 그 함정에 그대로 빠져 있었습니다.
`p10` 이 나오는 날 조용히 전건 실패가 됩니다.  같은 idiom 으로 바꿨습니다.

---

## 7. `V3-38` — 라벨을 보고 「facet 에 없다」고 했습니다

```
전   SELECT axis, COUNT(*) FROM dict_enum
     WHERE axis IN (fuel,color_ext,color_int,sell_type)
       AND source_endpoint='list'      →  41건
     → 「목록에만 있는 값 41건 — facet 에 없는 값이 왜 매물에 있나」
```

★ `source_endpoint` 는 **「어디서 처음 봤나」**지 「facet 에 있나」가 아닙니다.
facet 이 늦게 오면 이미 목록에서 본 값은 `'list'` 로 남습니다.

**값으로 견줘 봤습니다.**

```
color_ext  facet 30 · 사전 30 · facet 에 없는 값 0
color_int  facet 10 · 사전 10 · facet 에 없는 값 0
fuel       facet 12 · 사전 12 · facet 에 없는 값 0
sell_type  facet  3 · 사전  3 · facet 에 없는 값 0
```

**대조는 이미 완결돼 있었습니다.**  41건은 결함이 아니라 이력이었습니다.

```
더함   tools/build_dict.py  facet_value_set(conn, axis)
      사이트가 인정하는 값의 전체 집합을 raw_facet 에서 뽑는다
고침   validate/v3_logic.py  양쪽을 다 본다 (12-dict 규격 그대로)
        facet 에만 있는 값 → 사전에 안 들어왔으면 결함
        목록에만 있는 값   → 확인 필요
```

되살려 확인했습니다.

| 되살린 결함 | V3-38 |
|---|---|
| 사전에 `수소전기(되살림)` 을 넣음 | 실패 — `facet 에 없는 값 1건 … 왜 매물에 있나` |
| 사전에서 facet 값 `수소` 를 지움 | 실패 — `facet 이 준 값 1건이 사전에 안 들어왔다` |

---

## 8. ★★ `V11-77` — 미리보기에 사진이 영영 안 떴습니다 (진짜 결함)

```
/listings — .thumb 가 화면에 없다 (표로 내고 있다)
```

이건 검사가 맞았습니다.

```html
web/templates/listings.html:57
<img src="{{ r.photo_url }}" alt="" loading="lazy">      ← class 가 없다
```

```js
web/templates/listings.html:162
var pic = tr.querySelector('img.thumb');
if (pic) { … } else { img.removeAttribute('src'); img.style.display='none'; }
```

★ **JS 가 `img.thumb` 을 찾는데 그런 것이 없습니다.**
행에 손을 대면 미리보기가 뜨는데 **사진 자리는 늘 비어 있었습니다.**
`b-list.md:39` 「행에 손대면 미리보기 — **사진** · 주요 옵션 · 강점 3개」

`61-web.md:481` 클래스 표가 `thumb` · `thumb-none` 을 사진의 정본 이름으로
정해 뒀습니다.  **템플릿이 그것을 안 지킨 것입니다.**

```
고침   listings.html · recommend.html 의 <img> 에 class="thumb"
```

### 덤 — 씨앗 DB 에서만 나던 실패도 갈랐습니다

`StubEncar` 는 `"Photos": []` 를 줍니다.  씨앗에는 사진이 아예 없어
목록이 전건 `.thumb-none` 으로 나옵니다.

```
검사가 스스로 적어 둔 원칙
  「자료가 없어 못 그린 것과 구조가 없는 것은 다르다」
그런데 /listings 에는 그 갈래가 안 걸려 있었다
고침   SIAN_VISUAL 에 「그 자료가 없다」 표시(.thumb-none)를 더했다
```

---

## 9. ★★★ `V11-80` — 검사가 죽은 CSS 를 읽고 통과하고 있었습니다

**고치다가 새로 찾은 것입니다.  이것이 오늘 가장 나쁜 것입니다.**

```
V11-80  사진이 최소 크기 이상   →  통과 (128px)
```

읽고 있던 것

```css
app.css:1364
.thumb { width:128px; height:86px; … }     ← 목록 <img> 에 그 class 가 없었다
```

실제로 그려지던 것

```css
app.css:1595
.rows td.photo img { width:96px; height:72px; }    ← 이것이 이긴다
@media (max-width:1749px) { … width:88px }
@media (max-width: 639px) { … width:80px }
```

```
검사   「128px 이다」
화면   96px · 88px · 80px
```

★ **이 프로젝트가 막으려는 바로 그 「선언과 실제의 괴리」입니다.**
`V11-77` 을 고쳐 `class="thumb"` 를 붙이지 않았으면 영영 안 드러납니다.

```
고침   정본 표(61-web/a-common 사진 크기)를 읽어 최소치와 견준다
      CSS 가 그 표대로인지는 V11-107 이 본다 — 둘이 이어져야 성립한다
결과   ★ 이제 정직하게 실패한다 (10절)
```

---

## 10. 조치 후 결과 — ★ 실제 출력

```
python3.11 tools/check_all.py

DB carwatch.db · run 20260817T032131
통과 278 · fatal 2 · warn 7 · 환경 차이 1 · 미실행 2

■ FATAL — 고쳐야 한다
  V4-11     unclassified 존재 — 32
  V11-80    사진이 최소 크기 이상 — 관심 96/88/80px · 목록 96/88/80px
```

```
python3.11 tests/test_endtoend.py
결과: 실패 2 — 중단 없이 끝난다 / fatal 실패가 없다
★ 남은 둘은 위 fatal 2 를 그대로 비추는 것이다

python3.11 tools/run_tests.py
결과: 실패 — test_endtoend.py
  ruff  통과 (F821,F811,B905,F841,DTZ005,F401)
```

| | 전 | 후 |
|---|--:|--:|
| check_all 통과 | 274 | **278** |
| **fatal** | **6** | **2** |
| warn | 7 | 7 |
| test_endtoend 안의 실패 | 2 | 2 |

```
고친 것   V1-16 · V2-01 · V2-18 · V3-38 · V11-77
새로 드러난 것   V11-80  ★ 통과하고 있던 것이 거짓이었다
```

---

## 11. ★ 판단이 필요합니다 — `V11-80` 은 규격끼리 어긋납니다 (규칙 2)

**개발측이 정하지 않고 그대로 둡니다.**

```
R-206 (docs/trace/14-web.md:17)
  「사진 최소 128px — 좁아도 안 줄인다」  근거 [마스터] 개정 281
  마스터 지적 — 「차 이미지가 작다.  2배로 키워」
  ★ 상태가 이미 ◐ 다 — 가이드도 덜 됐다고 적어 뒀다

정본 (docs/chapters/61-web/a-common.md:17)
  | 목록 | 96×72 | 88×66 | 80×60 |
  | 관심 | 96×72 | 88×66 | 80×60 |

config/web.json  photo_min_px = 128
```

```
★ 개정 342 는 STEP 149o-3(최소 128px)의 「크기」 부분을 폐기라고 못박았다
  「이 파일의 STEP 149o-3 … 가운데 자리 · 크기 · 줄 수를 정한 부분은 폐기다
   지금 정본  chapters/61-web/a-common.md  폭 · 크기 · 줄 수」
★ 그런데 R-206 은 살아 있고 61-web.md:2149 의 검사 표에도 V11-80 이 남아 있다
```

### 셋 중 하나를 정해 주십시오

```
① a-common 이 맞다  →  photo_min_px 를 80 으로 내리고 R-206 을 닫는다
                      마스터의 「2배로 키워」는 64px→96px 로 이미 지켜진 셈이다
② R-206 이 맞다     →  a-common 표를 128/128/128 로 고쳐 주십시오
                      ★ 좁음(<640)에서 128px 이면 카드 6줄이 깨질 수 있습니다
③ 화면마다 다르다   →  V11-80 을 목록에만 걸고 관심은 뺀다
```

**제가 고르면 규칙 2 위반입니다.**  ①이 규격 이력상 자연스러워 보이지만
「2배로 키워」는 마스터 말이라 개발측이 닫을 수 없습니다.

---

## 12. 남은 fatal · 못 한 것

### `V4-11` — 미분류 32건은 마스터 몫입니다 (STEP 87)

```
자동 분류를 정본에 바로 넣지 않는다.  「제안」까지만 한다
python3.11 tools/classify_unclassified.py --suggest
  → config/field_usage.suggested.json
★ 사람이 확인한 뒤 config/field_usage.json 으로 옮깁니다
```

### warn 7 은 그대로입니다

```
V1-07 3067 · V4-06b 48 · V4-11b 317 · V4-21 1(put)
V3-39 13.2% · V3-03 2건 · V3-04 2건
```

---

## 13. 고친 파일

```
validate/base.py:71          Check.cumulative 신설
validate/v1_collect.py       V1-21 · V1-25 에 cumulative=True
                             _sql_groups() 신설 · _run_scope_check 다시 씀
validate/v2_load.py          V2-01 마지막 봉투 기준 · V2-18 갈래 셋으로
validate/v3_logic.py         V3-38 값으로 대조
validate/v11_web.py          V11-77 자료 없음 갈래 · V11-80 정본 표를 읽음
tools/build_dict.py          facet_value_set() 신설
web/templates/listings.html  <img class="thumb">
web/templates/recommend.html <img class="thumb">
docs/CHECKS.md · SOURCE.md   build_index.py 가 다시 만듦
```
