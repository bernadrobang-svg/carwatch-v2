# r515 단계 8 — 기아 인증중고차(CPO) 수집 · 1,020대

`SPEC-2026.08.22-r515` · `v204` · 2026-08-23 12:00

```
★ 명령서 3-1 — 「★ 개발측은 ★ 목록부터 붙인다」.  ★ 상세는 안 받았다
```

---

# 1. 붙인 것

| 파일 | 무엇 |
|---|---|
| `adapters/kia_cpo.py` ★ 신설 | URL · 헤더 · 커서 쪽넘김 · `endpoint_schema` |
| `parse/kia_cpo/mapping.py` ★ 신설 | 목록 원문 → CORE 필드 (KIA_CPO_API 2장) |
| `tools/collect_kia_cpo.py` ★ 신설 | 끝까지 받아 저장 (`--dry` 로 안 저장) |
| `config/sites.json` | `status` planned → **active** · `target_map` · `warranty_grades` · `warranty_items` · `purchase_cost` |
| `config/cli_screens.json` | pending 에 넣음 (화면은 가이드가 정한다) |

## ★ 쪽넘김 — 커서를 ★ 두 번 넘겨야 한다 (실측)

```
줄마다  cursors: ["22", "12921"]   (wishCount, id)
★ cursors=22,12921       → ★ 첫 쪽이 그대로 온다 (안 넘어간다)
★ cursors=22&cursors=12921 → ★ 넘어간다        ← ★ 이것이다
결과   12쪽에 ★ 1,020건 전부.  totalElements 와 같다
```

---

# 2. ★ 받은 것 — 숫자 (판정은 가이드 몫)

```
사이트가 말한 총   1,020건
받은 것            1,020건   ★ 어긋남 없음
저장               1,020건 · site='kia_cpo'

사이트별 매물   encar 8,478 · ★ kia_cpo 1,020
목록 화면      「전부 / 엔카만 / ★ 기아 인증중고차(CPO)만」 — ★ 배포 주소에서 확인
```

## ★★ 차종 — 우리 등록 차종과 겹치는 것이 ★ 0건이다

```
1,020건 ★ 전부 「차종 미정」이다
★ 버리지 않았다 — TARGET_KEY_MAP 6장 「mapped 가 빈 사이트값은 수집하되 버리지 않는다」
```

### 왜 0건인가 — ★ 대응표와 실측이 다르다

```
TARGET_KEY_MAP 2장   「스포티지 76대 → ★ SPORTAGE_LPI」
                    「스포티지는 연료가 갈라져 있지 않다 — 목록을 받아 연료로 걸러야 한다」

★ 걸렀다.  ★ 그런데 ★ LPI 가 ★ 한 대도 없다 —
   1.6 가솔린 하이브리드  43대
   2.0 디젤             20대
   1.6 가솔린            8대
   1.6 가솔린 터보        3대
   1.6 디젤              2대
                    합 76대 · ★ LPI 0대
```

```
★ 그래서 ★ SPORTAGE_LPI 로 걸린 것이 ★ 0건이다.  ★ 코드 잘못이 아니다
★ 대응표의 「우리 등록 차종 SPORTAGE_LPI」 줄은 ★ 실측과 다르다 — 지적한다
★ 겹침을 만들려면 ★ 스포티지 하이브리드/가솔린을 ★ 차종으로 등록해야 한다.
  ★ 그것은 마스터가 정하실 일이다.  ★ 개발측이 차종을 늘리지 않는다
```

---

# 3. ★★ 규격에 없던 것 — `classification` 이 ★ 넷이다

```
KIA_CPO_API 3장   「LITE · EXCLUSIVE · ★ PREMIUM ★ 셋이다」
실측 08-23        LITE 562 · ★ PREMIUM 333 · EXCLUSIVE 68 · ★★ ESSENTIAL 57
```

```
★ ESSENTIAL 이 ★ 규격에 없다.  ★ 단계를 안 정했다 — ★ 지어내지 않는다 (금지 6)
★ config/sites.json 의 _warranty_pending 에 적어 뒀다
★ KIA_CPO_API 5장 ② 가 「classification 단계는 ★ 가이드 몫」이라 했다 —
  ★ 넷째 값이 나왔으니 함께 정해 주시라
★ 지금은 kia_cpo 에 차종이 없어 ★ 채점을 안 한다.  ★ 점수에 영향이 없다
```

---

# 4. ★ 기아 반입이 검사 일곱을 깨뜨렸다 — 다 고쳤다

| 검사 | 무엇이었나 | 고침 |
|---|---|---|
| **V2-11** | ★ **번호판 원본 1,020건이 `plate_hash` 에 들어갔다** | ★ PII 다 (STEP 35). `_pii_plate_no` 로 넘겨 `split_pii` 가 해시한다. ★ 이미 넣은 1,020행을 지웠다 |
| **V4-24** | 축 함수가 `target_config[site_query]` 에서 매물 값을 읽었다 | 보증 기간표를 ★ 차종 키로 찾는다 (`by_maker` → `by_target`) |
| **V3-55** | `kia_cpo` 에 `warranty_items` 가 없다 | 넣었다 |
| **V11-120** | 사이트별 구매 총액이 없다 — 40건 | `purchase_cost` 를 넣었다 |
| **V11-72·V11-75** | `?site=kia_cpo&sell_type=` ★ 빈 조건 | 빈 칸은 주소에 안 붙인다 |
| **V11-45** | 새 CLI 에 화면이 없다 | pending 에 (가이드가 정한다) |
| `test_crosssite` | 「1차는 active 사이트가 1곳」 | ★ 사이트를 붙일 때마다 깨진다. ★ 그 시험 안에서만 한 곳으로 좁혀 잰다 |

```
★ V2-11 이 ★ 가장 무겁다 — ★ 번호판은 개인정보다.  ★ 검사가 잡아 줬다
```

---

# 5. ★ 명령서 2-3 도 함께 했다 (facet 과 무관하다)

```
config/scoring.json  axis_rules.warranty.maker_default 신설 —
  ★ 원문이 보증 개월을 안 줄 때만 쓰는 ★ 대체값이다.  ★ 원문이 있으면 원문이 이긴다
analyze/axis/site.py _maker_default() — ★ 차종 키로 찾는다

★ 값을 넣은 것은 ★ 볼보 5년/10만km 하나다 (명령서가 준 것)
★ 렉서스 · BMW · 벤츠 · 아우디는 ★ 값을 안 주셨다.  ★ 지어내지 않는다 (금지 6·12)
★ 그리고 ★ 그 차종들이 아직 등록돼 있지 않다 — 단계 7 이 407 로 막혔다
★ 차종이 들어오면 by_target 에 줄만 넣으면 된다.  ★ 코드는 이미 읽는다
```

---

# 6. 검사 — 실제 출력

```
check_all
  1차 (기아 반입 직후)  통과 336 · fatal ★ 10 · warn 10
  조치 후              통과 343 · fatal  3 · warn 10 · 환경 차이 1 · 미실행 10
      FATAL  V1-21 · V1-23 · V11-76 — ★ 셋 다 이번 작업 이전부터 있던 것

run_tests
  1차   결과: 실패 — test_crosssite.py, test_endtoend.py
  조치 후  결과: ★ 통과 (19파일 + ruff)

배포   deploy_check ○
      https://43.201.16.78.sslip.io/listings
      사이트 칸 ★ 「전부 · 엔카만 · 기아 인증중고차(CPO)만」
```

---

# 7. ★ 여쭐 것

```
1  ★★ classification ★ ESSENTIAL 57건 — 단계를 정해 주시라 (규격에 없다)
2  ★★ TARGET_KEY_MAP 「스포티지 → SPORTAGE_LPI」 — ★ LPI 가 0대다.
   ★ 스포티지 하이브리드/가솔린을 차종으로 등록할지 (마스터 몫)
3  ★ 상세(`/api/product/detail/{id}/`)를 언제 붙일지 —
   ★ 명령서가 「목록부터」라 해서 안 받았다.  ★ merchandising·warranties 가 거기 있다
4  ★ 제조사 보증 기간표 — 렉서스·BMW·벤츠·아우디 값 (명령서 2-3)
```

---

# 8. 커밋

```
931fb91
```
