# ★ SPEC-2026.08.21-r435 — 순서 1·2·3·4·11 · 「모름」을 source 로 가른다

2026-08-21 23:30 · v194

```
★ 오늘의 숫자
   화면이 「없음」으로 내던 15,709건 →  ★ 「? 모름」으로 고쳤다
   우리 잘못  4,973 → ★ 4,291건  (② 1,132 → 0 · ④ 신설 450)
   확인 안 됨 22,208 → ★ 20,197건 (−2,011)
   A 이상     67 → ★ 69건
★ 순서 11 은 못 닫았다 — ★ 그 실패가 순서 5 다.  8절 ① 에 근거를 적었다
```

---

## 1. 무엇을 하려 했나 (지시 원문)

> 1 개정 435 — 「확인 안 됨」의 정본은 `result_axis.source = 'missing'`.
>   ★ 화면이 value 로 가르는 것을 source 로 고쳐라. V3-95 · V3-96 신설
> 2 갈래를 넷으로 — ④ ★ 계산 쪽이 못 만든다. V1-27 을 넷으로 고쳐라
> 3 ★ 지금 고칠 것 둘 — `state.integrity` 675건 · `history.not_join` 7건.
>   ★ 고친 뒤 S9 부터 recalc → 등급 분포
> 4 ★ `tire_tread_mm` 은 죽은 필드다 … ★ 자리를 확인해 둬라
> 11 run_tests 실패 1건 — `test_endtoend.py` ★ 실패를 남긴 채 넘어가지 마라 (S38)

---

## 2. 1차 결과 (고치기 전 숫자)

```
check_all   통과 331 · fatal 4 · warn 10 · 환경 차이 1 · 미실행 10
run_tests   실패 1 — test_endtoend.py
화면        source='missing' 15,709건이 「· 없음」으로 나간다
갈래        셋 (①②③).  ④ 가 없다
확인 안 됨   22,208건 · 우리 잘못 4,973건
A 이상      67건
```

---

## 3. 순서 3 — 파서 둘을 고쳤다

### ① `state.integrity` — 계기판은 **원문에 있었다** (1,997건)

`analyze/axis/state.py:280` 이 **조건 없이** 「계기판 확인 못 함」을 달고 있었습니다.

```python
# 전 — 조건이 없다
# ★ 계기판 교환은 원문에 없다 — 그 2점은 확인 못 한 것이다
why.append("계기판 확인 못 함")
```

```
원문 800건을 열어 보니
  master.detail.mileageStateType   800건 전부 null      ★ 코드 말이 맞다
  master.detail.boardStateType     757/800 에 값이 있다  ★ 키를 잘못 봤다
     예)  {"code": "1", "title": "양호"}
```

**고친 것** — `carStateType` 과 **같은 모양**이라 같은 규칙을 씁니다.

```
contracts.py            board_state_ok 필드
parse/encar/mapping.py  inspection_board_state = boardStateType.title
sql/ddl/02_core.sql     core_inspection.inspection_board_state
store/core.py           board_state_ok = (그 값 == CAR_STATE_OK)
analyze/axis/state.py   ★ 실제로 본다 — 양호면 cluster 점을 준다
```

재처리는 **결정표대로** 돌렸습니다 — `parse_rule` → `S6 · S9 · S10`
(`collect/pipeline.py:106`). 임의로 단계를 고르지 않았습니다.

```
core_inspection.inspection_board_state  2,004 / 2,113 채워졌다 (「양호」)
result_axis                             integrity_계기판 확인 못 함 1,997 → 0
                                        ★ integrity_ok 1,997
```

★ 흠이 없을 때 `source` 가 `"integrity_"` 로 꼬리만 남아 읽을 수 없었습니다 —
`integrity_ok` 로 고쳤습니다.

### ② `history.not_join` — ★ 7건은 **② 가 아니었습니다.  제 도구가 틀렸습니다**

v193 에서 이 7건을 「② 못 읽는다」로 냈고 **가이드가 그대로 규격에
받았습니다** (개정 435 실측표 「② 675 + 7」). **틀렸습니다.**

```
제 도구가 본 키   openData        ← ★ 「보험이력이 열려 있는가」다.  늘 true 다
축이 실제로 쓰는 값 notJoinDate1~5  ← ★ 7건 전부 null 이다
→ 갈래는 ② 가 아니라 ① 이다.  ★ 개정 435 표의 「+7」은 빼야 합니다 (9절 ①)
```

**그런데 원문을 여니 다른 결함이 나왔습니다.**

```
매물 5702  openData=true · firstDate=2026-08-05 · notJoinDate1~5 전부 null
축         if s.not_join_months is None or not s.owned_months: → "missing"
실제       not_join_months = 0 (미가입 기간이 없다)
          owned_months    = 0 (2026-08-05 등록 · 오늘 08-21 — 보름 된 차다)
★ 「모른다」가 아니다.  ★ 「미가입 기간이 없다」다.  만점이어야 한다
```

**고침** — 미가입 기간이 0 이면 보유 기간과 무관하게 흠이 없습니다.

```
not_join_0%  1,074 → 1,081   (7건이 만점으로)
missing      1,361 → 1,354
```

### ③ recalc 뒤 등급 분포

| 등급 | 전 | **후** |
|:--:|--:|--:|
| S | 0 | **0** |
| **A** | 67 | **69** |
| B | 209 | **214** |
| C | 403 | **400** |
| D | 648 | **645** |
| E | 840 | **847** |
| F | 544 | **541** |
| G | 282 | **277** |
| 제외 | 776 | **776** |
| 등급 없음 | 72 | **72** |

★ A 이상 **67 → 69**. 합계 3,841 그대로입니다.

---

## 4. 순서 1 — 화면이 「모름」을 「없음」으로 내던 것

### 무엇이 틀렸나

`report/screens/build.py` 의 `chip()` 이 **`value` 로만** 가르고 있었습니다.
`source='missing'` 인 행은 `value=0 · excluded=0` 이라 「· 없음」이 됩니다.

★ 바로 아래 줄에 **「없음과 모름을 같은 기호로 내면 v1 사고가 되풀이된다」**
고 적혀 있었습니다.

### 고친 것

```python
# ★★ 개정 435 — 「확인 안 됨」은 value 가 아니라 source 에 있다
elif is_unknown(source):
    label, tone, bucket = vl["unknown"], TONE_UNKNOWN, "unknown"
```

★ `source` 이름을 코드에 안 박았습니다 (S14). `config/unknown_split.json` 이 정본입니다.

```
unknown_sources           missing · site_unavailable · origin_price_missing
                          · market_sample_short
unknown_source_marks      「확인 못 함」  ← 축이 사유를 이어 붙인 것을 잡는다
confirmed_absent_sources  no_choice_option · no_warranty · nothing_picked
                          ★ 「없다고 확인한 것」은 모름이 아니다.  먼저 가른다
```

**실제로 불러 확인했습니다**

```
missing                 모름=True     no_warranty       모름=False
site_unavailable        모름=True     nothing_picked    모름=False
market_sample_short     모름=True     no_choice_option  모름=False
integrity_계기판 확인 못 함  모름=True     integrity_ok      모름=False
```

### `V3-95` · `V3-96` 신설 — **되살려 확인했습니다 (S42-2)**

```
V3-95  화면이 source='missing' 을 「없음」으로 안 내는가        (FATAL)
      ★ DB 에 있는 source 종류를 전부 실제 chip() 에 넣어 본다 (S43-②)
      ★ 반대쪽도 본다 — 「없다고 확인한 것」이 모름으로 새는가
V3-96  value 로 센 것과 source 로 센 것의 차                  (WARN)
```

| 되살린 결함 | V3-95 |
|---|---|
| `chip()` 에서 `is_unknown` 갈래를 도로 뺌 | **실패** 「source='missing' (15,810건) 이 「-」로 나온다 — 「?」여야 한다」 · 「market_sample_short (450건)」 |
| 되돌린 뒤 | **통과** |

```
V3-96  value 로 세면 108건 · source 로 세면 20,197건 —  ★ 차 +20,089
       WARN 으로 늘 눈에 보이게 둔다
```

---

## 5. 순서 2 — 갈래를 넷으로

| 갈래 | 배점 | 전 (셋) | **후 (넷)** |
|:--:|---|--:|--:|
| ① 원문에 **값이 없다** | 딜러 | 17,235 | **15,906** |
| ② 파서가 못 읽는다 | ★ 우리 | 1,132 | **0** |
| ③ 엔드포인트를 안 받는다 | ★ 우리 | 3,841 | **3,841** |
| ④ ★ 계산 쪽이 못 만든다 | ★ 우리 | — | **450** |
| 모름 | | 0 | **0** |
| **확인 안 됨** | | **22,208** | **20,197** |
| **★ 우리 잘못** | | **4,973** | **★ 4,291** |

```
② 1,132 → 0   순서 3 으로 둘 다 고쳤다 (675 + 7 — 그중 7 은 애초에 ① 이었다)
④ 450        value.market — 이 매물의 원문에는 값이 있다.
             ★ 다른 매물이 표본 5건에 못 미쳐 우리가 중앙값을 못 만든다
             → 순서 7 「시세를 세 겹으로」가 푼다
★ 점수 상한   71,887점 (③ 26,887 · ④ 45,000)
```

### ★ `V1-28` 이 **갈래 신설을 「늘었다」로 잡았습니다** — 고쳤습니다

```
처음   ④ 계산이 못 만든다 0 → 450 (늘었다)     ★ 실패
실제   ② 에 있던 450건이 ④ 로 **옮겨간** 것이다.  우리 잘못은 줄었다
고침   ★ 갈래별이 아니라 **우리 잘못의 합**으로 본다.
      갈래별 움직임은 곁들여 적는다 (실패로 삼지 않는다)
후     합 4,973 → 4,291   ★ 통과
      곁들임: ② 1132 → 0 · ④ 0 → 450
```

**되살려 확인했습니다**

| 검사 | 되살린 결함 | 결과 |
|---|---|---|
| `V1-27` | 이력의 `state.frame` ② 를 99 로 | **실패** 「①②③④모름 합 1828 != 확인 안 됨 1729」 |
| `V1-28` | 지난번을 ② 5 · ③ 5 로 | **실패** 「★ 우리 잘못이 10 → 4291 로 늘었다」 |
| 둘 다 | 되돌린 뒤 | **통과** |

---

## 6. 순서 4 — `tire_tread_mm` 자리를 확인했습니다

```
contracts.py:208+        tire_tread_mm: float | None = None      ★ 필드가 있다
analyze/axis/state.py    tread = getattr(s, "tire_tread_mm", None)  ★ 축이 읽는다
★ 쓰는 곳                 없다.  어느 파서도 안 채운다
결과                     3,841건 전건 site_unavailable · 7점 축이 늘 0점
```

```
★ 리포트가 들어오면 채워질 자리 — 오늘 파서 둘을 고친 것과 **같은 길**이다
  ① parse/encar/mapping.py   parse_<리포트>() 에서 트레드를 꺼낸다
  ② sql/ddl/02_core.sql      core_<리포트>.tire_tread_mm
  ③ store/core.py            스냅샷 질의에 실어 tire_tread_mm 로 넘긴다
  ④ analyze/axis/state.py    ★ 고칠 것이 없다 — 이미 읽고 있다
  ⑤ collect/pipeline.py      parse_rule → S6 · S9 · S10 으로 재처리
★ 주소를 지어내지 않았습니다.  ★ unified_report 는 401 입니다
★ 오늘 boardStateType 을 ①~⑤ 그대로 밟아 넣었습니다 — 길이 뚫려 있음을 확인했습니다
```

---

## 7. 조치 후 결과 — ★ 실제 출력

```
python3.11 tools/check_all.py

통과 334 · fatal 3 · warn 11 · 환경 차이 1 · 미실행 9

■ FATAL
  V1-21     받아 두고 안 펼쳐진 원문이 없음 — 1
  V1-23     필요한 조합 대비 받은 카탈로그 비율 — 127조합 · 81.4%
  V11-60    시안 CSS 를 다시 만들지 않음 — 다름        ★ 8절 ① (순서 5)

■ warn (11)
  V1-07 3302 · V4-06 1 · V4-06b 48 · V4-11b 318 · V4-21 1
  V3-39 최대 13.2% · V3-03 2건 · V3-04 2건 · V3-85 40/3841
  V3-91     가이드 검산 일곱 줄이 표대로 나옴 — 6줄 다름     ★ 9절 ②
  V3-96     value NULL 108 · source 모름 20197 · 차 +20,089  ★ 신설

V1-27  통과  24축 · 확인 안 됨 20197 · ② 0 · ③ 3841 · ④ 450
V1-28  통과  합 4973 → 4291
V3-95  통과  0
```

```
python3.11 tools/run_tests.py
결과: 실패 — test_endtoend.py
      FAIL  fatal 실패가 없다  ['V11-60', 'STEP53-S11']
★ V11-60 하나만 남았다.  ★ 8절 ①
```

| | 전 | 후 |
|---|--:|--:|
| check_all 통과 | 331 | **334** |
| fatal | 4 | **3** |
| warn | 10 | **11** (V3-96 신설) |
| 시험 실패 | 1 | **1** |
| 「없음」으로 나가던 모름 | **15,709** | **0** |
| 확인 안 됨 | 22,208 | **20,197** |
| ★ 우리 잘못 | 4,973 | **4,291** |
| A 이상 | 67 | **69** |

★ `V11-76` 이 이번 실행에서는 **안 걸렸습니다** (전 4곳 · 2곳 · 이번 0곳).
코드는 안 바뀌었습니다 — 시간을 재는 검사라 그렇습니다 (v192 6절 ④).

---

## 8. ★ 순서 11 을 못 닫았습니다 — 근거

### ① `V11-60` 이 가리키는 다섯은 **순서 5 의 화면 요소**입니다

```
V11-60 다름 — v2_시안_목록상세.html
   .mono · .cond · .chips · .chip · .bar
```

★ 이 다섯은 **시안의 새 화면**이 쓰는 것입니다 — `.bar` 는 「막대 넷」,
`.chip` 은 「칩 7」, `.cond` 는 「고른 조건을 문장으로」입니다.

**지금 우리 CSS 는 같은 이름을 다른 뜻으로 쓰고 있습니다.**

```
시안   .chip  { font-family:var(--mono);font-size:12px;padding:4px 9px; … }
                ★ 값을 나타내는 표식이다
우리   .chip  { …display:inline-flex;gap:2px }  + .chip b · .chip .x · .chip .x:hover
                ★ **지울 수 있는 필터 칩**이다.  자식 요소가 셋 붙어 있다
시안   .cond  { max-width:1400px;margin:0 auto;padding:12px 16px 0 }   ★ 페이지 폭 감싸개
우리   .cond  { border:1px solid…;display:flex }  + .cond .q · .cond .n · 반응형 3줄
```

```
★ 지금 시안 값으로 덮으면 마스터가 보는 /listings 의 필터 칩과
  조건 문장이 그대로 깨집니다.
★ 「값을 버리지 마라」(개정 429)를 어기게 됩니다
★ 그래서 안 덮었습니다 — 순서 5 에서 화면을 함께 옮기며 닫겠습니다
```

★ 지시는 「1·2·3·4·11 을 먼저 닫아라. **짧다**」였습니다.
**11 은 짧지 않습니다 — 11 이 곧 5 입니다.** 9절 ③ 에 적었습니다.

---

## 9. ★ 가이드가 틀린 곳 · 규격에 안 적힌 것

### ① ★★ 개정 435 실측표의 「② 675 + 7」에서 **7 은 빼야 합니다**

```
규격 (f-table 개정 435 절)
  | ② 못 읽는다 | 675 + 7 | 3.1% |

★ 그 7 은 제 v193 도구가 키를 잘못 골라 생긴 것입니다.
  가이드가 제 숫자를 그대로 받으셨습니다 — ★ 제 잘못입니다
  history.not_join 이 쓰는 값은 notJoinDate1~5 인데 openData 를 봤습니다
  openData 는 「보험이력이 열려 있는가」라 늘 true 입니다
★ 그 7건은 ① 입니다 (미가입 날짜가 전부 null)
★ 다만 다른 결함이 그 자리에 있었습니다 — 보유 0개월에서 비율을 못 내
  「모름」이 되고 있었습니다.  고쳤습니다 (3절 ②)
★ 도구의 키를 notJoinDate1 로 고치고 config 에 까닭을 적었습니다
```

### ② `V3-91` 검산 표는 아직 6줄 어긋납니다 — 그대로입니다

```
점수 셋   +13 (값 만점을 237 로 쓰셨다 — 개정 432 절이 까닭은 적었으나
                ★ 표의 숫자 532 · 584 · 662 는 안 고쳐졌습니다)
등급 셋   개정 433 컷으로 다시 재면 A · S · B 인데 표에는 B · A · C
```

### ③ ★ 순서 11 은 「짧다」가 아닙니다

8절에 근거를 적었습니다. `V11-60` 의 실패가 곧 화면 개편입니다.

### ④ ★ 규격에 안 적힌 것 — `V1-28` 같은 「추이 검사」의 함정

```
갈래를 새로 나누면 ★ 그 갈래가 0 → N 이 되어 「늘었다」로 잡힙니다.
실제로는 옮겨간 것인데 실패가 납니다 (실측 08-21 — ④ 신설 때 겪음)
고침   ★ 합으로 본다.  갈래별은 곁들여 적는다
★ 다른 추이 검사에도 같은 함정이 있을 수 있습니다 — 대청소 때 훑을까요
```

### ⑤ ★ 규격에 안 적힌 것 — 죽은 필드가 둘입니다

```
tire_tread_mm   읽는 곳만 있고 쓰는 곳이 없다 (순서 4 · 6절)
owned_months    ★ collect/runner.py 가 채운다 — 살아 있다.  헛다리였습니다
★ 「읽기만 하고 아무도 안 쓰는 필드」를 잡는 검사가 아직 없습니다 (v193 9절 ④)
  V1-26 은 excluded 로 보는데 이런 축은 excluded=0 · value=0 이라 안 걸립니다
  만들까요
```

---

## 10. 못 한 것 · 판단이 필요한 것

| # | 무엇 | 상태 |
|---|---|---|
| ① | 순서 11 (`V11-60`) | **순서 5 에서 닫습니다** — 8절 근거 |
| ② | 개정 435 표의 「+7」 | **여쭙니다** (9절 ①) — 제 v193 숫자가 틀렸습니다 |
| ③ | `V3-91` 6줄 | **여쭙니다** (9절 ②) — 규칙 2 로 제가 못 고침 |
| ④ | 죽은 필드 검사 | **여쭙니다** (9절 ⑤) |
| ⑤ | 추이 검사 함정 | **여쭙니다** (9절 ④) |
| ⑥ | 순서 5 (화면) | **다음입니다. 가장 큽니다** |
| ⑦ | 순서 6~10 | 아직입니다 |
| ⑧ | 틴팅 | **내일** — 자리는 확인해 뒀습니다 (6절) |

---

## 11. 고친 파일

```
contracts.py                board_state_ok
parse/encar/mapping.py      inspection_board_state = boardStateType.title
sql/ddl/02_core.sql         core_inspection.inspection_board_state
store/core.py               스냅샷에 board_state_ok
analyze/axis/state.py       계기판을 실제로 본다 · integrity_ok
analyze/axis/history.py     미가입 0 개월이면 보유 0 개월이어도 만점
report/screens/build.py     ★ chip() 이 source 로 「모름」을 가른다 · is_unknown()
config/unknown_split.json   unknown_sources · confirmed_absent_sources
                            · 갈래 넷 · not_join 키 정정
tools/unknown_split.py      갈래 넷 · ④ 절 출력 · 이력에 kind4
validate/v3_logic.py        V3-95 · V3-96 신설
validate/v1_collect.py      V1-27 넷으로 · ★ V1-28 을 합으로
```

**커밋** `80ee715`
