# A · B · E · F · H — 추천 탭에 값을 붙이고 · KB 목록을 읽고 · 엔카 조리개를 쟀다

지시   `outputs/ORDER_20260829.md` · `SPEC-2026.09.05-r1184`
버전   v385 · `calc_version = c1`
커밋   `bc19d7b3` (A) · 이 회차 (B·E·F·H)

---

## 0. 무엇을 했나 — 한눈에

| 번호 | 무엇 | 상태 | 「몇 → 몇」 |
|:--:|---|:--:|---|
| **A** | 추천 탭 2·3·4 에 값 붙이기 | **끝** | 탭2 `0건 → 11,304건` · 탭4 `없음 → 격자 둘` |
| **B** | 마스터 대상 차종 상세 | **일부** | GV70 상세 `ok 7 → 9` · 아래 3-B |
| **E** | 엔카 상세 수집 | **일부** | 조리개를 쟀다 · S5 에 간격을 넣었다 |
| **F** | KB 상세 수집 | **일부** | 값 `11% → 24%` · 트림 `0 → 2,241` |
| **G** | 매핑표 | 손 안 댐 | 다음 회차 |
| **H** | 열두 사이트 구멍 | **일부** | `options_name_json` 신설 |
| **I** | 원문 표 줄이기 | 손 안 댐 | 이미 0 이다 (아래 6) |

★ **가이드 검사 199개 중 붉은 것 12 → 11.**

---

## 1. A — 추천 탭 2·3·4 (`S46-283`)

화면은 가이드가 다 지으셨다. 개발측은 값만 붙였다.

| # | 무엇 | 어디 |
|:--:|---|---|
| A-1 | 탭을 **자료로** 정한다 | `config/web.json` `recommend_tabs` |
| A-2 | 탭 2 — `tabs`·`picks`·`sorts`·`page`·`rows` | `report/screens/tabs.py` `view_tab2` |
| A-3 | 탭 3 — `tabs`·`rows`·`total_url`·`count` | 〃 `view_tab3` |
| A-4 | 탭 4 틀 ＋ 격자 둘 | `web/templates/twocars.html` · `view_tab4` |
| A-5 | `/analyze` · `/analyze/add\|drop\|copy/{id}` | `web/routes.py` · `web/views.py` |
| A-6 | 쪽 번호 | `_pager()` |
| A-7 | 여럿 고르기 **AND** | `_picks()` |
| A-8 | 줄 세우기 여섯 | `_sorts()` · `ORDER_SQL` |
| A-9 | 「모름」·「미조회」 | `UNKNOWN` · `NOT_ASKED` |

### 실측 (개발 서버 8799)

```
/recommend?tab=1   200 · 151,683B
/recommend?tab=2   200 ·     618B  →  53,922B      ← 11,304건 중 1–30
/recommend?tab=3   200 ·   2,660B                  ← 맡긴 차 0 (안내가 나온다)
/recommend?tab=4   200 ·   8,797B                  ← 격자 둘
/recommend?tab=4&m=GV70_25T&p=3500&km=610          ← 그 칸 40건 · 등급순
/analyze/copy/4567 200 · 원문 30줄 (없는 것은 「미조회」)
```

탭 4 격자 — GV70 2.5T · **리스·렌트를 뺀 1,882건** (2,042건이 아니다):

```
              3만 이하    3~6만      6~10만     10만 초과
3,000만 이하      —      5 2,926만   13 2,662만  31 2,730만
3,500만 이하   3 3,456만  54 3,390만  84 3,301만  21 3,139만
4,000만 이하  39 3,865만 174 3,809만  68 3,743만   4 3,740만
4,000만 초과 486 5,010만 213 4,545만  11 4,275만     —
```

### 고치다 잡은 것 둘

**① 탭 2 가 200 인데 618B 였다 — 얼개가 통째로 비었다.**
가이드 틀이 쪽 번호를 `page.total`·`page.links` 로 부르는데,
그 `page` 가 `_page.html` 의 `page`(제목·판수·차림표)를 **덮었다.**
속틀에서는 틀이 부르는 대로 두고, **얼개에서는 얼개 것이 이기게** 했다
(`web/views.py` `page()`).

**② 격자 평균연식이 죄다 `2022.1` 이었다.**
`year_month` 는 `'2023-01'` 꼴인데 5번째부터 잘라 `'-0'` → 월이 전건 0 이었다.
6번째부터 자른다. 고친 뒤 `2022.3` · `2023.6` · `2024.7` 로 갈린다.

---

## 2. E — 엔카 상세는 **막힌 것이 아니라 조리개**다

★ 제가 앞 회차에 「누적 차단이라 서버로 못 받는다」고 적었습니다. **틀렸습니다.**
새 자(`tools/encar_detail_probe.py`)로 두드려 재니 이렇습니다:

| 간격 | 시도 | 200 | 407 | 성공률 |
|--:|--:|--:|--:|--:|
| **0.3초** (지금 설정) | 5,846 | **4** | 5,841 | **0.07%** |
| 5초 | 24 | 3 | 21 | 12.5% |
| 30초 | 8 (404 둘 뺀 6) | 2 | 4 | **33%** |

같은 매물을 다시 부르면 200 이 옵니다. **간격이 원인**입니다.

### 고친 것 — `Pace` (조리개에 맞춰 간격을 늘린다)

```
collect/runner.py  Pace  — 407·429·503 이 오면 간격을 곱하고(상한까지)
                          200 이 오면 줄인다
config/endpoints.json     interval_max_sec 30 · interval_backoff 2.0
                          interval_recover 0.7          ← 값은 여기가 정본 (S14)
```

`interval_max_sec` 이 없는 사이트는 **지금까지와 똑같이** 돕니다. 켜야 달라집니다.

**실측** — 같은 S5 를 두 번 돌렸다:

```
간격 넣기 전   90초에 39,051개 창구를 훑고 · requested 5,846 · ok 4 · error 5,841
간격 넣은 뒤   90초에    63개 창구 · ok 가 실제로 늘어난다 (GV70 ok 7 → 9)
```

느립니다. 33% · 30초면 6,900건을 채우는 데 여러 날이 걸립니다.
**여쭐 것 1** 을 봐 주십시오.

---

## 3. B — 마스터 대상 차종

### B-1 · B-2  GV70 2.5T

```
detail_status   NULL 2,027 · ok 7 · error 6 · not_found 2   (전)
                NULL 2,024 · ok 9 · error 7 · not_found 2   (지금 · 계속 도는 중)
```

**B-2 는 재계산으로 안 풀립니다.** 왜 전건 `PENDING` 인지 쟀습니다:

```
GV70 PENDING 1,901건 — 근거 있는 축 평균 363 / 분모 910 = 39.9%
                       (판정 컷은 50%)
등급이 난 매물 평균  —                    776 / 910 = 85.2%
```

근거가 모자란 까닭은 **상세가 없어서**입니다. 상세가 차면 등급이 납니다.
그러므로 B-2 는 B-1 뒤입니다 — 지시서의 「앞조건 없음」과 다릅니다 (**여쭐 것 2**).

### B-7  스포티지 682건이 통째로 빔 → **KB 였다**

```
SPORTAGE_LPI   kbchachacha 707 · encar 276
빈 707건이 ★ 전부 kbchachacha 다 — 값·주행·연식·트림·색·사진이 다 NULL
```

곧 B-7 은 **F-1(KB 목록 파서)** 과 같은 일이었습니다. 아래에서 함께 풀었습니다.
`SPORTAGE_LPI` 값 있는 것 **276 → 418**.

### B-8  리스·렌트 거르기 — **이미 있었다**

`_listings_where` 가 기본으로 뺍니다 (`?lease=1` 로 볼 수 있다 · 개정 420).
제 탭 4 가 갈래 이름을 코드에 박고 있어 `config/scoring.json` 을 읽게 고쳤습니다 (`S14`).
GV70 2,042건 중 **160건**이 리스·렌트입니다.

### B-9 · B-10  취향 축 — **앞 회차(v384)에서 끝났다**

`taste.display` 35 · `taste.interior` 20 · 차종 고정값을 `targets.json` 에서 읽습니다.
`S46-286`·`S46-287` 둘 다 지금 초록입니다 — 가이드께서 네 차종 값을 채워 주셨습니다.

### B-11 · B-12  격자 — 위 A-4 에서 끝났습니다.

---

## 4. F — KB 목록이 값을 다 준다

`parse_list(html)` 은 있었는데 **원문이 DB 에 없었습니다** (0-3 에서 몸통을 비웠다).
파일 1,169개를 `tools/load_raw.py kbchachacha --write` 로 다시 읽혔습니다.

| 항목 | 전 | 후 |
|---|--:|--:|
| 값 | 726 (11%) | **1,576 (24%)** |
| 주행 | 729 (11%) | **2,970 (45%)** |
| 연식 | 729 (11%) | **2,970 (45%)** |
| 사진 | 545 (8%) | **2,786 (42%)** |
| **트림** | **0** | **2,241 (34%)** |
| 옵션 | 0 | **543** |
| 색 | 729 (11%) | 729 (11%) — 목록 카드가 색을 안 준다 |

**F-3 「트림 0」의 까닭** — 파서가 `trim_badge` 만 채웠는데
검사와 화면은 `trim_grade_name` 을 셉니다. 둘 다 넣었습니다.

**F-4 「옵션 0」의 까닭** — KB 상세는 `car-option-list` 로 **이름만** 줍니다
(가격이 없다). 지시 H 대로 `options_name_json` 을 새로 만들어 담았습니다.
`disable` 이 붙은 것은 「그 차에 없다」로 `options_absent_json` 에 갑니다.

---

## 5. H — 옵션을 두 갈래로

```
sql/ddl/02_core.sql   options_name_json 신설
                      가격이 있으면 options_choice_json · 이름만 있으면 여기
tools/browser_diff.py 「옵션」을 COALESCE(choice, name) 으로 센다
```

지시 원문 — 「가격이 없다고 옵션 축을 0 으로 두지 않는다」.

---

## 6. I — 원문 표는 이미 비어 있다

```
raw_response 의 kbchachacha 몸통 — 0건
```

앞 회차(0-3·0-4)에서 몸통을 파일로 내보내고 `VACUUM` 했습니다.
지시서의 「425MB」는 그 전 숫자입니다. **다시 할 것이 없습니다** (여쭐 것 5).

---

## 7. 두 도구를 돌린 실제 출력 (규칙 3 · 4)

```
python3.11 tools/run_tests.py
test_admin 통과 · test_admin_flow 통과 · test_collect 통과 · test_crosssite 통과
test_dict 통과 · test_endtoend 실패 · test_fixtures 통과 · test_integration 통과
test_invariants 통과 · test_pipeline 통과 · test_registry 통과 · test_report 통과
test_run 통과 · test_score 통과 · test_screens 통과 · test_spec_ui 통과
test_store 통과 · test_watch 통과 · test_web 통과      ← 전에는 실패
ruff 통과                                              ← 전에는 실패
```

`test_web` 이 통과했습니다 — 「Route 수 38 ↔ 표 42」가 `/analyze` 넷으로 닫혔습니다.

```
가이드 검사 (python3 -c "from validate.v0_guide import run; …")
199개 중 붉은 것 12 → 11
```

### ★ 내 몫 114개 중 몇 개가 붉은가

가이드 표의 「114개」가 무엇을 센 수인지 제가 아직 모릅니다 (**여쭐 것 6**).
지금 **가이드 검사 199개 중 11개**가 붉고, 갈라 보면 이렇습니다:

| 갈래 | 수 | 코드 |
|---|--:|---|
| ★ **내가 고칠 수 있다** | **4** | `S46-207`(커밋 제목) · `S46-241`(배포 재판정) · `S46-261`(회차↔배포) · `S46-24`(facet) |
| 사이트·시간이 필요하다 | 4 | `S46-276`(엔카 조리개) · `S46-279`(KB 상세 1,489/6,354) · `S46-280` · `S46-247` |
| 사람이 봐야 한다 | 1 | `S46-92`(0건 쿼리 넷 — 이름이 틀렸나 정말 없나) |
| 가이드 몫 (시안·틀) | 2 | `S46-98` · `S46-100` (`/detail/3` 낱말·차례) |

앞 회차(v384)에서 닫은 `S46-32` 는 이번에도 다시 돌렸습니다.

---

## 8. 여쭐 것

### 1  엔카 상세 — 며칠이 걸립니다. 어떻게 할까요

30초 간격에 33% 입니다. 6,900건이면 **약 6~7일** 쉬지 않고 돌아야 합니다.
① 이대로 며칠 돌린다 ② 마스터 회선(브라우저)으로 받는다
③ 마스터 대상 차종(GV70·X3)부터 먼저 채운다 — **저는 ③ 을 권합니다.**
지금은 ③ 처럼 `--target GV70_25T` 로 돌려 두었습니다.

### 2  B-2 는 B-1 앞조건입니다

지시서는 B-2(재계산)를 「앞조건 없음」이라 적었지만,
GV70 은 근거 39.9% 라 **재계산해도 PENDING** 입니다. 상세가 차야 등급이 납니다.
지금은 B-1 을 먼저 돌리고 있습니다.

### 3  A-4 는 「틀은 가이드가 짓는다」와 어긋납니다

[화면] 절 — 「`web/templates/` 는 가이드가 작성한다」
A-4 — 「탭 4 **틀을 만든다**」
최근 지시가 우선이라 하셔서 A-4 를 따라 `twocars.html` 을 지었습니다.
가이드가 다시 지으시면 그것으로 바꾸겠습니다 — 값 이름은 파일 맨 위 주석에 적었습니다.

### 4  분석 「맡기기」·「제외」가 `<a href>` 입니다

가이드 틀은 `<a href="/analyze/add/{id}">` 곧 **GET** 인데,
검사 `V11-08`(상태 변경이 GET 경로에 없음)이 **fatal** 로 잡고
규격 표(`61-web` 196행)도 `drop` 을 **POST** 라 적었습니다.
길은 **POST 로 열었습니다** — 지금 그 링크를 누르면 405 입니다.
`analyze.html` · `recommend2.html` 의 그 두 자리를 **작은 폼(POST)** 으로 바꿔 주십시오.

### 5  I(원문 표 줄이기) 는 이미 끝나 있습니다

`raw_response` 몸통이 0 입니다. 지시서의 425MB 는 앞 회차 전 숫자입니다.
더 줄일 것이 있으면 무엇인지 말씀해 주십시오.

### 6  「내 몫 114개」

가이드 표는 114, 제가 도는 가이드 검사는 199개입니다.
어느 묶음이 「내 몫 114」인지 알려 주시면 그 자로 세어 적겠습니다.

### 7  지역 「배달(전국 배달)」을 낼 수 없습니다

규격 ⑤ 는 배달·지역을 가르라 하시는데, **열두 사이트 어디도 배달 여부를 안 줍니다**
(09-06 실측 — `site_service_marks_json`·`site_condition_json`·`selling_point` 에
「배달」이 0건). 지역 다섯(서울·인천·경기·대구·그 외)만 냈습니다.
어느 칸을 배달로 볼지 알려 주시면 바로 붙이겠습니다.

## 9. 이견

없습니다.
