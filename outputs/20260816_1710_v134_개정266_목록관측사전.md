# 개정 266 — 목록 관측으로 사전을 채운다 (pending 으로만)

작업 시각 2026-08-16 16:45~17:10 · 소스 v134 · 실행 `python3.11`
커밋 `10ddc9d`

---

## 1. 무엇을 하려 했나 (지시 원문)

> ```
> 1) build_dict 가 목록 원문에서도 축을 읽게 해라
>    source='list' 로 pending 을 만든다 · confirmed 로 올리지 마라
>    V3-37 · V3-38 을 validate 에 구현
> 2) 그걸로 S3 를 돌려라 — V4-25 · V3-30 이 사라지는지 숫자로
> 3) 그 다음 한 차종만 S5 → S6 → S9 → S10 을 끝까지 돌려라
> 4) 파싱 뒤에 나오는 것을 숫자로 내라
> 5) S5 전체를 돌릴 때 중단·재개가 되는지 확인해라
> ```

## 2. 무엇을 했나

### ① 목록 관측 사전 (지시 1)

| 파일 | 무엇 |
|---|---|
| `tools/build_dict.py` `LIST_FALLBACK` | `fuel`·`color_ext`·`color_int`·`sell_type` 의 목록 경로 |
| `tools/build_dict.py` `build_dict` | facet 축이 **비었을 때만** 목록에서 읽는다. facet 이 있으면 facet 이 정본이다 |
| `store/dictionary.py` `upsert_enum(force_pending=)` | 목록 관측분은 `pending` 으로만 들어간다 |
| `tools/build_dict.py` `_mark_facet_substituted` | 대체 사실을 `STEP53-S2 · actual='list'` 로 감사에 남긴다 |
| `validate/v3_logic.py` | `V3-37`(출처가 list 인가) · `V3-38`(facet 수신 후 대조) |

**`source_endpoint='list'` · `status='pending'` 두 가지가 함께 남는다.**
「전체 집합을 봤다」가 아니라는 표시다.

### ② 그 과정에서 결함 2건을 잡았다

```
mark_step_imported 가 커밋하지 않아 CLI 로 돌리면 그 행이 사라졌다
  → build_dict 가 끝맺도록 고쳤다.  실측으로 확인했다 (첫 실행에서 S2 행이 없었다)

단계 행 run_id 를 저장마다 새로 주어 STEP53-S1 이 102행이 됐다
  → PK 가 (run_id, phase, code, target_key) 다.  방법당 하나로 바꿨다
```

### ③ S3 를 돌렸다 (지시 2)

```
python3.11 tools/build_dict.py carwatch.db d-266
  color_ext 값 23 신규 23 · color_int 10/10 · fuel 5/5 · sell_type 3/3 · trim 68/68
  pending 109건

python3.11 run.py collect --only S3 --target KOLEOS_HEV
  정상 종료
```

## 3. 왜 그렇게 했나 (규격)

- 개정 266 `12-dict.md` — 「facet 이 있으면 facet 이 정본」 · 「facet 이 없으면 목록에서 관측한
  값으로 pending」 · **「금지 목록 관측만으로 confirmed 를 만드는 것」** · 검산 `V3-37`·`V3-38`
- 개정 259 — 밖에서 확보한 단계는 `audit_validation` 에 `actual` 로 남긴다.
  `S2` 를 목록 관측이 대신한 것도 같은 방식으로 남겼다
- 4장 STEP 45 — 확정 전에는 그 축을 쓰는 판정이 돌지 않는다

## 4. 1차 결과 (작업 전 숫자)

```
dict_enum      color_ext 0 · color_int 0 · fuel 0 · sell_type 0 · trim 0
raw_facet      0행
precheck S3    (False, '선행 단계 미완료: S2')

check_all      통과 181 · fatal 3
  V4-25 판정에 쓰는 축의 사전이 비어 있지 않음 — 6건 (color_ext, color_int, fuel)
  V3-30 halt 축의 사전이 비어 있지 않음 — 4
  V3-32 seizing null
```

## 5. 조치 후 결과

```
dict_enum
  color_ext  pending / list   23
  color_int  pending / list   10
  fuel       pending / list    5
  sell_type  pending / list    3
  trim       confirmed / list 68
  accident_type · panel_rank · panel_status  confirmed / bootstrap

STEP53-S2  actual='list'  passed=1
precheck S3  (False,'선행 단계 미완료: S2')  →  (True, '')

python3.11 tools/check_all.py
통과 182 · fatal 3 · warn 7
  V3-08  사전 pending 이 판정에 쓰이지 않음 — 38   ← 신규
  V3-30  halt 축의 사전이 비어 있지 않음 — 1        ← 4 → 1
  V3-32  seizing null 매물이 「저당 없음」으로 판정되지 않음

V3-37 passed=True  color_ext=pending/list(23) · color_int=pending/list(10) · fuel=pending…
V3-38 passed=True  facet 을 아직 못 받았다
```

| 지표 | 작업 전 | 작업 후 |
|---|--:|--:|
| `check_all` 통과 | 181 | **182** |
| **`V4-25`** | fatal (6축 비었음) | **사라짐** |
| `V3-30` | 4 | **1** |
| 판정 축 사전 | 0종 | **41종** (pending) |
| `precheck('S3')` | 막힘 | **통과** |

**`V4-25` 가 사라졌다 — 지시 2의 답이다.**
`V3-30` 은 4 → 1 로 줄었다. 남은 1은 `LATE_AXES`(S6a 가 채우는 축)라 상세 파싱 뒤에 풀린다.

`V3-08`(사전 pending 이 판정에 쓰이지 않음 — 38)은 **신규지만 규격이 의도한 것**이다.
목록 관측분을 `pending` 으로 두었으니, 그 값을 쓰는 옛 판정 결과가 걸린다.
확정하면 사라진다.

## 6. 못 한 것 · 막힌 것 · 판단이 필요한 것

### ① ★ 지시 3·4·5 를 못 했다 — 사람이 사전을 확정해야 다음이 열린다

```
precheck S4  (False, '분류에 쓰는 사전에 pending 5건')
precheck S9  (False, '선행 단계 미완료: S6,S6a,S8,S8.5')
```

`fuel` 5종이 `pending` 이라 **S4(목록 파싱)부터 막힌다.** 이것은 결함이 아니라
개정 266 과 STEP 45 가 의도한 관문이다 — 「확정 전에는 판정이 돌지 않는다」.

확정해야 할 값 41종:

```
fuel        5종   가솔린 · 디젤 · 전기 · 가솔린+전기 · (1)
color_ext  23종   흰색 · 명은색 · 검정색 · 쥐색 …
color_int  10종   빨간색 계열 · 베이지색 계열 · 검정색 계열 …
sell_type   3종   일반 · 렌트 · 리스
```

**규격이 「목록 관측만으로 confirmed 를 만들지 마라」고 했으므로 내가 확정하지 않았다.**

### ② ★ 그런데 확정할 화면이 없다 — 이것이 다음 관문이다

```
store/dictionary.py:421  confirm_enum()   함수는 있다
web/templates/                            dict_enum 을 쓰는 화면이 하나도 없다
```

개정 250 이 「CLI 없이 돌아간다」고 했으므로 CLI 로 확정하는 것도 규격 위반이다.
**「사전 확정」 화면이 있어야 파이프라인이 끝까지 간다.**

`STEP 136d` 표에 `build_dict → /admin/registry` 가 있으니 그 화면에
「사전 생성」과 「사전 확정」을 함께 두는 것이 자연스럽다.

**만들까요?** 만들면 그 자리에서 41종을 확인하고 S4 → S10 까지 이어 돌릴 수 있다.

### ③ facet 은 여전히 0건이다

`V3-38` 이 「facet 을 아직 못 받았다」로 대기 중이다. facet 이 들어오면
목록 관측분과 대조해 **목록에만 있는 값**을 낸다 — 개정 266 이 중요하다고 한 그것이다.
`/admin/collect` 의 「facet 만」으로 시도해 주시면 된다.

### ④ 규모 확인 (개정 266 4절)

```
지금 core_listing 206건   ← 반입 203 + fixture 3
S4 가 돌면            7,631건 (목록 392쪽에서 펼친 것)
S5 는 그 4종 × 7,631 = 약 30,524 호출 · 진단 제외 약 23,961 · 간격 0.3초 → 약 2시간
```

지시대로 **한 차종만 먼저** 돌릴 준비는 돼 있다 (`--target KOLEOS_HEV`).
중단·재개는 `STEP 52` 의 `resume_point` 와 `raw_response` 에 이미 있는 것으로 확인한다 —
사전 확정 뒤 실제로 돌려서 숫자로 보이겠다.

### ⑤ 남은 것

`V10-22`·`V10-23`(큐 A/B 결정 대기) · `V11-45`(`STEP 136d`) ·
`STEP53-S1` 102행(과거 데이터 — 코드는 고쳤다) · 시험 3파일
