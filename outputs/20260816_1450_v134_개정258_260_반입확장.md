# 개정 258~260 — 메뉴 표 · S1·S2 반입 완료 · facet 형식 ④ · `V11-46`

작업 시각 2026-08-16 14:35~14:50 · 소스 v134 · 실행 `python3.11`
커밋 `09208cb` (258) · `4a09f89` (259·260) · `1282f9b` (V4-22 회귀 수정)

---

## 1. 무엇을 하려 했나 (지시 원문)

> ```
> 1) 개정 258 반영 후 V11-12 가 사라지는지 확인
>    test_web · test_admin · test_endtoend 도 함께 볼 것
> 2) 개정 259 대로 S1 · S2 반입 완료를 구현 · V11-46 을 validate 에 구현
> 3) 개정 260 대로 facet 형식(④)을 /admin/import 에 추가
>    받으면 S2 완료를 남긴다.  그 뒤 S3(build_dict) 가 도는지 확인
>    ★ facet 원문은 마스터가 휴대폰에서 받아 준다.  화면만 준비해라
> 4) 개정 261 — 큐를 어떻게 할지 먼저 보고해라.  고르기 전에 만들지 마라
> 5) S24 를 끝내라
> ```

## 2. 무엇을 했나

### ① 규격 12파일 반영

`spec_258_261.tar.gz` 를 전수 `diff`. 삭제 2줄은 「받는 형식 두 가지 → 네 가지」 교체분이고
나머지는 순증이다. 앞서 반영한 251~257 을 되돌리는 것은 없었다.

### ② 개정 258 확인 — `V11-12` 는 **안 사라졌다. 원인이 뒤집혔다** (커밋 `09208cb`)

```
반영 전   V11-12  표에 없다: /admin/import      (코드가 앞섰다)
반영 후   V11-12  표에만 있다: /admin/collect    (규격이 앞선다)
```

메뉴 표·라우트 표에 `/admin/import` 가 들어와 그쪽은 해소됐으나,
같은 개정이 `/admin/collect` 도 함께 등재했고 **그 화면은 CORS 결과 전까지
만들지 말라는 지시**가 있어 코드가 비어 있다.

`test_admin` 의 하드코딩 `12` 도 함께 걷어냈다.

```
tests/test_admin.py:29-41   _spec_menu_paths() — 13장 메뉴 표를 읽는다
tests/test_admin.py:432-440 코드 MENU 와 경로 집합으로 대조
```

숫자를 손으로 적어 두면 규격이 화면을 늘릴 때마다 시험이 먼저 막는다.
지금은 무엇이 모자란지 **이름으로** 말한다.

```
FAIL  메뉴가 13장 표와 같다 (14개)  코드 13 · 표 14 · 표에만 ['/admin/collect']
```

### ③ 개정 259 — 반입이 `S1` 도 연다 (커밋 `4a09f89`)

| 파일 | 무엇 |
|---|---|
| `contracts.py:118-122` | `S1_CODE`·`S2_CODE`·`IMPORT_STEP_CODES` |
| `store/adminops.py:212-217` | 목록 반입이 `STEP53-S1` 과 `STEP53-S4` 를 함께 연다 |
| `store/adminops.py:266-283` | `_mark_s4_imported` → `mark_step_imported(code, …)` 로 일반화 |
| `validate/v11_web.py:180-185 · 763-789` | `V11-46` 정의 · 구현 · 호출 |

`actual='import'` · `expected='collector 또는 import'` · `samples` 에 무엇을 반입했는지 남긴다.

### ④ 개정 260 — facet 형식 ④ (커밋 `4a09f89`)

| 파일 | 무엇 |
|---|---|
| `contracts.py:104` | `FORMAT_FACET` |
| `sql/ddl/01_raw.sql:31-34` | `raw_facet.request_url` 을 nullable 로 (STEP 136b ②) |
| `parse/importer.py:31-48` | `detect_format` 이 `iNav` 로 목록과 facet 을 가른다 |
| `parse/importer.py:51-78` | `parse_facet` — 축 이름과 값 수만 센다. 사전은 S3 이 만든다 |
| `store/raw.py:171-199` | `save_import_facet` — `raw_facet`(S3 이 읽는 자리) + `raw_response`(출처를 남기는 자리) |
| `store/adminops.py:236-265` | `_import_facet` — 매물을 안 넣고 `STEP53-S2` 만 연다 |
| `web/templates/admin_import.html` | 형식 ④ 설명 |

`python3.11 tools/migrate.py` → `raw_facet` · `raw_response` 2건 재작성. 데이터 보존.

### ⑤ ★ 내가 만든 회귀 하나를 잡아 고쳤다 (커밋 `1282f9b`)

개정 260 을 넣으며 `store/adminops.py` 가 `parse.importer` 를 import 했다.
**`store` 는 `contracts`·`errors` 만 부를 수 있다** (STEP 15a · `LAYER_ALLOW`).

```
check_all 에 V4-22 역방향 · 순환 import 없음 — 2  가 fatal 로 새로 떴다
```

facet 해석을 `report.screens.admin.parse_import_text()` 로 올리고
`store` 는 해석 결과(dict)를 인자로 받게 바꿨다. 앞서 web↔parse 를 이을 때 쓴 것과 같은 방식이다.

## 3. 왜 그렇게 했나 (규격)

- 개정 258 — 13장 메뉴 표에 `/admin/import` · `/admin/collect`
- 개정 259 · `13-pipeline.md:173-193` — 「S1·S2 도 반입 완료를 남길 수 있다 · `code='STEP53-S1'` ·
  `actual='import'` · 무엇을 반입했는지 samples 에 · **근거 없이 열지 않는다** · 검산 `V11-46`」
- 개정 260 · `c-tools.md:206-217` — 「④ facet 원문 JSON. `color_ext`·`color_int`·`fuel` 사전은
  facet 에서만 나온다 (실측 · `V4-25`)」
- STEP 136b ② — 반입이 못 채우는 칸은 nullable → `raw_facet.request_url`
- STEP 15a · `LAYER_ALLOW` — `store ← contracts·errors` 뿐 (⑤의 근거)
- 규칙 1 — 코드가 규격과 다르면 코드를 고친다 → `test_admin` 의 하드코딩 제거

## 4. 1차 결과 (작업 전 숫자)

```
python3.11 tools/check_all.py
통과 174 · fatal 4 · warn 7 · 환경 차이 2 · 미실행 1
  V4-25 · V3-30 · V3-32 · V11-12(표에만 있다: /admin/collect)

python3.11 tools/check_src.py
S16 검증 코드 대조   실패  실패 6 — V10-22 · V10-23 · V11-43 · V11-44 · V11-45 · V11-46
결과: 실패 — S16(6) / S25(1)

python3.11 tools/run_tests.py
결과: 실패 — test_admin.py, test_admin_flow.py, test_endtoend.py,
            test_integration.py, test_spec_ui.py, test_web.py
  test_admin  메뉴 12개 · 13        ← 손으로 적은 숫자
```

반입 상태: `STEP53-S4` 만 열려 있고 `precheck('S3')` = `(False, '선행 단계 미완료: S1,S2')`

## 5. 조치 후 결과

### 사본으로 전 사슬을 실측했다

```
① facet 반입
   형식 판별       facet
   미리보기        축 4 · 원문 있음
   raw_facet       ('encar','KOLEOS_HEV','unspecified', request_url=None, axis_count=4, 398자)
   STEP53-S2       actual='import' · passed=1

② 목록(CSV) 재반입
   203건 · 새 0 · 갱신 203
   STEP53-S1       actual='import' · passed=1

③ 그 결과
   완료 단계        ['S1','S2','S4','S5']
   precheck('S3')  (False,'선행 단계 미완료: S1')  →  (True, '')
   build_dict      충돌 {} · 새 값 5
   dict_enum       color_ext 2 · color_int 1 · fuel 1 · sell_type 1
                   accident_type 3 · panel_rank 5 · panel_status 4
```

**`V4-25` 가 지목한 `color_ext`·`color_int`·`fuel` 이 실제로 채워졌다.**
합성 facet 으로 낸 수치이고, 마스터가 실물 facet 을 주면 실제 값이 들어간다.

```
V11-46 passed=True  STEP53-S4=import · STEP53-S2=import
```

### 검사

```
python3.11 tools/check_all.py
통과 175 · fatal 4 · warn 7 · 환경 차이 2 · 미실행 1
  V4-25 · V3-30 · V3-32 · V11-12

python3.11 tools/check_src.py
S15 계층 의존 (STEP 15)   통과
S16 검증 코드 대조         실패 5 — V10-22 · V10-23 · V11-43 · V11-44 · V11-45
S23 · S24 · S26           통과
결과: 실패 — S16(5) / S25(1, 커밋 전 상태)

python3.11 tools/run_tests.py
  test_admin      메뉴가 13장 표와 같다 (14개)  코드 13 · 표 14 · 표에만 ['/admin/collect']
  test_admin_flow 등록부 재동기화
  test_endtoend   fatal ['V11-12']
  test_integration S5-8
  test_spec_ui    A-4 · S5-11 · 가이드-3b
  test_web        Route 수 소스 30 · 표 31
결과: 실패 — 6파일
```

| 지표 | 작업 전 | 작업 후 |
|---|--:|--:|
| `check_all` 통과 | 174 | **175** |
| `check_all` fatal | 4 | 4 (중간에 `V4-22` 로 5까지 갔다가 되돌림) |
| `S16` 미구현 검사 | 6 | **5** (`V11-46` 구현) |
| `precheck('S3')` | 막힘 | **통과** (반입 두 종을 받으면) |
| `color_ext`·`color_int`·`fuel` 사전 | 0 | **채워진다** |

**중간에 내가 `V4-22`(fatal)를 하나 만들었다가 같은 작업 안에서 잡았다.** 통과 174→175 는 그 결과다.

## 6. 못 한 것 · 막힌 것 · 판단이 필요한 것

### ① 지시 4번 — 큐를 A/B 중 무엇으로 할까 (내 판단은 아래 별도 절)

지시대로 **고르기 전에 만들지 않았다.** `V10-22`·`V10-23` 은 그래서 미구현이다.

### ② `V11-12` · `test_web` · `test_admin` 셋은 `/admin/collect` 하나에 걸려 있다

```
V11-12      표에만 있다: /admin/collect
test_web    Route 수 소스 30 · 표 31
test_admin  코드 13 · 표 14 · 표에만 ['/admin/collect']
```

셋 다 같은 뿌리다. **CORS 결과가 나와 `/admin/collect` 를 만들면 함께 사라진다.**
지금은 지시대로 만들지 않았다.

### ③ 지시 5번 S24 는 **이미 끝나 있다**

검토서 5절의 「run_tests 실패 6파일 (S24 미완 · 큐 잠금 · 관리자 2명 때문)」은
04:06 GitHub 기준이라 옛 정보다. S24 는 커밋 `29ef03d` 로 끝났고 지금 상태는 이렇다.

```
S24 시험 격리 (운영 DB 미사용)   통과
```

지금 남은 6파일 실패의 원인은 S24 도, 큐 잠금도, 관리자 2명도 아니다.

```
test_admin · test_web · test_endtoend   /admin/collect 미구현 (지시로 보류)
test_spec_ui  A-4 · S5-11 · 가이드-3b    이전부터 있던 3건
test_integration  S5-8                  이전부터 있던 1건
test_admin_flow   등록부 재동기화          이전부터 있던 1건
```

### ④ 실물 facet 대기

화면은 준비됐다. `/admin/import` 에 facet 원문 JSON 을 붙여넣고 차종을 고르면
`S2` 가 열리고 `S3`(build_dict)가 돈다. 마스터가 휴대폰에서 받아 주면 그 자리에서 넣는다.

받는 주소는 목록과 같은 자리다.

```
https://api.encar.com/search/car/list/mobile?count=true&q=...&inav=|Metadata|Sort
```

### ⑤ CORS 실측도 아직 대기

`http://43.201.16.78:8765/static/cors_test.html`
