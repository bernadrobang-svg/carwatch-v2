# CarWatch v2 — 시험자 보고서 (8회차 · 중복·차량키)

```
version   TEST-2026.09.02-r8
sources   배포 https://43.201.16.78.sslip.io (master) · 저장소 6932d318
          ★ 지난 회차 이후 ★ 새 커밋 0건 · 배포 빌드 20260902T040544 그대로
방법      /admin/query 로 DB 를 직접 세었다 · 화면과 대조했다
때        2026-09-02 19:10 ~ 19:40 KST
```

---

## 0. 이 회차가 무엇인가

지난 회차까지 안 잰 축을 팠다 — **시세 · 보증 · 크기 · 중복**.
앞 셋은 근거가 잘 붙는다. **중복 쪽에서 새 구멍이 나왔다.**

**이 보고서에 없는 것** — 급한 차례, 어느 대장으로 보낼지, 누가 고칠지, 무엇을 어떻게 고칠지.

---

## 1. ★ 근거가 잘 붙는 축 — 넷

| 축 | 근거 있음 | 근거 없음 |
|---|---|---|
| `value.market` 시세 | `market_median` **9,220** | `market_sample_short` 1,546 (표본 부족 — 근거는 있다) |
| `warranty.power` 동력계 | `encar_warranty_power` **9,669** · `maker_default` 292 | `missing` **805** |
| `warranty.site` 사이트 | 엔카진단 5,246 · 엔카진단+ 3,531 · 현대 인증 204 · 리볼트 47 | `rule_or_source_missing` 776 · `no_warranty` 478＋457 · `missing` 27 |
| `taste.size` 크기 | `size_curve` **10,213** | `missing` 553 |

```
★ 이 넷은 ★ 1장 ㉯(근거 없이 만점) 와 다르다 — ★ 근거 이름이 제대로 붙는다
★ ★ 다만 ★ warranty.power 는 ★ 805건이 missing 인데 ★ 점수가 어떻게 붙는지는
   ★ result_axis.score 가 NULL 이라 ★ 셀 수 없다 (7회차 D-1)
```

---

## 2. ★★★ 새로 잡은 것 — 중복 매물이 화면에 안 나온다

### 2-1. DB 와 화면이 갈린다

| | 건수 |
|---|--:|
| `copy_car = 'DUPLICATION'` | ★ **4,932건** |
| 그중 `status='active'` | ★ **3,012건** |
| `copy_car = 'ORIGINAL'` | 10,549 |
| `copy_car` 비어 있음 | 5,695 |
| ★ `/track` 화면이 말하는 「짝지어진 차」 | ★ **176대** |
| ★ `/listings` 카드의 「N곳」 배지 | ★ **0개** |

```
★★★ ★ DB 는 ★ active 중복차 ★ 3,012건이라 하고
★★★ ★ /track 은 ★ 176대라 한다.  ★ /listings 카드에는 ★ 배지가 ★ 하나도 없다
★ ★ /listings 의 「N곳」 1개와 /track 링크 1개는 ★ 설명 문단에 있는 것이다 (카드 아님)
```

★ `/listings` 화면은 스스로 이렇게 적어 놓았다:

```
「같은 차가 여러 곳에 있으면 낮은 값 하나만 낸다 (★「3곳」 배지를 누르면 추적으로)」
★ ★ 그 배지가 ★ 카드에 ★ 없다 (6·7회차와 같다 — ★ 8회차째)
```

### 2-2. ★★ 까닭 — 차량키가 매물 절반 이상에 없다

| | 건수 | 비율 |
|---|--:|--:|
| `core_vehicle` (짝짓기 단위) | **6,113대** | — |
| `vehicle_id` 붙은 매물 | **9,259** | 43.7% |
| ★ `vehicle_id` **없는** 매물 | ★ **11,917** | ★ **56.3%** |
| `vin` 있는 매물 | 7,969 | 37.6% |

★ **사이트별 — 차량키 없는 매물**

```
encar 6,250 · hyundai_cert 1,156 · reborncar 1,111 · kia_cpo 1,025
kcar 604 · kbchachacha 592 · bmw_bps 382 · heydealer 275
bobaedream 241 · volvo_selekt 160 · lexus_certified 64 · revolt 57
★★ ★ 열두 사이트 ★ 전부
```

★ **active 인데 VIN 이 없는 매물**

```
encar 2,236 · hyundai_cert 174 · kbchachacha 138 · volvo_selekt 131
heydealer 92 · reborncar 63 · bobaedream 17 · bmw_bps 9 · lexus_certified 7
★ 합 ★ 2,867건
```

```
★ 짝을 지으려면 ★ 같은 차인지 알아야 하고 ★ 그 키가 ★ vehicle_id 다
★ ★ 그것이 ★ 56.3% 에 없다.  ★ 없으면 ★ 짝을 못 짓는다
★ ★ ★ 원인은 안 적는다.  ★ 「키가 없다」와 ★ 「화면이 176대라 한다」까지가 시험자 몫이다
```

---

## 3. ★ 이번 회차 재측정 — 지난 회차와 같은가

| # | 무엇 | 7회차 | 8회차 | |
|---|---|---|---|---|
| 23 | 「N곳」 배지 | 0개 | ★ **0개** | ★ **8회차째** |
| — | `/track` 짝지어진 차 | — | ★ **176대** (DB 3,012) | ★ **새로 잼** |
| — | 전체 매물 | 5,504 | 5,522 | 늘었다 |
| — | 저장소 새 커밋 | — | ★ **0건** | — |
| — | 배포 빌드 | `20260902T040544` | ★ **같다** | — |

```
★ 지난 회차 이후 ★ 커밋이 ★ 하나도 없다.  ★ 배포도 그대로다
★ ★ 그래서 ★ 7회차의 「N회차째」 항목은 ★ 다시 재지 않았다 — ★ 바뀔 까닭이 없다
★ ★ 다만 ★ 매물 수는 ★ 5,504 → 5,522 로 늘었다 (수집은 돌고 있다)
```

---

## 4. 남긴 흔적

```
★ 없다 — ★ 이번 회차는 ★ 읽기만 했다
★ ★ /admin/query 로 세었고 ★ 화면을 열어 보았다
★ ★ 누른 것 ★ 없다 (scoring · registry · dict · targets · users · config · run 전부)
```

---

## 5. 시험자가 판단하지 않은 것

- **원인**을 적지 않았다. 2-2 의 「차량키가 56.3% 에 없다」는 **센 값**이다. 왜 없는지는 적지 않았다.
- **`/track` 176대와 DB 3,012건 중 무엇이 옳은지** 판정하지 않았다. 두 수가 다르다는 것까지만 적었다.
- **급한 차례**를 매기지 않았다.
- **어느 대장으로 보낼지 · 누가 고칠지**를 적지 않았다.

---

## 6. ★ 아직 못 잰 것

```
★ warranty.power missing 805건이 ★ 몇 점을 받는지 — ★ result_axis.score 가 NULL 이라 못 센다
★ market_sample_short 1,546건이 ★ 몇 점을 받는지 — ★ 같은 까닭
★ core_vehicle 6,113대가 ★ 실제로 몇 개씩 묶였는지 — ★ 짝 표를 못 찾았다
★★ ★ 셋 다 ★ 7회차 D-1 (result_axis.score 전부 NULL) 이 풀려야 잴 수 있다
```
