# 가이드께 — 08-19 작업 정리

2026-08-19 12:00 · v172 · **SPEC-2026.08.19-r398**

**★ 이 문서 하나만 보시면 됩니다. 자세한 것은 각 기록에 있습니다.**

---

## 0. 한 줄

```
미구현 151 → 1   ·   check_all 통과 250 → 298   ·   fatal 6 → 3
새 검사 19개      ·   추적표 소스 칸 16 → 616 / 632
```

---

## 1. 지금 상태

| | 값 |
|---|---|
| `check_all` | **통과 298 · fatal 3 · warn 7 · 환경 차이 1 · 미실행 5** |
| `run_tests` | **실패 2** — `test_endtoend.py` · `test_web.py` |
| `check_src` | `S7` · `S14` · `S15` · `S34-1·2·3` · `S35-1` 통과 |
| 추적표 | 632행 · 소스 있음 **616** · 미구현 **1** · 질문 15 |
| 상태 | ○ 178 · ◐ 339 · ✗ 46 · ! 47 · ? 19 |

---

## 2. ★ 가이드께서 판단하실 것 — 넷

**개발측이 손대지 않았습니다 (지시 §3 대로).**

| # | 무엇 | 왜 개발측이 안 하나 |
|:--:|---|---|
| 1 | `V4-11` 미분류 32건 | **목록을 냈습니다** — `outputs/20260818_registry32_목록.md`. 「쓴다/안 쓴다」는 가이드 몫 |
| 2 | `V3-38` 「기타 색상」 | `color_ext` 는 판정 축이라 사전에 넣으면 `pending` 이 되어 판정이 막힙니다 (STEP 45) |
| 3 | `V11-12` `/reports` | 라우팅 표에 두 줄이 없습니다. 개발측이 `docs/` 를 안 고칩니다 (규칙 2) |
| 4 | `V1-11` | ★ **제가 죽인 실행의 옛 기록**입니다 (3절). 다음 전체 실행 뒤 사라집니다 |

### 1번은 이제 재료가 다 있습니다

```
outputs/20260818_registry32_목록.md   32줄 · 파서가 읽는 파일·줄까지
/admin/registry                      항목마다 값 분포 · 옆 필드 · 안 정하면 뭐가 막히나
                                     [쓴다] [안 쓴다] [나중에] 단추
```

구성이 규격의 예상과 **정확히 같습니다** —
`record_summary 17 · inspection_summary 8 · facet 2 · inspection 2 ·
platform_check 1 · ev_battery 1 · list 1 = 32`

---

## 3. ★★ 제가 낸 사고 하나 — 먼저 적습니다

```
마스터가 저장한 목록 7쪽을 처리하려고
run_recalc(reason="listing_updated", scope="all") 을 돌렸습니다
★ scope="all" 이라 상세 31,995건을 다시 받기 시작했습니다
알아챈 때   4분 (S5 846/31995) · 바로 죽였습니다
남은 것     엔카에 2,744건을 더 던졌습니다 (원문은 P3 라 그대로 둡니다)
```

```
제대로 된 길   python3.11 run.py collect --only S4
             요청 0건 · 4.6초 · core_listing 7,809 → 8,055 · V1-21 7 → 0
```

**고친 것 셋**

```
① 뒤이은 S7 은 「1조합」임을 먼저 세고 돌렸습니다
② STEP 48 — 끊겨도 단계 리포트가 남게 했습니다 (V1-11 이 잡은 그것)
③ /tmp 가 100% 로 차 명령이 죽었습니다 — 임시 DB 2,155개.
   죽은 실행의 잔해도 치우게 했습니다 (S29)
```

---

## 4. 만든 것 — 개정별

| 개정 | 무엇 | 검사 |
|---|---|---|
| 375 | 상세에 사진 전부 (480 + 128 · **JS 없이** `:target`) | `V11-132` |
| 376 | 카드·행 전체 상세 링크 | `V11-116` |
| 378 | 「받은 원문」 절 — 파서 없어도 · **비어 있어도** 낸다 | `V11-134·135·136` |
| 380 | 배터리 SOH **가점** (+30) · 분모 안 늘림 | `V3-58·59·72` |
| 381 | 옵션 기준값 차종 단위 — **이미 그랬습니다** (확인만) | — |
| 382 | 트림을 **신차가 비율**로 (순위 → 값) | `V3-75` |
| 390 | 판정 막는 32건 **목록** | `V4-30` |
| 391 | 쿼리 오류를 **갈라** 던진다 | `V10-33~36` |
| 395 | 조각 실패 때 **서버 문구**를 낸다 | `V11-149` |
| 367 | 미분류에 판단 재료 다섯 + 단추 셋 | `V4-28·29` |
| 357 | 리포트 팝업 `/reports` | `V11-122` |
| 353 | 사이트별 구매비용 | `V11-120·121` |
| 362 | 진행 메모 (연락함·보러 감·끝) | `V7-15` |
| 306 | `site_unavailable` · 축×사이트 · 정렬 우선 · 거르기 단추 | `V9-01·02·09` |
| 307 | 응답에 새 토큰 · 403·502 안내 | `V11-99` |
| 61-web | 비교는 **옵션 차이만** | `V11-102` |
| 41-view | 세 값을 한 셀에 (표시가·현금·월) | — |
| a-auth | 관리자가 **잠금을 화면에서** 푼다 | — |
| STEP 48 | 끊겨도 단계 리포트 | `V1-11` |
| S29 | 죽은 실행의 임시 DB 도 치운다 | — |

---

## 5. ★ 검사가 옛 거짓을 여섯 번 잡았습니다

**「없는 결함을 있다」거나 「있는 결함을 없다」고 하던 것들입니다.**

| 검사 | 무엇이 거짓이었나 |
|---|---|
| `V1-16` | 누적 검사에 「이번 실행」 규칙을 걸어 여덟 달 fatal |
| `V2-01` | 누적 원문과 현재 상태를 견줌 — 24건은 팔려서 내려간 차였다 |
| `V2-18` | 「옛 판」과 「펼친 적 없음」을 섞음 · `MAX(parse_version)` 함정 |
| `V3-38` | `source_endpoint` 라벨을 보고 「facet 에 없다」 — 값으로 견주니 0건 |
| **`V11-80`** | ★ **죽은 CSS 를 읽고 통과** — 검사는 128px, 화면은 96px |
| **`V10-26`** | ★ 큐 60건 전부 실패를 **초록불**로 덮음 |

★ **`V11-80` 은 `V11-77`(미리보기에 사진이 안 뜸)을 고치다 우연히 드러났습니다.**
「선언과 실제의 괴리」가 검사 쪽에도 있었습니다.

---

## 6. ★★ 자동화가 한 번도 안 돌았습니다

```
recalc_job 60건 — 성공 0건
원인   run_recalc → run_pipeline 에 done 을 안 넘긴다.
      done=set() 으로 시작하니 S5 의 선행 S4 가 영원히 「미완료」
      ★ diagnose() 는 처음부터 completed_steps 를 썼다.  여기만 빠져 있었다
확인   사본 DB 로 끝까지 돌렸습니다 — S6 ok 49,327 · S9 85,368 · S10 3,557
```

★ **어제 「S4 를 고쳤다」고 보고했는데 원인이 다른 데 있었습니다 (규칙 4 위반).**
이번엔 사본으로 돌려서 확인했습니다.

---

## 7. 되살림 시험이 제 검사의 거짓을 잡은 것

```
V11-134   절을 지웠는데 ① HTML 주석의 「받은 원문」으로 통과
          ② 걷어냈더니 「안 받은 원문이 있으면」이라는 딴 문장에 통과
          → <h2> 제목으로 보게 고쳤습니다
V7-15     되살린 결함 앞에서 검사가 통째로 죽었습니다
          ★ 「죽는 검사」는 「없는 검사」와 같습니다
V10-33    제 기대가 규격 표와 달랐습니다 — DELETE 를 오타로 분류했습니다
```

**★ 있는 것을 있다고 해도 근거가 틀리면 거짓입니다.**

---

## 8. 규격에 관해 여쭐 것

### ① `docs/trace/` 소스 칸은 **기계가 추정한 것**입니다

```
단정인 것   「미구현」 뿐입니다 — 「그 층에서 못 찾았다」는 사실입니다
추정인 것   나머지 616건.  낱말·STEP 번호·힌트로 찾은 것입니다
```

`config/trace_hints.json` 에 **제가 확인한 111건**을 적었습니다.
「적은 것은 내가 확인했다는 뜻」이고, 확인 안 한 것은 안 적었습니다.
**틀린 것을 짚어 주시면 규칙을 고치겠습니다.**

### ② 남은 미구현 1건 — `OP-124`

```
「요구사항을 규격에서 역으로 뽑는다」
★ 가이드가 하는 일입니다.  개발측 산출물이 없어 소스 칸을 억지로 안 채웠습니다
```

### ③ `AD-119` 「비밀번호 8자」는 되돌리지 않았습니다

```
마스터 지시 「그냥 제한 없애」로 min_secret_length=1 입니다 (개정 359).
되돌릴 값과 이유는 outputs/LATER.md 가 갖습니다.
★ 여기서 8 로 되돌리면 마스터 지시를 어기는 것입니다
```

---

## 9. 다음 순서 (`inbox/ORDER_00_순서.md`)

```
끝냄   1  V11-149  조각 실패 때 서버 문구
      2  V10-33~36  쿼리 오류 분류
      3  V4-30      등록부 32건 목록
남음   4  V11-147·148  조각 절단점 · 조각별 해시
      5  run_tests 실패 2건 (test_endtoend · test_web)
      6  396  LABELS·MENU_TIPS 에 /reports · V11-54 확장 · V11-150
      7  397  층 29건 재실행 (★ 유지 6건은 미구현으로 둔다)
      8  398  「기타 색상」 other·confirmed · V3-78
      9  V3-76·V3-77  배점 갈래 합 대조 (★ 코드는 안 고친다.  검사만)
```

---

## 10. 오늘 기록 (자세한 것)

```
v166  순서 1~7 점검 · 화면이 옛 배점(605/555)을 말하던 것 · inbox 정리
v167  ★ 자동화가 한 번도 안 돌았다
v168  추적표 채움 (소스 16 → 849)
v169  층별 소스 재확인 (미확인 374 → 0)
v170  미개발 계속 · ★ 내가 낸 사고
v171  미개발 완료 (151 → 1)
v172  이 문서
```
