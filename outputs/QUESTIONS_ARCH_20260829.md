# 설계도 물음 — `docs/ARCHITECTURE_20260829.md` (SPEC-2026.08.29-r929)

```
읽은 것   docs/ARCHITECTURE_20260829.md 357줄 전체
낸 것     물음 58개.  ★ 한 번에 다 적었다 (8장 ③ — 회차마다 나눠 묻지 않는다)
안 한 것  코드 · 표(DB) · 파일 옮기기 — ★ 하나도 안 했다 (8장 금지)
```

★ 물음마다 **지금 코드·DB 의 실제 값**을 적었다. 짐작이 아니라 잰 것이다.

---

## ★ 먼저 — 설계도가 「아직 안 쟀다」고 한 것 셋을 재서 드립니다

| 설계도 자리 | 물은 것 | ★ 실측 08-30 |
|---|---|---|
| 9-3 #21 | `raw_response` 가 몇 MB 인가 | **194,583행 · `body` 합계 258.7MB** (zlib 압축 뒤. 상세 179.6 · 점검 24.2 · 목록 14.8) · DB 파일 전체 **655MB** |
| 9-4 ③ | `outputs/` 가 얼마인가 | ★★ **12GB**. 그중 `outputs/check-tmp` 하나가 **12GB · 8,861개** |
| 9-4 ④ | 파일 하루치 MB | ★ **못 쟀다** — 파일 꼴이 안 정해져 못 낸다 (물음 #7·#8 이 그것이다) |

★★★ `outputs/check-tmp` 12GB 는 **검사기가 흘린 것**입니다.
`tools/run_tests.py:_sweep_temp` 이 `tempfile.gettempdir()`(=`/tmp`)만 치우는데,
검사기 넷(`v1_collect`·`v3_logic`·`v10_admin`·`v11_web`)은 `outputs/check-tmp` 에 씁니다.
**아무도 안 치웁니다.** 하루 지난 것이 9,180개입니다.
지금 디스크는 30G 중 **7.3G** 남았습니다 — 물음 #57 을 먼저 봐 주십시오.

---

## 물음 58개

| # | 어느 장 | 물음 | 왜 물어야 하나 | 답이 없으면 어떻게 되나 |
|:--:|---|---|---|---|
| 1 | 2장 · 7-2 | 이 설계는 08-29 에 넣은 **「받기와 넣기 가르기」**(개정 857 · `S46-126`)를 **대신하는 것**입니까, 그 위에 얹는 것입니까 | KB·리본카·보배·현대·K카가 이미 그 꼴로 돕니다. 대신하는 것이면 그 코드를 걷어내야 하고, 얹는 것이면 두 꼴이 공존합니다 | 걷어낼지 남길지 몰라 **아홉 수집기를 못 건드립니다** |
| 2 | 2장 | 아홉 수집기를 **다시 씁니까**, 지금 것을 감싸 파일만 떨어뜨립니까 | 지금 수집기는 「받아서 곧바로 `upsert_core`」 꼴입니다. 파일로 가르면 각 파일의 절반이 바뀝니다 | 일의 크기가 10배 차이납니다 (감싸기 2일 vs 다시 쓰기 2주) |
| 3 | 2장 | 지금 세워 둔 검사 넷 — `S46-117`(gone) · `S46-124`(PRAGMA) · `S46-126`(통신·sleep 트랜잭션 밖) · `S46-128`(커밋 뒤 창) — 은 **새 걸음에서도 그대로 삽니까** | 넷 다 「수집기가 DB 를 어떻게 만지나」를 봅니다. 1걸음이 DB 를 안 열면 셋은 뜻이 없어집니다 | 검사가 **늘 통과하는 죽은 검사**가 되고 아무도 모릅니다 |
| 4 | 2장 · 6장 | 이 구조를 넣는 동안 **지금 수집을 멈춥니까**, 나란히 돌립니까 | 나란히 돌리면 같은 매물을 두 길(옛 꼴 · 새 꼴)이 함께 씁니다 | 값이 어느 쪽에서 왔는지 못 가르고, 틀리면 **어느 길이 틀렸는지 못 가릅니다** |
| 5 | 6장 | 7단계를 **사이트 하나씩** 갑니까, 아홉을 한꺼번에 갑니까 | 「⑤ 맞대기」는 사이트마다 목록이 주는 칸이 달라 조건이 다릅니다 | 한꺼번에 가면 한 사이트가 막힐 때 **아홉이 다 멈춥니다** |
| 6 | 7-5 | `raw/` 를 **어디에 둡니까** — `~/v2/raw/` 인가, 다른 디스크인가 | 지금 루트가 30G 중 **7.3G** 남았습니다. 미확정 22 가 「안 지운다」이므로 날마다 늡니다 | 자리를 안 정하면 **넣자마자 디스크가 찹니다** |
| 7 | 7-5 · 9-3 #22 | 파일을 **압축합니까** | 지금 `raw_response.body` 는 zlib 로 눌러 258.7MB 입니다 (`store/raw.pack_body` · `CWZ1`). 안 누르면 몇 배가 됩니다 | 하루치 MB(9-4 ④)를 **못 냅니다.** 보관 기간도 못 정합니다 |
| 8 | 7-5 | 한 파일에 **한 매물**입니까, 한 파일에 **여러 매물**입니까 | 2장은 `{source_id}.json` 이라 매물 하나입니다. 상세 24,717건이면 파일 24,717개 — 한 폴더에 수만 개가 들어갑니다 | ext4 에서 한 폴더 파일 수가 늘면 **읽기가 느려집니다.** 하위 폴더를 나눌지 정해야 합니다 |
| 9 | 7-5 | 같은 날 **같은 매물을 두 번** 받으면 — 덮어씁니까, `{source_id}-2.json` 입니까 | KB 는 회차를 나눠 받고, 마스터께서 「엔카 받기」를 하루에 여러 번 누르실 수 있습니다 | 덮어쓰면 **앞 원문을 잃습니다** (P3 「원문 무손실」에 어긋납니다) |
| 10 | 7-5 | `page-{NNNN}` 의 번호는 **무엇 안에서** 셉니까 | KB 는 차종군 **31묶음**, 현대는 **6묶음**을 따로 부릅니다. 묶음마다 1쪽이 있습니다 | 한 이름칸을 쓰면 **31개 묶음의 1쪽이 서로 덮어씁니다** |
| 11 | 7-5 | 쪽이 **없는** 목록(렉서스 `cur_page` · K카 재고목록 한 번 호출)은 파일 이름을 어떻게 합니까 | 이 둘은 「쪽」이 아니라 한 덩이입니다 | 이름 규칙이 없어 **사이트마다 제멋대로 됩니다** — 오판 193 과 같은 무늬 |
| 12 | 2장 | 파일 안의 `status` 는 **HTTP 코드**입니까, 우리 판정(`ok`·`walled`·`empty`)입니까 | KB 는 200 을 주면서 봇차단 본문을 줍니다 (2,759B). K카는 없는 매물에도 200 을 줍니다 | 200 만 적으면 **막힌 것과 받은 것을 못 가릅니다** |
| 13 | 2장 | **못 받은 것**(3회 다 막힘)도 파일로 남깁니까 | 지금 규칙은 「없음으로 저장하지 않는다」(금지 12)라 `raw_response` 에 **안 넣습니다** | 안 남기면 「몇 번 막혔나」가 사라집니다. 남기면 `load_log` 가 그것도 세야 합니다 |
| 14 | 2장 | 파일 쓰기가 **중간에 죽으면** 어떻게 압니까 (임시 이름 → 옮기기를 씁니까) | 반만 쓰인 JSON 을 2걸음이 읽으면 파싱이 죽습니다 | **반쪽 파일이 조용히 섞입니다** — v1 사고와 같은 무늬 |
| 15 | 7-2 | 엔카 목록은 마스터께서 브라우저로 받아 `POST /admin/collect` 로 보내십니다. 그 처리기가 **파일도 씁니까**, `raw_response` 만 씁니까 | 지금은 `store/adminops.save_browser_catch` 가 DB 에만 넣습니다. 조각 전송(`chunk_key`)도 거기서 이어붙입니다 | 엔카만 옛 꼴로 남아 **「열 사이트가 같은 걸음」(오판 193)이 다시 깨집니다** |
| 16 | 7-2 | `origin`(`collector`·`browser`·`import`)은 파일 **안**에만 적습니까, 파일 **경로**에도 적습니까 | 경로에 없으면 「브라우저로 받은 것만 다시 보기」를 하려면 파일을 다 열어야 합니다 | 나중에 갈래로 못 찾습니다 |
| 17 | 2장 | `stage_listing` 은 `core_listing` 의 **119칸을 다 갖습니까** | `core_listing` 은 지금 119칸입니다. 다 만들면 표가 둘로 갈려 한쪽만 고치는 일이 생깁니다 | 칸이 모자라면 **맞대기에서 그 칸은 영영 안 바뀝니다** |
| 18 | 2장 | `stage_listing` 에 **번호판·연락처(PII)** 가 들어갑니까 | 지금은 `store/core.split_pii` 가 `core_pii` 로 갈라 넣고 `core_listing` 에는 `plate_hash` 만 둡니다 (STEP 35) | 임시표에 원본이 들어가면 **PII 규칙이 뚫립니다** |
| 19 | 2장 | 상세가 만드는 **자식 표**(`core_record`·`core_inspection`·`core_diagnosis`·`core_diagnosis_item`)도 임시표를 둡니까 | 상세 하나가 자식 행 여럿을 냅니다. 임시표가 매물만 담으면 자식은 어디로 갑니까 | 자식은 **옛 꼴로 곧바로 들어가** 「임시표만 버리면 된다」(2장 까닭 ②)가 깨집니다 |
| 20 | 2장 · 7-3 | 두 사이트를 **동시에** 돌리면 임시표를 같이 씁니까 | 7-3 은 「그 `run_id` 것을 지운다」라 함께 쓸 수 있게 보입니다 | 한쪽이 끝나며 지울 때 **다른 쪽 것을 지울 수 있습니다** |
| 21 | 7-3 | 「다음 회차가 시작할 때 남은 것이 있으면 먼저 지운다」 — **도는 중인 다른 회차 것**은 어떻게 가릅니까 | 시각으로 가릅니까, 잠금 파일로 가릅니까 | 돌고 있는 회차의 임시표를 **지워 버립니다** |
| 22 | 3걸음 | 「값이 다르다」의 **값이 어느 칸입니까** | `core_listing` 119칸 중 `last_seen`·`collected_at`·`parsed_at` 은 회차마다 바뀝니다 | 다 대보면 **전건이 늘 「바뀜」**이 되어 3걸음이 아무것도 안 거릅니다 |
| 23 | 3걸음 | 목록이 **적게 주는 사이트**는 무엇으로 「같다」를 봅니까 | KB 목록은 매물번호(`carSeq`)뿐입니다 — 값·주행이 없습니다. 리본카 사이트맵도 매물번호뿐입니다 | 「값이 같다」를 못 재어 **KB·리본카는 늘 전건 상세**를 받습니다 — 7-2 가 노린 효과가 안 납니다 |
| 24 | 3걸음 | 리본카 사이트맵의 `lastmod` 를 「바뀌었나」의 근거로 씁니까 (가이드 몫 6건 중 하나) | 사이트맵에 `lastmod` 가 있으면 #23 이 리본카에서는 풀립니다 | 리본카 1,082건을 **날마다 다 받습니다** |
| 25 | 3걸음 | 「DB 에 있고 목록에 없다 → 상태를 바꾼다」에 **「끝까지 받았나」 가드**가 들어갑니까 | `S46-117`·`sweep_gone_groups` 가 그것입니다. 반만 받고 매기면 산 차를 죽입니다 (오판 161) | **08-29 에 고친 것이 되돌아갑니다** — 렉서스 38건이 다시 죽습니다 |
| 26 | 3걸음 | 「받을 것 목록」 파일(`_todo`)은 **누가 언제 지웁니까** | 날마다 쌓이면 그것도 공간입니다 | 9-4 ④ 에 이 몫이 안 들어갑니다 |
| 27 | 3걸음 | 3걸음이 낸 「받을 것」이 **너무 많으면**(첫 바퀴 KB 4,320건) 회차를 어떻게 나눕니까 | 지금은 `DETAIL_BATCH=10` · `BURSTS=4` 로 하루 40건입니다 (봇차단 실측 — 20건이면 40% 막힘) | 「첫 바퀴만 크다」(7-2)가 **KB 에서는 108일**입니다 |
| 28 | 4걸음 | 「바뀐 것만 재판정」 — **시세**는 어떻게 합니까 | 등급은 그 차종 시세와 견줘 냅니다. 한 대가 바뀌면 그 차종의 시세가 바뀌고 **안 바뀐 차의 등급도 달라집니다** | 화면에 **서로 다른 기준으로 매긴 등급이 섞입니다** (V2-01 이 잡는 그것) |
| 29 | 4걸음 | 그러면 **언제 전건을 다시 돕니까** — 날마다 한 번입니까, 시세가 N% 움직일 때입니까 | 지금은 날마다 13시에 전건입니다 (`carwatch-daily`) | 「전건을 안 돈다」와 「기준이 같아야 한다」가 **서로 부딪힌 채 남습니다** |
| 30 | 3장 | `sales_status` 에 「사이트가 준 말 그대로」를 넣으면, **목록 화면이 무엇을 숨길지**는 누가 정합니까 | 지금 DB 에 이미 `CONTRACT` 830 · `RESERVED` 98 · `listed` 226 · `reserved` 46 이 섞여 있습니다 (대문자·소문자가 갈려 있습니다) | 낱말표(미확정 20 · G17)가 없으면 **화면이 무엇을 숨길지 못 정합니다** |
| 31 | 3장 | 08-29 에 마스터께서 「팔린 것을 **목록에 두고** 딱지만 · 흐리게 · 정렬 맨 뒤」라 하셨고 그렇게 넣었습니다. 이 설계는 「**안 뿌린다**」입니다 — **어느 쪽입니까** | 정면으로 어긋납니다. `ORDER_SOLD`·`hide_sold`·`.row.sold` 가 이미 들어가 있습니다 | 되돌릴지 둘지 몰라 **화면을 못 건드립니다** |
| 32 | 3장 | 「팔린 차」 새 화면의 **경로 이름**은 무엇입니까 (`/sold`?) | `web/routes.py` 와 `docs/chapters/61-web.md` 라우팅 표에 넣어야 합니다. 규격이 정본입니다 (규칙 2) | 개발측이 지어 넣으면 **규격과 어긋납니다** |
| 33 | 3장 | 「어떤 가격일 때 잘 팔렸나」의 **잘 팔렸다**가 무엇입니까 | 빨리 팔린 것입니까(며칠), 처음 값에 가깝게 팔린 것입니까(할인율), 둘 다입니까 | 통계를 **못 만듭니다** — 5장이 「개발이 안 나왔다」고 적은 그 자리입니다 |
| 34 | 3장 | 「팔린 값」을 무엇으로 봅니까 | 사이트는 **판 값을 안 알려 줍니다.** 우리가 아는 것은 「사라지기 직전 값」(`last_price_won`)뿐입니다 | 「얼마에 팔렸나」를 **모르는데 낸 것**이 됩니다 — 선언과 실제의 괴리 |
| 35 | 3장 | 계약 중(`CONTRACT` 830건)은 **팔린 것**입니까, **아직 사는 것**입니까 | 계약은 깨질 수 있습니다. 지금은 목록에서 빼고 있습니다 | 통계의 분모가 흔들립니다 |
| 36 | 7-4 | `file_hash` 는 **무엇으로** 냅니까 (sha256?) · 압축 **전**입니까 **후**입니까 | #7 에서 압축을 정하면 여기가 따라옵니다 | 다시 받은 파일이 같은지 **못 가립니다** |
| 37 | 7-4 | 「이미 있으면 안 넣는다」인데, **파서를 고쳐 다시 뽑아야 할 때**는 어떻게 합니까 | 파일은 그대로인데 파서만 바뀝니다. 파서 결함 여섯을 고치면 **다 다시 뽑아야 합니다** | `load_log` 가 막아 **고친 파서가 옛 파일에 안 걸립니다** |
| 38 | 7-4 | `load_log` 에 `parse_version` 을 둡니까 | #37 의 답이 그것으로 보입니다만 설계도에는 없습니다 | 「두 번 넣지 않는다」와 「고쳐서 다시 뽑는다」가 **부딪힙니다** |
| 39 | 7-4 | `load_log` 는 **얼마나 오래** 둡니까 | 파일 하나에 한 줄이면 상세만 24,717줄로 시작해 날마다 늡니다 | 이것도 공간입니다 (9-4 에 안 들어 있습니다) |
| 40 | 2장 · 4장 | 「가이드가 임시표로 검증한다」(2장·4장) 인데, 9-1 은 「가이드가 **DB 를 못 연다**」고 적었습니다. **임시표는 DB 안에 있습니다** | 서로 어긋납니다 | 가이드가 검증을 **못 합니다** — 이 설계의 가장 큰 값(4장 「검증」 줄)이 안 생깁니다 |
| 41 | 2장 | 위가 「임시표를 **파일로 떠서** 드린다」는 뜻이면, 그 **꼴**(CSV? JSONL?)과 **누가 언제** 뜨는지 | 4걸음 검증이 그것에 달려 있습니다 | 검증 자리가 **비어 있습니다** |
| 42 | 6장 ⑥ | `raw_response` 를 지울 때 — **행 194,583개를 다 파일로 뺍니까**, 일정 날짜 뒤엣것만 뺍니까 | 258.7MB(압축)입니다. 다 빼면 파일이 그만큼 생기고 디스크가 그만큼 줍니다 (#6) | 옮기다 **디스크가 찹니다** |
| 43 | 6장 ⑥ | 「옮기고 **대조**」의 대조가 무엇입니까 — 바이트 같음입니까, 파싱 결과 같음입니까 | 08-28 압축 때는 **행마다 왕복 검증**을 했습니다 (186,547행 바이트 일치) | 대조 없이 지우면 **원문을 영영 잃습니다** (P3 위반) |
| 44 | 6장 ⑥ | `source_id` 가 **없는** 원문(목록 5,926행 · facet 213행)은 파일 이름을 무엇으로 합니까 | 2장 꼴이 `{source_id}.json` 입니다 | 목록 원문 5,926건을 **못 옮깁니다** |
| 45 | 4장 | 「상세 받기 — 전건을 다 받는다 → 새 것·바뀐 것만」이 **볼보·BMW 에도** 걸립니까 | 이 둘은 지금 상세를 **한 번도 안 받았습니다** — 첫 바퀴는 전건입니다 (볼보 157 · BMW 376) | 첫 바퀴 크기를 못 잡습니다 |
| 46 | 7-1 | 렉서스 상세를 **재 봅니까** (설계도 「아직 안 쟀다」) | 목록이 값·주행·연식·신차가를 다 주므로, 상세가 더 주는 것이 없으면 안 받아도 됩니다 | 「필요한가」가 빈 채로 남습니다 |
| 47 | 7-1 | 리본카가 `detail_urls` 를 안 쓰고 주소를 직접 만드는 것 — **고칩니까** | 설계도가 「우리가 안 만든 것」이라 적었습니다 | 어댑터 계약이 사이트마다 다른 채 남습니다 |
| 48 | 7-1 | 기아 CPO 는 「지금은 안 받는다」인데, **목록도 안 받습니까** | 지금은 목록을 받아 1,025건을 저장하고 있습니다 (대상 0건) | 받는지 마는지 몰라 **하루치에서 뺄지 못 정합니다** |
| 49 | 9-3 #23 | 성능점검 여섯 축을 ⑥(80%)로 볼지 — **마스터께서 정하실 것**인데, 그때까지 0점입니다 | 지금 이것 때문에 등급이 낮은 매물이 있습니다 | 파서를 고쳐도 **점수가 안 오릅니다** |
| 50 | 9-3 #24 | 전기차 누유 축 — 「그 차에 없는 것」 갈래를 **새로 만듭니까** | 지금은 「우연이 정한다」고 적혀 있습니다. 분모가 흔들립니다 | 전기차 등급이 **까닭 없이 낮게 남습니다** |
| 51 | 9-1 | 열한 건이 「파일이 나오면 풀린다」인데, **누가 풉니까** — 가이드입니까 개발측입니까 | #40 이 안 풀리면 가이드가 못 봅니다 | 파일만 쌓이고 **아무도 안 엽니다** |
| 52 | 1장 | 파서 결함 여섯(BMW 값·주행·연식 · 보배 연식 · 보배 신차가 · KB 성능점검 · K카 점검경로 · 볼보 상세)을 **③걸음에서 함께** 고친다 하셨는데, **구조 앞에** 고칠 수는 없습니까 | 지금도 값이 틀리게 화면에 나갑니다. 구조는 몇 주가 걸립니다 | 마스터께서 **몇 주 동안 틀린 값을 보십니다** |
| 53 | 6장 | 이 일이 도는 동안 **날마다 수집을 계속합니까** | 멈추면 가격 변동이 끊깁니다. 계속하면 옛 꼴 코드를 못 걷어냅니다 | #4 와 같은 자리입니다 |
| 54 | 4장 | `load_log` 와 `core_listing.first_seen`/`last_seen` 을 「섞지 않는다」 하셨는데, **`raw_response.fetched_at`** 은 어떻게 됩니까 | `raw_response` 를 지우면 그 시각도 사라집니다. 지금 「받은 때」의 정본입니다 | 「언제 받았나」의 정본이 **없어집니다** |
| 55 | 2장 | `stage_listing` 은 **어느 DB** 에 둡니까 — `carwatch.db` 입니까, 따로입니까 | 같은 파일에 두면 임시표 쓰기가 **판정과 잠금을 다툽니다**. 08-29 실측 — 재판정이 588초 중 559초(95%) 쓰기 잠금을 쥡니다 | 임시표 넣기가 `database is locked` 로 죽습니다 — 08-29 에 수집기 넷이 그렇게 죽었습니다 |
| 56 | 전체 | 이 설계에 **`S46-151`** 이 걸려 있습니다(머리말). 그 검사가 **무엇을 잽니까** | 개발측이 만들 것이면 무엇을 재는지 알아야 합니다 | 검사를 **지어 만들게 됩니다** (규칙 2 위반) |
| 57 | 9-4 ③ | ★★ `outputs/check-tmp` **12GB · 8,861개**를 **지워도 됩니까** | 검사기 넷이 만들고 아무도 안 치웁니다 (`run_tests._sweep_temp` 은 `/tmp` 만 봅니다). 디스크는 30G 중 **7.3G** 남았습니다. 검사를 돌릴 때마다 늡니다 | 디스크가 차면 **DB 쓰기가 죽고 마스터 화면이 멈춥니다.** 파일 구조를 넣을 자리도 없습니다 |
| 58 | 9-4 | 위를 고칠 때 `_sweep_temp` 이 `outputs/check-tmp` **도 치우게** 고쳐도 됩니까 | 지금 구조와 무관한 한 줄짜리 고침입니다만, 「수집·적재 구조를 손대지 마라」에 걸리는지 몰라 여쭙니다 | 지워도 **다음 검사부터 다시 쌓입니다** |

---

## ★ 물음 밖 — 설계도를 읽다 눈에 걸린 것 셋

```
① 4장 표 「상세 받기 — 지금: 전건을 다 받는다」
   ★ 지금 코드는 그렇지 않습니다.  ★ 아홉 중 여섯이 「이미 받은 것은 건너뛴다」입니다
     (KB·리본카는 08-29 에 ★ 원문이 있나로 가르도록 고쳤습니다 — 개정 857).
   ★ 다만 ★ 「바뀌었나」로는 안 가릅니다 — ★ 그 점에서 설계도의 뜻은 맞습니다

② 7-1 표 「기아 CPO — 우리 대상이 0건이라 지금은 안 받는다」
   ★ 지금은 ★ 목록을 받아 ★ 1,025건을 저장하고 있습니다 (대상 0건).
   ★ 「안 받는다」가 ★ 목록까지인지 상세만인지가 물음 #48 입니다

③ 9-2 「18 outputs/ 보관 정책 → 이 설계가 바꾼다」
   ★ 위에 적은 대로 ★ 12GB 중 ★ 12GB 가 ★ 검사 임시물입니다 —
   ★ ★ 「회차 기록이 쌓인다」가 아니라 ★ **검사기가 흘리는 것**입니다.
   ★ ★ `outputs/*.md` 회차 기록은 ★ 234개 · 합쳐서 몇 MB 밖에 안 됩니다
```
