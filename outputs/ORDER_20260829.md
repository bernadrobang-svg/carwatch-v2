# 오늘 할 것 (SPEC-2026.08.29-r941)

```
네가 잰 것 — save_listing_pii ★ 267회 · 합계 11ms · 한 번에 최대 0.1ms
             267/267 이 트랜잭션 ★ 밖 · 이번엔 재판정이 0/298초
★★ ★ **0.1ms 는 잠금을 만들 수 없다.  ★ 네 판단이 맞다** — 그 함수는 까닭이 아니다
★ ★ 그리고 ★ **타이머 셋을 찾은 것이 이번 회차의 값**이다
```

★ **답 — 2 로 간다.  ★ 다만 1 도 함께 한다.  ★ 둘 다 필요하다**

```
왜 2 인가
  ★ `CARWATCH_DEFER_RECALC` 는 ★ **이미 있다** — `daily_collect.py:50` 이 켜고
    `daily_enqueue.py:87` 이 본다.  ★ **새 규칙이 아니라 있는 것을 넓히는 것**이다
  ★ ★ 시각만 옮기면 ★ **마스터가 홈에서 「다시 판정」을 누르시는 순간 또 겹친다** —
    ★ 시각으로는 ★ 사람이 누르는 것을 못 막는다
  ★ ★ 그러므로 ★ **수집기가 도는 동안 재판정을 미루는 것**이 뿌리다

왜 1 도 하나
  ★ 13시가 ★ `daily`(수집)와 `recalc-catchup`(재판정) ★ **둘 다**다 [실측 08-29]
  ★ ★ 2 를 넣어도 ★ 같은 시각에 시작하면 ★ 한쪽이 늘 헛돈다 — ★ 겹치지 않게 옮긴다
  ★ ★ `check-light` 는 짝수시라 ★ 안 겹친다.  ★ 그대로 둔다
```

**할 것**
```
① recalc-catchup 을 ★ 13시에서 옮긴다 (01·05·09·★ 14·17·21 처럼)
   ★ 옮긴 뒤 ★ 두 타이머의 OnCalendar 을 나란히 적어 보고해 달라
② CARWATCH_DEFER_RECALC 를 ★ 타이머에도 준다 —
   ★ `recalc_catchup.py` 가 ★ **수집기가 도는지 보고 돌면 미룬다**
   ★ ★ 「도는지」는 ★ `collect_run` 표로 본다 (시각 짐작 말고 ★ 상태로)
③ 넣은 뒤 ★ 한 바퀴 돌려 ★ locked 가 몇 건인지 세어 달라 (지금 0 이면 0 이라 적어라)
```

★ **`busy_timeout` 을 올리지 마라** — 네가 안 올린 것이 옳다.

## ★★★★★ 0순위 b — **K카가 152 → 6 으로 떨어졌다** [가이드 실측 08-29]

```
08-29 낮   K카 152건 · A+ 0
08-29 지금 K카 ★ **6건**
  ★ `?site=kcar&hide_sold=0` 도 ★ 6 이다 — ★ `/sold` 로 간 것이 아니다
  ★ `/sold` 는 1,226건 · 전체는 5,785 → ★ **5,620** 으로 165 줄었다
```
★ **먼저 재라.  고치지 마라.**
① K카 `sweep_gone` 이 이번 회차에 **몇 건을 `gone` 으로 바꿨나**
② 그 근거가 된 목록이 **몇 건이었나** — `stockCar/list` 는 487건을 준다
③ **「끝까지 받았나」 가드가 걸렸나** — 반만 받고 나머지를 죽였을 수 있다 (오판 161·173 자리)
★★ **살아 있는 매물을 죽였으면 되돌려야 한다.**  ★ 원문은 남아 있으니 다시 살릴 수 있다.

## ★ sweep 을 끈 다섯 곳에 **까닭을 적어 달라** (`S46-117`)
★ **끈 것은 옳다.** 다만 지금 검사에는 「안 거른다」로만 보여 **왜 껐는지 알 수 없다.**
```
끈 파일 머리에 한 줄 —
  SWEEP_OFF = "08-29 — 목록에 없다고 죽이면 살아 있는 차를 죽인다 (11-store/a-key 08-29 절).
               상세로 확인한 뒤 죽이는 꼴로 바꾼 뒤 다시 켠다"
★ 낱말 `SWEEP_OFF` 를 그대로 쓴다 — 검사가 그것을 본다
```

## ★★★ 렉서스 gone 3건 — **가이드가 답한다: 상세가 있다**

★ **짐작으로 안 되돌린 것이 옳다.** 그리고 ★ **상세는 있다** — 내가 다섯 이름을 두드렸다.
```
/api/json/getData_car_detail.json.php?idx={idx}   → ★ 200 · 12,585B
getView_car · getData_car · /car/view · getDetail  → ★ 다 406
★ 살아 있다 → 키에 `car_detail` 이 있다 (12,585B)
★ 없다     → ★ 200 인데 71B · `car_detail` 이 없다
★★ 200 으로 가르지 마라 — 없는 것도 200 을 준다 (K카와 같은 꼴)
```
★ **그 셋을 눌러 보고, 살아 있으면 되돌려라.** 규격 `LEXUS_CERTIFIED_API` 08-29 절.
★ 규격 1c 「목록이 매물의 전부다」는 **물린다** — 상세가 있다. 목록 전량은 **73건**.

## ★★★★★ 0c — **같은 함정이 아홉 곳에 있다** (0a 를 넓힌다)

★ **잘 찾았다.** 「끝까지 받았나」 가드는 ★ **「이 창구를 끝까지 받았나」**이지
★ **「이 창구가 전량인가」가 아니다.** K카는 그래서 살아 있는 12대를 죽였다.

★ 그런데 ★ `sweep_gone` 을 부르는 수집기가 ★ **아홉이다** —
`bmw` · `bobaedream` · `heydealer` · `hyundai_cert` · `kbchachacha` · `kia_cpo` · `lexus` · `reborncar` · `volvo`

**할 것 — 재라. 고치지 마라.**
```
① 사이트마다 ★ 최근 한 바퀴에 ★ gone 이 된 것이 몇 건인가
② 그중 ★ 표본 열 건을 ★ **상세로 눌러** 살아 있는지 본다
   ★ K카처럼 ★ 「살아 있음」을 가르는 낱말이 사이트마다 다르다 — ★ 그것부터 찾아 적어라
③ ★ **살아 있는 것이 하나라도 나오면** ★ 그 사이트 sweep 을 ★ **끄고 되돌려라**
★ ★ 되돌릴 수 있다 — ★ 원문이 남아 있다 (K카에서 12대를 되살린 그대로)
```
★ 규격은 `11-store/a-key` 08-29 절에 박았다 — **목록에 없다고 바로 죽이지 않는다.**

## ★ 앞 회차 물음은 다 답했다 — **회차 표에서 지워라**
`outputs/ANSWERS_20260829_v304.md` · `TARGET_KEY_MAP` 08-29 절 · `UI_REVIEW` 30·31장.

## ★ 그다음 다섯
| | 무엇 |
|:--:|---|
| 1 | **배포에 올려라** — `watch.html:125` 는 고쳤는데 배포 `/watch` 가 옛 화면이다 (`S46-100`) |
| 2 | 신차가 「절약액」을 **남은 곳에서도** — 엔카 418 · 리본카 36 · KB 12 · 헤이딜러 9 · 렉서스 1. **뒤에 「절약·대비·싸다」가 붙으면 버린다 · 값이 현재값보다 작으면 신차가가 아니다** |
| 3 | K카 `cno` → `plate_hash` (`split_pii` 로. 번호판 원문은 `core_listing` 에 넣지 마라) |
| 4 | KB 부위별 파서 — **사고 있는 매물**을 찾아 꼴을 본 뒤에. 짐작으로 이름을 짓지 마라 |
| 5 | `V1-23` · `V2-01` |

## ★ 회차마다 이 세 수를 내라
```
① 아홉 사이트에 A 등급이 몇 건 (지금 0)
② 같은 차 짝이 몇 건 (지금 0)
③ 전기차 A 이상이 몇 건 (지금 13)
★ 이 셋이 안 움직이면 아무것도 안 바뀐 것이다
```

## 지킬 것
하나 닫고 「몇 → 몇」 적고 다음. 첫 줄은 「안 한 것」 표. 200·303 으로 세지 않는다.
「사이트가 안 준다」는 **표본 10건 전건이 빌 때만** 적고 `docs/evidence/` 에 증거를 남긴다.
남은 것은 `docs/guide/07_밀린일대장.md` · 설계 뒤에 할 것은 `docs/guide/09_설계뒤에할것_20260829.md`.

## 수집 범위
`config/targets.json` 의 26종만. 전량을 받지 않는다. 상한은 차종별 목록 끝까지.
전체 → 대상 20333 → 5770 [개발측 08-29]
가이드 실측 08-29 — KB 4,320 · 보배 222 · 볼보 223(20쪽) · 현대 1,138 · 헤이딜러 199
K카 487(이 창구) · BMW 364(38쪽 · X3 23) · 렉서스 74 · 리본카 1,082 · 엔카는 마스터 회선

## 금지
지우는 것 · 규격을 개발측이 고치는 것 · 새 색 · 클래스 이름 바꾸기 · 짐작으로 규격 적기.
★ 설계 2판 전에 수집·적재 **구조**를 손대는 것. ★ 화면은 여기 안 걸린다.

## 규격
`docs/ARCHITECTURE_20260829.md` · `docs/chapters/` 10-collect · 11-store · 41-view · 60-admin · f-table
KCAR_API · KBCHACHACHA_API · VOLVO_SELEKT_API · BOBAEDREAM_API · HEYDEALER_API · REBORNCAR_API
HYUNDAI_CERTIFIED_API · LEXUS_CERTIFIED_API · KIA_CPO_API · BMW_BPS_API · ENCAR_ROBOTS
TARGET_KEY_MAP · MULTISITE_MAPPING · CROSS_SITE_COMPARE · DEDUP_CROSS_SITE · UI_REVIEW · SERVER_SURVIVAL
