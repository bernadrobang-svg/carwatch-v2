# CarWatch v2 — 오늘 할 것 (SPEC-2026.08.29-r864)

## 마스터께서 누르실 것

홈에서 「엔카 받기」 한 번 · 「다시 판정」 한 번.
★ 볼보를 한 번 돌리면 EX30 다섯 대가 들어온다 (마스터 확정 08-29 「넣어」 · 이미 박았다).

## 개발측 물음에 답한다

개발측 — 「이어서 7 틀 39곳(`_head.html`·`_foot.html`)으로 가겠습니다」
→ **차례가 바뀌었다.** 아래 0 · 3 을 먼저 한다. 틀 39곳은 C 다.
개발측 — 「fatal 7→8 은 새로 세는 자리가 는 것과 렌더 시점 차이입니다.
다음 회차에 목록으로 짚겠습니다」 → **그렇게 한다. 재촉하지 않는다.**

## 0순위 — KB locked 가 다시 났다 (`store/pii.py:107`)

r857 은 ①(수집기 트랜잭션)만 고쳤다. 개발측이 적은 ②가 그대로다 —
「재판정이 4시간마다 타이머로 돈다 — 수집과 서로 모른다」

| | 파일 · 줄 | 무엇이 틀렸나 | 검산 |
|:--:|---|---|:--:|
| 0a | `collect/pipeline.py:365` | 이 파일에 `conn.commit()` 이 **하나**다 — 단계 끝에만 있다. `run_pipeline` 이 단계 안에서 안 끊어 S9 가 수천 행을 한 트랜잭션으로 쓴다 | — |
| 0b | `collect/worker.py:107` · `:155` | `sqlite3.connect` 를 맨으로 부른다 — `open_db` 를 안 써서 `busy_timeout=30000` 이 안 붙는다 (기본 5초). `web/app.py:405` 주석이 같은 것을 이미 적었다 | — |

**먼저 재라.** 단계 시작·끝 시각을 찍어 **한 단계가 몇 초를 쥐는지** 낸다.
30 초를 안 넘으면 0a 가 까닭이 아니다 — 그때 다시 정한다. 고치기 전에 수를 준다.

## 1순위 — 수집기가 저절로 안 돈다 (실측 08-29 · 가이드)

| | 파일 · 줄 | 무엇이 틀렸나 | 검산 |
|:--:|---|---|:--:|
| 1 | `config/cli_screens.json` pending | `collect_*.py` 열 개가 다 화면이 없다 — 마스터가 못 누르신다 | S46-127 |
| 2 | `deploy/carwatch-daily.service` ExecStart | `daily_enqueue.py` 뿐이다 — 재판정만 큐에 넣고 수집기를 안 부른다 | S46-127 |
| 3 | `tools/collect_lexus.py:41` · `:115` | 1쪽(36건)만 받고 `_done=bool(cars)` 로 `sweep_gone` 을 부른다 — 쪽넘김 열쇠는 `cur_page` 다 (`page`·`pageNo` 는 1쪽을 준다). 전수 74 · 우리 대상 53. **2·3쪽 38건이 안 팔렸는데 gone 이 된다** | S46-117 |
| 3b | `tools/collect_kcar.py:176` · `:191` | `todo` 가 `detail_status='ok'` 를 건너뛰어 **한 번 저장된 매물은 영영 `gone` 이 안 된다**. 목록 487건인데 저장 159 — 54건이 목록에 없는데 살아 있다. 마스터 08-29 「건별로 확인하지 말고 목록을 수집할 때 걸러야지」. `collect_bobaedream.py:238` 꼴로 `sweep_gone_groups` 를 부른다 | S46-117 |
| 4 | `config/endpoints.json` `lexus_certified._note` | 「전수 36건」이 틀렸다 — 74건이다 | — |
| 5 | `tools/collect_kbchachacha.py:57` `:65` | `DETAIL_BATCH=10` · `DEFAULT_BURSTS=1` — 한 회차에 상세 10건뿐. 근거였던 08-25 봇차단이 08-29 에 0건이다 (`KBCHACHACHA_API.md` 1-1b) | — |

3 · 3b 를 먼저 한다 — 렉서스는 산 차를 죽이고 K카는 팔린 차를 안 죽인다. 그다음 1·2. 5 는 봇 차단을 다시 재고 나서 값을 올린다.

## 그다음

| | 파일 · 줄 | 무엇이 틀렸나 | 검산 |
|:--:|---|---|:--:|
| A | `app.css:1646` · `listings.html:306` | 담을 `relative` 가 없어 하트 30개가 `y=4` 에 쌓인다 (`UI_REVIEW.md` 26장) | S46-118 |
| B | `build.py:998` `order_clause` | `first` 앞에 `ORDER_HEAD` 가 있다 | — |
| C | 틀 39곳 | 머리·발을 화면마다 다시 쓴다 — `_head.html`·`_foot.html` 부터 (`UI_REVIEW.md` 28장) | S46-122 |
| D | `app.css` header 23곳 | 화면마다 따로 적혀 위·아래가 바뀐다 (`UI_REVIEW.md` 27장) | S46-121 |
| E | `app.css` `:root` | 토스 스타일 값 표 그대로 (`UI_REVIEW.md` 29장) · C 와 같은 회차 | S46-123 |
| F | DB 한 줄 | `SELECT site,COUNT(*) FROM core_listing GROUP BY 1` — 아래 범위표 합이 안 맞는다 | — |

## 지킬 것

하나 닫고 「몇 → 몇」 적고 다음으로. 첫 줄은 「안 한 것」 표다.
`200`·`303` 으로 세지 않는다. 숫자를 옮겨 적지 말고 규격을 가리킨다.
남은 것은 `docs/guide/07_밀린일대장.md` 에 있다.

## 수집 범위

```
config/targets.json 의 26종만 받는다.  전량을 받지 않는다
사이트가 파는 전부가 아니다.  상한은 차종별 목록 끝까지다

전체 → 대상   18762 → 5756   [개발측 실측 08-29]
  ★ 아래 사이트별 합은 9,554 다 — 위 5,756 과 안 맞는다.  10번으로 다시 센다
  엔카 13878 → 8686 · 현대인증 1124 → 174 · 리본카 1046 → 72 · 기아 1021 → 0
  K카 604 → 34 [★ 실측 08-29 — 목록 487 · 우리 대상 105] · KB 445 → 287 · 볼보 143 → 140 · 헤이딜러 168 → 96
  보배 97 → 55 · 렉서스 36 → 10

★ 가이드 실측 08-29 — 보배 목록에 우리 차종 221건 (화면 33) · 렉서스 전수 74 · 대상 53 (화면 10)
  『BOBAEDREAM_API.md』 「얼마나 있나」 · 『LEXUS_CERTIFIED_API.md』 1c · 『KCAR_API.md』 08-29 재측을 보라
  ★ K카에 볼보는 XC60 한 대뿐이다
```

## 금지

```
지우는 것 · 규격을 개발측이 고치는 것 · 새 색을 만드는 것
200·303 으로 세는 것 · 짐작으로 규격을 적는 것 · 클래스 이름을 바꾸는 것
```

## 규격

```
docs/chapters/          10-collect · 11-store · 41-view · 60-admin
docs/ENCAR_API.md · KCAR_API.md · KBCHACHACHA_API.md · VOLVO_SELEKT_API.md
docs/BMW_BPS_API.md · BOBAEDREAM_API.md · HEYDEALER_API.md · REBORNCAR_API.md
docs/TARGET_KEY_MAP.md · HYUNDAI_CERT_API.md · KIA_CPO_API.md · ENCAR_ROBOTS.md
docs/CROSS_SITE_COMPARE.md · DEDUP_CROSS_SITE.md · UI_REVIEW.md · f-table.md
docs/HYUNDAI_CERTIFIED_API.md · LEXUS_CERTIFIED_API.md · MULTISITE_MAPPING.md
docs/SERVER_SURVIVAL.md · TARGETS.md
```
