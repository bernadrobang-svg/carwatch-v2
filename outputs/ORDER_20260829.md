# 작업 지시 · SPEC-2026.09.12-r1231

이 문서 하나만 보고 작업한다.
**끝남**으로 적힌 것은 다시 하지 않는다. **남음**만 한다.
막히면 그 항목만 남기고 다음으로 넘어간다. 질문은 회차 문서 하단에 적고 작업은 끝까지 한다.

| 번호 | 무엇 | 앞조건 | 상태 | 검사 |
|:--:|---|---|:--:|---|
| **A** | 추천 탭 2·3·4 에 값을 붙인다 | 없음 | **남음** | `S46-283` |
| **B** | 마스터 대상 차종의 상세를 채운다 | 없음 | **남음** | `S46-285` |
| **C** · **D** · **I** | 원문 저장 · 관리 질의 · 원문 표 줄이기 | — | **끝남** | — |
| **E** | 엔카 상세 수집 | 없음 | **남음** | `S46-276` |
| **F** | KB 상세 수집 | C | **남음** | `S46-279` |
| **G** | 매핑표를 파서가 읽게 | 없음 | **일부** | `S46-282` |
| **H** | 열두 사이트 구멍 메우기 | C | **남음** | `S46-280` |
| **J** | 차종 표를 분류의 정본으로 | 없음 | **남음** | — |
| **K** | 전국 배달 값 만들기 | 없음 | **남음** | — |
| **L** | 상세를 서버가 받는다 | 없음 | **남음** | `S46-290` |
| **M** | 추천4 GV70 전용 화면 | L | **남음** | `S46-294` |
| **P** | **이번 주 과제 — 더 나은 GV70 (9/11)** | 없음 | **남음** | `S46-298` |
| **Q** | 첫 화면을 목록으로 | 없음 | **남음** | `S46-300` |
| **N** | 자동화 — 사람이 없어도 돈다 | 없음 | **남음** | `S46-296` |

A · B · E · G 는 서로 기다릴 것이 없다. 동시에 해도 된다.

---

## P. 이번 주 과제 — 기한을 9/15 로 늘린다

마스터 09-12 — 「**그럼에도 불구하고 진행하자**」

9/11 기한이 지났지만 이어간다. **9/15 까지**다.
김포 앞 손님 결정이 **9/15**, 마스터 환불 기한이 **9/16** 이라 그때까지가 쓸 수 있는 시간이다.

### 지금 판

| 무엇 | 값 |
|---|---|
| 기준 — 김포 `42497136` | 3,850만 · 708점 · 2023.08 · 25,117km · 흰색 · 사고 0 · 보증 23개월 · **200 살아 있음** |
| 이기는 것 — `42543098` | 3,990만 · 2023.03 · **5,698km** · 흰색 · 엔카진단 · 골격 무사고 · HUD · **보증 60개월** · **200 살아 있음** |
| 물러설 자리 — `270오1279` | 3,490만 · 650점 · 카바조 검수 「상태가 매우 좋은 차량」 |

**한 대뿐이다.** 9/15 까지 더 찾는다.

### 기준차 — 김포 130호3966

```
708점 · 차값 3,850만 (갈아타면 3,930만)
2023.08 · 25,117km · 흰색 · 사고 0 · 보증 23개월
https://fem.encar.com/cars/detail/42497136
앞 손님 결정 9/15 — 못 잡을 수도 있다
```

계약하신 **270오1279**(3,490만 · 650점)는 이제 기준이 아니다. **물러설 자리**다.

### 줄세우기 — 값이 아니라 김포와 견준다

| 칸 | 뜻 | 조건 | 어떻게 |
|:--:|---|---|---|
| **1** | 김포보다 **뚜렷이 낫다** | 주행 2.5만↓ ＋ **2023년 이후 출시** ＋ 흰색 ＋ 골격 무사고 ＋ 진단 있음 ＋ **파퓰러 패키지Ⅰ 이상** ＋ 차값 3,999만 이하 | **두말없이 올린다** |
| **2** | 김포와 **비슷하다** | 위 **일곱 중 여섯**이 맞고 하나가 살짝 못하다. **연식과 옵션은 못해도 안 된다** | 못한 것 **한 줄** 적어 올린다 |
| **3** | 못 미친다 | 다섯 이하, 또는 2022년식, 또는 옵션이 파퓰러Ⅰ 미만 | **올리지 않는다.** 수만 센다 |

값 싼 순이 아니다. **김포에 얼마나 가깝거나 나은가**로 세운다.
차값 상한은 **3,999만**, 계약 포기 위약은 **80만**이다. 갈아탈 차는 차값 ＋ 80만으로 견준다.

### 09-11 — 마스터가 조건을 좁히셨다

마스터 — 「**KB 거는 연식이 매력 없어. 사고 2번 있고**」
마스터 — 「**엔카의 첫 번째는 팔렸고. 2번 3번도 연식이 아니야.
23년 이후 출시로 찾아. 옵션이 파퓰러 패키지Ⅰ 이상의 옵션이 있어야 된다**」

| 무엇 | 어떻게 |
|---|---|
| **연식** | **2023년 이후 출시**. 2022년식은 값이 싸도 **안 본다** |
| **사고** | **보험 사고는 거르지 않는다.** 골격이 상한 것만 뺀다. 횟수와 금액은 **화면에 낸다** — 마스터가 보고 판단하신다 |
| **옵션** | **파퓰러 패키지Ⅰ 이상**. 09-07 의 「깡통도 후보」를 **물린다** |

파퓰러 패키지Ⅰ 는 GV70 의 기본 묶음이다. 그것도 없으면 보지 않는다.

### 넷이 다 떨어졌다

| 매물 | 왜 |
|---|---|
| `42648257` | **팔렸다** |
| `28674398` | **2022.08** — 연식 때문이다. 보험 사고는 까닭이 아니었다 |
| `42648191` | 2022.07 · 청색 |
| `42654226` | 2022.08 · 주행 61,060km |

**44대를 새 조건으로 다시 가른다.** 2023년 이후 · 옵션 · 골격을 본다.

**보험 사고로 거르지 마라.** 09-11 에 가이드가 마스터의 지나가는 말을
조건으로 잘못 굳혔다. 마스터 — 「보험사고는 괜찮아. 내가 KB 를 마음에 안 든
이유를 설명한 것뿐이야」. **까닭 설명과 조건은 다르다.**

### 판단은 마스터 몫이다

마스터 — 「**아니야, 진행해. 구매 여부는 내가 판단하는 거다**」

가이드와 개발측은 **조건에 맞는 차를 찾아 사실을 갖춰 올린다.**
「이게 낫습니다」 「계약차가 낫습니다」 같은 판단을 앞세우지 않는다.

| 올릴 때 낼 것 |
|---|
| 차값 · 8.5% · 위약 · 보증 — **총비용을 따로** 적는다 |
| 주행 · 연식 · 색 · 감가율(**신차출고가** 기준) |
| 골격 · 교환 · 판금 **자리와 부위명** · 보험 횟수와 금액 |
| 옵션은 묶음 이름과 **속을 펴서** — 무엇이 들고 무엇이 빠졌는지 |
| 진단 · 보증 남은 기간 |
| 주소 · **올리기 직전에 두드려 200 인지** |

**파퓰러 패키지Ⅰ 에는 HUD 가 들어 있다** (마스터 09-11).
옵션을 값으로 견주지 마라. 같은 420만이라도 든 것이 다르다.

### 올리기 직전에 두드린다 — 두 번 어겼다

| 회차 | 올린 것 | 실제 |
|---|---|---|
| v400 | `42648257` 1위 | **팔렸다** |
| v401 | `42650594` C칸 | **404 — 팔렸다** |

**표만 보고 올리지 마라.** 올리기 직전에 그 자리에서 상세를 두드려 **200 인지 본다.**

| 응답 | 뜻 | 어떻게 |
|:--:|---|---|
| **200** | 살아 있다 | 올린다 |
| **404** | **팔렸다** | 올리지 않는다. `팔린_매물` 에 적는다 |
| **407** | 지금 막혔다 | **팔린 것이 아니다.** 잠시 뒤 다시 두드린다 |

실측 09-11 — `42650594` **404** · `42543098` **407** · 김포 `42497136` **200 · 3,850만**.

### 할 일

| # | 할 일 | 상태 |
|:--:|---|:--:|
| P-8 | **44대를 끝까지 받는다.** 진단·골격을 다 본다 | 남음 |
| P-9 | 44대를 **1·2·3 칸**으로 갈라 낸다 | 남음 |
| P-10 | 1칸이 나오면 **그 즉시** 보고한다. 모아서 내지 않는다 | 남음 |
| P-11 | 3칸은 수만 센다. 목록을 내지 않는다 | 남음 |
| P-7 | 김포 `42497136` 을 **한 시간마다** 두드린다 (9/15 까지) | 남음 |

**「진단 없음」과 「안 봤다」를 가른다.** v400 에서 이것 때문에 44대를 한때 다 떨어뜨렸다.
안 본 것은 **받아서 보고** 정한다.

**정비이력은 어느 사이트도 안 준다** (v400 실측). P-4 는 못 건다.
매물 화면에만 있으면 그 화면을 봐야 한다. 못 보면 「미조회」로 남긴다.

주소를 반드시 넣는다. 끝에 한 줄 — 「김포(3,850만 · 708점)를 무엇으로 이기는가」.

## P-8. 44대를 마저 확인한다 (마스터 09-11 「진행하게 시켜라」)

v400 에서 **이기는 차 4대**와 **확인하면 이기는 차 44대**가 나왔다.
44대는 값·연식·색이 맞는데 **진단·사고를 아직 안 본** 것들이다.

**「진단 없음」과 「안 봤다」는 다르다.** 안 본 것을 떨어뜨리지 마라.

| # | 할 일 | 상태 |
|:--:|---|:--:|
| P-8 | 44대의 **진단·골격**을 받는다 | 남음 |
| P-9 | `42648257` · `42648191` 의 골격을 마저 받는다 | 남음 |
| P-10 | 넷 중 값 낮은 순으로 다시 세운다 | 남음 |

기한이 지났다. **멈추지 말고 계속 받는다.**
새로 이기는 차가 나오면 그 자리에서 보고한다. 모아서 내지 않는다.

### 정비이력은 못 받는다 — 규격에서 뺀다

v400 실측 — **어느 사이트도 정비이력을 안 준다.** P-4(하체 낱말)를 걸 수 없다.
마스터께서 리본카에서 보신 「너클 628만」은 **매물 화면에만** 있었다.

M-4·M-5 는 **보류**한다. 대신 보험이력의 **부품값**으로 가늠한다.
부품값이 크면 하체일 수 있다 — 그때는 마스터께 「화면을 보십시오」로 낸다.

## M · L · Q. 추천4 · 상세 수집 · 첫 화면

**추천4 규격 정본은 `docs/GV70_TAB4.md`**, 값은 `config/targets.json` 의 `_탭4_규격`.
거르개는 `_탭4_거름` (M-0 을 보라). 시안은 `ref/screens/v4m_gv70_시안.html`.
격자 두 개에 넘길 값 — 칸마다 건수 · 평균가 · 평균주행 · **평균연식**.
남은 것 — M-1(신차출고가) · M-3(교환·판금·골격) · M-4·M-5(정비이력·하체 낱말) ·
M-7(옵션 이름·정가) · M-13(리스 승계 거르기) · M-16~M-19(거르개·네 묶음).

**상세는 서버가 받는다.** `/fetch` 는 쓰지 않는다 (마스터 09-07).
엔카 상세는 데이터센터 IP 에서도 열린다. 목록·성능점검·보험이력은 407 이다.
407 이면 그 건만 건너뛰고 다음으로. 한 바퀴 뒤 다시.

**첫 화면은 목록이다** — `/` → `view_listings`. 현황은 `/status`. 배포됐다.

## A · G · J · O · H · K. 화면 값 · 매핑표 · 차종 표 · 검사 자리 · 구멍 · 배달

**화면·CSS·시안은 가이드가 짓는다.** 개발측은 값만 연결한다.
넘길 값 이름은 각 틀 맨 위 주석에 있다. 없는 값은 「모름」·「미조회」를 넣는다.
탭 2 `tabs picks sorts page rows` · 탭 3 `tabs rows total_url count` ·
`encar_url` `site_badge` `photo_url` `thin_data` `accident_tone` `clean_car`
`insurance_cost` `model_name` `dealer_*`.

매핑표 — `field_map_ALL.json` 126줄이 `meta_field_usage` 에 있다. **파서가 읽게 넓힌다.**
차종 표 — `config/vehicle_table.json` 이 **분류의 정본**이다. 214종(수집 33).

**검사는 배포에서 돌린다.** `_pick_db()` 가 살아 있는 DB 를 고른다.
`ROUND.md` 맨 위에 본 DB 경로가 나온다.

구멍은 **1칸** — `bobaedream.트림`. 전국 배달은 `delivery_nationwide` 칸을 만들어
상세에서 뽑는다. 못 찾으면 「미조회」다.

## N. 자동화 — 사람이 없어도 돈다

마스터 — 「이제 자동화를 하자. **클로드가 없더라도 알아서 수행하는 것으로.**
모든 소스의 최적화가 이루어지고 자동 처리가 되어야 돼.
**아직도 안 도는 파서는 없어야 하고, 아직도 반영 안 된 페이지는 없어야 돼**」

가이드가 자 둘을 지었다.

| 자 | 무엇 |
|---|---|
| `tools/fill_gate.py` | 사이트 × 칸 채움율. **0% 인 칸은 그 파서가 그 칸을 못 읽는 것**이다. 되돌아간 칸(5%p↓)도 잡는다 |
| `tools/round.py` | 한 판 — 재고 · 검사하고 · **담당별로** `outputs/ROUND.md` 에 낸다 |

```
python3 tools/round.py 배포주소
크론:  0 * * * * cd /path && python3 tools/round.py https://... >> /tmp/round.log
```

### 지금 구멍 — 30칸

| 칸 | 0% 인 곳 |
|---|---|
| 옵션 | **엔카 말고 열한 곳 전부** |
| 원문 | 여덟 곳 (보배·헤이딜러·KB·K카·렉서스·리본카·리볼트·볼보) |
| 트림 | 아홉 곳 |
| 사진 | 보배·현대인증·기아CPO |

`outputs/FILL_GATE.md` 에 표로 있다.

| # | 할 일 | 상태 |
|:--:|---|:--:|
| N-1 | `tools/round.py` 를 크론에 건다 (한 시간마다) | 남음 |
| N-2 | 구멍 30칸을 메운다. **한 칸이라도 0% 면 `S46-296` 이 붉다** | 남음 |
| N-3 | 회차 문서에 `ROUND.md` 의 「개발측」 칸을 그대로 붙인다 | 남음 |
| N-4 | 되돌아간 칸이 생기면 그 회차에서 바로 되돌린다 | 남음 |

**시안은 다 깨끗하다** — 40장 × 네 너비, 가려진 글자 0 · 겹침 0 (09-08 실측).

## B · 차종 표

수집 범위 — `config/targets.json` 의 `active=true` 차종만. **전량을 받지 않는다.**
좁힌 결과 — 전체 **28,811건** 중 우리 차종은 **13,971건**(active), 나머지는 `out_of_scope` 다.
사진은 URL 만 저장하고 매물당 **상한 20장**. 좁힌 결과 — 전체 **28,811 → 13,971** 건이다. `detail_status` 가 `ok`·`empty`·`not_found` 면 재요청 않는다.

`config/vehicle_table.json` 이 **분류의 정본**이다. 214종(수집 33).
`collect`·`show_list`·`show_recommend`·`tab1`~`tab4`·`sites`·`taste_rank` 를 다 이 표에서 읽는다.
새 차종은 `collect` 를 Y 로 바꾼다. 목록을 뽑아 되묻지 않는다.

## R. 수집 시각과 KB new — 실측 09-12

### R-1. `collected_at` 을 엔카만 찍는다

| 사이트 | 매물 | 시각이 찍힌 것 |
|---|--:|--:|
| encar | 16,693 | **16,693** |
| **나머지 열한 곳** | 11,450 | **0** |

**수집은 도는데 시각만 안 찍힌다.** 그래서 현황 화면이 「안 돎」으로 잘못 낸다.
엔카 수집기에만 `collected_at` 을 넣고 나머지 열한 곳에 안 넣은 것으로 보인다.

| # | 할 일 | 상태 |
|:--:|---|:--:|
| R-1 | 열한 사이트 수집기에 `collected_at` 을 찍는다 | 남음 |
| R-2 | 이미 받은 것은 원문의 받은 때로 메운다. 못 메우면 「미조회」 | 남음 |
| R-3 | 현황 화면이 「안 돎」과 「시각 미조회」를 **가른다** | 남음 |

### R-2. KB new 356건은 상세를 못 받아 갇혔다

```
KB  active 4,870 · gone 1,134 · new 356 · out_of_scope 259
new 356 중 — 상세 받은 것 62 (17%) · 차종 붙은 것 278 (78%)
```

**차종은 278건에 붙었는데 상세가 62건뿐이다.**
`new → active` 는 상세가 있어야 하므로 **294건이 상세를 기다린다.**
못 살리는 것이 아니라 **아직 못 받은 것**이다. KB 는 10건마다 5분이라 오래 걸린다.

| # | 할 일 | 상태 |
|:--:|---|:--:|
| R-4 | KB new 356건의 상세를 이어 받는다. **차값 싼 것부터** | 남음 |
| R-5 | 회차마다 「new 356 → N」으로 보고한다 | 남음 |

### R-3. 화면 정본이 둘이다 — 마스터께 여쭌다

`web/templates/admin_pipeline.html` 이 검토자가 만든 화면 정본이다.
09-12 커밋 「수집 기록 표와 현황 화면」이 겹칠 수 있다.

**둘을 합치기 전에 어느 쪽이 정본인지 마스터께 여쭌다.** 말없이 고르지 않는다.

## S. CSS 가 들어갔다 빠졌다 하는 까닭 — 찾았다

마스터 — 「**왜 매번 CSS가 적용됐다가 빠지고 날아가고**」

**파일은 한 번도 안 줄었다.** 243K → 269K. `live` 와 `local` 이 똑같다(차이 0B).
`.g4-card`·`.pl-sum`·`.v3-bot`·`.rc2-card` 다 들어 있다.

### 까닭 — 지문을 서버가 뜰 때 한 번만 잰다

```python
# web/app.py:268
got = _STATIC_VERSION.get(name)
if got is None:            # ← 처음 한 번만 읽는다
    ... 파일 지문을 잰다
```

```
cache-control: max-age=3600
주소           /static/app.css?v=1ecdbc5297
```

**CSS 를 고쳐 배포해도 서버를 다시 안 띄우면 `v=` 가 옛 값 그대로다.**
그러면 브라우저가 **한 시간 동안 옛 CSS** 를 쓴다.
그래서 고친 직후엔 안 먹고, 한 시간 뒤에 갑자기 먹는다.

| # | 할 일 | 상태 |
|:--:|---|:--:|
| S-1 | 배포할 때마다 **서버를 다시 띄운다**. 그러면 지문이 다시 잡힌다 | 남음 |
| S-2 | 또는 파일 `mtime` 을 함께 보고 **바뀌면 다시 잰다** | 남음 |
| S-3 | 배포 뒤 `curl -s 배포/ \| grep app.css` 로 **`v=` 가 바뀌었는지 확인**한다 | 남음 |
| S-4 | 바뀐 것이 없으면 그 회차는 **CSS 가 안 먹은 것**이다. 회차 문서에 적는다 | 남음 |

### 가이드가 낸 사고 둘 — 이건 파일 탓이 아니다

| 날 | 무엇 | 지금 |
|---|---|---|
| 09-08 | `g4-*` 규칙 23개를 `app.css` 로 안 옮겼다 | 검사 `S46-291` 이 잡는다 |
| 09-10 | `--surface: var(--surface)` 자기참조로 값이 없어졌다 | 검사 `S46-299` 가 잡는다 |

**파일이 날아간 것이 아니라 가이드가 안 넣었거나 망가뜨린 것이다.** 둘 다 고쳤다.

## M-20. 사고는 두 줄로 낸다 (마스터 09-12)

마스터 — 「**내차 1회 62만 · 상대 2회 260만 / 골격 무사고 · 외판 판금 1곳(좌측 문)
— 이 정도만 보여줘도 좋겠다**」

```
첫 줄   내차 N회 N만 · 상대 N회 N만
둘째 줄  골격 무사고|상함 · 외판 교환 N곳 · 판금 N곳 (부위명)
```

| 규칙 |
|---|
| **내차와 상대를 반드시 가른다.** 상대는 내 차 손상이 아니다 |
| **골격을 앞에** 낸다. 마스터 기준이 「골격에 안 갔으면 통과」다 |
| 부위명을 괄호로 — 「(좌측 문)」 |
| 모르면 「미조회」. **「없다」로 적지 않는다** |
| 정비이력은 어느 사이트도 안 준다. 이 두 줄에 안 넣는다 |

값은 `config/targets.json` 의 `_사고_보이는꼴`. 시안 `v4m_gv70_시안.html` 에 반영했다.

## v402 물음 셋에 대한 답

| # | 물음 | 답 |
|:--:|---|---|
| 1 | 엔카 기본 옵션(001~099) 이름을 어디서 받나 | **안 받아도 된다.** 실측 — `options.standard` 48개는 **기본 사양**이라 값이 0 이고, 돈이 드는 것은 **`choice` 3개**(`1051`·`1055`·`1054`)뿐이다. 옵션값은 **신차출고가 − 신차정가**로 재므로 이름이 없어도 걸린다. 이름 사전 API 는 다 404·407 이다. **찾지 마라** |
| 2 | 정비이력 창구 | **P-4 를 접는다.** 어느 사이트도 안 준다(v400). 자리·사전·잡는 자는 그대로 두고 쓰지 않는다. 「미조회」로 남기고 넘어간다 |
| 3 | 보험 3회 322만을 어떻게 보나 | **판단은 마스터 몫이다.** 「어떻게 보십니까」를 묻지 말고 위 두 줄 꼴로 수를 내라 |

## M-0. 탭 4 에 거르개가 하나도 안 붙었다 — 급하다

마스터 — 「**GV70 추천 수준이 이따위야**」

배포 실측 09-11 —

```
913대를 값·연식·주행만으로 추려 총비용 순으로 세웠다.
1위가 3,290만 · 2024-03 · 6.1만km · 사고 미조회 · 옵션 미조회 · 잔가율 61.6%
미조회가 157개. 「깡통도 후보」가 아직 떠 있다 (09-11 에 물렸다).
```

**마스터가 정하신 조건이 화면에 하나도 안 먹었다.**
가이드가 조건을 `config/week_task.json` 에만 넣고 화면에 붙이라고 안 냈다.

### 거르개 정본 — `config/targets.json` 의 `_탭4_거름`

| 무엇 | 값 |
|---|---|
| 차종 | `GV70_25T` |
| 연식 | **2023년 이후** |
| 주행 | **2.5만km 이하** |
| 차값 | 3,999만 이하 |
| 색 | **흰색** |
| 옵션값 | **400만 이상** (신차출고가 − 신차정가) |
| 골격 | 상한 것은 뺀다 |
| 리스·렌트 승계 | 뺀다 |

**「깡통도 후보」는 물렸다.** 09-11 에 마스터가 「파퓰러 패키지Ⅰ 이상」으로 바꾸셨다.

### 미조회는 떨어뜨리지 않는다

「없다」가 아니라 **「안 봤다」**다. 아래 묶음으로 보내고 **무엇을 아직 못 봤는지** 적는다.

### 줄세우기 — 네 묶음

| 묶음 | 무엇 |
|:--:|---|
| 1 | 조건 다 맞고 **미조회 없음** |
| 2 | 조건 맞는데 **미조회 있음** |
| 3 | 하나가 아쉬움 |
| 4 | 못 미침 — 수만 센다 |

묶음 **안에서** 총비용 낮은 순. 묶음을 건너뛰어 섞지 않는다.

| # | 할 일 | 상태 |
|:--:|---|:--:|
| M-16 | 탭 4 를 `_탭4_거름` 으로 거른다. **코드에 박지 마라** | 남음 |
| M-17 | 네 묶음으로 갈라 낸다 | 남음 |
| M-18 | 화면의 **옛 기준 문구를 다 지운다** | 남음 |

### M-18 — 옛 기준이 새 기준과 겹쳐 떠 있다

배포 실측 09-12 — 「거른 기준」 띠에 **둘이 같이** 떠 있다.

| 지울 것 (옛것) | 남길 것 (새것) |
|---|---|
| 감가 78% 이하 | **2023년 이후** |
| 2022년 5월 ↑ | **2.5만km 이하** |
| 8만km 이하 | **3,999만 이하** |
| 진단 없어도 감가 70%↓면 후보 | **흰색** |
| 깡통도 후보 | **옵션값 400만 이상** |
| 단순교환까지 봄 | 골격 무사고 · 리스·렌트 승계 제외 |

**띠는 `config/targets.json` 의 `_탭4_거름` 에서 그려라.** 화면에 글자로 박지 마라.
그러면 조건이 바뀔 때마다 띠가 저절로 따라온다.
| # | 할 일 | 상태 |
|:--:|---|:--:|
| M-19 | 미조회 항목을 **카드에 낱개로** 적는다 | 남음 |

## 파이프라인 · 대상 차종 · 매핑표

S1 은 사이트별로 **이어 받는다**. **그날 다시 세어** 90% 면 완료로 본다.
`parsed_at` 이 비면 파싱 미완료다 — 차량 키가 안 붙는다.
진단 경로를 **엔카만 안다** — `SITE_ADAPTERS` 에 등록하고 `--site` 로 지정한다.

**새 사이트를 붙이지 않는다.** 이미 받은 것부터 채운다. 「몇 → 몇」으로 보고한다.
`SPORTAGE_LPI` 는 889 중 682건이 목록째 비어 있다. `GV70_25T` 는 **사고 이력**이 879건 다 비었다.
GV70 755건이 전부 `PENDING` 이었다 — 재계산이 돌아야 등급이 난다.
**리스·렌트 승계**는 목록에서 기본으로 거른다 (879 중 124건).
GV70 2.5T 는 09-06 에 `detail_status` NULL 이 **요청조차 안 된 873건**이었다.
**스포티지 LPi** 는 889 중 682건이 목록째 비어 있다.

매핑표 — `field_map_ALL.json` 126줄이 `meta_field_usage` 에 적재됐다.
파서가 한 사이트에서 읽는다. **나머지로 넓히고** `if`·정규식 하드코딩을 없앤다.

**검사는 배포에서 돌린다.** 가이드 자리의 `carwatch.db` 는 0바이트다.
`_pick_db()` 가 ① `CARWATCH_DB` ② `ROOT/carwatch.db` ③ `tmp/load-*.db`(옛 판 · 가늠) 을 고른다.
`ROUND.md` 맨 위에 본 DB 경로가 나온다.

구멍은 **1칸** 남았다 — `bobaedream.트림`.
전국 배달은 `delivery_nationwide` 칸을 만들어 상세에서 뽑는다. 못 찾으면 「미조회」다.

## 대상 차종 · 파이프라인

**새 사이트를 붙이지 마라.** 이미 받은 것부터 채운다. 「몇 → 몇」으로 보고한다.
**스포티지 LPi** 는 889 중 682건이 목록째 비어 있다.
GV70 2.5T 는 09-06 에 `detail_status` NULL 이 요청조차 안 된 873건이었다 — M·P 장이 이어받는다.

S1 은 사이트별로 **이어 받는다**. **그날 다시 세어** 90% 면 완료로 본다.
`parsed_at` 이 비면 파싱 미완료다 — 차량 키가 안 붙는다.
진단 경로를 **엔카만 안다** — `SITE_ADAPTERS` 에 등록하고 `--site` 로 지정한다.

## 닫힌 것

**배점 911 저장** 방어 · **등록부 333 → 331** · 축 점수 저장 · 가려진 글자 63 → 0.
막힌 응답 원문 저장(C) · 관리 질의 ORDER BY＋LIMIT(D) · 원문 표 줄이기(I) 는 끝났다.

## 참조 규격

| 주제 | 문서 |
|---|---|
| 수집 | `docs/chapters/10-collect/00-intro.md` |
| 저장 | `docs/chapters/11-store/a-key.md` |
| 파이프라인 | `docs/chapters/13-pipeline.md` |
| 화면 | `docs/chapters/61-web.md` |
| 추천 화면 | `docs/RECOMMEND_SCREEN.md` |
| 구조 | `docs/ARCHITECTURE_20260830.md` |
| 이번 주 과제 | `docs/WEEK_TASK.md` |
| 그 밖 | `docs/ARCHITECTURE_20260830.md` · `docs/BMW_BPS_API.md` · `docs/BOBAEDREAM_API.md` · `docs/CHECKS.md` · `docs/CROSS_SITE_COMPARE.md` · `docs/DEDUP_CROSS_SITE.md` · `docs/ENCAR_API.md` · `docs/ENCAR_ROBOTS.md` · `docs/HEYDEALER_API.md` · `docs/HYUNDAI_CERTIFIED_API.md` · `docs/INDEX.md` · `docs/KBCHACHACHA_API.md` · `docs/KCAR_API.md` · `docs/KIA_CPO_API.md` · `docs/LEXUS_CERTIFIED_API.md` · `docs/MAPPING.md` · `docs/MULTISITE_MAPPING.md` · `docs/OPTION_CATALOG.md` · `docs/README.md` · `docs/REBORNCAR_API.md` · `docs/RECOMMEND_SCREEN.md` · `docs/REVOLT_API.md` · `docs/ROADMAP_20260830.md` · `docs/SCHEMA.md` · `docs/SERVER_SURVIVAL.md` · `docs/SOURCE.md` · `docs/TARGET_KEY_MAP.md` · `docs/UI_REVIEW.md` · `docs/VOLVO_SELEKT_API.md` |

## 우선순위

| 규칙 | 내용 |
|---|---|
| 1 | 우선순위는 마스터가 정한다 |
| 2 | **최근 지시가 우선이다.** 예전 지시와 어긋나면 최근 것을 따른다 |
| 3 | 규칙끼리 어긋나 보이면 **회차 문서에 적어 확인을 받는다**. 말없이 고르지 않는다 |

## 지시를 바꾸지 않는다

무시하거나 비틀지 않는다. 「기존에 이렇게 해왔으니」는 근거가 되지 않는다.
지금 규격이 근거다. 틀렸다고 판단되면 그렇게 적고 지시대로 진행한다.
되돌리는 비용이 진행하지 않는 비용보다 싸다.

## 진행 방식

멈추지 않는다. 질문이 있어도 멈추지 않는다.

| 상황 | 어떻게 |
|---|---|
| 막히는 것이 있다 | 그 항목만 남기고 **다음 항목으로 넘어간다** |
| 판단이 필요하다 | 가장 안전한 쪽으로 정하고 **회차 문서에 적는다** |
| 지시가 틀린 것 같다 | **그렇게 적고 그대로 진행한다**. 되돌리는 것이 진행 안 하는 것보다 싸다 |
| 다 끝냈다 | 회차 문서 하단에 「여쭐 것」과 「이견」을 적는다 |

허락을 기다리지 않는다. 앞 회차에서 질문하고 멈춘 일이 있었다.
질문은 회차 문서에 적고 작업은 끝까지 한다.

## 작업 규칙

| 항목 | 내용 |
|---|---|
| 커밋 | `git fetch && git rebase origin/main` 후 push |
| 회차 문서 | `outputs/YYYYMMDD_HHMM_vNNN_제목.md` |
| 완료 기준 | 해당 검사가 통과할 것. 통과 전에 완료로 보고하지 않는다 |
| 수치 | 작업 전후 수치를 회차 문서에 기재한다 |
| 질문 | 회차 문서 하단에 「여쭐 것」으로 적는다 |

## 담당 구분

| 담당 | 범위 |
|---|---|
| 가이드 | 시안 · HTML · CSS · 규격 · 매핑표 |
| 개발 | 데이터 수집 · 파싱 · 값 연결 · 배포 |

화면 관련 파일(`ref/screens/` · `web/templates/` · `web/static/app.css`)은 가이드가 작성한다.

## 검사

`docs/CHECKS.md` 하단에 담당별 목록이 있다. 개발 담당은 114개.

```
python3 -c "from validate.v0_guide import run; raise SystemExit(1 if run() else 0)"
```
