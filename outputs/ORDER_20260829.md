# 오늘 할 것 (SPEC-2026.08.29-r936)

## ★★★★★ 0순위 b — **K카가 152 → 6 으로 떨어졌다** [가이드 실측 08-29]

```
08-29 낮   K카 152건 · A+ 0
08-29 지금 K카 ★ **6건**
  ★ `?site=kcar&hide_sold=0` 도 ★ 6 이다 — ★ `/sold` 로 간 것이 아니다
  ★ `/sold` 는 1,226건 · 전체는 5,785 → ★ **5,620** 으로 165 줄었다
```
★ **먼저 재라.  고치지 마라.**
① K카 `sweep_gone` 이 이번 회차에 **몇 건을 `gone` 으로 바꿨나**
② 그 근거가 된 목록이 **몇 건이었나** — `stockCar/list` 는 487건을 준다
③ **「끝까지 받았나」 가드가 걸렸나** — 반만 받고 나머지를 죽였을 수 있다 (오판 161·173 자리)
★★ **살아 있는 매물을 죽였으면 되돌려야 한다.**  ★ 원문은 남아 있으니 다시 살릴 수 있다.

## ★★★★★ 0순위 — **KB `database is locked` 가 다시 났다**

```
store/pii.py:107  save_listing_pii → sqlite3.OperationalError: database is locked
```
★ **내가 잰 것** [08-29] — `save_listing_pii` 는 `store/core.py:128·136·142` 에서만 불린다.
그 셋이 다 **`split_pii` 안**이고, `split_pii` 는 **`upsert_core` 흐름**이다.
★ KB 수집기는 **두 걸음으로 갈려 있다**(`fetch_details`/`load_details`) — **통신＋쓰기를 한 루프에서 하지 않는다.**
★★ **그러니 수집기 탓이 아니다.** 넣기 걸음과 **다른 무엇**이 같은 시각에 쓴다.

**먼저 재라. 고치지 마라.**
```
① locked 가 난 그 시각에 ★ 무엇이 함께 돌고 있었나
   (재판정 · 타이머 · 마스터가 누른 「다시 판정」 · 다른 수집기)
② save_listing_pii 가 ★ 한 회차에 몇 번 불리나 · 한 번이 몇 ms 쥐나
③ 그 트랜잭션이 ★ 넣기 걸음 커밋 안에 있나 밖에 있나
★ 수를 받은 뒤에 고친다.  ★ 지금 busy_timeout 을 올려서 덮지 마라
```

## ★ `S46-100` — **코드는 맞다. 배포가 안 됐다** [가이드 실측 08-29]
```
web/templates/watch.html:125  「비슷한 차 보기」 · 「지우기」  ← ★ 고쳐져 있다
배포 /watch                   「상세 ▸ · 관심 빼기 · 엔카 ↗ · 목표가」  ← ★ 옛 화면이다
```
★ **배포에 올려라.** 검사는 **배포를 잰다** — 코드만 고치면 안 닫힌다.
★ 올린 뒤 `/watch` 를 눌러 **낱말을 눈으로 보고** 「몇 → 몇」을 적어라.

## ★★ 네가 스스로 잡은 셋 — **잘했다**
```
① 소비기가 웹 서버 안에서 옛 코드를 메모리에 들고 있어 「0건」이 거짓이었다 → 재시작 뒤 132건
   ★ 「0건」을 그대로 안 믿고 다시 잰 것이 옳다
② 현대 신차가가 「절약액」이었다 — 「신차 가격 대비 9,100,000원 절약」을 신차가로 잡았다
   ★ 910만 → 3,410만~8,875만.  ★ 앞 회차 「197건 채웠다」는 수는 맞지만 값이 틀렸다
   ★★ 이것이 내가 준 낱말(`신차\s*가격…[\d,]{4,}`) 탓이다 — ★ 내 잘못이다.  아래 정정
③ V3-75 헛잡음 — 사다리가 오르며 낡은 판정과 부딪히던 것.  살아 있는 매물만 견주게
```

## ★ 신차가 낱말을 고친다 (내 잘못)
```
✘ 옛것  (신차\s*가격|신차\s*가|출고\s*가)[^0-9]{0,12}([\d,]{4,})
        → 「신차 가격 대비 9,100,000원 ★ 절약」을 잡는다
○ 새것  뒤에 「절약」·「대비」·「싸다」가 붙으면 ★ 버린다
        ★ 값이 ★ 현재값보다 작으면 ★ 신차가가 아니다 — ★ 그 자리에서 버린다
★ 남은 것 — 「신차가 < 현재값」이 엔카 418 · 리본카 36 · KB 12 · 헤이딜러 9 · 렉서스 1
  ★ ★ **같은 잣대로 다 걸러라** (이번 지시 밖이라 안 건드린 것이 맞다)
```

## ★★★ `v308` 물음 넷에 답한다

| 물음 | ★ 답 |
|---|---|
| ★ 5 — K카 점검경로를 **그래도 부를까요** (`cno` 는 얻는다 · 「마스터」) | ★ **마스터 몫이 아니다. 불러라.** 여섯 축은 못 채워도 ★ **`cno` 가 짝짓기 열쇠**다(`DEDUP_CROSS_SITE` 1장 — `plate_hash`). ★ **지금 같은 차 짝이 0건**이고 그것이 이 도구의 심장이다. ★ `split_pii` 를 거쳐 `plate_hash` 만 남기고 **번호판 원문은 `core_listing` 에 넣지 마라** |
| ★ 신차가 ③ (볼보 149·보배 227·BMW 8 · 「가이드」) | ★ **밑감은 현대 인증이다** — 상세 표본 8건 전건에 값이 있고 서로 다르다. 열쇠 넷(**제조사·모델·트림·연식**)으로 맞춘다. ★ **넷이 다 같을 때만** 쓴다 · 하나라도 다르면 **0점＋미확인** · 화면에 「다른 사이트 표에서 (현대인증 · 같은 트림 2023년식)」이라 적는다 |
| ★ 「그때 시세」 (「가이드」) | ★ **지금 중앙값을 쓰고 화면에 그렇게 적는 것이 맞다.** ★ `price_history` 가 **722건**뿐이라 과거 중앙값을 못 만든다. ★ **없는 것을 만들지 마라** — 「지금 시세 기준」이라 적은 것을 그대로 둔다 |
| 5 — KB 부위별 파서 (표본 6건이 다 무사고) | ★ **옳게 멈췄다.** 사고 있는 매물을 찾아 꼴을 본 뒤에 만든다. ★ 짐작으로 이름을 지어내지 마라 (오판 192) |

## ★★ 명령서 여섯을 다 끝냈다 — **확인했다**
`/sold`(163건을 목록에서 뺐다) · `/watch` 단추 차례 · 현대 신차가 **0→197** ·
볼보 상세 자동수신(값 **134→149**) · `sweep` 이 차종 없는 행도 훑는다 · 볼보 신차가 증거.
★ **`run_tests` 가 처음 통과했다.**

★ **`/sold` 를 눌러 봤다 — 200 · 26,638B** [가이드 실측 08-29].
★★ 다만 **`S46-100` 은 아직 실패다** — `/watch` 가 「상세 ▸ · **관심 빼기** · 엔카 ↗ · 목표가」인데
시안은 「**비슷한 차 보기 · 지우기**」다. **차례는 바꿨는데 낱말이 다르다.**
★ 시안 낱말을 그대로 쓰라는 뜻이다 — 「관심 빼기」→「지우기」 · 「상세 ▸」 앞에 「비슷한 차 보기」.
★ **글자를 새로 짓지 마라.**

## ★★★★★ 0순위 — 지금 바로. **디스크가 7.3G 남았다**
```
① outputs/check-tmp 12GB · 8,861개를 ★ 지워라
   ★ 검사 임시물이다 — P3(원문 무손실)에도 「구조를 손대지 마라」에도 안 걸린다
② tools/run_tests.py:_sweep_temp 이 outputs/check-tmp 도 치우게 고쳐라
   지금은 /tmp 만 본다.  검사기 넷이 거기 쓴다
   (v1_collect:16 · v3_logic:17 · v10_admin:17 · v11_web:5012)
③ 지운 뒤 「몇 GB → 몇 GB」와 디스크 남은 것을 보고해 달라
```

## ★★★ `v305` 「안 한 것」 다섯에 답한다 — **넷은 이미 닫혔다**

| 개발측이 적은 것 | ★ 지금 상태 |
|---|---|
| `sold.html` (이틀째 · 「마스터」) | ★ **만들어라.** 라우팅 표에 `/sold`·`view_sold` 를 **넣었다**(`61-web.md:193`). 「구조를 손대지 마라」는 **수집·적재**이고 **화면은 아니다** |
| `V11-12` 라우팅 중복 (이틀째 · 「가이드」) | ★ **내가 이미 고쳤다.** `61-web.md:209` 시안 줄을 지웠다. 파서를 돌려 **「행 38 · 중복 없다」** 확인. 남은 셋은 **본문 서술**이지 표가 아니다 — **네 커밋 전 상태를 본 것이다** |
| `check-tmp` 12GB (「마스터」) | ★ **마스터 몫이 아니다. 지워라.** 검사 임시물이라 P3 에도 「구조」에도 안 걸린다 (0순위) |
| `V1-23` · `V2-01` | 개발측 몫 — 그대로 |
| KB·리본카 `target_key` 40건 | 타이머에 맡긴 것이 맞다 |

★ **v303~v305 의 「마스터」는 다 내가 답했다** — `outputs/ANSWERS_20260829_v304.md` · **회차 표에서 지워라.**

## ★★ 「마스터 몫」 다섯에 다 답했다 — **회차 표에서 지워라**
`outputs/ANSWERS_20260829_v304.md` 한 장. 넷은 내가 이미 답했는데 명령서에만 있어 안 닿았다 — **내 잘못이다.**

## 지금 할 것 — 여섯
| | 무엇 | 어디 |
|:--:|---|---|
| 1 | `/sold` 화면 — 라우팅 표에 **`/sold`·`view_sold`** 를 넣었다 | `UI_REVIEW` 30장 · `ref/screens/v4m_sold_시안.html` |
| 2 | `/watch` 단추 차례 — 시안은 「비슷한 차 보기 · 지우기」, 화면은 「상세 ▸ · 엔카 ↗ · 목표가 · 관심 빼기」. **시안 차례로.** 단추를 지우지 말고 **글자를 새로 쓰지 마라** | `S46-100` |
| 3 | 신차가 ③ — 볼보 149 · 보배 227 · BMW 8. ★ **현대 인증이 원문에 준다**(상세 표본 8건 전건 · 값이 서로 다르다) → 그것이 밑감이다 | `f-table` ③ · `HYUNDAI_CERTIFIED_API` 08-29 |
| 4 | 볼보 빈 15건 — `sweep_gone_groups` 가 `target_key` 있는 행만 봐서 **값 없는 행이 영영 안 죽는다.** 결함이다 | 답변서 #4 |
| 5 | 파서 나머지 — KB 성능점검(`data-link-url` · 200·58,016B) · K카 점검경로 · 볼보 상세 | `MULTISITE_MAPPING` 08-29 |
| 6 | 볼보 신차가 **증거 파일** — `docs/evidence/{site}_{항목}_{날짜}.md` | `S46-161` |

## ★ 큰 구조는 **설계 2판 뒤**
설계도 `docs/ARCHITECTURE_20260829.md` · 물음 58개 답 `outputs/ANSWERS_ARCH_20260829.md`.
★★ **`raw_response` 를 지우지 마라** (감독 지시 08-29 — 되돌릴 수 없다).
파일 쓰기·넣기는 가도 된다. **지우는 것만** 안 한다.

## 지킬 것
하나 닫고 「몇 → 몇」 적고 다음. 첫 줄은 「안 한 것」 표. 200·303 으로 세지 않는다.
「사이트가 안 준다」는 **표본 10건 전건이 빌 때만** 적고 `docs/evidence/` 에 증거를 남긴다.
남은 것은 `docs/guide/07_밀린일대장.md` · 설계 뒤에 할 것은 `docs/guide/09_설계뒤에할것_20260829.md`.

## 수집 범위
`config/targets.json` 의 26종만. 전량을 받지 않는다. 상한은 차종별 목록 끝까지.
전체 → 대상 20333 → 5770 [개발측 08-29]
가이드 실측 08-29 — KB 4,320 · 보배 222 · 볼보 223(20쪽) · 현대 1,138 · 헤이딜러 199
K카 487(이 창구) · BMW 364(38쪽 · X3 23) · 렉서스 74 · 리본카 1,082 · 엔카는 마스터 회선

## 금지
지우는 것 · 규격을 개발측이 고치는 것 · 새 색 · 클래스 이름 바꾸기 · 짐작으로 규격 적기.
★ 설계 2판 전에 수집·적재 **구조**를 손대는 것. ★ 화면은 여기 안 걸린다.

## 규격
`docs/ARCHITECTURE_20260829.md` · `docs/chapters/` 10-collect · 11-store · 41-view · 60-admin · f-table
KCAR_API · KBCHACHACHA_API · VOLVO_SELEKT_API · BOBAEDREAM_API · HEYDEALER_API · REBORNCAR_API
HYUNDAI_CERTIFIED_API · LEXUS_CERTIFIED_API · KIA_CPO_API · BMW_BPS_API · ENCAR_ROBOTS
TARGET_KEY_MAP · MULTISITE_MAPPING · CROSS_SITE_COMPARE · DEDUP_CROSS_SITE · UI_REVIEW · SERVER_SURVIVAL
