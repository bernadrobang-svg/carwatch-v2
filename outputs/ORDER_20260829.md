# 오늘 할 것 (SPEC-2026.08.29-r887)

끝난 것은 지웠다 — 렉서스 `cur_page`(81512d2) · 타이머(540971e) · BMW(5f75d4d) ·
하트(a332f4e) · 정렬(43960c2) · 원문 잇기(e5b67aa) · 사이트별 합(55f5847).

## 0 잠금 — 뿌리가 남았다
개발측이 쟀다 — 「보배가 재판정을 큐에 넣었고 **09:16:20 → 09:25:25** 도는 동안 뒤의 넷이 죽었다」.
**한 판이 9분 5초** · `busy_timeout` 30초의 18배다. `worker.py` 와 「묶어 돌 때 큐에 안 넣기」는 **맞다. 닫는다.**
남은 것 — `collect/pipeline.py` 에 `conn.commit()` 이 **하나**(`save_step_report`)뿐이고
`run_pipeline:470` 이 **단계 안에서 안 끊는다**. 마스터가 「다시 판정」을 누르시는 동안 타이머가 돌면 또 죽는다.
**단계 안에서 N행마다 커밋해 달라** — `load_details` 가 200행마다 하는 것과 같은 꼴.

## 1 매물이 안 들어온다 — 마스터께서 차를 못 고르신다
| | 파일 · 줄 | 무엇이 틀렸나 | 검산 |
|:--:|---|---|:--:|
| a | `tools/collect_kbchachacha.py:57`·`:65` | `DETAIL_BATCH=10`·`BURSTS=1` — 한 회차 상세 10건. **대상 4,320 중 화면 116**. 근거였던 08-25 봇차단은 08-29 에 **0건**이다 | — |
| b | `tools/collect_kcar.py:176`·`:191` | `todo` 가 `detail_status='ok'` 를 빼서 저장된 매물이 gone 에 영영 안 닿는다. `collect_bobaedream.py:238` 꼴로 | S46-117 |
| c | `tools/collect_hyundai_cert.py` | `_V` 묶음은 목록에 연료가 없다 — 상세를 함께 받아야 전기 셋(GV70_EV 4·GV60 1·G80_EV)이 갈린다 | — |
| d | `tools/collect_reborncar.py` | 상세 1,082건을 받아야 차종이 갈린다. 좁힐 길이 없다 (규격 1b·1c) | — |
| e | `tools/collect_heydealer.py:77` | `pick` 에 `fuel` 이 없어 `fuel=electric` 질의가 **전량 1,330건**을 끌어온다. `fuel` 을 더해 달라 — **그 뒤에** 내가 폴스타를 넣는다 | S44-4 |

## 1b 값이 있는데 안 붙는다 — 가이드가 표본을 뽑아 자리를 짚었다
| | 파일 · 줄 | 무엇이 틀렸나 | 검산 |
|:--:|---|---|:--:|
| f | `tools/collect_bmw.py:100~104` | `row` 에 `site_model` 뿐 — **값·주행·연식을 한 줄도 안 넣는다**. 상세(`/shop/item.php?it_id=`)에 다 있다. 표본 3건 실측: 주행 27,300·34,119·24,130km · 연식 2022·2023·2024 · 사고 무 · 72가지 점검. 503 이 잦아 **네 번 재시도**. 값은 꼴이 셋(`판매가:`·`차량가격 :`·`판매 가격 :`)이고 **「신차가: 약 …」과 쪽 아래 거르개 「1500 만원 2000 만원 …」을 값으로 잡으면 안 된다** (BMW_BPS_API 08-29 절) | — |
| g | `parse/bobaedream/mapping.py:46` `RE_YMD` | `YY/MM/DD` 만 본다 — **`2024/10` 꼴을 못 읽는다**. 표본 10건 중 2건이 그 꼴이고 `year_month` 가 `None` 이다. 연식은 **10/10 다 온다** | — |
| h | `parse/bobaedream/mapping.py` `parse_detail` | `fields()` 에 **`신차가격: 15,953 만원`** 이 오는데 **읽는 줄이 없다**. 표본 10건 중 1건. 나머지 9건은 딜러 손글씨라 **안 읽는다** — ③ 으로 끌어온다 | — |

## 1c 성능점검 여섯 축 100점 — **사이트가 아니라 우리가 안 뽑는다** (가이드가 쟀다)
| | 파일 · 줄 | 무엇이 틀렸나 | 검산 |
|:--:|---|---|:--:|
| i | `contracts.py:159` `inspection_panels` | **이 칸을 채우는 파서가 하나도 없다.** 골격 30 ＋ 외판 20 = **50점이 전 사이트 0점**이다 (`analyze/axis/state.py:109`·`:129` 가 이 칸을 읽는다) | — |
| j | `parse/kbchachacha/mapping.py` | KB 상세 표본 **5/5 에 판금·용접·골격·부식·보험이력이 다 있다**. 파서가 한 줄도 안 뽑는다 | — |
| k | `parse/reborncar/mapping.py` | 리본카 상세 표본 **5/5 에 판금·용접·부식**이 있다. 〃 | — |
| l | `tools/collect_kcar.py` | 점검 경로 `GET /bc/car-insp/photo/cm?i_sCarCd={id}` 를 **안 부른다**. 실측 200·462B · `inspDetail` 에 `acdtHistYn`·`smplReprYn`·`pfmncNewVerYn`·`imgList` | — |
| m | `parse/*/mapping.py` | `inspection_inner_json`(누유 10)을 **엔카만 넣는다** — 아홉 사이트가 다 0점 | — |

| n | `tools/collect_volvo.py` | 볼보 상세를 **안 받는다**. 상세 표본 5/5 에 성능·점검·사고가 있고 판금 2/5·교환 3/5 다. 목록 카드에는 **없다** | — |

먼저 i 를 만들고 그다음 j·k·l·m·n. **⑥(80%)이 아니다 — 우리 결함이다.** 배점은 안 고친다.
★ **다만 규격이 먼저다** — 볼보·BMW·보배·렉서스 절에 칼럼 매핑이 **아예 없다**. 내가 채운 뒤에 시작해 달라.

## 2 화면
| | 파일 · 줄 | 무엇이 틀렸나 | 검산 |
|:--:|---|---|:--:|
| a | 틀 39곳 | `_head.html`·`_foot.html` 부터 (UI_REVIEW 28장) | S46-122 |
| b | `app.css` header 23곳 (27장) · `:root` 토스 (29장) | a 와 같은 회차 | S46-121 |
| c | `/notready` 「여쭐 것」 절 | 612건을 「마스터께서 정하십니다」로 낸다. 세어 보니 **우리 26종이 0건**이다. 가이드가 먼저 가려 26종에 닿는 것만 낸다 (확정 37) | — |

## 3 축 × 사이트 표를 채워 달라 (마스터 지적 08-29 · 가장 급하다)
**A 등급 166건이 전부 엔카다. 아홉 사이트의 A 가 0 이다** — 엔카만 축이 채워져서다.
매물 하나씩 열어 보니 근거 있는 축이 볼보 652 · K카 622 · 렉서스 622 · 보배 265 · **BMW 18** / 910 이다.
**축 26개 × 사이트 11개 표**를 매물 수로 채워 달라 — 「이 사이트가 이 축에 값을 주는 매물이 몇 건인가」.
그 표가 있어야 ⓐ상세를 안 받아서 / ⓑ사이트가 원래 안 줌 / ⓒ그 차에 원래 없음 을 가른다.
★ 마스터께서 **갈래 ⑥ 을 확정하셨다**(f-table) — ⓑ 는 0점이 아니라 **만점의 80%** 다.
신차가·트림처럼 **차종 속성인 것은 ③ 으로 다른 사이트에서 끌어온다**. 어느 축이 ⑥ 인지는 이 표로 정한다.
지금은 배점을 고치지 마라 — 표부터다.

## 3b 전기차 표본을 재 달라 (마스터 지적 08-29)
전기차 상세 표본 20건에서 성능점검부 `inners` 가 **어떤 꼴로 오는지** 재 달라 —
① 원동기·변속기 절이 **아예 없는가** ② 있는데 「해당없음」인가 ③ 「양호」로 오는가.
`analyze/axis/state.py:196` `_leak()` 은 **연료를 안 읽는다** — 절이 비면 만점 15, 절이 없으면 0점이다.
전기차 15점이 **우연에 걸려 있다.** 수를 받은 뒤 내가 규격을 정하고 마스터께 올린다. 지금은 고치지 마라.

## 4 답한 것 · 세어 줄 것
`bmw_bps` — **미니가 섞인다**(364 중 2건). `brand_scope` 는 그대로 둔다. 「BMW 만 판다」만 지우고 「미확인」을 닫아 달라.
`test_endtoend.py` 실패가 **어느 커밋부터**인지 한 줄 달라 — 「전부터」는 내가 확인할 수 없다.
K카 `sell_dcd=ALL` 이 몇 건인가 — 본문이 `enc` 라 가이드가 못 잰다.

## 지킬 것
하나 닫고 「몇 → 몇」 적고 다음. 첫 줄은 「안 한 것」 표. 200·303 으로 세지 않는다.
숫자를 옮겨 적지 말고 규격을 가리킨다. 남은 것은 `docs/guide/07_밀린일대장.md`.

## 수집 범위
`config/targets.json` 의 26종만 받는다. 전량을 받지 않는다. 상한은 차종별 목록 끝까지다.
전체 → 대상 20333 → 5770 [개발측 08-29 · 55f5847]
가이드 실측 08-29 — KB 4,320 · 보배 222 · 볼보 149 · 현대 179 · 헤이딜러 199 · K카 105
BMW 364(X3 23) · 렉서스 74(대상 53) · 리본카 1,082(미분) · 엔카는 마스터 회선

## 금지
지우는 것 · 규격을 개발측이 고치는 것 · 새 색 · 클래스 이름 바꾸기 · 짐작으로 규격 적기.

## 규격
`docs/chapters/` 10-collect · 11-store · 41-view · 60-admin · f-table
KCAR_API · KBCHACHACHA_API · VOLVO_SELEKT_API · BOBAEDREAM_API · HEYDEALER_API · REBORNCAR_API
HYUNDAI_CERTIFIED_API · LEXUS_CERTIFIED_API · KIA_CPO_API · BMW_BPS_API · ENCAR_ROBOTS
TARGET_KEY_MAP · MULTISITE_MAPPING · CROSS_SITE_COMPARE · DEDUP_CROSS_SITE · UI_REVIEW · SERVER_SURVIVAL
