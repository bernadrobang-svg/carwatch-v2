# CarWatch v2 — 오늘 할 것 (SPEC-2026.08.29-r855)

## 마스터께서 누르실 것

홈에서 「엔카 받기」 한 번 · 「다시 판정」 한 번.
안 누르시면 매물도 점수도 그대로다.

## 개발측 — 다섯

| | 파일 · 줄 | 무엇이 틀렸나 | 검산 |
|:--:|---|---|:--:|
| 1 | `tools/collect_*.py` 아홉 | `sweep_gone` 을 `kcar` 만 부른다 | S46-117 |
| 2 | `app.css:1646` · `listings.html:306` | 담을 `relative` 가 없어 하트 30개가 `y=4` 에 쌓인다 | S46-118 |
| 3 | 등록부 쓰는 쪽 | `seed.` 를 붙이고 읽는 쪽이 안 붙인다 | S46-120 |
| 4 | `build.py:998` | `first` 앞에 `ORDER_HEAD` 가 있다 | — |
| 5 | `admin_run.html:37` | 시키는 화면인데 5초마다 갱신한다 | S46-115 |
| 6 | `app.css` header 23곳 | 화면마다 따로 적혀 위·아래가 바뀐다 | S46-121 |
| 7 | 틀 39곳 | 머리·발을 화면마다 다시 쓴다 (`include` 셋뿐) | S46-122 |
| 9 | `collect/worker.py:107·155` | `sqlite3.connect` 를 직접 불러 `busy_timeout` 이 안 걸린다 | S46-125 |
| 8 | `app.css` 색·크기 | 토스 스타일로 바꾼다 (UI_REVIEW 29장 값 표) | S46-123 |

## 손대는 법

```
1  sweep_gone(conn, site, target_key, seen_source_ids, at)
   끝까지 받았을 때만 · seen 이 비면 안 부른다 · 그 사이트 그 차종만
   S46-117 은 첫 사이트에 붙일 때 함께 만든다
2  .heart 에 z-index:5 · listings.html:306 <div> 에 class 와 position:relative
   body·header 를 건드리지 않는다
4  first 를 ORDER_HEAD 앞으로.  ORDER_SOLD 는 맨 앞 그대로
9  collect/worker.py:107·155 의 sqlite3.connect 를 raw.open_db 로 바꾼다
   pii.py 는 연결을 안 연다.  받아 쓴다 — 범인은 부르는 쪽이다
   KB 수집이 끝난 뒤에 고친다.  도는 중에 손대면 더 잠근다

7  _head.html · _foot.html 을 먼저 만든다 (UI_REVIEW 28장)
   39곳이 한 번에 고쳐진다.  6번이 저절로 닫힌다
   그다음 _card · _filter · 나머지 넷

8  토스 스타일 — UI_REVIEW 29장 값 표 그대로 (7번과 같은 회차에 한다)
   app.css 맨 위 :root 의 값만 바꾼다
     --bg #ffffff · --text #191f28 · --dim #8b95a1
     --surface #ffffff · --line #e5e8eb · --amber #3182f6
     --down #00c471 · --up #f04452
   본문 15px · 줄 사이 1.6 · 모서리 12px (카드 16) · 여백 16~20px
   카드는 테두리 대신 그림자 — box-shadow 0 1px 3px rgba(0,0,0,.08)
   클래스 이름을 바꾸지 마라.  바꾸면 S46-67 이 다 깨진다
   목록부터 한 화면씩 사진으로 보여라

6  app.css 맨 위에  header { position:sticky; top:0; z-index:30 }
   화면별 header 23곳에서 position·top·z-index 를 지운다 (색·테두리는 그대로)
   모든 폭에서 같다.  아래 탭으로 안 간다
```

## 지킬 것

하나 닫고 「몇 → 몇」 적고 다음으로.
첫 줄은 「안 한 것」 표다.  200·303 으로 세지 않는다.

남은 것은 `docs/guide/07_밀린일대장.md` 에 있다.

## 수집 범위

```
config/targets.json 의 26종만 받는다.  전량을 받지 않는다
사이트가 파는 전부가 아니다.  상한은 차종별 목록 끝까지다

좁힌 건수 (실측 08-29) — 전체 → 대상  18762 → 5756

  사이트별 상한
    엔카 13878 → 8686
    현대인증 1124 → 174
    리본카 1046 → 72
    기아 1021 → 0
    K카 604 → 34
    KB 445 → 287
    볼보 143 → 140
    헤이딜러 168 → 96
    보배 97 → 55
    렉서스 36 → 10
```

## 금지

```
지우는 것 · 규격을 개발측이 고치는 것 · 새 색을 만드는 것
200·303 으로 세는 것 · 짐작으로 규격을 적는 것
```

## 규격

```
docs/chapters/          10-collect · 11-store · 41-view · 60-admin
docs/ENCAR_API.md · KCAR_API.md · KBCHACHACHA_API.md · VOLVO_SELEKT_API.md
docs/BMW_BPS_API.md · BOBAEDREAM_API.md · HEYDEALER_API.md · REBORNCAR_API.md
docs/TARGET_KEY_MAP.md · HYUNDAI_CERT_API.md · KIA_CPO_API.md · ENCAR_ROBOTS.md
docs/CROSS_SITE_COMPARE.md · DEDUP_CROSS_SITE.md · UI_REVIEW.md · f-table.md
docs/HYUNDAI_CERTIFIED_API.md · LEXUS_CERTIFIED_API.md · MULTISITE_MAPPING.md
docs/SERVER_SURVIVAL.md · TARGETS.md
```

