# 오늘 할 것 (SPEC-2026.08.29-r869)

## 0 잠금 — 먼저 재라, 고치지 마라
`collect/pipeline.py:365` 이 파일에 commit 이 하나뿐 (단계 끝에만) · `collect/worker.py:107`·`:155` 맨 sqlite3.connect 라 busy_timeout 이 안 붙는다.
한 단계가 몇 초 쥐는지 찍어 달라. 30초를 안 넘으면 365 가 까닭이 아니다.

## 1 매물이 죽거나 안 죽는다
| | 파일 · 줄 | 무엇이 틀렸나 | 검산 |
|:--:|---|---|:--:|
| a | `tools/collect_lexus.py:41`·`:115` | 1쪽 36건만 받고 `_done=bool(cars)` 로 sweep — 열쇠는 `cur_page` 다. 전수 74. 2·3쪽 38건이 안 팔렸는데 gone | S46-117 |
| b | `tools/collect_kcar.py:176`·`:191` | `todo` 가 `detail_status='ok'` 를 빼서 저장된 매물이 gone 에 영영 안 닿는다. `collect_bobaedream.py:238` 꼴로 | S46-117 |
| c | `tools/collect_hyundai_cert.py` | `_V` 묶음은 목록에 연료가 없다 — 상세를 함께 받아야 전기 셋이 갈린다 | — |
| d | `config/cli_screens.json` pending · `deploy/carwatch-daily.service` | 수집기 열 개가 화면도 타이머도 없다 | S46-127 |
| e | `tools/collect_heydealer.py:77` | `pick` 에 `fuel` 이 없어 `fuel=electric` 질의가 **전량 1,330건**을 끌어온다. `fuel` 을 더해 달라 — **그 뒤에** 내가 폴스타를 `targets.json` 에 넣는다 (HEYDEALER_API 폴스타 절) | S44-4 |

## 2 화면
| | 파일 · 줄 | 무엇이 틀렸나 | 검산 |
|:--:|---|---|:--:|
| a | `app.css:1646`·`listings.html:306` | 담을 relative 가 없어 하트가 쌓인다 (UI_REVIEW 26장) | S46-118 |
| b | `build.py:998` `order_clause` | first 앞에 ORDER_HEAD 가 있다 | — |
| c | 틀 39곳 | `_head.html`·`_foot.html` 부터 (UI_REVIEW 28장) | S46-122 |
| d | `app.css` header 23곳 (27장) · `:root` 토스 (29장) | c 와 같은 회차 | S46-121 |

## 3 세어 줄 것
`SELECT site,COUNT(*) FROM core_listing GROUP BY 1` · 리본카 우리 차종 몇 건인가.

## 지킬 것
하나 닫고 「몇 → 몇」 적고 다음. 첫 줄은 「안 한 것」 표. 200·303 으로 세지 않는다.
숫자를 옮겨 적지 말고 규격을 가리킨다. 남은 것은 `docs/guide/07_밀린일대장.md`.

## 수집 범위
`config/targets.json` 의 26종만 받는다. 전량을 받지 않는다. 상한은 차종별 목록 끝까지다.
전체 → 대상 18762 → 5756 [개발측 08-29 · 사이트별 합 9554 와 안 맞는다 — 3 으로 다시 센다]
가이드 실측 08-29 — 보배 221 · K카 105 · 볼보 149 · 현대 179 · 헤이딜러 199 · 렉서스 53 · 리본카 1083(미분)

## 금지
지우는 것 · 규격을 개발측이 고치는 것 · 새 색 · 클래스 이름 바꾸기 · 짐작으로 규격 적기.

## 규격
`docs/chapters/` 10-collect · 11-store · 41-view · 60-admin · f-table
KCAR_API · KBCHACHACHA_API · VOLVO_SELEKT_API · BOBAEDREAM_API · HEYDEALER_API · REBORNCAR_API
HYUNDAI_CERTIFIED_API · LEXUS_CERTIFIED_API · KIA_CPO_API · BMW_BPS_API · ENCAR_ROBOTS
TARGET_KEY_MAP · MULTISITE_MAPPING · CROSS_SITE_COMPARE · DEDUP_CROSS_SITE · UI_REVIEW · SERVER_SURVIVAL
