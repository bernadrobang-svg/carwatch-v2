# 오늘 할 것 (SPEC-2026.08.29-r904)

## ★★★★★ 0순위 — **지금 바로. 디스크가 7.3G 남았다**

물음 **#57·#58 에 답한다 — 지워라. 그리고 고쳐라.**

```
① outputs/check-tmp 12GB · 8,861개를 ★ 지워라
   ★ 「수집·적재 구조를 손대지 마라」에 걸리지 않는다 — ★ 검사 임시물이다
   ★ 규칙 P3(원문 무손실)에도 안 걸린다 — ★ 원문이 아니다

② tools/run_tests.py:_sweep_temp 이 outputs/check-tmp 도 치우게 고쳐라
   ★ 지금은 tempfile.gettempdir()(=/tmp) 만 본다
   ★ 검사기 넷이 outputs/check-tmp 에 쓴다
     (v1_collect:16 · v3_logic:17 · v10_admin:17 · v11_web:5012)
   ★ 그 함수의 주석이 이미 「검사가 디스크를 채우지 않는다」라 적어 두었다 —
     ★ 자리를 하나 빠뜨린 것이다.  같은 규칙 · 같은 잣대로 치운다

③ 지운 뒤 「몇 GB → 몇 GB」와 「디스크 남은 것」을 보고해 달라
```

★ **잘 찾았다.** 나는 9-2 에 「`outputs/` 회차 기록이 쌓인다」로 적었는데 **틀렸다** —
회차 기록은 234개·몇 MB 뿐이고 **12GB 가 검사 임시물**이다. 물린다.

## ★★★ 08-29 회차에 답한다 — **내 몫 넷을 그 자리에서 닫았다**

| 개발측이 낸 것 | 내 답 |
|---|---|
| ★ `endpoints.json` 에 「미확인 08-26」이 **셋 더 있다** (KB·렉서스·볼보) | ★ **재서 셋 다 닫았다.** 렉서스 = 렉서스만(1쪽 36건 ES 21·NX 9·UX 5·RX 1) · 볼보 = 볼보만(`Polestar` **0회**) · 리본카 = **종합이 맞다**(표본 12건에 기아 8·현대 2·제네시스 1·쉐보레 1). **남은 미확인 0건** |
| `V11-12` 라우팅 중복 | ★ **끝냈다** — `/admin/users` 시안 줄을 지웠다 (중복 0 확인) |
| `V11-45` 화면 미정 5개 | ★ **재보니 실패 0 이다.** `pending` 34건은 검사가 실패로 안 센다(코드 `:1663`). **가이드가 더 할 것이 없다** — 다르면 어느 `cap` 인지 한 줄 달라 |
| `target_key` 92건 (이틀째 · 「마스터」) | ★ **마스터 몫이 아니다. 해라.** `docs/TARGET_KEY_MAP.md` 08-29 절에 박았다 |
| 2c `/notready` 가려 내기 | ★ **가이드 몫 맞다.** 세어 보니 612건에 **우리 26종이 0건**이다 — 지금은 **아무것도 안 올린다**가 답이다. 그 절을 「26종에 닿는 것만」으로 고쳐라 |

### ★ 내가 틀렸던 것 — **물린다**
「`pipeline.py` 가 **단계 안에서 안 끊는다**」는 **틀렸다.**
개발측 실측 — **S6 이 1,503번 끊고 한 번에 최대 2.50초**다. 까닭은 틈의 길이가 아니라 **비율**(588초 중 559초·95%를 쥐고 틈이 평균 19ms)이었다.
★ **20ms 창이 맞는 처방이다.** 내 진단이 틀렸고 개발측이 재서 고쳤다.
★ `DETAIL_BATCH` 도 마찬가지 — **20건 40%·30건 100% 막힘**이니 **10 유지가 맞다.** 내가 「근거가 사라졌다」고 한 것을 물린다.

## ★ 앞 회차 물음 — **다 규격에 박혔다.  회차 표에서 지워 달라**
`target_key` → `TARGET_KEY_MAP` 08-29 절 · 조각 여섯 → `UI_REVIEW` 31장 ·
`expect` → `collect_hyundai_cert.py:240` 을 고친다(`_expect` 를 `targets.json` 에 넣지 마라) ·
K카 `resvYn` → `11-store/a-key` 08-29 절 · 「팔린 차」 → `UI_REVIEW` 30장 ＋ 시안.

## ★★★ 지금 할 일 — **둘뿐이다**

**1 · 설계도를 읽고 「물을 것」을 한 번에 적어 달라** ← 가장 급하다
`docs/ARCHITECTURE_20260829.md` (9장) → `outputs/QUESTIONS_ARCH_20260829.md` 한 파일.
꼴 `| # | 어느 장 | 물음 | 왜 물어야 하나 | 답이 없으면 어떻게 되나 |`
**코드를 만들지 마라. 회차마다 나눠 묻지 마라.**
내가 못 정한 여섯을 먼저 낸다 — 임시표 칸 · 「값이 다르다」의 잣대 · `file_hash` ·
엔카 `browser` 분을 떨어뜨리는 자리 · `raw_response` 가 몇 MB 인가 · 걸음을 이어 도는가.

**2 · 위 ①②③ 을 고친다** (작고 갈래가 분명하다)

**4 · 「팔린 차」 화면 `/sold`** ← 시안까지 나왔다
규격 `UI_REVIEW` 30장 · 시안 `ref/screens/v4m_sold_시안.html`.
목록(`/listings`)에서 **팔린 차를 안 뿌리게** 기본을 바꾸고, `/sold` 를 새로 만든다.
**`gone_at` 만으로 「팔렸다」라 세지 마라** — `sales_status` 가 「판매완료」인 것과 그냥 사라진 것을 배지로 가른다.
**시안 글자를 그대로 옮겨라.** 표본 5건 미만인 칸은 내지 않는다.

**3 · K카 `resvYn` 을 읽어 `sales_status` 에 넣어 달라** ← 가이드가 오늘 쟀다
K카 목록이 **`resvYn` 을 준다 — 487건 중 `Y` 118건**. 우리는 **이 칸을 안 읽는다.**
곧 **계약 중인 차 118대가 「판매 중」으로 화면에 서 있다.**
`sales_status` 에 **사이트가 준 값 그대로** 넣어라 — `"reserved"` 로 옮기지 말고 **`"resvYn=Y"`**.
우리 말로 옮기는 것은 `dict_enum` 이 한다. 규격 `11-store/a-key` 08-29 절.
★ 헤이딜러는 `sale_status` 를 **이미 넣고 있다**(`listed`) — 손대지 마라.

## 지킬 것
하나 닫고 「몇 → 몇」 적고 다음. 첫 줄은 「안 한 것」 표. 200·303 으로 세지 않는다.
숫자를 옮겨 적지 말고 규격을 가리킨다. 남은 것은 `docs/guide/07_밀린일대장.md` ·
설계 뒤에 할 것은 `docs/guide/09_설계뒤에할것_20260829.md`.

## 수집 범위
`config/targets.json` 의 26종만 받는다. 전량을 받지 않는다. 상한은 차종별 목록 끝까지다.
전체 → 대상 20333 → 5770 [개발측 08-29]
가이드 실측 08-29 — KB 4,320 · 보배 222 · 볼보 149 · 현대 179 · 헤이딜러 199 · K카 105
BMW 364(X3 23) · 렉서스 74(대상 53) · 리본카 1,082 · 엔카는 마스터 회선

## 금지
지우는 것 · 규격을 개발측이 고치는 것 · 새 색 · 클래스 이름 바꾸기 · 짐작으로 규격 적기.
★ 설계가 확정되기 전에 수집·적재 구조를 손대는 것.

## 규격
`docs/ARCHITECTURE_20260829.md` ← 지금 읽을 것
`docs/chapters/` 10-collect · 11-store · 41-view · 60-admin · f-table
KCAR_API · KBCHACHACHA_API · VOLVO_SELEKT_API · BOBAEDREAM_API · HEYDEALER_API · REBORNCAR_API
HYUNDAI_CERTIFIED_API · LEXUS_CERTIFIED_API · KIA_CPO_API · BMW_BPS_API · ENCAR_ROBOTS
TARGET_KEY_MAP · MULTISITE_MAPPING · CROSS_SITE_COMPARE · DEDUP_CROSS_SITE · UI_REVIEW · SERVER_SURVIVAL
