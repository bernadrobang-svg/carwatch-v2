# 작업 가이드 (SPEC-2026.09.02-r1085)

★ 순서대로 한다.  ★ 막히면 회차에 적고 다음으로 간다.  ★ 답을 기다려 멈추지 마라

```
★★★ 09-01 — ★ **「끝」은 ★ 배포에서 확인한 것만이다** (마스터 인수인계 ③)
   ★ v333 이 1·2·3 을 고쳐 ★ `S46-230` 이 통과인데
   ★ ★ 배포에서는 ★ **똑같은 두 건이 24시간째 죽어 있다** (`listing_id=127`·`7998`)
   ★ ★ ★ 코드를 고친 뒤 ★ **판을 다시 돌려 ★ 성공하는 것까지**가 1·2·3 이다
★ 검산이 바뀌었다 — ★ `S46-241` 이 ★ `/admin/status` 를 열어 ★ 직접 본다
```

| # | 어느 파일 · 몇 줄 | 무엇을 한다 | 검산 |
|:--:|---|---|---|
| **0** | `tools/collect_kbchachacha.py` · `collect/runner.py` | ★★★★★ **가장 급하다.  ★ 마스터 지시 09-02 「전수 검사해」.**  ★ 실측 — KB 에 **6,261대**인데 우리는 **589대(9.4%)**.  ★ 대상 **29종 중 26종이 0건**이다 — ★ 스포티지 747 · 그랜저 625 · 모델Y 266 · Q5 217 · **GV70 전동화 23** · **iX3 22** · **C40 2** 가 다 **0**.  ★★ **호출 쿼리는 정상이다** — 내가 29종을 다 두드려 6,261을 셌다 (`carCode=9999`→0 · `3077`→1,801 · 쪽넘김 45쪽까지).  ★ **사이트는 다 준다 — 우리가 안 받는다**.  ★ 리볼트도 **281 중 57(20%)** 이다.  ★ 자는 `tools/site_coverage.py` 다 | `S46-247` |
| **0-2** | `config/endpoints.json` 헤더 | ★ **헤이딜러가 HTTP 500** 을 낸다 — ★ 우리 헤더로 목록이 안 온다.  ★ 그런데 275건을 들고 있다 — ★ **헤더가 낡았다**.  ★ **현대인증도 HTTPError** 다.  ★ 둘 다 다시 재라 | `S46-247` |
| **1** | `store/core.py:224` `since = _today(parsed)` | ★ ④ 「동시」를 **날**로 센다.  ★ **같은 `run_id` 안**으로 좁혀라.  ★ 같은 날 다시 돌리면 앞 판이 남긴 행을 또 세어 ★ **실패가 다음 실패를 만든다** | `S46-230` ＋ ★ `S46-241`(배포) |
| **2** | `store/core.py:425` `record_change(… "invariant_violation" …)` | ★ ①③ 으로 갈려 **건너뛴 것**도 행을 남기고 ★ 그 행이 1번 셈에 다시 들어간다.  ★ 빼라 | 〃 ＋ ★ `S46-241` |
| **3** | `store/core.py:449` `raise ValidationError` | ★ 판을 통째로 죽인다.  ★ `STEP 50` 8번은 **「경고 — 기록하고 계속」**이다.  ★ 그 매물만 건너뛰고 ★ 리포트에 **수로** 내라 | 〃 ＋ ★ `S46-241` |
| **4** | `collect/runner.py` `sweep_gone_groups` 호출부 | ★ **엔카가 두 달에 3건**만 gone 을 매겼다 (KB 198 · K카 149).  ★ 엔카는 차종별로 나눠 받아 「끝까지」가 안 선다.  ★ **차종 단위로 끝났으면 그 차종만** 매기게 하라 | `S46-234` |
| **5** | `report/screens/*` 목록·추천 거르개 | ★★ **새로 만들 것이 아니다 — ★ 시안대로만 하면 된다.**  ★ `ref/screens/v4m_recommend_시안.html` 에 ★ 「**판매완료는 뺐습니다**」가 ★ **처음부터 적혀 있다**.  ★ 화면이 안 지켰다 — ★ `CONTRACT` **475건** · `reserved` **17건**이 그대로 뜬다 [실측 09-01] | `S46-235` |
| **6** | `web/views.py:490` `live = active_sites(sites)` | ★ 단추를 `status` 로 가른다 — ★ **매물 550건을 못 고른다**.  ★ 윗줄 `site_counts(conn)` 로 **매물이 있는가**로 갈라라 | `S46-231` |
| **7** | `analyze/axis/spec.py` 색 축 | ★ 배점이 바뀌었다 — ★ 외장 **25**(기피 0→7) · ★ **내장색 `taste.color_int` 10 신설**.  ★ `color_int_raw` 는 97.5% 차 있다 | `S46-236` |
| **8** | `report/screens/` 추천 | ★ 추천은 ★ **예산 안 ＋ 취향**만 낸다 (마스터 09-01).  ★ 목록은 다 보여도 된다 | `S46-237` |
| **9** | `web/templates/dashboard.html:310` · `why.html:51` | ★ `.heart` 가 `position:absolute` 인데 ★ 이 칸에 ★ **기준 상자가 없다** — ★ 하트 **6개**가 `top=-4` 로 ★ 문서 맨 위에 앉는다.  ★★ 이것이 ★ **진짜 글자 겹침의 뿌리**다 [실측 09-01 — 진짜 겹침이 거의 다 `♡`·`≡`].  ★ `listings.html:313` 처럼 ★ `.cardbody`(relative)로 감싸라 | `S46-118` |
| **10** | `config/dimensions.json` | ★ 대상 **32종 중 10종만** 실측돼 있다.  ★ 남은 **22종**을 ★ 제조사 제원표에서 ★ **브라우저로** 받아 채워라 (제네시스 공식 제원표는 ★ JS 로 채워져 `curl` 로는 「-」만 온다 · 실측 09-01).  ★★ **확인된 10종은 덮어쓰지 마라**.  ★★ **세대가 갈리면 ★ 연식 구간으로 나눠 적어라** — 모델Y `~2024 4,751 / 2025~ 4,790 / L 4,976` · iX3 `1세대 4,746 / 2세대(2026~) 4,782`.  ★ 값마다 `_확인` 에 ★ **출처와 날짜**를 남겨라 — ★ 기억으로 박지 마라 (가이드가 09-01 에 ★ iX3 4,734→**4,746** · C40 4,440→**4,431** ★ 두 번 틀렸다) | `S46-233` |
| **11** | 추천·목록 카드 | ★ **순위 번호가 없다** — ★ 시안에도 화면에도 없었다 (마스터 09-01).  ★ 시안에 `rc-rank`·`lst-rank` 를 넣었다.  ★ **1부터 차례로** 낸다 | `S46-242` |
| **12** | `config/web.json` `photo_base_url` | ★ 사진 밑주소가 ★ **엔카 하나뿐**이다 (`ci.encar.com`).  ★ **열두 사이트가 통째로 사진이 없다** [실측 09-01].  ★ `site_photo_base` 를 신설해 ★ 사이트마다 깔아라 | `S46-243` |
| **13** | `report/screens/build.py:1138` `_source_url()` | ★★ **주소는 ★ 가이드가 다 쟀다 (09-01) — ★ 너희는 재지 마라.**  ★ `site_detail_url` 이 ★ **다섯 중 넷**이 찼다.  ★ 남은 하나 `kcar` 는 ★ **robots 가 막는다** (`Disallow: /bc/detail/carInfoDtl?`) — ★ `null` 그대로 두고 ★ 화면에 그렇게 적어라.  ★★★ **고칠 것은 하나** — ★ `volvo_selekt` 틀에 `{site_model}` 이 더 든다.  ★ `_source_url()` 이 지금 `source_id` **한 칸만** 넣는다 — ★ **두 칸으로 늘려라** | `S46-94` |
| **14** | 목록·추천·상세 카드 | ★ 링크·사진이 없을 때 ★ **말없이 비운다**.  ★ 「★ K카는 원문 주소를 아직 못 쟀습니다」처럼 ★ **까닭을 글로** 내라.  ★ 「우리가 못 쟀다」와 「그 매물에 없다」를 가른다 (상세가 이미 이렇게 한다) | `S46-243` |
| **15** | `collect/pipeline.py` 원문 저장 | ★ **실패 응답이 원문으로 안 남는다**.  ★ 실측 09-01 판 `20260901T165912` — ★ 두드림 **318**(error 312·not_found 3·ok 3) 인데 ★ 원문은 **32건**.  ★ **286건이 사라졌다**.  ★ 규격은 ★ 「★ **실패 응답도 원문이다** · 삭제 금지」(5장 STEP 53-⑤).  ★ 차단당한 증거가 사라져 ★ 「왜 안 왔나」를 못 캔다 | `S46-244` |
| **16** | `/admin/collect` 뒤 화면 | ★ 수집만 하고 ★ **판을 안 돌린다**.  ★ 마스터께서 누르셨는데 ★ 매물만 21,156건으로 늘고 ★ **판정은 10,458건 그대로**였다.  ★ 화면이 ★ **아무 말도 안 한다** — ★ 「★ 담았습니다.  ★ **판을 돌려야 화면에 나옵니다**」를 내라 (RULES.md 2) | `S46-243` |
| **17** | `config/dimensions.json` `MODEL_Y` | ★★ **연식으로 갈랐다 (가이드 09-02) — 너희는 값을 재지 마라.**  ★ `by_year` 를 ★ **코드가 읽게** 하라: `~2024-12` **4,751** · `2025-01~` **4,790**(주니퍼).  ★ 우리 매물 **827건이 450 대 377 로 반씩** 갈린다 — ★ 한 값이면 **39mm** 틀린다.  ★ 다른 차종도 `by_year` 가 오면 같게 다뤄라 | `S46-233` |
| **18** | `config/dimensions.json` 남은 **12종** | ★ `ES_HEV` · `EX60_IMPORT` · `G80_EV` · `GLC_IMPORT` · `GRANDEUR_LPG` · `NX_HEV` · `POLESTAR3_EV` · `RX_IMPORT` · `S60_IMPORT` · `V60CC_IMPORT` · `X3_IMPORT` · `XC60_IMPORT`.  ★★ **X3·GLC 부터** — ★ 카탈로그 못 받는 상위 여덟에 들어 매물이 많다.  ★ `tools/fetch_dimensions.py` 를 그대로 쓰면 된다 | `S46-233` |

## ★ 물음에 답한다 — `carCheckGbn1`(8) · `carCheckGbn2`(11)

```
★ **자리 표를 내가 줄 수 없다.**  ★ 나도 원문을 못 봤다 — ★ 짐작으로 주면 규격 금지다
★★ 대신 ★ **역산해라.  ★ 정답지가 우리에게 있다**
   ① 같은 매물의 ★ 엔카 성능점검 화면에 ★ **부위 이름이 글로 나온다**
      (실측 09-01 — 「교환: 헤드램프어셈블리(우) · 판금: 프런트펜더(우)」)
   ② 그 매물의 `carCheckGbn1/2` 를 ★ 나란히 놓고 ★ **1 이 선 자리**를 맞춘다
   ③ ★ 표본 **다섯 건 이상**에서 ★ 같은 자리가 같은 부위로 나오면 ★ 그때 표로 굳힌다
★ 한 건으로 정하지 마라.  ★ 안 맞으면 ★ 「못 갈랐다」로 적고 넘어가라
```

## ★ 새 검사 넷 — 같은 회차에 만든다

```
S46-241  ★ **배포**에서 재판정이 불변 필드로 멈춰 있으면 실패 (★ 가이드가 만들었다)
S46-242  ★ 시안에 ★ 「반드시 있는 것」 머리가 없으면 실패 (★ 가이드가 만들었다)
S46-243  ★ 비는 자리가 ★ 까닭을 안 내면 실패 (★ 가이드가 만들었다)
S46-234  ★ 매물 최다 사이트가 ★ 며칠째 gone 0건이면 실패
S46-235  ★ 화면에 ★ 안 파는 것(CONTRACT·SOLD·reserved)이 있으면 실패
S46-236  ★ 내장색 축이 ★ 점수를 내는가 · ★ 기피가 0점이면 실패
S46-237  ★ 추천에 ★ 예산 넘는 것이 있으면 실패
★ 만들면 ★ 일부러 깨서 ★ 잡는 것을 보고 ★ 회차에 적는다
```

## ★ 규격은 여기가 정본이다

```
사이트   BMW_BPS_API · BOBAEDREAM_API · HEYDEALER_API · HYUNDAI_CERTIFIED_API
        KBCHACHACHA_API · KCAR_API · KIA_CPO_API · LEXUS_CERTIFIED_API
        REBORNCAR_API · REVOLT_API · VOLVO_SELEKT_API · ENCAR_ROBOTS
견주기   CROSS_SITE_COMPARE · DEDUP_CROSS_SITE · MULTISITE_MAPPING · TARGET_KEY_MAP
화면·옵션 UI_REVIEW · RECOMMEND_SCREEN · OPTION_CATALOG
설계·운영 ARCHITECTURE_20260830 · ROADMAP_20260830 · SERVER_SURVIVAL
★ 배점·곡선은 ★ config/scoring.json · ★ 제원은 config/dimensions.json 이 원천이다
★★ 화면 모양은 ★ ref/screens/ 시안이 원천이다 — ★ 쓰는 법은 ★ **ref/screens/RULES.md**
```

## ★ 수집 범위 — 마스터 확정 08-23

```
★ config/targets.json 의 차종만 받는다.  ★ 전량을 받지 않는다
★ 못 좁히면 ★ 상한(쪽 수)을 정한다.  ★ 「빈 쪽까지」는 금지다
★ 실측 09-01 — ★ 20,673 → ★ 5,492 (site=all)
★ 건수는 그날의 수다.  ★ 규격에 박지 않는다
```
