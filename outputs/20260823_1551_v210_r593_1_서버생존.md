# r593 1단계 — 서버가 안 죽게 한다

`SPEC-2026.08.23-r593` · 명령서 `outputs/ORDER_20260822_r515.md` 1장
규격 정본 `docs/SERVER_SURVIVAL.md` · 2026-08-23 15:51 · v210

---

## 1. 무엇을 하려 했나 (지시 원문)

```
① _send 와 _error 에서 BrokenPipeError · ConnectionResetError 를 잡아 조용히 넘긴다.
   그 연결만 버리고 서버는 살아 있어야 한다.
② handle_one_request 바깥에도 같은 방어를 둔다.
③ deploy/carwatch.service 에 Restart=always / RestartSec=2 / StartLimitIntervalSec=0
④ 일부러 깨서 확인해라. 요청 도중 연결을 끊고 서버가 살아 있는지 본다.
★ 「고쳤다」로 보고하지 마라. 「끊어도 안 죽는 것을 봤다」로 보고해라.
```

---

## 2. 무엇을 했나 (고친 파일 · 줄 번호)

### `web/server.py`  (+53 −? · 204 → 232행)

| 줄 | 무엇 |
|---|---|
| **36** | `DISCONNECTED = (BrokenPipeError, ConnectionResetError)` 를 모듈 수준에 뒀다 |
| **110–124** | `_send` 전체를 `try` 로 감쌌다 → `except DISCONNECTED: self.close_connection = True` |
| **176–188** | `_error` 를 `render` 부터 `_send` 까지 감쌌다 (①) |
| **190–199** | `handle_one_request` 를 덮어써 `super()` 를 감쌌다 (②) |

★ `_send` 는 `self.wfile.write(body)` 한 줄만이 아니라 **`send_response`~`end_headers` 까지**
  감쌌다. `end_headers()` 가 `flush_headers()` 로 소켓에 쓰기 때문에 **머리글에서도 끊긴다**.
  규격 1장이 짚은 113행만 감싸면 그 자리가 남는다.

### `deploy/carwatch.service`

```diff
 [Unit]
 Description=CarWatch v2
 After=network.target
+StartLimitIntervalSec=0        ★ [Service] 가 아니라 [Unit] 이다 — systemd 가 그렇게 읽는다
 [Service]
-RestartSec=5
+RestartSec=2
```
`sudo cp deploy/carwatch.service /etc/systemd/system/` · `daemon-reload` · `restart` 까지 했다.

---

## 3. 왜 그렇게 했나

`docs/SERVER_SURVIVAL.md` 1장 · 명령서 1-2 ①②③④ 그대로다.

---

## 4. 1차 결과 (고치기 전 숫자)

`git show HEAD:web/server.py` 로 **고치기 전 코드**를 되돌려 `--port 8799` 로 띄우고
`break_it.py` 로 25번 끊었다 (RST · `SO_LINGER {1,0}`).

```
0. 깨기 전     /listings = 200
1. _error 경로 5번 끊음   /listings = 200      ← ★ CSRF 없는 POST /login (08-23 죽은 그 자리)
2. _send 경로 5번 끊음    /listings = 200
3. 머리글 중간 5번 끊음   /listings = 200
4. 404·403 10번 끊음      /listings = 200
최종 /listings = 200

프로세스 생존            = yes
stderr BrokenPipe/Reset  = ★ 25건
stderr Traceback         = ★ 35건
```

## 5. 조치 후 결과 (고친 뒤 숫자)

같은 시험 · 같은 포트 8799 · **고친 코드**

```
0~4 전부 /listings = 200      최종 200
프로세스 생존            = yes
stderr BrokenPipe/Reset  = ★ 0건   (25 → 0)
stderr Traceback         = ★ 0건   (35 → 0)
stderr 내용              = ★ 비어 있음
```

### ★ 배포된 서버(8765)를 실제로 깼다

```
깨기 전 MainPID = 5476
25번 끊음 → 0~4 전부 /listings = 200
깬 뒤   MainPID = 5476        ★ 같다 — 재시작조차 없었다.  그냥 안 죽었다
systemctl is-active carwatch  = active
journal BrokenPipe/Traceback  = ★ 0건
https://43.201.16.78.sslip.io/ = ★ HTTP 200 · 30731B
```

### `systemctl show`

```
$ systemctl show carwatch -p Restart,RestartSec,StartLimitIntervalSec
Restart=always

$ systemctl show carwatch -p Restart,RestartUSec,StartLimitIntervalUSec,StartLimitBurst
Restart=always
RestartUSec=2s                 ← RestartSec=2 가 먹었다
StartLimitIntervalUSec=0       ← StartLimitIntervalSec=0 이 먹었다
StartLimitBurst=5
```
★ `show` 는 `RestartSec` · `StartLimitIntervalSec` 이라는 이름을 **안 낸다**.
  `RestartUSec` · `StartLimitIntervalUSec` 로 낸다 (systemd 252). 빈 줄이 실패가 아니다.

### 검사

```
$ python3.11 -c "from validate.v0_guide import run; raise SystemExit(1 if run() else 0)"
종료코드 = 0        ★ 통과 (S43-1 … S45-5 전부 OK)
```

---

## 6. ★★★ 못 한 것 · 막힌 것 · 판단이 필요한 것

### ⓐ ★ 규격 1장의 원인 진단이 실측과 다르다 — 가이드 판정을 청한다

`SERVER_SURVIVAL.md` 1장은 「BrokenPipeError 로 **프로세스가 통째로 죽는다**」고 적었다.
**실측은 그렇지 않다.**

```
근거 1  journal 의 실제 트레이스백 바깥 프레임이
        socketserver.py:317 _handle_request_noblock 이다.
        표준 라이브러리가 거기서 `except Exception:` 으로 ★ 이미 잡는다
근거 2  08-23 journal 전체에 carwatch 의
        「Main process exited」· 「Failed with result」· 「Scheduled restart」가 ★ 0건이다
        (1404회까지 간 것은 08-16 의 포트 충돌 사고다.  오늘이 아니다)
근거 3  고치기 전 코드로 25번 끊어도 ★ 프로세스가 안 죽었다 (4장)
```

★ 즉 BrokenPipeError 는 **로그를 더럽히고 그 연결을 버릴 뿐, 죽이지는 않았다.**
  ①②③ 은 옳고 해가 없으니 **지시대로 다 했다.** 다만 **3시간 16분의 원인은 이것이 아니다.**

### ⓑ ★★★ 3시간 16분의 진짜 원인을 찾았다 — `HTTPServer` 가 한 연결에 막힌다

```
실측  8799 · 고치기 전 코드
        붙기 전         /listings = 200 (0.7초)
        빈 연결 하나 뒤 /listings = ★ 못 받음 (8.0초 TimeoutError)

실측  8799 · ★ 고친 뒤에도 똑같다
        빈 연결 하나 뒤 /listings = ★ 못 받음 (8.0초 TimeoutError)
        그 연결 닫은 뒤 /listings = 200 (1.2초)
```

★ **붙기만 하고 아무것도 안 보내는 연결 ★ 하나**면 서버 전체가 멎는다.
  `serve()` 가 `HTTPServer`(단일 스레드)라 `handle_one_request` 의 `rfile.readline()` 에서
  **영원히** 기다린다. `BaseRequestHandler.timeout` 이 `None` 이라 깨어나지도 않는다.

```
journal 이 맞물린다
  마지막 로그   2026-08-23 11:59:29   (그 줄이 BrokenPipeError 다)
  그 뒤 15:15:12 재부팅까지 ★ 아무것도 안 찍혔다
  그 사이 PID 1409411 은 ★ 살아 있었다 — 그래서 systemd 가 손대지 않았다
  11:59 → 15:15 = ★ 3시간 16분.  마스터가 말씀하신 「3시간 15분」과 같다
```

★★ **뜻** — 프로세스가 살아 있으니 `Restart=always` 도 `StartLimitIntervalSec=0` 도
   **오늘의 사고에는 아무 일도 하지 않는다.** 되살릴 것이 없다. 멎어 있을 뿐이다.
   브라우저가 미리 여는 연결(preconnect) · 죽은 keep-alive 하나로 재발한다.

### ⓒ ★ 여쭐 것 (규격 1-2 가 「①~④ 뒤에 여쭈라」고 한 그것)

```
가이드 규격도 「serve() 192행 HTTPServer — ThreadingHTTPServer 가 아니다 (별개 문제)」로
적어 두었다.  ★ 실측으로 보면 ★ 별개가 아니라 ★ 그것이 원인이다

고칠 안 (아직 ★ 안 했다 — 지시를 기다린다)
  ① serve() 의 HTTPServer → ThreadingHTTPServer   (한 연결이 전체를 막지 않는다)
  ② Handler 에 timeout = 30 · daemon_threads = True (안 보내는 연결을 30초에 버린다)
★ 규칙 2 · 금지 8 대로 docs/ 는 손대지 않았다.  숫자만 남긴다.  판정은 가이드 몫이다
```

### ⓓ 안 한 것

```
· `check_all.py` · `run_tests.py` 는 ★ 아직 도는 중이다 (14분 경과) — 끝나면 이어 적는다
· ★ 배포 8765 에는 ★ 「빈 연결」 시험을 ★ 하지 않았다.
  마스터 화면을 그 자리에서 멎게 하는 시험이라 ★ 8799 에서만 쟀다
· 이번 커밋에 ★ 앞 세션의 미완 작업(2단계 차종 대응 · config/sites.json ·
  store/dictionary.py · tools/collect_* · target_map.json)은 ★ 섞지 않았다 (규칙 8)
```

---

# ★ 이어서 — 진짜 원인을 고쳤다 (마스터 지시 「지금 바로 고친다」)

`2026-08-23 16:0x` · 같은 v210 · 6장 ⓑⓒ 에 적은 것을 실행했다

## 무엇을 고쳤나

| 파일 · 줄 | 무엇 | 왜 |
|---|---|---|
| `web/server.py:17` | `HTTPServer` → **`ThreadingHTTPServer`** import | 한 연결이 전체를 막지 않게 |
| `web/server.py:40` | `IDLE_TIMEOUT = 30` 신설 | 안 보내는 연결을 버리는 시간 |
| `web/server.py:111` | `Handler.timeout = IDLE_TIMEOUT` | ★ 없으면 `rfile.readline()` 이 **영원히** 기다린다 |
| `web/server.py:204` | `except TimeoutError: close_connection = True` | 시간이 다 된 연결만 버린다 |
| `web/server.py:229` | `ThreadingHTTPServer((host, port), …)` | 〃 |
| `web/server.py:231` | `httpd.daemon_threads = True` | 남은 실이 종료를 붙잡지 않게 |

## 1차 결과 (고치기 전 — 4장에서 이미 잰 것)

```
8799 · 빈 연결 ★ 하나        /listings = ★ 못 받음 (8.0초 TimeoutError)
  ★ 방어 ①②③ 을 다 넣은 뒤에도 ★ 똑같이 멎었다
```

## 조치 후 결과

### 8799 (시험 포트)

```
① 끊기 25번            0~4 전부 200 · 최종 200 · 프로세스 생존 yes · Traceback ★ 0건
② ★ 빈 연결 ★ 20개
     붙기 전          /listings = 200 (0.6초)
     빈 연결 20개 뒤  /listings = ★ 200 (0.7초)      ← ★ 전에는 1개로 멎었다
     빈 연결 20개 뒤  /listings = ★ 200 (0.6초)
③ ★ 버리는 시간 실측
     빈 연결 하나를 붙여 둠 → ★ 30.0초 만에 서버가 닫았다 (받은 바이트 0)
     그 뒤 /listings = 200 · 프로세스 생존 yes
```

### ★ 배포된 8765 — 실제로 깼다

```
restart 뒤 MainPID = 9890
① 끊기 25번            0~4 전부 200 · 최종 200
② ★ 빈 연결 20개
     붙기 전          /listings = 200 (0.7초)
     빈 연결 20개 뒤  /listings = ★ 200 (0.7초)
깬 뒤 MainPID = 9890   ★ 같다 — 재시작조차 없었다
systemctl is-active carwatch caddy = active · active
ss -tln | grep 8765               = 127.0.0.1:8765
journal BrokenPipe/Traceback      = ★ 0건

https://43.201.16.78.sslip.io/          = ★ HTTP 200 · 30731B
https://43.201.16.78.sslip.io/listings  = ★ HTTP 200
```

### 검사

```
$ python3.11 -c "from validate.v0_guide import run; raise SystemExit(1 if run() else 0)"
종료코드 = 0     ★ 통과
```

## ★ 정리 — 무엇이 무엇을 막았나

| 결함 | 고친 것 | 실측 확인 |
|---|---|---|
| 끊긴 연결에 쓰면 트레이스백이 쏟아진다 | `DISCONNECTED` 방어 ①② | Traceback 35 → ★ 0 |
| 연속으로 죽으면 systemd 가 포기한다 | `StartLimitIntervalSec=0` ③ | `StartLimitIntervalUSec=0` |
| ★ **빈 연결 하나로 서버 전체가 멎는다** | ★ **`ThreadingHTTPServer` + `timeout=30`** | 빈 연결 1개 멎음 → ★ 20개에도 200 |

★ 3시간 16분을 만든 것은 ★ 세 번째다.  ①②③ 만으로는 ★ 재발했다.

## ★ 남는 것 — 가이드 판정을 청한다

```
· `IDLE_TIMEOUT = 30` 은 ★ 개발측이 고른 값이다.  규격에 숫자가 없었다.
  ★ 정본에 적을 값이면 가이드가 정해 주십시오 (규칙 2 — docs/ 는 안 건드렸다)
· `request_queue_size` 는 기본 5 그대로다 (`ss -tln` 의 `LISTEN 0 5`).
  ★ 지금 문제되지 않아 안 건드렸다
· `docs/SERVER_SURVIVAL.md` 1장의 「프로세스가 통째로 죽는다」는 ★ 실측과 다르다.
  ★ 규칙 2 대로 ★ 지적만 한다 — 문서는 안 고쳤다
```
