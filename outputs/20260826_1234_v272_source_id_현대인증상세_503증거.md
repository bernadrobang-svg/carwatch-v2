# 20260826_1234_v272 — 원문은 source_id 로 잇는다 · 현대인증 상세 177건 · ★ 503 의 증거를 잡았다

마스터 지시 08-26 ①②③⑤

---

## 1. 무엇을 하려 했나 — 지시 원문

```
① 원문 이음 — 정본은 사이트 매물번호(source_id)다
   ★ raw_response 에 source_id 를 채워라
   ★ request_url 에서 되뽑는 건 임시다. 규격이 아니다
   ★ listing_id 도 지우지 말고 둘 다 둬라
   ★ 검사 S46-97
② 상세는 ★ 현대인증부터. 한 번에 다 하지 마라
   ★ 하나 끝내고 「등급이 몇에서 몇으로」를 적고 다음으로
③ 파이프라인을 웹에서 떼어내라
   ★ 먼저 503 이 그 탓인지 로그로 짚어라. 아니면 또 죽는다
⑤ ★ 「판정 중」이 화면에 아직 안 보인다
   현대인증에 판정 중 0 · E/F 30 이다
   ★ 재판정이 안 돈 것인지 화면이 안 고쳐진 것인지 짚어라
```

---

## 2. ★ ⑤ 먼저 — 둘 다 아니었다.  ★ 규칙이 제대로 먹은 것이다

```
★ 재판정  돌았다 (DB 에 판정 중 410건이 들어가 있었다)
★ 화면    고쳐졌다 (「판정 중」이 나온다)
```

마스터께서 보신 **「30」은 한 쪽의 줄 수**였다. 실측 —

```
/listings?site=hyundai_cert                → 174건 (한 쪽 30줄)
/listings?site=hyundai_cert&grade=PENDING  →   6건
```

쪽을 넘겨 보면 그대로 보인다.

| 쪽 | 나오는 등급 |
|---|---|
| 1쪽 | E 15 · F 15 |
| 5쪽 | F 21 · G 9 |
| **6쪽 (마지막)** | G 18 · ★ **판정 중 6** |

★ **「판정 중」은 등급순 맨 뒤**라 첫 쪽 30줄에 안 온다 — 67장이 시킨 그대로다.
현황(`/`)의 등급 줄도 `… F · G · 제외 · 판정 중` 으로 **맨 뒤**에 있다.

★ **진짜 물음은 따로 있었다** — 「왜 현대인증은 판정 중이 6건뿐인가」.
근거가 **481·535/910** 으로 절반선 455 를 **넘어서**다. 그래서 등급이 나오고,
그 등급이 F 126 · G 27 이다. **그것이 ② 로 이어진다.**

---

## 3. ★ ① 원문 이음 — 정본은 `source_id` 다

### 3-1. 1차 결과 — ★ `source_id` 는 이미 다 차 있었다

```
raw_response — source_id 가 빈 것
  encar            174,618건 중 3,785건 (2.2%)  ← 전부 list 3,645 · facet 140
  kbchachacha          180건 중     0건
  kcar                  70건 중     0건
  lexus_certified       36건 중     0건
  reborncar             23건 중     0건

엔카 endpoint 별 — detail 20,741 · inspection 20,959 · record 20,959 …  ★ 전건 0
                  빈 것은 list 3,645 · facet 140 뿐  ★ 매물 봉투가 아니라 옳다
```

★ **채울 것이 없었다.  문제는 「잇는 쪽」이었다.**

```
tools/fill_photos.py:31  _sid_from_url()
   「★ raw_response.listing_id 는 우리 내부 번호다.  그러므로 주소에서 뽑는다」
   ★ 마스터께서 「임시다.  규격이 아니다」 하신 바로 그 자리다
```

### 3-2. 고친 곳

| 파일 | 무엇 |
|---|---|
| `tools/fill_photos.py` | `_sid_from_url()` 을 **지웠다**. `raw_response.source_id` 로 잇는다 |
| `store/raw.py` | `save_site_raw` 가 `source_id` 로 `core_listing` 을 찾아 **`listing_id` 도 함께** 넣는다 — 둘 다 둔다 |
| `tools/link_raw_ids.py` (새로) | 이미 쌓인 것을 메운다. **원문을 다시 안 받는다** |
| `validate/v0_guide.py` | 검사 **`S46-97`** |

★ 옛 방식과 **같은 숫자**가 나온다 — 이음이 맞다는 증거다.

```
python3.11 tools/fill_photos.py
  kbchachacha 180 → 사진 찾음 180      (옛 방식과 같다)
  kcar         70 → 사진 찾음  70      (〃)
  reborncar    23 → 사진 찾음  23      (〃)
```

### 3-3. 조치 후

```
python3.11 tools/link_raw_ids.py --write
  ★ source_id 로 이어지는데 listing_id 가 빈 원문
    kbchachacha  180 · kcar 70 · lexus_certified 35 · reborncar 3   합 288
  ★ 채웠다 288건 · 남은 것 0건
```

| | 1차 | 조치 후 |
|---|--:|--:|
| 이어지는데 `listing_id` 가 빈 원문 | **288** | ★ **0** |
| 주소에서 되뽑는 코드 자리 | **1** (`fill_photos.py:31`) | ★ **0** |

```
S46-97  통과 — source_id 로 이어진 원문 168,566건 · 되뽑는 자리 없다
```

`S46-97` 은 셋을 본다 — ① 매물별 원문에 `source_id` 가 비었나 (`list`·`facet` 등
매물 봉투가 아닌 것은 뺀다) ② `source_id` 로 이어지는데 `listing_id` 가 비었나
③ **코드가 아직 주소에서 매물번호를 되뽑고 있나**. ★ ③ 이 뿌리다 — 되뽑는 자리가
남아 있으면 언젠가 또 그리로 샌다.

---

## 4. ★ ② 현대인증 상세 — 「등급이 몇에서 몇으로」

★ 한 번에 다 하지 않았다. **현대인증 하나만** 했다.

### 4-0. 알아낸 것 — ★ 어댑터를 만들 필요가 없었다

```
tools/collect_hyundai_cert.py --detail [N]     ★ 이미 있었다
parse/hyundai_cert/mapping.py                  ★ 이미 있었다
```

★ **안 돌렸을 뿐이다.**  `raw_response` 가 0건이었던 까닭이 그것이다.
(v271 에서 「어댑터 5개를 새로 만들어야 한다」고 적었는데, 현대인증은
**어댑터 길이 아니라 도구 길**로 이미 뚫려 있었다. 그만큼 일이 작았다.)

### 4-1. 받았다

```
python3.11 tools/collect_hyundai_cert.py --detail 200
  ★ 저장 178건 · site='hyundai_cert'
  ★ 상세 — 정상 177 · 못 받음 1
  ★ 재판정을 큐에 넣었습니다 — a4636615
```

### 4-2. ★ 등급이 몇에서 몇으로 (현대인증 174건)

| 등급 | 상세 받기 전 | 상세 받은 뒤 | 차 |
|---|--:|--:|--:|
| F | 126 | **130** | +4 |
| G | 27 | **28** | +1 |
| E | 15 | **16** | +1 |
| **판정 중** | **6** | ★ **0** | **−6** |
| 합 | 174 | 174 | — |

| | 전 | 후 |
|---|--:|--:|
| `raw_response` | **0건** | ★ **177건** |
| 근거점수 평균 | 501.5 | ★ **531.6** |
| 근거점수 최소 | 330.0 | ★ **473.0** |
| 근거점수 최대 | 535.0 | 535.0 |

★★ **근거는 올랐는데 등급은 안 올랐다.**  판정 중이던 6건도 근거가
절반을 넘어 F·G 로 갔다 — ★ 마스터께서 ⑤ 에서 「판정 중 0」이라 하신 그 모습이
**이번에 진짜로** 됐다.

### 4-3. ★ 왜 안 올랐나 — 축으로 짚었다

현대인증 174건, 26축 가운데 **아직 비어 있는 축의 배점 합 241 / 910**.

| 축 | 배점 | 상세로 찼나 | 어디에 있나 |
|---|--:|:--:|---|
| `state.accident` 사고 | 51 | ★ **찼다** (0 → 174) | 「내차피해이력」 |
| `value.origin` 신차가 | 75 | ★ **찼다** | 상세 |
| `history.owner` 소유자 | 11 | ★ **찼다** | 「소유자 변경」 |
| `history.seizing` 압류 | 8 | ★ **찼다** | 「압류/저당」 |
| `warranty.power` 동력 | 32 | ★ 166/174 | 「잔여 보증」 |
| `warranty.general` 일반 | 22 | ★ 167/174 | 〃 |
| **`state.frame` 골격** | **43** | ✘ 0/174 | ★ **성능점검기록부** — 아직 안 열었다 |
| **`taste.option` 옵션** | **43** | ✘ 0/174 | ★ **아래 4-4** |
| **`state.my_cost` 내차수리비** | **28** | ✘ 0/174 | ★ 성능점검기록부 |
| **`state.outer` 외판** | **28** | ✘ 0/174 | ★ 성능점검기록부 |
| **`history.use` 용도** | **22** | ✘ 0/174 | ★ 「통합이력 조회하기」 |
| **`state.special` 특수사고** | **21** | ✘ 0/174 | ★ 성능점검기록부 |
| **`history.not_join` 자차미가입** | **18** | ✘ 0/174 | ★ 「통합이력 조회하기」 |
| **`state.consumable` 소모품** | **15** | ✘ 0/174 | ★ 사이트가 안 준다 (규격이 그렇게 적었다) |
| **`state.leak` 누유** | **15** | ✘ 0/174 | ★ 성능점검기록부 |
| **`state.integrity` 정비** | **8** | ✘ 0/174 | ★ 성능점검기록부 |

```
★ 성능점검기록부 안일 것  frame 43 + outer 28 + my_cost 28 + special 21
                        + leak 15 + integrity 8  =  ★ 143점
★ 「통합이력 조회하기」 안  use 22 + not_join 18                =  ★  40점
★ 옵션                                                        =  ★  43점
★ 사이트가 안 준다        consumable                          =     15점
```

★ **규격이 이미 그렇게 적어 두었다** (`HYUNDAI_CERTIFIED_API.md` 「못 채우는 축」) —
「`state.frame` 골격 · `state.outer` 외판 — ★ 「성능점검기록부」 안에 있을 것이다.
★ **아직 안 열었다**」 · 「`history.not_join` · `history.usage` — ★ 「통합이력 조회하기」 안일 수 있다」.

★ **그러므로 다음 걸음은 「그 두 문을 여는 것」이다** — 새 사이트가 아니라
**같은 사이트의 안쪽 문 둘**이다. 143 + 40 = **183점**이 거기 있다.

### 4-4. ★ 옵션 43점은 ★ 결함이 아니었다 — ★ 규격대로다

파서는 옵션을 **이름으로 잘 읽고 있다.**

```
core_listing 중 옵션이 든 현대인증 매물  ★ 1,123건
예) ["내비게이션","하이패스","열선 스티어링 휠","열선시트","통풍시트",
     "전동시트","가죽 시트","전동식 트렁크","선루프","헤드업 디스플레이", …]
```

그런데 축은 0/174 다. 규격을 열어 보니 **그렇게 하라고 적혀 있다.**

```
docs/chapters/30-score/f-table.md:1222  4-2. 옵션 ★ 43 (축 id taste.option)
  식   내 옵션가 합 v · 그 차종·트림의 옵션가 합 P90 을 B 로
      score = min(v / B, 1) × 20
  ② 카탈로그가 없어 ★ 가격 미상  →  0점 · ★ 확인 안 됨
```

★ **우리는 옵션의 「값」으로 잰다.  현대인증은 「이름」만 준다.**
그래서 ② 로 떨어져 「확인 안 됨」이 된다 — ★ 코드가 규격대로 움직인 것이다.

★ **판단을 청한다 (규칙 2).**  규격 자신이 「채우는 법은 `f-table` 「사이트별
채우기」가 정한다」고 미뤄 두었다. **이름만 주는 사이트의 옵션 축을 어떻게
채울지** 정해 주시면 그대로 한다. 지어내지 않는다.

---

## 5. ★★★ ③ 503 — ★ 증거를 잡았다

### 5-1. 코드로 짚은 것 — 파이프라인은 웹과 ★ 같은 프로세스다

```
collect/worker.py:125   """소비기를 띄운다.  ★ 웹 서버와 같은 프로세스에서 돈다.
                           데몬 스레드다 — 서버가 죽으면 함께 죽는다"""
collect/worker.py:145   threading.Thread(target=loop, name="carwatch-queue", daemon=True)
run.py:283-285          cmd_web 안에서 그 스레드를 띄운다
```

### 5-2. ★ 로그로는 못 짚었다 — ★ 기록이 없었다

```
Caddy 접근 로그가 ★ 꺼져 있었다.  경고만 남아 있었다
  8월 25 19:31  readfrom tcp 127.0.0.1:…->127.0.0.1:8765: clie…
  8월 26 02:45  (ACME 갱신 — 그 뒤 죽을 때까지 아무것도 없다)
carwatch 는 GET·POST 를 ★ 한 줄도 안 남긴다
```

★ **그래서 「503 이 파이프라인 탓인가」를 잴 수가 없었다.  모른다고 적는다.**

### 5-3. ★ 그래서 재는 것을 먼저 세웠다 (마스터 「먼저 로그로 짚어라」)

| 어디 | 무엇 |
|---|---|
| `/etc/caddy/Caddyfile` | `log { output stderr · format json }` — journald 가 받는다 |
| `collect/pipeline.py` | `rss_mb()` — 단계 **시작·완료 줄에 메모리**를 함께 찍는다 |

```
확인   {"…","uri":"/listings","status":200,…}      ★ 접근 로그가 남는다
      S10  8288/8288  완료 · 7.3초 · 메모리 17MB   ★ 메모리가 찍힌다
```

★ 파일로 쓰려다 `caddy validate` 를 root 로 돌리는 바람에 root 소유
`access.log` 가 생겨 **reload 가 한 번 실패**했다. **사이트는 200 그대로였다.**
파일을 지우고 `stderr` 로 바꿔 통과시켰다.

### 5-4. ★★★ 그리고 바로 증거가 나왔다

현대인증 상세를 받자 도구가 **재판정을 큐에 넣었고**, 웹 프로세스가
**`from_step=S5` 로 전량 재처리**를 시작했다 — ★ **08-26 에 죽기 직전과 같은 모양이다.**
`/proc/1836/status` 로 20초마다 쟀다.

| 시각 | carwatch RSS | 남은 메모리 | swap | `/listings` |
|---|--:|--:|--:|:--:|
| 12:17:51 | **615MB** | 473MB | 572MB | 200 |
| 12:18:31 | 667MB | 432MB | 659MB | 200 |
| 12:19:31 | 717MB | 392MB | 680MB | 200 |
| 12:20:40 | **758MB** | 351MB | 719MB | 200 |
| 12:21:32 | 760MB | 343MB | 716MB | 200 |
| 12:23:12 | **761MB** | 339MB | 726MB | 200 |
| 12:24:08 | 721MB | 383MB | 733MB | 200 |
| 12:31:34 | 722MB | 369MB | 732MB | 200 |

```
★ 100초에 ＋102MB 로 올랐다 (615 → 717)
★ swap 을 572 → 733MB 까지 먹었다.  ★ 장비 메모리는 1,841MB 다
★ 760MB 에서 평평해졌고 ★ 이번에는 안 죽었다.  ★ 화면도 내내 200 이었다
```

★★ **잰 것으로 말할 수 있는 것**

```
① 파이프라인은 ★ 웹 프로세스 안에서 돈다 (코드 · 실측 둘 다)
② 그것이 돌면 ★ 웹 프로세스가 ★ 760MB 를 쥔다 — ★ 장비의 41% 다
③ swap 을 733MB 까지 쓴다 — ★ 남은 여유가 340MB 밖에 안 된다
④ ★ 이번에는 버텼다.  ★ 08-26 에는 못 버텼다 —
   그때는 ★ S5 66,717건 ＋ S6 130,040행이었고 ★ 이번은 훨씬 작다
```

★ **모르는 것** — 08-26 의 그 죽음이 **정말 메모리였는지는 여전히 못 갈랐다.**
이번 기록은 「그럴 만하다」를 보여 줄 뿐 **그때를 증명하지 못한다.**
★ 이제 접근 로그와 메모리 줄이 남으므로 **다음 한 번이면 갈린다.**

### 5-5. ★ 떼어내는 것은 아직 안 했다

마스터께서 **「먼저 503 이 그 탓인지 로그로 짚어라」** 하셨다.
아직 **한 바퀴치 기록밖에 없다.** 떼어내는 것은 구조를 바꾸는 일이라
**한 바퀴 더 재고 결정하기를 청한다.** 지금 재 둔 것으로는 —

```
★ 떼어낼 만하다는 쪽   웹이 760MB 를 쥔다.  화면과 메모리를 다툰다
★ 급하지 않다는 쪽    이번엔 버텼고 화면이 내내 200 이었다
★ 못 정할 것         08-26 의 죽음이 이 탓인지 — ★ 다음 한 번이면 갈린다
```

---

## 6. check_all · run_tests 실제 출력

```
python3.11 tools/check_all.py
  통과 394 · fatal 6 · warn 14 · 환경 차이 1 · 미실행 8
  ■ FATAL
    V1-23 · V2-01 · V11-12 · V11-159 · V11-45 · V11-76
    ★ 여섯 다 전부터 있던 것이다.  ★ S46-97 은 통과다

python3.11 tools/run_tests.py
  결과: 실패 — test_endtoend.py  (1파일)
  ★ V11-12 규격 중복 행 탓 — ★ 네 번째 지적이다
```

---

## 7. 지적 — 규격 쪽 (규칙 2)

1. ★★ **`docs/chapters/61-web.md:196`·`:209` 의 `/admin/users` 중복** — **다섯 번째 지적**.
   한 줄만 지우면 `test_endtoend` 가 통과한다.
2. ★ **`taste.option` — 이름만 주는 사이트를 어떻게 채우나** (4-4장).
   규격 자신이 「`f-table` 「사이트별 채우기」가 정한다」로 미뤄 두었다.
3. ★ **`S46-97` 이 규격에 없다** — 코드에만 있다 (`S46-94`·`S46-95`·`S46-96` 과 같다).
4. ★ **67장의 절반선** — 현대인증이 이번에 **판정 중 0** 이 됐다. 근거 531.6/910 인데
   등급은 F 130 · G 28 이다. `pending_confirmed_ratio` 판단을 다시 청한다.

---

## 8. 못 한 것 · 다음

| # | 무엇 | 왜 |
|:--:|---|---|
| **1** | ★ **현대인증 안쪽 문 둘** — 성능점검기록부(143점) · 통합이력(40점) | ★ **다음 걸음이다.** 새 사이트보다 이것이 먼저다 — 같은 사이트에서 183점이 나온다 |
| 2 | ★ 볼보 → 리본카 → BMW → 렉서스 | 마스터께서 정하신 차례. **현대인증을 마저 끝내고** 간다 |
| 3 | 파이프라인 떼어내기 | 한 바퀴 더 재고 결정을 청한다 (5-5장) |
| 4 | 미분류 9,089건 | 71장 그대로 (마스터 ④) |
| 5 | ⓑ 코드 47칸 | 마스터께 청해 두었다 (v271 7장) |
