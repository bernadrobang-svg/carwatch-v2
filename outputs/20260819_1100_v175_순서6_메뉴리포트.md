# 순서 6 — `LABELS`·`MENU_TIPS` 에 `/reports` · `V11-54` 확장 · `V11-150` 신설

`SPEC-2026.08.19-r409` · 2026-08-19 11:00 · v175

---

## 1. 무엇을 하려 했나 (지시 원문)

```
6   LABELS·MENU_TIPS 에 /reports — 지금 LABELS 는 /admin/status 에서 끝난다
    V11-54 를 STEP 142a 표까지 넓히고 V11-150 신설
```

근거 `docs/chapters/61-web.md` STEP 142a (개정 396)

```
검산   V11-54  ★ 관리 메뉴 + 이 표 둘 다와 대조한다
       V11-150 메뉴 라벨이 「/」로 시작하는 것이 하나라도 있는가 (있으면 실패)
```

---

## 2. 1차 결과 (고치기 전 숫자)

```
check_all   통과 300 · fatal 3 · warn 7 · 환경 차이 1 · 미실행 5
run_tests   결과: 통과
화면        메뉴에 「/reports」가 경로 그대로 떴다
V11-54      통과 — ★ 거짓 통과다.  관리 메뉴표(/admin…)만 보고 있었다
V11-150     없음
```

★ `web/app.py:95` 에 「이름이 없으면 경로를 그대로 낸다」는 대체 규칙이 있어
**빠뜨려도 화면이 안 죽습니다.** 조용히 경로가 나옵니다.
`web/app.py:53` 주석이 이미 그 경고를 적어 뒀는데 또 반복됐습니다 —
**검사가 절반만 봐서 막지 못했습니다.**

---

## 3. 무엇을 했나 · 왜 그렇게 했나

### ① 이름을 넣었다 — `web/app.py`

| 파일 · 줄 | 넣은 것 |
|---|---|
| `web/app.py` `LABELS` | `"/reports": "리포트",` |
| `web/app.py` `MENU_TIPS` | `"/reports": "낸 리포트를 화면에서 읽고 내려받습니다",` |

값은 제가 지은 것이 아니라 **STEP 142a 표에 적힌 그대로**입니다.

### ② `V11-54` 를 두 표 다 보게 넓혔다 — `validate/v11_web.py:1334`

```
전   MENU_TABLE(60-admin/c-tools.md) 에서 `/admin[a-z/]*` 만 긁었다
후   _menu_paths() 가 둘을 합친다
     ① 관리 메뉴표    60-admin/c-tools.md      — 이름 칸이 없어 경로만
     ② STEP 142a 표   61-web.md                 — (경로, 이름) 둘 다
```

`61-web.md` 는 **「STEP 142a」부터 다음 `---` 까지만** 봅니다.
문서 전체를 훑으면 딴 표의 줄을 주워 거짓 실패를 냅니다.
표의 굵은 표시(`| **`/reports`** | **리포트** |`)도 벗겨 냅니다.

넓히면서 잡는 것이 셋으로 늘었습니다.

```
① 표에 있는데 LABELS 에 이름이 없다
② ★ 표는 「리포트」인데 코드는 딴 이름이다      ← 새로 잡는다
③ MENU_TIPS 에 설명이 없다                    ← 새로 잡는다
★ 표가 정본이다.  코드에 딴 이름을 지어 두지 않는다 (규칙 1)
```

### ③ `V11-150` 을 만들었다

```
V11-150  메뉴 라벨이 「/」로 시작하는 것이 있는가   FATAL · run · KIND_CODE
★ LABELS 를 보지 않는다.  menu_items() 가 실제로 그린 메뉴를 본다
  LABELS 만 보면 위 ①의 대체 규칙(이름이 없으면 경로를 낸다)을 못 잡는다
익명 · 마스터 두 사람 몫을 다 그려 본다
```

★ **이것이 이번 결함을 바로 잡는 검사입니다.** `V11-54` 는 「표와 코드가
어긋났다」를 잡고, `V11-150` 은 「그래서 화면에 무엇이 나왔나」를 잡습니다.
둘 다 있어야 합니다 — 표에 없는 경로가 메뉴에 붙어도 `V11-150` 이 잡습니다.

---

## 4. ★ 검사가 진짜로 잡는지 되살려 봤다

```
되살린 결함                       V11-54                              V11-150
① LABELS 에서 /reports 를 뺐다    실패 「/reports — 메뉴표에 있는데      실패 「메뉴에 경로가
                                       이름이 없다」                    그대로 나온다: /reports」
② 이름을 「보고서」로 바꿨다      실패 「/reports — 표는 「리포트」인데   통과 ★ 맞다.  경로가
                                       코드는 「보고서」다」             아니라 이름은 나왔다
되돌린 뒤                         통과                                 통과
```

★ ②에서 `V11-150` 이 통과하는 것이 **맞습니다.** 그 검사는 「경로가
나왔는가」만 봅니다. 이름이 틀린 것은 `V11-54` 몫입니다 — 나눠 놨습니다.

★ 화면으로도 봤습니다.

```
python3.11 tools/render_screens.py
grep -o ">리포트<"  outputs/render/listings.html   →  >리포트<
grep -c ">/reports<" outputs/render/listings.html  →  0
```

---

## 5. 조치 후 결과 — ★ 실제 출력

```
python3.11 tools/check_all.py

통과 301 · fatal 3 · warn 7 · 환경 차이 1 · 미실행 5

■ FATAL — 고쳐야 한다
  V1-11     예외로 종료된 실행이 없음 — 0단계
  V4-11     unclassified 존재 — 32
  V3-38     facet 수신 후 목록 관측분과 대조함 — facet 과 맞은 값 55 · 목록에만 있는 값 0
```

```
python3.11 tools/run_tests.py
결과: 통과
```

```
python3.11 tools/build_index.py     ★ 검사를 더했으므로 다시 돌렸다
  소스 135개 → docs/SOURCE.md
  DB 표 44개 → docs/SCHEMA.md
  지시서 87파일 → docs/INDEX.md

python3.11 tools/check_src.py
  S28 검사 색인 (규격 ↔ 코드)   부분  통과 339 · 미착수 44 · 실패 0
  결과: 실패 — S13(20) / S16(68) / S25(2) / S26(21)
```

| | 전 | 후 |
|---|--:|--:|
| check_all 통과 | 300 | **301** |
| fatal | 3 | **3** |
| run_tests | 통과 | **통과** |
| S28 실패 | 8 | **0** |
| 메뉴에 뜬 경로 | 1 (`/reports`) | **0** |
| 메뉴표를 보는 검사 | 1표 | **2표** |

★ fatal 셋은 이번 작업과 무관하게 남아 있던 것입니다.
`V4-11`(32건)·`V1-11` 은 지시대로 **가이드 몫**입니다.
`V3-38` 「기타 색상」은 **순서 9** 에서 닫습니다.

---

## 6. 고친 파일

```
web/app.py                  LABELS · MENU_TIPS 에 /reports
validate/v11_web.py         _menu_label_check(V11-54) 확장 · _menu_paths() 신설
                            _menu_no_path_check(V11-150) 신설 · C 표에 V11-150 등록
docs/CHECKS.md · SOURCE.md · INDEX.md · SCHEMA.md    build_index.py 가 다시 만듦
outputs/render/*.html       render_screens.py 가 다시 만듦
```

---

## 7. 못 한 것 · 막힌 것 · 여쭐 것

### 못 한 것

없습니다. 순서 6 은 닫혔습니다.

### 여쭐 것 ①  관리 메뉴표에는 이름 칸이 없습니다

`60-admin/c-tools.md` 의 표는 경로만 있고 이름 칸이 없어서,
`V11-54` 는 그쪽에 대해서는 **「이름이 있는가」만** 봅니다.
`61-web.md` STEP 142a 처럼 **이름 칸을 붙여 주시면** 관리 메뉴도
「표가 정본」으로 대조할 수 있습니다. 지금은 못 합니다.

### 그대로 남은 것

```
S13 본문 config 예시 대조 20 · S16 검증 코드 대조 68 · S26 작업 기록 21
★ S16 68 은 규격이 요구하는 검사 중 아직 안 만든 것입니다 —
  순서 11(검사 1차 10개) · 12 에서 줄어듭니다
```

---

## 8. 커밋

```
V11-54·V11-150: 메뉴에 /reports 이름을 넣고 검사를 STEP 142a 표까지 넓혔다 (순서 6)
해시  28eb769
```
