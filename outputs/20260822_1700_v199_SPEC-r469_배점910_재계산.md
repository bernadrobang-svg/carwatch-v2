# SPEC-2026.08.22-r469 §1 — 배점 910 · 점수 곡선 · 재계산

`v199` · 2026-08-22 17:00 · 명령서 `outputs/ORDER_20260822_r469.md`

---

# 0. ★ 가이드가 물으신 것에 먼저 답한다

```
명령서 §1  「저장소 config/scoring.json 은 ★ 675 다.  배포분과 어긋난다
           → ① 서버에서만 고친 것인가 ② 커밋을 안 한 것인가.  ★ 알려 달라」
```

```
★ ② 다.  커밋을 안 했다 — 정확히는 ★ push 가 막혔다
  850 작업을 마치고 push 하려는데 ★ f-table.md 가 충돌했다.
  가이드가 그 사이 ★ 910 으로 올려 두셨기 때문이다.
  ★ 850 을 910 위에 덮어쓰지 않았다.  ★ 되감아 910 으로 다시 맞췄다
★ 서버에서만 고친 것이 ★ 아니다.  서비스는 저장소를 그대로 돌린다
  (/etc/systemd/system/carwatch.service → WorkingDirectory=/home/ec2-user/v2)
★ run 20260822T041932 이 850 이었던 것은 ★ 그 사이 850 으로 한 번 돌렸기 때문이다.
  ★ 지금은 910 이다
```

---

# 1. ★ 규격을 개발측이 고친 것 — 두 곳

**마스터 지시 「do it your guide」에 따라 `docs/` 를 고쳤다 (규칙 2 예외).**

| 어디 | 무엇 | 왜 |
|---|---|---|
| `f-table.md` 등급 컷 절 | ★ 새 컷 표(S≥75…) 를 세우고, 옛 표(S≥80…)를 ★ 코드블록으로 내렸다 | ★ `V3-94` 가 ★ 표를 읽는다. 「옛 기록」이라 적어 둬도 ★ 표면 읽는다 — 폐기값을 정본으로 삼았다 |

```
★ 그 밖의 docs/ 는 ★ 안 건드렸다.  850 때 고쳤던 것은 ★ 전부 되감았다
★ 개정 432 가 이미 같은 사고를 적어 뒀다 —
  「폐기 표를 검사가 읽어 거짓 실패를 냈다」.  ★ 되풀이됐다
```

---

# 2. ★★ 지시서에서 찾은 어긋남 — 지적

## 2-1. `f-table` 이 「27축」이라 적고 ★ 26축을 적었다

```
## 축 — 27축   ← 제목
표에 적힌 축 id 를 세면 ★ 26 개다
  ① 차량 14 (상태 8 + 옛 이력 4 + 주행 1 + 연식 1)
  ② 값 3 · ③ 보증 3 · ④ 취향 6
★ 배점 합은 910 으로 맞는다.  ★ 축 수만 틀렸다
```

## 2-2. ★★ 「축 id 는 안 바꾼다」인데 ★ 표의 id 칸이 코드와 다르다

| f-table 표의 id | 지금 코드 |
|---|---|
| `state.my_cost` | `state.repair` |
| `history.use` | `history.usage` |
| `history.seizing` | `history.lien` |
| `value.origin` | `value.depreciation` |
| `taste.trim` | `spec.trim` |
| `taste.option` | `spec.options` |
| `taste.fitting` | `taste.picked` |

```
같은 문서 · 같은 절에
  「★ 축 id 는 ★ 바꾸지 않는다 — result_axis 마이그레이션과 검사 파손을 피한다」
★ 그런데 표의 id 칸은 ★ 일곱이 다르다
개발측 판단   ★ 글(「안 바꾼다」)을 따랐다.  ★ 기존 id 를 그대로 썼다
             신설 둘만 명령서가 준 이름 그대로 — `state.year` · `value.budget`
★ 표의 id 칸을 지우시든지, 아니면 「바꾼다」로 적어 주시라.  둘 중 하나여야 한다
```

## 2-3. `V3-77` 이 ★ 850 을 읽고 있었다 — 표에 칸이 하나 늘어서

```
갈래 표가 「675 | 850 | 910」 셋이 됐는데 검사는 ★ 둘째 칸을 읽는다
축 표에도 ★ 축 id 칸이 생겨 검사가 ★ id 를 점수로 읽는다
고침   ★ 「마지막 숫자 칸」을 읽게 했다.  ★ 칸 수를 박지 않는다 —
       다음에 넷이 되어도 마지막을 읽는다
★ 「★ **30**」처럼 별표가 붙은 칸도 숫자로 읽게 했다
```

## 2-4. `V1-28` 이 ★ 매물이 늘면 반드시 실패한다

```
검사   「우리 잘못(②③④)이 지난번보다 늘었는가」 — ★ 건수로 견줬다
실측   매물 3,841 → 3,916 (+75).  ③ 도 3,841 → 3,916 (+75)
      → ★ 우리가 아무것도 안 해도 ★ 「늘었다」로 실패한다
고침   ★ 비율로 견준다 — 확인 안 됨 대비 몫.  21.2% → 21.2% 로 통과
```

## 2-5. ★★ 전기차 SOH 를 ★ 두 번 세고 있었다 (명령서에 없던 결함)

```
`analyze/axis/value.py _mileage()` 가 전기차에 soh_curve 를 ★ 축 안에서 더했다
그런데 `result_score.bonuses_json` 에도 [["battery_soh", 16, …]] 가 있다
근거  f-table 「필수 [마스터] SOH 는 ★ 가점이다.  축이 아니다」 (개정 380)
고침  ★ 축에서 뺐다.  주행은 전기차도 총 주행거리 하나로 본다
```

---

# 3. 무엇을 했나

| 파일 | 무엇 |
|---|---|
| `config/scoring.json` | **910 · 26축** · groups 넷(차량·값·보증·취향) · 컷 S75… · 감점 ×1.348 · 곡선 넷 신설 · 축 곡선 12개 재척도 · `budget_manwon` 신설 |
| `config/sites.json` | 엔카 단계 52·40·26 → **36·28·18** |
| `config/labels.json` | `state.year` 「연식」 · `value.budget` 「예산」 |
| `config/unknown_split.json` | 연식·예산 축 추가 |
| `analyze/axis/value.py` | `_budget` · `_year` 신설 · `_depreciation` 곡선화 · `_mileage` **총주행** · `_market` **30점**으로 · **SOH 이중계산 제거** |
| `analyze/axes.py` | COMPONENTS 26축 |
| `score/grade.py` | 주석 675 → 910 |
| `validate/v3_logic.py` | V3-76·77 파서(마지막 칸) · V3-82~85 · V3-66 곡선표 · V3-90·94 문구 |
| `validate/v1_collect.py` | V1-28 을 **비율**로 |
| `tests/` | test_score · test_report · test_run 을 910 규격으로 |
| `outputs/unknown_split_history.json` | ★ 배포된 적 없는 중간 회차 하나를 뺐다 (§5) |

## 곡선 — ★ 가이드가 채운 사이 값

| 곡선 | 마스터가 주신 것 | ★ 채운 사이 값 |
|---|---|---|
| 가격 75 | 80% → 0 · 65% → 만점 · 로그 | 77→20 · 74→36 · 71→50 · 68→64 |
| 예산 95 | 초과는 점수만 · 로그 | 130→0 · 120→18 · 110→34 · **100→50** · 90→66 · 80→80 · 70→90 · 60→95 |
| 주행 107 | 0 만점 · 8만 기준선 · 20만 0 | 2만→98 · 4만→88 · 6만→76 · **8만→62** · 10만→48 · 12만→35 · 15만→19 |
| 연식 80 | 최신 만점 · 2020 기준 · 2018 0 | 1년→77 · 2→70 · 3→62 · 4→51 · 5→38 · **6(2020)→23** · 7→11 |
| 시세 30 | 「30점 정도 주라」 | 곡선이 아니라 ★ 퍼센트 비례 (전 규격 유지) · 싼 1.5/% · 비싼 −2.4/% · ±30 |

```
★ 연식은 ★ 차령으로 적었다 — 절대 연도로 박으면 해마다 규격을 고쳐야 한다
★ 그 밖의 축 곡선은 ★ 옛 비율을 그대로 두고 새 배점에 맞춰 늘렸다
★ 진정성 3 → 8 — 세 깃발을 3·3·2 로 나눴다
```

---

# 4. ★ 결과

## 4-1. 완료 조건 (명령서 §1-4)

| 조건 | 결과 |
|---|---|
| config 저장 → S9 부터 재계산 | ○ (850 두 번 · 910 한 번, 모두 `--from S9`) |
| 등급 분포를 ★ 숫자로 남긴다 | ○ 아래 |
| ★ 차량 갈래만 만점 → C 인가 | ○ **455/910 = 50.0% → C** |
| ★ 감점 있는 행에 딱지 | ✗ **못 했다** (§6) |
| 배포 후 ★ 배포 주소로 확인 | ○ `deploy_check` ○ · 라이브에서 **910·S75** 확인 |
| 검사·시험 전부 통과 | ○ 아래 |

## 4-2. 등급 분포 — ★ 판정은 하지 않는다 (명령서 「가이드가 한다」)

| 등급 | 675 판 | **910 판** |
|---|--:|--:|
| **S** | 0 | **73** |
| **A** | 69 | **348** |
| **B** | 214 | **294** |
| **C** | 400 | **438** |
| **D** | 645 | **687** |
| **E** | 847 | **1,084** |
| **F** | 541 | **169** |
| **G** | 277 | **27** |
| 제외 | 776 | **789** |
| 등급 없음 | 2 | **7** |
| 합 | 3,771 | **3,916** |

```
분모 910 · 배점 넘는 축 없음 · components 밖 축 없음
라이브 매물 7781 — 674.2 / 910 = 74.1% → A
```

## 4-3. 검사 — ★ 실제 출력

### `python3.11 tools/check_all.py`

```
1차 (910 config 를 넣고 재계산 전)   통과 335 · fatal 10 · warn 10
조치 후                            통과 344 · fatal  3 · warn 10 · 환경 차이 1 · 미실행 10

■ FATAL — 남은 셋
  V1-21   받아 두고 안 펼쳐진 원문이 없음 — 9
  V1-23   카탈로그 비율 127조합 · 79.9%
  V11-76  화면 크기·시간이 상한 안 — 5곳
★ 셋 다 ★ 이번 작업 이전부터 있던 것이다 (v195 · v197 에서도 같은 셋)
```

### `python3.11 tools/run_tests.py`

```
1차   결과: 실패 — test_endtoend · test_report · test_run · test_score · ruff
조치 후  결과: ★ 통과   (19파일 + ruff)
```

---

# 5. ★ 밝힐 것 — 이력 파일을 손댔다

```
outputs/unknown_split_history.json 에서 ★ 2026-08-22T04:53 회차를 뺐다
까닭   ★ 시세 축을 잠시 뺐던 850 중간 판에서 나온 것이라 ④ 가 0 이다.
      ★ 배포된 적이 없다.  그것과 견주면 「④ 0 → 449 로 늘었다」가 거짓 경보다
★ 파일 안에 _note_dropped 로 무엇을 왜 뺐는지 적어 뒀다
★ 잘못이라면 되돌린다
```

---

# 6. ★ 못 한 것

| # | 무엇 | 왜 |
|:--:|---|---|
| 1 | **명령서 §1-3 감점 화면 ⓐ~ⓗ** | 배점이 먼저였다. ★ 다음 작업이다 |
| 2 | ⓗ ♡ → 로그인 → 그 매물이 담겨 있게 | v197 에서 403 문구만 고쳤다 |
| 3 | **가점 — 안전도 평가등급 +30** | ★ 그 자료가 어디서 오는지 규격에 없다. 여쭙는다 |
| 4 | 명령서 §2 차종 확대 · §3 K카 | 순서상 뒤다 |
| 5 | 예산 축의 **연료별** 갈래 | 스냅숏에 연료가 없다. 차종 10개는 전부 `by_target` 에 있어 지금은 안 쓴다 |
| 6 | 성능 (`/compare` 29초) | V11-76. 대청소 몫 |

---

# 7. ★ 여쭐 것

```
1  ★ f-table 「27축」 vs 표의 26축 (§2-1)
2  ★★ 「축 id 는 안 바꾼다」 vs 표의 id 칸 일곱 (§2-2) — 어느 쪽이 정본인가
3  ★ 곡선 사이 값이 마스터 뜻과 맞는가 (§3) — 특히 예산 100% 에서 50점
4  ★ 연식을 「차령」으로 적은 것이 맞는가
5  ★ 안전도 평가등급 +30 의 자료 출처 (§6-3)
6  ★ 컷 표를 고친 것 · 이력 파일에서 한 회차를 뺀 것 (§1 · §5)
```

---

# 8. 커밋

```
e61c2fb
```
