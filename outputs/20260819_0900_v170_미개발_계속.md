# 미개발 계속 — AD-096 · 비교 차이 · 중단 리포트 · ★ 내가 낸 사고

2026-08-19 09:00 · v170 · **SPEC-2026.08.19**

`inbox/ORDER_00_build_all.md`

---

## 1. ★★★ 먼저 — 제가 사고를 냈습니다

마스터가 저장한 목록 7쪽이 안 펼쳐져 있어(`V1-21`) 그것을 처리하려고
`run_recalc(reason="listing_updated", scope="all")` 을 돌렸습니다.

```
★ scope="all" 이라 상세를 31,995건 다시 받기 시작했습니다
★ 제가 의도한 것은 7쪽이었습니다
```

```
알아챈 때   S5 846/31995 (약 4분)
한 것       바로 죽였습니다
남은 것     raw_response 96,975 → 99,719 (2,744건 더 받음)
           ★ 원문은 버리지 않습니다 (P3).  받은 것은 그대로 둡니다
```

### 무엇을 잘못했나

```
① 「7쪽을 처리한다」와 「전건을 다시 받는다」를 안 갈랐다
   scope 를 안 보고 reason 만 골랐다
② 망을 쓰는 일을 확인 없이 돌렸다.  엔카에 2,744건을 더 던졌다
③ 프로세스를 죽여 단계 리포트가 안 남았다 — V1-11 이 그것을 잡았다
```

★ **검사가 제 실수를 잡았습니다.**  그것이 이 검사의 값입니다.

### 제대로 한 방법 — 망을 안 쓰는 길이 있었습니다

```
python3.11 run.py collect --only S4     ← 목록 봉투를 펼치기만 한다
  S4  6,292 적재 · 4.6초 · 요청 0건
결과   core_listing 7,809 → 8,055 (마스터가 저장한 246건이 들어왔다)
       V1-21  7 → 0
```

**S4 하나면 4.6초에 끝날 일이었습니다.**

---

## 2. ★ 그래서 고쳤습니다 — 끊겨도 리포트가 남는다

```
규격   STEP 48 「중단은 리포트를 내는 것이지 죽는 것이 아니다」
실측   밖에서 죽이면 요청 5,000건은 남고 단계 리포트가 하나도 안 남는다
      → 「무엇이 돌다 끊겼나」를 아무도 모른다
```

```python
# collect/pipeline.py  run_pipeline()
except (KeyboardInterrupt, SystemExit) as stop:
    rep.halted = True
    rep.halt_reason = f"밖에서 끊겼다 ({type(stop).__name__})"
    save_step_report(conn, ctx.run_id, rep)
    raise          # ★ 삼키지 않는다
```

**되살려 확인했습니다**

```
끊김이 그대로 올라온다 (삼키지 않는다) ✓
남은 단계 리포트: [('STEP53-S4', 0, '{"requested": 0, …}')]
```

---

## 3. 만든 것

### `AD-096` — 응답에 새 토큰을 실어 보낸다 (개정 307)

```
규격   「그래도 갱신이 필요하면 응답에 새 토큰을 실어 보낸다.  JS 가 갈아 끼운다」
지금   그 자리가 없었다 — 한 화면에서 수십 번 POST 하는데 토큰이 만료되면 통째로 막힌다
```

```
web/app.py       redirect(..., csrf=) → X-CSRF-Token 헤더
                 ★ 안 넘기면 안 실린다.  늘 실으면 새 나간다
web/views.py     브라우저 수집이 조각마다 · 저장마다 싣는다
admin_collect.html  takeToken() 이 fetch 응답에서 받아 갈아 끼운다
V11-99           ★ 실제로 부른다.  「코드에 있다」로 통과시키지 않는다
```

### `WB-065` · `V11-102` — 비교는 차이만

```
규격   「옵션 차이만 낸다.  같은 것은 접는다」
      「A 에만 있음: 파노라마 선루프 140만 · 드라이빙 어시스트 150만」
지금   비교 화면에 옵션 절이 아예 없었다
```

```
store/core.py   option_diff() — 한쪽에만 있는 것 · 같은 것
                ★ 이름이 사전에 없으면 코드를 그대로 쓴다 (추정하지 않는다)
compare.html    「옵션 차이」 절 · 같은 것은 <details> 로 접는다
V11-102         ★ 옵션이 실제로 다른 두 매물을 골라 연다.
                같은 것끼리 보면 늘 통과한다
```

실측 — `4625 에만 4개 · 4639 에만 2개 · 같은 것 1개`

### `V3-22` 가 거짓 경보를 내고 있었습니다

```
전   「빠진 것 중에 경고가 있나」를 셌다
     → target_key 가 없어 빠진 두 건이 걸렸다.  경고 때문이 아니다
후   「경고 때문에 빠졌나」를 본다 — target_key 가 있는 것만
```

---

## 4. 데이터 상태를 맞췄습니다 (망을 최소로)

| 무엇 | 도구 | 요청 | 결과 |
|---|---|--:|---|
| 마스터 목록 7쪽 펼치기 | `--only S4` | 0 | `core_listing` 7,809 → **8,055** |
| 못 부른 카탈로그 1조합 | `--only S7` | 1 | `not_called` 1 → **0** |
| 카탈로그를 사전으로 | `--only S8` | 0 | `dict_model_option` +4 |

```
V1-21  7 → 0      V1-23  통과 (127조합 · 85.2%)      V1-24  통과
```

★ **S7 은 「1조합」임을 먼저 세고 돌렸습니다.**  1절의 잘못을 되풀이하지 않았습니다.

---

## 5. 조치 후 결과 — ★ 실제 출력

```
python3.11 tools/check_all.py
통과 291 · fatal 4 · warn 7 · 환경 차이 1 · 미실행 5

■ FATAL
  V1-11     예외로 종료된 실행이 없음 — 0단계     ← ★ 내가 죽인 그 실행이다 (6절 ①)
  V4-11     unclassified 존재 — 32               ← 마스터 몫
  V3-38     facet 이 준 「기타 색상」이 사전에 없다  ← ★ 사람이 확정할 일 (6절 ②)
  V11-12    라우팅 표에 /reports 가 없다          ← 가이드 몫

python3.11 tools/run_tests.py
결과: 실패 — test_endtoend.py, test_web.py

python3.11 tools/check_src.py
S7 · S14 · S15 전부 통과
```

| | 전 | 후 |
|---|--:|--:|
| check_all 통과 | 285 | **291** |
| fatal | 5 | **4** |
| 새 검사 | — | V11-99 보강 · **V11-102** 신설 |

---

## 6. 막힘 · 판단이 필요한 것

### ① `V1-11` — 제가 죽인 실행의 기록입니다

```
run_id 20260818T215533 — 요청 5,000여 건이 나갔고 단계 리포트가 없다
```

옛 기록입니다.  **2절의 고침으로 다시는 안 생깁니다.**
다음 전체 실행이 끝나면 사라집니다.  ★ 지어서 채우지 않았습니다.

### ② `V3-38` — facet 이 새 색상을 줬습니다.  사람이 확정해야 합니다

```
color_ext 「기타 색상」 — facet 이 주는데 사전에 없다
★ color_ext 는 판정 축이다 (JUDGING_AXES).
  사전에 넣으면 pending 이 되고, pending 이 있으면 판정이 막힌다 (STEP 45)
```

**개발측이 확정하지 않습니다.**  `/admin/dict` 에서 확정해 주십시오.

### ③ 남은 미구현

```
AD-* 브라우저 수집 대부분은 코드가 있는데 검색기가 못 잇습니다.
확인한 것만 config/trace_hints.json 에 적습니다 — 확인 안 한 것은 안 적습니다
```

---

## 7. 고친 파일

```
collect/pipeline.py    run_pipeline — 끊겨도 단계 리포트를 남긴다
web/app.py             redirect(csrf=) · CSRF_HEADER
web/views.py           브라우저 수집이 새 토큰을 싣는다
web/templates/admin_collect.html   takeToken()
store/core.py          option_diff() · _option_names()
report/screens/build.py · views.py  CompareView.option_same · option_only
web/templates/compare.html         「옵션 차이」 절
validate/v11_web.py    V11-99 보강 · V11-102 신설
validate/v3_logic.py   V3-22 를 「경고 때문인가」로 좁힘
```
