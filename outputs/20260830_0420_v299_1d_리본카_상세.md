# 1d — 리본카 상세 1,082건 · 3 답한 것

## 안 한 것

| 무엇 | 왜 못 했나 | 며칠째 | 누가 답해야 하나 |
|---|---|--:|---|
| 1a KB 상세 상한 | 마지막 남은 항목. 지금 간다 | 오늘 | 개발측 |
| 2 화면 | 0·1 뒤다 | 오늘 | 개발측 |
| `target_key` 를 여덟 수집기가 안 붙인다 | 규격이 없다. **92건**이다 (아래) | 이틀째 | **마스터** |
| V11-12 라우팅 표 중복 | 규격의 중복 행 | 오늘 | **가이드** |

## 1d — 무엇을 했나

ORDER r879 1d — 「상세 1,082건을 받아야 차종이 갈린다. 좁힐 길이 없다」

### ★ 먼저 재보니 다른 것이 나왔다

```
리본카 전체            1,107
  detail_status='ok'  1,106
  ★ 상세 원문            301      ← 805건이 「받았다」고 서 있는데 원문이 없다
  target_key 있음         72
```

★ 「이미 받았나」를 `detail_status='ok'` 로 갈랐던 것이 까닭이다.
원문이 없으면 **다시 캘 수가 없다** — 명령서 3-2 「원문은 남긴다. 갈래를
넓히시면 다시 판다」가 안 지켜진 상태였다.

| 파일 · 줄 | 무엇 |
|---|---|
| `tools/collect_reborncar.py:117` | 「이미 받았나」를 **원문**으로 가른다 (KB 개정 857 과 같은 꼴) |
| `tools/collect_reborncar.py:127` | `detail_status` 와 원문 수가 어긋나면 그 수를 말한다 |

### 몇 → 몇

```
사이트맵 420,542B · <loc> 2,716개 · ★ 매물 1,082건 (중복 0)
★ 상세 — 받을 것 787건 (원문이 있는 것 301건은 건너뛴다)
  ★ `detail_status='ok'` 는 1,106건인데 원문은 301건이다 — 805건이
    「받았다」고 서 있으나 원문이 없다 (그래서 다시 받는다)
★ 상세 — 정상 787 · 못 받음 0
★ 우리 대상 — 144건 / 787건 · ★ 안 넣은 것 643건 (원문은 남는다)
```

| | 고치기 전 | 고친 뒤 |
|---|--:|--:|
| 리본카 상세 **원문** | **301** | **1,088** |
| 받은 상세 | — | **787건 · 못 받음 0** |
| 「이미 받았나」의 근거 | `detail_status='ok'` | **원문이 있는가** |
| check_all 통과 | 415 | **416** |
| run_tests 실패 | 1파일 `test_endtoend` | 1파일 `test_endtoend` |

## 3 — 답한 것 · 세어 드릴 것

### ① BMW 미니 — 닫았습니다

`config/endpoints.json` `bmw_bps._brand_scope_why` 의 **「[미확인 08-26]」을 닫았습니다.**
`brand_scope` 는 `["BMW"]` 그대로 두었고, 「BMW 만 판다」는 낱말은 안 씁니다.
가이드 실측(364건 중 미니 2건)과 판단(「우리 26종에 없어 어차피 범위 밖으로 접힌다」)을
그 자리에 적었습니다.

★ `endpoints.json` 에 「미확인 08-26」이 **셋 더** 남아 있습니다 —
`kbchachacha`(S46-96) · `lexus_certified` · `volvo_selekt`. 함께 봐 주십시오.

### ② `test_endtoend.py` 는 어느 커밋부터인가

★ **독립된 시험 실패가 아닙니다.** `test_endtoend` 는 두 가지를 봅니다 —

```
FAIL  중단 없이 끝난다   ['S11: V11-12 … / V11-106 … / V11-45 …']
FAIL  fatal 실패가 없다  ['V11-12', 'V11-106', 'V11-45', 'STEP53-S11']
```

즉 **`check_all` 에 fatal 이 하나라도 있으면 실패**합니다. 그 판정문
(「fatal 실패가 없다」)은 `5db8c493` (2026-08-16 · v134 기준선 — AWS 배포 시점)부터
있었고 그 뒤 바뀐 적이 없습니다.

★ 그러므로 답은 「어느 커밋에서 깨졌다」가 아니라 **「fatal 이 0 이 된 적이 없다」**입니다.
오늘 이 회차에 V11-106 을 닫았고, 남은 것은 **V11-12 · V11-45** 둘입니다.
둘 다 개발측이 못 닫습니다 —

- **V11-12** — 규격의 중복 행입니다. `docs/chapters/61-web.md:196` 과 `:209` 가
  둘 다 `/admin/users` 입니다 (196 은 「STEP 136z」· 209 는 「시안」). 한 행에 한 경로여야
  합니다. **규칙 2 대로 제가 안 고칩니다 — 가이드께서 지워 주십시오.**
- **V11-45** — 화면 미정 5개가 `pending` 에 있습니다 (앞 회차에 올렸습니다).
  가이드 판단 대기입니다.

★ **이 둘이 닫히면 `test_endtoend` 도 함께 통과합니다.**

### ③ K카 `sell_dcd=ALL` 이 몇 건인가

★ **못 쟀습니다.** 규격이 적은 그대로입니다 —
`docs/KCAR_API.md` 「`sell_dcd=ALL` 쪽에 더 있는지는 못 쟀다 — `enc` 라 못 잰다」.

제가 부를 수 있는 것은 **재고 목록**(`stock_list`)이고 오늘 **487건**입니다.
`sell_dcd` 를 바꿔 부르는 길은 `POST /bc/search/list/drct` 뿐인데 그 본문이 `enc` 라
막혀 있습니다. **명령서가 「`enc` 를 풀지 마라 · 우회를 만들지 마라」로 금했으므로
시도하지 않았습니다.**

★ 오늘 K카 실측 — 재고 목록 **487건** · 우리 대상 **105건** · 저장 604건 ·
gone 156건 · 살아있음 448건.

## ★ 마스터께 다시 여쭙니다 — `target_key` 를 누가 붙입니까 (이틀째)

여덟 수집기가 `site_model_group` 만 넣고 `target_key` 를 안 넣습니다.
넣는 것은 `hyundai_cert` · `kia_cpo` 둘뿐입니다 (`target_of` 를 부릅니다).

★ **재보니 92건입니다** — 「8,756건이 막혔다」가 아닙니다.

```
site_model_group 이 있는데 target_key 가 없는 것        8,756건
  그중 ★ 규칙(`parse/target_rules.target_by_rules`)으로
       우리 26종에 실제로 붙는 것                       ★ 92건

  보배 45 · KB 20 · 리본카 20 · 볼보 3 · 헤이딜러 3 · K카 1
  차종 — G80_25T 42 · GLC_IMPORT 15 · GRANDEUR_LPG 7 · X3_IMPORT 6 …
```

여덟 수집기에도 `target_by_rules` 를 부르게 하면 **92대가 판정에 들어옵니다.**
새 규칙을 만드는 것이 아니라 **이미 있는 것을 같게 쓰는 것**입니다.
그래도 여덟 파일을 건드리는 일이라 여쭙고 합니다. 「해라」 하시면 다음 회차에 넣겠습니다.
