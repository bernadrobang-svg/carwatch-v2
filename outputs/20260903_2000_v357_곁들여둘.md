# v357 — 곁들여 닫을 것 둘 ＋ 리베이스가 낸 사고

가이드 지시 09-03 — 「곁들여 닫을 것 둘」

## ★ 먼저 — ★ **내가 낸 사고를 적는다**

```
시험 실패  ★ **10개** (test_admin_flow · test_dict · test_integration · … · ruff)
```

★ 까닭 둘 —

### ① 리베이스 충돌을 ★ **고리로 자동 해소**했다

```
validate/v0_guide.py 의 꼬리(`_as_check`·`results`·`save`·`run`)가 ★ **두 번** 들어갔다
★ ruff  F811 Redefinition ★ 셋
★ 파일 6,718줄 중 ★ 6,627줄부터 끝까지가 ★ 앞과 똑같았다
```

★★ `git checkout --ours` 를 ★ **고리로 돌려** 코드 파일까지 받아 붙였다.
★ 생성물(`docs/CHECKS.md` 등)에만 써야 하는 것을 ★ 코드에도 썼다.
★ ★ 잘라 냈다. ★ 기억에 남겼다 (`carwatch-v2-rebase-ours-duplicates`).

### ② `part` 축이 ★ `halt` 가 아니었다

```
errors.ValidationError: part 는 halt 축이 아니다. 부트스트랩 대상이 아니다 [STEP 41]
```

★ 가이드가 개정 1105 로 ★ `fixed_enums.json` 에 ★ `part` **열한 값**을 심었다.
★ ★ 부트스트랩은 ★ **`halt` 축만** 심는다 — ★ 「고정 집합이 정해진 축」이라는 뜻이다.
★ ★ ★ `AXIS_POLICY` 의 `part` 는 ★ `pending` 이라 ★ 죽었다.
★ 부위는 ★ 정부 서식이 정한 ★ **닫힌 집합**이다 — ★ `halt` 로 맞췄다.

```
시험 실패  10 → ★ **1** (남은 하나는 fatal 을 따라가는 것)
```

---

## 곁들여 ① — ★ `hood` 를 `HD_PART` 에

```
정부 서식 외판 여덟 중 ★ 열 칸을 적어 두고 ★ **후드 하나만** 없었다
★ 사전에도 없고 코드에도 없어 ★ **두 번 샜다**
★ ★ 이것은 판정 축이다 — `state.outer` **28점** · 헤이딜러 **275건**
```

| | 앞 | 뒤 |
|---|--:|--:|
| `HD_PART` 칸 | 10 | **11** |
| `hood` | **없다** | `RANK_ONE`(외판) |

## 곁들여 ② — ★ `target_key_of` 가 거르개 둘을 더 읽는다

```
앞   `fuel_contains` 하나만 읽는다
뒤   ★ `trim_contains` — 이름이 같은데 트림으로 갈리는 차 (엔카 「GV70」 전기↔가솔린)
    ★ `year_from`     — 이름이 같은데 연식으로 갈리는 차 (모델Y 주니퍼 2025-01~)
```

★ 값은 ★ `config/dictionaries/target_map.json` 이 정본이다 — ★ **코드는 읽기만** 한다.
★ 없으면 ★ 안 거른다. ★ 연식을 모르면 ★ **안 건다** (모르는 것을 틀렸다 하지 않는다).
★ 하나라도 안 맞으면 ★ `None` — ★ 「아마 이것일 것」으로 안 넘긴다 (금지 6).

```
확인   연료만        → GRANDEUR_LPG
      새 인자 있어도 → GRANDEUR_LPG   (기존 호출부 둘이 안 깨진다)
```

★ 값은 가이드가 넣으면 ★ **그때부터 곧장 듣는다**.
