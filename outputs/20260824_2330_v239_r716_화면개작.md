# r716 — 1장 화면 개작 · 추적 화면 신설 · 목록이 같은 차를 접는다

`SPEC-2026.08.24-r715` · 2026-08-24 23:30 · v239

## 1-1 상단 메뉴 다섯 — ★ 넣었습니다 (마스터 허락 · 이번만)

```
config/web.json  top_menu
   매물 · 관심 · ★ 추적 · 현황 · 관리
★ 추적을 ★ 관심과 현황 사이에 두었다 (시안 차례 그대로)
★ `_top_menu_why` 에 ★ 「메뉴는 마스터 몫이다」를 적어 두었다
```

★ 배포 확인 — ★ 손님에게는 ★ 「매물 · 추적 · 현황」 셋이 보인다
(관심·관리는 ★ 권한으로 가린다 — `web/app.py`).

## 1-2 ★★ 추적 화면 — ★ 신설

| 파일 | 무엇 |
|---|---|
| `report/screens/views.py` | `TrackPair` · `TrackView` |
| `report/screens/build.py` | `view_track()` · `_grade_order()` · `_grade_step()` · `_miss_axes()` · `_accident_split()` |
| `web/routes.py` | `/track` |
| `web/views.py` | `track()` 어댑터 |
| `web/templates/track.html` | ★ 신설 — 절 셋 |
| `config/labels.json` | `MISS_KIND` · `MISS_KIND_LABEL` |

### 배포 실측

```
/track → ★ 200 · 0.38초
짝지어진 차 ★ 44대
값이 30% 넘게 갈린 것 ★ 0
등급이 두 칸 갈린 것 ★ 42
사고 판정이 갈린 것 ★ 32
```

★ ★ **짝이 0대에서 44대가 됐습니다** — ★ 리본카가 들어와 ★ 엔카와 겹쳤습니다.

### 지시대로 한 것

```
★ 절 셋 — 1 값 갈림 · 2 등급 갈림 · 3 사고 갈림
★ 정렬 — 차액 큰 순(기본) · 등급 차 큰 순 · 사이트 많은 순
★★ 분모는 ★ **910 고정** — ★ 화면에 「earned / 910」으로 낸다.  ★ 갈리는 것은 earned 다
★ 「빠진 축」에 ★ 갈래(①~⑤)를 함께 — ★ 「소모품 ⑤」 꼴
★ 짝 없는 매물은 ★ 안 낸다 (SQL 이 `HAVING COUNT(DISTINCT site) > 1` 로 거른다)
★★ 합치지 않는다 — ★ 갈린 채로 낸다.  ★ 「우리가 어느 쪽으로 정하지 않습니다」를 화면에 적었다
```

### ★ 갈래(①~⑤)를 ★ 지어내지 않았습니다

```
config/labels.json  MISS_KIND
   state.consumable ⑤ · value.market ④ · state.integrity ② · history.not_join ①
★ ★ f-table 434행(뜻)·476행(축별 표)에서 ★ 옮긴 것만 넣었다
★ ★ 표에 없는 축은 ★ **갈래를 안 붙인다** — ★ 축 이름만 낸다 (금지 6)
```

### ★ 등급 차례를 코드에 안 박았습니다

```
★ 처음에 ★ ("S","A","B","C","D","E") 여섯으로 박았더니 ★ 등급 갈림이 ★ 0 으로 나왔다 —
  ★ ★ 실제는 ★ `grade_cuts` 가 ★ S·A·B·C·D·E·F·G ★ 여덟이다 (개정 433).
    ★ ★ F·G 가 「모르는 등급」이 되어 ★ 갈린 것을 못 셌다
★ ★ `config/scoring.json` 의 `grade_cuts` 에서 ★ 읽게 고쳤다 (S14)
   → ★ 등급 갈림 ★ 0 → ★ **43**
```

## 1-3 ★★ 상세 11절 「중복매물」 — ★ 10절 뒤에 붙였습니다

```
report/screens/build.py  duplicate_listings()
web/templates/detail.html  <div class="sec" id="sec-11">
★★ 열 절 차례는 ★ 안 바꿨다 (V11-159) — ★ 11절을 ★ 맨 뒤에 더했다
★ 짝이 없으면 ★ 절 자체를 안 낸다
```

```
배포 — /detail/199 → ★ 「11 ★ 중복매물 — 이 차가 다른 곳에도 있다」
   엔카 3,300만 · D · 사고 — · 8,814km · 36개월
   리본카 3,300만 · E · 사고 — · …
```

★ 칸은 시안 그대로 — ★ 사이트 · 값 · 등급 · 사고 · 주행 · 보증.

## 1-4 ★★ 목록 「N곳」 배지 ＋ ★ 같은 차는 하나만

```
web/templates/listings.html  {% if r.multi_site %}<a class="chip on" href="/track">N곳</a>
report/screens/build.py      SELECT COUNT(DISTINCT l3.site) … · WHERE 로 접는다
```

| | 전 | 후 |
|---|--:|--:|
| `/listings` 총건수 | 5,319건 | ★ **3,046건** |
| ★ 접힌 것 | — | ★ **1,416건** |
| 「N곳」 배지 | 없음 | ★ **43건** (`?all=1`) |

★ ★ **지우지 않았습니다** — ★ 접었을 뿐입니다. ★ 추적에서 다 폅니다.

### ★ 배지가 「1곳」으로 다 나오던 것 — ★ 잡았습니다

```
{% if r.site_count > 1 %}  ← ★ 템플릿은 ★ `>` 비교를 못 한다 (V11-104).  ★ 늘 참이었다
★ ★ 판단을 코드로 옮겼다 — ★ `multi_site: bool`
```

## 1-5 ★ 「무엇을 하는 곳인가」 — ★ 여덟 장 다

```
★ 시안 주황 상자(`border-color:var(--amber)`) 글자를 ★ 기계로 뽑아 ★ 그대로 옮겼다
★ ★ 개발측이 새로 쓰지 않았다
```

| 화면 | 「~와 다르다」 |
|---|---|
| 관심 | ★ 추적과 다르다 — 추적은 「같은 차가 여러 곳에」 · 여기는 「내가 지켜보는 차」 |
| 비교 | ★ 추적과 다르다 — 추적은 같은 차가 사이트마다 어떻게 다른지 |
| 추적 | ★ 관심·비교와 다르다 — 여기는 같은 차끼리다 |

★ 배포 확인 — ★ `/listings` `/track` `/notready` `/compare` `/detail` ★ 다 1개씩 있다.
★ 화면 렌더 ★ 31 → ★ **32**.

## 2. ★★★ 목록이 128초였습니다 — ★ 색인이 없었습니다

★ 「같은 차는 하나만」을 켜자 ★ 배포가 ★ **5분 안에 응답을 못 했습니다.**

```
접기 쿼리 실측 → ★ **127.9초**
PRAGMA index_list(core_listing) → ★ plate_hash 색인 ★ 없음
```

```sql
sql/ddl/02_core.sql
CREATE INDEX IF NOT EXISTS ix_listing_plate
  ON core_listing(plate_hash, status, price_current_won, listing_id);
```

| | 전 | 후 |
|---|--:|--:|
| 접기 쿼리 | ★ **127.9초** | ★ **0.00초** |

### 배포 실측 — 전 화면

```
/           200 · 0.68초
/listings   200 · 0.85초
/track      200 · 0.38초
/notready   200 · 0.36초
/compare    200 · 0.28초
/detail/199 200 · 1.02초
```

## 3. ★★ 씨앗 DB 가 ★ 실물과 달랐습니다 — ★ 고쳤습니다

★ 「같은 차는 하나만」을 켜자 ★ `test_screens` · `test_integration` 이 깨졌습니다.

```
씨앗 매물 열둘이 ★ 번호판을 ★ **하나로 나눠 쓰고** 있었다 —
   (5,'encar','active','11938e4cd40b4be4', …)
   (6,'encar','active','11938e4cd40b4be4', …)   ← ★ 열둘이 다 같다
★★ ★ 그래서 ★ **한 대의 차가 열둘로** 보였고 ★ 접기가 열둘을 하나로 만들었다
★ ★ 실물은 ★ 번호판이 다 다르다.  ★ 씨앗이 실물과 달랐다
```

```
tests/test_run.py  StubEncar
   doc["vehicleNo"] = f"{12 + n % 80}가{3456 + n:04d}"
```

```
test_screens      실패 2 → ★ 통과
test_integration  항목 136 · 합격 136 · ★ 불합격 0
```

★ ★ **검사가 옳았습니다.** ★ 화면을 되돌리지 않고 ★ 씨앗을 실물에 맞췄습니다.

## 4. 규칙 3 — 두 도구

```
run_tests   실패 3파일 — test_endtoend · test_spec_ui · test_web
            ruff ★ 통과
```

```
test_spec_ui  ← L-3 색 158 (열 회차째)
test_web      ← ① Route 수 (아래 5장) ② admin·dashboard 시안 절
test_endtoend ← 위 둘의 fatal 을 그대로 읽는다
```

## 5. ★ 지시서에 적어 주실 것 — ★ `/track` (규칙 2)

```
★ Route 수 == 지시서 표 행 수 — ★ 소스 37 · 표 36
★ ★ `/track` 을 ★ 새로 냈습니다 (마스터 허락 08-24 · 명령서 1-2).
  ★ ★ 지시서의 ★ 라우트 표에 ★ 그 줄이 아직 없습니다.
  ★ ★ 저는 `docs/` 를 안 고칩니다 (규칙 2) — ★ 한 줄 넣어 주십시오
     `/track` · GET · view_track · ROLE_ANONYMOUS · 추적
```

## 6. 못 한 것

```
1-6  현황·관리 두 장 · 상세 사진 320×180 (눈으로 재기)
3    재수집·필터링 (3-1 좁혀 받기 · 3-2 저장할 때 거르기 · 3-3 되돌리기 · 3-4 XHR)
```

★ ★ 「1 → 2 → 3 차례다.  화면이 먼저다」 하셔서 ★ 1장을 먼저 끝냈습니다.
★ 3-3 의 「미분류 2,930 → 몇 건」은 ★ 3장을 한 뒤에 재겠습니다.

## 7. ★ 마스터께 드릴 물음 — ★ 하나 (r674 ⓕ)

### ★ 목록에서 겹친 차를 ★ 「낮은 **값**」으로 접었습니다 — ★ 맞습니까

★ ★ **시안 안에서 두 줄이 어긋납니다** —

```
v3_listings_시안 주황 상자   ★ 「같은 차가 여러 곳에 있으면 ★ **낮은 값 하나만** 낸다」
v3_listings_시안 지켜야 하는 것 ★ 「목록에는 ★ **낮은 점수 하나만** 낸다」
v3_track_시안 지켜야 하는 것   ★ 「목록에는 ★ **낮은 점수** 를 낸다」
```

| | 갈래 | 무엇을 뜻하나 |
|:--:|---|---|
| **A** | 낮은 **값** (지금) | ★ 싼 쪽을 남긴다.  ★ 사는 사람이 원하는 것은 싼 쪽이다 |
| **B** | 낮은 **점수** | ★ 나쁜 쪽을 남긴다.  ★ 「좋아 보이는 것」을 안 내세운다 |
| **C** | 높은 **점수** | ★ 좋은 쪽을 남긴다.  ★ 등급순 목록과 뜻이 맞는다 |

```
★ 개발측 안   ★ A
   까닭       ★ 주황 상자가 ★ **사는 사람에게 보이는 글**이고 ★ 「어디가 싸고」가
              ★ 추적 화면의 뜻이다.  ★ 나쁜 쪽을 남기면 ★ 등급순 목록과 어긋난다
★ 안 정하시면  ★ A 로 둡니다 (지금 배포된 것 · 3,046건)
```

## 7a. 쌓아 둔 것

```
ⓐ L-3 색 158 — ⓐ/ⓑ/ⓒ (열 회차째 · test_spec_ui 가 이것 하나로 막혀 있다)
ⓑ 지시서 라우트 표에 `/track` 한 줄
ⓒ admin · dashboard 시안 절 (1-6 에서 한다)
ⓓ S43-2c 가 HEYDEALER_API 의 「HDA2」를 잡는다 — 검사냐 문서냐
ⓔ S43-3 버전 어긋남 · S44-4 가 「없다」를 잡는다
ⓕ 사전의 가짜 낱말 셋 · 축 여럿이 전건 0
```

커밋 해시 — `79829de`
