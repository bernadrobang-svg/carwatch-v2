# ★ 상태표가 거짓말하고 있었습니다 — 원문은 있는데 「못 받았다」로 8,450건

지시   「do it」 (v391 여쭐 것 1·3)
버전   v392

---

## 0. ★ 찾은 것 — **가지고 있는데 없다고 적혀 있었습니다**

09-06 에 마스터 회선으로 받아 `ok` 였던 상세 둘이
하룻밤 사이 **`error` 로 덮여** 있었습니다. **원문 파일은 그대로 `ok` · 9,880B 였습니다.**

그 자리를 뒤지니 **둘이 아니었습니다.**

```
원문은 ok 인데 DB 는 error/empty 로 적힌 줄
  encar.detail              ★ 8,416건
  encar.record_summary            6
  encar.inspection                7
  encar.record                    5
  encar.ev_battery · platform_check · sellingpoint · inspection_summary  각 4
                                  ─────────
                            ★ 합 8,450건
```

★ 이것이 **이 프로젝트가 막으려는 바로 그 괴리**입니다 —
★ 「저장했다」고 선언했으나 실제와 다른 상태.

---

## 1. ★ 「몇 → 몇」

| 항목 | 전 | 후 |
|---|--:|--:|
| **`/fetch` 2단계 상세 「받을 것」** | **8,684** | **1,753** |
| 상태가 어긋난 줄 | 8,450 | **0** |
| 엔카 `warranty_body_month` | 9,291 | **9,300** |
| 엔카 `price_origin_won` | 9,256 | **9,265** |
| 엔카 `options_choice_json` | 9,291 | **9,300** |
| 원문에서 칸을 채운 매물 | — | **9,300대** |

★ **6,931건을 안 받아도 됩니다.** 이미 갖고 있었습니다.
★ 마스터께서 「8,684건을 받아야 한다」는 말을 믿고 폰으로 도셨으면
★ ★ **여섯 시간을 헛되이 쓰셨을 것**입니다.

---

## 2. 세 가지를 했습니다

### ① 되덮는 자리를 막았습니다

```
collect/runner.py         S5 가 상태를 쓸 때
report/screens/fetch.py   /fetch/put 이 상태를 쓸 때
  → ok · not_found 를  error · empty 로  ★ 못 낮춘다
```

★ 407 은 「그때 못 받았다」는 말이지 ★ **「자료가 없다」가 아닙니다.**

### ② 이미 어긋난 것을 원문 기준으로 되돌렸습니다

```
tools/heal_status_from_raw.py     ★ 신설
  raw/ 를 훑어 ★ ok 인 파일이 있는데 DB 가 error 면 되돌린다
  ★ 다시 받지 않는다 — ★ 파일이 정본이다 (S46-185)
실측  8,450건 되돌림
```

### ③ 원문을 칸에 넣었습니다

```
tools/parse_stored_encar.py       ★ 신설
  이미 가진 상세 원문을 ★ 파서가 주는 칸에 다 넣는다
실측  채움 9,300대 · 건너뜀 9,171 (같은 매물의 옛 파일)
```

★ `refill_encar_detail` 은 칸 **둘**만 썼습니다 — 이 자는 **파서가 주는 칸을 다** 넣습니다.

---

## 3. v391 여쭐 것 3 — `inspection` · `record` 파싱을 붙였습니다

```
report/screens/fetch.py  _parse_into → _into_side
  inspection → core_inspection   (parse_inspection)
  record     → core_record       (parse_record)
실측  40개씩 시험 → ★ 40/40 넣음
```

★ 이제 「⤓ 이 차의 성능·보험 받기」가 ★ **누르는 즉시** 화면에 반영됩니다.
★ 매물을 못 찾으면 아무것도 안 합니다 — ★ 떠도는 줄을 만들지 않습니다.

---

## 4. 흰색 후보 — 남은 23대는 배경에서 받는 중입니다

```
흰색 29대   상세 ok 5 · error 1 · 미조회 23
탭 3        조건에 다 맞는 차 ★ 4대 (그대로)
```

★ 서버 회선이 5분에 1대라 ★ 23대면 두 시간입니다 — ★ 배경에서 돌려 두었습니다.
★ **마스터 폰이면 3초**입니다.

---

## 5. 두 도구

```
python3.11 tools/run_tests.py
결과: 실패 — test_endtoend.py   (그 밖 열여덟 파일 전부 통과 · ruff 통과)
fatal 넷 — V4-28 · V3-77 · V11-12 · V11-34   ★ 넷 다 이번 작업이 만든 것이 아니다
```

---

## 6. 여쭐 것

### 1  ★ 무엇이 8,450건을 덮었는지는 **아직 못 짚었습니다**

막았고 되돌렸지만 ★ **범인을 못 찾았습니다.**
S5 는 `should_refetch` 로 `ok` 를 건너뜁니다 — 그 길은 아닙니다.
★ 이제 되덮기가 막혔으니 ★ **다시 생기면 그때 잡힙니다.**
★ 검사를 하나 두는 것이 좋겠습니다 — 「원문이 ok 인데 DB 가 error 인 줄이 0」.
★ 가이드께서 `S46-292` 로 세워 주시면 제가 코드를 맞추겠습니다.

### 2  ★ 「받을 것」 수를 화면이 **믿을 수 있어야** 합니다

이번에 화면이 8,684 이라 했는데 실제는 1,753 이었습니다.
`/fetch` 가 **원문 파일도 함께 보고** 세도록 할까요?
지금은 DB 상태만 봅니다 — 상태가 어긋나면 화면도 같이 틀립니다.

### 3  CSS 세 자리는 그대로입니다 (v389 여쭐 것 1·2·3)

## 7. 이견

없습니다. ★ 「상태가 곧 사실」이라 믿은 것이 제 잘못입니다.
