# v290 — ③ 수가 화면마다 다르다 · STEP 82c 딜러 정직도

```
지시   명령서 92장 ③ · 마스터 「do it」
정본   outputs/20260828_2040_시험자_1_2_3회차_결과.md
새 규칙 회차 보고 첫 줄은 「안 한 것」 표다 (확정 35 · 08d8a7b2)
```

## 0. ★ 안 한 것 — 이 회차 시작 시점

| 무엇 | 왜 못 했나 | 며칠째 | 누가 답해야 하나 |
|---|---|--:|---|
| ④ 판정 다섯 겹침 (사진 둘) | 앞 항목을 먼저 닫느라 | 1일 | 개발측 |
| ⑤ 기아 CPO 0건 | 안 재 봤다 | 4일 | 개발측 |
| ⑥ 렉서스 0건 | 안 재 봤다 | 2일 | 개발측 |
| ⑦ 27축→26축 문장 | 안 넣었다 | 1일 | 개발측 |
| ⑨ 시험자 흔적 · 지우기 단추 | 안 만들었다 | 1일 | 개발측 |
| 89장 엔카 원문 주소 | 안 했다 | 2일 | 개발측 |
| #75 「910 고정」 여부 | STEP 128 과 어긋나 여쭘 | **1회차 미답** | ★ 마스터 |
| GV70_EV 173/170 | 실측 88/88 — 어느 화면 쌍인지 모름 | 1일 | ★ 가이드 |
| KB 값 단위 | 표본은 원 단위로 정상 — 어느 자리인지 모름 | 1일 | ★ 가이드 |
| 소모품 2c | 물음이 어디 있는지 못 찾았다 | 4회차 | ★ 가이드 |
| test_spec_ui L-3 | 〃 | 12회차 | ★ 가이드 |
| CSS 이름 14개 | 〃 | ? | ★ 가이드 |

★ **답이 온 것 하나** — #9 정직도.  가이드가 08-29 에 `STEP 82c` 규격을 썼다
  (개정 822 · `08d8a7b2`).  ★ 이 회차에 바로 구현했다 (아래 2장).

---

## 1. ③ 수가 화면마다 다르다 — 다섯을 한 표로

| # | 어디 | 그 수 | ★ 무엇을 센 수인가 | 조치 |
|---|---|--:|---|---|
| #14 | `/listings` 차종 드롭다운 | 336 → **1,378 등** | 전에는 `status='active'` 하나만 봤다 | ★ **목록과 같은 조건으로 고쳤다** |
| #16 | 드롭다운 19종 합 | 9,563 → **5,088** | 위와 같은 원인 | ★ 고쳤다 (＋차종 미정 3 = 5,091) |
| #18 | 사이트 칩 옆 숫자 | 8,686(엔카) | 「우리 대상(active)」이다 — 기아 CPO 1,021건을 다 받고도 대상 0 인 것을 가르려고 일부러 둔 값 (개정 306) | ★ 곁말에 적었다 |
| #40 | `/admin/targets` 매물 합 | **9,567** | `target_key IS NOT NULL` 전부 — 내려간 것(gone) 포함 | ★ 열에 적었다 |
| #52 | `/notready` 「범위 안」 | **9,567** | 같은 출처 | ★ 옆에 적었다 |
| #43 | `/recommend` 제외 사유 | **1,817** | 매물이 아니라 **사유** 수다 | ★ 표 위에 적었다 |

### 고친 것 — 드롭다운

```
원인   store.core.listing_models 가 status='active' 하나만 보고 세었다.
       목록은 리스 · 관문 제외 · 같은 차 접기 · 팔린 것 · 계약예약중 ·
       범위 밖을 전부 뺀다 — ★ 두 수가 다른 것을 세고 있었다
고침   report/screens/build.py model_counts — _listings_where 를 그대로 쓴다
       ★ 차종 조건만 뺀다 (하나를 고르면 나머지가 0 이 되어 못 바꾼다)
```

```
⑬ 다시 눌러 봤다 (배포 · HTTP)
  G80_25T   드롭다운 1378  → 목록 1,378
  GLC       드롭다운  665  → 목록   665
  X3         드롭다운  582  → 목록   582
  XC60      드롭다운  326  → 목록   326
  19종 합 5,088 ＋ 차종 미정 3 = 5,091 = 목록 전체
```

### 재 보니 이미 맞던 것 — #18

```
같은 조건으로 사이트를 세면 12 사이트 합이 ★ 정확히 5,091 이다
  encar 4,489 · hyundai_cert 174 · volvo_selekt 143 · kbchachacha 100 ·
  heydealer 79 · bobaedream 33 · reborncar 30 · kcar 24 · lexus 10 · bmw 9
★ 시험자는 12 사이트만 더해 4,663 을 얻었다 — 화면이 10개를 낸다
```

### 숫자를 억지로 맞추지 않았다

```
#40 · #52 는 「내려간 것 포함」이 맞는 수다 (관리 화면은 커버리지를 본다).
#43 은 사유별로 세는 것이 맞다 — 한 매물에 사유가 여럿이다 (112건).
#18 은 「받았지만 대상 0」을 가르려고 일부러 그렇게 둔 값이다.
★ 그래서 ★ **조건을 옆에 적었다** (마스터 지시 ③ 그대로)
```

---

## 2. ★ STEP 82c — 딜러 정직도 (가이드 답이 와서 바로 했다)

```
전   core_dealer 1,182곳 ★ 전부 trust_score NULL · 거르개가 늘 0건
지금 ★ 214곳이 매겨졌다
```

`store/core.py` `dealer_trust()` — 규격 그대로

| 칸 | 어떻게 셌나 |
|---|---|
| `drop_event_rate` | `status='gone'` 이 된 매물 ÷ 전체 |
| `price_volatility` | `core_listing_change` 의 `price_current_won` 변경 ÷ 매물 수 |
| `median_dom_days` | `first_seen`~`gone_at` 가운데값 (30일 만점 · 120일 0 · 사이는 곧게) |
| `info_score` | 사진 · 사고(`core_record`) · 보증 · 옵션 넷 중 채운 칸 ÷ 4 |

```
trust_score  넷을 25점씩.  ★ 값이 없는 것은 분모에서 뺀다 (규격)
표본         매물 10건 이상만 잰다.  아래는 NULL 그대로 — ★ 추정으로 안 채운다
quadrant     매물 수 중앙값 · 점수 60 으로 넷
```

★ 한 번 틀렸다가 고쳤다 — 중앙값을 **못 재는 968곳까지 넣어** 세니
  중앙값이 1~2 로 내려가 재는 딜러가 전부 「많다」가 됐다 (Q1 213 · Q3 1).
  ★ **재는 딜러들**의 매물 수로 내도록 고쳤다.

```
실측
  매겨짐 214곳 · 표본 부족 968곳 · 점수 54.2~99.2 · 평균 84.0
  quadrant Q1 115 · Q2 98 · Q4 1   (중앙 매물 수 15)
  ★ 검산 S46-108 (sample_sufficient=1 인데 trust_score NULL) — ★ 0건
  거르개  >=0 2,092건 · >=70 1,997건 · >=90 342건   (전에는 늘 0건)
```

★ 화면도 사실에 맞췄다 — 「10건 미만 딜러는 못 잽니다」.
  어제 붙인 「아직 안 매겨졌습니다」를 되돌렸다.

---

## 3. 1차 → 조치 후

```
check_all  통과 397 · fatal 10 · warn 12 · 환경 차이 1 · 미실행 8   (앞뒤 같다)
run_tests  실패 1파일 test_endtoend · ruff 통과                    (앞뒤 같다)
test_store · test_screens · test_web · test_spec_ui · test_report ·
test_integration — 그 자리에서 다시 돌려 전부 통과
```

```
「틀리다」 47 → 33건   (① 4건 · ② 4건 · ③ 5건 · #9 1건)
```

## 4. ★ 여쭐 것

```
① #75 「910 아니면 막아라」 — ★ 1회차째 미답
   STEP 128 「total_points 직접 입력 금지 · 성분 합으로만 정해진다」와 어긋납니다.
   910 을 고정하면 성분을 더하거나 스킵하는 일 자체가 불가능해집니다
② STEP 82c 상수(10건 · 30일 · 120일 · 60점)를 config 로 옮길까요
   명령서가 scoring.json 을 손대지 말라 하셔서 코드 기본값으로 뒀습니다
③ GV70_EV 173/170 · KB 값 단위 — 어느 화면 쌍인지 못 찾았습니다
   실측은 GV70_EV 88/88 이고 KB 값은 원 단위로 정상입니다
④ 소모품 2c(4회차) · test_spec_ui L-3(12회차) · CSS 이름 14개 —
   물음이 어느 문서에 있는지 못 찾았습니다.  가리켜 주시면 바로 하겠습니다
```
