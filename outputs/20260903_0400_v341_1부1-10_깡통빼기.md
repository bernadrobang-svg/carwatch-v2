# v341 — 1부 1-10 · 깡통 빼기

명령서 1부 1-10 (시험자 10) — 「★ **「깡통 빼기」가 없다** — 거르개가 다섯뿐이다」

## 1차 결과

```
화면 거르개   model · fuel · min_grade · order · region  ＋ 「깡통 빼기 N종 이상」 칸
★ 시안 `v3_listings_시안.html:133` 은 ★ **단추**다 — 「깡통 빼기」
★ 화면은 ★ **수를 적어야** 했다 — ★ 「깡통이 몇 종인지」를 알아야 쓸 수 있었다
```

## 무엇을 · 왜 했나

| 어디 | 무엇 |
|---|---|
| `config/web.json` | `bare_option_won` = **1,000,000** 신설. 시안 216줄 「★ 깡통은 ★ **옵션가 100만원 이하** (마스터 확정 08-24)」. **코드에 안 박는다** (`S14`) |
| `ListingFilter.no_bare` | 거르개 칸 |
| `_listings_where` | 선택 옵션이 **하나도 없는 차**를 뺀다 |
| `listings.html` | **한 번 눌러 빼는 단추** (시안 꼴) |
| `_pick_state` | 「깡통 제외」를 조건 문장에 낸다 |

## 조치 후 결과 — ★ 배포 브라우저

```
  /listings           건수   5,549 · 단추 있음
  /listings?no_bare=1 건수 ★ 1,852 · 단추 있음
```

| | 앞 | 뒤 |
|---|--:|--:|
| 「깡통 빼기」 단추 | 없음 | **있다** |
| 눌렀을 때 매물 | — | 5,549 → **1,852** |

## 여쭐 것

```
★ 「깡통」을 ★ **무엇으로 가를지** 못 정할 자리였다
★ 규격은 ★ 「옵션가 **100만원 이하**」인데
  ★ ★ 옵션가는 ★ 코드 목록에 ★ 값을 곱해야 나온다 — ★ SQL 로는 못 곱한다
  ★ ★ (`options_choice_json` 은 `["1050","1046",…]` 꼴 · 값은 카탈로그에 있다)
★ 안 ① **종 수**로 가른다 — ★ 옵션이 하나도 없으면 깡통    ← ★ **이것으로 갔다**
★ 안 ② 판정이 낸 `option_price_won` 을 ★ `result_score` 에 저장하고 ★ 그것으로 가른다
   ★ ★ 그러면 ★ 규격대로 ★ 100만원 잣대를 쓸 수 있다.  ★ 표를 하나 늘려야 한다
★ 안 ①로 넣고 진행했다 — ★ 잣대(`bare_option_won`)는 ★ 이미 config 에 뒀으니
  ★ ★ 안 ②로 갈 때 ★ **그 값을 그대로 쓰면 된다**
```
