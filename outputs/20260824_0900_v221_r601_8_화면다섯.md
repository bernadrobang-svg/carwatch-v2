# r601 8단계 — 화면 ②④⑤⑥⑦ · L 재판정 채우기

`SPEC-2026.08.23-r601` · 명령서 13-2 · 14-3
2026-08-24 09:00 · v221

---

## 1. ★ 화면 다섯 — ★ 무엇이 몇에서 몇이 됐나

| | 무엇 | 1차 | ★ 조치 후 |
|:--:|---|--:|--:|
| ② | 상세·`/why` 에 그대로 나온 ★ 옵션 코드 | **48개** | ★ **0개** |
| ④ | `/why` 근거 칸의 ★ 영문 코드 | **24종 · 25자리** | ★ **0종 · 0자리** |
| ⑤ | 상세 축의 ★ 「0」이 한 얼굴 | 26축이 ★ 숫자뿐 | ★ **OK 24 · × 1 · — 1** |
| ⑥ | 「엔카에서 보기」가 ★ 채운 단추 | **0 / 52** | ★ **52 / 52** |
| ⑦ | 보증을 ★ 날짜로 | 점수뿐 (`3/22점`) | ★ **2029년 8월까지 (60,000km 까지)** |

---

## 2. ② 옵션 코드 — ★ 까닭은 사전이 비어 있어서가 아니었다

```
★ 실측 — `dict_option_code` 가 ★ 151행 있다.  ★ 비어 있지 않다
★ ★ 그런데 ★ 전부 ★ `display == code` 다 —
   ('PPF','PPF') · ('033','033') · ('097','097')
★ ★ 카탈로그를 못 받아 ★ 코드가 ★ 자기 이름으로 들어 있었다
★ ★ 옛 코드는 ★ `known = c in names` 로 봐서 ★ 「안다」고 판정하고
   ★ 코드를 그대로 화면에 냈다 — ★ 1011 · 001 · 004 … 48개
```

★ 고침 — ★ 이름은 ★ `display != code` 일 때만 ★ 이름이다.
★ 모르는 것은 ★ 세기만 한다 (규격 2장 「코드 46개 나열은 아무 도움이 안 된다」).

```
지금 화면 —  「선택 옵션」 2종
             2종 — 이름 미조회 (카탈로그를 아직 받지 못했습니다)
             「기본 옵션」 46종
             46종 — 이름 미조회 (카탈로그를 아직 받지 못했습니다)
★ 명령서 13-2 ② 「패키지가 들어오기 전까지 ★ 「옵션 2종 · 이름 미조회」로」 그대로다
```

## 3. ④ `/why` 근거를 한국어로

★ 뜻은 ★ **그 코드를 내는 자리를 읽어** 적었다 (`analyze/axis/*.py`) — ★ 지어내지 않았다.
★ 표는 ★ `config/dictionaries/source_labels.json` 이 정본이다 (S14).

```
exact 34가지 · prefix 12가지 · suffix 1가지
★ 표에 없는 코드는 ★ 코드를 그대로 낸다 — ★ 짐작으로 이름을 붙이지 않는다
★ 합성(`a+b+c`)은 ★ 조각마다 옮겨 「·」로 잇는다
★ ★ 「엔카진단+」는 ★ 합성이 아니라 ★ 그 자체가 코드다 — ★ exact 를 먼저 본다
```

| 코드 (`title=` 로 뒤에) | ★ 보이는 글 |
|---|---|
| `not_join_0%` | 자차 미가입 0% |
| `owner_0` | 소유자 변경 0회 |
| `detail_seizing` | 압류 · 저당을 상세에서 확인했습니다 |
| `record_accident_count` | 보험 사고 횟수로 봤습니다 |
| `site_unavailable` | 이 사이트가 주지 않는 값입니다 |
| `frame_sheet1_insurance_trace` | 골격 — 판금 1곳 (보험에 수리 흔적이 있어 한 단계 내렸습니다) |
| `usage_change_types+record_use+plate_use_char` | 용도 변경 이력으로 봤습니다 · 자동차이력의 용도 이력으로 봤습니다 · 번호판 용도 기호로 봤습니다 |

★ 시세 축 설명 넷도 ★ 코드를 `title=` 로 넘겼다 (UI_REVIEW 4장 「코드가 먼저 나온다」).
★ **원문 보기표의 `<code>` 는 그대로 두었다** — ★ 거기는 ★ 원문 키를 내는 자리다.

## 4. ⑤ 0 을 셋으로 가른다

| 뜻 | 기호 | 언제 |
|---|:--:|---|
| 확인했고 좋다 | **OK** | 값이 0보다 크다 |
| 확인했고 없다/나쁘다 | **×** | 값이 0이고 ★ 근거가 「확인함」이다 |
| 확인 못 했다 | **—** | 근거가 `missing`·`site_unavailable`·`catalog_missing` 등 · 제외된 축 |

```
배포 실측 (상세 26축) — ★ OK 24 · ★ × 1 · ★ — 1
   소모품  ★ —   (0/15점 · 확인하지 못했습니다)      ← 엔카 미제공
   선루프  ★ ×   (0/12점 · 확인했고 해당하지 않습니다) ← 확인했고 없다
★ UI_REVIEW 5a 가 든 ★ 그 두 예와 ★ 같다
★ 기호 옆에 ★ 한 줄 까닭을 ★ `title=` 로 함께 낸다
★ 점수는 ★ 안 건드렸다 — ★ 910점 체계 그대로다.  ★ 보이는 것만 바꿨다
★★ 검산 S46-18 — ★ 확인 못 한 축이 `×` 로 나오나 → ★ 0건 (통과)
★★ `/why` 는 ★ 숫자 그대로다 — ★ 축 표에 숫자 26쌍 · ★ 기호가 안 샜다
```

## 5. ⑥ 나가는 단추

```
★ v3 시안이 ★ `.btn.go{background:var(--amber);border-color:var(--amber);color:#111;font-weight:600}` 를 준다
★ `app.css` 에 ★ 그대로 넣었다 (시안이 정본)
배포 실측 — 「엔카에서 보기」 52개 · ★ 그중 채운 단추 ★ 0 → ★ 52
   /detail 1 · /why 1 · /listings 50
```

## 6. ⑦ 보증을 날짜로

```
전   제조사 보증 (일반)   3/22점
후   제조사 보증 (일반)   ★ 2029년 8월까지 (60,000km 까지)  3/22점
     제조사 보증 (동력계) ★ 2031년 8월까지 (100,000km 까지) 32/32점
★ 우리가 가진 것은 ★ 남은 개월이라 ★ 오늘에 더해 끝나는 달을 낸다
★ 0 개월은 ★ 「만료」로 적는다 — ★ 확인한 값이다
★ 없으면 ★ 「기간 미조회」다 — ★ 「없다」로 적지 않는다 (개정 325)
```

---

## 7. ★★★ L — 재판정 채우기 (명령서 14-3)

### 7-1. ★ ① 먼저 재서 적는다 — ★ 어제 13시 타이머가 돌았나

```
★★ ★ 안 돌았다.  ★ 확실하다 —

systemctl show carwatch-daily.timer -p LastTriggerUSec
  → Sun 2026-08-23 13:32:46 KST        ★ 트리거 기록은 ★ 있다

journalctl -u carwatch-daily (boot -1 · boot 0 전부)
  → ★ 마지막이 ★ 2026-08-22 13:00:36 「하루치를 큐에 넣었습니다 — 752c334f」
  → ★ 08-23 에는 ★ 항목이 ★ 하나도 없다

★ ★ 즉 ★ 타이머는 ★ 「돌았다」고 적혀 있는데 ★ 서비스는 ★ 안 돌았다
★ ★ 그날 서버가 ★ 11:59~15:15 멎어 있었다 (SERVER_SURVIVAL 0a) — ★ 그 사이다
```

### 7-2. ★ ② 네 시간마다 보는 타이머

```
config/admin.json   recalc_catchup_at = ["01:00","05:00","09:00","13:00","17:00","21:00"]
                    recalc_stale_hours = 4          ★ 시각·기준의 정본이다 (V10-31)
deploy/carwatch-recalc-catchup.timer    OnCalendar=*-*-* 01,05,09,13,17,21:00:00
                                        ★ Persistent=true (14-3 ③)
deploy/carwatch-recalc-catchup.service  ExecStart=python3.11 tools/recalc_catchup.py
tools/recalc_catchup.py                 ★ 신설
설치 — systemctl enable --now · ★ 다음 실행 Mon 2026-08-24 05:00 KST
```

★ **밀린 것만 채운다.** ★ 무조건 돌리지 않는다.
★ 「마지막 재판정」은 ★ `result_score.calculated_at` 을 본다 — ★ 큐에 넣은 시각이 아니다.
★ 넣기만 하고 안 돌면 ★ 「돌았다」가 되어 ★ 영영 안 채우기 때문이다.

### 7-3. ★★ 일부러 밀려 놓고 ★ 잡는 것을 봤다 (원칙 6)

```
① 방금 돈 것처럼    (0.0시간 전)  → ★ 「안 밀렸다.  아무것도 안 한다」
② 일부러 밀림       (17.2시간 전) → ★ 「밀렸다.  채운다」
③ 다시 방금 돈 것처럼 (0.0시간 전) → ★ 「안 밀렸다.  아무것도 안 한다」
★ ★ 운영 DB 를 안 건드리고 ★ 작은 DB 로 시험했다
```

### 7-4. ★★★ 실제로 밀린 것을 잡았다

```
넣기 전   마지막 재판정 ★ 2026-08-23T13:10:59  (4.0시간 전 · 기준 4시간)
          큐 — done 5 · failed 133 · ★ queued 0

실행      python3.11 tools/recalc_catchup.py
          → ★ 「마지막 재판정 4.0시간 전 · 기준 4시간 — ★ 밀렸다.  채운다」
          → ★ 「재판정을 큐에 넣었습니다 — 9a316dcc」

일꾼이 집었다   9a316dcc  queued → ★ running → ★ done
넣은 뒤   마지막 재판정 ★ 2026-08-23T17:17:11   ← ★ 13:10 에서 ★ 갱신됐다
```

★ **「밀린 것을 잡는 것을 봤다.」**

### 7-5. ④ 새 사이트를 저장한 단계에서 함께 큐에 넣는 것

★ **아직 안 했다.** ★ 아래 9장 ⓑ 에 적었다.

---

## 8. 검사

```
run_tests   1차 — 실패 둘 · 2차 — ★ V11-60 하나만 남았다
              V11-45 「CLI 로만 되는 기능」 → ★ tools/recalc_catchup.py 를
                     config/cli_screens.json 에 등재해 ★ 통과
              ★ V11-60 「시안 CSS 를 다시 만들지 않음: 다름」 → ★ 아래 9장 ⓐ
                     ★ 어긋난 28곳이 ★ 전부 v3 시안 두 파일에서 나온다 —
                       v3_detail_watch_compare 12곳 · v3_listings 16곳
                     ★ 내가 넣은 `.btn.go` 는 ★ 그 목록에 없다
                     ★ 그 두 파일은 ★ 이번 pull(5c27fbf · r622)로 들어왔다
            나머지 19개 · ruff ★ 전부 통과
```

---

## 9. ★ 못 한 것 · 알릴 것

### ⓐ ★★ `V11-60` 이 ★ 새로 FATAL 이 됐다 — ★ 내가 낸 것이 아니다

```
★ 이번 `git pull` 로 ★ v3 시안 두 파일이 들어왔다 —
   ref/screens/v3_listings_시안.html · v3_detail_watch_compare_시안.html
★ ★ `V11-60` 은 ★ `ref/screens/*.html` 의 ★ 모든 `.class{…}` 를
   ★ `web/static/app.css` 와 ★ 값까지 견준다
★ ★ 그래서 ★ 28곳이 어긋난다 — ★ `.top` `.brand` `.note` `.btn` `.sec` `.lbl` `.g2` `.mono` …
★ ★ 앞 회차(v220)에서는 ★ 20/20 통과였다 — ★ 시안이 들어오기 전이다

★★ ★ 이것은 ★ v3 시안으로 ★ CSS 를 통째로 옮기는 일이다 —
   ★ 명령서 13-2 가 ★ 지금 하라고 한 다섯(②④⑤⑥⑦)에 ★ 없다
   ★ 그리고 ★ 「한 번에 다 하지 마라」고 하셨다
★ ★ 그래서 ★ 28곳을 ★ 임의로 고치지 않았다.  ★ 숫자만 적는다
★ ★ 여쭐 것 — ★ v3 시안으로 ★ CSS 를 맞추는 것이 ★ 다음 일입니까
   ★ 맞다면 ★ 어느 화면부터 할지 적어 주십시오 (절 차례는 안 바꿉니다 · V11-159)
★ ★ `.btn.go` 하나는 ★ ⑥ 이 시킨 것이라 ★ 시안 값 그대로 넣었습니다
```

### ⓑ ★ 14-3 ④ — 새 사이트 저장 뒤 재판정을 함께 큐에

```
★ 아직 안 했다.  ★ 다음에 한다
★ 지금은 ★ 네 시간 타이머가 ★ 최대 네 시간 안에 잡는다 — ★ 하루가 아니다
```

### ⓒ 아직 안 한 것

```
· 검사 둘 (명령서 15-2 ① nav 차례 · ② V11-158 「문이 있는가」)
· E — KB 목록 한 번에 받고 상세만 회차 나누기 (14-1)
· 7 — 보배드림 67쪽 · 리본카 · 렉서스 · 볼보
```
