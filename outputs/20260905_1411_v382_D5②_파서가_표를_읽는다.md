# D5 ② — ★ 파서가 매핑표를 읽는다 · ★ 트림 0 이 **열 곳 → 여덟 곳**

```
지시문   개발팀 명령 r1168 · D5 ②
검산     S46-282 · S46-280
날짜     2026-09-05 14:11 (KST)
```

## 1. 무엇을 했나

| 자리 | 무엇 |
|---|---|
| `tools/load_field_map.py` | ★ 등록부에서 ★ **파서가 읽을 표**를 낸다 → `config/dictionaries/field_map.json` |
| `parse/field_map.py` (새로) | ★ 공용 읽개 — ★ `apply_map(site, item, out)` |
| `tools/load_raw.py` | ★ **한 자리**에 꽂았다 — ★ 목록·상세 둘 다 지나간다 |
| `config/dictionaries/field_map_extra.json` (새로) | ★ **원문에서 캔 길** (개발측 몫) |

```
★ 파서는 ★ **DB 를 안 연다** — ★ config 를 읽는다 (`V11-01` 과 같은 뜻)
★ ★ **덮지 않는다** — ★ 파서가 이미 채운 칸은 그대로.  ★ 표는 ★ **빈 칸만** 메운다
★ 「한 번 고치면 열두 곳이 함께 산다」 — ★ `load_raw` 한 자리를 ★ 열두 곳이 지난다
```

## 2. ★★ 먼저 밝힐 것 — ★ 가이드 표만으로는 **0칸**이 메워졌다

```
★ 꽂고 나서 ★ 여섯 사이트에 돌렸다 — ★ 메운 칸 ★ **0**.
★ 까닭을 짚었다 — ★ 길은 원문과 ★ **맞는다** (기아 8/11 · 리볼트 3/6).
  ★ ★ 그런데도 0 인 것은 ★ **파서가 이미 그 칸을 채우기** 때문이다.
★★★ 곧 ★ 가이드 표는 ★ **파서 코드에서 뽑은 것**이라 ★ 파서가 읽는 길뿐이다 —
  ★ ★ 「**파서가 안 읽는 칸**」은 ★ 거기 없다.  ★ 그것이 ★ 옵션·트림이 0인 까닭이다
```

★ 이 사실을 먼저 적는다 — ★ 「표를 꽂았다」만 적으면 ★ **선언과 실제가 갈린다**.

## 3. ★ 그래서 — ★ **원문에서 길을 캤다**

```
kia_cpo   원문에 ★ `modelTrim` 「프레스티지」 가 있다
          (★ `modelName` 「스포티지 2.0 디젤 프레스티지 2WD A/T」 의 트림 자리)
revolt    원문에 ★ `grade_name` 「퍼포먼스 AWD」 가 있다
★ 두 줄을 ★ `config/dictionaries/field_map_extra.json` 에 적었다 — ★ **본 값**과 함께
★ ★ K카는 ★ `trim_grade_name` 에 맞는 칸이 ★ **없다** · ★ 렉서스·현대인증은 ★ 목록에 트림이 없다
  ★ ★ ★ **지어내지 않았다**
```

## 4. 조치 후 결과 (실측)

```
매핑표가 메운 칸   ★ kia_cpo **447** · ★ revolt **3,617**
trim_grade_name
   revolt      0 → ★ **74 / 74 (100%)**
   kia_cpo     0 → ★ **93 / 1,081**
   heydealer   315 / 315 (그대로)
★ 트림 0 인 곳 ★ **열 곳 → 여덟 곳**
등록부  981 → **983줄** · `field_map.json` **303줄 · 10곳**
```

## 5. 규칙 3 의 두 도구

```
python3.11 tools/run_tests.py
  실패 1파일 — test_endtoend.py
    FAIL  fatal 실패가 없다  ['V1-27', 'V4-28', 'V3-77', 'V11-34']
  ★ 이 회차 앞과 ★ **같다**
```

## 6. 못 한 것 · 다음

```
① ★ 남은 트림 여덟 곳 — ★ KB(목록 파서가 트림을 준다 · 판이 도는 중) ·
   ★ 리본카·현대인증·K카·BMW·보배·볼보·렉서스는 ★ **원문을 더 봐야** 한다
② ★ 옵션 0 열한 곳 — ★ 같은 길이다.  ★ 원문에서 길을 캐야 한다 (D6)
③ ★ KB 목록 판이 아직 돈다 — ★ 원문 **233쪽**.  ★ 끝나면 넣고 수를 적는다
④ ★ 앞 회차의 여쭐 것 — ★ 엔카 상세는 ★ 마스터 회선이 필요하다 (407 누적 차단)
```
