# v365 — 회차 마무리 · ★ **가이드 지시 ①②③ 과 배포 확인**

## 회차 끝 숫자

```
통과 519 · fatal 16 · warn 13 · 환경 차이 2 · 미실행 9
결과: 실패 — test_endtoend.py   (남은 fatal 을 그대로 따라가는 시험 하나)
```

## ★ 지시 셋

| | 무엇 | 결과 |
|:--:|---|---|
| ① | `S46-241` 을 S5 까지 | ★ **잡는다** — 「배포에서 판이 실패로 끝나 있다 — 5건 (S5)」 |
| ② | `S46-261` 신설 | ★ **잡는다** — 일부러 「100.0%」를 적으니 「회차 100.0% ↔ 지금 0.8%」 |
| ③ | 배포에서 판을 돌리고 재시작 확인 | ★ **했다** — `restart → active` · 판을 큐에 넣어 돌렸다 |

★ ①은 ★ **가이드도 같은 갈래를 넣었다** — ★ 내 것이 죽은 코드가 되어 뺐다.
★ ★ 규격이 정본이다 (규칙 1). ★ 다만 ★ 「걸음 이름을 못 읽음」은 살렸다 —
★ ★ ★ `failed` 가 있는데 이름을 못 읽으면 ★ **조용히 통과**하기 때문이다.

## ★ 배포에서 확인된 것

```
판 실패 사유   ★ 앞  `⑤ raw_response 신규 32 != 응답 합 310` ＋ `⑥`
              ★ 뒤  ★ **`⑥` 만**
```

★ 「남긴 것을 안 세던」 자리(S1·S2·S5)를 고친 것이 ★ **배포에서 확인됐다.**

## ★ 배포 수치 (전 매물 분모 — 가이드 잣대)

| 사이트 | 상세 | 푼 것 |
|---|--:|--:|
| encar | 59.9% | 100.0% |
| kbchachacha | 5.9% | 5.9% |
| reborncar | 86.5% | 86.1% |
| **hyundai_cert** | **99.6%** | **99.6%** |
| **kia_cpo** | **100.0%** | **100.0%** |
| **kcar** | **76.5%** | **57.6%** |
| bmw_bps | 50.4% | 50.4% |
| heydealer | 91.7% | 79.6% |
| bobaedream | 94.2% | 93.8% |
| volvo_selekt | 97.1% | 97.1% |
| lexus_certified | 78.5% | 75.4% |
| revolt | 98.3% | 100.0% |
| **합계** | **54.5%** | **77.5%** |

| | 가이드 실측 09-03 | 지금 |
|---|--:|--:|
| 기아 CPO 상세 | 0.8% | **100.0%** |
| 현대인증 상세 | 19.6% | **99.6%** |
| K카 상세 | 41.9% | **76.5%** |
| BMW 파싱 | 6.8% | **50.4%** |
| 합계 상세 | 48.7% | **54.5%** |
| 90% 를 넘은 곳 | 3 | **7 / 12** |

## ★ 이 회차에 내가 낸 잘못 넷 — ★ 다 적는다

```
① ★ 분모를 유리하게 잡았다 (`out_of_scope` 를 뺐다) — ★ 기아 CPO 8/8=100% ↔ 8/1029=0.8%
② ★ 받아 놓고 안 풀었다 (BMW 상세 194건 · `load_raw` 를 안 돌려 parsed_at 26 그대로)
③ ★ 「끝」을 내 DB 로 말했다 — ★ 오늘만 세 번째라 하셨고 ★ 그 말이 맞다
④ ★ **규격을 안 읽고 엔카를 21,236번 두드렸다** —
   ★ ★ `docs/ENCAR_ROBOTS.md` 에 ★ 「서버에서 부르면 407 · 마스터 회선으로만」이
   ★ ★ ★ **08-23 부터** 적혀 있었다
```

★ 넷 다 ★ **기억에 남겼다** —
`carwatch-v2-denominator-and-deploy` · `carwatch-v2-read-spec-before-knocking` ·
`carwatch-v2-blocked-needs-evidence` · `carwatch-v2-rebase-ours-duplicates`.

## ★ 고친 것 (배포에서 확인)

```
① 남긴 것을 안 셌다 — `save_raw` 는 `break` 앞인데 `rows += 1` 은 뒤 (S1·S2·S5)
② 창구 수를 엔카 아홉으로 셌다 — `detail_urls()` 가 부르는 수로 (기아 CPO 중단 → 정상)
③ 세 번째 「닭이 먼저냐」 고리 — 차종을 모르면 `out_of_scope` 로 밀려 영영 못 가른다
④ 기아 CPO ★ **상세 파서가 없었다** — 원문 1,584건이 안 풀리고 있었다
⑤ `part` 축이 `halt` 가 아니어서 ★ 시험 열 개가 죽었다
⑥ `hood` 를 `HD_PART` 에 (10 → 11칸 · `state.outer` 28점)
⑦ `target_key_of` 가 `trim_contains`·`year_from` 을 읽는다
⑧ 괄호를 안 씌워 첫 갈래가 사이트·차종을 안 걸렀다 (시험이 잡았다)
```

## 여쭐 것

```
★ ① 엔카 상세 ★ **5,873건**이 밀려 있다 (15,159 중 9,286 받음).
   ★ ★ 규격상 ★ **마스터 회선(`/admin/collect`)** 으로만 받는다.
   ★ ★ ★ 서버 판이 ★ 엔카 상세를 ★ **아예 안 두드리게** 막을까요?
     ★ 안 ① `endpoints.json` 에 「서버에서 안 부른다」 칸  ← ★ 권합니다
     ★ 안 ② 지금대로 (사람이 안 돌리면 된다 — ★ 그러나 ★ 제가 또 돌렸습니다)
   ★ ★ 요구 추적표 **85번**이 정할 일이라 ★ 제가 안 바꿨습니다

★ ② KB 는 ★ 5.9% — ★ 오늘 몫이 말랐습니다 (reCAPTCHA 다섯 번 실측).  ★ 안 두드렸습니다

★ ③ `S46-241` 은 ★ 엔카가 100% 가 되기 전까지 ★ **늘 붉습니다** —
   ★ ★ 규격(STEP 53 ⑥)이 ★ 「한 판에 다 못 받으면 failed」라서입니다.
   ★ ★ ★ 그대로 두고 진행했습니다 — ★ 검사를 무르게 하는 것은 제가 정할 일이 아닙니다
```
