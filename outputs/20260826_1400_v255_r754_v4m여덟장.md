# v4m 여덟 장을 정본으로 — 화면을 다 옮겼다

```
버전     소스 v255 · 지시서 SPEC-2026.08.26-r754
때       2026-08-26 14:00
지시     ★ 마스터 확정 「그래 관심 이걸로 가자」 · ref/screens/v4m_* 여덟 장이 정본
         ★ S46-22(시안 절 대조)를 v4m_* 로 옮겨라
앞 작업  3cb0a58 (한 쪽 30장 · 비교 네모)
```

---

## 1. 무엇을 하려 했나 (지시 원문)

```
★ 지킬 것 (여덟 장 공통)
  ★ 모바일이 기준이다.  넓어지면 카드가 「펴진다」.  옆으로 붙이지 마라
  ★ 정보를 빼지 마라.  세 폭에서 같다
  ★ 한 화면에 한 가지 일.  다른 화면의 일은 문(단추)으로만 잇는다
  ★ 맨 위에 「이 화면은 무엇을 하는 곳인가」 ＋ 「~와 다르다」
  ★ 단추 44px 이상 · 아래 탭(640 아래) · 링크의 한글은 인코딩

★ 화면마다 새로 든 것
  매물   ★ 한 쪽 30장 · 기본 다섯 ＋ 자세한 조건 다섯 절
  상세   ★ 열한 절 · 11절 「중복매물」 · 절 차례를 바꾸지 마라
  추적   ★ 빠진 축에 갈래(①~⑤) · ★ 분모는 910 · 정렬 「차액 큰 순」
  미판정 ★ 「여쭐 것」을 맨 위에 크게 · 나머지 둘은 건수만
  비교   ★ 좁으면 줄 · 넓으면 칸 · 두 대 넘게~다섯까지

★ S46-22(시안 절 대조)를 v4m_* 로 옮겨라
```

---

## 2. 1차 결과 (고치기 전 숫자)

시안 여덟 장을 받고 **처음** 대조한 것이다.

```
S46-22 를 v4m 으로 돌리자마자
  detail.html    절 11개 · 시안 5개
  notready.html  절 3개 · 시안 4개
  dashboard.html 1번째 — 시안 「오늘 바뀐 것」 · 화면 「필터」
  admin.html     절 5개 · 시안 4개
  compare.html   절 0개 · 시안 4개
tools/check_screens.py   시안 절 대조 실패 9
```

★ 여덟 쌍 가운데 **다섯이 어긋났다.**

---

## 3. 무엇을 했나 (고친 파일 · 줄)

### ① S46-22 · `check_screens.py` 를 v4m 으로

```
validate/v0_guide.py  SIAN_PAIRS 여덟 쌍을 v3_* → v4m_* 로
                      절 이름 정규식에 `v4-` 를 더했다
tools/check_screens.py  _pairs() — ★ 같은 이름의 옛 판을 안 본다
                      ★ v4m > v3 > v2.  ★ 새 판이 정본이다
```

★★ 이것을 안 하면 **새 시안대로 고칠 때마다 옛 시안이 빨간불을 낸다.**
실측 — `admin` · `dashboard` · `notready` 아홉 곳이 그랬다.

★ 시안이 가운데 절을 「… 4~10절 (…)」로 **줄여 적는다.**
그때는 **개수가 아니라 차례만** 본다 (두 검사 다).

### ② 현황 — 절 다섯을 시안 차례로 (`dashboard.html` 통째로 다시 엮었다)

```
전   해야 할 일 · 등급 분포 · 조건 완화 · 축별 미달 · 차종별 시세 ·
     차종별 · 주의 · 1 오늘 · 사라짐 · 관심매물 · 수집 · 상위 후보
후   1 오늘 바뀐 것 · 2 등급 분포 · 3 차종별 시세 · 4 사이트별 수집 · 5 해야 할 일
```

★ **덩이를 옮겼을 뿐 하나도 안 버렸다.**  옛 `h3` 다섯은 절이 아니라
그 안의 작은 머리말이므로 `h4` 로 내렸다.

★ 함께 고친 것 — 「재판정은 **네 시간마다** 확인합니다」가 두 곳에 있었다.
마스터 확정 ③ 대로 「**날마다 13:00 에 전량** · 못 돌았을 때만 네 시간 뒤」로 바꿨다.

### ③ 미판정 — 「여쭐 것」을 맨 위에 크게

```
web/templates/notready.html
  절 이름 넷을 시안 그대로 — 여쭐 것 · 접어 둔 것 (갈래가 달라 뺐다) ·
                            팔린 것 · 등록부 미분류 (사전이 막는 것)
  「여쭐 것」을 .ask 상자에 — 주황 테두리 · 건수를 크게
  ★ 「팔린 것」 절을 새로 냈다 (gone_count) — 전에는 접힌 글 속에만 있었다
```

★★ **찾은 것 — 절 넷이 화면에 두 번 나오고 있었다.**
템플릿에 **똑같은 덩이가 한 벌 더** 있었다 (전에도 그랬다 · 내가 못 봤다).
한 벌을 지웠다 — 화면이 22KB → **12KB** 가 됐다.

### ④ 관리 — 절 넷

```
전   운영 · 조정 · 탐색 · 화면 — ★ 둘만 남는다 · ★ 계정
후   운영 (＋ 탐색을 h4 로 안에) · 조정 · ★ 계정 · 내려온 것 둘
```

★★ 시안에 **「탐색」이 없다.**  그러나 **문 다섯을 지울 수는 없다** (개정 427).
그래서 절이 아니라 **운영 안의 한 덩이**로 넣었다 — 문은 그대로다.
★ 화면에도 그렇게 적어 두었다.  **규칙 2 로 여쭙는다** (7장).

### ⑤ 비교 — 절 넷 · 좁으면 줄 · 넓으면 칸

```
web/templates/compare.html  고른 셋 · 점수 · 사고 · 골격 · 주행 · 연식
web/static/app.css          .vlist · .vline
   기본     한 줄이 매물 하나 (세로로 쌓인다)
   ≥640     칸으로 갈라진다 — ★ 견주는 것끼리 나란히 선다
```

### ⑥ 추적 — 표를 버리고 카드로 · 갈래(①~⑤)

```
web/templates/track.html  절 셋을 다 .kcard 로 (관심과 같은 카드다)
  1 값이 크게 갈린 것 — 차액·%를 카드 맨 앞에 · 30% 넘으면 붉게 경고
  2 등급이 갈린 것   — 사이트마다 등급·earned·★ 빠진 축 갈래
  3 사고 판정이 갈린 것
```

★ 갈래(①~⑤)는 `config/labels.json` 의 `MISS_KIND` 가 정본이다 —
f-table 476행에서 옮긴 것이고 **표에 없는 축은 갈래를 안 붙인다.**

### ⑦ 여덟 장 공통 · 검사 S46-75

```
dashboard · admin 에 「~와 다르다」를 더했다 (나머지 여섯은 이미 있었다)
validate/v0_guide.py  s46_75_v4m_common()
  ① 여덟 장에 머리 상자 ＋ 「~와 다르다」
  ② 640 아래에서 상단 메뉴가 아래 탭
  ③ 단추 44px 이상
```

---

## 4. ★★★ 함께 잡은 것 — 추적 화면이 **빈 칸으로 나가고 있었다**

★ 카드로 옮기다가 알았다.

```
web/templates/track.html
  {{ s.0 }} · {{ s.3 }} · {{ s.4 }} · href="/detail/{{ s.1 }}"
```

★★ **틀은 점 뒤에 숫자를 못 쓴다.**
`web/template.py` `_step()` 은 점 뒤를 **늘 글자로** 본다 —
튜플은 `[0]` 꼴로만 짚힌다.  숫자를 점으로 쓰면 **조용히 빈 값**이다.

```
실측 (고치기 전 렌더)
  <td data-label="사이트"></td>              ← 비어 있다
  <td data-label="등급"><span class="grade-none"></span></td>
  <td class="num mono" data-label="earned"> / 910</td>
  <a class="btn mini" href="/detail/">상세 ▸</a>   ← ★ 눌러도 아무 데도 안 간다
```

★ 추적 2절·3절이 **처음부터 그랬다.**  내가 만들 때 넣은 것이다.

```
고쳤다  TrackPair.sites 를 ★ 튜플 → ★ **이름 있는 칸**으로
        {badge · listing_id · price_won · grade · earned · misses}
```

```
실측 (고친 뒤)
  차액 3,911만 · 814.8% · G80_25T · 2곳 · 480만 ~ 4,391만
  ★ 30% 넘습니다 — 짝짓기가 틀렸을 자리입니다
  보배드림 (점수 없음) · 엔카 EXCLUDED 507.9/910
  [보배드림 상세 ▸] [엔카 상세 ▸]      ← ★ 주소가 들어갔다
```

★ 「점수 없음」도 함께 고쳤다 — 전에는 `{{ s.earned | num }}/910` 이
**「없습니다/910」**으로 나갔다.

---

## 5. 조치 후 결과

```
python3.11 tools/check_screens.py
  시안 ↔ 템플릿 짝     통과  통과 8 · 실패 0
  시안 핵심 문구       통과  통과 21 · 실패 0
  시안 절 대조         통과  통과 29 · 실패 0
  상단 메뉴           통과  통과 8 · 실패 0
  전 화면 렌더         통과  통과 32 · 실패 0
결과: 통과

S46-22  시안 8쌍의 절 차례가 화면과 같다
S46-68  카드 · 폭 세 구간 · 22가지를 다 담는다 · 좁아도 안 감춘다
S46-74  목록·관심·추적·미판정이 다 한 쪽 30장 (규격에서 읽는다)
S46-75  여덟 장이 머리 상자 · 「~와 다르다」 · 아래 탭 · 44px 를 지킨다
```

```
python3.11 tools/run_tests.py
  test_admin         통과
  test_admin_flow    통과
  test_collect       통과
  test_crosssite     통과
  test_dict          통과
  test_endtoend      통과
  test_fixtures      통과
  test_integration   통과
  test_invariants    통과
  test_pipeline      통과
  test_registry      통과
  test_report        통과
  test_run           통과
  test_score         통과
  test_screens       통과
  test_spec_ui       통과
  test_store         통과
  test_watch         통과
  test_web           통과
  ruff               통과  (F821,F811,B905,F841,DTZ005,F401)

  임시 DB 62개 치움 (검사가 디스크를 채우지 않는다)

결과: 통과
```

```
python3.11 tools/check_all.py
DB carwatch.db · run 20260825T000022
통과 351 · fatal 4 · warn 11 · 환경 차이 2 · 미실행 8

■ FATAL — 고쳐야 한다
  V1-08     동일 코드 실패율 100% 인 엔드포인트 없음 — 2건 (예: detail: 328건 전량 실패, inspection: 182건 전량 실패)
  V1-21     받아 두고 안 펼쳐진 원문이 없음 — 182
  V1-23     필요한 조합 대비 받은 카탈로그 비율 — 127조합 · 78.9% · 카탈로그 없는 매물 633건 · 사이트가 안 줌 empty 21조합 · 매물 586건 (예: 8238609202207
  V2-01     ok 원문 수 == CORE 행 수 — 5744

■ warn — 진행은 된다
  V1-07     매물별 엔드포인트 4종 상태 존재 — 4242
  V4-06     RAW 경로가 등록부에 있는가 — 3
  V4-06b    등록부에 있는데 RAW 에 없는 유령 경로 — 48
  V4-11b    판정에 안 쓰는 미분류 경로 — 319
  V4-21     같은 이름의 공개 함수가 두 모듈에 없음 — 7
  V3-39     이론가와 실제 중앙값의 차가 상한 안 — 최대 33.2%
  V3-47     축별 차종 간 결측률 편차가 상한 안 — 3축
  V3-03     축별 source 값 종류 >= 2 — 1건 (예: taste.fitting)
  V3-04     축별 값 종류 >= 2 — 2건 (예: state.consumable, taste.fitting)
  V3-91     가이드 검산 일곱 줄이 표대로 나옴 — 7줄 다름
  V3-96     value IS NULL 과 source 모름 건수 차 — source 모름 33989 · 차 +32,797

■ 환경 차이 — 결함이 아니다
  V1-05     실행 시작 시각 기준이라 DB 만으로는 재현되지 않는다
  V4-01     HMAC 키가 다르면 plate_hash 가 전건 불일치한다

■ 미실행 — 이번 실행에서 그 단계를 안 돌았다
  V1-20     이번 실행에 카탈로그를 안 받았다
  V4-29     판정을 막는 미분류가 없다 — 잴 것이 없다
  V4-30     판정을 막는 것이 없다
  V3-68     부록 F 에 축 목록이 없다
  V3-64     규격에서 경계를 못 읽었다
  V9-09     비율이 같은 매물 짝이 없다 — 잴 것이 없다
  V10-24    사전 확정 이력이 없다
  V11-121   두 사이트에 같이 올라온 차가 없다 — 나란히 낼 것이 없다

★ fatal 넷은 다 「원문(raw)」쪽이다 — ★ 6장 마지막에 적은 것과 같은 뿌리다.
  ★ V2-01(ok 원문 수 == CORE 행 수 — 5744)이 ★ 여태 그것을 말하고 있었다.
```

---

## 6. 못 한 것 · ★ 여쭐 것

### ★ 여쭐 것 ① — 관리의 「탐색」이 시안에 없다

```
시안 절 넷   운영 · 조정 · 계정 · 내려온 것 둘
지금 문      운영 5 · 조정 5 · ★ 탐색 5 · 계정 1 · 내려온 것 2
  탐색 — 조회 쿼리 · API 조회 · 관리 도구 · 문서 뷰어 · 개발 요청
```

★ 지금은 **운영 안에 한 덩이(h4)로** 넣어 두었다.  문은 하나도 안 지웠다.

```
여쭙는다   ⓐ 지금대로 운영 안에 둘까
          ⓑ 절을 다섯으로 (시안에 「탐색」을 더해 주실까)
          ⓒ 다른 절로 옮길까
★ 문을 지우는 안은 넣지 않았다 (개정 427 「화면을 지우지 않는다」)
```

### ★ 여쭐 것 ② — 매물 카드에도 비교 네모를 둘까

v4m `listings` 시안 카드에도 `v4-pick`(✓)이 그려져 있다.
그러나 마스터 확정 17 은 **「비교→관심」**이다 — 비교로 가는 문은 관심이다.

```
지금   매물 카드에는 ♡(관심에 담기)만 두었다.  네모는 관심에만 있다
여쭙는다  매물에서도 바로 비교로 갈 수 있게 할까
★ 지어 넣지 않았다
```

### ★ 여쭐 것 ③ — 축 × 사이트 갈래표가 아직 없다

추적의 「빠진 축 갈래(①~⑤)」는 `config/labels.json` `MISS_KIND` 넷뿐이다
(`state.consumable` ⑤ · `value.market` ④ · `state.integrity` ② ·
`history.not_join` ①).  나머지 축은 **갈래를 안 붙인다** — 지어내지 않는다.

★ 이것이 **남은 일 4 「멀티 매핑 — 축 × 사이트 표」**와 같은 것이다.

### ★★★ 보고 — 다른 사이트 원문이 여태 하나도 안 남고 있었다

```
SELECT site, COUNT(*) FROM raw_response GROUP BY site
  encar 147,061   ← ★ 이것뿐이다.  ★ 그 밖 사이트 0건
```

★ `tools/collect_*.py` 열 개 가운데 `save_raw` 를 부르는 것이 **하나도 없었다.**
★ 명령서 3-2 의 **필수**와 어긋난다 —
「★ `raw_response` 에는 남긴다 — 갈래를 넓히시면 다시 판다」

```
고쳤다  store/raw.py  save_site_raw() 를 새로 만들었다
        ★ 못 받은 것은 안 넣는다 · ★ 같은 자리에 이미 있으면 건너뛴다
        tools/collect_kbchachacha.py  상세를 받으면 ★ 파싱보다 먼저 원문을 남긴다
        ★ KB 수집을 멈추고 다시 걸었다 (이미 받은 상세는 건너뛴다)
남았다  ★ 나머지 아홉 도구 · ★ KB 목록 쪽(list)
        ★ 목록 쪽은 봇 차단 0건이라 다시 받을 수 있다 — 상세를 먼저 막았다
```

### 못 한 것 (그대로)

```
① KB 전량 — 도는 중 (엿새)
② 3-2 걸러 저장 — K카 526 · 리본카 1,038 · 렉서스 36
③ 3-3 되돌리기 · 미분류 다시 세기
④ 멀티 매핑 (45-2) · 견주기 (45-3)
⑤ 나머지 아홉 수집 도구에 save_site_raw
```
