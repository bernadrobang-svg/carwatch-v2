# 407 배너와 `S46-88` · 렉서스 보증은 ★ 우리가 안 받는다 · 전기 세대 전수

```
버전     소스 v263 · 지시서 SPEC-2026.08.26-r764
때       2026-08-27 12:00
지시     ① 407 이 이틀 넘으면 현황 맨 위에 「엔카가 막혀 있습니다 …」 · S46-88
         ② GV70 은 전기만 · ELECTRIFIED·EV 든 세대 코드를 짚어 줘라
            3253 을 안 넣은 까닭을 적어라 · 「5,955 → 몇 건」
         ③ 네 몫 셋 — STEP 149j · 렉서스 보증 두 축 · fill_raw_run_id 화면
앞 작업  4cd7ddf
```

---

## 1. ★★★ ① 407 배너 — ★ 그런데 **더 큰 것을 먼저 잡았다**

★ 배너를 넣기 전에 ★ 「왜 지금은 안 뜨나」를 재 봤다.

```
sqlite> SELECT MAX(fetched_at) FROM raw_response WHERE endpoint='list' AND status='ok';
   ★ 2026-08-25T04:28:36        ← ★ 오늘이다
sqlite> … AND site='encar';
   ★ 2026-08-24T07:54:37        ← ★ 어제다
```

★★★ **`list_at` 이 사이트를 안 가리고 있었다.**
★ 08-26 에 원문 보관을 켜면서 ★ 렉서스 목록 원문(04:28)이 들어왔고,
★ ★ 그것이 ★ **엔카 목록을 신선한 것처럼 보이게** 했다.
★ ★ 그래서 ★ 「엔카 목록이 N일째 갱신되지 않았습니다」 배너가 ★ **안 떴다.**

```
고쳤다  store/core.py  state_counts()  list_at 에 ★ `AND site='encar'`
       ★ 배너 글이 「엔카 목록」이다 — ★ 재는 것도 엔카여야 한다
★ ★ 이것도 ★ 내가 원문 보관을 켜며 낸 것이다 (V4 죽은 것 · run_id 빈 것에 이어 셋째다)
```

### ★ 그 위에 407 배너를 얹었다

```
web/app.py  _encar_blocked(at407, list_at)
  ★ 407 을 마지막으로 받은 때가 ★ 목록을 마지막으로 받은 때보다 **뒤**여야 한다
    → ★ 그 뒤로 목록이 들어왔으면 ★ 마스터가 눌러 주신 것이다.  ★ 안 낸다
  ★ 며칠은 ★ **목록이 마지막으로 들어온 때**부터 센다 —
    ★ 「막힌 지 며칠」이 아니라 ★ 「목록이 며칠째 없나」가 ★ 사람이 겪는 것이다
  ★ 문턱 ★ config/web.json `encar_blocked_days` = 2 — ★ 코드에 안 박는다

web/app.py  _banner()  ★ 「갱신되지 않았습니다」보다 ★ **먼저** 낸다
  "엔카가 막혀 있습니다 — N일째입니다"
  "관리 ▸ 수집에서 「브라우저 수집」을 눌러 주십시오"   ★ 무엇을 하면 되는지
  href=/admin/collect

store/core.py  encar_407_at — ★ 마지막 407 (audit_request · site=encar · kind=list)
```

★ `407` 은 ★ **고장이 아니다** — ★ 규격이다 (`ENCAR_API.md:48` 「서울 IP 407 · 브라우저 수집」).
★ ★ 우회하지 않았다 (금지 13).

### ★ `S46-88` — ★ 일부러 깨서 봤다 (㉲)

```
판단 함수를 불러 네 자리를 잰다 (글자가 코드에 있는 것과 뜨는 것은 다르다)
  ① 문턱(2일)을 넘으면 → 뜬다        ② 문턱 안이면 → 안 뜬다
  ③ 막힌 뒤 목록이 들어왔으면 → 안 뜬다  ④ 407 이 없으면 → 안 뜬다
  ⑤ 배너 글에 「엔카가 막혀 있습니다」·「브라우저 수집」·「/admin/collect」가 있는가

★ 일부러 깼다
  ① config 에서 문턱을 지우니 → (False, 'config/web.json 에 encar_blocked_days 가 없다')
  ② 배너 글을 바꾸니       → (False, '★ … 배너 글에 「엔카가 막혀 있습니다」가 없다')
  ③ 되돌리니             → (True,  '2.0일 넘으면 뜨고 · 안이면 안 뜨고 · …')
```

```
★ 지금은 안 뜬다 — ★ 엔카 목록이 마지막으로 들어온 것이 ★ 08-24T07:54 (하루 반).
  ★ 문턱이 이틀이라 ★ 아직이다.  ★ 이틀이 지나면 뜬다
```

---

## 2. ② 전기 세대 — ★ **전수로 짚었다.  둘뿐이다**

★ 우리 17묶음의 세대 이름을 다 훑어 ★ `ELECTRIFIED · EV · 전기` 가 든 것을 골랐다.

| 차종 | `carCode` | 세대 이름 | 연식 | 지금 |
|---|--:|---|---|---|
| G80 (189/2684) | **3235** | ELECTRIFIED G80 | 2022~현재 | ★ 이미 넣었다 (`G80_EV`) |
| GV70 (189/2771) | **3253** | ELECTRIFIED GV70 | 2022~현재 | ★ **안 넣었다** |

```
★ 나머지 열다섯 묶음에는 ★ 이름에 전기 표시가 든 세대가 ★ **없다**
   (G70 5 · GV60 1 · 그랑콜레오스 1 · 스포티지 11 · 그랜저 15 · Model Y 1 ·
    X3 4 · GLC 2 · Q5 2 · NX 2 · RX 3 · XC60 2 · S60 3 · XC40 2 · V60 3)
★★ ★ 하나만 짚어 둔다 — ★ XC40 의 ★ `3244 XC40 Recharge`(2022~).
   ★ 「Recharge」는 ★ 볼보의 전기 이름이다 — ★ 이름에 EV 가 없어 이 훑기에 안 걸린다.
   ★ ★ 다만 ★ `XC40_IMPORT` 는 ★ 전기 차종이 아니라 ★ 지금은 상관이 없다.  ★ 적어만 둔다
★ GV60 은 ★ 전기 전용이고 ★ 세대가 하나(3236)뿐이라 ★ carCode 를 안 넣는다 (지시대로)
```

### ★★ 3253 을 안 넣은 까닭

```
★ 08-26 에 ★ 세대를 넣을 때 ★ 가이드께서 ★ 「나머지 차종의 세대 코드는
  ★ 재서 ★ **표로만** 줘라.  ★ 내가 짝짓는다」 하셨다.
★ ★ 그리고 ★ 「★ 지어 넣지 마라.  ★ 그랜저를 그렇게 한 것이 옳다」 하셨다
★ ★ GV70 은 ★ 마스터께서 짝지어 주신 셋(그랜저·스포티지·G80)에 ★ 없었다 —
  ★ ★ 그래서 ★ **표에만 적고 안 넣었다** (08-26 기록 3장 · 08-27 기록 2장).
  ★ ★ 「GV70_EV 는 전기인데 내연 728건을 함께 받는다」고 ★ 두 회차 적었다
★ ★ G80_EV 에 3235 를 넣은 것은 ★ 마스터께서 ★ 「G80_25T·G80_EV → RG3」로
  ★ ★ **그 차종을 짚어 주셨기** 때문이다.  ★ GV70 은 짚어 주신 적이 없다
```

### ★ 「5,955 → 몇 건」

```
★ 아직 안 넣었다 — ★ 「내가 넣는다」 하셨다.  ★ 넣으신 뒤 다시 재서 적는다
★ 지금 잰 조각으로 셈하면 —
   5,955 − 752(GV70 차종 단위) + 23(3253 ELECTRIFIED GV70) = ★ **5,226건**
   ★ 상세 받는 날짜 2.1일 → ★ **1.8일**
★ ★ 이것은 ★ 셈한 값이다.  ★ 넣으신 뒤 ★ `--narrow --dry` 로 ★ 다시 잰다
```

---

## 3. ③-2 ★★ 렉서스 보증 두 축 — ★ **규격이 옳다.  우리가 안 받는다**

★ 실제로 상세를 한 건 불러 봤다.

```
GET https://certified.lexus.co.kr/api/json/getData_car_detail.json.php?idx=6351
→ car_detail.car_info.warranty = ★ 「2030년 10월까지 (120,000km)」
                          fuel = 「하이브리드」 · number_plate · displacement ·
                          transmission · color · innerColor · registration_date …
```

★ 규격이 적은 그대로다.

```
docs/MULTISITE_MAPPING.md:823
  | ★ **렉서스 인증** | ★ **없다** | 〃 · ★ `car_info.warranty` 가 만료일＋상한km 를 준다 |
```

★★ 그런데 **우리는 렉서스 상세를 한 건도 안 받는다.**

```
tools/collect_lexus.py   목록만 받는다 — ★ 모든 행이 `detail_status='not_requested'`
parse/lexus/             ★ **디렉터리 자체가 없다**
목록 항목의 칸 15개       idx · model_name · class_name · price · release_price ·
                       year · mileage · branch · color · sell_type …  ★ warranty 없음
```

```
★★★ 곧 — ★ **규격이 틀린 것이 아니다.  ★ 우리가 안 받는 것이다**
★ ★ 축 × 사이트 표의 ★ 「렉서스 warranty 0%」는 ★ 그 뜻이다
★ ★ 볼보셀렉트·BMW 의 ★ 「거의 전 축 0%」도 ★ 같은 까닭이다 (상세를 안 받는다)
```

```
★ 고치지 않았다 — ★ 규격이 「개발측이 채운다」로 적어 둔 ★ **멀티 매핑(항목 4)**의 일이다.
  ★ ★ 그리고 ★ 이 보증이 ★ `warranty.site` 인지 ★ `warranty.power` 인지는
    ★ ★ **배점 몫이라 마스터가 정하신다** (원칙 1-a · 규칙 2)
여쭙는다  ★ `car_info.warranty` 를 ★ 어느 축에 넣을까
         ★ 「2030년 10월까지 (120,000km)」 — ★ 만료일과 상한km 를 함께 준다
```

---

## 4. ③-3 `fill_raw_run_id.py` 화면 — ★ 올렸다

```
config/cli_screens.json
  pending 에서 빼고 → screens 에 ★ `tools/fill_raw_run_id.py`: `/admin/tools`
  ★ 원문 표를 손보는 도구다 — ★ `migrate` · `export` · `report` 와 같은 자리다
  screens 26 · pending 29
```

---

## 5. ③-1 STEP 149j ↔ `v4m_admin_시안` — ★ 내 쪽은 다 했다

```
★ 화면    시안의 절 넷이다 (운영 · 조정 · 계정 · 내려온 것 둘)
★ 문      탐색 다섯을 ★ 운영 표에 이어 낸다 — ★ 하나도 안 지웠다
★ 잠금    STEP 149j 의 목적은 지켜진다 — ★ 조정만 잠긴다 (b-ops 302행)
★ 라우팅   3분류(운영·조정·탐색) 그대로 — ★ `V11-24` 가 그것을 본다.  ★ 통과다
★ 시험    `test_spec_ui` F-1 을 시안의 넷으로 · ★ F-1b 로 문 다섯이 사는지 잰다
```

```
★★ 남은 것은 ★ `docs/chapters/61-web.md` STEP 149j 의 ★ **글**이다 —
   「★ 운영 · 조정 · **탐색** 셋 … ★ 금지 — ★ 4번째 분류를 만드는 것」
★ ★ 그것은 ★ `docs/` 다.  ★ 내가 안 고친다 (규칙 2 · 금지 8)
★ ★ 코드·화면·시험 쪽에서 ★ 내가 할 수 있는 것은 ★ 다 했다
```

---

## 6. 결과

```
python3.11 tools/run_tests.py
  test_admin         통과
  test_admin_flow    통과
  test_collect       통과
  test_crosssite     통과
  test_dict          통과
  test_endtoend      통과
  test_fixtures      통과
  test_integration   통과
  test_invariants    통과
  test_pipeline      통과
  test_registry      통과
  test_report        통과
  test_run           통과
  test_score         통과
  test_screens       통과
  test_spec_ui       통과
  test_store         통과
  test_watch         통과
  test_web           통과
  ruff               통과  (F821,F811,B905,F841,DTZ005,F401)

  임시 DB 91개 치움 (검사가 디스크를 채우지 않는다)

결과: 통과
```

```
python3.11 tools/check_all.py
DB carwatch.db · run 20260825T054753
통과 385 · fatal 5 · warn 12 · 환경 차이 2 · 미실행 9

■ FATAL — 고쳐야 한다
  V1-08     동일 코드 실패율 100% 인 엔드포인트 없음 — 1건 (예: list: 18건 전량 실패)
  V1-19     이번 실행이 저장한 원문에 run_id 가 있음 — 1
  V1-21     받아 두고 안 펼쳐진 원문이 없음 — 182
  V1-23     필요한 조합 대비 받은 카탈로그 비율 — 127조합 · 78.9% · 카탈로그 없는 매물 633건 · 사이트가 안 줌 empty 21조합 · 매물 586건 (예: 8603350202607
  V2-01     ok 원문 수 == CORE 행 수 — 5951

■ warn — 진행은 된다
  S46-24    facet 미확인 차종이 없는가 — ★ facet 미확인 47종 — G80_25T.bobaedream · G80_25T.kbchachacha · G80_EV.bobaedream · G80
  V1-07     매물별 엔드포인트 4종 상태 존재 — 4242
  V4-06     RAW 경로가 등록부에 있는가 — 3
  V4-06b    등록부에 있는데 RAW 에 없는 유령 경로 — 48
  V4-11b    판정에 안 쓰는 미분류 경로 — 319
  V4-21     같은 이름의 공개 함수가 두 모듈에 없음 — 7
  V3-39     이론가와 실제 중앙값의 차가 상한 안 — 최대 33.2%
  V3-47     축별 차종 간 결측률 편차가 상한 안 — 3축
  V3-03     축별 source 값 종류 >= 2 — 1건 (예: taste.fitting)
  V3-04     축별 값 종류 >= 2 — 2건 (예: state.consumable, taste.fitting)
  V3-91     가이드 검산 일곱 줄이 표대로 나옴 — 7줄 다름
  V3-96     value IS NULL 과 source 모름 건수 차 — source 모름 33989 · 차 +32,797

■ 환경 차이 — 결함이 아니다
  V1-05     실행 시작 시각 기준이라 DB 만으로는 재현되지 않는다
  V4-01     HMAC 키가 다르면 plate_hash 가 전건 불일치한다

■ 미실행 — 이번 실행에서 그 단계를 안 돌았다
  V1-14     이번 실행에 S5 를 안 돌았다
  V1-20     이번 실행에 카탈로그를 안 받았다
  V4-29     판정을 막는 미분류가 없다 — 잴 것이 없다
  V4-30     판정을 막는 것이 없다
  V3-68     부록 F 에 축 목록이 없다
  V3-64     규격에서 경계를 못 읽었다
  V9-09     비율이 같은 매물 짝이 없다 — 잴 것이 없다
  V10-24    사전 확정 이력이 없다
  V11-121   두 사이트에 같이 올라온 차가 없다 — 나란히 낼 것이 없다

★ `S46-36` 이 사라졌다 — ★ 가이드께서 두 줄을 고치셨다.  ★ fatal 6 → 5

★★ `V1-19` 가 다시 떴다 — ★ 새 결함이 아니다.
   ★ **지금 도는 KB 수집이 ★ `proc_run_id()` 를 넣기 전에 뜬 것**이라
     ★ ★ 옛 코드를 메모리에 들고 ★ `run_id` 를 비운 채 넣고 있다 (60건).
   ★ `tools/fill_raw_run_id.py` 로 채웠다 — ★ 그 도구가 바로 이 자리를 위한 것이다
   ★★ ★ GV70 3253 을 넣으실 때 ★ 수집을 다시 걸면 ★ 새 코드로 돌아 ★ 안 생긴다
```

---

## 7. 남은 것

```
1 KB 상세 — 도는 중 · 2 걸러 저장 끝 · 3 세대 표 드렸다 (전기 둘 짚었다)
4 멀티 매핑 — ★ 렉서스 상세를 받는 것이 ★ 여기 일이다 (3장)
5 견주기 · 6 화면 v4m 여덟 장 끝 · 7 검사 여덟 (S46-79~86)
★ 여쭐 것
   ① 렉서스 `car_info.warranty` 를 어느 축에 넣을까
   ② GV70 3253 을 넣으신 뒤 ★ 다시 재서 적는다
   ③ STEP 149j 의 글 (규칙 2)
★ 앞 회차에서 넘어온 것 — 세대 판단 둘 (3436 · 3235)
```
