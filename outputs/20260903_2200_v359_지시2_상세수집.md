# v359 — 가이드 지시 ② · S5 상세를 끝까지 받는다

```
② S5 상세를 끝까지 받는다
   KB 9.7% · 기아 CPO 0% · 현대인증 19.6% · K카 41.9%
   ★ **파싱이 아니라 상세가 병목**
끝났다는 뜻   ★ 사이트별 상세율 **90%**
```

## ★ 뿌리 — ★ **두 겹의 「닭이 먼저냐」 고리**

### ① `active` 여야 상세를 받는데 ★ 상세를 받아야 `active` 다

```python
# collect/runner.py:728  (S5 가 고르는 줄)
"SELECT listing_id, source_id FROM core_listing WHERE status='active'"
```

```
실측 09-03 — ★ `status='new'` 인 매물이 ★ **5,900건**
   차차차 5,050 · 리본카 342 · BMW 185 · 보배 144 · 헤이딜러 121 · 리볼트 58
★ 목록으로 매물번호만 넣은 것이 `new` 다 — ★ 상세를 받아야 `active` 가 된다
★ ★ 그런데 `active` 만 골라 ★ **영영 못 받았다**
```

### ② 차종을 알아야 상세를 받는데 ★ 상세를 받아야 차종을 안다

```python
# collect/runner.py:_scope
f" AND {col} IN ({','.join('?' * len(scope))})"      ← ★ 차종으로 거른다
```

```
실측 09-03 — ★ `new` 이면서 차종이 빈 매물 ★ **787건**
   리본카 341 · BMW 171 · 헤이딜러 114 · 보배 98 · 차차차 53
★ 차종은 ★ 상세를 풀어야 정해진다 (`fill_target_key`)
★ ★ 기아 CPO 는 ★ `active` 가 **0건** · 차종도 **전부 NULL** → ★ 상세율 **0%**
```

★ **모르는 것(NULL)은 통과시킨다** — ★ 받아 보고 나서 가른다.
★ 우리 차종이 아니면 ★ `upsert_core` 가 ★ `out_of_scope` 로 적는다.
★ ★ 「전량을 받는다」가 아니다 — ★ 목록이 이미 우리 질의로 좁혀졌다.

## ★ 곁들여 — 판이 엔카 아닌 곳에서 죽던 자리 셋

```
① `interval_sec` 이 ★ 꼴이 다르다 — 엔카는 `[lo,hi]` · 나머지는 수 하나
   → TypeError: cannot unpack non-iterable float object
② `LISTING_ENDPOINTS` 가 ★ **엔카의 아홉 창구**로 박혀 있다 —
   기아 CPO 는 `detail` 하나다  → ValueError: zip() argument 2 is shorter
③ `endpoint_schema()` 의 첫 창구가 `list` 라 ★ `list_status` 칸을 찾다 죽는다
   → sqlite3.OperationalError: no such column: list_status
```

★ 셋 다 ★ **엔카 전용 가정**이었다. ★ 어댑터가 주는 만큼만 돌게 고쳤다.

## 조치 후 결과

```
$ python3.11 run.py collect --site kia_cpo --only S5
단계        expected requested   ok empty n/f error
S5               72         8    8     0   0     0

기아 CPO 상세율  ★ **8/8 = 100.0%**   (앞 0/8 = 0%)
```

| | 앞 | 뒤 |
|---|--:|--:|
| **기아 CPO 상세율** | 0% | **100%** |
| S5 가 고르는 매물 | `active` 만 | **`active` ＋ `new`** |
| 차종 모르는 매물 | 안 받는다 | **받아 보고 가른다** |
| 판이 도는 사이트 | 6 | **12** (v358) |

## 남은 것

```
★ KB 6.3% · BMW 11.9% 는 ★ 아직이다 —
  ★ ★ KB 는 ★ 오늘 몫이 말랐다 (v355 · reCAPTCHA 실측)
  ★ ★ BMW 는 ★ 지시 ③ (파서) 자리다
★ 나머지 사이트는 ★ 상세 원문이 매물보다 많다 (앞서 받아 둔 것이 있다)
```
