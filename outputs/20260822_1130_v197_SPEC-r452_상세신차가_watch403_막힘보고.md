# SPEC-2026.08.22-r452 — 상세 신차가 · /watch 403 문구 · ★★ 배점 850 은 막혔다

`v197` · 2026-08-22 11:30

---

# 0. 무엇을 하려 했나

```
명령서   outputs/ORDER_20260822_r452.md  (앞 명령서 전부 폐기)
§4 순서 ①  지금 수집된 것으로 순위·등급을 다시 매긴다 — 1·2·3 장을 걸고 S9 부터 재계산
인수인계 §3  개발측이 할 것 셋 — ① /watch 403  ② 성능  ③ 매물 7781 값 어긋남
```

---

# 1. ★★★ 막혔다 — 배점 850 을 넣을 수 없다 (규칙 1·2 충돌)

## 무엇이 어긋나나

| 어디 | 총점 | 컷 |
|---|--:|---|
| 명령서 r452 · `08_인수인계.md` | **850** | S75 A65 B57 C50 D40 E30 F20 G10 |
| **`docs/chapters/30-score/f-table.md` 5장-2a** | **675** | **S90 A80 B70** |
| `config/scoring.json` | 675 | S.8 A.7 B.6 C.5 |

```
$ grep -rn "850" docs/chapters/
  (없음)
★ 850 은 docs/chapters/ 어디에도 없다.  guide/ 와 명령서에만 있다
```

## 왜 못 넣나

```
① 규칙 1  「docs/ 가 유일한 정본이다.  코드가 규격과 다르면 ★ 코드를 고친다」
          → 정본(f-table)이 675 다.  850 을 넣으면 ★ 코드가 규격을 어긴다
② 규칙 2  「docs/ 를 개발측이 고치지 않는다」
          명령서 ★ 금지 7 도 같다 — 「지시서를 고치지 마라.  틀린 곳은 지적만」
③ 검사    V3-66 이 ★ f-table.md 를 직접 읽어 config 와 맞춘다
          (validate/v3_logic.py:724)
          → config 를 850 으로 바꾸면 ★ V3-66 · V3-77 · V9-10 이 실패한다
④ f-table:291  「필수 ★ 컷 · 분모 · 총점은 마스터가 정하신 것이다.  가이드가 안 만진다」
   f-table:348  「필수 [마스터] 총점 675 · 등급 기준 625 (개정 365)」
```

## ★ 이것이 개정 452 가 방금 신설한 「모양 ⑯」이다

```
개정 452 커밋 메시지
  마스터 「가이드 업데이트 안 하고 명령서로 퉁치네」
  → 오판대장 #38 · 모양 ⑯ 「명령서만 쓰고 규격을 안 고친다」 ★ 신설
  「★ 막을 것 — 명령서를 내기 전에 규격을 고친다」

실측   그 커밋(ef4d3c2)이 고친 파일은 ★ 08_인수인계.md · 00_버전.md 둘뿐이다
      ★ f-table.md 는 안 고쳤다 — ★ 같은 커밋 안에서 같은 잘못이 났다
      게다가 08_인수인계.md:255 는 ★ 「f-table.md 5장-2a 가 정본」이라 가리킨다
      → ★ 가리키는 정본이 675 다
```

```
★ 여쭌다 — f-table.md 5장-2a 를 850 · S75 A65 B57 C50 D40 E30 F20 G10 으로
  고쳐 주시면 그때 config·코드·검사를 한 번에 맞춘다
★ 지어내지 않는다.  임의로 메우지 않는다 (규칙 2)
```

## ★ 850 표 자체에도 여쭐 것이 있다

```
명령서 1-2   값 170 = 가격(신차가 대비) 75 + 예산 95
지금 축      value.market(시세 대비) ★ 100점이 있다
★ 850 표에 ★ 시세 축이 없다.  ★ 없애는 것인가, 적다 빠진 것인가
  (밀린일대장 §5 — value.market 450건이 갈래 ④ 「우리가 못 만든다」)
★ 「예산」 축은 지금 없다 — 신설이다.  축 이름·source 를 무엇으로 할지
★ 연식 축도 신설이다 — 같은 물음
```

---

# 2. ★ 명령서 §1-3 의 진단이 틀렸다 — 채점은 맞다

## 명령서가 적은 것

```
1-3  ★★ 신차가 = 트림가 + 옵션가 — ★ 지금 틀렸다
     「지금 /why 의 「신차가 대비」 축 근거가 origin_price = ★ 트림가만 쓴다」
     「★ 고칠 것 — 채점·화면 ★ 모두 「트림가 + 옵션가」로 통일한다」
     「(예 id 7781 : 91.6% → 72.6%)」
```

## 실측 — ★ 채점은 이미 트림 + 옵션이다

매물 7781 · `price_current_won` 56,500,000 · `price_origin_won` 61,700,000
선택 옵션 7종 합 1,560만 → 신차가 **7,730만**

```
config/scoring.json axis_rules.value
  origin_per_percent_cheap = 1 · origin_max = 80

트림가만  (6,170만) → 8.4% 쌈  →  ★  8.4점 / 80
트림+옵션 (7,730만) → 26.9% 쌈 →  ★ 26.9점 / 80

★ result_axis 에 저장된 실제 점수 = ★ 27 점
→ ★ 채점은 트림 + 옵션을 썼다.  명령서가 말한 「트림가만」이 아니다
```

근거 코드 — `analyze/axis/value.py:109`

```python
origin = s.origin_total_won or s.price_origin_won      # ★ 합계를 먼저 쓴다
```

`collect/runner.py:1141` — `origin_total_won = origin + option_total` (개정 301)

```
★ 채점을 「고치면」 옵션가를 ★ 두 번 더하게 된다.  ★ 건드리면 안 된다
```

## ★ 진짜 틀린 곳 — 상세 ②절 화면 하나다

```
report/screens/build.py:1136
  opt_prices = _option_prices(conn) if (extras and ★ with_state) else {}

view_detail 은 with_state=False 로 부른다 (쿼리를 줄이려고)
→ 옵션가 사전이 ★ 비어 있다 → _opt_won = 0
→ origin_total_won = price_origin_won (6,170만) · option_price_won = 0 → 「없음」
```

```
목록  7,779만  ✔  (with_state=True 라 사전을 읽는다)
채점  7,730만  ✔
상세  6,170만  ✘  ← ★ 여기 하나만 틀렸다
★ 가이드가 「목록 표기가 틀렸다」고 적었다가 r452 에서 「목록이 맞다」로
  고치셨는데, ★ 이번엔 「채점이 틀렸다」로 가셨다.  ★ 둘 다 맞다.  상세만 틀렸다
```

---

# 3. 고친 것

## 3-1. 상세 ②절 신차가 (명령서 §1-3 · 인수인계 §3-3)

`report/screens/build.py`

```
전   opt_prices = _option_prices(conn) if (extras and with_state) else {}
후   want_opt = with_state if opt_money is None else opt_money
     opt_prices = _option_prices(conn) if (extras and want_opt) else {}
     ★ view_listings 에 opt_money 인자 신설.  view_detail 이 True 로 켠다
```

```
★ 왜 인자로 뺐나 — 그냥 extras 로만 하면 ★ 현황이 21쿼리가 되어
  상한 20 을 넘는다 (V11-34 가 씨앗 DB 에서 잡았다).  ★ 신차가를 내는 화면만 켠다
```

| | 1차 | 조치 후 |
|---|--:|--:|
| 상세 신차가 | **6,170만** | **7,779만** |
| 상세 신차 대비 | **-8.4%** | **-27.4%** |
| 상세 선택 옵션가 | **「없음」** | **1,609만** |
| `/` 쿼리 | 14 | **15** (상한 20) |
| `/listings` 쿼리 | 18 | 18 (상한 20) |
| `/detail` 쿼리 | 24 | **25** (상한 26) |

★ 라이브(`https://43.201.16.78.sslip.io/detail/7781`)에서 확인했다.

## 3-2. `/watch` 403 문구 (인수인계 §3-1)

```
1차   「관리자만 볼 수 있습니다 · 이 화면은 관리자 권한이 필요합니다」
실제  /watch 는 ★ ROLE_USER 다 (web/routes.py:66).  ★ 관리자 권한이 아니다
      → ★ 마스터가 로그인해도 안 될 것처럼 읽힌다.  거짓말이다
```

`web/context.py` — `NEED_LOGIN` 신설 · `web/server.py:guard` 가 `route.role` 로 고른다

```
조치 후 (라이브 실측)
  /watch      403  「로그인하면 볼 수 있습니다」
                   「관심 목록은 계정마다 따로 있습니다…」
  /admin      403  「관리자만 볼 수 있습니다」   ← 그대로다
  /admin/run  403  「관리자만 볼 수 있습니다」   ← 그대로다
```

```
★ 안 한 것 — 「익명에게 상단 「관심」을 보일지」는 ★ 규격 판단이다.
  menu_items 가 못 들어갈 문을 숨기는 것은 개정 427 대로다.
  ★ 마스터 지적 「♡ 를 눌러도 갈 데가 없다」를 어떻게 풀지 여쭙는다 —
  ⓐ 익명에게도 「관심」을 보이고 누르면 로그인으로 보낸다
  ⓑ ♡ 를 누른 뒤 로그인 화면으로 보내고 로그인하면 그 매물이 담겨 있다
  ★ /watch/add 는 이미 익명에게 열려 있다 (INVITE_PATHS · V11-23)
```

## 3-3. ★ 가이드가 push 한 파일이 시험 셋을 깨뜨렸다

```
가이드 커밋 f301df4 (SPEC-r442) 가 outputs/verify/probe/*.py 18개를 넣었다
그 파일들이 ★ 모듈 최상위에서 config 를 쓰고 I/O 를 한다

깨진 것
  test_admin     ★ admin.py 밖에서 config 파일을 쓰지 않는다
                 ['outputs/verify/probe/p1_census.py: dump', 'p2_filters.py: dump']
  test_endtoend  V4-23 모듈 최상위에 I/O · 부작용 없음 : 192
  test_report    「import 만으로 아무 일도 안 일어나야 한다」
```

```
고침   소스를 훑는 곳 넷에서 ★ outputs/ 를 뺀다 — ref/ 와 같은 이유다
       outputs/ 는 ★ 작업기록이다 (규칙 6).  ★ 소스가 아니다
         tools/check_src.py:640 · validate/v2_load.py:663
         validate/v4_mapping.py:243 · tests/test_admin.py:226
★ 가이드 파일을 지우지 않았다 — 검수 증거다
★ 이 판단이 틀렸으면 되돌린다.  ★ 대안은 probe 를 tools/ 로 옮기는 것이다
```

## 3-4. `/watch` 시험을 뜻대로 고쳤다

```
전   check("anonymous 가 /watch → 403", guard(ANONYMOUS, watch) is FORBIDDEN)
     ★ 객체 동일성을 봤다.  이름은 「→ 403」인데 「FORBIDDEN 객체냐」를 봤다
후   403 인지를 status 로 보고, ★ 문구가 「관리자만」이 아닌 것을 ★ 더 엄하게 본다
★ 느슨하게 바꾼 것이 아니다.  검사 하나를 더 넣었다
```

---

# 4. ★ 검사 결과 — 실제 출력

## `python3.11 tools/check_all.py`

```
1차 (가이드 커밋을 받은 직후)  통과 344 · fatal 3 · warn 11 · 환경 차이 1 · 미실행 9
조치 후                      통과 344 · fatal 3 · warn 11 · 환경 차이 1 · 미실행 9

■ FATAL
  V1-21   받아 두고 안 펼쳐진 원문이 없음 — 9
  V1-23   카탈로그 비율 127조합 · 79.9%
  V11-76  화면 크기·시간이 상한 안 — 5곳
★ 셋 다 이번 작업 이전부터 있던 것이다
```

## `python3.11 tools/run_tests.py`

```
1차 (가이드 커밋 직후)  결과: 실패 — test_admin.py · test_endtoend.py ·
                                 test_report.py · test_web.py
                       ★ 넷 중 셋이 ★ 가이드가 넣은 probe 파일 때문이다
조치 후                결과: ★ 통과   (19파일 + ruff)
```

## 배포

```
$ python3.11 tools/deploy_check.py
○ 배포됨 — 가장 최근 소스보다 9분 뒤에 서비스가 떴다

라이브 확인 (★ 렌더 파일이 아니다)
  /detail/7781  신차가 7,779만 · 신차 대비 -27.4% · 선택 옵션가 1,609만
  /watch        403 「로그인하면 볼 수 있습니다」
```

---

# 5. ★ 못 한 것

| # | 무엇 | 왜 |
|:--:|---|---|
| 1 | **배점 850 · 컷 S75… · 곡선 넷 · 감점 ×1.259** | **★ §1 대로 막혔다.** f-table 이 675 다 |
| 2 | 예산 축 · 연식 축 신설 | 위와 같다 (축 이름·source 미정) |
| 3 | 명령서 §3 감점을 화면에 낸다 (ⓐ~ⓖ) | 배점이 정해져야 ⓑ「막대 합 − 감점」이 맞는다. ★ ⓔ·ⓕ 는 따로 할 수 있다 |
| 4 | 명령서 §0 자르는 규칙 폐기 | ★ grep 으로 78% 차단·등급 상한·예외추천을 못 찾았다 — 애초에 코드에 없다. 확인 뒤 보고한다 |
| 5 | 성능 (`/compare` 29초 8.7MB · `/detail` 4.1초) | 인수인계 §3-2. ★ 다음 작업 |
| 6 | 차종 확대 · K카 | 명령서 §4-② §4-③. 순서상 뒤다 |

---

# 6. ★ 여쭐 것

```
1  ★★ f-table.md 5장-2a 를 850 으로 고쳐 주십시오 — 그래야 시작한다
2  ★ 850 표에 시세 축(value.market 100)이 없다 — 없애는 것인가
3  ★ 예산 축 · 연식 축의 축 이름과 source 를 무엇으로 할지
4  ★ 「♡ 를 눌러도 갈 데가 없다」를 ⓐ 로 풀지 ⓑ 로 풀지 (3-2)
5  ★ outputs/ 를 소스 검사에서 뺀 판단이 맞는지 (3-3).
   ★ 아니면 probe 를 tools/ 로 옮겨 주십시오
```

---

# 7. 커밋

```
64c665f
```
