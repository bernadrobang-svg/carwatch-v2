# A — 탭 3 을 GV70 후보 화면으로 · K — 전국 배달 · 「없다」가 아니라 안내문이었다

지시   `outputs/ORDER_20260829.md` · `SPEC-2026.09.06-r1188` ＋ 가이드 실측 09-06
버전   v387 · `calc_version = c1`

---

## 0. ★ 먼저 — 가이드 지적을 받았습니다. 맞습니다

> ★ 가이드 — 「★ **「없다」가 아니라 팔린 차를 눌렀습니다**.
>   ★ `success` 가 `true` 로 오기 때문에 ★ 성공한 줄 알고 ★ 「데이터가 없다」고 본 것입니다.
>   ★ **「못 찾았다」와 「없다」는 다릅니다**」

★ **열두 사이트를 다 재어 확인했습니다** (`tools/thin_detail_report.py` 신설).

| 사이트 | 상세 | 참 상세 | **안내문(작다)** | 그중 팔린 차 | 평균 |
|---|--:|--:|--:|--:|--:|
| **kcar** | 569 | 431 | **138** | **138** | 55,079B |
| **heydealer** | 510 | 425 | **85** | 0 | 26,783B |
| **encar** | 18,451 | 17,984 | **467** | 0 | 9,065B |
| **kbchachacha** | 768 | 739 | **29** | 0 | 242,085B |
| 나머지 여덟 | — | 전건 | **0** | 0 | — |

★ 가이드 실측 그대로입니다 — ★ **K카 569건 중 138건이 팔린 차 안내문**이고 ·
★ 그 138건이 ★ **전부** 「판매완료」 글자를 담고 있습니다.

★ 잣대를 세웠습니다 — ★ **크기로 가른다.**  ★ 기준은 사이트마다 다르므로
★ `config/endpoints.json` 의 `min_detail_bytes` 가 정본입니다 (없으면 6,000B).

---

## 1. A-18 ~ A-21 — 사이트 진단 · **있었습니다**

가이드께서 짚어 주신 경로가 실제로 옵니다 (`EC61313925` · 80,271B):

```
data.warrantyCount                ★ 5
data.masterInfo.inspNm            ★ 모빌리티365 수원센터 고도균
data.masterInfo.pfmncStatCkqtYn   ★ Y
data.masterInfo.pfmncCkqtDt       ★ 20260819
```

`parse/kcar/mapping.py` 가 `data.master` 만 보고 `data.masterInfo` 를 안 봤습니다.

| | 전 | 후 |
|---|--:|--:|
| **K카 `site_inspection`** | **0/604** | **348/604** |
| 파서가 뽑는 수 (파일 기준) | 0 | **431/569** |

★ 「K카 진단」으로만 적습니다. ★ 「K카 홈서비스」와 가르는 값이 원문에 없어
★ **짐작으로 붙이지 않았습니다** (지시 「짐작으로 확정하지 마라」).

### 엔카 — `site_inspection` · `delivery_nationwide`

```
advertisement.diagnosisCar  True/False  → 「엔카진단」
view.encarDiagnosis         -1/0/1/2    → 등급이 셋인데 이름은 둘이다 (여쭐 것 2)
advertisement.homeService   True/False  → ★ 전국 배달
```

| | 전 | 후 |
|---|--:|--:|
| 엔카 `site_inspection` | 0 | **7,084 / 16,693** |
| 엔카 `delivery_nationwide` | 0 | **Y 5,913 · N 3,382** (미조회 7,398) |

`tools/refill_encar_detail.py` 로 ★ **다시 받지 않고** 파일 18,451개를 다시 읽었습니다.

---

## 2. K — 전국 배달 값 만들기

| # | 무엇 | 상태 |
|:--:|---|:--:|
| K-1 | `core_listing.delivery_nationwide` (Y/N/미조회) | **끝** |
| K-2 | 상세 원문에서 뽑는다 | **엔카 끝** · 나머지 남음 |
| K-3 | 매핑표 등록 | 남음 |
| K-4 | 못 찾으면 「미조회」. N 으로 두지 않는다 | **끝** |

★ 판매글의 「전국 탁송」 **글자는 안 씁니다** — 60건 중 15건에 나오는데
★ 그것은 **딸린 말**이지 값이 아닙니다 (v1 사고 · 금지 12).
★ 구조화된 값 `advertisement.homeService` 만 씁니다.

★ 탭 2 의 **배달 고르개**를 살렸습니다 (전국 배달 · 배달 없음 · 배달 미조회).
★ 지난 회차에 「못 낸다」고 적은 것이 ★ 이번에 풀렸습니다.

---

## 3. A-14 ~ A-25 — 탭 3 = GV70 후보

★ 가이드께서 **시안만** 고치시고 `web/templates/analyze.html` 은 09-05 판이었습니다.
★ 지시 A-14 가 「★ 탭 3 을 GV70 후보 화면으로 **만든다**」이므로 ★ 시안대로 틀을 지었습니다.

| # | 무엇 | 상태 |
|:--:|---|:--:|
| A-14 | 탭 3 = GV70 후보 | **끝** |
| A-15·A-16 | 사이트 · 판매 지역·대리점·배달 | **끝** |
| A-17·A-24 | 조건마다 `O`·`X`·**「미조회」** 여덟 | **끝** |
| A-22 | `matched` · `near` 두 묶음 | **끝** |
| A-23 | 아쉬운 까닭 `miss[]` (「＋0.5만km」·「진단 미조회」) | **끝** |
| A-25 | 아래 묶음에도 좋은 점 한 줄 | **끝** |

조건은 `config/web.json` `tab3_candidate` 가 정본입니다 — 코드에 안 박았습니다.

### 실측 — 조건 깔때기

```
다섯 사이트 GV70 (리스·렌트 뺀)        1,430
  ＋값 3,600만 이하                     163
  ＋2022년식 이상                       163
  ＋8만km 미만                           89
  ＋흰색                                 29
  ＋보증 남음                          ★  0
```

★ **조건에 다 맞는 차 0대**입니다. ★ 까닭이 분명합니다 —
★ 그 흰색 96대가 ★ **`detail_status` 가 하나도 없습니다** (미조회 92 · error 3 · not_found 1).
★ 상세를 한 번도 못 받아 ★ 보증·보험·진단이 전부 「미조회」입니다.
★ **차가 없는 것이 아니라 자료가 없는 것**입니다 — 화면이 그렇게 적습니다.

### 「하나가 아쉬운 차」

```
1,430대 중 30대를 냅니다 — ★ 아쉬운 것이 적은 차부터
```

★ 지우지 않습니다. ★ 전체 수를 화면에 그대로 적습니다.
★ 다 그리면 **2.69MB** 라 못 씁니다 (실측) — ★ 60KB 로 줄였습니다.
★ 자른 수는 `config/web.json` `near_max` 입니다.

### ★ 등급으로 자르지 않았습니다

지시대로 군을 나누지 않고 값 낮은 순 그대로 두었습니다.
대신 `thin_data`·`thin_why` 로 까닭을 냅니다 — 「신차가 미조회 · 보험 이력 미조회」.

---

## 4. A-6 ~ A-13 — 탭 2 에 빠진 값

마스터 지시 — 「목록과 추천 1 의 정보는 추천 2·3 에 다 들어가야 한다」

| 값 | 전 | 후 |
|---|---|---|
| `site_badge` | 없음 | 「엔카」 |
| `encar_url` | 없음 | 원문 매물 링크 |
| `accident_tone` | 없음 | `clean`·`mid`·`bad`·**`none`(안 칠한다)** |
| `clean_car` | 없음 | 무사고 또는 보험 100만 이하 |
| `insurance_cost` | 없음 | 「2,751만」 · 없으면 「미조회」 |
| `thin_data`·`thin_why` | 없음 | `confirmed < 700` ＋ 까닭 |
| `region_kind` | 지역만 | 전국 배달이면 `deliver` |

★ `none`(미조회)은 ★ **띠를 안 칠합니다** — 모르는 것을 좋게도 나쁘게도 칠하지 않습니다.

---

## 5. 화면을 브라우저로 재었습니다 — 네 너비

`networkidle` 뒤에 쟀습니다. 캡처 12장 `outputs/shots/`.

| 화면 | 390 | 600 | 760 | 900 |
|---|--:|--:|--:|--:|
| **탭 3** 가려진 글자 | **0** | **0** | **0** | **0** |
| **탭 3** 상자 겹침 | **0** | **0** | **0** | **0** |
| 탭 2 가려진 글자 | 0 | 0 | 0 | 0 |
| 탭 2 상자 겹침 | 2 | 12 | 12 | 12 |
| 탭 4 가려진 글자 | **2** | 0 | 0 | 0 |

★ 탭 2 겹침 — 「값순/연식순」과 「쪽번호」가 겹칩니다. **CSS 라 가이드 몫**입니다.
★ 탭 4 390px 가려진 2 — 격자 넷째 칸(「10만 초과」)이 잘립니다.
  ★ 표를 가로로 굴리려면 CSS 가 있어야 합니다 — **가이드께 올립니다**.

---

## 6. 두 도구 · 검사

```
python3.11 tools/run_tests.py
test_admin 통과 · test_admin_flow 통과 · test_collect 통과 · test_crosssite 통과
test_dict 통과 · test_endtoend 실패 · test_fixtures 통과 · test_integration 통과
test_invariants 통과 · test_pipeline 통과 · test_registry 통과 · test_report 통과
test_run 통과 · test_score 통과 · test_screens 통과 · test_spec_ui 통과
test_store 통과 · test_watch 통과 · test_web 통과
ruff 통과  (F821,F811,B905,F841,DTZ005,F401)
결과: 실패 — test_endtoend.py

가이드 검사 201개 중 붉은 것 17개
```

`test_endtoend` 의 fatal 넷 — ★ 넷 다 이번 작업이 만든 것이 아닙니다.

```
V4-28   관리 사전 화면의 문구 다섯   web/templates/ — 가이드 몫
V11-12  라우팅 표 ↔ 10·13장         규격 표 — 가이드 몫
V3-77   갈래 합 ↔ 갈래 표기          ★ 규격이 스스로 어긋난다 (v384 7-2)
V11-34  /detail 이 35 쿼리 (상한 28)  ★ v385 전에도 35 였다
```

### ★ 내 몫 114개 중 몇 개가 붉은가

가이드 답 — 「`docs/CHECKS.md` 하단 담당별 목록. **개발 59 ＋ 배포·값 55**」.
그 표대로 갈랐습니다.

| 갈래 | 수 | 코드 |
|---|--:|---|
| ★ **내가 고칠 수 있다** | **6** | `S46-288`(옛 도구 파일) · `S46-32`(생성물) · `S46-246`(analyze_list 시안 없음) · `S46-207`(커밋 제목) · `S46-24`(facet) · `S46-271`(탭4 390px) |
| 자료·시간이 필요하다 | 5 | `S46-276` `S46-279` `S46-280` `S46-247` `S46-261` |
| 가이드 몫 | 5 | `S46-284`(지시문 표) · `S46-152`(회차 읽기) · `S46-184`(규격 208행) · `S46-98` · `S46-100` |
| 사람이 봐야 한다 | 1 | `S46-92` |

★ `S46-32` 는 이 회차에서 다시 닫았습니다 (`build_index`).
★ `S46-246` 은 제가 만든 `analyze_list.html` 에 시안이 없어서입니다 —
  ★ 그 틀은 **가이드가 09-05 에 지으신 것**을 이름만 옮긴 것입니다 (여쭐 것 3).

---

## 7. 여쭐 것

### 1  ★ 탭 3 「조건에 다 맞는 차」가 0 인 것 — 자료가 없어서입니다

흰색·3,600만·2022↑·8만km 미만 GV70 **96대**의 상세를 ★ 한 번도 못 받았습니다.
★ 마스터 회선으로 그 96대만 받아 주시면 ★ 바로 채워집니다 (0.1초 × 96 = 10초).
★ 서버로는 ★ 33% · 30초라 ★ 한 시간이 걸립니다.

### 2  엔카 진단 이름이 **둘인데 자리는 셋**입니다

```
view.encarDiagnosis   -1 (없음) · 0 · 1 · 2
_사이트_진단.encar    「엔카진단」 · 「엔카진단 플러스」   ← 둘
```

어느 자리가 「플러스」인지 원문이 말하지 않습니다. **짐작으로 안 붙였습니다** —
지금은 셋 다 「엔카진단」입니다. 자리와 이름을 짝지어 주십시오.

### 3  `analyze_list.html` 에 시안이 없습니다 (`S46-246`)

탭 3 이 GV70 후보가 되면서 ★ **「맡긴 차 목록」이 갈 곳을 잃었습니다**.
저는 `/analyze` 길에 그 화면을 남겼습니다 (가이드가 09-05 에 지으신 틀 그대로).
① 그 화면을 그대로 두고 시안 이름만 이어 주실지 ② 아주 없앨지 정해 주십시오.

### 4  K카 「홈서비스」·엔카 「플러스」 — 나머지 세 사이트

KB·리본카·헤이딜러의 진단 표기는 아직 안 찾았습니다.
K카처럼 **살아 있는 차**로 상세를 열어 확인하겠습니다 — 다음 회차입니다.

### 5  탭 2 상자 겹침 12 · 탭 4 390px 가려진 2 — CSS

`.rc2-sorts` 와 `.rc2-pager` 가 겹치고, `.v3-g` 표가 390px 를 넘습니다.
`web/static/app.css` 는 가이드 몫이라 손대지 않았습니다.

## 8. 이견

없습니다. ★ 「크기로 가른다」는 제가 먼저 세웠어야 할 잣대였습니다.
