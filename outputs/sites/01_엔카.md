# 엔카 — 항목 실측

`https://car.encar.com/list/car` · 실측 2026-08-21 · 개발측
원문 `outputs/sites/shot/01_엔카_목록.html` (1,664바이트)

---

```
★★ 막힘 — 자동 접속을 막는다

받은 것   1,664바이트.  <head> 의 meta·script 뿐이고 **본문 글자가 0줄**이다
실측      HTTP 200 인데 화면이 없다 — 사람이 아닌 접속을 가려낸다

이 프로젝트 기록에도 같은 것이 남아 있다
  · 서버에서 엔카 목록(list)은 **407** 이 돌아온다
  · 그래서 목록은 마스터가 브라우저로 받아 넣는다 (/admin/collect)
  · 개정 388 이 그 때문에 자동 수집을 13:00 으로 옮겼다

① 검색 필터   막힘
② 목록 한 행   막힘
③ 매물 상세   막힘

★ 짐작으로 채우지 않는다
★ 지시 §6-3 「막힌 곳은 모바일 웹 · 앱 화면 캡처로 대신한다」 —
  캡처 도구가 없어 그것도 못 한다.  **마스터가 찍어 주셔야 합니다** (S41)
```

```
★ 우리가 이미 아는 것 (원문 기준 — 화면이 아니다)
  이 프로젝트가 엔카 원문을 137,000건 넘게 갖고 있다.
  다만 지시가 「금지 — API 를 뒤져 필드명을 적는 것.  화면에 보이는 것만이다」
  라고 했으므로 **여기 적지 않는다**
```
