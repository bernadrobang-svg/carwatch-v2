# v284 — 설정 캐시.  `/detail` 8.64초 → 1.27초

```
지시   마스터 08-28 「가끔 서버가 부하를 못 견뎌서 죽는다.  막을 방법 없나」
```

## 1. 무엇을 하려 했나

「부하로 죽는다」의 실체를 재고, 막는다.

## 2. 먼저 잰 것 — ★ 서버는 죽지 않았다

```
sudo journalctl -u carwatch --since "-14 days" | grep "Main process exited|killed"
→ 없음.  14일간 종료는 전부 "Deactivated successfully" — 내가 낸 재시작이다
```

★ 프로세스는 안 죽는다.  **응답이 멎는 것**이었다.

| 잰 것 | 값 |
|---|---|
| 장비 | t4g.small · vCPU 2 · 램 1,841MB · 스왑 2GB |
| DB | `carwatch.db` **1.3GB** (raw_response 186,547행 · 삭제 금지) |
| 재부팅 | 08-23 15:15 · 08-26 08:29 · 08-28 18:29 (13일에 3회) |
| 08-21 21:53 | ★ **전역 OOM** — `Free swap = 0kB`.  램 1.8G + 스왑 2G 를 다 썼다 |
| 08-23 마지막 20분 | ★ IMDS(169.254.169.254)조차 20~90초 타임아웃 — 장비 전체가 굶었다 |

★ OOM 이 죽인 것은 `carwatch` 가 아니라 `claude`(547MB+스왑 652MB)였다.
   ★ 그래서 `carwatch` 로그에 흔적이 없었다.
★ 그때 웹 서버(python3.11)는 **RSS 2.7MB · 스왑 598MB** — ★ 통째로 스왑에 밀려 있었다.
   ★ 「죽은」 것이 아니라 **밀려나 못 움직인** 것이다.

### 부하는 크지 않았다 — 한 장이 비쌌다

```
caddy 접속 로그 30분 (2026-08-28 18:1x~18:4x)
  요청 68건 = 2.3건/분          ← 부하가 아니다
  응답 중앙 3.87초 · 90% 8.65초 · 최대 16.30초
  68건 중 55건이 3초 초과
  59건이 /detail/* — 크롤러 두 대 (34.24.201.81 · 35.237.153.65 · curl/8.5.0)
  마스터(SamsungBrowser)는 4건뿐
```

★ `config/web.json` 의 규격 상한은 `screen_max_sec = 2.0` 다.

## 3. 뿌리 — `/detail` 한 장이 설정 파일을 27,336번 연다

`cProfile` — `/detail/1839` 10.8초 중 **6.9초가 `json.load`** (43,353회 파싱).
DB 쿼리는 1ms 대였다.  ★ 느린 것은 쿼리가 아니었다.

| 설정 파일 | 크기 | 한 장에 여는 횟수 |
|---|--:|--:|
| `scoring.json` | 31KB | **11,795회** |
| `unknown_split.json` | 7.5KB | 7,761회 |
| `web.json` | 26KB | 6,206회 |
| `sites.json` | 19KB | 1,552회 |
| **합** | | **27,336회 ≈ 614MB** |

★ `_row`(1,546회) · `_pen_axes`(7,730회) 같은 **행마다 도는 함수 안에서**
   `open()` + `json.load()` 를 한다.  ★ 20KB 짜리 한 장에 614MB 를 읽었다.

## 4. 무엇을 했나

`report/screens/build.py` — **mtime 을 보는 설정 캐시** 하나를 두고 20자리를 옮겼다.

```
새로 넣은 것   load_config(path)          build.py:29~52
              _JSON_CACHE                파일이 그대로면 지난 것을 준다
              ★ mtime·크기가 바뀌면 다시 읽는다
                → 관리 화면이 config 를 고쳐도 다음 요청이 새 값을 받는다.
                  ★ 서버를 껐다 켤 필요가 없다
```

옮긴 자리 (20) — `_site_badge` · `_labels` · `_unknown_cfg` · `_total_points` ·
`_pen_axes` · `_view_cfg` · `_site_detail_urls` · `_view_str` · `_lease_kinds` ·
`_option_*`(2) · `fuel_groups` · `view_listings` · `_dep_cfg` · `_group_caps` ·
`_view_list` · `_view_dict` · `_soh_low` · `_view_int` · `_high_km`

```
★ 안전 — 돌려준 것을 고치는 자리가 없음을 전 사용처에서 확인했다 (8곳).
   ★ 안쪽을 통째로 내주는 _view_list · _view_dict 만 베껴서 낸다
★ 치환 도중 가드가 진짜 버그를 잡았다 — load_config 본문이 치환 대상 패턴과
   똑같아, 그대로 뒀으면 ★ 로더가 자기를 부르는 무한 재귀가 될 뻔했다.
   본문 이름을 f/got → fp/data 로 바꿔 피했다
```

## 5. 1차 결과 (고치기 전)

```
check_all    통과 398 · fatal 9 · warn 12 · 환경 차이 1 · 미실행 8
  FATAL      S44-5 S46-32 S46-36 V1-21 V1-23 V2-01 V11-12 V11-159 V11-45
run_tests    실패 — test_collect, test_endtoend, test_run  (CLAUDE.md 기준값과 같다)

/detail/1839   8.64초 · 설정 열기 27,336회
```

## 6. 조치 후 결과

```
check_all    통과 397 · fatal 10 · warn 12 · 환경 차이 1 · 미실행 8
  ★ 는 것 하나 — V11-76 (아래 7항에 적는다)
run_tests    실패 — test_collect, test_endtoend, test_run  (그대로.  늘지 않았다)
             ruff  ★ 통과 (F821,F811,B905,F841,DTZ005,F401)
             ★ 한때 F401 2건이 났다 — 안 쓰게 된 지역 import json 이 남아서다.  치웠다
```

| 화면 | 전 | 후 | 설정 열기 |
|---|--:|--:|--:|
| `/detail/1839` | 8.64초 | ★ **1.27초** | 27,336 → **38** |
| `/listings` | 0.71초 | 0.60초 | → 47 |
| `/` | 0.80초 | 0.66초 | → 44 |
| `/why/1839` | — | 1.87초 | |

★ 전 화면이 규격 상한 `screen_max_sec = 2.0` 안에 들어왔다 (비로그인 기준).

## 7. 못 한 것 · 판단이 필요한 것

### ★★★ `/` 가 11초인 진짜 까닭 — 가이드 진단과 다르다

가이드 지시 (90장 1순위):
> `report/screens/build.py:2060` 조회 ②가 18,762건을 통째로 읽는다.
> NTILE 로 옮기거나 판정 때 한 번 세어 표에 넣어라

★ **재 봤더니 그렇지 않다.**

```
① 등급 세기 (GROUP BY)     40.7ms ·   157행
② 값 전건  (ORDER BY)      47.1ms · 9,543행   ← 18,762 이 아니라 9,543행
   _quartiles_by_target     3ms   ·    19차종
   합쳐서 0.1초.  ★ 11초의 원인이 아니다
```

★ 진짜 자리를 짚었다 — `web/app.py:467`

```
conn = _sq.connect(db_path)      ← ★ 요청마다 새로 연다
try:  ...  finally: conn.close() ← ★ 요청 끝에 버린다
```

```
실측 (관리자 계정 · V11-76 과 같은 조건)
  같은 연결 1회차   12.45초   ← 26쿼리가 10.57초 (한 쿼리 0.41초)
  같은 연결 2회차    0.83초   ← 26쿼리가  0.63초
  ★ 새 프로세스 1회차 10.72초  ← OS 디스크 캐시가 아니다.  연결마다 비싸다
```

★ 뿌리 — **DB 1.3GB 가 램 1.8GB 에 안 들어간다.**
   ★ SQLite 는 연결마다 페이지 캐시를 새로 갖는다 (기본 2MB).
   ★ 요청마다 연결을 버리니 **늘 빈 캐시로 시작해 EBS 에서 다시 읽는다.**
   ★ 매물이 늘수록 나빠진다 — 가이드가 본 11.7초 → 21.0초가 이것이다.

```
★ 고칠 안 (개발측 의견) — 마스터 판단을 청한다
  ⓐ 연결을 재사용한다 (작은 풀).  ★ 실측이 12.45초 → 0.83초를 보증한다
  ⓑ PRAGMA mmap_size 를 준다 — 연결마다 복사하지 않고 OS 캐시를 본다
  ★ 파이프라인이 같은 프로세스에서 쓰기를 하므로 ⓐ 는 잠금을 같이 봐야 한다
    (db_busy_timeout_ms · db_batch_commit_rows 가 이미 그 버팀목이다)
★ 이번 커밋에 안 넣었다 — 작업 하나에 커밋 하나 (규칙 8)
```

### V11-76 이 fatal 로 늘었다

```
V11-76  화면 크기·시간이 상한 안 — 4곳
  / — 41KB · 11.23초                 ← 위의 연결 문제다
  /admin/registry — 54KB · 18.91초   ←   〃
  /why/{listing_id} — 30KB · 2.16초  ← 상한 2.0 바로 위.  흔들린다
  /admin/query — 17KB · 2.46초
```

★ 이 검사는 **관리자 계정**으로 렌더해 시간을 잰다 (`validate/v11_web.py:4402`).
★ 내 수정이 만든 것이 아니라 **드러낸** 것이다 — 기준 실행에서는 통과였는데,
   ★ 그때 크롤러 두 대가 장비를 때리고 있어 검사가 흔들린 것으로 보인다.
   ★ 시간 기반 검사라 장비 부하에 따라 갈린다.  ★ 이 점을 가이드에 보고한다.

### 장비 쪽 — 마스터만 하실 수 있는 것

```
★ `carwatch.service` 에 메모리 상한이 없다 (MemoryMax=infinity).
  → 내 도구(check_all·run_tests)나 claude 가 장비를 통째로 굶기면
    마스터가 보는 화면이 함께 밀려난다.  08-21 이 정확히 그것이었다
★ 밀린일 94·95·96 (감시·SSM·탄력적 IP)이 그대로다 — SERVER_SURVIVAL 3장
★ t4g.small(램 2GB)에 1.3GB DB 는 좁다.  램을 늘리는 것이 가장 큰 지렛대다
```

