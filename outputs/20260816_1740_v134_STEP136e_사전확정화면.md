# `STEP 136e` — `/admin/dict` 사전 확정 화면 · S4 가 7,750건을 펼쳤다

작업 시각 2026-08-16 17:10~17:40 · 소스 v134 · 실행 `python3.11`
커밋 `10ddc9d`(개정 266) · `<이번 커밋>`(개정 267)

---

## 1. 무엇을 하려 했나 (지시 원문)

> ```
> 1) /admin/dict 를 만들어라 (STEP 136e)
>    ① 확정 대기 목록 ② 값마다 확정·보류·폐기 ③ 축 단위 묶음 확정
>    ④ 「확정하면 매물 N건이 판정 가능해집니다」
>    사유 필수 · 미리보기 필수 · 'list' 출처 표시 필수 · V10-24 · V10-25
> 2) 그 화면으로 41종을 실제로 확정해라 — 네가 눌러 보고 숫자로
> 3) 그 다음 한 차종만 S5 → S6 → S9 → S10 끝까지
> 4) 파싱 뒤에 나오는 것을 숫자로
> ```

## 2. 무엇을 했나

### ① `/admin/dict` (지시 1)

| 파일 | 무엇 |
|---|---|
| `store/adminops.py` `DICT_AXIS_COLUMN` | 축 → `core_listing` 칸. 「몇 건이 이 값을 갖나」를 세려면 필요하다 |
| `store/adminops.py` `pending_enums` | 축·값·관측수·출처·처음 본 시각·매물수 |
| `store/adminops.py` `pending_axis_summary` | 축 단위 묶음 (41종을 하나씩 안 누르게) |
| `store/adminops.py` `apply_dict_decision` | 확정·보류·폐기. 사유 필수. 이력을 `config_change` 에 남긴다 |
| `report/screens/admin.py` `dict_state` · MENU | 화면 데이터 · 「조정」 메뉴 |
| `web/routes.py` · `web/views.py` | `/admin/dict` GET·POST · `admin_dict` |
| `web/templates/admin_dict.html` | 화면 |
| `validate/v10_admin.py` | `V10-24`(사유가 남는가) · `V10-25`(list 출처를 말하는가) |

확정 이력은 `/admin/audit` 의 「설정 변경」 탭에서 보인다 — 별도 표를 만들지 않았다.

### ② S4 가 밖에서 온 봉투를 못 펼치던 것

`collect/runner.py` 의 S4 는 「이번 실행 봉투만」 펼친다 (STEP 50a).
그 판별을 **시각**으로 하는데, 브라우저 수집분은 파이프라인 밖에서 먼저 저장되므로
시각으로는 절대 안 걸린다.

```
목록 392쪽(7,750건)을 받아 두고도 S4 가 0건을 펼쳤다 (실측)
```

`origin='browser'` 는 시각과 무관하게 포함하도록 고쳤다. 다시 펼쳐도 `upsert` 라 결과가 같다.

## 3. 왜 그렇게 했나 (규격)

- 개정 267 `STEP 136e` — 화면 4요소 · 사유 필수 · 미리보기 필수 · `list` 출처 표시 ·
  **「금지 확정을 자동으로 하는 것」** · 검산 `V10-24`·`V10-25`
- 4장 STEP 45 — 확정 전에는 그 축을 쓰는 판정이 돌지 않는다
- STEP 50a — 「옛 수집분을 매번 훑지 않는다」가 뜻이지 「밖에서 온 것을 버린다」가 아니다

## 4. 1차 결과 (작업 전 숫자)

```
사전       color_ext 23 · color_int 10 · fuel 5 · sell_type 3  — 전부 pending/list
확정 화면   없음 (confirm_enum 함수만 있고 부르는 화면이 없었다)
precheck S4  (False, '분류에 쓰는 사전에 pending 5건')
core_listing 206건
등록부 미분류 48
```

## 5. 조치 후 결과

### 화면으로 실제로 눌렀다 (지시 2)

```
GET /admin/dict                     200 · 「확정 대기 41종」 · 「전체 집합이 아닙니다」 표시
POST 사유 없이 확정                   400        ← 관문이 막는다
POST fuel / color_ext / color_int / sell_type  각 303
```

```
사전 (확정 후)
  color_ext  confirmed / list  23
  color_int  confirmed / list  10
  fuel       confirmed / list   5
  sell_type  confirmed / list   3
  trim       confirmed / list  68
  accident_type · panel_rank · panel_status  confirmed / bootstrap

확정 이력 (config_change · file='dict_enum')
  fuel[5] · color_ext[23] · color_int[10] · sell_type[3]
  전부 사유 있음 · account_id 2
```

**41종 전부 확정. `precheck S4` 막힘 → 통과.**

### S4 를 돌렸다

```
단계        expected requested   ok empty n/f error 거부  상태
S4             7750      7750 7750     0   0     0    0  정상
```

### ★ 지시 4 — 파싱 뒤에 나오는 숫자

| 항목 | 값 |
|---|--:|
| `core_listing` | 206 → **7,661건** |
| 판정 대상(`active`) | **3,473건** |
| 범위 밖(`out_of_scope`) | **4,188건** |
| **매핑 실패** | **0건** (`core_parse_issue` 0 · 버려진 필드 0) |
| **사전 미분류(pending)** | **0종** |
| **등록부 미분류** | **48종** |

차종별:

```
G80_25T 1,715 · MODEL_Y 522 · GRANDEUR_LPG 242 · G70_20T 240
KOLEOS_HEV 238 · SPORTAGE_LPI 177 · GV60 116 · G70_25T 92
GV70_EV 71 · G80_EV 60 · (차종 미정 4,188)
```

분류 출처:

```
list_fuel_badge / provisional  7,631   ← 목록의 fuel·badge 로 1단 분류
import / confirmed                30
```

**`out_of_scope` 4,188건이 절반이다.** 목록 쿼리가 `ModelGroup` 단위라 그 안에
우리가 안 보는 트림·연료가 함께 오기 때문이다. 이것이 v1 이 못 본 자리다 —
「받았는데 판정 대상이 아닌 것」이 얼마인지 숫자로 남는다.

## 6. 못 한 것 · 막힌 것 · 판단이 필요한 것

### ① 지시 3(한 차종 S5~S10)은 지금 돌고 있다

```
python3.11 run.py collect --from S5 --target KOLEOS_HEV
S5  ...  /952
```

`KOLEOS_HEV` 238건 × 4종 = 952 요청이다. 끝나면 그 숫자와 등급 분포를 보고한다.

### ② `S4` 의 봉투 범위를 고친 것은 규격 확인이 필요하다

`origin='browser'` 를 시각과 무관하게 포함시켰다. STEP 50a 의 뜻을
「옛 수집분을 매번 훑지 않는다」로 읽었고, 밖에서 받아 온 것은 그 대상이 아니라고 보았다.
**다르게 읽어야 하면 알려 주십시오.**

### ③ facet 은 여전히 0건이다

`V3-38` 이 「facet 을 아직 못 받았다」로 대기 중이다. 지금 사전은 전부 `source='list'` 라
**「전체 집합이 아니다」** 상태다. `/admin/collect` 의 「facet 만」으로 받아 주시면
목록 관측분과 대조해 「목록에만 있는 값」을 낸다.

### ④ 남은 것

`V10-22`·`V10-23`(큐 A/B 결정 대기) · `V11-45`(`STEP 136d`) · 등록부 미분류 48종 ·
시험 3파일 · `STEP53-S1` 102행(과거 데이터)
