# ★★★ 엔카 화면 필드 — **우리가 이미 다 받아 두었습니다**

작업 시각 2026-08-17 10:00~10:30 · 소스 v134 · 실행 `python3.11`
직전 커밋 `dbf09f4` · 이번 커밋 `<이번 커밋>`

---

## 1. 무엇을 하려 했나 (지시 원문)

```
STATUS_20260816 §6
1  엔카 화면 필드를 어디서 받는지          ★ 이것이 나머지를 바꿉니다
   보증 · 옵션가 · 틴팅 · 신차대비
★ 1번 없이 2~4 를 하면 또 뒤집힙니다
★ 엔카가 이미 주는 값을 우리가 계산하고 있을 수 있습니다

FOUND_encar_detail_fields §3
2  우리가 받은 4종에 이것들이 있습니까
3  없으면 어느 엔드포인트를 더 불러야 하는지 찾으십시오
```

---

## 2. ★★★ 답 — **더 부를 것이 없습니다. 저장까지 돼 있습니다**

| 마스터가 화면에서 본 것 | 어디에 | 받았나 | 저장했나 |
|---|---|:--:|:--:|
| **보증 일반/차체** | `detail.category.warranty.bodyMonth` · `bodyMileage` | **3,648/3,648 (100%)** | **`warranty_body_month` · `_km` 3,438건** |
| **보증 엔진/미션** | `…transmissionMonth` · `transmissionMileage` | **100%** | **`warranty_power_month` · `_km` 3,438건** |
| **신차가** | `detail.category.originPrice` (만원) | **3,641/3,648 (100%)** | `price_origin_won` |
| **이 차만의 옵션** | `detail.options.choice` (코드) | **2,895 (79%)** | `options_choice_json` 3,438건 |
| **옵션 이름 · 가격** | `catalog` = `options/choice` | 87종 | **`dict_model_option` 1,474행** |
| 기본 옵션 | `detail.options.standard` | 100% | `options_standard_json` |
| **틴팅** | — | **없음** | — |
| 차 키 개수 · 타이어 트레드 | — | **없음** | — |

```
★ 「우리가 안 받았다」가 아니었습니다.  받아서 저장까지 했는데 화면이 안 냅니다
```

---

## 3. ★ 지금 바로 낼 수 있는 것 — 실측

### 보증 잔여 — **개월 + km 로 나옵니다**

```
보증 값이 있는 매물 3,433건 · 잔여 계산 3,433건
차체 보증이 아직 남은 것 2,309건 (67%)

  #3  차체 12개월 / 56,153km 남음 · 엔진·미션 36개월 / 96,153km 남음
  #4  차체 16개월 / 59,748km 남음 · 엔진·미션 40개월 / 99,748km 남음
  #6  차체 19개월 / 38,514km 남음 · 엔진·미션 43개월 / 78,514km 남음
```

**마스터가 물으신 「일반보증 0 은 얼마가 남았는지 알고 싶은데」의 답이 이미 DB 에 있습니다.**
개정 280 의 「`2027-03까지`」보다 더 정확합니다 — **개월과 km 둘 다** 있습니다.

### 옵션가 합 — **지금 계산됩니다**

```
옵션 코드 매칭 9,411회 · 못 맞춘 것 0회
옵션가 합을 낼 수 있는 매물 2,706건

  최저 8만 · 중앙 620만 · P90 1,330만 · 최고 2,515만

  #830  G80_25T  옵션 2,515만 · 표시가 4,700만
  #2796 G80_25T  옵션 2,400만 · 표시가 4,890만
```

**마스터가 「제네시스는 옵션으로 값이 결정된다」 하신 그것이 나옵니다.**
개정 292 ③ 「옵션 합 30점 — 그 차종 최대 대비 백분위」를 **바로 계산할 수 있습니다.**

### 신차가 = 트림 기본 + 옵션 (개정 288)

```
originPrice 3,641건 (100%) + 옵션가 합 2,706건 → 둘을 더하면 그 차의 신차가다
```

---

## 4. ★ 없는 것 셋 — 확인했습니다

```
틴팅          detail 3,648건에 없다
             ★ 「틴팅」이라는 낱말이 8건 나오는데 전부 딜러 광고문(contents.text)이다
             성능점검부 화면 항목인데 우리가 받는 inspection API 에는 없다
차 키 개수     없다
타이어 트레드   없다
```

**이 셋은 다른 엔드포인트가 필요합니다.**
**마스터께 — 그 화면(157호6282)에서 개발자도구 → 네트워크를 열어
`틴팅`이 담긴 응답의 주소를 알려 주시면 바로 받겠습니다.**
우리가 지금 부르는 6종(`list` `detail` `inspection` `record` `diagnosis` `catalog`)에는 없습니다.

---

## 5. ★ 덤 — `V4-20` 176건의 원인이 확정됐습니다

```
dict_option_code   327행 = 숫자 코드 165 + 문장 176
dict_model_option  1,474행 — 코드 → 이름 → 가격.  ★ 깨끗하다
```

**옵션 사전이 두 벌입니다.** 하나(`dict_model_option`)는 카탈로그에서 온 정상이고,
다른 하나(`dict_option_code`)에 딜러 광고문이 섞여 들어갔습니다.

**`dict_option_code` 를 카탈로그 기준으로 다시 세우면 176건이 사라집니다.**
네 번째로 물었던 「어느 필드가 옵션 코드인가」의 답이 이것입니다 — **`options.standard`·`options.choice` 뿐입니다.**

---

## 6. ★ 이것이 뜻하는 것 — 개정 292 를 바꿉니다

```
개정 292 ①  「신차가 대비 감가 70점 — 기준 1년 12% · 2년 22% · 3년 33%」
실제        엔카가 신차가를 주고 우리가 옵션가까지 더할 수 있다
           ★ 감가 곡선을 추정할 필요가 없다.  실제 신차가로 직접 잰다

개정 292 ③  「옵션 합 30점 — 그 차종 최대 대비 백분위」
실제        지금 계산된다 (2,706건 · 중앙 620만 · 최고 2,515만)

개정 280    「일반보증 → 2027-03까지」
실제        개월 + km 둘 다 있다.  더 정확하게 낼 수 있다
```

**어제 제가 낸 「이론가 vs 실제 중앙값」 숫자(개정 283)도 다시 봐야 합니다.**
엔카가 「신차대비 61%」를 준다면 그것과 우리 계산을 대조할 수 있습니다 —
**다만 그 값은 아직 우리 원문에서 못 찾았습니다.** `originPrice` 와 표시가로 직접 계산하면
같은 값이 나오는지 확인하겠습니다.

---

## 7. 가이드 §4 표 정정 — 셋은 이미 됐습니다

`STATUS_20260816` §4 가 `✗` 로 적은 것 중 셋은 커밋 `d4ad457` 에 들어가 있습니다.

| # | 가이드 표 | 실제 |
|:--:|:--:|---|
| ① 툴팁 | ✗ | **○** 메뉴 24개에 설명 · 전 화면 title 0 → **최소 25 · 최대 291** |
| ⑤ 딜러 | ✗ | **○** 139KB → 68KB · 50곳/쪽 15쪽 · 링크 25 → 176 · 차종 분포 |
| ⑧ 차종 드롭다운 | ✗ | **○** 제조사·모델군·모델·연료 5개 · 키 자동 생성 |

**`outputs/render/dealers.html` · `admin_targets.html` 에서 확인하실 수 있습니다.**

## 8. 왜 그렇게 했나 (규격)

- `STATUS_20260816` §6 1번 · `FOUND_encar_detail_fields` §5 「없다고 판단하기 전에」
- 규칙 11 — 색인으로 짚고 그 부분만 본다
- 0장 — 선언과 실제의 괴리 차단. **「안 받았다」는 선언이 틀렸다**

## 9. 1차 결과 (작업 전 숫자)

```
가이드 판단   「우리가 안 받았거나 파싱을 안 했다」
              「없으면 어느 엔드포인트를 더 불러야 하는지 찾으십시오」
화면          보증을 점수 0/1 로만 냄 · 옵션가 없음
```

## 10. 조치 후 결과

```
보증        원문 100% · 저장 3,438건 · 잔여 계산 가능 (차체 남은 것 2,309건 67%)
옵션가       코드 매칭 9,411/9,411 · 합 계산 가능 2,706건 (중앙 620만)
신차가       3,641건 (100%)
틴팅·키·타이어  원문에 없음 → 엔드포인트 확인 요청
V4-20       원인 확정 — 옵션 사전이 두 벌이고 한쪽에 광고문이 섞였다

check_all   통과 224 · fatal 6 (바뀐 것 없음 — 이번엔 재기만 했다)
```

## 11. 못 한 것 · 판단이 필요한 것

### ① 아직 고치지 않았습니다 — **재기만 했습니다**

지시가 「1번 없이 2~4 를 하면 또 뒤집힌다」였습니다.
**보증·옵션을 화면과 배점에 넣는 것은 답을 받은 뒤에 하겠습니다.**

### ② ★ 틴팅·차 키·타이어의 엔드포인트를 알려 주십시오

마스터 화면에 있으니 API 에도 있습니다. **개발자도구 네트워크 탭의 주소** 하나면 됩니다.

### ③ 개정 292 를 조정할지

```
「신차가 대비 감가 70점」  →  엔카 신차가 + 옵션가로 직접 잴 수 있다
「옵션 합 30점」          →  지금 계산된다
```

**감가 곡선 추정 대신 실제 신차가를 쓰면 어제 보고한 ±8% 오차가 사라집니다.**
**규격을 그렇게 바꿀지 판단을 부탁합니다.**

### ④ 지난 요청들

0장 7.1 ↔ 개정 289 충돌 · `c3` 잔재 1행 · 등급 경계 순서 · 전기차 배점 ·
색인이 낸 미구현 검사 17개 · config 부록 B 등재 24개.
