# 0e ③ 끝 · 축 천장 (r956)

## ★ 안 한 것

| 무엇 | 왜 못 했나 | 며칠째 | 누가 답해야 하나 |
|---|---|---|---|
| KB 부위별 | 사고 있는 매물을 아직 못 찾았다 (`state.accident` 가 아홉 사이트 전건 0점) | 3 | — (내가 찾는다) |
| V1-23 카탈로그 17.0% | 축 재기가 먼저였다 | 2 | — (다음 회차) |
| `out_of_scope` 3,036건 다시 받기 | 화면 밖이라 안 받았다 | 1 | ★ 마스터 — 받을까 |
| V2-01 남은 −379 | 아래 ③ 참고 — 규격이 「마지막 봉투 ok」와 「CORE ok」를 같다고 본다.  ★ 사이트가 200 으로 「없다」를 주면 둘이 갈린다 | 오늘 | ★ 마스터 |

## 회차마다 낼 세 수 (화면 기준 · `tools/three_numbers.py`)

| | 수 | 지난 회차 |
|---|--:|--:|
| ① 아홉 사이트 A 이상 | **0건** | 0 |
| ② 전기차 A 이상 | **13건** | 13 |
| ③ 같은 차 짝 (`/track` 「짝지어진 차 N대」) | **161대** | 245 |

★ ③ 이 245 → 161 로 준 까닭 — 0e 로 **378건이 gone 이 됐다.**
상세를 눌렀더니 사이트가 「없다」고 답한 것들이다.  짝 한쪽이 빠지면 짝이 아니다.

★ 세는 법도 고쳤다 — 전에 나는 「짝에 든 **매물**」을 셌다 (142).
`/track` 은 **대**를 센다.  이제 `view_track(...).total_pairs` 를 그대로 부른다
(`tools/three_numbers.py:52`).  손으로 SQL 을 짜지 않는다.

---

## 1. 0e ③ — 다시 받기 끝

### 969 → 몇

| | 건 |
|---|--:|
| 처음 (detail_status 를 비운 것) | 969 |
| 그중 화면 밖(`out_of_scope`) — 안 받았다 | 455 |
| **받은 것** | **514** (1차) + **159** (2차 · 원문 저장) |
| ★ 지금 남은 「원문 없는 산 매물」 | **0** |

### 갈래 셋

| 갈래 | 건 | 어떻게 적었나 |
|---|--:|---|
| 살아 있다 | **155** | `detail_status` 그대로 · `status='active'` |
| detail_gone | **378** | ★ 셋을 함께 — `detail_status='not_found'` · `sales_status='detail_gone'` · `status='gone'` |
| unreachable | **4** | kia_cpo — 사흘 잇달으면 문다 |

★ 마스터 정정대로 칸을 나눴다.  `detail_status` 는 CHECK 로 다섯에 묶여 있어
`not_found` 를 쓰고, 묶임 없는 `sales_status` 에 `detail_gone` 을 적었다.
DDL 을 안 고쳤다.  `tools/refetch_unsourced.py:135`

★ 덮어쓴 옛 `sales_status` (`reserved` 4 · `resvYn=Y` 6) 는
`core_listing_change` 에 `old_value` 로 남겼다 — 사실을 안 지웠다 (P3).

### 사이트별 (1차 514건)

| 사이트 | 살아 있다 | detail_gone | unreachable |
|---|--:|--:|--:|
| kbchachacha | 28 | 198 | 0 |
| kcar | 24 | 137 | 0 |
| heydealer | 85 | 4 | 0 |
| hyundai_cert | 15 | 0 | 0 |
| bobaedream | 2 | 8 | 0 |
| reborncar | 1 | 8 | 0 |
| kia_cpo | 0 | 0 | 4 |
| **합** | **155** | **355** | **4** |

★ 200 으로 안 갈랐다.  사이트마다 **파서가 값을 뽑는가**로 갈랐다
(`tools/undo_wrong_gone.py` `probe_body`).

### V2-01 이 몇으로

| | 전 | 후 |
|---|--:|--:|
| ok 원문 | 10,291 | **11,053** |
| CORE 행 | 10,901 | **10,674** |
| 차 | **+610** | **−379** |
| ★ 행이 없는 매물 | 0 | **0** |

★ **부호가 뒤집혔다.**  전에는 「원문 없이 CORE 만 있다」였다 — 근거 없는 행이었다.
지금은 「원문은 ok 인데 CORE 가 ok 가 아니다」다.  갈래는 이렇다 —

| 무엇 | 건 |
|---|--:|
| hyundai_cert — `detail_status` 가 NULL/error 인데 마지막 봉투는 ok | 198 |
| volvo_selekt — `not_requested` 인데 봉투는 ok | 149 |
| ★ kbchachacha — 봉투는 200/ok 인데 **속이 「없는 차」** | 23 |
| bobaedream·reborncar — CORE 는 ok 인데 봉투가 없다 | 3 |

★★ **마스터께 여쭙는다** — 셋째 줄이 규격의 틈이다.
`V2-01` 은 「마지막 detail 봉투가 ok」 == 「CORE 가 ok」로 잰다.
그런데 K카·KB 는 **없는 차에도 200 을 준다.**  봉투는 ok 가 맞고
(받기는 받았다) 판정은 `not_found` 가 맞다 — 둘 다 옳은데 검사가 문다.
★ 규격을 어떻게 할지는 내가 정하지 않는다 (규칙 2).

## 2. 116 이 어디 것인가

내 2,406 표의 여섯 줄 합이 2,290 이었다.  차 116 = **reborncar 60 + kcar 56**.

★ 까닭 — 내가 쿼리에 `LIMIT 6` 을 걸어 놓고 **합계는 전건을 냈다.**
자른 줄을 「없는 것」처럼 보이게 낸 것이다.  표를 자를 때는 「…밖 N줄」을 적는다.

★ 지금 어긋난 연결은 **0건**이다 (2,406 전건 끊었다).
원문 줄은 하나도 안 지웠다 — 끊긴 엔카 원문 8,365봉투가 `listing_id` NULL 로 그대로 있다.

## 3. ★ 짝이 있는데 A 가 0 인 까닭 — 어느 축이 비는가

`tools/measure_axis_gap.py`

### ③ 짝에 든 아홉 사이트 163건 — 축마다

| 축 | ★ 늘 0 | 받은 점 / 만점 | 비율 |
|---|:--:|--:|--:|
| `state.frame` (프레임) | ★ | 0 / 7,009 | **0.0%** |
| `taste.option` (옵션) | ★ | 0 / 7,009 | **0.0%** |
| `warranty.site` (사이트 보증) | ★ | 0 / 5,868 | **0.0%** |
| `state.outer` (외판) | ★ | 0 / 4,564 | **0.0%** |
| `history.use` (용도이력) | ★ | 0 / 3,586 | **0.0%** |
| `taste.hud` · `taste.sunroof` | ★ 행이 비었다 | 0 / 4,890 | **0.0%** |
| `history.not_join` (미가입) | ★ | 0 / 2,934 | **0.0%** |
| `state.consumable` · `state.leak` | ★ | 0 / 4,890 | **0.0%** |
| `state.integrity` | ★ | 0 / 1,304 | **0.0%** |
| `state.accident` (사고) | | 54 / 8,313 | **0.6%** |
| `state.my_cost` | | 29 / 4,564 | 0.6% |
| `warranty.power` · `warranty.general` | | 25 / 8,802 | 0.3% |
| `history.owner` | | 18 / 1,793 | 1.0% |
| `state.special` | | 84 / 3,423 | 2.5% |
| — 여기부터는 찬다 — | | | |
| `state.year` | | 9,528 / 13,040 | 73.1% |
| `taste.trim` | | 5,711 / 9,617 | 59.4% |
| `value.mileage` | | 7,277 / 17,441 | 41.7% |

### ④ 한 짝을 열었다 — `G70_25T` · 둘 다 3,070만 · 엔카 B(563.7) ↔ 리본카 D(424.4)

| 축 | encar | reborncar |
|---|---|---|
| `state.accident` | 51/51 `record_accident_count` | **0/51 `missing`** |
| `state.frame` | 43/43 `frame_none` | **0/43 `missing`** |
| `state.outer` | 28/28 `outer_none` | **0/28 `missing`** |
| `state.leak` | 15/15 `leak_none` | **0/15 `missing`** |
| `state.my_cost` | 28/28 `record_my_cost` | **0/28 `missing`** |
| `state.special` | 21/21 `record_special` | **0/21 `missing`** |
| `state.integrity` | 8/8 `integrity_ok` | **0/8 `missing`** |
| `history.owner` | 11/11 `owner_0` | **0/11 `missing`** |
| `history.seizing` | 8/8 `detail_seizing` | **0/8 `missing`** |
| `history.not_join` | 2/18 | **0/18 `missing`** |
| `warranty.general` | 21/22 `encar_warranty_general` | **0/22 `missing`** |
| `warranty.power` | 22/32 `encar_warranty_power` | **0/32 `missing`** |
| `warranty.site` | 18/36 `엔카진단` | **0/36 `rule_or_source_missing`** |
| `taste.option` | 34/43 `options_choice_price` | **0/43 `missing`** |
| `value.market` | 22/30 `market_median` | **0/30 `market_sample_short`** |
| `state.year` | 79/80 | 79/80 (같다) |
| `taste.trim` | 50/59 | **59/59 (리본카가 더 높다)** |
| `value.budget`·`value.mileage`·`value.origin`·`taste.color`·`taste.fitting` | | 같다 |

★ **비는 것은 값이 아니라 「근거」다.**  `source='missing'` 이다 —
아홉 사이트에는 **성능기록부·보증·옵션코드**가 없다.
값이 나쁜 것이 아니라 **볼 것이 없어서 0점**이다.

### ⑤ ★★ 천장 — A 는 65% 다.  여섯 곳은 **닿을 수가 없다**

| 사이트 | 매물 | 늘 0인 축 | 그 배점 | 천장 | A 가능 |
|---|--:|--:|--:|--:|:--:|
| encar | 9,324 | 1 | 15 | **98.4%** | 예 |
| kcar | 25 | 11 | 258 | 71.6% | 예 |
| hyundai_cert | 174 | 12 | 271 | 70.2% | 예 |
| heydealer | 92 | 16 | 377 | 58.6% | ★ 못 한다 |
| kbchachacha | 123 | 17 | 423 | 53.5% | ★ 못 한다 |
| reborncar | 63 | 18 | 431 | 52.6% | ★ 못 한다 |
| volvo_selekt | 132 | 18 | 511 | 43.8% | ★ 못 한다 |
| bobaedream | 47 | 18 | 511 | 43.8% | ★ 못 한다 |
| bmw_bps | 9 | 19 | 526 | 42.2% | ★ 못 한다 |
| lexus_certified | 7 | 21 | 636 | **30.1%** | ★ 못 한다 |

★★ **이것이 답이다.**  ① 이 0 인 것은 판정이 틀려서가 아니다.
여섯 사이트는 **만점을 다 받아도 A 선(65%)에 못 닿는다.**
kcar·hyundai_cert 는 천장이 70% 대라 한 축만 더 비어도 떨어진다.

★ 3단계(맞대기)가 무엇부터인가 — **배점 큰 순으로 이것부터다**

| 순 | 무엇을 끌어오나 | 한 대당 배점 | 어디서 |
|---|---|--:|---|
| 1 | `state.*` 성능기록부 묶음 (사고·프레임·외판·누유·소모품·자가부담·특별·완전성) | **209점** | 짝지어진 엔카 매물 · 같은 `plate_hash` |
| 2 | `taste.option` + `taste.hud` + `taste.sunroof` (옵션코드) | **73점** | 같은 트림의 카탈로그 (f-table 갈래 ③) |
| 3 | `warranty.*` (사이트·동력·일반) | **90점** | 사이트 규칙표 — 인증중고차는 이미 몫이 있다 |
| 4 | `history.*` (명의·용도·미가입) | **51점** | 엔카 성능기록부 |

1+2 만 채워도 천장이 **+282점 ≈ +31%p** 오른다 — 리본카 52.6% → 83.7%.
★ 다만 1은 **짝이 있어야** 된다.  지금 짝은 161대뿐이고 아홉 사이트 매물은 672건이다.
★ 그러니 **2(카탈로그)가 먼저다** — 짝이 없어도 트림만 알면 된다.
★ 정할 것은 마스터 몫이다.  나는 수만 낸다.

---

## 곁들여 고친 것

| 무엇 | 어디 | 왜 |
|---|---|---|
| `ruff` F401 — `import re as _re` 가 안 쓰인다 | `validate/v0_guide.py:4058` | 가이드 r953 이 넣은 줄 · `run_tests` 가 실패했다 |
| 새 도구 셋을 `pending` 에 넣었다 | `config/cli_screens.json` | V11-45 가 「CLI 로만 된다」로 물었다.  ★ 화면은 안 정했다 — 가이드 판단이다 (규칙 2) |
| 짝 세는 법 | `tools/three_numbers.py:52` | 매물이 아니라 **대**를 센다 |

## 도구 · 검사 결과

```
python3.11 tools/check_all.py
  전   통과 460 · fatal 4 · warn 13 · 환경 차이 1 · 미실행 8
  후   통과 462 · fatal 2 · warn 13 · 환경 차이 1 · 미실행 8
  남은 fatal   V1-23 (카탈로그 17.0%) · V2-01 (−379 · 위 ★)

python3.11 tools/run_tests.py
  전   결과: 실패 — test_endtoend.py, ruff
  후   결과: 통과
```

## 못 한 것 · 판단이 필요한 것

1. ★ `V2-01` 의 남은 −379 — 「200 인데 없는 차」를 규격이 어떻게 볼지 (위 1절)
2. ★ `out_of_scope` 3,036건도 다시 받을지
3. ★ 3단계 맞대기를 1(성능기록부)부터 할지 2(카탈로그)부터 할지 — 수는 위 ⑤ 에 있다
