# 0b — 일꾼이 `busy_timeout` 없이 DB 를 열었다 (S46-124)

## 안 한 것

| 무엇 | 왜 못 했나 | 며칠째 | 누가 답해야 하나 |
|---|---|--:|---|
| 0a (`pipeline.py:365` 단계 통째 트랜잭션) | **재보니 까닭이 아니다** — 한 번에 최대 19.3초 (30초 아래). 마스터께서 「30초를 안 넘으면 0a 가 까닭이 아니다 — 그때 다시 정한다」 하셨다 | 오늘 | 마스터 |
| `DB_TIMEOUT_SEC = 30` 을 손으로 박은 것 (`daily_enqueue.py:35` · `recalc_catchup.py:25`) | 값은 맞지만 정본(`config/web.json db_busy_timeout_ms`)을 안 읽는다. 이번 지시 밖이라 안 건드렸다 | 오늘 | 마스터 |

## 1 무엇을 하려 했나

ORDER_20260829 0b — 「`collect/worker.py:107` `:155` 가 `sqlite3.connect` 를 맨으로
부른다 — `open_db` 를 안 써서 `busy_timeout=30000` 이 안 붙는다 (기본 5초)」

## 2 무엇을 했나

| 파일 · 줄 | 무엇 |
|---|---|
| `store/raw.py:221` | `connect_db(path)` 를 새로 만듦 — 여는 PRAGMA 만 (DDL 없음) |
| `store/raw.py:249` | `open_db` 가 `connect_db` 를 쓰도록 고침 |
| `collect/worker.py:19` | `from store.raw import connect_db` |
| `collect/worker.py:111` · `:159` | 맨 `sqlite3.connect` → `connect_db` |
| `validate/v0_guide.py` | `s46_124_db_opened_with_pragmas` · 등록 |

`open_db` 를 그대로 쓰지 않은 까닭 — 그것은 부를 때마다 `sql/ddl/*.sql` 을 전부
다시 돌린다. `run_once` 는 폴링마다 도는 자리라 무겁다. 여는 규칙만 뗐다.

## 3 왜 그렇게 했나

`store/raw.open_db` 주석 (08-26) · `config/web.json _db_busy_timeout_why` ·
S14 (busy_timeout 의 정본은 `config/web.json`)

## 4 · 5 몇 → 몇

| | 고치기 전 | 고친 뒤 |
|---|--:|--:|
| PRAGMA 없이 여는 자리 (서비스 · 상시 쓰는 것) | **1곳** `worker.py:111` | **0곳** |
| 일꾼의 `busy_timeout` | 파이썬 기본 **0** (부딪히면 그 자리에서 죽음) | **30,000ms** |
| check_all 통과 | 410 | **411** |
| check_all fatal | 6 | 6 |
| run_tests 실패 | 1파일 `test_endtoend` | 1파일 `test_endtoend` |

검사가 사는지 되돌려 확인했다 —
`worker.py:111` 을 맨 connect 로 되돌리니 `S46-124` 가 **1곳**으로 실패했고,
되돌리니 통과했다. (검사가 늘 통과하는 죽은 검사가 아니다)

### 처음에 40곳이 나왔다 — 좁힌 까닭

맨 `sqlite3.connect` 는 온 저장소에 40곳이었다. 그런데

- `tools/` 일회성 것 (`build_index` · `inspect_*` 등) — 개발측이 손으로 돌린다. 뺐다
- `tools/daily_enqueue.py` · `recalc_catchup.py` — `timeout=30` 이 이미 붙어 있다.
  **파이썬의 `timeout=` 이 곧 `busy_timeout` 이다.** 결함이 아니다
- `tools/daily_check.py:59` — `mode=ro` 읽기 전용이다. WAL 에서 쓰기 잠금을 안 다툰다

★ **진짜 결함은 `worker.py` 하나뿐이었다.** 40곳으로 실패를 내면 그 하나가 묻힌다.

## 6 못 한 것

위 「안 한 것」 표를 보라.
