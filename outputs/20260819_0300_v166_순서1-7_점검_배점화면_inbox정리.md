# 순서 1~7 재점검 · ★ 화면이 옛 배점을 말하고 있었다 · inbox 정리

2026-08-19 03:00 · v166 · **SPEC-2026.08.18-r365**

---

## 1. 무엇을 하려 했나 (지시 원문)

```
나머지는 다한거니 ?
you must delete compketed md so we checked
```

★ **「끝났다」를 믿지 않고 하나씩 돌려서 확인했습니다.**

---

## 2. 순서 1~7 실측

### 1. 배점 675 — ○ 끝났습니다

```
config/scoring.json   total 675 · grade_base 625
                      warranty.general 20 · warranty.power 30 · warranty.site 50

검사를 지금 돌린 결과
  V3-45  통과   기대 675 · 실제 675
  V3-56  통과   기대 675 · 실제 675
  V3-57  통과   기대 625 · 실제 625
  V3-70  통과   일반·동력계를 따로 냄
  V3-71  통과   기간·거리 중 낮은 쪽
  V9-10  통과   사이트 보증 항목 합 = 만점 50

등급 분포   A 1 · B 188 · C 296 · D 2,371 · E 672
```

★ **`audit_validation` 에 남은 기록은 08-17 것이라 605·555 를 가리킵니다.**
검사를 지금 돌리면 675·625 입니다.  **기록이 낡은 것이지 값이 틀린 것이 아닙니다.**
(그래서 `docs/CHECKS.md` 의 「마지막 통과」 칸도 08-17 에 멈춰 있습니다 — 7절 ②)

### 2. 추적표 — ◐ 도구는 됐고 빈 칸이 남았습니다

```
python3.11 tools/trace_verify.py

추적표 865행 (온전한 표 509행)
빈 칸
  소스      493 / 509
  화면      392 / 509
  검사      322 / 509
  테스트    506 / 509

★ 검사가 가리키는 것 중 코드에 없는 것 17건
★★ 상태가 의심스러운 것 28건
```

### 3. 수집 넷 — **셋만 됐습니다**

| # | 무엇 | 상태 | 실측 |
|---|---|---|---|
| 1 | 카탈로그 조합 | ○ | 필요 148 중 **126** (85.1%) |
| 2 | **타이어 트레드** | **✗** | **컬럼 자체가 없습니다** |
| 3 | EV 배터리 SOH | ○ | **23**건 |
| 4 | 엔카진단 | ○ | **3,471**건 (검증 3,470) |

★ **2번이 안 됐습니다.**  `unified-report` 가 401 (로그인 필요)입니다.

```
소모품 축   state.consumable = missing · 3,528건 전건
→ 10점이 죽어 있습니다
★ K카는 이 값을 줍니다 (tirResQty).  K카 목록이 뚫리면 K카 매물만 살아납니다
```

### 4. 화면 10건 — ◐ 여덟이 됐고 ★ 하나가 틀려 있었습니다

```
4-1 낱말 갈림     ○  text-overflow:ellipsis
4-3 「—」          ○  목록 0곳 · 상세 0곳
4-4 계산식        ○  목록에서 뺐다 (title 에만 있다)
4-5 추천 사진     ○  10장
4-6 행 링크       ○  10행
4-7 관심 사진     ○  3장
4-9 차종 드롭다운  ✗  목록에 <select> 가 없다
```

### 5. 미분류 — ◐ 판정을 막는 것만 32 로 줄였습니다

```
전체 349   ④ 새로운 것 120 · ② 이름만 다름 68 · ③ 늘 비어 있음 161
V4-11 (판정을 막는 것) 100 → 32
```

★ **349 는 그대로입니다.**  「막는 것만 막는다」가 규격이라 32 만 fatal 입니다.

### 6. 검사 색인 — ◐

```
S28 검사 색인   통과 315 · 미착수 31
S16 검증 코드   통과 308 · 실패 42   ← 규격에 있는데 코드에 없는 검사
S34-3 빈 칸     통과 509
S34-2 표의 소스·검사가 실재   실패 8
```

### 7. 자동화 — ○ 돌지만 ★ 마지막 실행이 전부 실패했습니다

```
systemctl list-timers
  carwatch-daily.timer        매일 04:00   다음 08-19 04:00
  carwatch-check-light.timer  4시간마다
  carwatch-check-daily.timer  매일 23:00
  carwatch-check-weekly.timer 매주

journalctl -u carwatch-daily
  8월 18 04:00:36  하루치를 큐에 넣었습니다 — 77eb7b1d

recalc_job   08-18 04:55~04:56 에 12건 · 전부 failed
             「S5: 선행 조건 미충족: 선행 단계 미완료: S4」
```

★ **S4 를 고친 것은 그 뒤입니다.**  고친 뒤로 새 작업이 안 들어왔습니다.
**다음 04:00 (08-19) 이 첫 시험대입니다.**

---

## 3. ★★★ 그러다 찾았습니다 — 화면이 옛 배점을 말하고 있었습니다

```
web/templates/listings.html:121
  총점 605 중 등급은 취향 50점을 뺀 555 기준입니다
  (docs/ref/F-scoring.md · 24축)

web/templates/dealers.html:3
  딜러는 차량 판정에 들어가지 않습니다.  555점과 무관한 별도 축입니다
```

```
config    675 / 625        ← 개정 365 · 어제 바꿨다
화면      605 / 555        ← 안 따라왔다
문서 경로  docs/ref/F-scoring.md   ← 개정 342 로 옮겨진 옛 경로
```

★ **마스터가 매일 보는 화면이 하루 내내 틀린 값을 말하고 있었습니다.**
「배점 675 를 끝냈다」고 보고했는데 **화면은 605 라고 적혀 있었습니다.**
이 프로젝트가 막으려는 「선언과 실제의 괴리」 그대로입니다.

### 고쳤습니다 — 숫자를 없애는 것이 아니라 config 에서 받습니다

```
web/views.py   page_extras() · _points()
               전 화면에 {{ points.total }} · {{ points.grade }} · {{ points.taste }}
listings.html  총점 {{ points.total }} 중 … 취향 {{ points.taste }}점을 뺀
               {{ points.grade }} 기준
dealers.html   {{ points.grade }}점과 무관한 별도 축
```

```
지금 화면   「총점 675 중 등급은 취향 50점을 뺀 625 기준입니다」
           「딜러는 … 625점과 무관한 별도 축입니다」
```

### ★ 다시 못 생기게 검사에 물렸습니다 — `S14-1`

```
S14-1 화면에 배점을 박지 않음 (V4-17)   통과 37 · 실패 0
```

```
어떻게   템플릿에서 주석·{{ }}·태그를 걷어낸 뒤
        「세 자리 숫자 + 점/중/기준」을 찾는다
안 걸리게 한 것   「STEP 135」 · 「개정 365」는 식별자다.  배점이 아니다
```

**되살려 확인했습니다**

| 되살린 결함 | S14-1 |
|---|---|
| `listings` 를 「총점 605」로 되돌림 | 실패 — `templates/listings.html — 배점을 글자로 박았다 (605)` |
| `dealers` 를 「555점과」로 되돌림 | 실패 — `templates/dealers.html — 배점을 글자로 박았다 (555)` |
| 되돌린 뒤 | 통과 |

---

## 4. ★ 만들다 낸 결함 하나

| 무엇 | 잡은 것 |
|---|---|
| `points` 를 `page()` 안에서 얹었다 | **V11-38** 「templates 가 쓰는 값을 뷰가 넘김」 — `points 를 아무도 안 넘긴다` |

```
왜     검사는 page() 에 들어가는 ctx 만 엿본다.
      page() 가 스스로 얹는 것은 안 보인다
고침   web/views.py  page_extras() 를 공개 함수로 두고
      page() 와 V11-38 이 같은 하나를 부른다
      ★ 각자 목록을 들면 「아무도 안 넘긴다」는 거짓 경보가 난다
```

★ 이 세션에서 세 번째입니다 — `NON_SCREEN_VIEWS`(9-4) · `_cumulative_codes`(9-2) ·
`page_extras`(지금).  **두 곳이 각자 목록을 들면 반드시 갈립니다.**

---

## 5. `inbox/` 를 비웠습니다 (규칙 5 ③)

**마스터 지시 — 「끝난 md 는 지워야 확인이 된다」**

```
지운 것 14개
  ANSWER_scoring_675.md          ORDER_00_순서.md
  ORDER_S30_S31.md               ORDER_S32_source.md
  ORDER_S34_trace.md             ORDER_doc_structure.md
  ORDER_master_answers.md        ORDER_scoring_675.md
  ORDER_trace_14chapters.md      ORDER_trace_all.md
  ORDER_trace_verify.md          REPORT_trace_complete.md
  REPORT_trace_source.md         VERIFY_0818_0310.md
```

```
남긴 것   inbox/README.md — 「이 파일만 있으면 처리할 것이 없다」는 표시
★ inbox/ 는 .gitignore 에 있어 README 는 커밋에 안 들어갑니다.
  무엇을 처리했는지는 outputs/ 가 갖습니다
```

★ **지운 것은 지시서 사본입니다.  `docs/` 는 손대지 않았습니다 (규칙 2).**

---

## 6. 조치 후 결과 — ★ 실제 출력

```
python3.11 tools/check_all.py

DB carwatch.db · run 20260817T032131
통과 280 · fatal 3 · warn 7 · 환경 차이 1 · 미실행 3

■ FATAL
  V4-11     unclassified 존재 — 32
  V11-12    라우팅 표의 view 가 10·13장에 실재함 — 2
  V11-80    사진이 최소 크기 이상 — 관심 96/88/80px · 목록 96/88/80px
```

```
python3.11 tools/run_tests.py
결과: 실패 — test_endtoend.py, test_web.py

python3.11 tools/check_src.py
S14   상수 등록·성격        통과 510 · 실패 0
S14-1 화면에 배점을 박지 않음  통과 37 · 실패 0
```

| | 전 | 후 |
|---|--:|--:|
| check_all 통과 | 280 | **280** |
| fatal | 3 | **3** |
| 화면이 말하는 배점 | **605 / 555** | **675 / 625** |
| 검사 | — | **S14-1 신설** |
| inbox | 14개 | **0개** |

---

## 7. ★ 남은 것 — 정직하게 적습니다

### ① 안 된 것 넷

```
3-2  타이어 트레드          unified-report 가 401 이다.  소모품 축 10점이 죽어 있다
4-9  차종 추가 드롭다운      목록에 <select> 가 없다 (STEP 149r)
8-4  K카 한 차종 끝까지      목록 경로를 못 찾았다
8-5  차대번호 크로스사이트    K카 매물이 0건이라 견줄 것이 없다
```

### ② `audit_validation` 이 08-17 에 멈춰 있습니다

```
check_all 은 결과를 DB 에 안 남깁니다.  마지막 기록이 2026-08-17T05:33 입니다
그래서 docs/CHECKS.md 의 「마지막 통과/실패」 칸이 이틀 낡았습니다
```

★ 검사 자체는 매번 돌아 맞는 값을 냅니다.  **기록만 안 남습니다.**
남기게 할까요 — 남기면 「언제 마지막으로 통과했나」를 화면에서 볼 수 있습니다.

### ③ 자동화는 다음 04:00 이 시험대입니다

```
08-18 04:00 에 넣은 12건이 전부 「S4 미완료」로 실패했습니다
S4 는 그 뒤 고쳤습니다 (fetched_at 순서 · 낡은 봉투 건너뛰기)
고친 뒤로 새 작업이 안 들어왔습니다
```

**08-19 04:00 이 지나면 결과를 확인해 보고하겠습니다.**

### ④ 사람이 정해야 하는 셋 (규칙 2)

```
V4-11   미분류 32 — 마스터가 field_usage.suggested.json 을 옮깁니다
V11-80  R-206(128px) vs a-common(96/88/80) — 규격끼리 어긋납니다
V11-12  /reports 두 줄이 라우팅 표에 없습니다
```

### ⑤ 오래된 S 실패

```
S13 본문 config 예시 대조   20
S16 검증 코드 대조         42   ← 규격에 있는데 코드에 없는 검사
S34-2 표의 소스·검사가 실재  8
S26 작업 기록             12   ← 6절이 없는 옛 기록들
```

---

## 8. 고친 파일

```
web/views.py                 page_extras() · _points()
web/templates/listings.html  총점·기준을 config 에서
web/templates/dealers.html   배점을 config 에서
tools/check_src.py           S14-1 신설
validate/v11_web.py          V11-38 이 page_extras 를 본다
inbox/                       14개 지움 · README.md 남김
```
