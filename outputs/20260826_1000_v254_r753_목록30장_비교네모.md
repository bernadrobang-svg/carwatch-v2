# 목록 30장 · 관심 카드에 비교 네모를 되살렸다

```
버전     소스 v254 · 지시서 SPEC-2026.08.26-r753
때       2026-08-26 10:00
지시     0장·0a장 — 마스터 확정 셋 가운데 ①(30장) · 비교 네모(개정 751)
근거     docs/UI_REVIEW.md 16장 · ref/screens/v4m_watch_시안.html (a84d2d1)
앞 작업  d643cc6 (V11-76 색인 다섯)
```

---

## 1. 무엇을 하려 했나 (지시 원문)

```
① ★★ 목록은 한 쪽에 30장 (UI_REVIEW 16장 · 마스터 「30개 해」)
   50 → 30장 · 쪽 57 → 94 · 화면 306KB → 약 160KB
   ★ 카드에서는 아무것도 빼지 마라.  줄인 것은 「장 수」다
   ★ 네가 낸 ⓒ(곁말 줄이기)는 안 한다.  마스터 「보여지는 정보는 빼지 마라」
   ★ 관심·추적·미판정도 같이 30장.  화면마다 다르면 헷갈린다
   검산 S46-74

★ 비교 네모 — 관심 카드에 되살려라.  내 시안이 틀렸다
   ★ 마스터 확정 17 「비교→관심」이 있다.  비교로 가는 문은 관심이다
   ★ 사진 왼쪽 위 네모 · 고르면 아래에 「N대 비교하기」 띠
   ★ 거르개는 그대로 안 둔다.  담은 차는 스무 대다
```

---

## 2. 무엇을 했나 (고친 파일 · 줄)

### ① 한 쪽 30장

```
config/web.json 11행        rows_per_page  50 → 30
```

★ 한 자리만 고쳤다.  네 화면이 **이미 같은 자리에서 읽고 있었다** —
목록 · 미판정은 그대로 따라왔다.  안 따라오던 둘을 붙였다.

```
report/screens/build.py 3101~3112  view_track
   pairs · grade_split · accident_split 을 ★ 한 쪽 몫만 자른다
   ★ 합(total_pairs)은 ★ 전건을 센다 — ★ 자른 것을 합으로 내지 않는다 (검토 17)
   ★ TrackView.page_rows · cut 을 더했다

report/screens/build.py 2394      view_watch
   ★ 담은 차가 스무 대라 쪽이 안 생기지만 ★ 수는 맞춰 둔다

report/screens/views.py           TrackView.page_rows · cut
web/templates/track.html 58~62    「N쌍 가운데 30쌍만 냅니다」
```

★★ **카드에서 아무것도 빼지 않았다.** 곁말(`title=`)도 그대로다.

### ② 비교 네모 — 카드에 되살렸다

가이드가 시안을 고쳤다 (`a84d2d1`).  그 시안 그대로 옮겼다.

```
web/templates/watch.html 27~31    카드 무리 전체를 <form action="/compare"> 로 감쌌다
                        36~46    사진 왼쪽 위 네모 (.kpick) — ★ 하트와 마주 본다
                       129~137   아래 띠 (.kcmp) — 「N대 골랐다」 · 「비교하기 ▸」
                       (지움)     내가 아래에 두었던 「견주기」 절
web/static/app.css 2296~2318      .kthumbwrap · .kpick · .kcmp
                   2360~2363      ≥640 에서는 아래 탭이 없으므로 띠도 바닥에
web/templates/watch.html 맨 아래   네모를 세는 짧은 스크립트
```

```
★ 짚는 자리는 44px 다 — ★ 보이는 네모는 26px 이고 ★ 둘레가 손가락 몫이다
★ 세는 것은 브라우저만 할 수 있다 — ★ 값을 다시 실어 보내지 않는다
★ JS 가 없어도 ★ 띠가 보이고 ★ 단추가 산다 — ★ 못 쓰게 되지 않는다 (개정 248)
★ 거르개는 두지 않았다
```

### ③ 검사 S46-74

```
validate/v0_guide.py  s46_74_rows_per_page()
  ① 규격(docs/UI_REVIEW.md 16장)에서 ★ 수를 읽는다 — ★ 코드에 30 을 안 박는다
     ★ 여기에 30 을 적어 두면 ★ 마스터가 수를 바꾸실 때
       ★ 검사가 ★ 「옛 수를 지키는 쪽」이 된다
  ② config/web.json rows_per_page 와 같은가
  ③ view_watch · view_track · _unmatched_rows 가 ★ 그 자리에서 읽는가
     ★ 따로 박아 두면 ★ 화면마다 갈린다
```

```
S46-68 도 두 줄 넓혔다 — 「비교 네모」·「비교로 가는 문」을 센다 (22가지)
```

---

## 3. 1차 결과 (고치기 전 숫자)

```
/listings   306KB · 카드 50장       ← V11-76 이 잡던 것
/watch       29KB · 카드 (담은 것 전부)
/track       69KB · 짝 전부
/notready    22KB · 이미 rows_per_page 를 따르고 있었다
V11-76      3~5곳 (그 가운데 /listings 306KB 는 흔들리지 않는 것)
```

---

## 4. 조치 후 결과

```
/listings   ★ 159KB · 카드 30장     ← 가이드 예상 「약 160KB」와 같다
/watch       29KB · 카드 9장 (담은 것이 아홉이다)
/track       69KB · 절마다 30쌍 · 「N쌍 가운데 30쌍만 냅니다」
/notready    22KB
V11-76      ★ **풀렸다** — /listings 가 목록에서 빠졌다
```

```
python3.11 tools/run_tests.py
  test_admin         통과
  test_admin_flow    통과
  test_collect       통과
  test_crosssite     통과
  test_dict          통과
  test_endtoend      통과
  test_fixtures      통과
  test_integration   통과
  test_invariants    통과
  test_pipeline      통과
  test_registry      통과
  test_report        통과
  test_run           통과
  test_score         통과
  test_screens       통과
  test_spec_ui       통과
  test_store         통과
  test_watch         통과
  test_web           통과
  ruff               통과  (F821,F811,B905,F841,DTZ005,F401)

  임시 DB 62개 치움 (검사가 디스크를 채우지 않는다)

결과: 통과
```

```
python3.11 tools/check_all.py
DB carwatch.db · run 20260825T000022
통과 351 · fatal 4 · warn 11 · 환경 차이 2 · 미실행 8

■ FATAL — 고쳐야 한다
  V1-08     동일 코드 실패율 100% 인 엔드포인트 없음 — 2건 (예: detail: 328건 전량 실패, inspection: 182건 전량 실패)
  V1-21     받아 두고 안 펼쳐진 원문이 없음 — 182
  V1-23     필요한 조합 대비 받은 카탈로그 비율 — 127조합 · 78.9% · 카탈로그 없는 매물 633건 · 사이트가 안 줌 empty 21조합 · 매물 586건 (예: 8245694202307
  V2-01     ok 원문 수 == CORE 행 수 — 5744

■ warn — 진행은 된다
  V1-07     매물별 엔드포인트 4종 상태 존재 — 4242
  V4-06     RAW 경로가 등록부에 있는가 — 3
  V4-06b    등록부에 있는데 RAW 에 없는 유령 경로 — 48
  V4-11b    판정에 안 쓰는 미분류 경로 — 319
  V4-21     같은 이름의 공개 함수가 두 모듈에 없음 — 7
  V3-39     이론가와 실제 중앙값의 차가 상한 안 — 최대 33.2%
  V3-47     축별 차종 간 결측률 편차가 상한 안 — 3축
  V3-03     축별 source 값 종류 >= 2 — 1건 (예: taste.fitting)
  V3-04     축별 값 종류 >= 2 — 2건 (예: state.consumable, taste.fitting)
  V3-91     가이드 검산 일곱 줄이 표대로 나옴 — 7줄 다름
  V3-96     value IS NULL 과 source 모름 건수 차 — source 모름 33989 · 차 +32,797

■ 환경 차이 — 결함이 아니다
  V1-05     실행 시작 시각 기준이라 DB 만으로는 재현되지 않는다
  V4-01     HMAC 키가 다르면 plate_hash 가 전건 불일치한다

■ 미실행 — 이번 실행에서 그 단계를 안 돌았다
  V1-20     이번 실행에 카탈로그를 안 받았다
  V4-29     판정을 막는 미분류가 없다 — 잴 것이 없다
  V4-30     판정을 막는 것이 없다
  V3-68     부록 F 에 축 목록이 없다
  V3-64     규격에서 경계를 못 읽었다
  V9-09     비율이 같은 매물 짝이 없다 — 잴 것이 없다
  V10-24    사전 확정 이력이 없다
  V11-121   두 사이트에 같이 올라온 차가 없다 — 나란히 낼 것이 없다
```

---

## 5. ★★★ 못 한 것 · ★ 새로 찾은 것

### ★★★ 여쭐 것 없음.  ★ 대신 ★ **찾은 것 하나를 보고한다**

★ 재다가 알았다.  ★ **엔카 말고는 ★ `raw_response` 에 원문이 한 건도 없다.**

```
SELECT site, COUNT(*) FROM raw_response GROUP BY site
  encar  147,061      ← ★ 이것뿐이다
  (그 밖 사이트 ★ 0건)

SELECT COUNT(*) FROM core_listing WHERE site='kbchachacha'   265건
SELECT COUNT(*) FROM raw_response WHERE site='kbchachacha'     0건
```

★ `tools/collect_*.py` ★ 열 개를 다 훑었다 — ★ **`save_raw` 를 부르는 것이 하나도 없다.**

```
collect_bmw · bobaedream · heydealer · hyundai_cert · kbchachacha ·
kcar · kia_cpo · lexus · reborncar · volvo   ★ 열 개 다
★ 셋은 주석에 「raw_response 에는 남는다」고 ★ 적어만 두었다 (kcar 135행 ·
  reborncar 113행 · kia_cpo 8행) — ★ 실제로는 안 남는다
```

★★ 이것이 명령서 3-2 의 **필수**와 어긋난다.

```
필수  ★ ★ **`raw_response` 에는 남긴다** — ★ 갈래를 넓히시면 ★ 다시 판다
      ★ ★ 다시 받을 일이 없다.  ★ 그것이 ★ 「보관만 한다」는 뜻이다
```

★★★ **지금 도는 KB 수집(엿새치)이 ★ 원문을 안 남기고 있다.**
★ 지금 멈춰 고치지 않으면 ★ 엿새 뒤에 ★ 다시 엿새를 받아야 한다.

```
★ `V2-01`(ok 원문 수 == CORE 행 수 — 5744)이 ★ 여태 이것을 말하고 있었다.
  ★ 나는 그것을 ★ 「수집이 도는 중이라 그렇다」로 ★ 두 회차 넘겼다.  ★ 내 잘못이다
```

★ **다음 바퀴에서 이것부터 고친다** — ★ 열 개에 `save_raw` 를 넣고
★ KB 수집을 다시 건다.  ★ 이미 받은 상세는 건너뛰므로 다시 받지 않는다.

### 못 한 것 (그대로)

```
① KB 전량 — 도는 중 (지금 265건).  ★ 위 때문에 ★ 다시 걸어야 한다
② 3-2 걸러 저장 — K카 526 · 리본카 1,038 · 렉서스 36
③ 3-3 되돌리기 · 미분류 다시 세기
④ 멀티 매핑 (45-2) · 견주기 (45-3)
⑤ v4m 일곱 장 — ★ 가이드가 그린다.  ★ 관심이 통과된 뒤다.  ★ 안 손댄다
```
