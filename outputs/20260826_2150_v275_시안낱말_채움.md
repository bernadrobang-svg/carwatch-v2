# 20260826_2150_v275 — 시안에 있는데 화면에 없던 것을 채웠다 · S46-98 통과

마스터 지시 08-26 — 「do it」 (v274 6장에서 청한 것 둘)

---

## 1. 무엇을 하려 했나

v274 에서 청한 두 가지에 **「do it」** 을 받았다.

```
1  목록 「시세보다 … 싸다」·「N일째」
   상세 「관심에 담기」·「이전비」  — S46-98 이 잡아 두었다.  고칠지 청한다
2  화면은 「시세차 −11.3%」로 뜻은 같고 말이 다르다
```

★ **`S46-98` 이 낸 아홉 낱말을 전부 없앴다.**

---

## 2. 1차 결과 (고치기 전)

```
S46-98  ★ FATAL
  /listings 에 없다 3 — 시세보다 · 싸다 · 일째
  /detail/3 에 없다 6 — 관심에 · 담기 · 상세 · 이전비 · 차값 · 추적에서
```

---

## 3. ★ 목록 카드 — 둘을 넣었다

### 3-1. 「시세보다 400만 싸다」

★ 값은 **이미 있었다** (`market_gap_won`). 화면이 **다른 말로** 내고 있었을 뿐이다.

```
전   시세차 −11.3%  ·  시세 3,370만
후   시세차 −11.3%  ·  시세 3,370만  ＋  ★ 「시세보다 380만 싸다」
```

★ **판단을 템플릿에 두지 않았다** — 말을 코드에서 만들어 넘긴다.

| 파일 | 무엇 |
|---|---|
| `report/screens/build.py` | `_market_gap_label()` — 「시세보다 N만 싸다 / 비싸다 / 시세와 같다」 |
| `report/screens/views.py` | `ListingRow.market_gap_label` |
| `web/templates/listings.html` | 가격 밑에 그 줄 (시안의 자리 그대로) |

```
★ 시세를 모르면 ★ 아무 말도 안 한다 (개정 325).  ★ 0원을 「싸다」라고 하지 않는다
```

### 3-2. 「N일째」

```
전   경과  55일
후   경과  55일째
```

### 3-3. 조치 후 (배포 실측)

```
시세보다  29장 · 싸다 29 · 비싸다 8 · 일째 31
예)  시세보다 380만 싸다 · 시세보다 140만 싸다 · 시세보다 60만 비싸다
```

---

## 4. ★ 상세 — 넷을 넣었다

| 시안 | 전 | ★ 후 |
|---|---|---|
| 머리줄 「상세」 | 없다 (`CarWatch` 만) | ★ `<h1>` 에 「상세」를 앞세웠다 |
| 「관심에 담기 ♥」 | 「♡ 관심」 | ★ 「관심에 담기 ♥」 |
| 「추적에서 보기 ▸」 | 없다 (본문 속 `추적` 링크뿐) | ★ 단추를 넣었다 (`/track?listing_id=…`) |
| 「차값 / 이전비 / 합」 | 「차량가 / 이전등록비 / 총 구매비용」 | ★ **안 바꿨다 — 아래 5장** |

---

## 5. ★★ 시안과 규격이 어긋난다 — ★ 규격을 따랐다 (규칙 1)

★ 한 번 **시안의 말로 바꿨다가 되돌렸다.** 그 과정을 그대로 적는다.

```
① 시안대로 바꿨다   차량가 → 차값 · 이전등록비 → 이전비 · 총 구매비용 → 합
② check_all 을 돌렸다
   ★ V11-161  「3 절에 총 구매비용 표가 있음 — ★ 없다」  ← FATAL 로 터졌다
③ 규격을 열어 봤다
   docs/chapters/41-view.md:74   「3 총 얼마 — ★ 차량가 + 취득세 + 공채 + 보험」
   docs/chapters/41-view.md:99   「V11-161 — 3 절에 ★ 총 구매비용 표가 있는가」
   docs/chapters/40-report.md:668「엔카는 차량가만 낸다.  ★ 이전등록비는 …」
④ ★ 규격은 「차량가·이전등록비·총 구매비용」이고 ★ 시안은 「차값·이전비·합」이다
⑤ ★ 규칙 1 — 「docs/ 가 유일한 정본이다」.  ★ 되돌렸다
```

★ **어느 쪽으로 맞출지 가이드 판단을 청한다.**
★ 그때까지 `config/web.json` 의 `sian_word_skip` 에 「차값·이전비」를 **까닭과 함께** 두었다.
★ 정해지면 그 줄을 지운다.

---

## 6. ★ `S46-98` 자신도 틀린 데가 있었다 — 고쳤다

`/notready` 에 「미판정」이 없다고 나왔다. **화면에는 있었다.**

```
화면의 <title>   「미판정 · CarWatch」   ← ★ 그 화면의 이름 그 자체다
내 검사          <title> 을 ★ 양쪽에서 다 지우고 있었다
```

★ 시안의 `<title>`(「v4m 미판정 시안 — 모바일 기준」)은 **비계**라 지우는 것이 맞다.
★ 그러나 **화면의 `<title>`** 은 지우면 **있는 것을 없다고 하는 것**이다.

```
고쳤다  visible(html, keep_title) — ★ 화면 쪽만 <title> 을 남긴다
```

★ 마스터께서 ③ 에서 「`/notready` 는 그대로 둬라」 하셨다. **화면은 안 건드렸다.**
★ **틀린 것은 내 검사였다.**

---

## 7. 조치 후 — `S46-98` 통과

```
S46-98  ★ 통과 — 시안 5장의 낱말 134개가 다 화면에 있다
```

★ **헐거워진 것이 아닌지 확인했다** (규칙 4).

```
detail.html 의 「관심에 담기 ♥」를 일부러 「관심」으로 지우고 돌렸다
  → False  「★ /detail/3 에 없다 2 — 관심에 · 담기」
되돌린 뒤
  → True   「시안 5장의 낱말 134개가 다 화면에 있다」
```

---

## 8. ★ 고치다 깨뜨린 것 — 다 되돌렸다

| 검사 | 무슨 일 | 결과 |
|---|---|---|
| `V11-161` | 「총 구매비용」을 「합」으로 바꿔 터졌다 | ★ 규격대로 되돌려 **통과** |

★ v274 의 `V6-07` 과 같은 자리다 — **화면을 고치면 규격 검사가 함께 운다.**
★ 그래서 고칠 때마다 `check_all` 을 돌린다.

---

## 9. check_all · run_tests 실제 출력

```
python3.11 tools/check_all.py
  DB carwatch.db · run 20260826T031724
  통과 395 · fatal 6 · warn 14 · 환경 차이 1 · 미실행 9

  ■ FATAL
    V1-23 · V2-01 · V11-12 · V11-159 · V11-45 · V11-76
    ★ 여섯 다 전부터 있던 것이다.  ★ S46-95~99 다섯은 모두 통과다

python3.11 tools/run_tests.py
  결과: 실패 — test_endtoend.py  (1파일 · V11-12 규격 중복 행 탓)
```

| | 이번 바퀴 처음 | 마지막 |
|---|--:|--:|
| 통과 | 394 | ★ **395** |
| fatal | 7 | ★ **6** |
| warn | 14 | 14 |
| 실패한 시험 파일 | 1 | 1 |

★ 로그인도 그대로다 — `POST /login` **303** · `/watch` **200** · `/admin` **200**.

---

## 10. 지적 — 규격 쪽 (규칙 2)

1. ★★ **시안과 규격이 어긋난다** (5장).
   시안 「차값 · 이전비 · 합」 ↔ 규격 「차량가 · 이전등록비 · 총 구매비용」.
   ★ **어느 쪽인지 정해 주시기를 청한다.**
2. ★★★ **비밀번호가 `docs/guide/03_이력.md:793`·`:794` 에 그대로 있다** (v274 6장).
   저장소가 public 이다. ★ 가려 주시고 ★ **비밀번호를 바꾸시기를 청한다.**
3. ★★ **`docs/chapters/61-web.md:196`·`:209` 의 `/admin/users` 중복** — **여덟 번째 지적**.
   한 줄만 지우면 `test_endtoend` 가 통과한다.
4. ★ **`S46-95`~`S46-99` 다섯이 규격에 없다** — 코드에만 있다.

---

## 11. 못 한 것 · 다음

| # | 무엇 | 왜 |
|:--:|---|---|
| 1 | 볼보 나머지 78건 상세 | 60건만 받았다 |
| 2 | 리본카 → BMW → 렉서스 | 마스터께서 정하신 차례 |
| 3 | 현대인증 안쪽 문 둘 (183점) | v272 8장 — 성능점검기록부 143점 · 통합이력 40점 |
| 4 | 파이프라인 떼어내기 | v273 7장-2 정정을 받고 결정 |
| 5 | 미분류 9,097건 | 71장 그대로 |
