# v360 — 가이드 지시 ③ · BMW · ★ **파서가 아니라 판이 JSON 만 알았다**

```
③ BMW 파서 — 상세 385/385 받고 26건(7%)만 폈다.  ★ **여기만 진짜 파서 문제**
```

## ★ 재 보니 — ★ **파서는 멀쩡하다**

```
BMW 목록 원문   820건
그중 ★ **우리 차종은 42건**  (X3 · iX3 — ★ 나머지는 M8·520i·X5·X7…)
```

★ 사전을 두드려 봤다 —

```
「X3」  known_model_of → X3   · target_key_of → ★ X3_IMPORT
「iX3」 known_model_of → iX3  · target_key_of → ★ IX3_EV
「M8」  둘 다 → None          ← ★ 우리 차종이 아니다.  ★ 바르게 버린다
```

★★ 「385/385 받고 26건만 폈다」는 ★ **분모가 다른 것**이다 —
★ ★ 385 는 ★ **BMW 가 파는 전부**고 ★ 우리 것은 42 다.
★ ★ ★ 차종이 붙은 23건은 ★ 상세도 23건 · ★ **100%** 였다.

## ★ 진짜 막힌 자리 — ★ **판이 JSON 만 안다**

```
$ python3.11 run.py collect --site bmw_bps --only S5
S5   expected 1746 · requested 194 · ok 0 · ★ **error 194**
★ 그런데 ★ audit_request 의 http_code 는 ★ **전부 200** 이다
```

```python
# collect/fetcher.py:82
body = json.loads(res.body_text)
except ValueError:
    return FetchResult(..., "error", ...)      ← ★ HTML 이면 무조건 error
```

★★ BMW BPS·현대인증·K카·보배드림 상세는 ★ **HTML 쪽**이다.
★ ★ 그것이 그 사이트의 ★ **바른 원문**이다 (파서가 그렇게 받는다).
★ ★ ★ 판은 ★ 엔카(JSON)만 알아 ★ **200 을 error 로 적고 있었다.**

## 고친 것 — ★ **어댑터가 말한다**

```
① config 에 ★ 「이 창구는 HTML 이다」를 적는다 (`root_type.detail = "html"`)
   ★ BMW BPS · 현대인증 · K카 · 보배드림 넷
② `fetch()` 가 ★ 그 말을 보고 ★ 글월 그대로 받는다
   ★ ★ 말이 없으면 ★ **옛날처럼 `error`** 다 — ★ 조용히 안 삼킨다
③ `verify_shape()` 도 ★ HTML 이면 ★ 「비어 있지 않은가」만 본다
   ★ ★ `required_keys` 는 ★ JSON 의 열쇠다 — ★ HTML 에는 그런 것이 없다.
   ★ ★ ★ 내용은 ★ **파서가 본다** (층을 지킨다)
```

## 조치 후 결과

```
$ python3.11 run.py collect --site bmw_bps --only S5
S5   requested 194 · ★ **ok 194** · error 0 · ★ **거부 0**

BMW 상세 원문  23 → ★ **194건**
```

| | 앞 | 뒤 |
|---|--:|--:|
| BMW 상세 원문 | 23 | **194** |
| 판이 낸 `error` | 194 | **0** |
| 형식 검증 거부 | 194 | **0** |
| 시험 실패 | 1 | 1 |

## 여쭐 것

```
★ 「BMW 파서 문제」는 ★ **아니었다** — ★ 파서도 사전도 멀쩡하다.
  ★ ★ 막은 것은 ★ **판이 JSON 만 아는 것**이었고 ★ 그것을 고쳤다.
★ 그러면 ★ 같은 자리가 ★ 현대인증·K카·보배드림에도 있다 —
  ★ ★ 셋 다 ★ `root_type.detail = "html"` 로 적어 뒀다.  ★ 이제 판으로 받는다
★ BMW 「상세율 90%」는 ★ 분모를 ★ **우리 차종 42건**으로 보면 ★ 이미 넘었다.
  ★ ★ 「BMW 가 파는 385건」으로 보면 ★ 규격이 「전량을 받지 않는다」와 어긋납니다 —
  ★ ★ ★ 어느 쪽으로 재야 하는지 ★ 정해 주십시오
```


---

# 같은 고침이 ★ **셋에 더 들었다**

★ 「판이 JSON 만 안다」는 ★ BMW 만의 일이 아니었다 —

```
$ run.py collect --site kcar         --only S5   →  ok ★ **54**
$ run.py collect --site bobaedream   --only S5   →  ok ★ **191**
$ run.py collect --site hyundai_cert --only S5   →  ok ★ **210**
```

★ 현대인증은 ★ 한 자리가 더 있었다 —

```
sqlite3.OperationalError: no such column: ★ **list_coming_status**
★ 현대인증은 ★ `list` 말고 ★ `list_coming` 창구도 있다.
★ ★ 「목록 갈래를 뺀다」를 ★ 이름으로 하면 ★ 또 샌다 —
★ ★ ★ 「`core_listing` 에 `{창구}_status` 칸이 있는가」로 가른다 (`S14`)
```

# ★ 지시 ② 의 끝 조건 — ★ **열두 곳 중 열하나가 넘었다**

```
  encar            9284/9354    99.3%
  kbchachacha  ★    328/5188     6.3%   ← ★ 하나만 남았다
  reborncar        1200/405    296.3%
  heydealer         265/213    124.4%
  hyundai_cert      226/211    107.1%
  bmw_bps           194/194    100.0%
  bobaedream        227/191    118.8%
  volvo_selekt      169/165    102.4%
  revolt             57/58      98.3%
  lexus_certified    51/36     141.7%
  kcar              115/27     425.9%
  kia_cpo             8/8      100.0%
```

★ 100% 를 넘는 것은 ★ **앞서 받아 둔 원문**이 매물보다 많아서다
(우리 차종이 아닌 것도 원문은 남긴다 — P3).

| | 앞 | 뒤 |
|---|--:|--:|
| **90% 를 넘은 사이트** | 3 | **11 / 12** |
| 기아 CPO | 0% | **100%** |
| BMW | 11.9% | **100%** |
| 현대인증 | 19.6% | **107.1%** |
| K카 | 41.9% | **425.9%** |

★ KB 하나가 남았고 ★ **오늘 몫이 말랐다** (v355 · reCAPTCHA 실측).
★ ★ 규격대로 ★ **하루 한 판씩 이어 받는다** — ★ 더 안 두드린다.
