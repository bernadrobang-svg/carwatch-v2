# D — app.css header 23곳 (UI_REVIEW 27장 · S46-121)

## 안 한 것

| 무엇 | 왜 못 했나 | 며칠째 | 누가 답해야 하나 |
|---|---|--:|---|
| 없음 | — | — | — |

## 1 무엇을 하려 했나

ORDER_20260829 D — 「`app.css` header 23곳 · 화면마다 따로 적혀 위·아래가 바뀐다」 · 검산 S46-121

## 2 무엇을 했나

| 파일 | 무엇 |
|---|---|
| `web/static/app.css` | `body.s-… header { … }` 21곳 삭제 · 공통 `header` 하나에 주석 |
| `web/static/app.css:2522` | `.run-hours { color:var(--accent) }` 새로 만듦 |
| `validate/v0_guide.py:1303` | `s46_121_header_rule_in_one_place` |
| `validate/v0_guide.py:2812` | S46-121 등록 |

## 3 왜 그렇게 했나

UI_REVIEW 27장. 실측 — 화면별 규칙 21곳이 **글자까지 같고 `z-index` 만 갈렸다**
(30 이 12화면 · 20 이 9화면). 그것이 「위·아래가 바뀐다」의 까닭이다.
공통 `header { position:sticky; top:0; z-index:50 }` 하나로 둔다.

`.run-hours` 는 지난 회차에 내가 `admin_run.html:74` 에 넣고 CSS 에 안 만든 것이다 —
V11-59 fatal 이 내 것이었다. 새 색을 만들지 않고 `--accent` 를 쓴다.

## 4 · 5 몇 → 몇

| | 고치기 전 | 고친 뒤 |
|---|--:|--:|
| 화면별 `body.s-… header` 규칙 | **21곳** | **0곳** |
| 공통 `header` 규칙 | 1 | 1 |
| `z-index` 값 갈래 | 30/20/50 **3가지** | **1가지** |
| check_all 통과 | 407 | **410** |
| check_all fatal | 8 | **6** |
| V11-59 시안 클래스 없음 | 1종 | **0** |
| run_tests 실패 | 1파일 `test_endtoend` | 1파일 `test_endtoend` (그대로) |

사진으로 확인 — `outputs/shot/admin_run_360.png` 머리띠 정상 (CarWatch · 마스터 · admin · 로그아웃 · sticky).

## 6 못 한 것 · 판단이 필요한 것

- 남은 fatal 6 — S46-32(색인) · S46-97(listing_id 빈 원문 21건) · V1-23 · V2-01 · V11-12 · V11-106 · V11-45.
  다음 회차에 목록으로 짚는다 (마스터께서 「재촉하지 않는다」 하셨다).
- `test_endtoend` 1파일은 이번 변경 전부터 실패하던 것이다 — 이 회차가 만든 것이 아니다.
