# v338 — 「무엇으로 쟀는가」 · 브라우저로 다시 쟀다

가이드 지적 09-02 —

```
1  1-1·1-2·1-4·1-8 을 ★ 「안 닫혔다」로 되돌려 가이드에 다시 올린다
   ★ 그리고 ★ 「무엇으로 쟀는가」를 적게 한다 — DB 가 아니라 ★ 브라우저다
2  검사에 ★ 브라우저 확인을 붙인다 (S46-253)
3  1-7 은 닫는다 — ★ 5회차 만이다
```

## 1. 내가 무엇으로 쟀었나 — ★ 그대로 적는다

| # | 앞 회차에 내가 쓴 자 | 브라우저인가 | 배포인가 |
|:--:|---|:--:|:--:|
| 1-1 | `curl \| grep` | ✗ | ○ |
| 1-2 | **로컬 `outputs/render/*.html`** ＋ 로컬 CSS 주입 | ○ | ✗ |
| 1-4 | `curl \| grep` | ✗ | ○ |
| 1-7 | 파이썬으로 **처리기 직접 호출** | ✗ | ✗ |
| 1-8 | 파이썬으로 **처리기 직접 호출** (DB 사본) | ✗ | ✗ |

★ **가이드 지적이 맞다.** 다섯 중 ★ 브라우저로 배포를 연 것은 ★ **하나도 없다.**
★ `curl` 은 ★ **글자**를 본다. ★ 브라우저는 ★ **보이는 것**을 본다.

## 2. 브라우저로 다시 쟀다 — `tools/browser_verify.py` (신설)

크로미움으로 ★ **배포 주소**(`config/deploy.json`)를 연다.
「보이는가」는 ★ 상자 크기 · `display` · `visibility` · `opacity` 를 다 본다.

```
★ 배포 https://43.201.16.78.sslip.io 를 ★ 브라우저로 열어 쟀다
```

| # | 브라우저가 낸 값 | 판정 |
|:--:|---|:--:|
| **1-1** | 기본 목록 — ★ 카드 30 · 배지 ★ **0** · `?order=price` 에서만 3 | ★ **안 닫혔다** |
| **1-2** | 목록·추천·추적 셋 다 ★ 360:104×78 → 900:180×135 → 1200:**240×180** | ★ 닫혔다 |
| **1-4** | 보배드림 30/30 · 헤이딜러 30/30 에 까닭이 **보인다** · 엔카 0 (사진이 다 있다) | ★ 닫혔다 |
| **1-7** | 「저장하지 않았습니다 — 배점 합 **913** 이 분모 **910** 을 넘습니다」 · 파일 910/910 그대로 | ★ 닫혔다 |
| **1-8** | 「분류를 저장했습니다 — 미분류 **332 → 331건**」 · 새로 고쳐도 331 | ★ 닫혔다 |

★ 1-7·1-8 은 ★ **로그인해서 실제로 눌렀다** (아래 5절).

## 3. ★ 1-1 은 ★ **정말 안 닫혀 있었다** — 까닭을 쟀다

```
기본 목록 30장 중 배지 ★ 0개
배지 대상 161행 중 ★ **가장 앞 차례가 233위**
   앞 30행 안 0개 · 앞 60행 안 0개 · 앞 120행 안 0개 · 앞 300행 안 1개
```

★ 곧 ★ **1쪽에서는 볼 수 없다.** 8회차 동안 「0개」로 적힌 까닭이 이것이다.

★ 배지 행을 뜯어봤다 —

```
사이트     엔카 135 · KB 11 · 리본카 8 · 렉서스 5 · BMW 2
평균 점수  배지 행 302.6  ↔  전체 340.8      ★ 38점 낮다
```

★ 틀은 맞다 (`?order=price` 에서 「2곳 · 4,164만~4,164만」이 실제로 보인다).
★ ★ **차례가 막고 있다.**

★★ 그리고 이번에 ★ **자 하나를 맞췄다** — 목록은 「같은 차」를 **번호판**으로,
`/track` 은 **번호판＋차대**로 재고 있었다. 화면도 차대를 보게 했다(154 → **161**).
★ `/track` 의 176대와는 아직 15대 다르다 (물음 23).

★ **차례를 바꾸는 것은 규격이라 제가 안 정했다** — 물음 24.

## 4. `S46-253` 신설 — ★ 일부러 깨서 잡히는 것을 봤다

★ 검사가 ★ `tools/browser_verify.py` 를 불러 ★ 배포를 브라우저로 연다.

★★ **일부러 깼다** — ★ CSS 로만 감췄다 (`​.photo-why { display: none }`).
★ ★ 글자는 그대로 두고 ★ **눈에만 안 보이게** 한 것이다.

```
curl 로는     60건 「있다」          ← ★ 못 잡는다
브라우저로는  ★ 실패 — 「bobaedream 카드 30장에 까닭이 0 · heydealer 30장에 까닭이 0」
되돌린 뒤     ★ 1-4 는 다시 통과 · ★ 1-1 만 남는다
```

★★ **이것이 브라우저를 쓰는 까닭이다.** `curl` 로는 영원히 못 잡는다.

## 5. ★ 밝힐 것 — 운영 DB 에 계정을 만들었다

1-7·1-8 은 ★ 로그인이 있어야 눌러 볼 수 있다.
★ 마스터의 비밀번호를 저는 모르고 ★ **재설정하면 마스터가 못 들어오신다.**
★ 그래서 ★ 검사용 계정을 만들었다 —

```
python3.11 run.py admin create --name 검사자    →  account_id 6 · admin
★★ `CARWATCH_DB` 를 줬는데 ★ **그 명령이 그것을 안 본다** —
  ★ ★ 사본이 아니라 ★ **운영 DB 에 만들어졌다** [실측 09-02]
★ 비밀번호는 ★ 저장소 밖(`$CLAUDE_JOB_DIR/tmp`)에 뒀다 — ★ 커밋 안 했다
```

★ **물음 26** — 이 계정을 ★ 그대로 둘까요, 지울까요?
★ `S46-253` 이 로그인 없는 화면만 보므로 ★ 검사에는 필요 없습니다.
★ 다만 ★ 앞으로도 관리 화면을 **눌러서** 확인하려면 있어야 합니다.
★ 지우라 하시면 지웁니다 (`disabled_at` 을 채웁니다).

## 6. 곁들여 — ★ 화면 글이 거짓이 돼 있었다

1-7 을 고치고 나니 `/admin/scoring` 의 안내가 거짓이 됐다 —

```
앞   「그래서 여기서 성분을 바꾸면 ★ **총점이 그 합으로 다시 더해집니다**」
뒤   「★ `total_points`(분모)는 ★ **여기서 안 움직입니다** —
     ★ 성분 합이 분모를 ★ **넘으면 저장이 막힙니다**.
     ★ 한 축을 올리려면 ★ 다른 축을 그만큼 줄이십시오」
```

★ 화면이 거짓을 말하면 ★ 그것이 이 판이 막으려는 바로 그것이다.

## 7. 남은 것

| | 무엇 |
|---|---|
| **1-1** | ★ **안 닫혔다.** 배지는 나오나 ★ 1쪽에 안 온다(가장 앞 233위). 차례를 바꿀지 정해 주십시오 (물음 24) |
| 물음 23 | 목록과 `/track` 이 아직 15대 다릅니다 — `pair_key` 를 둘까요 |
| 물음 26 | 검사자 계정을 둘까요 지울까요 |
| 물음 22·25 | 앞 회차 것 (`S46-100` 절 차례 · 열 사이트 차종별 재기) |


---

# 8. 곁들여 — 2부 S1 의 뿌리를 찾았다

★ KB 목록은 ★ **제대로 받는다** — 로그가 그것을 말한다:

```
G80_25T      50쪽 · 매물 1487      SPORTAGE_LPI 21쪽 · 매물  624
GRANDEUR_LPG 19쪽 · 매물  498      MODEL_Y      10쪽 · 매물  221
★ 합 5,294건  (35묶음 · 봇차단 0)
```

★★ 그런데 ★ **DB 에는 627건**이다. ★ 어디서 새는지 찾았다 —

```python
ids = [i for _g, gids in by_group for i in gids]     # ★ 5,294건
for turn in range(1, bursts + 1):
    store_details(adapter, cfg, ids, want)           # ★ want = 10건
★★ ★ **매물번호를 `core_listing` 에 안 넣는다.**
   ★ 상세로 받은 10건만 들어가고 ★ 나머지 5,284건은 ★ **판마다 버려진다**
★ `--pages` 길은 이미 넣고 있었다 (`resolve_listing_id`) —
  ★ ★ 우리 차종만 받는 `--narrow` 길에만 ★ 그 걸음이 없었다
```

★ 넣게 고쳤다. ★ **차종도 함께 붙인다** — 어느 묶음이 찾았는지 아니까
(그래서 차종 미정 260건이 쌓여 있었다).

```
앞   KB 627건 · 차종 3종
```

★ 새 코드로 다시 돌고 있다. ★ 결과는 다음 회차 첫 줄에 적는다.

# 9. 시험 셋 — ★ **옛 규칙을 들고 있었다**

1-7 로 규칙이 바뀌자 시험 둘이 옛 규칙으로 실패했다.

| 시험 | 옛 규칙 | 새 규격 (명령서 1-7) |
|---|---|---|
| `test_admin_flow` | 「`total_points` **가 성분 합과 같다**」 | 「합이 분모를 **넘지 않는다**」 ＋ 「분모는 **안 움직인다**」 |
| `test_integration` 40 | HUD 10 → **25 로 바뀐다** | ① 넘으면 **안 바뀐다** ② 다른 축을 줄이면 **바뀐다**(→12) |

```
PASS  배점 — 성분 합이 분모를 넘지 않는다   합 741 / 분모 910
PASS   40 분모를 넘으면 안 바뀐다           303 · hud=10
PASS  40.5 자리를 만들면 바뀐다             303 · hud=12
```

★ ★ 막이가 ★ **길을 아주 막는 것은 아님**을 함께 시험한다 — ★ 줄이면 된다.

# 10. ★ 내가 더한 쿼리를 도로 뺐다 (`V11-34`)

1-4 의 「왜 없나」를 넣으며 ★ 쪽마다 쿼리가 하나씩 늘었다.

```
앞(이 회차 처음)   / 22 (상한 21)  ·  /detail 32 (상한 28)
뒤                / ★ 넘지 않음   ·  /detail ★ 30
★ 내 photo_note 를 넣기 전에도 ★ /detail 은 **30** 이었다 —
  ★ ★ 남은 2 는 ★ **내 것이 아니다** (재서 확인했다)
```

★ 고친 법 — ★ **사진이 있으면 안 센다.** 거의 모든 매물은 사진이 있어
그때는 한 번도 안 센다. 목록도 ★ 빈 사진이 있는 쪽에서만 센다.


# 11. 회차 끝 숫자

```
통과 502 · fatal 16 · warn 13 · 환경 차이 2 · 미실행 9
결과: 실패 — test_endtoend.py
```

| | 회차 처음 | 회차 끝 |
|---|--:|--:|
| `check_all` 통과 | 501 | **502** |
| 실패한 시험 파일 | **3** | **1** |
| `/` 쿼리 | 22 (상한 21) | **넘지 않음** |
| `/detail` 쿼리 | 32 (상한 28) | **29** |
| 브라우저로 확인한 1부 줄 | **0** | **5** (1-1 은 안 닫힘으로 확인) |

★ 남은 시험 하나는 ★ `test_endtoend.py` — ★ 남은 fatal 을 그대로 따라간다.
★ 그 fatal 넷 중 ★ `V11-34`(29 vs 28)만 화면 것이고 ★ **내가 더한 2 는 도로 뺐다**
(내 고침 전에도 30 이었다). ★ 나머지 셋은 가이드 몫이다.
