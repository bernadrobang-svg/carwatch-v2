# r723 — 옵션 코드 이름 사전 · L-3 토큰 밖 **158 → 0** · 옵션을 이름으로 건다

`SPEC-2026.08.25-r722` · 2026-08-25 04:30 · v242

## 1. ★★★ ⓐ L-3 — ★ **158 → 0** (열두 회차 만에 풀렸습니다)

★ 가이드 정정 — 「원천을 ★ `app.css` 의 `:root` 토큰으로 바꿔라.  ★ 시안이 아니다」

### 무엇이 틀려 있었나

```
tests/test_spec_ui.py:746   ★ `ref/screens/*.html` 의 색을 ★ 허용 목록으로 썼다
★★ ★ 그러면 ★ 시안이 색을 늘릴 때마다 ★ `app.css` 의 색이 ★ 조용히 늘어난다 —
   ★ ★ 「토큰 밖 색값 없음」이 ★ **아무것도 안 막는 검사**가 된다
```

### 고친 것 둘

```
① tests/test_spec_ui.py  ★ 허용 목록을 ★ `:root` 토큰 하나로.  ★ 시안을 안 본다
   ★ 아울러 ★ `#0B0D10` 과 `#0b0d10` 을 ★ 같은 색으로 본다 (대소문자로 안 가른다)
② web/static/app.css     ★ 토큰 밖에 있던 ★ **색 스물하나**에 ★ 이름을 준다
```

| | 전 | 후 |
|---|--:|--:|
| 리터럴 | 456 | 476 |
| 토큰 | 292 | ★ **312** |
| ★ **토큰 밖** | ★ **158** | ★ **0** |

```
--ink · --ink2~5 · --ok-bg · --ok-line · --no-bg · --no-line
--warn-bg · --warn-bg2 · --warn-line · --warn-line2 · --warn-text
--api-bg · --api-surface · --api-line · --api-dim · --line3 · --violet
```

★ ★ **값은 한 글자도 안 바꿨습니다** — ★ 이름만 줬습니다.
★ 화면 체계(네 막대·등급 배지)를 ★ 안 건드렸습니다 (33-1).

### 일부러 깨서 봤습니다 (원칙 6)

```
① `.x-broken { color:#abc123 }` 를 더했을 때
   → ★ FAIL  L-3  리터럴 477 · ★ 토큰 밖 1
★ 되돌린 뒤 → ★ PASS  리터럴 476 · 토큰 밖 0
```

```
test_spec_ui  ★ 실패 → ★ **통과**
```

## 2. ★★ B — 옵션 코드 이름 사전

### ⓐ 먼저 ★ 이름을 저장하게 했습니다

```
tools/collect_heydealer.py  ★ 옵션을 ★ **한글 이름으로** 저장한다
   options_standard_json  ← 장착된 것
   options_etc_json       ← 안 장착된 것
★ ★ 전에는 ★ 파서에 `options_of()` 가 있는데 ★ 수집기가 안 썼다 — ★ 0건이었다
★ ★ `choice=absent` 와 `availability=unavailable` 을 ★ 안 섞었다 (명령서 37-3)
```

```
헤이딜러 상세 88건을 다시 받아 → ★ 옵션 이름 ★ **72가지**
   내비게이션(정품) 88 · 통풍시트(앞좌석) 73 · 메모리시트(운전석) 70
   서라운드뷰 모니터(SVM) 63 · 헤드업 디스플레이(HUD) 59 · 열선핸들 51 …
```

### ⓑ `config/dictionaries/option_names.json` — ★ 신설

```
by_site.heydealer  ★ 이름 72가지 (상세 detail_info.options[].name — 원문 그대로)
by_site.encar      ★ 코드 151개 — ★ **전부 「이름 미조회」**
```

★ ★ 「이름 미조회」로 둔 까닭 —

```
★ 엔카 상세는 ★ **코드만** 준다 — `"001"` · `"004"` · `1037` · `1040`
★ ★ 상세 원문 ★ 표본 400건을 훑어 ★ 코드↔이름 짝을 찾았으나 ★ **0건**이었다
★ ★ `dict_option_code` 표에 ★ 151행이 있으나 ★ `display` 가 ★ 코드 그대로다
★ ★ 카탈로그 경로가 줄 수 있으나 ★ 아직 안 이었습니다 — ★ **0 으로 안 적었습니다**
```

### ⓒ 거르개를 ★ **이름으로** 겁니다

```
report/screens/views.py  ListingFilter.option_name
report/screens/build.py  WHERE options_standard_json LIKE ? OR options_choice_json LIKE ?
web/views.py             _option_name_buttons() — ★ 사전에 있는 이름만 단추를 낸다
web/templates/listings.html  옵션 절
```

```
★ 약자(HDA)로 안 겁니다 — ★ 이름으로 겁니다 (가이드 지시)
★ 축이 아니라 ★ 거르개입니다 — ★ HDA 축 폐기(요구 61)와 어긋나지 않습니다
★★ 사전에 없는 이름은 ★ **단추를 안 냅니다** — ★ 눌러도 0건이면 거짓말입니다
```

### 배포 실측

```
옵션 절 단추 —
   차로 유지 보조(LKA) · 스마트 크루즈 컨트롤(SCC) · 증강현실 헤드업 디스플레이
   선루프 · 전동·통풍시트(뒷좌석)
걸어 보니 —
   ?option_name=선루프                → ★ 229건
   ?option_name=스마트 크루즈 컨트롤(SCC)  → ★ 36건
```

## 3. ★★★ 「고속도로 주행보조」 — ★ 우리 자료에 ★ **그 이름이 없습니다**

★ 지어내지 않고 ★ 그대로 적습니다.

```
★ 헤이딜러 옵션 이름 72가지를 ★ 다 훑었습니다 — ★ 「고속도로」가 든 것 ★ **0가지**
★ 비슷한 것은 있습니다 —
   차로 유지 보조(LKA) 7 · 차로 이탈방지 보조 3 · 차선이탈 방지 보조 시스템(LKA) 9
   스마트 크루즈 컨트롤(SCC) 38 · 어댑티브 크루즈 컨트롤(ACC) 23 · 액티브 크루즈컨트롤(ACC) 10
   후측방 충돌방지 보조 11 · 다이내믹 레이더 크루즈 컨트롤(DRCC) 1
★★ ★ 「고속도로 주행 보조(HDA)」는 ★ **현대·기아의 이름**입니다 —
   ★ 헤이딜러가 파는 차에 ★ 그 이름으로 안 붙습니다
```

★ ★ 그래서 ★ 그 단추는 ★ **안 냈습니다.** ★ 사전에 없는 이름은 안 냅니다.
★ 여쭐 것에 올렸습니다 (7장).

## 4. ★ ⓑ 규격 표 두 줄 — ★ **다른 파일에 들어갔습니다**

★ 가이드께서 ★ `UI_REVIEW.md` 848~849행에 ★ 넣어 주셨는데,
★ **검사는 다른 파일을 읽습니다.** ★ 정확히 짚습니다 —

```
① 라우트 표 — 검사가 읽는 곳은 ★ `docs/chapters/61-web.md` 다
   validate/v11_web.py:854  `os.path.join("docs","chapters","61-web.md")`
   ★ 지금 그 파일에 `/track` 이 ★ 0곳 → ★ 「소스 37 · 표 36」

② 관리 메뉴 표 — 검사가 읽는 곳은 ★ `docs/chapters/60-admin/c-tools.md` 다
   tests/test_admin.py:35   `docs/chapters/60-admin/c-tools.md`
   ★ 지금 그 파일에 `/admin/users` 가 ★ 0곳
```

★ ★ `UI_REVIEW.md` 는 ★ 두 검사가 ★ 안 읽습니다.
★ ★ **위 두 파일에 넣어 주십시오** (규칙 2 — 제가 `docs/` 를 안 고칩니다).

★ `/admin/users` 문은 ★ 이미 달았습니다 (관리 「★ 계정」 묶음 · r718).

## 5. ★ ⓒ watch 「아래에 남는 절 셋」 — ★ 화면에 안 냈습니다

★ 가이드 — 「메모다.  화면에 내지 마라」. ★ 그대로 뒀습니다.

```
★ 다만 ★ 시안(`v3_watch_시안.html:114`)이 ★ 아직 ★ `<div class="v3-lbl">` 로 적고 있어
  ★ ★ 「시안 절 대조」가 ★ 그것을 ★ 절로 셉니다 — ★ 남은 실패 ★ 하나가 그것입니다
★ ★ 시안은 ★ 마스터 허락을 받은 것이라 ★ 제가 안 고칩니다 (요구 72)
★ ★ `v3-lbl` → `v3-note` 로 옮겨 주시면 ★ 0 이 됩니다
```

## 6. 규칙 3 — 두 도구

```
run_tests   실패 3파일 — test_admin · test_endtoend · test_web
            ruff ★ 통과
★ test_spec_ui ★ 통과로 돌아왔습니다 (L-3)
```

```
시안 절 대조   ★ 통과 38 · 실패 1 (위 5장)
전 화면 렌더   ★ 32/32
```

★ 남은 셋은 ★ **다 4장·5장의 문서 세 줄**입니다 — ★ 코드 쪽에 남은 것이 없습니다.

## 7. ★★ 마스터께 드릴 물음 — ★ 하나 (r674 ⓕ)

### ★ 엔카 옵션 코드 151개의 ★ 이름을 ★ 어디서 받습니까

```
★ 사전은 만들었고 ★ 이름을 주는 사이트(헤이딜러 72가지)는 채웠습니다.
★★ ★ 그런데 ★ **엔카 151개가 전부 「이름 미조회」**입니다 —
   ★ 엔카 매물이 ★ 4,129건으로 ★ 가장 많은데 ★ 옵션 이름으로 못 겁니다
```

| | 갈래 | 무엇을 뜻하나 |
|:--:|---|---|
| **A** | 엔카 카탈로그 경로를 받아 이름을 딴다 | ★ `catalog` 가 config 에 이미 있다. ★ 한 번 받아 사전을 채운다 |
| **B** | 짝지어진 차로 잇는다 | ★ 같은 차(번호판)가 엔카·헤이딜러에 있으면 ★ 코드와 이름을 맞댄다. ★ 지금 짝이 44대뿐이다 |
| **C** | 엔카는 코드로 두고 ★ 이름 사이트만 건다 | ★ 지금 그대로. ★ 엔카 매물은 옵션 거르개에 안 걸린다 |

```
★ 개발측 안   ★ A
   까닭       ★ `catalog` 경로가 ★ 이미 config 에 있고 ★ 한 번만 받으면 된다.
              ★ B 는 ★ 짝 44대로는 ★ 151개를 못 덮는다
★ 안 정하시면  ★ C 로 둡니다 — ★ 이름 주는 사이트만 걸립니다 (지금 배포된 것)
```

## 7a. 쌓아 둔 것

```
ⓐ 「고속도로 주행보조」 — ★ 우리 자료에 그 이름이 없다 (3장).
   ★ 시안에서 뺄지 · 비슷한 이름(차로 유지 보조)으로 낼지
ⓑ 규격 표 두 줄을 ★ `61-web.md` · `60-admin/c-tools.md` 에 (4장)
ⓒ v3_watch_시안:114 의 `v3-lbl` → `v3-note` (5장)
ⓓ S43-3 버전 어긋남 · S44-4 가 「없다」를 잡는다
ⓔ 사전의 가짜 낱말 셋 (`더`·`올`·`Electrified`) · 축 여럿이 전건 0
```

## 8. 다음

```
★ 3장 — 재수집·필터링 넷
   3-1 좁혀 받기 여덟 · 3-2 걸러 저장 셋 · 3-3 되돌리기 · 3-4 XHR
★ 3-3 뒤에 ★ 「미분류 2,930 → 몇 건」을 잽니다
```

커밋 해시 — `5bbb90f`
