# r639 — `HEAD` 가 501 이었다 · 사전 화면 배포

`SPEC-2026.08.24-r675` · 2026-08-24 02:45 · v230

## 1. 무엇을 했나

### ⓐ ★ `HEAD` 요청이 ★ 501 이었다 — ★ 사이트는 멀쩡했다

```
curl -sI https://43.201.16.78.sslip.io/listings   →  ★ 501
curl -s  … /listings                             →  ★ 200
```

★ ★ **`CLAUDE.md` 가 ★ 건강 확인으로 ★ 바로 그 명령을 적어 두었다.**
★ 그래서 ★ 사이트가 멀쩡한데 ★ 「501」로 보였다 — ★ 오래 그랬다.

```
web/server.py   do_HEAD() ★ 신설 — ★ GET 과 같은 머리글 · ★ 몸통은 안 보낸다
web/server.py   _send() — ★ head_only 면 ★ wfile.write(body) 를 건너뛴다
근거            RFC 9110 9.3.2
```

### 실측 (시험 포트 8799 — ★ 8765 를 안 뺏었다)

| | 전 | 후 |
|---|--:|--:|
| `HEAD /listings` | ★ **501** | ★ **200** |
| `Content-Length` | — | ★ **290,739** (GET 과 같다) |
| HEAD 몸통 크기 | — | ★ **0B** |
| `GET /listings` | 200 · 290,739B | 200 · 290,739B |

### ⓑ 배포

```
sudo journalctl -u carwatch --since "-10min" | grep -c POST   →  ★ 0  (마스터가 안 쓰고 계셨다)
sudo systemctl restart carwatch                               →  ★ active
```

```
curl -sI …/listings   →  ★ HTTP/2 200          ← ★ 건강 확인이 이제 맞는 값을 낸다
/ · /listings · /detail/166 · /compare          →  ★ 200 · 200 · 200 · 200
```

★ **재시작은 이번 한 번만 했다.** ★ 사전 화면(r638)과 `do_HEAD` 를 ★ 묶어서 올렸다.

## 2. 1차 결과 · 조치 후

```
       전                          후
HEAD   501 (전 경로)               200
run_tests  실패 3파일               ★ 실패 3파일 (그대로 — 내 변경과 무관)
```

```
test_endtoend · test_spec_ui · test_web
   ← ★ V11-30 (목록 다섯 절) · V11-13 (L-3 색 158) ★ 둘 다 마스터 답을 기다린다
```

## 3. 못 한 것

★ 없다. ★ 물음은 ★ 지난 회차 것(목록 거르개 A·B·C)이 ★ 그대로 서 있다 — ★ ⓕ 대로 ★ 새로 안 드린다.

커밋 해시 — `0a8eedb`
