# N — 자동화를 세웠다 · 옵션 1곳 → 7곳 · `/detail` 500 을 내가 냈다가 고쳤다

지시   `outputs/ORDER_20260829.md` · `SPEC-2026.09.08-r1211` N
버전   v395

---

## 0. ★ CSS 를 먼저 올렸습니다

```
git pull  f6725ddc 추천4 CSS 를 app.css 로 (규칙 46개)
          f3b740cc 자동화 — fill_gate · round.py
systemctl restart carwatch   → carwatch · caddy 둘 다 active
app.css   200 · ★ 267,457B · g4-* · v3-* 규칙이 붙었다
```

---

## 1. ★ 「몇 → 몇」

| 항목 | 전 | 후 |
|---|--:|--:|
| **옵션이 찬 사이트** | **1곳** | **★ 7곳 / 12** |
| 채움율 구멍(0%) | **30칸** | **★ 24칸** |
| 「되돌아간 칸」 | 2칸 | **0칸** (자의 헛것이었다) |
| `/detail/3` | **500** | **200** |
| 한 판(`round.py`) | 손으로 | **매시 정각 자동** |
| `app.css` | 안 붙음 | **267,457B** |

사이트별 옵션 —

```
kia_cpo    0 → 1,063/1,087 (97%)     bobaedream 0 → 225/241 (93%)
reborncar  0 → 1,460/1,648 (88%)     heydealer  0 →   308/351 (87%)
revolt     0 →    57/90   (63%)      encar   9,373 (56%) · kbchachacha 543 (8%)
```

---

## 2. N-1 — 한 판을 자동으로 (매시 정각)

★ 지시서는 `crontab` 을 적었는데 ★ **이 장비에 `crontab` 이 없습니다.**
★ 다른 점검들과 ★ **같은 꼴**(systemd timer)로 세웠습니다 — 여쭐 것 1.

```
/etc/systemd/system/carwatch-round.service   ExecStart=python3.11 tools/round.py {base_url}
/etc/systemd/system/carwatch-round.timer     OnCalendar=*-*-* *:00:00 · Persistent=true
systemctl enable --now  →  NEXT 20:00:00 (48분 뒤)
```

★ 재기만 합니다. ★ **고치지 않습니다** — 고치면 마스터의 작업을 끊습니다.

---

## 3. N-2 — 옵션 구멍을 메웠습니다 (G 를 앞세워서)

지시대로 ★ **G(매핑표를 파서가 읽게)를 먼저** 봤습니다.
표에는 옵션 길이 ★ **엔카 하나뿐**이었습니다. 그래서 원문을 직접 뒤졌습니다.

★ 먼저 확인한 것 — ★ **원문에 옵션이 있는가.** 없는데 「못 읽는다」고 적으면 안 됩니다.

```
kcar · reborncar · heydealer · bobaedream · kia_cpo · bmw_bps ·
hyundai_cert · revolt · volvo_selekt   →  ★ 아홉 곳 원문에 옵션 낱말이 있다
lexus_certified                        →  ★ 없다 (그 사이트가 안 준다)
```

★ 곧 ★ **파서가 안 읽고 있었던 것**입니다. 다섯 곳을 붙였습니다:

| 사이트 | 어디서 | 값도 오나 |
|---|---|:--:|
| heydealer | `detail_info.options[].content.option_name` ＋ `car_spec.option_packages` | ★ **온다** (「파노라마 선루프 (110만원)」) |
| revolt | `car_info.options[].content.option_name` | 안 온다 |
| kia_cpo | `mainOptions[].mainOption` ＋ `optionPrice` | ★ **합계로 온다** |
| reborncar | `<p class="car-option01">하이패스</p>` | 안 온다 |
| bobaedream | `class="icon-option01-sunroof"` | 안 온다 |

★ 값이 있으면 `options_choice_json` · ★ 이름만이면 `options_name_json` (지시 H).
★ 기아CPO·보배는 이름이 ★ **영문 코드**입니다 (`LEATHER_SEATS`·`sunroof`) —
★ **한글로 바꾸지 않았습니다.** 사전이 생기면 그때 붙입니다 (금지 6).

### 자를 먼저 고쳤습니다

`all_sites_report` 가 ★ `options_choice_json` **한 갈래만** 세고 있었습니다.
지시 H 는 두 갈래인데 ★ 09-06 에 `_KB_SQL` 만 고치고 ★ **여기를 안 고쳤습니다.**
★ 이름으로 받은 것이 ★ 0 으로 보이던 자리입니다.

---

## 4. ★ N-4 — 「되돌아간 칸 2」는 **자의 헛것**이었습니다

```
kia_cpo.원문   1441.7% → 1433.8%
revolt.상세      77.0% →   63.3%
```

수로 보니 —

```
revolt  상세 있는 것 ★ 57건 그대로 · 매물이 74 → 90 으로 늘었다
kia_cpo 원문 15,585줄 / 매물 1,087 = 1,434%   ← 채움율이 아니라 배수다
```

★ **분모가 큰 것**이지 되돌아간 것이 아닙니다.
★ 가이드 개정 1061 — 「건수가 달라도 검사를 실패시키지 마라 — **매물은 날마다 는다**」.

★ `fill_gate` 를 ★ **건수로 견주게** 고쳤습니다 (비율은 보이기 위한 것으로 남깁니다).
★ 되돌아감 **2 → 0**.

---

## 5. ★ 제가 낸 사고 — `/detail` 이 500 이 됐습니다

옵션에 dict 를 넣자 ★ 그것을 **표의 열쇠**로 쓰던 세 자리가 죽었습니다.

```
TypeError: unhashable type: 'dict'
  report/screens/build.py:777   opt_prices.get(c) for c in _codes
  collect/runner.py:1766        prices.get(c) for c in codes
  store/core.py:1780            str(c) for c in codes
```

★ 세 곳 다 ★ **두 꼴을 받게** 고쳤습니다 — 값이 함께 오면 그것을 쓰고, 아니면 표를 봅니다.
★ `/detail/3` **500 → 200** · `/listings` 200 · `/recommend?tab=4` 200.

★ 한 판(`round.py`)이 ★ 그것을 **바로 잡아 줬습니다** — 자동화가 값을 했습니다.

---

## 6. N-3 — 한 판의 「개발측」 칸

```
S46-280  열두 사이트에 같은 구멍  ★ 옵션 0 — 11곳 → ★ 5곳 (이번 회차)
S46-279  KB 값·항목·사진          값 24% · 주행 45% · 연식 45% · 색 11% · 사진 42%
S46-276  엔카 수집 구멍            상세율 64.1% · catalog 0.8%
S46-247  사이트에 있는 것          kbchachacha 592/6,261 (9%)
S46-227  밝힌 없음에 안 준 것      있다·없다가 겹치는 매물 1건
S46-32   생성물이 최신인가         이 회차에서 다시 돌렸다
```

---

## 7. 두 도구

```
python3.11 tools/run_tests.py
test_admin 통과 · test_admin_flow 통과 · test_collect 통과 · test_crosssite 통과
test_dict 통과 · test_endtoend 실패 · test_fixtures 통과 · test_integration 통과
test_invariants 통과 · test_pipeline 통과 · test_registry 통과 · test_report 통과
test_run 통과 · test_score 통과 · test_screens 통과 · test_spec_ui 통과
test_store 통과 · test_watch 통과 · test_web 통과
ruff 통과  (F821,F811,B905,F841,DTZ005,F401)
결과: 실패 — test_endtoend.py
fatal 넷 — V4-28 · V3-77 · V11-12 · V11-34
```

★ 넷 다 이번 작업이 만든 것이 아닙니다.
★ 제가 낸 `/detail` 500 은 ★ **이 회차에서 냈다가 이 회차에서 닫았습니다.**

---

## 8. 여쭐 것

### 1  ★ `crontab` 이 이 장비에 없습니다

`0 * * * *` 대신 ★ **systemd timer** 로 세웠습니다 (다른 점검 넷과 같은 꼴).
★ 「매시 정각·`Persistent=true`」로 같은 일을 합니다. 이대로 두어도 될지 봐 주십시오.

### 2  남은 옵션 구멍 다섯 — **넷은 자리를 아직 못 짚었습니다**

```
kcar             `reqVo.i_*OptionCd` 가 전부 None 이다 — 다른 자리를 더 봐야 한다
bmw_bps          `option_information` 안이 그림(gif)이다
hyundai_cert     `<ol class="option_01">` 안이 onclick 자바스크립트다
volvo_selekt     상세에 옵션 목록이 안 보인다
lexus_certified  ★ 원문에 옵션 낱말이 **아예 없다** — 그 사이트가 안 준다
```

★ 렉서스는 ★ 「못 읽는 것」이 아니라 ★ **안 주는 것**입니다.
★ `S46-296` 이 그것도 fatal 로 잡습니다 — **안 주는 곳을 가릴 자리**가 필요합니다.

### 3  M-1(신차출고가)은 다음입니다

이번 회차는 N 이 중심이라 옵션을 먼저 했습니다.
`price_origin_won` 이 6,061건 비어 있고, ★ 그것이 **출고가인지 등급기준가인지**
아직 확인 못 했습니다 (v394 여쭐 것 2 그대로).

## 9. 이견

없습니다. ★ 옵션 꼴을 바꾸면서 ★ **읽는 쪽 셋을 안 본 것**이 제 잘못입니다.
