# r1007 / 로드맵 차례 1 — 파서가 읽게 하라 (566점)

## ★ 안 한 것

| 무엇 | 왜 못 했나 | 며칠째 | 누가 답해야 하나 |
|---|---|---|---|
| 1-7 현대인증 남은 98점 | ★ **잰 결과 파서가 아니었다** — 사고·소유자·압류·저당은 이미 다 들어와 있다. 남은 것은 **골격 43 × 205건**이고 그것은 **차례 4(성능점검부)** 다 | 오늘 | — |
| 볼보 96건 · KB 23건 | ★ 근거 라벨이 원문에 **없다**. 「무사고」로 지어 넣지 않았다 (금지 12) | 오늘 | — |
| 차례 2 `model_catalog_key` (536점) | 차례 1 을 먼저 닫았다 | 오늘 | — |
| 차례 3·4·5 | 〃 | 오늘 | — |
| `V11-159` 상세 12절 · `S46-98` 「점보모터스」 | 규격이 바뀌며 생겼다. 차례 밖이다 | 2 | — |
| ★ `V1-21` — 엔카 목록 원문 1건의 매물 `42634604` 가 core 에 없다 | 오늘 새로 떴다 | 오늘 | — (다음 회차) |
| `V2-01` −379 · `V1-23` 17.0% | 차례 3·5 | 4·6 | ★ V2-01 은 마스터 |

## ★★ 회차마다 낼 세 수 (화면 기준)

| | v319 | 이번 |
|---|--:|--:|
| ① 아홉 사이트 A 이상 | 0 | **0건** |
| ② 전기차 A 이상 | 11 | **11건** |
| ③ 같은 차 짝 | 165 | ★ **169대** |

★ ①②는 안 움직였습니다. **주행 곡선이 지금 빗장입니다** — 아홉 사이트 매물이 주행에서 깎여
축을 채워도 A 선(65%)을 못 넘습니다. ★ 대신 **천장이 크게 올랐습니다** (아래).

## ★★★ 천장 — 「몇 → 몇」

| 사이트 | v319 | ★ 이번 | A 가능 |
|---|--:|--:|:--:|
| ★ **BMW** | 38.9% | ★ **63.2%** | 못 함 (A 선 코앞) |
| ★ **볼보** | 42.7% | ★ **53.5%** | 못 함 |
| ★ **KB차차차** | 66.9% | ★ **73.2%** | 예 |
| ★ **K카** | 71.6% | ★ **76.5%** | 예 |
| 헤이딜러 | 81.0% | 81.7% | 예 |
| 렉서스 | 44.8% | 47.2% | 못 함 |
| 현대인증 | 70.4% | 70.4% | 예 |
| 리본카 | 66.0% | 66.0% | 예 |
| 보배 | 42.7% | 42.7% | 못 함 |
| 엔카 | 100.0% | 100.0% | 예 |

---

## ★★★★ 1b — 가이드 물음에 답한다: **우리 서버에서는 된다**

★ 리본카 `/api/v1/car/carOption.rb` — ★ 가이드 창에서 **999(봇 차단)** 였습니다.
★ **우리 서버에서 200 · 16,032B 로 옵니다.**

★★ **가른 것은 쿠키입니다.** 상세 쪽을 받은 **그 세션**(`JSESSIONID`)으로 두드려야 합니다.
토큰만 옮기면 막힙니다 — `RB_TOKEN` 은 **30분짜리 JWT** 라 저장된 원문의 토큰은 이미 죽어 있습니다.

★ 두 번째로 걸린 것 — **`Accept` 헤더**입니다.
상세용 `Accept: text/html` 을 그대로 쓰면 **표본 5건 전건이 안 왔습니다.**
`application/json` 으로 바꾸니 **5/5** 왔습니다.

```
쓰는 법 (우리 코드)
  ① 상세 쪽을 받는다 — 쿠키 항아리를 쓴다 (`_OPENER`)
  ② 그 쪽에서 RB_TOKEN · _csrf 를 뽑는다
  ③ 같은 항아리로 POST — Authorization / X-CSRF-TOKEN / X-Ajax-call: true
     ＋ Accept: application/json  ← ★ 이것이 빠지면 안 온다
```

| 실측 [표본 `C26082400119`] | |
|---|---|
| 옵션 | **75종** (CE 15 · IE 26 · SE 16 · TE 18) |
| 달린 것 (`carApply="1"`) | **39종** |
| HUD · 썬루프 | ★ **둘 다 있다** |

★ 넣은 결과 — **리본카 옵션 0 → 125/125건 전건**.
`tools/collect_reborncar.py --options` 로 돌립니다.

## 1-5 KB차차차 (169점) — **덩이 하나에 다 있었다**

상세에 구조가 또렷한 덩이가 있습니다 [실측 · 표본 295건 중 **272**] —

```
보험사고정보 사고없음 전손이력 없음 침수이력 없음 용도이력 없음 소유자변경 없음
보험사고정보 조회일자 : 2026.08.12
```

| 넣은 칸 | 건 |
|---|--:|
| `accident_my_cnt` · `total_loss_cnt` · `flood_total_cnt` · `owner_change_cnt` | **272** |
| `use_gov`·`use_business` (용도이력 **없음** 일 때만) | **96** |
| `use_cd` (용도이력 **있음** — ★ 축을 안 열었다) | 176 |
| 덩이가 없어 NULL 로 둔 것 | 23 |

★★ **「용도이력 있음」은 어느 용도인지 안 알려 줍니다.** 그래서 「자가용 22점」을 안 줬습니다 —
사실만 남기고 축을 열지 않았습니다.

★★ **마스터께 올립니다** — KB 는 「내 차 피해」와 「남의 차 가해」를 **안 가릅니다.**
한 수로 옵니다. 사고 축은 `내차+타차` 를 회수로 쓰므로 그 한 수를 회수로 넣었는데,
칸 이름(`accident_my_cnt`)과 뜻이 어긋납니다. 가르는 자리가 규격에 없습니다.

## 1-6 볼보 (129점) — **라벨이 있을 때만**

딜러 글에 라벨 꼴로 옵니다 — 「- 사고이력: **무사고** / 침수 및 용도이력 **없는** 신차 컨디션」

| | 건 |
|---|--:|
| 라벨이 있어 읽은 것 | **56** |
| 그중 「무사고」 | **50** |
| 침수·용도까지 함께 읽은 것 | **32** |
| ★ 라벨이 없어 **NULL 로 둔 것** | **96** |

★★ **라벨이 있을 때만 읽습니다.** 낱말을 글 속에서 줍지 않습니다 —
보배 「판매완료 573」이 딜러 실적이었던 것과 같은 함정입니다 (0c).
★ 부위·판금·교환은 **안 읽었습니다** — `f-table` **3d** 가 「볼보는 아직 안 쟀다 ·
개발측은 표가 있는 것만 연다」입니다 (규칙 2).

## 1-7 현대인증 (98점) — **잰 결과 파서가 아니었다**

| 칸 | 상태 |
|---|---|
| `accident_my_cnt` · `owner_change_cnt` | ★ **1,155행 이미 다 있다** |
| `seizing_cnt` · `pledge_cnt` | ★ **210/211 이미 다 있다** |
| 용도 · 자차 미가입 | ★ **원문에 없다** (본문에 그 말이 없다) |
| ★ 남은 것 | ★ **골격 43점 × 205건** — ★ 성능점검부다 (**차례 4**) |

★ 곧 1-7 은 **파서를 더 파도 안 나옵니다.** 차례 4 에서 열립니다.

## 1-8 BMW (91점) — ★ **칸 이름이 틀려 통째로 버려지고 있었다**

★ `parse/bmw_bps/mapping.py` 가 `accident_raw` · `seizure_count` · `mortgage_count` ·
`transmission_raw` 에 넣고 있었습니다. ★ **`core_listing` 에 없는 칸**입니다 —
`upsert_core` 가 **조용히 버렸습니다.**

| | 전 | 후 |
|---|--:|--:|
| BMW `core_record` | **0행** | ★ **23행** |
| 압류·저당 (`seizing_cnt`·`pledge_cnt`) | 0 | ★ **17** |
| 차량번호 → `plate_hash` (`split_pii`) | 0 | ★ **23** |
| 사이트 검증 (「72가지 점검」) | 0 | ★ **23** |
| ★ **BMW 천장** | **38.9%** | ★ **63.2%** |

★ 「사고유무 무」는 **0건으로만** 넣었습니다. 「유」면 몇 건인지 모르므로 수를 지어내지 않았습니다.

## 1-9 K카 (79점)

| 넣은 칸 | 건 |
|---|--:|
| `use_gov` · `plate_change_cnt` · `owner_change_cnt` | **96** |
| ★ `not_join_json` (자차 미가입) | **68** — 「2026년03월~2026년05월」 → `["202603~202605"]` |
| `use_cd` (렌트 — ★ 축을 안 엶) | 28 |

★ 용도 코드는 `f-table` **3c** 를 그대로 옮겼습니다. `useCd` 는 **숫자라 뜻을 모르므로**
원문만 `use1_json` 에 남기고 「자가용」으로 안 매겼습니다.

## ★ STEP 41 `part`·`repair` — **사흘 막혔던 것이 열렸다**

가이드가 08-30 에 표에 두 행을 넣어 주셨습니다. `store/dictionary.py` 에 정책을 더했습니다 —
`scope`=사이트별 · `on_new`=**pending** · `on_conflict`=**halt**.

★ 결과 — `dict_enum(heydealer, part)` **7코드 pending** 으로 들어갑니다.
★ 사이트끼리 섞지 않습니다 (오판 218).

---

## 고친 파일

| 파일 | 무엇 |
|---|---|
| `parse/reborncar/mapping.py` | 1b — `option_keys` · `options_of` |
| `tools/collect_reborncar.py` | 1b — 쿠키 항아리 · `_options` · `--options` · `S46-126` 커밋 |
| `parse/kbchachacha/record.py` | 새 — 1-5 보험사고정보 덩이 |
| `parse/volvo_selekt/mapping.py` | 1-6 — 라벨만 읽는 `record_of` |
| `parse/bmw_bps/mapping.py` | 1-8 — 칸 이름 정정 · `record_of` · 번호판 |
| `parse/kcar/mapping.py` | 1-9 — 용도·미가입·번호판이력 (`f-table` 3c) |
| `store/dictionary.py` | STEP 41 `part`·`repair` |
| `tools/backfill_from_raw.py` | 갈래 넷 추가 (`bmw_bps`·`kcar`·`kb_record`·`volvo`) |
| `tests/test_dict.py` | 축 14 → 16 (까닭을 적었다) |

## 도구 · 검사 결과

```
python3.11 tools/check_all.py
  전   통과 477 · fatal 4
  후   통과 477 · fatal 4 · warn 13 · 환경 차이 1 · 미실행 8
  ★ 닫힌 것 — S46-126 (수집기가 커밋 전에 잤다 · 내가 넣은 것)
  ★ 새로 뜬 것 — V1-21 (엔카 목록 원문 1건)
  남은 fatal   S46-32 (색인 · 커밋 전 재생성) · V1-21 · V1-23 · V2-01 · V11-159

python3.11 tools/run_tests.py
  전   결과: 실패 — test_dict.py
  후   결과: 통과
```

## 마스터·가이드께 올릴 것

1. ★ **KB 가 내차/타차를 안 가른다** — 한 수로 온다. 어느 칸에 넣을지 규격에 없다
2. ★ 「용도이력 있음」(KB 176건) — 어느 용도인지 안 준다. 축을 안 열었다
3. ★ 볼보 96건 · KB 23건은 근거가 원문에 없다 — NULL 로 뒀다
4. ★ 볼보·BMW 부위 코드표 (`f-table` 3e·3f) 가 오면 골격·외판이 열린다
5. ★ ①②를 움직이려면 **주행 곡선**이 빗장이다 — 축을 채워도 A 선을 못 넘는다
