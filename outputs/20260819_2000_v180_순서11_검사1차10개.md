# 순서 11 — 검사 1차 10개 (개정 407 · 요구 19건)

`SPEC-2026.08.19-r409` · 2026-08-19 20:00 · v180

---

## 1. 무엇을 하려 했나 (지시 원문)

```
11  검사 1차 10개 — inbox/ORDER_checks_1st.md
    V11-45 · V11-103 · V11-85 · V3-58 · V3-66 · V3-50 · V11-100 · V11-96 · V11-94 · V7-14
    ★ 4~10 을 먼저 끝내고 11 은 따로 커밋한다
    ★ V3-58 은 SOH 없는 전기차 표본으로 되살린다
    11번은 검사마다 「무엇으로 실패시켰나」를 표로 적는다 (S42-2)
```

---

## 2. 1차 결과 (고치기 전 숫자)

```
check_all   통과 306 · fatal 2 · warn 7 · 환경 차이 1 · 미실행 5
run_tests   결과: 통과
검사        열 개 중 아홉이 없음.  V3-58 만 있었고 config 만 보고 있었다
```

---

## 3. ★ 지시와 규격이 셋에서 다릅니다 — 규격을 따랐습니다

| 검사 | `ORDER_checks_1st.md` | **`docs/` (정본)** |
|---|---|---|
| `V11-45` | 실패·진행·최초 계정 안내가 화면에 나오는가 | **CLI 로만 되는 기능이 없는가 (부트스트랩 제외)** |
| `V7-14` | `gone` 매물을 목록에서 지우지 않는가 | **재등록 횟수가 화면에 나오는가** |
| `V3-58` | 배터리 SOH 가 가점인가 | f-table **「SOH 가 가점으로 더해지는가」** · a-frame:718 은 아직 **「전기차에 SOH 축이 적용되는가」**(개정 380 이전 문구) |

규칙 1 대로 `docs/` 를 따랐습니다. `V11-45` 는 지시의 「최초 계정 안내」도
함께 재게 했습니다 — 그 문장이 같은 「필수」 묶음에 있습니다.

★ **`30-score/a-frame.md:718` 은 개정 380 이 폐기한 문구입니다.** 정정해 주십시오.

---

## 4. ★ 검사마다 「무엇으로 실패시켰나」 (S42-2)

| 검사 | 통과 표본 | ★ 실패 표본 | 무엇으로 실패시켰나 | 낸 말 |
|---|---|---|---|---|
| `V11-45` | 지금 소스 | `cli_screens.json` `pending` 에서 한 줄 뺌 | 화면 없는 CLI 를 하나 늘렸다 | 「tools/build_index.py — 대응 화면이 없다」 |
| `V11-103` | 지금 소스 | `web/app.py` 에서 「그동안의 가격 변동을 알 수 없습니다」 제거 | 문구를 지웠다 | 「가격 변동이 멈춘다는 것을 안 적는다」 |
| `V11-85` | 지금 소스 | `build.py:643` 「세부등급 없음」 대체 규칙 제거 | 빈 세부등급을 그대로 뒀다 | 「23건 · 「1.5 E-TECH 아이코닉 2WD」 에 세부등급이 없다」 |
| `V3-58` ① | 지금 소스 | `scoring.json components.battery_soh = 30` | SOH 를 **축**으로 만들었다 | 「battery_soh 가 components 에 있다 — 축이 아니다」 |
| `V3-58` ② | 지금 소스 | ★ **SOH 없는 전기차 5건의 분모를 705 로** (사본 DB) | 가점이 분모를 늘렸다 | 「3 — SOH 가 없는데 분모가 705.0 다 (675 여야 한다)」 |
| `V3-66` | 지금 소스 | `state.accident_curve` 두 번째 칸 22 → 23 | 곡선을 한 칸 고쳤다 | 「state.accident_curve 의 점수 […] 와 같은 표가 f-table 에 없다」 |
| `V3-50` | 지금 소스 | `listings.html` 의 `record_mismatch`·`mismatch=1` 제거 | 화면에서 표시와 필터를 뗐다 | 「목록에 어긋남 표시가 없다」 · 「따로 볼 수 있는 길이 없다」 |
| `V11-100` | 지금 소스 | `listings.html` 의 `option_price_won` 제거 | 합계를 뺐다 | 「목록 화면이 옵션 합계를 안 낸다」 |
| `V11-96` | 지금 소스 | `listings.html` 의 `♡` 전부 제거 | ♡ 를 없앴다 | 「목록에 ♡ 가 없다」 |
| `V11-94` | 지금 소스 | `recommend.html` 「왜 이 순위인가」 → 「비고」 | 이유 칸을 없앴다 | 「매물마다 「왜 이 순위인가」 칸이 없다」 |
| `V7-14` | 지금 소스 | `watch.html` 의 「재등록」·`relist_times` 제거 | 횟수를 뺐다 | 「관심 화면에 「N번 재등록」이 없다」 |

**되돌린 뒤 열 개 다 통과했습니다.**

★ `V3-58` 은 **두 가지로** 되살렸습니다. 지시가 「있는 표본만 보면 통과한다」고
경고한 그대로였습니다 — 전에는 `config` 만 봤습니다. 이제 **SOH 가 없는
전기차 40건을 실제로 꺼내** 분모가 675 인지, 가점이 안 붙었는지, `result_axis`
에 SOH 축 행이 없는지를 봅니다.

---

## 5. ★ 검사가 찾아낸 것 셋 — 만들자마자 나왔습니다

### ① `V3-50` — 성능부와 보험이력이 **857건** 어긋납니다

```
둘 다 받은 매물            1,949건
성능부 무사고 · 보험 수리비 있음   836건   ← 「흔적이 없는데 비용이 있었다」
성능부 사고 · 보험 흔적 없음       21건
합                          857건 (44.0%)
```

★ **이 대조를 아무도 한 적이 없었습니다.** 둘 다 갖고 있으면서 안 맞춰
보고 있었습니다. 목록에 「성능부≠보험」 배지를 달고 `?mismatch=1` 로
그것만 따로 볼 수 있게 했습니다.

### ② `V11-45` — CLI 로만 되는 도구가 **11개** 있습니다

```
tools/build_index.py · classify_registry.py · classify_unclassified.py
daily_check.py · daily_enqueue.py · light_check.py · repair_facet_chunks.py
trace_fill.py · trace_verify.py · verify_axes.py · weekly_check.py
```

`S27` 이 이미 「미착수 11」로 세고 있었는데 **아무도 안 봤습니다.**

```
한 것   config/cli_screens.json 에 pending 으로 명시했다.  빚을 적어 뒀다
       ★ V11-45 는 이 목록을 실패로 세지 않되,
         목록에도 screens 에도 없는 것이 새로 생기면 그 자리에서 문다
안 한 것 어느 화면인지 제가 정하지 않았다 (규칙 2) — 8절 ①
```

### ③ `V11-45` — 규격이 요구한 안내가 화면에 없었습니다

`60-admin/a-auth.md` 「필수 화면에 「최초 계정은 서버에서 만듭니다」를 안내한다」.
로그인 화면은 「첫 관리자는 CLI 에서만 만듭니다」라고만 했습니다.

★ 고치면서 **`V11-30` 이 물었습니다** — 시안이 「CLI 에서만」을 요구합니다.
**둘 다 적었습니다.**

---

## 6. 무엇을 했나 — 만든 것

### 검사 열 개

```
V11-45   CLI 로만 되는 기능이 없음        FATAL  contract
V11-103  목록이 오래되면 화면에 나옴        FATAL  code
         ★ 문자열이 있는지가 아니라 실제로 오래된 시각을 넣어 문구가 만들어지는지 본다
V11-85   트림에 세부등급이 포함됨          FATAL  code
V11-100  목록에 옵션 개수와 합계가 나옴      FATAL  code
V11-96   ♡ 가 제목 줄에 있음             FATAL  code   ★ 표 머리말에서 자리를 본다
V11-94   추천 조건이 화면에 적혀 있음        FATAL  code
V3-50    성능부와 보험이력이 어긋난 건을 셈   WARN   external
V3-66    각 축의 계산이 f-table 과 같음     FATAL  code
V7-14    재등록 횟수가 화면에 나옴          FATAL  code
V3-58    (있던 것) SOH 없는 전기차로 실제로 잰다
```

### 기능 — 검사가 잴 것이 없어 함께 만든 것

```
store/core.py     record_mismatch_sql() · record_mismatch_count() · relist_counts()
                  ★ SQL 을 화면·검사에 두지 않는다.  세는 곳과 뽑는 곳이 같은 것을 본다
report/screens/   ListingRow.record_mismatch · WatchRow.relist_times/low/high
web/templates/    listings.html 「성능부≠보험」 배지 + ?mismatch=1
                  watch.html 「2번 재등록 · 3,200만 → 2,990만」
                  login.html 「최초 계정은 서버에서 만듭니다」
web/app.py        banner_of() — 세어 놓은 것으로 머리말을 정한다 (조회에서 분리)
web/views.py      ?mismatch=1 필터
config/cli_screens.json   ★ 새로 만듦 — CLI ↔ 화면 지도 (S27 · V11-45 가 함께 본다)
```

★ **`_SCREEN_FOR` 를 `config` 로 옮겼습니다.** `check_src` 안에 있어서
`V11-45` 가 쓰려면 import 해야 하는데, `check_src` 는 **import 만 해도
전체 검사가 도는** 스크립트입니다. 두 벌로 적으면 한쪽만 늘어납니다.

★ **`값이 안 바뀌었으면 「3,200만 → 3,200만」을 안 냅니다.**

---

## 7. 조치 후 결과 — ★ 실제 출력

```
python3.11 tools/check_all.py

통과 315 · fatal 2 · warn 7 · 환경 차이 1 · 미실행 5

■ FATAL — 고쳐야 한다
  V1-11     예외로 종료된 실행이 없음 — 0단계
  V4-11     unclassified 존재 — 32
```

```
python3.11 tools/run_tests.py
결과: 통과

python3.11 tools/check_src.py
  S15 계층 의존 (STEP 15)   통과  통과 16 · 미착수 0 · 실패 0
결과: 실패 — S13(20) / S16(57) / S25(1) / S26(21)
```

```
sudo systemctl restart carwatch   ★ 재시작 전 최근 15분 POST 0건
curl https://43.201.16.78.sslip.io/listings   200
```

| | 전 | 후 |
|---|--:|--:|
| check_all 통과 | 306 | **315** |
| fatal | 2 | **2** |
| run_tests | 통과 | **통과** |
| S16 남은 검사 | 64 | **57** |
| 새 검사 | — | **9개 (V3-58 은 넓힘)** |

### ★ 만들다 낸 결함 셋 — 전부 제 것

| # | 무엇 | 잡은 것 |
|---|---|---|
| ① | `report` 가 `store` 의 **대문자 상수**를 import | **S15 계층 의존** — `record_mismatch_sql()` 함수로 (`our_fault()` 와 같은 자리) |
| ② | `V10-37` 이 안내 문구의 「복사」로 거짓 통과 | 되살리기가 잡았다 (순서 10) |
| ③ | `V11-100` 이 씨앗 DB 에서 실패 | 옵션 원문이 없는 것은 **자료가 없는 것**이지 결함이 아니다 — 화면 검사만 남겼다 |

---

## 8. 못 한 것 · 막힌 것 · 여쭐 것

### 여쭐 것 ①  ★ CLI 전용 도구 11개 — 어느 화면입니까

`config/cli_screens.json` 의 `pending` 에 적어 뒀습니다. 제가 안 정했습니다.
제 짐작은 아래와 같습니다 — **정해 주시면 그대로 넣겠습니다.**

| 도구 | 짐작 | 왜 제가 안 정했나 |
|---|---|---|
| `classify_registry` · `classify_unclassified` | `/admin/registry` | 그 화면이 결과를 이미 냅니다 |
| `daily_check` · `light_check` · `weekly_check` | `/admin/status` | 4시간 점검이 거기 뜹니다 |
| `daily_enqueue` | `/admin/run` | 큐 화면입니다 |
| `verify_axes` | `/admin/tools` | 「검증 실행」과 겹칩니다 |
| `build_index` · `trace_fill` · `trace_verify` · `repair_facet_chunks` | **없음** | 개발측 도구로 보입니다 — 화면에 두면 안 될 수도 있습니다 |

★ **화면 이름만 적고 단추를 안 만들면 그것이 「선언과 실제의 괴리」입니다.**
그래서 짐작으로 채우지 않았습니다.

### 여쭐 것 ②  성능부 ↔ 보험 어긋남 857건 — 판정에 반영합니까

지금은 **화면에 표시만** 합니다. 규격(`d-history`)도 「낸다 · 표시한다」까지입니다.
**44%가 어긋납니다.** 이것을 사고 축 판정에 쓸지는 정해 주셔야 합니다.
★ 어느 쪽이 맞는지는 사람이 봐야 하므로 자동 감점을 넣지 않았습니다.

### 여쭐 것 ③  `price_curve` 는 죽은 config 로 보입니다

```
config/scoring.json price_curve      11구간
analyze/axis/price.py score_from_curve  → analyze_price 가 부른다
analyze_engine                        ★ analyze_price 를 아무도 안 부른다
                                        (engine.py:21 주석에만 이름이 있다)
```

`V3-66` 에서 뺐습니다 — 축 곡선이 아니라 넣으면 거짓 실패가 됩니다.
**지우는 것이 맞습니까, 살리는 것이 맞습니까.** 제가 안 지웠습니다.

### 여쭐 것 ④  규격 문구 셋이 지시와 다릅니다 (3절)

### 못 한 것

없습니다. 순서 11 은 닫혔습니다. **다음은 순서 12** — `V3-76`·`V3-77` 입니다.

### 그대로 남은 것

```
check_all fatal 2   V1-11 · V4-11 (가이드 몫)
check_src           S13(20) · S16(57) · S26(21)
K카 대기 5개        V9-01 · V9-03 · V9-04 · V9-05 · V9-08 — 지시대로 안 만들었습니다
검사 나머지 257건    S42 기준에 안 걸리면 만들지 않습니다 (지시 §5)
```

---

## 9. 커밋

```
V11-45·V3-50 외 8: 검사 1차 10개 (순서 11)
해시  235f796
```
