# v345 — 2부 S9 · 등급 현실화

명령서 2부 S9 — 「★ **등급 현실화** — `grade_gates.state_ok_floor`:
★ 상태 축 여덟이 정상이면 ★ 바닥 `C`. ★ 그 위는 값·취향이 올린다」
끝났다는 뜻 — 「★ **D 이하 비율이 59% 아래로**」

## 1차 결과

```
등급: E 2,445 · D 2,111 · EXCLUDED 1,927 · F 1,546 · C 1,063 · B 736 ·
     PENDING 354 · A 172 · G 104
★ D 이하 ★ **6,206 / 10,458 = 59.3%**
```

## ★ 찾은 것 — ★ **규격은 있는데 코드가 안 읽었다**

```
config/scoring.json   grade_gates.state_ok_floor   ★ **있다** (개정 1093)
   {"grade": "C", "state_axes": [여덟], "need_ratio": 0.9}

$ grep -rn "state_ok_floor" --include=*.py .
   validate/v0_guide.py:5708     ← ★ **검사만 있다**
   (판정 코드에는 ★ 한 군데도 없다)
```

★★ 검사 `S46-248` 은 ★ 「그 규칙이 **있는지**」만 봤다.
★ ★ **판정이 그것을 쓰는지는 아무도 안 봤다.**
★ ★ ★ 그래서 ★ 규격이 들어온 뒤에도 ★ 등급이 하나도 안 움직였다.

## 무엇을 · 왜 했나

`score/grade.py` `grade_of()` 에 바닥을 넣었다.

```
★ 규칙은 ★ `config/scoring.json` 이 정본이다 — ★ **등급도 축 이름도 코드에 안 박았다** (S14)
★ 「정상」의 뜻 — ★ 그 축이 ★ 만점의 `need_ratio`(0.9) 이상을 받았는가
★ 여덟 중 ★ `need_ratio` 만큼이 정상이면 ★ 바닥을 준다
★ 규칙이 없으면 ★ `None` — ★ 코드가 지어내지 않는다
```

★ 손으로 시험했다 —

```
여덟 다 만점 →  C
사고만 0     →  None   ★ 흠이 있으면 바닥을 안 준다
아무것도 없음 →  None
```

★ 실제 매물로도 재 봤다 — ★ 상태 축 여덟이 다 정상인 매물이 ★ **1,217건** 있다.

## ★ 곁들여 찾은 것 — ★ 등급은 `S10` 이 매긴다

★ `S9` 를 돌렸는데 ★ 분포가 **한 톨도 안 바뀌었다.**
★ ★ `S9` 는 ★ **축**을 셈하고 ★ 등급은 ★ **`S10`(채점)** 이 매긴다.
★ ★ ★ 「재판정」이라 하면 ★ **둘 다** 돌려야 한다.

## 조치 후 결과

```
등급: PENDING 5,388 · E 2,652 · D 2,194 · EXCLUDED 2,005 · C 1,107 ·
     F 1,007 · B 956 · A 260 · G 44 · ★ **S 2**
★ D 이하 ★ **5,897 / 15,615 = 37.8%**
```

| | 앞 | 뒤 | 규격 |
|---|--:|--:|:--:|
| **D 이하 비율** | 59.3% | **37.8%** | 59% 아래 ★ **넘었다** |
| A 이상 | 172 | **262** (S 2 포함) | |
| C | 1,063 | **1,107** | |
| B | 736 | **956** | |
| 판정된 매물 | 10,458 | **15,615** | |

★ 배포 브라우저 — ★ 목록 앞 10 등급 `['S','A','A','A','A','A','A','A','A','A']`
★ ★ 매물 ★ **10,446건** (앞 5,549 — ★ KB 수집이 들어왔다)

## 여쭐 것

```
★ `PENDING` 이 ★ 5,388건으로 늘었다 (앞 354).
  ★ ★ 새로 들어온 KB 5,530건이 ★ 상세를 아직 안 받아 ★ 확인율이 낮아서다.
  ★ ★ ★ 이것은 ★ **S5(상세 수집)** 이 돌면 줄어든다 — ★ 결함이 아니다.
★ 다만 ★ 마스터께서 ★ 「왜 갑자기 미확정이 많으냐」 하실 수 있어 적어 둔다
```
