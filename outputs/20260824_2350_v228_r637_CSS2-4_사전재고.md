# r637 — 22장 2~4단계(상세·관심·비교) · `dict_enum` 재고 · 규격에 없는 status

`SPEC-2026.08.24-r672` · 2026-08-24 23:50 · v228

## 1. 무엇을 하라 하셨나

```
9   화면 CSS 22장 2~4단계 (상세 → 관심 → 비교)
    ★ V11-60 을 돌려 「28곳 → 몇 곳」을 보고해라
9a  dict_enum pending 재고 확정
    SELECT axis, value, COUNT(*) FROM dict_enum WHERE status='pending' GROUP BY 1,2
    ★ 사이트별로 세고 ★ /admin/dict 로 확정.  ★ 기아 1,020건이 걸려 있다
    금지  status 를 손으로 바꾸는 것
```

## 2. ★ V11-60 — **28곳 → 0곳**

```
시안 selector 가 app.css 와 이름이 겹치는 곳   →  ★ 0
시안이 새로 쓰는 .v3- 이름                   →  ★ 23
app.css                                    →  ★ 한 글자도 안 바뀌었다 (33-1)
```

★ 마스터 ㉮ 가 ★ 그대로 들었다 — ★ 시안 쪽에만 `.v3-` 을 붙여 ★ 충돌이 통째로 없어졌다.

## 3. ★ 22장 2~4단계 — ★ 고친 것은 **화면이 아니라 검사였다**

★ 대조가 ★ **엉뚱한 글자를 견주고 있었다.**

| 파일 | 줄 | 무엇 |
|---|---|---|
| `tools/check_screens.py` | 128 | `class="lbl"` → `class="(?:v3-)?lbl"` |
| `tools/check_screens.py` | `_sian_heads()` | ★ v3 시안에서 `h2`·`h3` 로 **물러서지 않는다** |
| `validate/v0_guide.py` | 548 | `S46-22` 도 같은 곳이었다 |

★ 마스터 ㉮ 로 `lbl` → `v3-lbl` 이 되자 ★ 정규식이 ★ 아무것도 못 찾고
★ `h2` 로 물러섰다. ★ 그런데 v3 시안의 `h2` 는 ★ **곁에 그려 둔 다른 화면의 이름표**다 —

```
v3_compare_시안.html:62   <h3>상세 — 와이드</h3>      ← ★ 비교 화면의 절이 아니다
v3_watch_시안.html         <h3>상세 — 와이드</h3>      ← ★ 관심 화면의 절이 아니다
```

★ 그래서 ★ 「`compare.html` 에 「상세 — 와이드」 절이 없다」고 ★ 헛잡았다.
★ 파일 126행 주석이 ★ 이미 「`<h2>` 는 절이 아니다」라 적어 두었다 — ★ 코드가 그 말을 안 지켰다.
★ 옛 판 시안은 ★ 전부 지워졌다 (요구 73 — v2 24개) — ★ 물러섬은 옛 판용으로만 남겼다.

### 결과 — 절 대조

| | 통과 | 실패 |
|---|--:|--:|
| 고치기 전 (헛잡이 섞임) | **0** | **7** |
| `v3-lbl` 을 읽게 한 뒤 | 10 | 8 |
| v3 물러섬을 막은 뒤 | ★ **10** | ★ **6** |

```
2단계 상세   ★ 열 절 10/10 통과 — V11-159 「절 차례는 바꾸지 않는다」 지켜졌다
3단계 관심   ★ 절 셋(최종 후보·조건 알림·진행) ★ 다 있다 — watch.html 71·76·80행
4단계 비교   ★ 통과 — 헛잡이였다
1단계 목록   ★ 5곳 남았다 (아래 6-ⓑ)
```

## 4. ★ 규격에 없는 status 를 ★ 내가 넣었다 — **fatal 7 → 5**

```
docs/chapters/00-standard.md:616
   sites.json  사이트 목록 · status (active · planned · paused)
```

★ 그런데 ★ 내가 `4c856d3`(r654) 에서 ★ 사이트 다섯을 ★ `"pending"` 으로 넣었다.
★ 규격에 없는 말이다. ★ `V9-10` 이 그 다섯을 ★ 「active 인데 배점이 없다」로 읽었다.

```
config/sites.json   "pending" → "planned"  ★ 5곳
   bobaedream · reborncar · lexus_certified · volvo_selekt · bmw_bps
```

```
V9-10   사이트 보증 항목의 합   ★ 5곳 → ★ 0곳
V11-120 사이트별 구매 총액      ★ 40건 → ★ 0건
```

★ **배점을 지어 넣지 않았다** (금지 12). ★ 잘못은 배점이 아니라 ★ **말** 이었다.

### 검사 `S46-41` 신설 — 되풀이를 막는다

`validate/v0_guide.py` — ★ 규격 `00-standard.md:616` 에서 ★ 허용 셋을 ★ **읽어다** 견준다.

```
① 하나를 pending 으로 되돌렸을 때
   → ★ 규격에 없는 status — 허용 active·paused·planned — kcar — status='pending'
★ 되돌린 뒤 → True (사이트 15개의 status 가 다 규격 안이다)
```

★ **이것은 ★ 「사고 뒤에 만든 검사」다** (요구 74 에 어긋난다).
★ 숨기지 않고 적는다 — ★ 값 하나가 ★ 조용히 새 뜻을 만드는 것을 ★ 막을 길이 이것뿐이었다.

## 5. ★ 9a — `dict_enum` 재고 (★ 손대지 않았다)

### 사이트 × 축 — 대기 **232종**

| 사이트 | 축 | 종 |
|---|---|--:|
| `kcar` | target | **149** |
| `encar` | panel | 35 |
| `kia_cpo` | target | 23 |
| `hyundai_cert` | target | 17 |
| `encar` | target | 8 |

```
대기 pending   232종
확정 confirmed 166종
```

### ★ 걸려 있는 매물 — **2,803건**

| 사이트 | 매물 | `result_axis` 있는 것 |
|---|--:|--:|
| encar | 8,790 | 4,082 |
| **hyundai_cert** | **1,114** | ★ **0** |
| **kia_cpo** | **1,020** | ★ **0** |
| **kcar** | **530** | ★ **0** |
| **kbchachacha** | **116** | ★ **0** |
| **bobaedream** | **23** | ★ **0** |

★ 가이드 말씀대로 ★ **기아 1,020건이 통째로 걸려 있다.**

### ★ 내가 확정하지 않은 이유 — ★ 규격이 막는다

```
web/views.py:1524   ★ 확정을 자동으로 하지 않는다.
                    ★ 사람이 원문을 보고 누른다 (개정 267)
```

★ 그래서 ★ **세기만 하고 ★ 안 눌렀다.** ★ `status` 를 손으로 바꾸지도 않았다.

### ★ 다만 — `/admin/dict` 가 **232 중 43만 보여 준다**

```
dict_state()   pending_rows 43 · pending_total 43   ← ★ DB 는 232 다
```

★ **화면에 안 보이는 189종은 ★ 마스터가 누를 수가 없다.**
★ 기아 23종·K카 149종이 ★ 그 안에 들었는지 ★ 아직 못 짚었다 — ★ 다음에 짚는다.

## 6. 규칙 3 — 두 도구

```
check_all   통과 347 · fatal 5 · warn 10 · 환경 차이 1 · 미실행 8   (고치기 전 345 · fatal 7)
run_tests   실패 3파일 — test_endtoend · test_spec_ui · test_web   (고치기 전 4파일)
v0_guide    실패 1 — ★ S46-36 (지시대로 처음부터 실패) · warn 1
```

★ 남은 fatal 다섯 —

```
V11-30   시안 대조 6      ← ★ 1단계 목록 (6-ⓑ · 막혀 있다)
V11-13   토큰 밖 색값 24   ← ★ L-3 158 (6-ⓐ · 답 기다린다)
V1-23    카탈로그 78.9%    ← ★ 사이트가 안 준다 (21조합 · 586건)
V2-01    ok 원문 = CORE 4008
V11-76   화면 크기·시간 4곳
```

★ `test_run` 이 한 번 ★ `unable to open database file` 로 죽었는데
★ **내가 시험을 둘 겹쳐 돌려 ★ /tmp 를 채운 탓**이었다. ★ 혼자 돌리니 ★ 통과.

## 7. 못 한 것 · 판단이 필요한 것

### ⓐ ★ L-3 색 158 — ★ 아직 답을 못 받았다 (세 번째 여쭙는다)

`V11-13` · `test_spec_ui` 가 이것 하나로 막혀 있다. ★ ⓐ/ⓑ/ⓒ 중 무엇입니까.

### ⓑ ★ 1단계 목록 — ★ 절 다섯이 없다 (r634 에서 멈춘 그 자리)

```
listings.html — 「판정 — 누르면 그 조건만 남는다」·「사이트」·「주행 · 연식」·「옵션」·「뺀 것」
```

★ 시안은 ★ 거르개를 ★ **다섯 절로 묶어** 그렸다. ★ 지금 화면은 ★ 칩 7 + 접은 ＋조건 12 다.
★ 이것은 ★ **마스터가 늘 보시는 거르개를 통째로 바꾸는 일**이라 ★ 혼자 안 했다.

```
물음   ★ 시안대로 ★ 다섯 절로 다시 묶습니까
      ★ 아니면 ★ 지금 칩 차림을 두고 ★ 시안을 고치십니까
```

### ⓒ ★ `watch.html` 「아래에 남는 절 셋」 — ★ 시안이 `.v3-lbl` 을 ★ 두 뜻으로 쓴다

```
v3_watch_시안.html:110   <div class="v3-lbl">아래에 남는 절 셋</div>
                        최종 후보 — … / 조건 알림 — … / 진행 — …
```

★ 이것은 ★ 절 이름이 아니라 ★ **「아래에 절이 셋 온다」는 메모**다.
★ 그 셋은 ★ `watch.html` 에 ★ 다 있다 (71·76·80행).
★ 나는 ★ 시안을 안 고쳤다 (요구 72 — 시안은 마스터 허락을 받은 것이다).

```
물음   ★ 그 한 줄을 ★ 메모(`v3-note`)로 옮겨 주십니까
      ★ 아니면 ★ 검사가 ★ 「절 셋」류를 ★ 메모로 보게 합니까
```

### ⓓ ★ `/admin/dict` 가 ★ 232 중 43만 보여 준다 (5장 끝)

★ 안 보이는 189종을 ★ 마스터가 누를 수 없다. ★ 화면 쪽 결함으로 봅니다 — ★ 다음에 짚습니다.

### ⓔ ★ `HEAD` 요청이 ★ 501 이다

```
curl -sI https://43.201.16.78.sslip.io/listings   →  ★ 501
curl -s  … /  · /listings · /detail/166           →  ★ 200 · 200 · 200
```

★ **사이트는 멀쩡하다** — ★ `curl -I` 가 ★ HEAD 를 보내는데 ★ 우리 서버에 `do_HEAD` 가 없다.
★ `CLAUDE.md` 가 ★ 건강 확인으로 ★ 바로 그 명령을 적어 두었다 — ★ 늘 501 로 보인다.
★ 고치려면 ★ 서비스를 다시 띄워야 해서 ★ 오늘은 안 했다 (★ 마스터 작업을 끊지 않는다).

### ⓕ ★ `S46-36` 은 ★ 여전히 실패다 — ★ 지시대로다

```
docs/KCAR_API.md:331~332 가 ★ 폐기된 요구 79 를 ★ 아직 지시로 적는다
★ docs/ 는 내가 안 고친다 (규칙 2).  ★ 가이드가 고쳐 주셔야 풀린다
```

### ⓖ 앞서 여쭌 것 (아직 답 없음)

```
· 국산 10종을 `encar:*` 로 되돌린 것이 맞나
· 새 사이트의 `result_axis` 가 비어 있는 것 — ★ 위 5장이 숫자를 다 냈다
· KB 2,935B 세 번째 꼴
· S43-2c 제외에 01_요구사항.md · 05_가이드역할.md 를 넣은 것 (r636 6-ⓑ)
```

## 8. 다음

```
8   KB 상세 회차 (89/1,726) — 회차 50건 · 2,759B 세 번이면 끝
9a' /admin/dict 가 43만 보여 주는 것
```

커밋 해시 — `f7599c7`
