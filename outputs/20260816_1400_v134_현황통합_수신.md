# 현황 통합 — 2026-08-16

**GitHub 소스를 직접 읽어 확인했습니다.**

```
저장소   https://github.com/bernadrobang-svg/carwatch-v2  (public · main)
최신     04:06  「v134 기준선 — AWS 배포 시점」
★ 그 뒤 push 가 없습니다.  작업은 했는데 안 올라왔습니다
```

---

# 1. 이미 고쳐진 것 — 확인했습니다

## ★ `B-7` 「지금 조건으로」가 축을 안 넘긴다 — 고쳐졌습니다

```python
# store/watch.py
AXIS_BUCKETS = {
    "1":       "a.value > 0 AND a.excluded = 0",
    "0":       "a.value = 0 AND a.excluded = 0",
    "unknown": "a.value IS NULL OR a.excluded = 1",
    "na":      "a.value = -1",
}
```

```html
<!-- web/templates/listings.html:75-76 -->
<input type="hidden" name="axis"   value="{{ filter.axis }}">
<input type="hidden" name="bucket" value="{{ filter.bucket }}">
```

**「HUD 있는 차가 새로 뜨면 알림」이 됩니다.** 주석에 `STEP 149g` 를 인용한 것도 맞습니다.

## `I-6b` 마지막 관리자 — 문구가 있습니다

```
"마지막 관리자의 역할은 내릴 수 없습니다"
"마지막 관리자는 중지할 수 없습니다"
```

**다만 「대안 안내」는 아직 없습니다. 아래 미처리에 남깁니다.**

---

# 2. ★ 아직 안 고쳐진 것 4건

## ① `_lookup` 이 대괄호 첨자를 못 읽는다 (N-1)

```python
# web/template.py:105  — GitHub 현재 상태
for part in path.split("."):     # 점만 처리
```

```
AWS 서버에서는 dashboard 템플릿을 우회해서 넘겼습니다
근본은 안 고쳐졌습니다.  다른 화면에서 또 납니다
```

## ② `V11-34` 가 `view_listings` 만 센다

```python
# validate/v11_web.py:659  _query_budget_check
from report.screens.build import view_listings
```

```
주석에는 「실측 08-15: dashboard 는 21회였다」라 적어 놓고
검사는 여전히 listings 만 셉니다
dashboard 를 고쳤어도 다른 화면이 넘으면 못 잡습니다
```

## ③ 전 차종 수집에 확인 절차가 없다 (H-3)

```
규격   STEP 149l — 높음: 문구 입력 또는 영향 명시
시안   「진행하려면 아래에 all 을 입력하십시오」
실제   라디오 버튼 하나
```

## ④ 마지막 관리자 거부에 대안이 없다 (I-6b)

```
실제   「마지막 관리자는 중지할 수 없습니다」
규격   「다른 계정을 관리자로 올린 뒤 바꾸십시오」 (STEP 149m)
```

---

# 3. 이번에 제가 처리한 규격 7건

| 개정 | 무엇 |
|:--:|---|
| 251 | E-2 블록 복구 — **제 실수였습니다** |
| 252 | `raw_response.origin` 에 `import` · `browser` 추가 |
| 253 | 라우트 표에 `/admin/import` · `/admin/collect` 등재 |
| 254 | **CLI 전제 전수 개정** — 9곳 |
| 255 | 부트스트랩만 예외 명시 |
| 256 | **`/why` 절 순서를 v2 실물로** |
| 257 | **형상 관리 · 작업 기록 규정 신설** · `S25`·`S26` |

## 254 — CLI 전제 9곳

```
b-ops:204   「신호는 화면에, 실행은 CLI」        절 제목이 그랬습니다
b-ops:209   화면에 「CLI 명령 문구」를 낸다
b-ops:210   원문 확인 → CLI 재수집
a-auth:85   잠기면 「CLI 로 가야 한다」
a-auth:107  계정을 「CLI 또는」 화면에서
verify:158  실행 「run.bat screens」
web:1162    관리자 0명이면 「CLI 로만 복구」
B-config    잠기면 「CLI 로 가야 한다」
00:170      Presentation = 화면 · 리포트 · CLI
00:776      「실행 | CLI 배치」  →  「화면 · 타이머」
```

## 256 — `/why` 순서

```
v2 실제 순서를 규격으로 삼습니다
  1 매물요약 · 2 무엇을조회 · 3 축별판정 · 4 엔카진단
  5 확인못한것 · 6 왜N순위 · 7 비용 · 8 옵션 · 9 참고자료
근거   v2 가 새 절 셋을 넣었으므로 읽는 순서가 달라졌다.  실물이 옳다
```

## 257 — 형상 관리 · 작업 기록

```
S25   커밋되지 않은 변경이 없는가
S26   작업마다 outputs/ 기록이 있는가
```

**마스터가 심으신 규칙 6·8 을 규격으로 올렸습니다. 메모리에만 있으면 사라집니다.**

---

# 4. 마스터가 직접 하실 것 하나

```bash
cd ~/v2
python3.11 -c "
import sqlite3
c=sqlite3.connect('carwatch.db')
c.execute(\"UPDATE recalc_job SET status='cancelled' WHERE status='queued'\")
c.commit(); print('정리됨')"
```

**Claude Code 가 권한에 막혔습니다. 이게 남아 있으면 시험이 계속 깨집니다.**

---

# 5. 다음 순서

```
1  CORS 확인                     ← 최우선.  이걸로 뒤가 갈립니다
2  /admin/collect 또는 /admin/import
3  실제 조회 → 저장 → S4~S10
4  미처리 4건 (①~④)
5  S24 시험 격리
6  STEP 136d 나머지 (migrate · build_dict · sync_registry · run_tests)
7  V11-45 구현
```

---

# 6. Claude Code 에게 줄 지시

```
inbox 확인해라. spec_251_257_all.tar.gz 하나에 개정 251~257 이 다 있다.
앞서 받은 spec_246_247 · spec_248_250 · spec_251_253 · spec_254_255 가
있으면 이걸로 덮어라 — 상위 집합이다.

먼저 push 해라. 04:06 이후 작업이 GitHub 에 안 올라와 있다.
가이드가 GitHub 을 읽는다 (개정 257 · S25).

그 다음 순서
1) CORS 확인 — web/static/cors_test.html
   마스터가 휴대폰에서 열어 결과를 알려준다
   그 결과 전까지 /admin/collect 를 만들지 마라

2) 기다리는 동안 미처리 4건을 고쳐라
   ① web/template.py:105 _lookup 이 대괄호 첨자를 못 읽는다
      {{ d.gc[g] }} 가 그대로 찍힌다. 근본을 고쳐라
   ② validate/v11_web.py:659 V11-34 가 view_listings 만 센다
      전 화면을 세도록. ROUTES 를 훑어라
   ③ /admin/run 전 차종에 확인 절차가 없다 (STEP 149l · 높음)
      시안대로 「all 을 입력하십시오」
   ④ 마지막 관리자 거부에 대안 문구가 없다 (STEP 149m)
      「다른 계정을 관리자로 올린 뒤 바꾸십시오」

3) 개정 256 대로 /why 절 순서를 확인해라
   규격을 v2 실물에 맞췄다. 지금 화면이 그 순서인지 검사로 확인

4) 개정 257 대로 S25 · S26 을 validate 에 구현해라

작업마다 outputs/ 기록을 남기고 push 해라.
```
