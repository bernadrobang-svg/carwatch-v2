# `STEP 136c` — `/admin/collect` 브라우저 수집

작업 시각 2026-08-16 15:00~15:20 · 소스 v134 · 실행 `python3.11`
커밋 `be89f5f`

---

## 1. 무엇을 하려 했나 (지시 원문)

> ```
> 1  CORS 확인          ← 최우선.  이걸로 뒤가 갈립니다
> 2  /admin/collect 또는 /admin/import
> ```
> CORS 결과는 마스터가 알려줄 때까지 `/admin/collect` 를 만들지 마라.

마스터 회신: **`Count = 148592`**

## 2. 무엇을 했나

### ① CORS 결론 — 장애물이 아니었다

휴대폰 실측이 `Count = 148592` 로 왔다. 서버에서도 헤더를 재 보았다.

```
/v1/readside/vehicle/39554653   200   access-control-allow-origin: *
/search/car/list/mobile         407   access-control-allow-origin: http://43.201.16.78:8765
```

**엔카는 두 엔드포인트 모두 CORS 를 허용한다. 407 은 CORS 가 아니라 AWS IP 차단이다.**
그래서 `STEP 136c` 가 성립한다.

### ② `/admin/collect` (커밋 `be89f5f`)

| 파일 | 무엇 |
|---|---|
| `contracts.py:118` | `ORIGIN_BROWSER` |
| `sql/ddl/01_raw.sql:26-29` | `origin` CHECK 에 `browser` 추가 |
| `store/raw.py:60-63` | `ORIGIN_BROWSER` |
| `store/raw.py:171-215` | `save_browser_raw` · `save_browser_facet` |
| `store/adminops.py:236-283` | `save_browser_catch` — 저장하고 그 단계를 연다 |
| `store/adminops.py:300-303` | `mark_step_imported(actual=…)` 로 일반화 — `browser` 도 남긴다 |
| `run.py:200-231` | `_collect_urls()` — 어댑터가 주소를 만든다 |
| `web/app.py:173-182 · 255` | `collect_urls` 주입점 |
| `web/routes.py:73-75` | `/admin/collect` GET·POST · `ROLE_ADMIN` · 운영 |
| `report/screens/admin.py:43 · 512-534` | 메뉴 · `collect_state()` |
| `web/views.py:823-861` | `admin_collect` 핸들러 |
| `web/templates/admin_collect.html` (신규) | 화면 + 바닐라 JS |
| `validate/v11_web.py` | `V11-43` · `V11-44` 정의·구현·호출 |

**계층을 지켰다.** `web` 은 `adapters` 를 못 부른다 (`LAYER_ALLOW`). 그래서 주소 조립을
`run.py` 가 주입한다 — `plan`·`fetch` 를 주입하는 것과 같은 방식이다 (STEP 15a).

**JS 는 「브라우저만 할 수 있는 일」 하나만 한다** (개정 248).

```
한 파일 · CDN 없음 · npm 없음
하는 일   fetch 로 엔카를 부르고, 받은 글자를 폼에 넣는다
안 하는 일 화면을 그리는 것 — HTML 은 서버가 만든다
JS 꺼짐    「직접 열기」 링크로 새 탭에서 연 뒤 붙여넣기로 저장한다
```

### ③ 내가 만든 회귀 3건을 같은 작업에서 잡았다

| 검사 | 무엇 | 어떻게 |
|---|---|---|
| `V11-19` | 「템플릿에 `fetch(` 가 있으면 실패」 | 개정 248 이 JS 를 허용했다. `admin_collect.html` 만 예외로 열고 나머지는 그대로 금지 |
| `V11-33` | 「저장 없이 성공 메시지」로 오인 | 쓰기 함수 목록에 `save_browser_catch`·`import_listings` 등재 |
| `S7` 매직 넘버 3건 | `0.3` 을 코드에 적었다 | `cfg["interval_sec"][0]` 를 그대로 쓴다 — 기본값을 코드에 두지 않는다 |

## 3. 왜 그렇게 했나 (규격)

- `STEP 136c` — ②를 건너뛰지 않는다 · 원문 그대로 · `origin='browser'` · `interval_sec` 준수 ·
  `407`·`403` 을 삼키지 않는다 · 서버가 다시 부르지 않는다 · 검산 `V11-43`·`V11-44`
- 개정 248 · 14장 — 바닐라 JS 한 파일 · JS 꺼져도 화면이 돈다 · JS 로 화면을 그리지 않는다
- 개정 259 — 밖에서 확보한 단계는 `audit_validation` 에 남긴다. 목록이면 `S1`, facet 이면 `S2`
- STEP 15a · `LAYER_ALLOW` — `web ← contracts·errors·store·score·report`. `adapters` 가 없다

## 4. 1차 결과 (작업 전 숫자)

```
python3.11 tools/check_all.py
통과 175 · fatal 4 · warn 7
  V4-25 · V3-30 · V3-32 · V11-12(표에만 있다: /admin/collect)

python3.11 tools/run_tests.py
결과: 실패 — test_admin.py, test_admin_flow.py, test_endtoend.py,
            test_integration.py, test_spec_ui.py, test_web.py
  test_admin  코드 13 · 표 14 · 표에만 ['/admin/collect']
  test_web    Route 수 소스 30 · 표 31
  test_endtoend  fatal ['V11-12']

python3.11 tools/check_src.py
S16 실패 5 — V10-22 · V10-23 · V11-43 · V11-44 · V11-45
```

## 5. 조치 후 결과

### 실물 서버에서 저장 경로를 눌러 확인했다

```
GET  /admin/collect            200 · 조회 단추 16개 (collect_group 8 × 목록·facet)
POST previewed 없이 저장 시도    400   ← ②를 건너뛰면 막힌다 (V11-44)
POST 확인 뒤 저장               303
  raw_response  origin='browser' · endpoint='facet' · http_code=200 · request_url 있음
  raw_facet     axis_count=2 · request_url 있음
  STEP53-S2     actual='browser' · passed=1
  V11-43        통과
```

★ 이 시험은 합성 facet 으로 했고, **운영 DB 에 남지 않도록 넣은 뒤 다시 걷어냈다.**

```
raw_facet 0 · browser raw 0 · STEP53-S2 없음
```

### 검사

```
python3.11 tools/check_all.py
통과 177 · fatal 3 · warn 7 · 환경 차이 2 · 미실행 2
  V4-25 · V3-30 · V3-32

python3.11 tools/run_tests.py
결과: 실패 — test_admin_flow.py, test_integration.py, test_spec_ui.py

python3.11 tools/check_src.py
S7 · S14 · S23 · S24 · S26 통과
S16 실패 3 — V10-22 · V10-23 · V11-45
```

| 지표 | 작업 전 | 작업 후 |
|---|--:|--:|
| `check_all` 통과 | 175 | **177** |
| `check_all` fatal | 4 | **3** |
| `run_tests` 실패 파일 | 6 | **3** |
| `S16` 미구현 검사 | 5 | **3** |

`test_admin` · `test_web` · `test_endtoend` 가 **통과로 바뀌었다.** 셋 다 `/admin/collect`
하나에 걸려 있던 것이고, 화면이 생기자 함께 풀렸다.

## 6. 못 한 것 · 막힌 것 · 판단이 필요한 것

### ① 남은 fatal 3건은 전부 실물 facet 대기다

```
V4-25  color_ext · color_int · fuel 사전이 비었다   ← facet 축이다
V3-30  halt 축의 사전이 비었다
V3-32  seizing null 판정
```

화면은 준비됐다. **휴대폰으로 `/admin/collect` 에 들어와 `KOLEOS_HEV facet` 의 「조회」를
누르고, 뜬 내용을 확인한 뒤 사유를 적어 저장**하면 `S2` 가 열린다.
이어서 같은 화면에서 `KOLEOS_HEV 목록 1쪽` 을 저장하면 `S1` 도 열려
`S3`(사전) → `S4` 가 이어진다.

### ② `V10-22` · `V10-23` · `V11-45` 미구현

`V10-22`·`V10-23` 은 큐 A/B 결정 대기다 (지시 4번 — 「고르기 전에 만들지 마라」).
`V11-45`(CLI 로만 되는 기능 없음)는 `STEP 136d` 와 함께 한다.

### ③ 큐 A/B — 내 판단은 그대로다

**A 이되 systemd 가 아니라 웹 서버 안의 소비 스레드.** 근거는 앞 기록에 적었다.
전 차종 3.8시간을 HTTP 요청이 붙들 수 없고, `recalc_job` 에 진행 컬럼이 이미 있으며,
개정 250 이 CLI 를 금지했기 때문이다. 아울러 `_gate()` 가 메뉴 분류를 무시하고
전 화면을 잠그는 것은 A/B 와 무관하게 고쳐야 한다 (STEP 132a 금지 조항).

### ④ 남은 시험 3건은 이번 범위 밖이다

```
test_admin_flow   등록부 재동기화
test_integration  S5-8 안 돈 단계는 미실행
test_spec_ui      A-4 · S5-11 · 가이드-3b
```
