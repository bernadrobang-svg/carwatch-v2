# v350 — 2부 S11 · 「죽은 검사」가 죽은 게 아니었다

명령서 S11 — 「★ 죽은 검사 **42개** · 시험 실패 **3**」 끝났다는 뜻 — 「둘 다 준다」

## 1차 결과

```
docs/CHECKS.md   「② 죽은 검사 — 통과도 실패도 한 적 없다」  ★ **36개**
★ 그 36개가 ★ **전부 `tools/check_src.py` 것**이었다
```

## ★ 재 봤더니 — ★ **돈다**

```
$ python3.11 tools/check_src.py
  S35-1 자기 칸만 고침            통과  통과 1 · 미착수 0 · 실패 0
  S36-1 「정식 서비스 착수」 목록…  통과  통과 1 · 미착수 0 · 실패 0
  …
  결과: 실패 — S7 매직 넘버(370) / S13(19) / S14(11) / S16(56) / S25(1) / S26(184)
```

★ 통과도 하고 실패도 한다. ★ **한 번도 안 돈 것이 아니다.**

## 까닭 — ★ 「돈다」와 ★ **「남는다」는 다르다**

```
색인 `tools/build_index.py` `last_runs()` —
  ★ `audit_validation` **표**를 보고 ★ 「돌았나」를 정한다

check_all.py  → ★ `save_results()` 로 ★ **남긴다**
check_src.py  → ★ 화면에만 낸다.  ★ **표에 안 남긴다**
```

★★ 그래서 ★ 36개가 ★ 「한 번도 안 돌았다」로 세어졌다.
★ ★ 「있다」와 「돈다」가 다르다는 것은 규격이 적어 뒀는데,
★ ★ ★ **「돈다」와 「남는다」도 다르다.**

## 무엇을 했나

`tools/check_src.py` 의 `say()` 가 ★ 결과를 `audit_validation` 에 남긴다.

```
★ 못 남겨도 ★ **검사를 멈추지 않는다** — ★ 남기기는 곁일이다
★ `run_id` 는 `src-YYYYMMDDTHHMMSS` — ★ `check_all` 판과 섞이지 않는다
```

## 조치 후 결과

```
audit_validation 에 남은 S 검사  ★ **36개**
docs/CHECKS.md  「② 죽은 검사」  ★ 36 → **0**
```

| | 앞 | 뒤 |
|---|--:|--:|
| **죽은 검사** | 36 | **0** |
| 표에 남는 `check_src` 결과 | 0 | **36** |
| 검사 총수 | 555 | 555 |

## 남은 것 · 여쭐 것

```
★ 「⑤ 규격에 있는데 코드에 없는 검사」가 ★ **35개** 남아 있다 —
  ★ ★ 그것은 ★ **만들어야 하는 것**이라 ★ 이 회차 몫이 아니다.
  ★ ★ ★ V0-01·V0-02·V0-03 · V1-22 · V2-03 · V3-12 … 서른다섯

★ `check_src` 가 낸 실패 여섯은 ★ 오래된 것이다 —
  S7 매직 넘버 370 · S26 작업 기록 184 · S16 검증 코드 대조 56 …
  ★ ★ 이번에 ★ **처음으로 표에 남았다** — ★ 이제 추이를 잴 수 있다
★ 안 ① 다음 회차부터 ★ 그 수를 줄여 간다
★ 안 ② 규격이 「지금은 안 본다」고 정한 것이 있으면 알려 주십시오
★ ★ **안 ①로 갑니다**
```
