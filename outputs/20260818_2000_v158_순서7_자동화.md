# 순서 7 — 자동화 · `V10-26`~`V10-30`

2026-08-18 20:00 · v158 · **SPEC-2026.08.18-r365**

`inbox/ORDER_00_순서.md` **7번**

---

## 1. 무엇을 하려 했나 (지시 원문)

```
1  목록 저장이 끝나면 나머지가 이어서 돈다 (STEP 136g)
   ★ 마스터가 「이어서 해라」를 말하지 않아도 되게
2  매일 04:00 (STEP 136h)
3  재수집·재파싱·재판정을 가른다 (개정 317)
```

---

## 2. ★★ 자동화는 이미 돌고 있었습니다 — 다만 전부 실패했습니다

```
carwatch-daily.timer   04:00:36 에 돌았습니다
                       「하루치를 큐에 넣었습니다 — 77eb7b1d」
목록 저장 → 큐          store/adminops.py 가 reason='listing_updated' 로 넣습니다
```

**그런데 큐 작업 42건이 전부 `failed` 였습니다.**

```
40건   S5: 선행 조건 미충족: 선행 단계 미완료: S4
```

★ **원인은 순서 1 에서 적은 그것입니다** — `S4` 가 옛 봉투를 다시 읽어
불변 가드에 걸려 멈추고, 그 뒤 `S5`·`S6`·`S9`·`S10` 이 줄줄이 실패했습니다.

**자동화가 없던 것이 아니라, 첫 단계가 막혀 아무 일도 못 하고 있었습니다.**

---

## 3. ★★★ S4 를 고쳤습니다 — 마스터 목록이 들어왔습니다

### 원인

`S4` 는 목록 봉투를 **순서 없이 전건** 다시 읽었습니다.
08-16 봉투가 08-17 봉투 뒤에 적용되면 **옛 값이 새 값을 덮으려 하고**,
불변 가드(STEP 29)가 그것을 막아 단계 전체가 멈췄습니다.

```
42478722   08-16 'AWD' → 08-17 '스탠다드'
S4 가 08-16 을 다시 읽어 '스탠다드' → 'AWD' 를 쓰려다 멈춤
```

### 고침

```
collect/runner.py s4
  ① 봉투를 fetched_at 오름차순으로 읽는다 — 마지막이 가장 새 관측이다
  ② 이미 더 새 관측(last_seen)이 있으면 그 봉투는 적용하지 않는다
  ③ 몇 건을 왜 건너뛰었는지 화면에 낸다 — 조용히 버리지 않는다
```

★ 처음 고쳤을 때 **새 매물 38건이 안 실렸습니다.**
`resolve_listing_id` 가 방금 만든 빈 행은 `last_seen` 이 「지금」이라
**모든 옛 봉투가 오래된 것으로 보였습니다.**
`status='new'` 인 행은 건너뛰지 않게 다시 고쳤습니다.

### 결과

```
S4   중단 → 865건 적재 → (다시 돌려) 38건
매물  active 3,528 → 3,557 · 오늘 새로 뜬 것 38건
V1-21  실패 → 통과
```

★ **마스터가 12:37 에 보낸 목록 73쪽이 8시간 만에 들어왔습니다.**

---

## 4. 검사 다섯

```
V10-26  목록 저장 후 큐에 작업이 들어가는가
V10-27  중간 실패에서 다음 단계로 넘어가지 않는가
V10-28  타이머가 겹쳐 돌지 않는가 (daily_enqueue · systemd timer 등록)
V10-29  목록 저장이 전건 재수집을 부르지 않는가 (detail_refresh_days)
V10-30  재판정이 수집 없이 도는가 (run.py --only S9)
```

config 둘을 넣었습니다.

```
endpoints.json  detail_refresh_days 7
                ★ 3,470건 × 9종 = 3만 호출이다.  매일 할 일이 아니다
admin.json      collect_daily_at "04:00"
```

---

## 5. ★ 만들다 낸 결함 둘 — 전부 제 것

| # | 무엇 | 증상 |
|---|---|---|
| ① | 빈 행의 `last_seen` 을 관측 시각으로 봤다 | 새 매물 38건이 영영 안 실린다 |
| ② | `listing_updated` 를 `trigger` 로 찾았다 | 그것은 `reason` 이다.  `trigger` 는 누가 눌렀나(manual·schedule)다 |

★ 그리고 `V10-26` 이 **자료를 요구해** 빈 시험 DB 에서 걸렸습니다.
`check_all` 은 시험 DB 로 돕니다 — 「목록을 받은 적이 없으면 해당 없음」으로
고쳤습니다.  **검사는 「할 수 있는가」를 봐야지 「한 적이 있는가」를 보면
안 됩니다.**

---

## 6. 조치 후 결과 — ★ 실제 출력

```
python3.11 tools/check_all.py
통과 274 · fatal 6 · warn 7 · 환경 차이 1 · 미실행 2
  V1-16 · V2-01 · V2-18 · V4-11(32) · V3-38 · V11-77

python3.11 tools/run_tests.py
결과: 실패 — test_endtoend.py
```

| | 전 | 후 |
|---|--:|--:|
| check_all 통과 | 268 | **274** |
| fatal | 7 | **6** |
| 시험 실패 | 1 | **1** |
| 큐 작업 성공 | 0 / 42 | **S4 가 풀림** |
| active 매물 | 3,528 | **3,557** |

---

## 7. 못 한 것 · 판단이 필요한 것

### ① 새 매물 38건은 아직 상세가 없습니다

```
detail_status  전건 NULL
```

`S5`(상세 4종)를 돌리면 엔카를 38 × 4 = 152번 부릅니다.
**돌릴까요.**  자동으로는 04:00 타이머가 가져갑니다.

### ② 큐 작업 42건이 `failed` 로 남아 있습니다

이제 원인이 풀렸으니 다음 타이머에서 새 작업이 성공할 것입니다.
**옛 실패 기록은 지우지 않았습니다** — 기록을 지우는 것은 되돌릴 수 없습니다.

### ③ `V10-27` 은 「넘어가지 않았다」만 봅니다

「선행 단계 미완료인데 done 이 된 작업이 있는가」를 봅니다.
지금 0건입니다 — 하지만 **그것은 애초에 다 failed 였기 때문**입니다.
정상 흐름에서 다시 확인해야 합니다.

### ④ 그대로 남은 fatal 6

```
V1-16 · V2-01 · V2-18 · V4-11(32 · 마스터 몫) · V3-38 · V11-77
```
