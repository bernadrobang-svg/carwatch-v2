# 0 잣대 · 0e 찌꺼기 끊기 (ORDER r950)

## 안 한 것

| 무엇 | 왜 못 했나 | 며칠째 | 누가 답해야 하나 |
|---|---|--:|---|
| 1 배포 | ★ 서버는 새 틀을 들고 있고 `S46-100` 도 통과한다. 화면에 「지우기」가 안 보이는 것은 **관심 12건이 다 `active`** 라서다 — 그 카드는 팔린 매물의 것이다 | — | **마스터** — 그래도 다르면 무엇이 보이시는지 |
| 2 KB 부위별 | 사고 있는 매물을 찾은 뒤 (지시대로) | 오늘 | 개발측 |
| 3 V1-23 | 0e 뒤 | 오늘 | 개발측 |

---

# 0 — ★ 잣대가 달랐다.  **내가 틀렸다**

## ★ 세는 법을 적는다 — 앞으로 **화면 기준**으로 낸다

| 수 | 내가 쓰던 것 | ★ 화면 기준 | 왜 달랐나 |
|---|--:|--:|---|
| ③ 전기차 A 이상 | **195** | ★ **13** | ★ 아래 |
| ② 같은 차 짝 | **275** | ★ **245** | 화면 조건(리스·관문·접기)을 안 거쳤다 |
| ① 아홉 사이트 A | 0 | **0** | 같다 |

### ★★ ③ 이 185 나 달랐던 까닭 — **「가솔린+전기」를 전기차로 셌다**

화면이 쓰는 조건을 뜯어 보니 `l.fuel_raw IN (…)` 로 **정확한 낱말만** 봅니다.
제 것은 `fuel_raw LIKE '%전기%'` 였습니다 —

```
내 LIKE 가 잡은 것 (A 이상)
   가솔린+전기   ★ 122건   ← ★ 하이브리드다.  전기차가 아니다
   전기            73건
```

★ 거기에 `target_key LIKE '%_EV%'` 를 또 더해 부풀렸습니다.
★★ 「전기차」를 **넓게 잡아 좋아 보이게** 만든 셈입니다. 정정합니다.

### ② 275 → 245

`plate_hash` 묶음 수를 셌는데, 화면은 **거르개를 거친 뒤** 보이는 매물만 셉니다
(리스·렌트 제외 · 관문 제외 · 같은 차 접기 · 팔린 것 제외).

### ★ 앞으로 이렇게 센다

```python
from report.screens.build import _listings_where
from report.screens.views import ListingFilter
# ★ 화면이 쓰는 조건 그대로 — ★ 손으로 SQL 을 짜지 않는다
parts, args = _listings_where(ListingFilter(fuel="electric", min_grade="A"))
```

★ **`_listings_where` 를 그대로 부릅니다.** 손으로 조건을 짜면 또 어긋납니다.

---

## ★ 116 이 어디 것인가 — ★ 내가 표를 잘랐다

```
원문 encar → 매물 kbchachacha      882
             hyundai_cert          731
             bobaedream            385
             heydealer             140
             lexus_certified        80
             bmw_bps                72
           ★ reborncar              60   ← ★ 표에서 빠졌다
           ★ kcar                   56   ← ★ 표에서 빠졌다
합계                              2,406
```

★ 앞 기록에서 `limit 6` 으로 뽑아 놓고 **여섯 줄이 전부인 것처럼** 적었습니다.
★ 116 = **리본카 60 ＋ K카 56** 입니다. 합계 2,406 은 맞았습니다.

---

# 0e — ① 끊었다 · ② 비웠다

```
① 남의 매물을 가리키던 원문 2,406건 — ★ 끊었다
   확인 — 남의 매물을 가리키는 원문 ★ 0건
   확인 — 원문 줄 수 ★ 195,028줄 (한 줄도 안 지웠다 · P3)
② 근거 없는 행 969건 — ★ detail_status 를 비웠다
   ★ status 는 안 건드렸다 — 목록에서 빼지 않았다
```

★ ②를 셀 때 **열쇠를 `(site, source_id)` 로** 했습니다.
`listing_id` 로 세면 **11,511건**이 나옵니다 — 0d 에서 배운 그 자리입니다.

## ③ 다시 받기 — 사이트마다 끊어서 (도는 중)

★ **200 으로 안 가릅니다** (마스터 지시). `undo_wrong_gone.probe()` 를 같이 씁니다 —
사이트마다 「그 파서가 값을 뽑는가」로 가릅니다.

★ 셋으로 나눕니다 — **살아 있다 / `detail_gone` / `unreachable`**.
못 받은 것은 「없다」로 안 적고 다음 바퀴가 다시 봅니다. 잇달아 사흘이면 `unreachable` 입니다.

### ★ 두 가지가 걸렸습니다 — 적어 둡니다

**① `detail_gone` 이라는 값을 못 씁니다**

```
CHECK constraint failed: detail_status IS NULL OR detail_status IN
  ('ok','empty','not_found','error','not_requested')
```

★ 마스터께서 말씀하신 「`detail_gone`」이 그 다섯에 없습니다.
★ **새 이름을 만들지 않았습니다** (규칙 2) — 뜻이 같은 **`not_found`** 를 씁니다.
사이트가 「없다」고 답한 것이 곧 `not_found` 입니다.
★ 새 값을 쓰라 하시면 DDL 을 고쳐야 하니 **말씀해 주십시오.**

**② 969 → 514 로 줄어 보입니다**

①②를 돌리면 969건의 `detail_status` 가 비워집니다. 그다음 ③이 그것을 다시
찾아야 하는데, 처음 도구가 `detail_status='ok'` 만 봐서 **0건**을 냈습니다.
「상세 원문이 없는데 목록에는 사는 것」으로 고쳤더니 **514건**이 나왔습니다.

★ 969 − 514 = **455건**은 `out_of_scope` 입니다 — **우리 대상이 아닌 차**라
상세를 받을 까닭이 없습니다. 그래서 안 받습니다.

```
받을 것 514건
  kbchachacha 169 active · 57 new     kcar 142 gone · 19 active
  heydealer    85 active ·  4 gone    hyundai_cert 14 active · 1 new
  bobaedream    8 gone ·  2 active    reborncar 8 gone · 1 active
  kia_cpo       4 new
```

---

# ★ 잣대를 코드로 굳혔다 — `tools/three_numbers.py`

★ 손으로 SQL 을 짜면 또 어긋납니다. **`_listings_where()` 를 그대로 부릅니다** —
화면이 바뀌면 이 수도 함께 바뀝니다.

```
★ 회차마다 낼 세 수 (화면 기준)
  ① 아홉 사이트 A 이상        0건
  ② 같은 차 짝             245건
  ③ 전기차 A 이상           13건
     (참고 — 화면 전체 5,670건 · 전기만 550건)
```

★ 마스터께서 보신 값(0 · 13)과 **맞습니다.**
