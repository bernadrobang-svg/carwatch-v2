# 축 점수(`result_axis.score`)를 넣었다 — ★ 08-16 이래 한 번도 안 넣고 있었다

```
지시   마스터 09-05 ① — 「★ 지시문 맨 앞에 올린다 — ★ `runner.py:1855` 에 ★ `score` 를 넣어라.
                      ★ 값은 `v.values[comp]` × 규칙 → 점수.  ★ `max_points` 는 이미 넣고 있다」
       마스터 09-05 ② — 「★ 검사를 세운다 — ★ `result_axis.score` 가 NULL 이면 실패」
       마스터 09-05 ③ — 「★ 왜 여태 안 올렸는지 적는다」
검산   ★ `S46-273` (새로) · `V9-09`
날짜   2026-09-05 01:35 (KST)
```

## 1. 1차 결과

```
result_axis 행        502,936
  ★ `max_points` 가 빈 것   ★ **0건**      ← 넣고 있었다
  ★ `score` 가 빈 것        ★ **502,936**  ← ★ **전부 비어 있었다**
```

★ 한 줄에 ★ 두 칸을 적으면서 ★ **한쪽만** 넣고 있었다.

## 2. ★★★ ③ 왜 여태 안 올렸나 — ★ **처음부터 안 넣었다**

```
git log -S "score        REAL"        sql/ddl/04_result.sql
   → 5db8c493  v134 기준선 — AWS 배포 시점  (2026-08-16 12:50)
git log -S "prio,excluded,max_points"  collect/runner.py
   → 5db8c493  v134 기준선 — AWS 배포 시점  (2026-08-16 12:50)
git log -S "excluded,max_points,score" collect/runner.py
   → ★ **없다**
```

★ 곧 ★ **칸은 기준선에 있었고 ★ 넣는 코드는 ★ 한 번도 없었다.**
★ ★ 되돌아간 것이 아니다 — ★ **애초에 안 만들었다.**

★★ **그런데 ★ 읽는 자리는 있었다** — ★ 두 곳이 ★ 스무 날 동안 ★ NULL 을 읽고 있었다.

| 자리 | 무엇에 쓰나 |
|---|---|
| `report/screens/build.py:1183` `SITE_WARRANTY_ORDER` | ★ **목록 정렬의 갈림쇠** — 「같은 점수면 사이트 보증이 높은 쪽이 앞」 |
| `validate/v9_multisite.py:247` `V9-09` | ★ 그 정렬이 도는지 보는 검사 |

```
★★★ 마스터 08-28 — 「★ **최고급 우선이야**」.  ★ 그 말이 정렬에 들어간 자리다.
  ★ ★ 그런데 ★ `score` 가 늘 NULL 이라 ★ `ORDER BY … w DESC` 가 ★ **아무 일도 안 했다.**
  ★ ★ ★ 화면은 ★ 「사이트 보증으로 가른다」고 적어 두고 ★ 안 가르고 있었다 —
    ★ ★ ★ ★ 이 프로젝트가 막으려는 ★ **「선언과 실제의 괴리」** 그대로다
```

★ **내가 왜 못 봤나** — ★ `V9-09` 가 ★ **통과하고 있었다.**
★ ★ 같은 비율인 짝 안에서 ★ 순서가 안 뒤집히면 통과인데,
★ ★ ★ `w` 가 전부 NULL 이면 ★ **뒤집힐 일이 없어** ★ 늘 통과였다.
★ ★ ★ ★ **아무 일도 안 하면 통과하는 검사**였다.

## 3. ① 무엇을 했나

| 파일 | 무엇 |
|---|---|
| `score/scorer.py` | ★ `axis_points(v, policy, snapshot)` 를 ★ **새로** — 규칙 한 자리 |
| 〃 `score()` | ★ 그 표를 쓰게 했다 — ★ 두 벌로 안 적는다 |
| `collect/runner.py` (S9) | ★ `result_axis` INSERT 에 ★ **`score`** 를 더했다 |

★ 규칙 (`axis_points`)

```
① policy.skipped(comp)          → ★ 표에 아예 안 넣는다 (전 매물 스킵)
② excluded · 값 없음 · None      → ★ **None**  (「0점」과 「안 봤다」는 다르다)
③ 인증중고차 몫 — 근거 없는 축이 0 → ★ `배점 × 비율` (마스터 08-28)
④ 그 밖                          → ★ `v.values[comp]` 그대로
```

★★ **규칙을 한 자리에 둔 까닭** — ★ 채점기가 `earned` 를 만들 때 쓰는 것과
★ **같은 표**여야 한다.  ★ 여기서 다시 셈하면 ★ 화면과 표가 갈린다.

## 4. 조치 후 결과 (실측)

```
판    S9 ok **502,936** · S10 ok 17,962
result_axis 502,936행
  ★ `score` 가 빈 것        28,586  ← ★ **전부 `excluded=1`** 이다 (「안 봤다」가 맞다)
  ★ `excluded=0` 인데 빈 것  ★ **0**
S46-273  ★ 통과 — 「축 점수가 다 차 있다 (474,350행)」
```

★ **`earned` 가 안 바뀌었는지 견줬다** (리팩터가 값을 흔들지 않았나)

```
판 앞에 200건을 적어 두고 판 뒤에 다시 읽었다
  ★ 같음 **200** · 다름 **0**   (earned · denominator · grade 셋 다)
합계 earned  4,892,747.7 / 17,962건
run_tests    실패 1파일 (test_endtoend) — ★ 이 회차 앞과 같다
```

★ **정렬이 살아났다**

```
warranty.site 축   17,962행 · ★ score 가 다 찼다 · 값이 ★ 4가지로 갈린다
같은 비율인 무리    3,092개  ← ★ 정렬이 실제로 갈리는 자리
V9-09              ★ 통과 · ★ **13,812짝**을 사이트 보증으로 가른다
```

## 5. ② 검사 — `S46-273`

```
S46-273  「축 점수(result_axis.score)를 넣는가」
잣대  ① 넣는 코드가 있는가 (`runner.py` 의 `result_axis` INSERT 에 `score`)
      ② 규칙이 ★ **한 자리**인가 (`score.scorer.axis_points`)
      ③ 표에 ★ `score` 가 NULL 인 행이 없는가 (★ `excluded` 는 뺀다 — 「안 봤다」다)
1차   ★ 실패 — 「`result_axis.score` 가 빈 행 474,350개 / 474,350개」
지금   ★ 통과 — 「축 점수가 다 차 있다 (474,350행)」
```

## 6. ★ 알아 둘 것 — ★ 축 합은 `earned` 와 **다르다** (틀린 것이 아니다)

```
표본 200건 중 ★ 89건이 ★ 축 합 ≠ earned 였다.  ★ 처음엔 결함으로 봤다 — ★ 아니다.
  `score/scorer.py:231`  earned = **축 합 ＋ 감점(minus) ＋ 가점(plus)**
  보기 — 매물 3 · 축 합 447.8 · 감점 −22.0 · ★ earned **425.8**
★ 그래서 ★ `S46-273` 은 ★ 「합이 같은가」를 ★ **안 본다** — ★ 「빈 칸이 없는가」만 본다.
  ★ ★ 없는 불변식을 검사에 넣으면 ★ 늘 붉어져 ★ 아무도 안 본다 (STEP 49c 와 같은 뜻)
```

## 7. 못 한 것 · 여쭐 것

```
① ★ 「아무 일도 안 하면 통과하는 검사」가 ★ `V9-09` 말고 ★ 또 있을 수 있다.
   ★ 훑어볼까요 — ★ 자리와 품이 드는 일이라 ★ 지시를 기다립니다
② 앞 회차의 여쭐 것 다섯은 그대로다 (BMW 사진 · sold.html 사진 자리 ·
   엔카 상세 회선 · KB · `check_all` 타이머 권한)
```
