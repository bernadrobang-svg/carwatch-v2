# ★ 범인을 찾았습니다 — S5 가 **이미 가진 상세를 다시 불러** 407 을 상태로 굳혔습니다

지시   「doit」 (v392 여쭐 것 1·2)
버전   v393

---

## 1. ★ 범인 — 요청 기록이 말해 줍니다

v392 에서 「무엇이 8,450건을 덮었는지 못 짚었다」고 적었습니다. **찾았습니다.**

```
audit_request · site=encar · kind=detail
  2026-09-03   http 407  ★ 127,129건     http 200  34
  2026-09-05   http 407  ★  33,868건     http 200  52
  2026-09-06   http 407      1,081건     http 200 272
  2026-09-07   http 407        701건     http 200 218
```

★ **되풀이가 끊이지 않는 고리**였습니다:

```
① 원문은 파일에 있다 (0-3 에서 raw_response 를 비우고 파일로 옮겼다)
② 그런데 DB 칸은 NULL·error 다
③ S5 는 DB 칸만 보고 「아직 안 받았다」로 여겨 ★ 다시 부른다
④ 407 이 온다 → 상태가 또 error 가 된다
⑤ ①로 돌아간다 — ★ 영영 안 끝난다
```

★ 그동안 저는 ★ 「엔카가 막는다」고만 적었습니다. **절반만 맞았습니다** —
★ **부르지 않아도 될 것을 불러 놓고** 막힌다고 한 것입니다.

---

## 2. ★ 「몇 → 몇」

| 항목 | 전 | 후 |
|---|--:|--:|
| **고친 뒤 30분에 「파일이 있는데 다시 부른 것」** | (하루 8,416) | **0개** |
| `/fetch` 2단계 「받을 것」 | 8,684 | **1,713** |
| `/fetch` 화면 그리는 시간 | 31초 | **3초** (다음 장 0.2초) |
| 검사 `V2-20` | 없었다 | **통과 · 0건** |

---

## 3. 세 가지를 했습니다

### ① S5 가 **파일도 봅니다** — 뿌리를 막았습니다

```
store/rawfile.py   have_ok(site, endpoint)   ★ 신설
  그 창구를 이미 잘 받은 매물번호를 집합으로 준다
collect/runner.py  S5 가 창구마다 한 번만 훑어
  파일이 있으면 ★ 안 부르고 ★ 칸을 'ok' 로 고친다
```

실측 — S5 를 돌리니 화면이 이렇게 적습니다:

```
파일에 이미 있는 것  detail 9,322 · inspection 5,422 · record …
```

★ **고친 뒤 30분: 파일이 있는데 다시 부른 것 0개.**

### ② 검사를 세웠습니다 — `V2-20`

```
V2-20  원문이 ok 인데 DB 가 「못 받았다」인 줄 없음   (fatal)
  자는 tools/heal_status_from_raw.py 와 ★ 같은 셈을 쓴다 —
  자와 고치는 자가 갈리면 「고쳤다」가 거짓이 된다
실측  통과 · 0건 · 62초
```

★ v392 에서 「가이드께서 `S46-292` 로 세워 주시면」이라 적었는데,
★ **`V-` 갈래는 제 몫**이라 제가 세웠습니다. 가이드께서 따로 두실 것이 있으면 말씀해 주십시오.

### ③ `/fetch` 가 **파일도 함께 셉니다** (v392 여쭐 것 2)

```
step_counts · queue  →  파일에 이미 있는 것은 안 센다 · 안 준다
```

★ 화면이 거짓 수를 내면 ★ 마스터께서 헛되이 도십니다.
★ 파일 훑기가 6초씩이라 ★ 60초만 담아 둡니다 (`have_ok_forget()` 로 버립니다) —
★ 받은 뒤에는 바로 버려 ★ **방금 받은 것을 또 주지 않습니다.**

---

## 4. 두 도구

```
python3.11 tools/run_tests.py
test_admin 통과 · test_admin_flow 통과 · test_collect 통과 · test_crosssite 통과
test_dict 통과 · test_endtoend 실패 · test_fixtures 통과 · test_integration 통과
test_invariants 통과 · test_pipeline 통과 · test_registry 통과 · test_report 통과
test_run 통과 · test_score 통과 · test_screens 통과 · test_spec_ui 통과
test_store 통과 · test_watch 통과 · test_web 통과
ruff 통과  (F821,F811,B905,F841,DTZ005,F401)
결과: 실패 — test_endtoend.py
fatal 넷 — V4-28 · V3-77 · V11-12 · V11-34
```

★ 넷 다 이번 작업이 만든 것이 아닙니다.
★ 새로 세운 **`V2-20` 은 통과**했습니다 (0건).

---

## 5. 여쭐 것

### 1  ★ `V2-20` 이 62초 걸립니다

원문 파일 3만 5천 장을 훑습니다. `run_tests` 가 그만큼 길어집니다.
① 이대로 둘지 ② 엔카 상세만 볼지 ③ `check_all` 에서만 돌릴지 정해 주십시오.
★ 저는 **① 이대로**를 권합니다 — 이 고리가 이 프로젝트에서 가장 비쌌습니다.

### 2  흰색 후보는 여전히 서버로는 느립니다

배경에서 돌고 있지만 5분에 1대입니다.
★ 마스터께서 `/fetch?step=2&n=30` 을 누르시면 **3초**입니다.

### 3  CSS 세 자리는 그대로입니다 (v389 여쭐 것 1·2·3)

## 6. 이견

없습니다. ★ 「엔카가 막는다」고만 적고 ★ **우리가 왜 부르고 있는지**를
안 물은 것이 제 잘못입니다.
