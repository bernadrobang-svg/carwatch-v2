# r515 단계 1~4 — 감점 상한 · 감점 화면 · /why 문구 · config

`SPEC-2026.08.22-r515` · `v200` · 2026-08-23 02:00

---

# 단계 1 — 감점 상한 (명령서 1-1 · 개정 491)

## 무엇을 했나

```
config/scoring.json  ★ penalty_axis 신설 — 어느 감점이 어느 축에 붙는가
                     ★ 점수는 안 적는다.  상한은 components 가 정본이다 (S14)
score/penalty.py     ★ cap_penalties() 신설 — 축별로 묶어 합산한 뒤 그 축 배점에서 자른다
score/scorer.py      penalties_of → cap_penalties 를 거쳐 나간다
```

★ 자른 것을 **감추지 않는다** — 되돌린 몫을 `cap:축` 줄로 함께 낸다.

```
frame_swap      -108  골격 용접·교환
frame_sheet      -54  골격 판금
cap:state.frame  119  골격 -162 → 상한 -43        ← ★ 이 줄이 화면에 그대로 나간다
```

## 실측 — f-table 표와 대조

| 짝 | 전 | 후 | f-table 상한 |
|---|--:|--:|--:|
| 골격 교환+판금 | −162 | **−43** | −43 ✔ |
| 렌트 | −67 | **−22** | −22 ✔ |
| 사이트 진단없음 | −40 | **−36** | −36 ✔ |
| 계기판 (겹치는 축 없음) | −81 | **−81** | −81 그대로 ✔ |

## ★ 사이트 검증 — 상한과 별개 (f-table 「사이트 검증」 절)

```
전   warranty.site 를 만점 36 으로 주고 ★ 「판매자가 점검을 올렸다」로 −27 을 또 뺐다
후   ★ seller_inspection 감점을 없앴다.  ★ 대신 단계를 한 칸 낮춘다
     analyze/axis/site.py  _seller_only() · _one_step_down()
★ 「감점을 없애지 마라」(금지 2)와 어긋나지 않는다 — f-table 이 그렇게 적었다
```

---

# 단계 2 — 감점을 화면에 낸다 (명령서 1-2 ⓐ~ⓗ)

| | 무엇 | 결과 |
|:--:|---|---|
| ⓐ | 상세 축 표 아래 마이너스 줄과 합계 | ○ `detail.html` |
| ⓑ | 네 막대 옆 감점 — 「막대 합 − 감점 = 등급 점수」 | ○ `listings.html` |
| ⓒ | 목록 한 행에 감점 딱지 | ○ `ListingRow.penalty_won` 신설 · 같은 조인 (쿼리 안 늘었다) |
| ⓓ | 관문 칩을 감점과 맞춘다 | ○ 매물 16 — 「골격 이상 없음 O」 → **「골격 감점 있음 ·」** |
| ⓔ | 진단 미조회인데 사이트 보증 만점 | ○ 맨 위 단계에 `needs_ok: diagnosis_status` |
| ⓕ | 확인율이 화면마다 다르다 | ○ 채점이 저장한 `confirmed_points` 하나로 |
| ⓖ | 순위를 감점 반영 점수로 | ○ **이미 그렇다** — `ORDER_SQL["rank"]` 가 `earned/denominator` 이고 `earned` 에 감점이 들어 있다 |
| ⓗ | ♡ → 로그인 → 그 매물이 담겨 있게 | ○ `/login?listing_id=` → 폼 hidden → `_open_session(watch=)` |

## ★★ ⓔ 실측 — 43건이 ★ 전부 미조회였다

```
warranty_extend=1 (엔카진단++ 만점)            43건
그중 diagnosis_status 가 'ok' 가 아닌 것       ★ 43건 — 전부다
→ ★ 진단을 한 번도 안 받고 「하부 점검·선별된 성능장」 만점을 주고 있었다
고침   store/core.py SITE_WARRANTY_FIELDS 에 diagnosis_status 를 넣고
      config/sites.json 맨 위 단계에 needs_ok 를 걸었다
```

---

# 단계 3 — `/why` 문구 (명령서 1-3)

```
전   「등급은 ★ 취향을 뺀 910 기준 …점입니다」
후   「등급도 같은 910 기준 …점입니다」
근거 개정 431 이 「갈래를 등급에서 빼는 것」을 폐기했다.  분모는 늘 총점이다
```

---

# 단계 4 — `config/scoring.json` (명령서 1-4)

```
★ 이미 f-table 값이다 — v199(커밋 20ed6db)에서 올렸다.  ★ 명령서가 그 push 전에 실측한 것이다
실측  총점 910 · 26축 · 컷 S.75 A.65 B.57 C.50 D.40 E.30 F.20 G.10
      감점 교환 −108 · 판금 −54 · 불법구조 −54 · 렌트 −67 · 미가입 −54 ·
           압류 −40 · 진단없음 −40 · 계기판 −81
검사  V3-76 통과 · V3-77 「4갈래 모두 맞다」
★ seller_inspection 만 뺐다 — 개정 491 이 「감점이 아니라 단계를 낮춘다」로 옮겼다
```

---

# ★ 가이드가 push 한 코드가 검사 셋을 깨뜨렸다 (개정 505 · 커밋 4ec5f75)

```
① analyze/axis/spec.py:46   r = ctx.policy.rule("spec") 이 ★ 안 쓰인다 → ruff F841
   ★ HDA 를 지우면서 남은 이름이다.  ★ 지웠다
② validate/v0_guide.py      ★ __main__ 블록 → V4-23 위반
   「import 만으로 아무 일도 안 일어나야 한다.  실행은 run.py · tools/ 에서만」
   ★ 블록을 지우고 돌리는 법을 주석에 적었다
③ tools/gen_table.py        ★ 대응 화면이 없다 → V11-45
   ★ config/cli_screens.json 의 pending 에 넣었다 (화면은 가이드가 정한다)
★ analyze/ · validate/ · tools/ 는 ★ 개발측 몫이다 (규칙 9).  ★ 손대시면 검사가 깨진다
```

---

# 검사 — ★ 실제 출력

```
1차   결과: 실패 — test_endtoend.py, test_report.py, ruff
      (위 ①②③ · 전부 가이드 커밋 4ec5f75 이 낸 것)
조치 후  결과: ★ 통과   (19파일 + ruff)
```

★ `check_all` 과 배포는 ★ 단계 5 에서 함께 낸다 (재계산이 있어야 숫자가 맞다).

---

# 커밋

```
1cbfa26
```
