# 20260828_0308_v277 — K카 487건 · ★ KB 가 새는 자리를 짚었다 · 문턱 60% 가 걸렸다

명령서 **80장** ①②③ · 볼보 C40 확인 · **71~79장은 다 뒤로**

---

## 1. 무엇을 하려 했나 — 지시 원문

```
① ★ K카 — GET mapi.kcar.com/bc/stockCar/list?pageSize=1000
   암호화 안 돼 있다. 472건을 받아 22종을 걸러라 (지금 160건)
   ★ 「472 중 몇 건이 우리 것인가」를 적어라
② ★ KB — 15,836 받았는데 화면에 100 만 보인다. 어디서 새는지 짚어라
   ① 원문이 없다 ② core 에 안 들어갔다 ③ out_of_scope 로 접혔다 ④ 화면이 안 낸다
③ ★ 재판정 한 번 — 돌린 뒤 「S 몇 · 판정 중 몇 · 주행 최대 몇」
★ targets.json 에 볼보 C40 — 엔카 ModelGroup=C40 이 맞는지만 확인해 줘라
★ scoring.json 은 손대지 마라
```

---

## 2. ★ 볼보 C40 — ★ 맞다 (실측)

★ **지어내지 않고 이미 받아 둔 엔카 봉투에서 눈으로 봤다.**

```
raw_response  site='encar' · endpoint in ('facet','list')
  Manufacturer.볼보  바로 뒤의 ModelGroup 값
    EX60 · XC60 · XC90 · S90 · XC40 · V60 · S60 · V90 · V40 · S80 · XC70
    EX30 · ★ C40 ★ · C30 · S40 · C70 · V70 · S70 · 850 · 기타
```

★ **`ModelGroup=C40` 이 맞다.** 볼보는 맨 이름을 쓴다 — `XC60`·`XC40`·`V60`·`S60` 과 같은 꼴이다.
★ 엔카 매물의 `site_model_group` 실측도 같다 (XC60 480 · XC40 354 · V60 236 · S60 217).
★ `targets.json` 의 `_확인` 줄을 **잰 내용으로 바꿔** 두었다 (지운 것이 아니다).

---

## 3. ★ ① K카 — ★ 487 중 105건이 우리 것이다

### 3-1. 길이 열린다 (실측)

```
GET https://mapi.kcar.com/bc/stockCar/list?pageSize=1000
  → 200 · 889,585B · 평문 JSON · 헤더·토큰 불필요
  → data.listCount = ★ 487   (마스터께서 말씀하신 472 는 그날 수치다.  날마다 움직인다)
  → carCd 487개 · ★ 중복 0
```

### 3-2. ★ 「487 중 몇 건이 우리 것인가」 — ★ **105건**

```
python3.11 tools/collect_kcar.py --list
  목록 — 사이트가 말한 총 487건 · 받은 487건 · 889,585B
  ★ 우리 대상 — ★ 105건 / 487건
  ★ 저장할 것 105건 · 안 넣는 것 382건 (원문은 남는다)
  ★ 저장된 K카 매물 — 604건
```

| | 전 | ★ 후 |
|---|--:|--:|
| K카 `core_listing` | 602 | **604** |
| ★ 우리 것으로 걸러 넣은 것 | — | ★ **105 / 487** |
| 상세 원문 | 70 | **72** |

### 3-3. ★ 고친 것 — ★ 887KB 봉투를 그냥 버리고 있었다 (P3)

```
raw_response  site='kcar' · endpoint='stock_list'  →  ★ 0건
```

★ `fetch_stock()` 이 받은 목록 봉투를 **저장하지 않았다.**
★ 「원문은 남긴다 — 갈래를 넓히시면 다시 판다」(명령서 3-2)가 안 지켜져 있었다.

| 파일 | 무엇 |
|---|---|
| `tools/collect_kcar.py` | `fetch()` 에 `want_text` · `fetch_stock()` 이 원문 글자를 돌려준다 |
| 〃 | `collect_list()` 가 `save_site_raw(…, 'stock_list', …)` 로 **먼저 남긴다** |

```
★ 목록 원문을 남겼다 (864,973자 · stored)
raw_response  stock_list  ★ 0 → 1건
```

### 3-4. ★ 105건 중 차종이 붙은 것은 34건이다 (그대로 적는다)

| 차종 | 건수 |
|---|--:|
| G70_20T | 14 |
| G80_25T | 13 |
| X3_IMPORT · KOLEOS_HEV · GLC_IMPORT | 각 2 |
| XC60_IMPORT | 1 |
| **합** | **34** |

차종이 안 붙은 것의 이름을 세어 보면 —

```
그랜저 86 · 스포티지 22 · G80 21 · 싼타페 TM 18 · GV80 16 · 카니발 14 · 팰리세이드 12
```

★ 그랜저·스포티지는 **우리 차종이 연료로 좁혀져 있다** (`GRANDEUR_LPG` · `SPORTAGE_LPI`).
★ K카의 그랜저 86건이 다 LPG 는 아니다 — **갈래로 갈린 것**이다.
★ **미분류는 71장이라 이번에 안 건드렸다** (마스터 지시).

---

## 4. ★★ ② KB — ★ 새는 자리를 짚었다

### 4-1. 먼저 — ★ 「15,836」은 ★ **우리가 받은 수가 아니다**

```
outputs/20260826_2000_v258_r757_KB20종_15825.md
  「★★★ KB 전량 수집을 멈춰라.  내가 잘못 시켰다 (명령서 60장 · 오판 130)」
  pkill -f collect_kbchachacha   ★ 멈췄다
  ★ 그때까지 받은 것 — ★ 매물 295건
  15,825 는 ★ `--narrow --dry` 로 ★ **세기만 한 수**다
```

★ **15,836 은 「KB 에 그만큼 있다」는 수**다. ★ 우리가 저장한 것은 **445건**이다.
★ 그러므로 「15,836 이 샜다」가 아니다. **받은 적이 없다.**

### 4-2. ★ 445 → 100 — ★ 조건마다 몇 건이 빠지는지 그대로 셌다

`_listings_where()` 의 조건을 하나씩 더해 가며 셌다.

| 조건 | 남는 건수 | 빠진 것 |
|---|--:|--:|
| `l.site = 'kbchachacha'` | **445** | — |
| ＋ 광고 갈래 거르개 | 445 | 0 |
| ＋ 판매 갈래 거르개 | 445 | 0 |
| ＋ 제외(EXCLUDED) 아님 | **397** | **−48** |
| ★ ＋ **같은 차 접기** | ★ **139** | ★ **−258** |
| ＋ `status <> 'out_of_scope'` | **100** | **−39** |

★ **화면 = 100.  셈이 정확히 맞는다.**

### 4-3. ★ 답 — ★ **④ 살아 있는데 화면이 안 낸다**

★ 가장 크게 무는 것은 **「같은 차가 여러 곳에 있으면 낮은 값 하나만 낸다」**(교차 사이트 접기)다.

```
report/screens/build.py
  "(l.plate_hash IS NULL OR l.listing_id = ("
  "  SELECT l2.listing_id FROM core_listing l2"
  "   WHERE l2.plate_hash = l.plate_hash AND l2.status = 'active'"
  "   ORDER BY l2.price_current_won, l2.listing_id LIMIT 1))"
  ★ 마스터 확정 08-24 · v3_listings_시안 · S46-57
```

★ **접힌 뒤 이긴 쪽이 어디인가** — 잰 것

| 이긴 사이트 | 건수 |
|---|--:|
| **encar** | **158** |
| kbchachacha (자기들끼리) | 128 |
| bobaedream | 1 |

★ **KB 매물의 대부분이 엔카에도 있고, 엔카 쪽이 싸서 접힌다.**
★ 결함이 아니라 **마스터께서 정하신 규칙이 그대로 도는 것**이다.
★ 다 펴서 보는 자리는 `/track` 이라고 코드 주석이 적어 두었다.

★ 나머지 39건은 `out_of_scope`(20종이 아닌 것)로 접힌 것이다 — ③ 에 해당한다.

```
★ 정리 — ① 아니다(원문은 있다) · ② 아니다(445건 다 들어갔다)
         ★ ③ 39건 · ★★ ④ 258건 ← ★ 여기가 크다
```

---

## 5. ★ ③ 재판정 — ★ 문턱 60% 가 걸렸다

### 5-1. ★ 먼저 고칠 것이 있었다 — 코드가 새 열쇠를 안 읽고 있었다

```
config/scoring.json  pending_rule.evidence_ratio_below = ★ 0.60   (가이드 r788)
score/grade.py       PENDING_RATIO_KEY = "pending_confirmed_ratio" = ★ 0.5
```

★ **새 규칙이 config 에 들어왔는데 코드가 옛 열쇠를 읽고 있었다.**
★ 규칙 1 대로 **코드를 고쳤다** — `scoring.json` 은 안 건드렸다.

| 파일 | 무엇 |
|---|---|
| `score/grade.py` | `pending_ratio(policy)` — `pending_rule.evidence_ratio_below` 가 있으면 그것이 정본. 없으면 옛 열쇠 |

```
확인   문턱 = 0.6 · 910 의 ★ 546.0점
```

### 5-2. ★ 마스터께서 물으신 세 숫자

```
python3.11 run.py collect --from S9
  S9   248508/248508  완료 · 16.7초 · 메모리 32MB
  S10    9558/9558    완료 ·  7.9초 · 메모리 32MB
```

```
★ S 61 · 판정 중 930 · 주행 최대 890,000km
```

| 등급 | 건수 |
|---|--:|
| E | 1,834 |
| D | 1,821 |
| EXCLUDED | 1,696 |
| C | 1,190 |
| B | 1,052 |
| **판정 중** | ★ **930** |
| A | 819 |
| F | 147 |
| **S** | ★ **61** |
| G | 7 |
| NO_GRADE | 1 |
| 합 | 9,558 |

```
★ 60% 아래인데 등급이 붙은 것 ★ 0건 — 문턱이 제대로 걸렸다
```

### 5-3. ★ 마스터 말씀대로 됐다 — 사이트별

| site | 매물 | 판정 중 | % | 근거 평균 | S |
|---|--:|--:|--:|--:|--:|
| encar | 8,681 | 125 | 1% | 807.1 | **61** |
| **hyundai_cert** | 174 | **174** | ★ **100%** | 531.6 | 0 |
| **volvo_selekt** | 140 | **140** | ★ **100%** | 350.5 | 0 |
| **reborncar** | 72 | **72** | ★ **100%** | 470.3 | 0 |
| kbchachacha | 287 | 239 | 83% | 484.2 | 0 |
| heydealer | 96 | 72 | 75% | 513.8 | 0 |
| bobaedream | 55 | 55 | 100% | 309.0 | 0 |
| kcar | 34 | 34 | 100% | 367.0 | 0 |
| lexus_certified | 10 | 10 | 100% | 387.0 | 0 |
| bmw_bps | 9 | 9 | 100% | 18.0 | 0 |

★★ **S 61건이 전부 엔카다.** ★ 상세가 찬 곳만 등급이 난다 —
★ 마스터께서 「상세가 전부다」 하신 것이 **숫자로 그대로 나온다.**

### 5-4. ★★ 주행 최대 890,000km — ★ 그리고 8만km 규칙이 ★ **안 걸린다**

```
listing_id 16100 · encar · X3_IMPORT · ★ 890,000km · 2023-03 · EXCLUDED
```

★ 2023년식에 89만km 다. **값 자체가 수상하다** — 다만 이번에는 짚기만 한다 (71~79장 뒤로).

★★★ **그리고 더 큰 것이 나왔다.**

```
마스터께서 고치신 것   axis_rules.★mileage★.zero_km  100000 → 80000
실제로 쓰는 것        axis_rules.★value★.mileage_curve
   [[0,107],[20000,98],[40000,88],[60000,76],[80000,62],
    [100000,48],[120000,35],[150000,19],[200000,0]]
```

★ `value.mileage` 축은 **`value.mileage_curve` 를 읽는다.**
★ 마스터께서 고치신 `axis_rules.mileage` 를 읽는 자리는 `analyze/axis/mileage.py`
  의 `analyze_mileage()` 하나뿐인데 — ★ **아무도 그것을 부르지 않는다** (grep 실측).

```
곡선대로 재면
   80,000km → ★ 62점   (0점이 아니다)
  122,423km → ★ 34점
  200,000km → ★ 0점    ← ★ 0 이 되는 자리는 20만km 다
실측  8만km 이상 2,528건 중 ★ 주행 축 0점은 96건뿐 (＝ 20만km 넘는 것들)
```

★★ **「8만킬로 거의 0점」이 ★ 죽은 규칙에 들어갔다.**
★ `scoring.json` 은 **손대지 말라 하셔서 안 건드렸다.**
★ **둘 중 하나를 정해 주시기를 청한다** —
  ⓐ `value.mileage_curve` 를 80,000 에서 0 이 되게 고친다 (config 만 고치면 된다)
  ⓑ 축이 `axis_rules.mileage` 를 읽도록 코드를 바꾼다

---

## 6. check_all · run_tests 실제 출력

```
python3.11 tools/check_all.py
  DB carwatch.db · run 20260826T164607
  통과 396 · fatal 7 · warn 13 · 환경 차이 1 · 미실행 8

  ■ FATAL
    ★ S44-5   명령서이 사이트를 한 가지로 적는가 —
              명령서가 같은 사이트를 두 가지로 적는다 — ★ K카(목록만·상세만)
    V1-23 · V2-01 · V11-12 · V11-159 · V11-45 · V11-76   ★ 여섯은 전부터 있던 것

python3.11 tools/run_tests.py
  결과: 실패 — test_endtoend.py  (1파일 · V11-12 규격 중복 행 탓)
```

| | 앞 바퀴 | 이번 |
|---|--:|--:|
| 통과 | 395 | **396** |
| fatal | 6 | **7** |
| warn | 14 | 13 |

★ 는 `S44-5` 다 — **규격이 K카를 「목록만」과 「상세만」 두 가지로 적어** 생긴 것이다.
★ 이번에 K카 목록이 실제로 열려 그 어긋남이 드러났다. **규칙 2 — 아래에 적는다.**

---

## 7. 지적 — 규격 쪽 (규칙 2)

1. ★★★ **주행 8만km 규칙이 죽은 자리에 들어갔다** (5-4장). ★ ⓐ 인지 ⓑ 인지 정해 주시기를 청한다.
2. ★★ **`S44-5`** — 명령서가 K카를 「목록만」·「상세만」 두 가지로 적는다.
   ★ 목록은 **열린다** (오늘 487건 실측). 한 가지로 적어 주시기를 청한다.
3. ★★★ **비밀번호가 `docs/guide/03_이력.md` 에 그대로 있다.** 저장소가 public 이다.
   ★ 가려 주시고 **바꾸시기를 청한다** (v274·v275·v276 에서도 청했다 — **네 번째**).
4. ★★ **`docs/chapters/61-web.md:196`·`:209` 의 `/admin/users` 중복** — **열 번째 지적**.
5. ★ **`grade_gates.warranty_required_above`(r788 신설)를 코드가 안 읽는다.**
   ★ 「S급이 보증 끝난 것도 포함」을 막는 문인데 ★ 아직 아무도 안 본다.
   ★ 넣을지 청한다 (이번에는 80장만 하라 하셔서 안 넣었다).

---

## 8. 못 한 것 · 다음

| # | 무엇 | 왜 |
|:--:|---|---|
| **1** | ★ **K카 상세** — 105건 중 상세 원문 72건 | 마스터 차례 「현대인증 → 볼보 → **K카** → KB」의 셋째 |
| **2** | ★ **KB 상세** | 넷째. 지금 180건뿐이다 |
| 3 | 8만km 규칙 | 위 7장-1 답을 받고 |
| 4 | `grade_gates` | 위 7장-5 답을 받고 |
| 5 | 71~79장 (검사·미분류·사진) | ★ 마스터께서 다 뒤로 미루라 하셨다 |
