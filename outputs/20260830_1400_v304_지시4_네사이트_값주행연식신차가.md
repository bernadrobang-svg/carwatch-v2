# 마스터 지시 4 — 볼보·BMW·보배·렉서스의 값·주행·연식·신차가

## 안 한 것

| 무엇 | 왜 못 했나 | 며칠째 | 누가 답해야 하나 |
|---|---|--:|---|
| 볼보 **신차가** | ★ **원문에 없다.** 목록·상세를 다 눌러 봤다 (아래) | 오늘 | **마스터** — ③(우리 표)으로 끌어올까요 |
| 볼보 값·주행·연식 빈 **15건** | 목록에 없는 낡은 행이다. `target_key` 가 없어 `sweep_gone` 이 못 건드린다 (아래) | 오늘 | **마스터** |
| 보배 신차가 **227건** | 규격대로다 — 원문 칸은 **1/10** 이고 나머지는 딜러 손글씨다. ★ 지어내지 않았다 | 오늘 | **마스터** — ③으로 끌어올까요 |
| `test_web` (`sold.html` 없음) | 가이드가 `365797f0` 로 올린 새 시안. 설계도 ⑦번이라 안 만들었다 | 이틀째 | **마스터** |
| KB·리본카 `target_key` (40건) | 한 회차가 20~30분이라 하루치 타이머(13시)에 맡겼다 | 오늘 | — |
| `outputs/check-tmp` 12GB | 물음지 #57·#58 로 여쭈었다. **안 지웠다** | 오늘 | **마스터** |

## 1 무엇을 하려 했나

마스터 지시 4 — 「볼보·BMW·보배·렉서스의 값·주행·연식·신차가」

## 2 ★ 먼저 쟀다 — 넷이 서로 달랐다

```
사이트          전체   값 없음  주행 없음  연식 없음  신차가 없음
volvo_selekt    149     15       15        15        149
bmw_bps         376    376      376       376        376     ← ★ 전부 비었다
bobaedream      228     26        0       109        228
lexus_certified  61      0        0         0          0     ← ★ 다 차 있다
```

★ **렉서스는 고칠 것이 없었습니다.** 목록이 넷을 다 줍니다.

---

## BMW — ★ 376건 전부 비어 있었다

### 무엇을 했나

| 파일 | 무엇 |
|---|---|
| `parse/bmw_bps/mapping.py` | **새 파서** — 값·신차가·주행·연식·등록·연료·배기량·변속기·사고·압류저당·점검 |
| `tools/collect_bmw.py:130~` | 우리 대상만 상세를 받고 넣는다 |
| `tools/collect_bmw.py` | `_have_detail()` — 「이미 받았나」를 **원문**으로 가른다 |

### ★ 규격이 짚은 함정 둘을 다 막았다

```
① 「신차가: 약 11300만원」이 같은 글에 있다 — ★ 판매가가 아니다
② 쪽 아래 거르개에 「최저 가격 1500 만원 2000 만원 …」이 서른 번 넘게 나온다
   ★ 「만원」만 찾으면 거르개 숫자를 값으로 넣는다 — ★ 반드시 라벨을 함께 본다
```

라벨을 함께 보는 하나의 정규식으로 잡고, 신차가는 **따로 뽑아 `price_origin_won`** 으로
보냈습니다. 표본 셋을 눌러 확인했습니다 —

```
1787904423  값 71,000,000  주행 27,300  연식 2022  등록 2022-08  신차가 113,000,000
1787972049  값 45,000,000  주행 34,119  연식 2023  등록 2023-08  신차가 없음
1787961293  값 52,000,000  주행 24,130  연식 2024  등록 2023-11  신차가 없음
```

★ 규격 표(27,300 · 34,119 · 24,130km)와 **정확히 맞습니다.**

### ★ 함께 잡힌 것 — 연료가 없어 차종이 안 붙었다

상세를 넣고도 `target_key` 가 9건뿐이었습니다. 까닭은 **연료**입니다 —
X3 는 휘발유·경유가 갈리는데 `fuel_raw` 가 비어 규칙이 못 골랐습니다.
상세가 「연료 휘발유」·「배기량 1998」을 주므로 그것도 읽게 했습니다.

또 하나 — 상세에는 **차종 이름이 없습니다.** 목록이 줍니다. 이어 주지 않으면
`fill_target_key` 가 이름이 없어 못 붙입니다. 그것도 이었습니다.

### 몇 → 몇

| | 전 | 후 |
|---|--:|--:|
| 우리 대상 BMW (`target_key`) | **9** | **23** |
| 값 | **0** | **15** |
| 주행 | **0** | **23** |
| 연식 | **0** | **22** |
| 연료 | **0** | **23** |

★ 값 15/23 입니다. 나머지 8건은 **딜러가 값을 안 적었습니다** —
「값 없음으로 남긴다. 지어내지 않는다」(규격)를 지켰습니다.

---

## 보배 — ★ 연식 119 → 225

### 무엇을 했나

| 파일 · 줄 | 무엇 |
|---|---|
| `parse/bobaedream/mapping.py:47` | `RE_YM4` — 둘째 꼴 `2024/10` |
| `parse/bobaedream/mapping.py:127` | 옛 꼴이 안 걸릴 때만 둘째 꼴을 본다 |
| `parse/bobaedream/mapping.py:53` | `신차가격` 칸을 읽는다 |

규격이 자리까지 짚어 주셨습니다 — 「연식 칸의 꼴이 **둘**인데 파서가 하나만 본다」.
다섯 꼴을 다 시험했습니다 —

```
11/10/24(12년형) → 201110    20/09/14(21년형) → 202009    23/02/10 → 202302
2024/10          → 2024-10   2024/05          → 2024-05   ← ★ 앞서 못 읽던 것
```

### 몇 → 몇

| | 전 | 후 |
|---|--:|--:|
| 연식 | **119 / 228** | **225 / 228** |
| 값 | 202 | 202 |
| 신차가 | **0** | **1** |

★ 신차가 1건은 **규격이 예측한 그대로**입니다 — 「표본 10건 중 칸이 온 것은 1건,
나머지 9건은 딜러가 손으로 쓴 글에만 있다. 그것은 지어내지 않고 안 읽는다」.
쌓인 원문 222건에 걸어 보니 **연식 222/222 · 신차가 1/222** 였습니다.

---

## 볼보 — ★ 신차가는 원문에 없다 (눌러서 확인)

목록과 상세를 **둘 다 눌러** 봤습니다.

```
목록 카드   Volvo S60 Inscription, B5 mild hybrid  27,000,000원  …
            ★ 값은 있다.  ★ 「신차」 낱말 0회
상세        주행거리 : 18940km 는 있다.  ★ 「신차가」·「판매가」 0회
```

★ **「사이트가 안 준다」로 규격에 적지 않았습니다** (오판 189). 잰 것만 적습니다 —
「목록·상세를 눌러 보니 신차가가 없었다 (08-30)」.

### 볼보의 빈 15건 — 다른 결함입니다

15건이 다 `status='new'` · `target_key` 없음 · 값·주행·연식 전부 `None` 입니다.
`first_seen` 이 08-29 입니다. **`sweep_gone_groups` 가 `target_key` 있는 행만 보므로
이 15건은 목록에서 사라져도 영영 `gone` 이 안 됩니다.**

★ 이것은 앞서 여쭌 「`target_key` 를 누가 붙이나」와 같은 뿌리입니다.
지금 `fill_target_key` 를 붙였으니 다음 회차에 이름이 붙으면 함께 풀립니다 —
다만 **값이 없는 행은 이름도 못 붙습니다.** 여쭙습니다.

---

## 검사

| | 회차 처음 | 지금 |
|---|--:|--:|
| check_all 통과 | 418 | **418** |
| check_all fatal | 5 | 5 |
| run_tests 실패 | 2파일 | 2파일 (`test_endtoend` · `test_web`) |

★ `test_web` 은 가이드가 올린 `v4m_sold_시안.html` 에 맞는 틀(`sold.html`)이 없어서입니다.
그 화면은 설계도 ⑦번이고 「구조를 손대지 마라」에 걸려 **안 만들었습니다.**

## ★ 여쭐 것 — 신차가를 ③(우리 표)으로 끌어올까요

지금 신차가가 없는 것 —

```
볼보    149 / 149   ★ 원문에 없다 (눌러서 확인)
보배    227 / 228   ★ 원문 칸은 1건뿐 (규격이 그렇게 적었다)
BMW      8 /  23    ★ 딜러가 안 적었다
렉서스     0 /  61   ★ 다 있다
```

규격이 「나머지는 **③ 으로 끌어온다** (마스터 확정 08-29)」라 적었습니다.
③이 무엇을 가리키는지 — 우리 차종별 신차가 표인지 — **정하시면 넣겠습니다.**
지금은 **비운 채 두었습니다** (지어내지 않았습니다).
