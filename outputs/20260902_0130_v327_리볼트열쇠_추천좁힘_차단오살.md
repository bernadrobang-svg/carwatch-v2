# v327 — 리볼트 질의 열쇠 · 추천을 고르신 차종으로 · 차단 페이지 오살 되살림

일시 2026-09-02 01:30 · 앞 회차 v326

---

## 1. 무엇을 하려 했나 (지시 원문)

```
마스터 (앞 회차)  「Step 1부터 순차적르로 이번네 수정사항 그리거 내가ㅠ지시한것레
                맞추러서 지침에 맞데 다 진행해.  그리고 질문사할들 모두 기재하고.
                해당부준은 답번 기다리지 말고 스킵하고 모든 전스텝 다 진행해」
마스터 (회차 중)  「너 추천,화면에서 내가 고르지 않은 차들을 보게하는 거지?
                ★ 내가 고른 차종만 보이게 지금 당장 바꿔」
```

---

## 2. 1차 결과 (고치기 전 숫자)

```
check_all   통과 471 · fatal 16 · warn 13 · 환경 차이 1 · 미실행 11
run_tests   실패 — test_endtoend.py
리볼트      우리 대상 6건 / 270건  (264건을 못 넣었다)
추천 화면   차종 24가지 · 5,470건  ← ★ 그중 15가지 · 3,660건이 고르신 열셋 밖
V2-01       ok 봉투 11,253 · CORE 11,034 — ★ 차 219
```

---

## 3. 무엇을 했나 · 왜

### 3-1 `S46-127` 손으로만 도는 수집기 11 → 0

| 파일 | 무엇 |
|---|---|
| `tools/daily_collect.py` | 주석의 `( … )` 를 없앴다 |
| `config/cli_screens.json` | 보배에 `/admin/collect` 자리를 줬다 |

검사의 정규식이 `SITES[^=]*=\s*\((.*?)\)` 다.  내 주석 안의 괄호가 **먼저** 걸려
사이트 목록이 빈 것으로 읽혔다.  보배는 `paused` 라 타이머에서 빠졌으므로
**수집기를 지우지 않고** 관리 화면에 자리를 줬다 (다시 켤 수 있어야 한다).

### 3-2 ★ 리볼트 — 질의 열쇠로 받는다 (`S46-137` 18 → 0)

가이드가 `targets.json` 에 `brand_hash_id` · `model_hash_id` 를 줬다.
규격 `docs/REVOLT_API.md` — 「★ 거르개는 `?model_hash_id=` 다.  `?model=` 은 안 걸린다」

| 파일 · 줄 | 무엇 |
|---|---|
| `tools/collect_revolt.py:79` `_targets()` | `targets.json` 에서 열쇠를 읽는다 |
| `tools/collect_revolt.py:113` | **차종마다** `?brand_hash_id=&model_hash_id=` 로 받는다 |
| `tools/load_raw.py` `query_target()` | 파일에 남은 URL 의 열쇠로 차종을 짚는다 |
| `tools/load_raw.py` `body_target()` | 상세는 `car_info.model_hash_id` 로 짚는다 |

★ 왜 이름으로 짚으면 안 되나 — **목록 항목에 `model_hash_id` 가 없다** [실측 09-01].
있는 것은 영문 `model_name`(「Model Y」)뿐이라 이름을 옮겨야 했고, 그래서 264건을 놓쳤다.
질의로 가르면 **옮길 것이 없다** — 사이트가 이미 갈라 줬다.

★ `brand_hash_id` 도 걸린다 [실측 09-01] — 폴스타 `RWlnAZ` 7건 · 볼보 `Jo6rOo` **0건**.
볼보 0 은 「리볼트가 안 판다」가 아니라 **지금 재고가 없는 것**이다 (규격이 XC40·C40 을 0 으로 적었다).

두 가지를 더 고쳤다 — 둘 다 **「못 짚었다」를 「없다」로 저장하던 것**이다 (금지 12).

```
① `fill_target_key` 가 못 짚으면 None 을 써서 ★ 이미 짚어 둔 것을 지웠다
   → 키를 빼면 upsert 가 그 칸을 안 건드린다
② `known_model_of`(이름)가 먼저라 ★ 이름이 걸리는 것은 열쇠를 안 봤다
   → 「ID.4」·「iX3」 7건이 차종 없이 남았다.  ★ 열쇠를 먼저 본다
```

### 3-3 ★★ 차단 페이지를 보고 죽인 매물 23건을 되살렸다

`V2-01` 을 파고들다 찾았다.  KB 매물 23건이 **`status='gone'`** 인데
그 상세 봉투는 200 · 1,382B **「로봇여부 확인 | KB차차차」** 였다.

```
★ 「없다」가 아니다.  ★ 「우리가 못 받았다」다 (개정 289·434 셋째)
★ 74대 오살과 ★ 같은 꼴이다 — ★ 파서가 None 을 낸 까닭을 안 갈랐다
```

| 파일 | 무엇 |
|---|---|
| `tools/list_diff_check.py` `_bot_wall()` | 어댑터의 `is_bot_wall` 에 묻는다 · 차단이면 **안 죽인다** |
| DB | 봉투 23건 `status` `ok` → `error` (`http_code` 200 은 그대로) |
| DB | 매물 23건 `gone` → 되살림.  차종을 못 짚은 8건은 `new` 로 (`V2-31`) |

`is_bot_wall` 은 **이미 있었다** (`adapters/kbchachacha.py:73`) — 대조 도구가 안 불렀을 뿐이다.

### 3-4 ★★★ 추천 화면 — 고르신 차종만 (마스터 지시)

| 파일 · 줄 | 무엇 |
|---|---|
| `report/screens/build.py` `_active_targets()` | `targets.json` 의 `active` 가 정본 |
| `report/screens/build.py` `view_recommend_tabs` | `l.target_key IN (…)` 을 건다 · 「N건 중」도 같은 잣대 |
| 〃 | 이름을 열쇠(`G80_25T`)가 아니라 `label`(「G80 2.5T」)로 낸다 |

`/listings` 는 **안 고쳤다** — 거기서는 그대로 다 보신다.

### 3-5 화면 결함 넷 (내 `/recommend` 다시 쓰기가 떨어뜨린 것)

| 검사 | 무엇이 빠졌나 | 무엇을 했나 |
|---|---|---|
| `V11-94` | 추천 조건·이유 | 시안 두 줄은 **글자 하나 안 건드리고** 조건 줄과 「왜 이 순위인가」를 덧붙였다 |
| `V11-69` | ♡ · 미리보기 · 원문 링크 · 비교 · 정렬 **다섯** | 카드 안에 되살렸다 (`/listings` 와 같은 부품) |
| `V11-120` | 사이트별 구매 총액 | 리볼트 `purchase_cost` 를 넣었다 · 쉬는 사이트는 건너뛴다 |
| `V11-77` | 막대가 `.pbar` 가 아니다 | `class="pbar pickbar"` 로 둘을 함께 단다 (시안 이름은 `S46-67` 이 막는다) |

★ 리볼트 총액은 **사이트가 직접 준다** — `purchase_cost.total` [실측 6건].
`total` = `car_price` ＋ `registration_cost` 다.  `green_car_acquisition_tax_discount` 140 은
여섯 건 다 같고 합에 안 들어가 있다 — **안내 문구라 안 뺀다**.

### 3-6 상세 화면에 막대가 없었다 (부록 G A-3)

부록 G 는 「막대는 비율을 그린다 (★ 추천 · **상세**)」인데 상세에는 숫자만 있었다.
`.got`(받은 몫) · `.may`(근거는 있는데 못 받은 몫) 두 칸을 달았다.
나눗셈·뺄셈은 `ScoreView` 에서 한다 — 틀은 못 한다 (`V11-04`).

### 3-7 ★ 가이드 답 반영 — 만점 910 · 메뉴 여섯

가이드가 09-01 에 v326 물음 여섯에 다 답했다 (개정 1051).

| 무엇 | 전 | 후 | 근거 |
|---|--:|--:|---|
| `total_points` | 895 | **910** | 가이드가 소모품 15 를 되살렸는데 이 칸이 안 따라왔다 |
| `grade_base_points` | 895 | **910** | 개정 431 「빼는 갈래가 없다」 · `GRADE_EXCLUDED_AXES` 가 비었다 |
| 메뉴 상한 (`V11-157`) | 5 | **6** | 「09-01 에 추천이 들어갔다 · ★ 규격이 바뀌면 검사도 바꾼다」 |

★ 둘 다 **성분 합에서 낸다** — 손으로 안 적는다 (`STEP 128` 「직접 입력 금지」).

### 3-8 `V2-01` 219 → 1

```
볼보 148 · 렉서스 49  상세 봉투는 ok 인데 칸이 `not_requested` 였다
  → `tools/load_raw.py`  상세를 넣으면 `detail_status='ok'` 를 적는다
  → `tools/backfill_from_raw.py` 에 `lexus_detail` 을 더했다 (사이트를 안 두드린다)
KB 23                  차단 페이지 오살 (3-3)
`listpage` 20          내가 쓰던 이름이라 「매물 봉투가 아니다」 목록에서 빠졌다
  → `list` 로 맞췄다 (`S46-97`) · 파일 20개 · DB 봉투 20건
```

---

## 4. 조치 후 결과 (실제 출력)

```
$ python3.11 tools/check_all.py
통과 480 · fatal 9 · warn 13 · 환경 차이 1 · 미실행 11

$ python3.11 tools/run_tests.py
  test_run 통과 · test_score 통과 · test_screens 통과 · test_spec_ui 통과
  test_store 통과 · test_watch 통과 · test_web 통과
  ruff 통과 (F821,F811,B905,F841,DTZ005,F401)
결과: 실패 — test_endtoend.py

$ python3.11 tools/render_screens.py
렌더 33화면 · 못 낸 것 1 (/reports/{name} — 표본이 없다)
```

### 「몇 → 몇」

| 잣대 | 전 | 후 |
|---|--:|--:|
| `check_all` 통과 | 471 | **480** |
| `check_all` fatal | 16 | **9** |
| 리볼트 우리 대상 | 6 | **54** |
| 추천 화면 차종 | 24가지 | **13가지** (고르신 것) |
| 추천 화면 건수 | 5,470 | **1,810** |
| `V2-01` 차 | 219 | **1** |
| 차단으로 죽었던 매물 | 23 | **0** (되살림) |
| `S46-127` 손 수집기 | 11 | **0** |
| `S46-137` 안 읽는 열쇠 | 18 | **0** |

### 회차 잣대 셋 [실측 09-02 · `run.py collect --from S9` 뒤]

| 잣대 | 앞 회차 | 이번 |
|---|--:|--:|
| ① 아홉 사이트 A 이상 | 1 | ★ **67** |
| ② 전기차 A 이상 | 22 | ★ **42** |
| ③ 같은 차 짝 | 165 | ★ **180** |

```
잰 법   ①②  `_listings_where(ListingFilter())` 그대로 (손 SQL 을 안 짰다)
       ③   `view_track(...).total_pairs` — ★ 화면이 쓰는 그 함수다
까닭    ★ 가이드가 ★ 소모품 15 를 되살렸다 (총점 895 → **910**).
       ★ ★ 45점이 다 돌아와 ★ 등급이 통째로 움직였다
```

### 등급 분포 (추천 잣대와 같은 거르개)

| 등급 | 건수 |
|---|--:|
| E | 2,170 |
| D | 1,208 |
| F | 851 |
| C | 495 |
| B | 340 |
| PENDING | 94 |
| ★ **A** | ★ **67** |
| G | 44 |

---

## 5. 못 한 것 · 막힌 것 · 판단이 필요한 것

### 남은 fatal 10

| 검사 | 무엇 | 누구 몫 |
|---|---|---|
| `V3-77` | 갈래마다 하위 축 합 ≠ 갈래 표기 (2갈래) | ★ **가이드** |
| `V4-28` | 미분류 항목에 값 분포·선택지가 모자라다 | 나 |
| `V1-08` | 엔카 `list` 24건 전량 407 | 나 |
| `V1-21` | 받아 두고 안 펼쳐진 원문 1 | 나 |
| `V2-01` | 차 1 | 나 |
| `S46-96` | 파는데 코드가 없다 89칸 (reborncar 27 · kcar 26 · revolt 22 …) | ★ **가이드** — 이름 표 |
| `S46-142` | 「N 사이트」가 config 와 다른 곳 28 | ★ **가이드** — 문서 |
| `S46-152` · `S46-183` · `S46-207` · `S46-32` | 문서·이력 쪽 | ★ **가이드** |

### 마스터께 여쭐 것

```
1  리볼트 54건 중 ★ MODEL_Y 가 42 다.  ★ 규격은 38 이라 했다 —
   ★ 나흘 사이에 늘었다.  ★ 규격 표를 고칠지는 ★ 가이드 몫이라 안 건드렸다
2  보배(`paused`)의 `purchase_cost` 를 ★ 안 넣었다.
   ★ 안 사는 사이트의 구매비용을 ★ 지어낼 수 없다 (금지 6).
   ★ 다시 켜시면 ★ 그때 재서 넣는다
3  리볼트 매물 중 ★ 3건(GV60 2 · UX300e 1)이 ★ 차종 미정으로 남아 있다.
   ★ 옛 전량 훑기 때 이름으로 걸린 것이다.  ★ 버리지 않고 뒀다
4  추천 탭 2·3 은 ★ 여전히 빈 채다 — ★ 무엇을 낼지 ★ 마스터께서 정하실 자리다
```

---

## 6. 커밋

`30e00c0b` · `d53db52b` · (이 회차)
