# 20260828_1404_v281 — 전기차만 보는 길 · 인증 몫 여덟 곳 · 판매완료가 왜 보이나

명령서 **87장**(연료 거르개) · **certified_credit** · 마스터 물음 둘

---

## 1. ★★★ 87장 — 「전기차만 보고 싶은 거야」

### 1-1. 1차 결과 — 못 걸렀다

```
연료 거르개가 ★ 「자세한 조건」 안에 접혀 있었다 (listings.html:138)
게다가 ★ 원값 33가지를 그대로 늘어놓아 ★ 「전기만」을 고를 길이 없었다
```

### 1-2. ★ 값을 전수로 재서 묶었다 (지어내지 않았다)

`fuel_raw` 33가지 실측 —

| 값 | 건수 | | 값 | 건수 |
|---|--:|---|---|--:|
| 가솔린 | 11,160 | | EV | 84 |
| 가솔린+전기 | 2,402 | | 3.5 가솔린 | 78 |
| 디젤 | 1,647 | | LPG | 59 |
| **전기** | **1,196** | | 하이브리드(가솔린) | 50 |
| LPG(일반인 구입) | 525 | | 1.6 가솔린 하이브리드 | 298 |
| 하이브리드 | 240 | | 수소+전기 | 3 |

★★ **「전기만」은 ★ 정확히 `전기` · `EV` 다.**
★ `가솔린+전기`(하이브리드)와 `수소+전기`(연료전지)는 **안 넣었다.**

### 1-3. 고친 곳

| 파일 | 무엇 |
|---|---|
| `config/web.json` | `fuel_groups` — 전기만(값 못 박음) · 하이브리드만 · 가솔린 · 디젤 (**정본**) |
| `report/screens/build.py` | `fuel_groups()` · `_fuel_where()` — 갈래를 SQL 로 건다 |
| `web/views.py` | `_fuel_options()` — 원값 33가지가 아니라 **갈래 넷**을 낸다 |
| `web/templates/listings.html` | 연료 칩을 **기본 자리**로 (가격대 다음 · 등급 앞) |

★ **「자세한 조건」에 안 접었다** (마스터 ②).
★ **매물의 연료 칸으로** 거른다 — 차종이 아니다 (③).

### 1-4. 조치 후 (배포 실측)

```
/listings                    5,244건
/listings?fuel=electric      ★   431건
/listings?fuel=hybrid            576건
/listings?fuel=diesel            535건
「전기만」 첫 쪽 — MODEL_Y 20 · GV60 8 · GV70_EV 2   ★ 전기만 나온다
```

### 1-5. ★ 거르개 차례 (마스터 87장 ①)

```
차종 → 트림 → 가격대 → ★ 연료 → 등급 → 자세한 조건 → 정렬
```

★ 82장에서 말씀하신 차례(「등급 → 차종 → 트림 → 가격」)는 **87장으로 갈음**했다.
★ 시안 파일에는 **아직 연료 칩이 없다** — 「시안에 그려 뒀다」 하셨으나 파일에서 못 찾았다
(`grep 연료 v4m_listings_시안.html` → **0**). ★ 그래서 **말씀하신 차례로** 견주게 적어 두었다.

### 1-6. ★ ⑤ 검사 `S46-102`

```
S46-102  통과 — 「전기만」 431건이 다 전기다 (EV · 전기)
```

★ **화면이 쓰는 그 조건 그대로** 세어 본다 — 밖에서 따로 세지 않는다.
★ 0건이어도 실패로 낸다 (거르개가 안 먹는 것이다).

### 1-7. ★★★ 그런데 — ★ 볼보 전기차가 한 대도 없다

```
/listings?fuel=electric&site=volvo_selekt  →  ★ 0건
볼보 셀렉트 143건의 fuel_raw — ★ 가솔린 137 · None 6
슬러그도 xc60 · v60-cross-country · xc40 · s60 넷뿐이다
```

★ **사이트는 전기를 준다.** 목록 원문 40쪽을 훑어 슬러그를 셌다 —

```
xc60 · v60-cross-country · xc90 · xc40 · s90 · v90-cross-country
★ ex30 ★ · s60 · ★ ex30-cross-country ★ · v60cc
```

★★ **`ex30`·`ex30-cross-country`(전기)를 우리가 안 받고 있다.**
★ `targets.json` 의 `site_query.volvo_selekt.slug` 에 없어서다.
★ **`targets.json` 은 마스터께서 넣으신다 하셨으므로 안 건드렸다** — 잰 것만 낸다.
★ **물음 — EX30 을 22종에 넣으시겠습니까.** (C40 은 오늘 사이트에 없다.)

---

## 2. ★★ `certified_credit` — 여덟 곳에 몫을 준다

### 2-1. 1차 결과 — ★ config 에 있는데 코드가 안 읽었다

```
config/scoring.json  certified_credit  ratio 0.8 · 사이트 여덟 · 축 열하나
grep -rn certified_credit --include=*.py  →  ★ 한 곳도 없다
```

★ 규칙 1 대로 **코드를 고쳤다** — `scoring.json` 은 손대지 않았다.

| 파일 | 무엇 |
|---|---|
| `score/scorer.py` | `_certified_credit()` · 점수 자리에서 ① 사이트가 목록에 있고 ② 그 축이 **근거 없이 0** 이면 ③ 배점 × 0.80 |

★ **분모(910)는 그대로** 둔다.
★ **받은 몫을 「근거 있는 축의 합」에도 넣는다** (규격 `_pending_note`) — 그래야 판정 중에서 풀린다.
★ 근거가 있으면 안 덧붙인다. 이미 점수가 났으면 안 덧붙인다.

### 2-2. ★ 마스터께서 재라 하신 넷 — ★ 다 넘었다

| site | 전 | 말씀 | ★ 실측 | % | 문턱 60% |
|---|--:|--:|--:|--:|---|
| heydealer | 563 | 761 | ★ **766.8** | 84% | ★ 넘는다 |
| reborncar | 479 | 677 | ★ **723.3** | 79% | ★ 넘는다 |
| volvo_selekt | 399 | 597 | ★ **603.5** | 66% | ★ 넘는다 |
| kcar | 488 | 595 | ★ **613.2** | 67% | ★ 넘는다 |

★ 말씀하신 값보다 조금씩 높다 — 그 사이 상세를 더 받았기 때문이다.

### 2-3. 사이트별 판정 중

| site | 매물 | 판정 중 | 근거 |
|---|--:|--:|--:|
| encar | 8,686 | 125 | 807.1 |
| kbchachacha | 287 | **239** | 484.2 |
| hyundai_cert | 174 | ★ **0** | 714.6 |
| volvo_selekt | 140 | ★ **6** | 603.5 |
| heydealer | 96 | ★ **0** | 766.8 |
| reborncar | 72 | ★ **0** | 723.3 |
| bobaedream | 55 | **55** | 309.0 |
| kcar | 34 | ★ **0** | 613.2 |
| lexus_certified | 10 | ★ **0** | 640.0 |
| bmw_bps | 9 | **9** | 18.0 |

★ 안 주는 곳(엔카·KB·보배)과 원문이 없는 BMW 만 판정 중이 남는다.

### 2-4. ★ 「S/A/B 몇 · 판정 중 몇 · 수입차만 따로」

| 등급 | 전체 | 수입차만 |
|---|--:|--:|
| **S** | **18** | **0** |
| **A** | **425** | **179** |
| **B** | **893** | **610** |
| C | 907 | 507 |
| D | 1,950 | 1,017 |
| E | 2,395 | 1,277 |
| F | 801 | 413 |
| G | 41 | 24 |
| **판정 중** | **434** | **192** |
| EXCLUDED | 1,697 | 982 |
| 합 | 9,563 | 5,203 |

★ 수입 13종 기준이다 (`site_query.encar.Manufacturer` 로 갈랐다).

---

## 3. ★ 마스터 물음 — 「판매완료·취소 매물이 왜 보이나」

### 3-1. 답 — ★ 목록 조건에 그것을 빼는 것이 **없다**

```
목록이 거는 조건 (실측)
  1=1 · 광고 갈래 · 판매 갈래 · 제외 아님 · 같은 차 접기 · status <> 'out_of_scope'
★ 「gone」(내려간 것)을 빼는 조건이 ★ 없다
★ 「sales_status」(계약완료·예약중)를 보는 조건도 ★ 없다
```

| 지금 목록 5,244건 안에 | 건수 |
|---|--:|
| 내려간 것 (`status='gone'`) | **136** |
| 예약중 (`sales_status='reserved'`) | **17** |
| ★ 합 | **153건 (2.9%)** |

★ 계약완료(`CONTRACT`) 771건은 **다른 조건에 이미 걸려** 목록에 안 나온다.

### 3-2. ★ 안 고쳤다 — 규격이 「빼라」고 안 했다

```
docs/chapters/41-view.md:257
  | `gone` | ★ **목록에서 사라짐** | muted |
★ 규격은 ★ 「그렇게 **적어라**」고만 한다.  ★ 「목록에서 빼라」가 아니다
```

★ **물음 — 목록에서 뺄까요, 지금처럼 두고 딱지만 달까요.**

### 3-3. ★ 그리고 딱지가 규격과 다르다

```
규격  gone → 「목록에서 사라짐」   (41-view.md:257)
화면  gone → 「내려감」          (config/labels.json STATUS_LABELS)
```

★ **안 바꿨다** — 낱말은 규격이 박아 둔 것이라 개발측이 안 고친다 (규칙 1·2).
★ **어느 쪽으로 맞출지 청한다.**

---

## 4. ★ 마스터께서 보신 「내부 오류」 — 짚었다

```
Caddy 접근 로그 (새로 켠 것이 값을 했다)
  최근 90분 500 ★ 두 건 — ★ 둘 다 "uri":"/login"
```

★ **재판정이 도는 동안 DB 쓰기가 막혀 로그인이 500 이 된 것**이다 (v273 과 같은 자리).
★ 그때 내 CLI 재판정도 같은 까닭으로 죽었다 (`s9: database is locked`).

### 고친 곳

| 파일 | 무엇 |
|---|---|
| `collect/runner.py` | **S9 · S10** 반복문에도 `raw_tick()` 을 넣었다 |

★ 전에는 **S4·S6 에만** 있었다 — 그래서 S9·S10 이 도는 동안 잠금이 안 끊겼다.

```
실측  고치기 전 쓰기 5/6 성공 · 한 번 10초 넘김
     (웹은 30초를 기다리므로 그보다 긴 창에서 500 이 났다)
```

---

## 5. ★ 시험 둘을 새 정책에 맞췄다 (규칙 1)

가이드가 r798 로 곡선 둘을 바꾸자 `test_score` 가 깨졌다.

```
옛 시험  「예산과 같으면 절반(50/95)」 · 「신차가 65% 면 만점 75」
새 정책  기준점 = 60% 수준(57) · 신차가 40% 만점 · 80% 거의 0 (마스터 확정 08-28)
고쳤다  tests/test_score.py — 새 곡선으로.  ★ config 가 정본이다
확인   test_score ★ 통과 · ruff ★ 통과 (안 쓰는 import 하나도 뺐다)
```

---

## 6. check_all · run_tests 실제 출력

```
python3.11 tools/check_all.py
  통과 396 · fatal 10 · warn 12 · 환경 차이 1 · 미실행 8
  ■ FATAL
    S44-5 · S45-3(docs/INDEX.md) · S46-36 · V1-21 · V1-23 · V2-01
    V11-12 · V11-159 · V11-45 · V11-76
  ★ S46-98 · S46-99 · S46-100 · S46-102 ★ 넷 다 통과

python3.11 tools/run_tests.py
  결과: 실패 — test_endtoend.py  (1파일 · V11-12 규격 중복 행 탓)
  ★ test_score · ruff 는 ★ 고쳐서 통과한다
```

★ `fatal` 이 8 → 10 으로 늘었다. **둘 다 규격·생성물 쪽이다** —
`S45-3`(옛 총점이 `docs/INDEX.md` 에) · `V1-21`(안 펼친 원문 16건 — 오늘 받은 K카 상세다).

---

## 7. ★ 물음 (본문에 적는다)

1. ★★★ **볼보 EX30 을 22종에 넣으시겠습니까** (1-7장). ★ 사이트가 주는데 우리가 안 받는다.
   ★ 마스터께서 볼보 전기차를 고르시는데 **지금은 한 대도 없다.**
2. ★★ **판매완료·내려간 매물을 목록에서 뺄까요** (3-2장). ★ 규격은 「그렇게 적어라」고만 합니다.
3. ★ **`gone` 딱지 — 규격 「목록에서 사라짐」 ↔ 화면 「내려감」** (3-3장).
4. ★★ **시안에 연료 칩이 없습니다** (1-5장). ★ 그려 주시면 `S46-100` 이 시안으로 견줍니다.
5. ★★ **주행 문(gate)** · **볼보 보증 셋(V60CC·XC40·C40)** — v279 에서 청한 것 그대로입니다.
6. ★★★ **비밀번호가 `docs/guide/03_이력.md` 에 그대로 있습니다** — **여덟 번째 청입니다.**

---

## 8. 못 한 것 · 다음

| # | 무엇 | 왜 |
|:--:|---|---|
| 1 | 볼보 EX30 수집 | 위 7장-1 답을 받고 |
| 2 | KB 상세 | 마스터 차례의 넷째 |
| 3 | 판매완료 거르기 · `gone` 딱지 | 위 7장-2·3 |
| 4 | 주행 문 · 볼보 보증 셋 | 위 7장-5 |
| 5 | 71~79장 (검사·미분류·사진) | ★ 뒤로 미루라 하셨다 |
