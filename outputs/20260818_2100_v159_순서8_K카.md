# 순서 8 — K카 · `adapters/kcar.py`

2026-08-18 21:00 · v159 · **SPEC-2026.08.18-r365**

`inbox/ORDER_00_순서.md` **8번**

---

## 1. 무엇을 하려 했나 (지시 원문)

```
1  source_id 컬럼이 문자를 받는지 확인 (EC61393706)
2  XHR 경로 6개를 실제로 눌러 본다
3  adapters/kcar.py
4  한 차종만 끝까지 — 목록 → 상세 → 파싱 → 판정
5  차대번호로 크로스사이트
```

---

## 2. 8-1 `source_id` — 통과

```sql
core_listing.source_id   TEXT NOT NULL   -- API 호출용.  키가 아니다
raw_response.source_id   TEXT
```

**둘 다 TEXT 입니다.** `EC61393706` 을 그대로 받습니다.  고칠 것이 없습니다.

---

## 3. ★★★ 8-2 여섯 경로를 눌렀습니다 — 문서에 없던 호스트를 찾았습니다

### ① 문서가 적은 경로는 전부 껍데기이거나 404 였습니다

```
m.kcar.com/bc/car-info-detail-of-ng      404
m.kcar.com/bc/detail/popup/CarBasInfo    200  2,125,487B  ← SPA 껍데기 HTML
m.kcar.com/bc/detail/popup/CarInsp       200  2,125,341B  ← 껍데기
m.kcar.com/bc/detail/popup/InsuHistSmry  200  2,125,485B  ← 껍데기
m.kcar.com/bc/detail/popup/DgnosSmry     200  2,125,458B  ← 껍데기
m.kcar.com/bc/car-option                 404
```

**`/bc/detail/popup/*` 는 API 가 아니라 화면 경로입니다.**

### ② `api.kcar.com` · `marketm-api.kcar.com` 도 전부 404

여덟 가지 조합을 눌렀습니다.  하나도 안 열립니다.

### ③ SSR HTML 에도 값이 없습니다

```
window.__NUXT__  991자 · carNm · prc · mlgKm · insp — 하나도 없음
```

### ④ ★ 번들에서 baseURL 을 뽑았습니다

`/_nuxt/*.js` 아홉 개를 받아 `baseURL` 을 찾았습니다.

```
https://mapi.kcar.com            ← ★ 문서에 없던 것
https://marketm-api.kcar.com
https://marketm-api.kcar.com/api/v1
```

### ⑤ `mapi.kcar.com` 이 JSON 을 줍니다

```
mapi.kcar.com/bc/car-info-detail-of-ng   200   74,052B   JSON
mapi.kcar.com/bc/car-insp/photo/cm       200      462B   JSON
```

---

## 4. ★★ 한 경로가 전부를 줍니다 — 엔카는 아홉 번 부릅니다

`/bc/car-info-detail-of-ng` 하나에 **48개 묶음**이 들어 있습니다.

| 무엇 | 자리 | 실측 |
|---|---|---|
| 상세 | `rvo` · `masterInfo` | 차종 · 상태 · 판매자 |
| 옵션 | `optList` 42 · `mainOptList` 12 | |
| **보증** | `warrantyInfo` 5 | `KW6` · 가입금액 498,000 |
| **타이어** | `tireDtlList` 5 | **`tirResQty: 6.2`** · 235/50R19 |
| 소유이력 | `carOwnrChngHistList` 2 | 신규등록(신조차) 2025-07-22 |
| 보험 | `carhistory` | 조회일 · 최초보험일 |
| 진단 | `master` | 진단일 · 진단자 |
| 사진 | `photoList` 7 · `outerPhotoList` 2 | |

★★ **타이어 트레드가 여기 있습니다.**
엔카는 `unified-report` 가 401 이라 못 받는 값입니다 (순서 3-2 막힘).
**K카를 붙이면 소모품 축 10점이 K카 매물에서는 살아납니다.**

---

## 5. 8-3 `adapters/kcar.py` 를 만들었습니다

```
base_url   https://mapi.kcar.com
paths      detail      /bc/car-info-detail-of-ng?i_sCarCd={source_id}
           inspection  /bc/car-insp/photo/cm?i_sCarCd={source_id}
timeout 20 · interval 1.5초
```

```
★ list_url 은 PolicyError 를 던집니다 — 지어내지 않습니다 (STEP 17a)
  「K카 목록 경로를 아직 못 찾았다.  상세는 source_id 를 알면 받을 수 있다」
```

---

## 6. 조치 후 결과 — ★ 실제 출력

```
python3.11 -c "… KcarAdapter(load_config('.')).detail_urls('EC61393706')"
https://mapi.kcar.com/bc/car-info-detail-of-ng?i_sCarCd=EC61393706
https://mapi.kcar.com/bc/car-insp/photo/cm?i_sCarCd=EC61393706

python3.11 tools/check_all.py
통과 274 · fatal 6 · warn 7 · 환경 차이 1 · 미실행 2

python3.11 tools/run_tests.py
결과: 실패 — test_endtoend.py
```

---

## 7. ★ 막힘 — 8-4 · 8-5 를 못 했습니다

### 목록 경로를 못 찾았습니다

```
mapi.kcar.com 에서 여섯 경로를 눌렀습니다 — 전부 404
  /bc/car-list · /bc/search/car-list · /bc/car-search-list
  /bc/car-list-of-ng · /bc/search/CarList · /bc/car-list-ng
```

★ 번들의 호출이 `$AsyncAxiosBuilder.params(searchCondition).build()` 라
**파라미터가 객체입니다.**  이름을 모르면 못 부릅니다.

```
못 함   8-4 한 차종 끝까지 (목록이 없어 매물 ID 를 못 모읍니다)
       8-5 차대번호 크로스사이트 (매물이 없어 견줄 것이 없습니다)
```

### 어떻게 뚫을까요 — 셋 중 하나

```
① 마스터가 K카 목록 화면에서 브라우저 수집을 눌러 주십니다
   ★ 엔카와 같은 길입니다.  /admin/collect 에 K카를 더하면 됩니다
② 번들을 더 파서 searchCondition 의 키 이름을 찾습니다
   ★ 2.7MB 짜리가 있어 시간이 걸립니다
③ 매물 ID 를 다른 곳에서 얻습니다 (검색엔진 · 사이트맵)
```

**①이 가장 확실합니다.**  정해 주시면 그대로 하겠습니다.

---

## 8. 못 한 것 · 판단이 필요한 것

### ① `sites.json` 의 K카는 아직 `planned` 입니다

어댑터는 생겼지만 **목록이 없어 매물이 안 들어옵니다.**
`status` 를 `active` 로 올리면 V9-06 이 「K카 배지가 없다」로 걸립니다.
목록이 뚫린 뒤에 올리겠습니다.

### ② K카 파서가 없습니다

`parse/kcar/` 가 없습니다.  원문 구조는 4절에 적었으니
목록이 뚫리는 대로 만들 수 있습니다.

### ③ 그대로 남은 fatal 6

```
V1-16 · V2-01 · V2-18 · V4-11(32) · V3-38 · V11-77
```
