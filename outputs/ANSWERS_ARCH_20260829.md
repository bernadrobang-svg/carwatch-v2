# 물음지 58개에 답한다 (SPEC-2026.08.30-r1003)

```
version  SPEC-2026.08.30-r1003
follows  `docs/ARCHITECTURE_20260830.md` · outputs/QUESTIONS_ARCH_20260829.md
sources  개발측 물음지 7b5c7f4 · 가이드 실측 08-29
```

★ **잘 물었다.** 58개 중 **내가 안 정한 것이 34개**다. 설계도가 그만큼 성겼다.
★ 아래는 **내 답**이다. ★ 별이 붙은 다섯은 **마스터께서 정하신다** — 따로 올린다.

---

# A. 급한 것 — **먼저 한다** (2건)

| # | 답 |
|--:|---|
| 57 | ★ **지워라.** 검사 임시물이라 P3(원문 무손실)에도 「구조를 손대지 마라」에도 안 걸린다 |
| 58 | ★ **고쳐라.** `_sweep_temp` 주석이 이미 「검사가 디스크를 채우지 않는다」다 — **자리를 하나 빠뜨린 것**이다 |

---

# B. 크기와 차례 — **한꺼번에 안 간다** (7건)

| # | 답 |
|--:|---|
| 1 | ★ **얹는 것이 아니라 이어받는 것**이다. 개정 857 의 「받기/넣기 가르기」가 **1·2걸음의 앞선 꼴**이다. 걷어내지 마라 — **같은 뜻을 파일로 넓히는 것**이다 |
| 2 | ★ **감싼다.** 다시 쓰지 마라. 지금 수집기의 「받는 부분」은 그대로 두고 **`upsert_core` 부르는 자리만 파일 쓰기로 바꾼다** |
| 3 | ★ **셋 다 산다.** 1걸음이 DB 를 안 열면 `S46-124`·`S46-126`·`S46-128` 은 **「그 자리가 없다」로 통과**한다. ★ 다만 **죽은 검사가 된다** — `S46-151` 이 그것을 대신한다(#56) |
| 4 | ★ **나란히 돌린다.** 멈추면 가격 변동이 끊긴다(#53 도 같다). 다만 **한 사이트는 한 길만** — 옮긴 사이트는 새 꼴만 쓴다 |
| 5 | ★ **사이트 하나씩.** 차례 — ① 렉서스(가장 작다·74건) ② 볼보 ③ BMW ④ 보배 ⑤ 헤이딜러 ⑥ 현대 ⑦ K카 ⑧ KB ⑨ 리본카 |
| 45 | ★ 걸린다. 볼보·BMW 는 **첫 바퀴가 전건**이다(볼보 157·BMW 376) — 작으니 한 회차에 된다 |
| 53 | #4 와 같다 — **계속한다** |

---

# C. 파일 꼴 — 내가 안 정한 자리 (11건)

| # | 답 |
|--:|---|
| 6 | ★ **`config/deploy.json` 의 `work_dir` 아래 `raw/`.** 지금 7.3G 뿐이니 **#57 을 먼저** 한 뒤 다시 잰다 |
| 7 | ★ **누른다.** `store/raw.pack_body`(`CWZ1`)를 **그대로 쓴다** — 새로 만들지 마라 |
| 8 | ★ **한 파일에 한 매물.** 폴더는 **`{source_id}` 앞 두 글자**로 나눈다 — `raw/{site}/detail/{날짜}/{앞2}/{source_id}.json` |
| 9 | ★ **덮어쓰지 마라.** `{source_id}__{HHMMSS}.json` 으로 붙인다. P3 다 |
| 10 | ★ **묶음마다 센다** — `page-{묶음키}-{NNNN}.json`. 묶음키는 그 사이트의 질의 열쇠(KB `carSeq` 묶음 = `maker-class-car`) |
| 11 | ★ **`page-0001.json`** 을 쓴다. 한 덩이도 1쪽이다 — **사이트마다 다른 꼴을 만들지 마라**(오판 193) |
| 12 | ★ **둘 다 적는다** — `http_code`(숫자)와 `status`(우리 판정). 지금 `raw_response` 가 이미 둘을 갖고 있다 |
| 13 | ★ **남긴다.** `status='error'` 로. 「없음으로 저장하지 않는다」는 **`core_listing`** 규칙이지 원문 규칙이 아니다 |
| 14 | ★ **임시 이름 → 옮기기.** `.part` 로 쓰고 `os.replace` 한다. 반쪽 파일은 2걸음이 **안 읽는다** |
| 16 | ★ **파일 안에만.** 경로에 넣으면 같은 매물이 갈래마다 흩어진다. 갈래로 찾는 것은 `load_log` 가 한다 |
| 44 | ★ `page-{NNNN}.json` 을 쓴다. **`source_id` 가 없는 것은 목록이다** |

---

# D. 임시표 — 내가 안 정한 자리 (9건)

| # | 답 |
|--:|---|
| 17 | ★ **다 갖지 않는다.** 목록·상세가 주는 칸만. `core_listing` 을 늘리면 두 곳을 고쳐야 한다 |
| 18 | ★ **안 들어간다.** `split_pii` 를 **2걸음에서 먼저** 부른다. 임시표에는 `plate_hash` 만 |
| 19 | ★ **자식 표도 임시표를 둔다** — `stage_record`·`stage_inspection`. 안 그러면 「임시표만 버리면 된다」가 깨진다(개발측 말이 맞다) |
| 20 | ★ **같이 쓴다.** `run_id` 로 가른다 |
| 21 | ★ **`run_id` 가 살아 있나로 가른다** — 시각으로 가르지 마라. `collect_run` 표가 이미 회차를 안다 |
| 40 | ★ **내가 틀렸다.** 「가이드가 임시표로 검증한다」는 **DB 를 못 여는 나로선 안 된다** → **#41 로 바꾼다** |
| 41 | ★ **2걸음이 `outputs/stage/{run_id}.jsonl` 을 함께 뜬다.** 한 줄 = 한 매물. 내가 그것을 읽는다 |
| 51 | ★ **내가 푼다.** #41 이 있어야 푼다 |
| 55 | ★ **따로 둔다** — `stage_{run_id}.db`. 회차가 끝나면 **파일째 지운다**. 잠금을 안 다툰다 |

---

# E. 맞대기 — 내가 안 정한 자리 (8건)

| # | 답 |
|--:|---|
| 22 | ★ **값·주행·연식·`sales_status` 넷만** 대본다. `last_seen`·`collected_at`·`parsed_at` 은 **빼라** |
| 23 | ★ **못 재는 사이트는 「늘 받는다」가 아니다** — `load_log.file_hash` 로 가른다. 목록 원문이 같으면 그 매물도 안 바뀐 것이다 |
| 24 | ★ **쓴다.** 재 봤다 — 리본카 사이트맵 `<url>` 블록 2,716개 중 **매물 1,082건에 `lastmod` 가 다 있다**(`2026-08-29`). ★ 이것으로 #23 이 리본카에서 풀린다 |
| 25 | ★ **반드시 들어간다.** `sweep_gone_groups` 의 「끝까지 받았나」를 그대로 쓴다 — 오판 161 을 되돌리지 마라 |
| 26 | ★ **넣기가 끝나면 그 자리에서 지운다.** 하루만 남긴다 |
| 27 | ★ **`DETAIL_BATCH` 를 올리지 마라**(20건 40% 막힘). 3걸음이 **줄여 주는 것**이 답이다 — 첫 바퀴만 108일이 아니라, **KB 는 목록이 매물번호뿐이라 #23·#37 로 줄인다** |
| 28 | ★ **시세를 회차 앞에 한 번 낸다.** 그 회차 안에서는 **한 기준**을 쓴다 |
| 29 | ★ **날마다 한 번 전건**은 그대로 둔다. 4걸음의 「바뀐 것만」은 **회차 안**의 이야기다 |

---

# F. 적재 로그 (4건)

| # | 답 |
|--:|---|
| 36 | ★ **sha256 · 압축 전 원문**으로 |
| 37 | ★ **`parse_version` 을 둔다** — 개발측 말이 맞다 |
| 38 | ★ **둔다.** `load_log(file_path, parse_version)` 이 열쇠다. 파서가 바뀌면 다시 뽑는다 |
| 39 | ★ **안 지운다.** 한 줄이 작다 — 24,717줄은 몇 MB 다 |

---

# G. 옮기고 지우기 (3건)

| # | 답 |
|--:|---|
| 42 | ★★ **08-29 감독 지시로 멈춘다 — 지우지 마라.** 옮기기까지는 해도 된다. ~~다 뺀다.~~ 다만 **#57 로 12GB 를 비운 뒤**에. 258.7MB 이니 자리는 넉넉하다 |
| 43 | ★ **바이트 같음.** 08-28 압축 때 한 **행마다 왕복 검증** 그대로 |
| 54 | ★ **`load_log` 에 `fetched_at` 을 함께 넣는다.** 파일 안에도 있다 — 정본이 사라지지 않는다 |

---

# H. 파서·사이트 (6건)

| # | 답 |
|--:|---|
| 46 | ★ **재라.** 목록이 다 주면 상세를 **안 받는다**로 닫는다 |
| 47 | ★ **고친다.** 다만 **그 사이트를 옮길 때 함께** — 지금 따로 건드리지 마라 |
| 48 | ★ **목록도 안 받는다.** 대상 0건인 1,025건을 날마다 받을 까닭이 없다. ★ 마스터께서 「기아를 볼까」 하시면 그때 켠다 |
| 52 | ★★ **구조 앞에 고친다.** 개발측 말이 맞다 — **마스터께서 몇 주 동안 틀린 값을 보시면 안 된다.** 파서 결함 여섯은 **지금 고쳐라** |
| 56 | ★ `S46-151` — **「열 수집기가 `fetch_*`/`load_*` 로 갈려 있는가」**를 잰다. 내가 규격에 적었으니 **개발측이 지어 만들지 마라** |
| 15 | ★ **`save_browser_catch` 도 파일을 쓴다.** 엔카만 옛 꼴로 남으면 오판 193 이 되풀이된다 |

---

# I. ★★ 마스터께서 정하실 것 — **다섯**

| # | 무엇 | 가이드 안 |
|--:|---|---|
| ★ 31 | 팔린 차를 **목록에 두고 딱지만**(08-29 지시) vs **안 뿌린다**(설계도) — **어느 쪽인가** | ★ **08-29 지시가 나중이다.** 「목록에 두고 딱지·흐리게·맨 뒤」가 맞고 **내 설계도가 틀렸다**. 되돌리지 마라 |
| ★ 33 | 「**잘 팔렸다**」가 무엇인가 — 빨리? 값에 가깝게? | 둘 다 내고 **마스터께서 보시고 고르시게** |
| ★ 35 | 계약 중(`CONTRACT` 830건)은 **팔린 것인가 아직 사는 것인가** | 계약은 깨진다 → **「아직 사는 것」**으로 보고 통계 분모에서 뺀다 |
| ~~49~~ | 성능점검 여섯 축을 ⑥(80%)로 볼지 | ★ **마스터 몫이 아니다** — 파서가 뽑으면 수로 갈린다.  가이드가 정한다 |
| ~~50~~ | 전기차 누유 축 | ★ **닫혔다** — 마스터 「전기차라 누유가 없으니 **만점** 줘야지」 · `f-table` 갈래 ⑦.  ★ **분모는 910 그대로** |

★ 30·32·34 는 내가 답한다 —
**30** `sales_status` 낱말이 대소문자로 갈려 있다(`CONTRACT` 830·`RESERVED` 98·`listed` 226·`reserved` 46).
★ **`dict_enum` 에 넣어 우리 말로 옮긴다.** 원문은 그대로 두고 **화면은 `mapped` 를 본다**.
**32** `/sold` 다 — `UI_REVIEW` 30장·시안에 이미 있다.
**34** **「사라지기 직전 값」**이다. 화면에 **「판 값이 아니라 마지막에 본 값」**이라 적는다 — 모르는 것을 아는 척하지 않는다.
