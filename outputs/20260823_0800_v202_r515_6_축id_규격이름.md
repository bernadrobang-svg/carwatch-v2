# r515 단계 6 — 축 id 7개를 규격 이름으로 (S43-2b)

`SPEC-2026.08.22-r515` · `v202` · 2026-08-23 08:00

---

# 0. ★ 지시서가 서로 어긋난다 — 지적 (금지 8)

```
명령서 r515 머리말
  「★ 축 id 는 ★ config/scoring.json 이 정본이다 —
    state.repair · history.usage · history.lien · spec.trim · spec.options ·
    taste.picked · value.depreciation
    ★ 규격 문서에 다른 이름이 보이면 ★ config 가 맞다 (개정 489)」

같은 명령서 ★ 작업 순서 6
  「★★ 축 id 7개를 ★ 규격 이름으로 — 5 가 끝난 뒤에  (1-5 · S43-2b)」
```

```
★ 두 줄이 ★ 반대 방향이다.  머리말은 「코드가 기준」 · 순서 6 은 「규격이 기준」
★ 어느 쪽을 따랐나 — ★ 순서 6 이다.  까닭 셋
  ① 개정 504 가 ★ 마스터 확정을 적었다 — 「규격이 기준이다. 코드 이름을 규격에 맞춘다」
  ② 검사 `S43-2b` 가 ★ 그 방향으로만 통과한다 (「config 에 옛 이름이 남았나」)
  ③ 마스터 지시 08-23 — 「6번(축 id 7개)은 5번 뒤에」 → ★ 하라는 뜻이다
★ 머리말 줄은 개정 489 것이고 개정 504 가 뒤집은 것으로 읽었다.
  ★ 아니면 되돌린다 — git revert 하나면 된다
```

---

# 1. 바꾼 이름 일곱

| 전 (코드) | 후 (규격) |
|---|---|
| `value.depreciation` | **`value.origin`** |
| `state.repair` | **`state.my_cost`** |
| `history.usage` | **`history.use`** |
| `history.lien` | **`history.seizing`** |
| `spec.trim` | **`taste.trim`** |
| `spec.options` | **`taste.option`** |
| `taste.picked` | **`taste.fitting`** |

---

# 2. 개정 504 「반드시 적을 것 넷」을 그대로 했다

## ⓐ `scoring.json` 은 `components` 와 `axis_rules` 둘 다

```
components   일곱 이름을 갈았다
groups       취향 ["taste.", "spec."] → ★ ["taste."]  (spec. 은 죽은 접두가 됐다)
axis_rules   ★ spec 묶음을 taste 로 합쳤다 —
             trim_single_full · option_percentile · option_min_sample
             ★ 한쪽만 고치면 배점은 새 이름인데 규칙이 옛 이름을 찾는다
쓰는 곳       collect/runner.py 의 policy.rule("spec") 4곳 → rule("taste")
```

## ⓑ `result_axis.axis` 마이그레이션을 ★ 재계산 ★ 전에

```
value.depreciation → value.origin      3,916행
state.repair       → state.my_cost     3,916행
history.usage      → history.use       3,916행
history.lien       → history.seizing   3,916행
spec.trim          → taste.trim        3,916행
spec.options       → taste.option      3,916행
taste.picked       → taste.fitting     3,916행
                              합 ★ 27,412행 · 옛 이름 남음 ★ 없음
★ 그 다음에 run.py collect --from S9 를 돌렸다 (순서를 지켰다)
```

## ⓒ `labels.json` · `unknown_split.json` · `build.py` · `tests/*`

```
고친 파일 19 — analyze/axes.py(7) · analyze/axis/{history,state,taste,trim,value}.py(7) ·
  config/{labels,scoring,unknown_split}.json(30) · report/screens/build.py(11) ·
  score/penalty.py(6) · tests/{test_score(44),test_report,test_run,test_screens}(48) ·
  tools/verify_axes.py(7) · validate/v3_logic.py(21)

★ 안 바꾼 파일 둘 — validate/v0_guide.py · tools/gen_table.py
  ★ 둘은 ★ 옛↔새 대조표를 가진 파일이다.  바꾸면 검사가 스스로를 못 잡는다
★ 안 바꾼 문자열 둘 — spec.colorName · spec.mileage
  ★ 이것은 ★ 원문 경로다.  축 id 가 아니다.  경계(\\b)를 보고 건드리지 않았다
```

## ⓓ 배포 주소 「축별 미달」 표를 ★ 눈으로

```
$ curl https://43.201.16.78.sslip.io/
축별 미달 — 확인 안 된 건수 · 그래서 잃은 점수
  state.frame      1,763건  75,809점
  ★ taste.fitting   3,916건  70,488점      ← 새 이름
  state.accident   1,369건  69,819점
  state.consumable 3,916건  58,740점
  state.outer      1,763건  49,364점
  ★ state.my_cost   1,386건  38,808점      ← 새 이름
★ 렌더 파일이 아니라 ★ 배포 주소다
```

---

# 3. 검산

```
$ python3.11 -c "from validate.v0_guide import s43_2b_axis_renamed as f; print(f())"
전   (False, 'config 에 옛 축 이름이 ★ 7개 남았다 — value.depreciation→value.origin · …')
후   (True,  'config 가 규격 이름을 쓴다')          ★ S43-2b 통과

config 에만 있는 축 : 없음
DB 에만 있는 축     : 없음
분모                : 910
```

## 등급 분포 — ★ 이름만 바꿨으므로 그대로다 (숫자만 남긴다)

| 등급 | 전 | 후 |
|---|--:|--:|
| S | 48 | 48 |
| A | 375 | 375 |
| B | 448 | 448 |
| C | 537 | 537 |
| D | 948 | 948 |
| E | 699 | 699 |
| F | 52 | 52 |
| G | 17 | 17 |
| 제외 | 789 | 789 |
| 등급 없음 | 3 | 3 |

---

# 4. 검사 — 실제 출력

```
run_tests
  1차   결과: 실패 — test_admin_flow.py, test_integration.py
        까닭  ★ 시험이 「spec」 축 묶음을 재배분한다.  ★ 그 묶음이 없어졌다
              「축에 성분이 없다: spec」 (score/adjust.py:92)
        고침  두 시험을 ★ taste 로 (개정 504 대로 spec.* 가 taste.* 가 됐다)
  조치 후  결과: ★ 통과 (19파일 + ruff)

check_all  통과 343 · fatal 3 · warn 10 · 환경 차이 1 · 미실행 10
  FATAL  V1-21 · V1-23 · V11-76 — ★ 셋 다 이번 작업 이전부터 있던 것
```

---

# 5. 커밋

```
8d07b5e
```
