# 20260826_1337_v273 — ★ 로그인 500 의 뿌리를 잡았다 · 볼보 상세 60건 · S46-98

마스터 지시 08-26 (둘째 묶음) ①②③ · 앞 묶음 ②(볼보 차례)

---

## 1. 무엇을 하려 했나 — 지시 원문

```
① 목록 카드에 셋이 없다
   ✘ 제조사 보증 · ✘ 사이트 보증 — ★ 판정 다섯을 다섯 다 내라
   ✘ 순위 「N위」 — ★ 정렬이 추천 순위인데 순위가 없다
② ★★★ /watch · /admin 이 403 이다. 로그인이 안 먹는다
   ★ 74장에서 지적했는데 그대로다. 이것부터 고쳐라
   ★ 마스터께서 담아 두신 차를 못 보신다
   ★ 고친 뒤 관심 카드에 「바뀐 것·담은 날·비교 네모·관심 빼기」가 있는지 재라
③ /track · /notready · / 는 빠진 것 없다. 그대로 둬라
★ 검사 S46-98 — 시안에 있는 낱말이 화면에 없으면 실패
```

---

## 2. ★★★ ② 로그인 500 — ★ 뿌리를 잡았다

### 2-1. 1차 결과 — ★ 「비밀번호가 틀린 것」이 아니었다

```
auth_login_attempt 최근 (고치기 전)
  60  display_name=''      succeeded=0  reason='없는 계정'  08-26 04:00
  59  display_name=''      succeeded=0  reason='없는 계정'  08-25 15:27
  58  display_name=''      succeeded=0  reason='없는 계정'  08-25 12:23
  57  display_name='admin' succeeded=1                     08-25 10:26  ← 마지막 성공
```

★ 이름이 **빈 문자열**로 들어와 있었다. 실제로 두드려 보니 —

```
POST /login (csrf 정상 · name=admin · secret=틀린비번)   →  ★ 500
```

★ **403 이 문제가 아니었다.** `/watch` · `/admin` 의 403 은 **로그인 앞이라 정상**이다
(명령서 74장 실측과 같다). 못 들어가신 까닭은 **로그인 자체가 500 이라서**다.

### 2-2. ★ 뿌리 — 파이프라인이 DB 를 통째로 잠근다

실측으로 세 겹을 벗겼다.

```
① 재판정이 도는 동안 ★ 쓰기가 죽는다
   sqlite3.connect('carwatch.db', timeout=3) → INSERT auth_login_attempt
   → ★ 「database is locked」 (3.0초 기다린 뒤)
   ★ 읽기는 WAL 이라 200 이다 — ★ 그래서 ★ 쓰기만 골라 죽었다
   ★ GET / · /listings · /track · /notready · /login 은 전부 200 이었다

② ★ busy_timeout 이 ★ 어디에도 없었다
   grep -rn "busy_timeout" store/ web/ run.py  →  ★ 한 곳도 없다
   ★ SQLite 기본값은 ★ 0 이다 — ★ 부딪히면 ★ 그 자리에서 죽는다

③ ★★ busy_timeout 30초를 걸어도 ★ 여전히 죽었다
   → ★ 30.1초를 기다린 뒤 「database is locked」
   ★ 까닭 — ★ `BATCH_STEPS = {S4, S6, S6a, S8, S9, S10}` 이
     ★ **한 단계를 통째로 한 트랜잭션**으로 묶는다 (`collect/pipeline.py:27`).
     ★ S6 은 130,040행이다 — ★ 잠금을 ★ **몇 분씩** 쥔다
```

★★★ **그리고 여기서 ③(떼어내기)에 대한 정정이 나온다.**

```
★ 웹에서 떼어내도 ★ 이 결함은 ★ 안 낫는다.
  ★ SQLite 는 ★ **프로세스가 아니라 ★ 파일**을 잠근다.
  ★ 다른 프로세스에서 돌려도 ★ 같은 `carwatch.db` 를 ★ 똑같이 잠근다
★ 떼어내기는 ★ 메모리를 나누는 데는 값이 있다 (v272 5장) — ★ 잠금에는 값이 없다
```

### 2-3. 고친 곳

| 파일 | 무엇 |
|---|---|
| `config/web.json` | `db_busy_timeout_ms` **30000** · `db_batch_commit_rows` **200** — 정본은 config (S14) |
| `store/raw.py` `open_db` | `PRAGMA busy_timeout` 을 건다 — ★ 전에는 안 걸었다 |
| `store/raw.py` `commit` | ★ batch 안에서 ★ **정해진 행수마다 한 번 커밋**한다 — 잠금 창을 잘라 낸다 |

★ 행마다 커밋하던 옛 결함으로는 **안 돌아간다** — 그것이 `batch` 를 만든 까닭이다
(커밋은 fsync 라 행 수만큼 느려진다). **200행마다 한 번**이다.

★ 안전 — 원문(RAW)이 이미 있어 중간에 죽어도 재파싱으로 복구된다
(`store/raw.batch` 가 처음부터 그렇게 적어 두었다).
★ **다만 되돌리기(rollback)가 마지막 커밋까지만 간다 — 가이드께 알린다.**

### 2-4. ★ 2,000행으로는 모자랐다 — 200 으로 내렸다 (실측)

```
db_batch_commit_rows = 2000  (재판정 도는 중)
  2초 안에 쓰기 성공 0 · 실패 12
  POST /login  13:17:48 500 · 13:18:23 500 · 13:18:58 500 · 13:19:34 500 · 13:20:09 500

db_batch_commit_rows = 200   (같은 재판정 · 재시작 없이 config 만 바꿨다)
  10초 안에 쓰기 성공 8 · 실패 0 · 기다린 시간 6.65 · 1.43 · 0 · 0 · 3.03 · 0 · 0.63 · 0 초
  POST /login  13:24:04 200 · 13:24:24 200 · 13:24:45 200 · 13:25:05 200
```

★ 2,000행은 **5초에 한 번, 그것도 찰나**라 웹이 비집을 틈이 없었다.

### 2-5. 조치 후

| | 전 | 후 |
|---|--:|--:|
| `POST /login` (재판정 안 돌 때) | **500** | ★ **200** |
| `POST /login` (★ **재판정 도는 중**) | **500** | ★ **200** |
| 시도 기록 | `display_name=''` · 「없는 계정」 | ★ `display_name='admin'` · 「비밀번호 불일치」 |

★ 폼이 서버에 제대로 닿는다는 뜻이다.

### 2-6. ★ 못 한 것 — ★ 마스터 비밀번호가 없다

★ **로그인 성공 → `/watch` 200** 까지는 **못 재 봤다.** 비밀번호를 모른다.
★ 운영 DB 에 시험 계정을 만들지 않았다 (마스터 계정을 건드리지 않는다).
★ **마스터께서 한 번 눌러 보시기를 청한다.** 그래도 안 되면 그 화면 글자를 주십시오.

```
★ 시험은 로그인 길을 덮고 있고 ★ 통과한다 (test_spec_ui · test_web).
  tests/test_spec_ui.py:587  u.login("규격대기", "pending12345")
★ 그러므로 코드 길은 성하다 — ★ 운영에서 죽던 것은 ★ 잠금이었다
```

### 2-7. ★ 관심 카드 넷 — 다 있다 (마스터 지시대로 쟀다)

| 낱말 | `v4m_watch_시안.html` | `web/templates/watch.html` |
|---|--:|--:|
| 바뀐 | 2 | ★ 2 |
| 담은 | 5 | ★ 7 |
| 비교 | 3 | ★ 6 |
| 관심 빼기 | 3 | ★ 1 |

★ 넷 다 화면에 있다. ★ 다만 **로그인해야 보이는 화면**이라 실물은 못 열어 봤다 (2-6장).

---

## 3. ★ ① 목록 카드 — 마스터 지적 둘이 맞고, ★ 둘을 더 찾았다

시안(`v4m_listings_시안.html`)의 카드 한 장은 이렇다.

```
|89| 차량 |80| 값 |59| 보증 |73| 취향|
|S|KOLEOS_HEV|♡|
|1.5 E-TECH 에스프리 알핀 2WD · 세부등급 없음|
|2024-12 · 55,763km · 쥐색 / 검정색 계열|
|점보모터스 · |엔카| · 게시중 · |3곳|
|2,990만|
|시세보다 400만 싸다|
|사고 OK|골격 OK|용도 OK|제조사보증 OK|사이트보증 OK|
|1위 · 742.1 / 910 · 7일째|
```

배포 화면(`/listings`)을 두드려 낱말마다 셌다.

| 시안의 낱말 | 시안 | ★ 배포 화면 | |
|---|--:|--:|---|
| 사고 | 3 | **62** | 있다 |
| 골격 | 2 | **62** | 있다 |
| 용도 | 2 | **31** | 있다 |
| **제조사보증** | 2 | ★ **0** | ★ **없다** — 마스터 지적 맞다 |
| **사이트보증** | 2 | ★ **0** | ★ **없다** — 마스터 지적 맞다 |
| **시세보다** | — | ★ **0** | ★ **없다** — ★ 새로 찾았다 |
| **싸다** | — | ★ **0** | ★ **없다** — ★ 새로 찾았다 |
| **일째** | 2 | ★ **0** | ★ **없다** — ★ 새로 찾았다 |
| `N위` | 2 | **30** | ★ **있다** |
| `/ 910` | — | **30** | 있다 |
| 게시중 | — | **30** | 있다 |

★★ **「순위 「N위」가 없다」는 지금 배포에서는 다릅니다** — 카드 30장에 다 있습니다
(`|S| |1위|` 꼴). ★ 없는 것은 그 줄의 **뒤쪽** `742.1 / 910 · 7일째` 중 **「7일째」**입니다.

★ **고치지는 않았습니다.** 마스터께서 ③ 에서 「/track · /notready · / 는 그대로 둬라」
하셨고, ① 은 **무엇이 빠졌는지 재라**는 것으로 읽었습니다. **고칠지 청합니다.**

---

## 4. ★ 검사 `S46-98` — 만들었다

```
★ 마스터 — 「S46-22 는 절 이름·차례만 본다.  ★ 카드 속을 안 본다」
```

| 파일 | 무엇 |
|---|---|
| `config/web.json` | `sian_screens` (시안 ↔ 주소 다섯) · `sian_word_skip` (안 세는 낱말 + **까닭**) |
| `validate/v0_guide.py` | `s46_98_sian_words_on_screen()` · **fatal** |

재는 법 — 시안의 **보이는 글**에서 한글 낱말을 뽑아, 그 화면을 **실제로 두드려** 있는지 본다.
안 세는 것 — `<style>·<script>·<title>` · `class` 에 `note` 가 든 메모 ·
「지켜야 하는 것」 아래 · `sian_word_skip`.

```
S46-98  ★ FATAL
  /listings 에 없다 5 — 사이트보증 · 시세보다 · 싸다 · 일째 · 제조사보증
  /detail/3 에 없다 6 — 관심에 · 담기 · 상세 · 이전비 · 차값 · 추적에서
```

★ **상세에서도 빠진 것이 나왔다.** 템플릿을 직접 세어 갈랐다 —

| 낱말 | `detail.html` | 배포 화면 | 무엇 |
|---|--:|--:|---|
| **담기** (`관심에 담기 ♥`) | ★ **0** | 0 | ★ **템플릿에 아예 없다** — 로그인 탓이 아니다 |
| **이전비** (`이전비 약 89만`) | ★ **0** | 0 | ★ **템플릿에 아예 없다** |
| 곳에도 (`다른 곳에도 있다`) | 1 | 0 | 중복 매물이 있는 매물에만 나온다 — 표본 탓 (건너뛴다) |

★ `sian_word_skip` 은 **까닭을 적어** 두었다 — 늘어나면 검사가 헐거워지므로 못 박아 두었다.

---

## 5. ★ 볼보 상세 — 「등급이 몇에서 몇으로」 (앞 묶음 지시 ②)

★ 차례대로 현대인증 다음이 볼보다.

### 5-1. 없던 것을 만들었다

```
tools/collect_volvo.py  에 ★ --detail 이 없었다.  ★ 「볼보는 상세가 없다」고 적혀 있었다
docs/VOLVO_SELEKT_API.md 0b  「★★ 상세 — ★ 뚫렸다 (표본 12건 · 개정 580)」
★ 규격이 이미 뚫렸다고 적어 두었다.  ★ 도구가 안 따라간 것이다
```

★ **HTML 자리는 규격에 없었다.** 그래서 **표본을 받아 눈으로 보고 적었다** (지어내지 않았다).

```
GET selekt.volvocars.co.kr/kr/vehicles/volvo/xc60/b5-awd-ultra-bright-ejbbg46
  → 200 · 70,924B
칸은 두 칸짜리 표다
  <td><div class="small"> 모델 년도 </div></td><td><div class="small">2026</div></td>
가격만 표 밖이다
  <div class="… h3">64,000,000원</div>
```

| 파일 | 무엇 |
|---|---|
| `parse/volvo_selekt/mapping.py` (새로) | 라벨/값 표 여덟 칸 + 가격 + 사진 |
| `tools/collect_volvo.py` | `--detail [N]` — 원문을 먼저 남기고(P3) 매핑한다 |

★ **안 뽑은 것** — `state.accident` 사고 · `warranty.*` 보증.
규격 0b·3장이 「★ 딜러가 소개글에 손으로 적은 자유 문장이다 · 12건 중 2건뿐 ·
★ 「볼보 셀렉트는 인증이니 무사고」로 만점을 지어 주지 마라」고 못 박았다. **안 지어냈다.**

★ 표본에서 두 곳이 틀려 고쳤다 (실측으로 잡았다) —
`1,969 cm<sup>3</sup>` → 태그를 떼면 `1,969 cm3` 라 **배기량이 19,693** 이 됐다 (→ `cm` 앞까지만).
사진 주소가 **상대경로**(`/picserver1/…`)라 정규식이 안 맞았다 (→ 고쳐서 22장).

### 5-2. 등급이 몇에서 몇으로 (볼보 140건)

```
python3.11 tools/collect_volvo.py --detail 60
  ★ 저장 138건 · 저장된 볼보 매물 143건
  ★ 상세 — 정상 60 · 못 받음 0
```

| | 상세 받기 전 | 상세 받은 뒤 |
|---|--:|--:|
| **판정 중** | **140** | **140** |
| 그 밖 등급 | 0 | 0 |
| 근거점수 평균 | **18.0** | ★ **155.7** |
| 근거점수 최대 | 18.0 | ★ **369.0** |
| `raw_response` | **0** | ★ **80** |
| ★ **사진 있는 매물** | **0** | ★ **60** |

★★ **등급은 안 바뀌었고 ★ 그것이 맞다.**
근거 369점은 절반선 455 아래다 → **「판정 중」이 참이다** (67장 그대로).

★ 볼보는 **앞으로도 판정 중일 것**이다. 규격이 그렇게 적었다 —

```
docs/VOLVO_SELEKT_API.md  「그 밖 못 채우는 축」
  ✘ 사고 · 성능점검 · 보험 · 압류저당 · 소유자변경 · 용도 — ★ 전부 없다.  ★ NULL 이다
  ✘ 신차가 · 시세 — 없다
```

★ **이것이 67장이 하려던 일이다** — 「모르는 차를 나쁜 차로 내지 않는다」.
볼보는 **F·G 로 떨어지지 않고 판정 중에 머문다.**

### 5-3. ★ 볼보가 DB 잠금에 걸려 한 번 죽었다 (이번 결함의 증인)

```
sqlite3.OperationalError: database is locked
  File "tools/collect_volvo.py", line 155, in main
    save_site_raw(conn, SITE_CODE, "list", None, _u, _b, at)
```

★ 2장의 고침 뒤 다시 돌리니 **한 번에 통과**했다.
★ 로그인만이 아니라 **수집 도구도 같은 잠금에 죽고 있었다.**

---

## 6. check_all · run_tests 실제 출력

```
python3.11 tools/check_all.py
  DB carwatch.db · run 20260826T031724
  통과 393 · fatal 7 · warn 14 · 환경 차이 1 · 미실행 9

  ■ FATAL
    ★ S46-98  시안의 낱말이 화면에 있는가 —
              /listings 에 없다 5 — 사이트보증 · 시세보다 · 싸다 · 일째 · 제조사보증
              /detail/3 에 없다 6 — 관심에 · 담기 · 상세 · 이전비 · 차값 · 추적에서
    V1-23 · V2-01 · V11-12 · V11-159 · V11-45 · V11-76   ★ 여섯은 전부터 있던 것

python3.11 tools/run_tests.py
  결과: 실패 — test_endtoend.py  (1파일 · V11-12 규격 중복 행 탓)
```

| | 앞 바퀴 | 이번 |
|---|--:|--:|
| 통과 | 394 | 393 |
| fatal | 6 | **7** |
| warn | 14 | 14 |

★ **fatal 이 하나 늘었다 — 그것이 이번에 만든 `S46-98` 이다.**
★ 없던 결함이 생긴 것이 아니라 **안 보이던 결함이 보이게 된 것**이다 (오판 141 과 같은 자리).

---

## 7. 지적 — 규격 쪽 (규칙 2)

1. ★★ **`docs/chapters/61-web.md:196`·`:209` 의 `/admin/users` 중복** — **여섯 번째 지적**.
2. ★★ **③ 「파이프라인을 웹에서 떼어내라」에 정정을 청한다** (2-2장).
   ★ 떼어내도 **잠금은 안 낫는다** — SQLite 는 파일을 잠근다.
   ★ 메모리에는 값이 있다 (v272 5장). **두 가지를 갈라 적어 주시기를 청한다.**
3. ★ **`commit` 이 batch 안에서 부분 커밋을 한다** — 되돌리기가 마지막 커밋까지만 간다.
   원문 무손실(P3)은 그대로다. **규격에 적을 자리를 청한다.**
4. ★ **`S46-95`·`S46-96`·`S46-97`·`S46-98` 이 규격에 없다** — 코드에만 있다.
5. ★ **목록 카드·상세 카드에 빠진 것을 고칠지** 청한다 (3장 · 4장).

---

## 8. 못 한 것 · 다음

| # | 무엇 | 왜 |
|:--:|---|---|
| **1** | ★ **로그인 성공 → `/watch` 실측** | ★ 마스터 비밀번호가 없다. **한 번 눌러 봐 주시기를 청한다** |
| **2** | 목록 카드 다섯 · 상세 카드 둘 채우기 | `S46-98` 이 잡아 두었다. **고칠지 청한다** |
| 3 | 볼보 나머지 78건 상세 | 60건만 받았다 (「한 번에 다 하지 마라」) |
| 4 | 리본카 → BMW → 렉서스 | 마스터께서 정하신 차례 |
| 5 | 현대인증 안쪽 문 둘 (183점) | v272 8장 |
| 6 | 파이프라인 떼어내기 | 위 7장-2 정정을 받고 결정 |
| 7 | 미분류 9,089건 | 71장 그대로 |
