# 흰색 후보를 받았습니다 — 탭 3 「조건에 다 맞는 차」 **0 → 4대**

지시   「do it」 — v390 여쭐 것 2 (흰색 후보의 상세부터 받는다)
버전   v391

---

## 0. ★ 먼저 정정합니다 — **96대가 아니라 29대**입니다

v387~v390 에서 제가 「흰색 96대」라고 적었습니다. **틀렸습니다.**
96 은 **리스·렌트를 빼기 전** 수였습니다. 조건대로 세면 **29대**입니다.

```
다섯 사이트 GV70 (리스·렌트 뺀)   1,430
  ＋3,600만 이하 ＋2022↑ ＋8만km 미만 ＋흰색   ★ 29대  (전부 엔카 · 상세 0)
```

---

## 1. ★ 「몇 → 몇」

| 항목 | 전 | 후 |
|---|--:|--:|
| **탭 3 「조건에 다 맞는 차」** | **0대** | **4대** |
| 흰색 후보 상세 `ok` | 0/29 | **5/29** |
| 그 29대 `warranty_body_month` | 0 | **5** |
| 그 29대 `delivery_nationwide` | 0 | **5** |
| 그 29대 `site_inspection` | 0 | **4** |
| 그 29대 `price_origin_won` | 0 | **5** |

배포 실측 —

```
다섯 사이트에서 1430대 — 조건에 맞는 ★ 4대 · 하나가 아쉬운 1426대
3,439만 · 조건 다 맞음 · encar · 엔카진단
  2022-04 · 7.8만km · 흰색 / 갈색 계열 · 2.5T 가솔린 2WD
  판매 (주)빅보스모터스 · 경기 · 전국 배달
  값O 연식O 주행O 제조사 보증O 보험미조회 외장O 배달O 사이트 진단O
```

---

## 2. ★ 받다가 **큰 것을 하나 놓쳤던 것**을 찾았습니다

상세 5건을 받아 두었는데 **칸이 하나도 안 찼습니다.**

```
받은 뒤   detail_status  ok 5
          warranty_body_month  ★ 0/29
          delivery_nationwide  ★ 0/29
          site_inspection      ★ 0/29
```

★ 까닭 — `/fetch/put` 이 ★ **원문만 남기고 파싱을 안 했습니다.**
★ 지시 L-5 는 「원문 그대로 올린다. **파싱은 서버가 한다**」인데
★ ★ 제가 앞 절반만 만들었습니다.

★ **「올렸다」가 「반영됐다」가 아니었습니다** — 이 프로젝트가 막으려는 바로 그 괴리입니다.

### 고쳤습니다

```
report/screens/fetch.py  _parse_into()
  받은 자리에서 바로 parse/{site}/mapping.parse_detail 로 칸에 넣는다
  못 읽으면 0 을 돌려주고 넘어간다 — 원문은 이미 남았다 (나중에 load_raw 가 읽는다)
```

이미 받아 둔 5건도 같은 길로 채웠습니다 — **다시 받지 않았습니다.**

---

## 3. ★ 서버 회선의 실제 속도를 쟀습니다

`/fetch` 와 **같은 길**로(`queue?listing_id=` → `put`) 29대를 돌렸습니다.

```
받음 5 · 잠김 25회 · 1,502초   →  ★ 5분에 1대
```

★ v389 에 제가 「상세는 서버에서도 열린다」고 적었는데
★ **오늘 실측은 다릅니다** — 상세도 서버에서는 거의 잠깁니다 (25회 잠김).
★ 오늘 하루 종일 두드린 탓에 그 IP 가 더 굳은 것으로 보입니다.

★ **마스터 폰이면 29대가 3초입니다.** 서버로는 2시간이 넘습니다.

---

## 4. 두 도구

```
python3.11 tools/run_tests.py
결과: 실패 — test_endtoend.py   (그 밖 열여덟 파일 전부 통과 · ruff 통과)
fatal 넷 — V4-28 · V3-77 · V11-12 · V11-34   ★ 넷 다 이번 작업이 만든 것이 아니다
```

---

## 5. 여쭐 것

### 1  ★ 남은 24대 — **마스터께서 눌러 주십시오**

```
{base_url}/fetch?step=2&n=30   →  「2단계 상세 30건 받기 ▸」
```

★ 폰이면 **3초**입니다. ★ 그러면 「조건에 다 맞는 차」가 **4 → 20대 안팎**이 됩니다.
★ 서버로 두면 두 시간이라 ★ 제가 더 돌리지 않았습니다.

### 2  보험이력이 미조회라 「자료가 얇습니다」로 나옵니다

지금 맞는 4대도 ★ **보험만 미조회**입니다.
★ 그 차들만 상세 화면에서 ★ 「⤓ 이 차의 성능·보험 받기」를 누르시면 됩니다 (L-12).
★ 4대 × 2창구 ÷ 3 × 60초 ≒ **3분**입니다.

### 3  `inspection` · `record` 는 아직 파싱을 안 붙였습니다

`_parse_into` 는 지금 `detail` 만 넣습니다.
그 둘은 `core_inspection` · `core_record` 로 가는데 표가 따로라
`S6` 이 맡는 자리입니다. **원문은 남으니 잃는 것은 없습니다.**
「받자마자 반영」까지 원하시면 다음 회차에 붙이겠습니다.

### 4  CSS 세 자리는 그대로입니다 (v389 여쭐 것 1·2·3)

## 6. 이견

없습니다. ★ 「파싱은 서버가 한다」를 반만 만든 것은 제 잘못입니다.
