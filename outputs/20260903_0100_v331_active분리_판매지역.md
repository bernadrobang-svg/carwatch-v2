# v331 — `active` 와 `recommend` 를 가른다 · 판매지역을 카드에

일시 2026-09-03 01:00 · 명령서 `SPEC-2026.09.02-r1082` 0순위 둘 · 앞 회차 v330

---

## 1. 지시 원문

```
0순위 ★ 「active 와 recommend 를 가른다」 (마스터 정정 09-02)
  마스터 — 「★ 내가?  ★ 왜 제네시스랑 그랜저는 쉬게 해?
           ★ 추천에서 뺀 거지 ★ 수집에서 뺀 거니?」
0순위 ★ 「연식·거리·감점·판매지역을 카드에」 (마스터 확정 09-02)
  마스터 — 「★ 판매지역을 표시했으면 해.  ★ 딜러의 지역을
           ★ 도시 또는 서울인 경우는 구로 ★ 전체적으로」
```

---

## 2. ★★ 내 잘못을 물렸다 — `active` 를 추천 거르개로 썼다

```
★ 09-01 에 마스터께서 ★ 「추천 화면에서 내가 고른 차종만」이라 하셨다
★ ★ 나는 ★ `targets.json` 의 `active` 로 좁혔다 —
★ ★ ★ 그것은 ★ **「받는가」**다.  ★ 수집·판정·매물 화면까지 함께 멈췄다 (오판 237)
★ ★ ★ ★ 09-02 의 ★ `G80_25T/list` 500 도 ★ **이것 때문**이다
```

| 무엇 | 전 | 후 |
|---|--:|--:|
| `active` (받는가) | 10종 | ★ **32종** (가이드가 되살렸다) |
| `recommend` (추천에 내는가) | — | ★ **10종** |

| 파일 | 무엇 |
|---|---|
| `report/screens/build.py` `_active_targets()` | ★ `recommend` 로 거른다 · 키가 없으면 **낸다** |
| 〃 `NOT_TARGETS` | `SPEC_DEFAULT_ON/OFF` 는 차종이 아니다 |

★ 확인 [실측 09-03] — 추천 **10종 · 543건 중** · `/listings` 는 **32종이 다 대상**.

## 3. ★★ 판매지역을 카드에 (`S46-225`·`S46-226`)

★ 가이드가 ★ 지점 이름 표를 만들어 줬다 (`dealer_region.json` — K카 19 · 리본카 10 · 볼보 6).

| 파일 | 무엇 |
|---|---|
| `report/screens/build.py` `region_of()` | ① 사이트가 주소를 주면 ★ **표를 안 본다** ② 없으면 표 ③ 표에 없으면 ★ **이름 그대로** ④ 없으면 `None` |
| 〃 `_region_short()` | ★ 서울이면 **구까지** · 그 밖은 **시까지** |
| `report/screens/views.py` | `ListingRow`·`RecommendRow`·`SoldRow` 에 `region_label` |
| `web/templates/listings.html` · `recommend.html` | 사이트 딱지 옆 · 없으면 「지역 —」 |

★ 잰 것 [실측 09-03]

| 넣은 것 | 나온 것 |
|---|---|
| `('encar','서울 강서구',None)` | **강서구** |
| `('encar','경기 수원시 영통구',None)` | **수원** |
| `('kcar',None,'경기서부직영점')` | **경기 부천** |
| `('reborncar',None,'서울지점')` | **강서구** |
| `('volvo_selekt',None,'김포 전시장')` | **경기 김포** |
| `('kbchachacha','경기',None)` | **경기** (도까지만 온다 — 시를 안 지어낸다) |
| `('kcar',None,'없는지점')` | **없는지점** (이름 그대로 · 가이드께 올린다) |
| `('heydealer',None,None)` | **None** → 화면에 「지역 —」 |

★ 라이브 확인 — `/listings` 31줄 중 ★ **30줄에 지역**이 나온다.

## 4. ★ 내 검사 넷이 「다」로 뭉갰다 (`S46-168`)

```
★ 「11곳이 **다** 거른다」처럼 ★ 예외를 세면서 ★ 그 수를 안 냈다 (오판 210).
★ ★ 넷을 고쳤다 — `S46-208` · `S46-214` · `S46-215` · `S46-229`
★ ★ ★ 이제 ★ 「수집기 **11개**가 active 를 본다 (예외 **0곳**)」처럼 낸다
```

---

## 5. 「몇 → 몇」

| 잣대 | 전 | 후 |
|---|--:|--:|
| `active` (받는가) | 10종 | ★ **32종** |
| `recommend` (추천) | — | **10종** |
| 매물 카드에 지역 | **0** | ★ **30 / 31줄** |
| `check_all` 통과 | 485 | ★ **490** |
| `check_all` fatal | 9 | ★ **5** |
| 브라우저 어긋난 자리 | 0 | **0** |

### 회차 잣대 셋 [실측 09-03 · 재판정 뒤]

| 잣대 | 앞 회차 | 이번 |
|---|--:|--:|
| ① 아홉 사이트 A 이상 | 63 | ★ **72** |
| ② 전기차 A 이상 | 41 | ★ **43** |
| ③ 같은 차 짝 | 180 | **180** |

★ ①②가 움직인 까닭 — ★ `active` 가 32종으로 돌아와 ★ **판정 대상이 늘었다**.
  ★ 내가 껐던 차종들이 ★ 다시 판정에 들어왔다.

### 검사·시험 (실제 출력)

```
$ python3.11 tools/check_all.py
통과 490 · fatal 5 · warn 13 · 환경 차이 1 · 미실행 10

$ python3.11 tools/run_tests.py
결과: 실패 — test_endtoend.py   (나머지 일곱 · ruff 통과)

$ python3.11 tools/browser_diff.py   (300·360·600·900px)
★ 어긋난 자리 0
```

---

## 6. 못 한 것 · 물음

### 남은 fatal 5

| 검사 | 무엇 | 누구 |
|---|---|---|
| `S46-207` · `S46-183` · `S46-32` | 커밋 제목 · 문서 · 생성물 | ★ **가이드** |
| `V1-21` | 받아 두고 안 펼쳐진 원문 4 | 나 |
| `V2-01` | ok 원문 수 ≠ CORE 행 수 | 나 |
| `V3-77` | 갈래 합 ≠ 갈래 표기 (2갈래) | ★ **가이드** |

### ★ 물음

```
10 ★ 엔카 지역에 ★ 「**전남광주**」가 637건 온다 — ★ 엔카가 ★ 전남·광주를 **묶어** 준다.
   ★ ★ 「온 만큼만 낸다」는 규칙대로 ★ 그대로 냈다 — ★ 쪼개지 않았다.
   ★ ★ ★ 갈라야 하면 ★ 규칙을 주십시오 (「전남」과 「광주」는 다른 곳이다)

11 ★ 명령서 0순위의 ★ **①연식·거리 셈 · ②감점 까닭**은 ★ 아직이다.
   ★ ★ 이번 회차는 ★ **③판매지역**만 닫았다.  ★ 다음 회차에 이어서 한다
```

---

## 7. ★ 이어서 — 0순위 ①② (연식·거리 셈 · 감점 까닭)

### ① 연식·거리에 셈을 함께

```
★ 마스터 — 「★ 연식 2022-07 ★ (3년 2개월)」 · 「★ 주행 139,571km ★ (연 4.4만)」
   ★ ★ **한 해에 얼마나 탔나**가 ★ 많이 탔는지를 가른다 — ★ 총 거리만으로는 모른다
```

| 파일 | 무엇 |
|---|---|
| `report/screens/build.py` `_age_label()` | 「3년 4개월」 · 못 재면 **빈 글자** |
| 〃 `_km_per_year()` | 「연 0.6만」 · ★ **한 해가 안 됐으면 안 낸다** (늘려 잡으면 거짓) |
| 〃 `_ym_parts()` | ★ `202207` 도 `2022-07` 도 받는다 |

★★ 처음에 ★ 여섯 자리만 봤다 — ★ 화면이 ★ `2023-05` 꼴로 들고 있어 ★ **하나도 안 나왔다**.
  ★ 라이브에서 재서 찾았다 (「연식 셈 0」) — ★ 고친 뒤 ★ **26/30줄**.

### ② 감점을 까닭까지

```
★ 마스터 — 「★ 지금 「감점 -40」이라고만 나온다.  ★ **무엇 때문인지 안 낸다**.
   ★ 접힌 채로 ★ 가장 큰 것 하나 · ★ 누르면 ★ 다 펴진다」
```

| 파일 | 무엇 |
|---|---|
| `report/screens/build.py` `_pen_top()` | 가장 큰 감점 하나 · ★ 「상한」 줄은 **안 고른다**(흠이 아니다) |
| `web/templates/listings.html` | `<details>` — 접힌 채 요약 · 누르면 다 펴진다 |

★ 라이브 확인 — 「**감점 -43 (골격 판금 -54) ▾**」 · 감점 딱지 11개.

### 「몇 → 몇」 (0순위 ①②③)

| 잣대 | 전 | 후 |
|---|--:|--:|
| 매물 카드 연식 셈 | **0** | ★ **26 / 30줄** |
| 매물 카드 주행 셈 | **0** | ★ **26 / 30줄** |
| 추천 카드 연식·주행 셈 | **0** | ★ **59** |
| 감점 까닭 (겉에) | **0** (툴팁에만) | ★ **11** |
| 매물 카드 지역 | **0** | ★ **30 / 31줄** |

---

## 8. ★ 이어서 — 0순위 「추적: VIN 으로 짝짓고 · 리스 빼고」

### ① 짝의 열쇠를 차대번호(VIN)로

```
★ 마스터 확정 — 「★ 차량번호는 ★ **바뀐다**(번호판 교체·이전).
   ★ 차대번호는 ★ **평생 안 바뀐다**」
★ 필수 — 「★ 둘 다 VIN 이 있으면 ★ VIN 으로 · ★ 한쪽이라도 없으면 ★ 번호로 내린다」
★ 필수 — 「★ VIN 도 ★ **HMAC 로 감춘다**(vin_hash) — ★ 차주를 가리킬 수 있다」
```

| 파일 | 무엇 |
|---|---|
| `sql/ddl/02_core.sql` | `vin_hash TEXT` ＋ 색인 `ix_listing_vin` |
| `store/pii.py` `vin_hash()` | HMAC-SHA256 · 대문자로 맞춘다 · **앞 몇 자만 안 견준다** |
| `store/core.py` `split_pii()` | 넣을 때 감춘다 |
| `report/screens/build.py` `_pair_rows()` | ★ 번호판으로 모으고 ★ **VIN 이 같으면 이어 붙인다** |
| `tools/migrate.py` | ★ DDL 을 **두 판** 돌린다 |

★★★ **첫 판에 틀렸다** — ★ `COALESCE(vin_hash, plate_hash)` 로 한 줄에 뭉갰더니
  ★ ★ 한쪽만 VIN 이 있는 차의 열쇠가 갈려 ★ **짝이 180 → 32 로 무너졌다**.
  ★ ★ ★ 규격을 다시 읽고 ★ **묶음 단위**로 고쳤다 (union) — ★ 176 으로 돌아왔다.

★ 마이그레이션도 걸렸다 — ★ 새 칸에 색인을 걸면 ★ 칸이 아직 없어 ★ 첫 판이 죽는다.
  ★ ★ 칸을 더한 뒤 ★ **다시 돌린다**.  ★ 두 판 다 실패하면 ★ 그때 멈춘다.

### ② 리스·렌트를 짝과 시세에서 뺀다

```
★ 마스터 — 「★ **왜 리스가 비교가 되니?**」
★ ★ 「엔카 3,200만 ↔ K카 리스 승계 1,900만」은 ★ **같은 값이 아니다**
★ ★ ★ 매물 화면에는 ★ **남긴다** — ★ 지우지 않는다
```

| 파일 | 무엇 |
|---|---|
| `config/labels.json` `LEASE_SELL_TYPES` | 낱말의 정본 (`리스`·`렌트`) — 코드에 안 박는다 |
| `report/screens/build.py` `view_track` | 짝에서 뺀다 |
| `collect/runner.py` `_market_medians` | 시세 표본에서 뺀다 |

### ③ 추적 화면을 시안대로

| 시안 | 무엇 |
|---|---|
| `.v4-plate` ＋ `.v4-vin` | ★ 차량번호 ＋ 「VIN ○ / VIN —」 딱지 |
| `.v4-j` | ★ **값을 등급 앞에** — 「엔카 **5,800만** B 638/910」 |

★ 마스터께서 ★ **어디가 싼지를 보러 오신다.  ★ 값이 먼저다.**
★ 번호판은 ★ 감춘 값이라 ★ **앞 여섯 자만** 낸다 (STEP 35 — 원본을 안 낸다).

### 「몇 → 몇」

| 잣대 | 전 | 후 |
|---|--:|--:|
| `vin_hash` 든 매물 | **0** | ★ **7,954** |
| 짝 (리스 포함 · 번호판만) | 180 | ★ **176** (리스 뺀 값) |
| ★ 그중 번호판만으로 | — | 169 |
| ★ VIN 이 이어 준 것 | — | ★ **20** |
| 시세 표본 | 9,606 | **9,590** (리스·렌트 16건을 뺐다) |
| 추적 VIN 딱지 | 없음 | ★ **30개** (VIN○ 5) |
| 추적 사이트 줄 | 등급 먼저 | ★ **값 먼저** |

---

## 9. ★ 이어서 — 마스터 물음 여섯 (09-02)

| 물음 | 무엇을 했나 |
|---|---|
| ① 보험사고가 있는데 무사고? | ★ **낱말을 갈랐다** — 「무사고」 → 「보험 이력 없음」·「보험 N건」 |
| ② 상세에 보험 정보가 없다 | 창구는 이미 받고 있다 (아래) |
| ③ ★ 전기차 누유 만점 | ★ **넣었다** — `leak_ev_full` · 하이브리드는 안 쓴다 |
| ④ ★ 시세 미확인 80% | ★ **넣었다** — `market_unknown_ratio` |
| ⑤ 엔카 골격 | ★ **이미 `inspection.inners` 를 읽는다** (5,441건) |
| ⑥ 압류·저당 / 진정성 | ★ **이미 규격대로다** — 둘 다 없으면 미확인 0점 |

### ① 보험과 성능점검은 다른 것이다 (`S46-218`)

```
★ 마스터 — 「★ **보험 = 보험금이 나갔는가** · ★ **성능점검 = 골격이 상했는가**.
   ★ 보험 건수가 있으면 ★ **「무사고」라 쓰지 마라**」
★ ★ 우리는 ★ `state.accident`(보험 이력) 축의 값을 ★ 「무사고」라 적고 있었다 —
   ★ ★ **낱말이 뜻을 넘어섰다**.  ★ 「보험 이력 없음」·「보험 N건」으로 바꿨다
```

### ③ 전기차 누유 만점

```
★ 마스터 — 「★ 전기차엔 ★ 엔진오일·미션오일·냉각수 누유가 ★ **구조상 없다**」
★ ★ **하이브리드엔 쓰지 않는다** — ★ 콜레오스는 그대로 잰다.
★ ★ ★ 그래서 ★ `not_ev`(하이브리드·HEV·PHEV·플러그인)를 ★ **먼저** 본다
```

| 파일 | 무엇 |
|---|---|
| `analyze/axis/state.py` `_is_ev()` · `_leak()` | 전기차면 만점 · 근거 `leak_ev_none` |
| `config/labels.json` `EV_FUEL_WORDS` | 낱말의 정본 — 코드에 안 박는다 (`S14`) |

★ 실측 09-03 — ★ 전기차(하이브리드 뺀) **2,350건** 중 ★ 누유가 0점이던 것 **1,188건**.

### ④ 시세 미확인 80%

```
★ 마스터 — 「★ 화면에 ★ 「24/30 (표본 부족 · 80%)」 — ★ **만점처럼 보이면 안 된다**」
★ ★ 0점도 ★ 만점도 아니다 — ★ **모르는 자리**를 그만큼만 채운다.
★ ★ ★ **시세 축에만** 쓴다 (`S46-221`)
```

★ 실측 09-03 — ★ 시세가 0점이던 매물 **2,367건** → ★ **24점**씩.
