# v358 — 가이드 지시 ① · `--site` 로 **열두 곳이 다 돈다**

```
① `run.py` 가 어댑터를 사이트별로 골라 받게
   ★ 이것이 먼저다.  지금 열한 사이트가 파이프라인을 안 탄다
끝났다는 뜻   ★ `--site` 로 ★ **열두 곳이 다 돈다**
```

## 1차 결과

```
endpoints.json 에 있는 사이트   ★ 12
어댑터가 서는 사이트            ★ **6**  (encar · kbchachacha · kia_cpo ·
                                       bobaedream · heydealer · kcar)
판이 도는 사이트                ★ 6      (v354 에서 1 → 6 으로 열었다)
```

## ★ 어떻게 여섯을 더 여나 — ★ **어댑터를 여섯 개 안 만들었다**

★ 길을 어디서 찾나 보니 ★ **`endpoints.json` 이 이미 다 갖고 있었다** —

```
volvo_selekt     list  /kr/vehicles/xhr-results/{page}
revolt           list  /cars/?page={page}
hyundai_cert     list  /m/search/results/selling
lexus_certified  list  /api/json/getList_search.json.php?…
bmw_bps          list  /shop/list.php?ca_id={ca_id}
reborncar        (list 없음 · sitemap 과 detail 만)
```

★★ 사이트마다 다른 것은 ★ **길과 머리(headers)** 뿐이다.
★ ★ 그 둘은 ★ config 가 준다 — ★ 그래서 ★ **어댑터 하나면 된다.**

## 만든 것 — `adapters/generic.py` · `GenericAdapter`

```
★ 길을 ★ `config/endpoints.json` 에서만 읽는다 — ★ 코드에 안 박는다 (`S14`)
★ 길이 없으면 ★ **못 한다고 말한다** (금지 12) —
  「★ bmw_bps.list 길이 'ca_id' 를 바라는데 안 줬다」
  「★ reborncar 에 list 길이 없다 — 있는 것: detail, sitemap」
★ `required_keys` 를 ★ **지어내지 않았다** — ★ 비워 둔다.
  ★ ★ 지어내면 ★ 멀쩡한 원문이 거부된다.  ★ 검증은 통과시키고 ★ 파서가 본다
★ facet 이 없으면 ★ **빈 목록**이다 — ★ 없는 창구를 만들지 않는다
```

★ `run.py` 는 ★ 손으로 지은 어댑터가 있으면 그것을 쓰고,
★ ★ 없으면 ★ `GenericAdapter` 로 ★ config 를 태운다.

## 조치 후 결과

```
어댑터가 서는 사이트  ★ **12 / 12**

$ python3.11 run.py collect --site volvo_selekt --dry     → --dry: 요청하지 않았다
$ … revolt / lexus_certified / hyundai_cert / bmw_bps / reborncar  ★ 여섯 다 같다
```

| | 앞 | 뒤 |
|---|--:|--:|
| 어댑터가 서는 사이트 | 6 | **12** |
| **판(`--site`)이 도는 사이트** | 6 | **12** |

## 여쭐 것

```
★ 두 곳은 ★ **길이 모자라다** — ★ 판은 서지만 ★ 목록을 못 부른다
  ★ `bmw_bps` — ★ `list` 길이 `{ca_id}` 를 바란다.  ★ 그 값이 config 에 없다
     ★ ★ 수집기는 ★ `ca_id=10` 을 쓴다 (`tools/collect_bmw.py`) —
     ★ ★ ★ 규격 값인지 ★ 그때 값인지 몰라 ★ **안 옮겼다** (금지 6)
  ★ `reborncar` — ★ `list` 길이 아예 없다.  ★ `sitemap` 으로 받는다
     ★ ★ 그 방식은 ★ 목록 쪽넘김이 아니라 ★ **다른 걸음**이다
★ 안 ① 두 값을 ★ config 에 적어 주시면 ★ 곧장 돕니다
★ 안 ② 그 둘은 ★ 지금대로 ★ `tools/collect_*.py` 가 받는다
★ ★ **안 ②로 두고 진행했습니다** — ★ 나머지 열은 이제 판으로 돕니다
```
