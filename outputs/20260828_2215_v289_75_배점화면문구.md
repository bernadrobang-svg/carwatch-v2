# v289 — #75 배점 화면이 자기 말을 안 지킨다

```
지시   명령서 92장 ① — 「#75 배점 911 저장 · 910 아니면 저장을 막고 · 이미 든 911 을 되돌려라」
정본   outputs/20260828_2040_시험자_1_2_3회차_결과.md  #75 · #76
규칙   ⑩ 하나씩 닫는다 · ⑪ 「이미 안다」로 안 넘긴다 · ⑫ 시험자에게 안 묻는다 · ⑬ 그 자리를 다시 돌린다
```

## 1. 시험자가 본 것

```
#75  /admin/scoring 선루프 12→13 저장
     화면 안내 「합계가 total_points 와 맞지 않으면 저장할 수 없습니다」
     → 303 저장됨.  성분 합 911.  ★ 막지 않았다
#76  /why/202 는 698.4 / 910 그대로 · 선루프도 12 그대로
     → 두 화면이 갈렸다
```

## 2. 재현했다 (고치기 전)

```
지금 파일 — total_points 910 · 성분 합 910
선루프 12 → 13 조작 뒤 성분 합 = 911

  지금 코드 (also 로 total_points 를 다시 더한다)   ★ 통과 (저장된다)
  also 없이 (화면 문구가 약속한 대로)               막힘 — 배점 합 911 != total_points 910
```

★ 원인 — `web/views.py:2007`

```python
change = apply_config(conn, account, "scoring.json", "components",
                      new, ..., also={"total_points": total_of(new)})
```

★ `total_points` 를 **성분 합으로 다시 채워 넣는다.**  그래서 `_validate_blob` 의
`Σ == total_points` 는 **늘 참**이다.  ★ 화면의 `gap` 도 늘 0 이라
「차이 N — 저장할 수 없습니다」 경고가 **뜬 적이 없다.**

## 3. ★★★ 그런데 코드가 규격대로다 — 규격을 먼저 봤다

`docs/chapters/60-admin/a-auth.md:253` **STEP 128**

```
출력    갱신된 components + total_points
금지    total_points 를 직접 입력하는 것.  성분 합으로만 정해진다
검산    Σ components == total_points

조정 3종 — 성분 점수 변경 | 그 성분만 바꾸고 축 총점·총점을 ★ 다시 더한다
```

```
★ 규격은 ★ total_points 가 ★ 성분 합을 따라간다고 못박는다.
★ 그러므로 ★ 911 은 ★ 「잘못 저장된 값」이 아니라 ★ 규격대로 ★ 새 총점이다.
★ ★ 틀린 것은 ★ 코드가 아니라 ★ **화면 문구**였다 —
  ★ 같은 문단 안에 ★ 서로 어긋나는 두 문장이 나란히 있었다:

  4행  합계가 total_points 와 맞지 않으면 저장할 수 없습니다     ← 거짓
  6행  ★ total_points 는 직접 입력하지 않습니다 — 성분 합으로만 정해집니다  ← 참
```

★ 규칙 1 대로 **코드를 규격에 맞춘다**.  ★ 여기서는 코드가 이미 맞았으므로 **문구를 고쳤다.**

## 4. 「911 을 되돌려라」 — ★ 이미 되돌아가 있다

★ 지어내지 않고 이력을 읽었다 (`config_change`).

```
2026-08-28T10:39:17  시험자 2회차 검사 — 합계 검증
   taste.sunroof 12 → 13   성분합 910 → 911
2026-08-28T10:39:56  시험자 2회차 — 원상복구
   taste.sunroof 13 → 12   성분합 911 → 910
```

```
지금 config/scoring.json — total_points 910 · 성분 합 910 · taste.sunroof 12
★ ★ 시험자가 ★ 스스로 되돌려 놓았다.  ★ 내가 손댈 것이 없었다
★ ★ 그래서 ★ scoring.json 을 ★ 건드리지 않았다 (명령서 「손대지 마라」와도 맞는다)
```

## 5. 무엇을 했나

`web/templates/admin_scoring.html` — 안내 문단을 **사실로** 바꿨다.

```
지웠다  「합계가 total_points 와 맞지 않으면 저장할 수 없습니다」   ← 일어날 수 없는 일
넣었다  「total_points 는 성분 합으로만 정해집니다.  그래서 여기서 성분을 바꾸면
         총점이 그 합으로 다시 더해집니다 — 총점 자체가 달라집니다」
넣었다  「바꾼 값은 재판정하기 전까지 판정 화면(상세·판정 근거)에 안 나옵니다.
         두 화면의 총점이 다르면 아직 재판정을 안 한 것입니다」   ← ★ #76 을 함께 닫는다
고쳤다  gap 경고 문구 — 「저장할 수 없습니다」 → 「파일이 어긋나 있습니다.
         config 를 손으로 고친 자국입니다」   ← 손편집일 때만 뜬다.  뜻이 맞다
```

## 6. ⑬ 그 자리를 다시 돌렸다

```
화면 렌더 (관리자 · 실제 HTTP 경로)
  상태 200 · 39,021자
  '맞지 않으면 저장할 수 없습니다'  → ★ 없다
  지금 문구 — 「… 성분 합으로만 정해집니다. 그래서 여기서 성분을 바꾸면
               총점이 그 합으로 다시 더해집니다 — 총점 자체가 달라집니다. …
               바꾼 값은 재판정하기 전까지 판정 화면에 안 나옵니다 …」
  합계 910 / 910

시험 (그 자리)
  test_admin_flow  통과
  test_admin       통과
  test_web         통과
  test_spec_ui     통과
```

## 7. 1차 → 조치 후

```
check_all  통과 397 · fatal 10 · warn 12   (앞뒤 같다)
run_tests  실패 1파일 test_endtoend · ruff 통과   (앞뒤 같다)
```

★★ **`fatal 10` 은 내가 늘린 것이 아니다.**  ★ 앞서 9 로 적었던 것은
   ★ `outputs/render/` 가 **낡아 있어** `V11-106` 이 못 잡던 것이다.
   ★ 렌더를 새로 뜨니 2건이 드러났다.  ★ **내 수정을 빼고 렌더해도 똑같이 2건**이다
   (확인함).  ★ ⑧ 에 함께 올린다.

```
V11-106  값 자리에 「—」가 없음 — 2
  outputs/render/admin_requests.html  <td data-label="반영 STEP">—</td>
  outputs/render/admin_requests.html  <td data-label="사유">—</td>
```

## 8. ★ 마스터께 여쭐 것 — 규격과 어긋납니다

```
지시   「★ 910 아니면 저장을 막고」
규격   STEP 128 「금지 — total_points 를 직접 입력하는 것.  성분 합으로만 정해진다」
       「성분 점수 변경 → 그 성분만 바꾸고 축 총점·총점을 다시 더한다」
```

```
★ 910 을 고정값으로 두고 막으면 ★ 배점을 늘리거나 줄이는 일 자체가 불가능해집니다
  (성분을 하나 더하거나 스킵하면 합이 반드시 달라집니다 — 그것이 STEP 128 의 조정 3종입니다).
★ 그래서 이번에는 ★ 규칙 1 대로 규격을 따르고 ★ 문구를 사실로 고쳤습니다.
★ ★ 총점을 910 에 못박는 것이 마스터 뜻이시라면 ★ STEP 128 이 먼저 바뀌어야 합니다 —
  ★ 규칙 2 대로 제가 docs 를 고치지 않고 여쭙니다.
★ 정하시면 그대로 하겠습니다
```

## 9. 진척

```
「틀리다」 47건 →  ★ 46건   (#75 닫음 · #76 은 같은 문단으로 함께 닫힘)
```
