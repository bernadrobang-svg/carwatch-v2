# v339 — 1부 1-1 · 짝지어진 차를 찾을 수 있게

명령서 `outputs/ORDER_20260829.md` (r1101) 1부 1-1 · 규격 `CROSS_SITE_COMPARE` 3b-2

## 무엇을 하려 했나

```
★★★ 금지 — ★ 짝이 있다고 위로 올리지 마라
   ★ 순위는 마스터 기준(348점)이 정한다.  ★ 올리면 나쁜 차가 먼저 온다
★★ 대신 셋 — ① 목록 머리에 수  ② 배지는 카드에 그대로  ③ 거르개에 「짝지어진 것만」
```

## 1차 결과 (고치기 전)

```
/listings          머리 (없음) · 배지 ★ 0/30
★ 화면 글은 「같은 차가 여러 곳에 있으면 … (「3곳」 배지를 누르면 추적으로)」
★ ★ 그런데 ★ **1쪽에 배지가 하나도 없다** → ★ 그 글이 ★ **거짓말**이었다
```

## 무엇을 · 왜 했나

| ① | 목록 머리에 **지금 조건 안의 수** | `paired_count()` 신설 (`report/screens/build.py`) |
| ③ | 거르개 `paired` | `ListingFilter.paired` ＋ `_listings_where` 의 EXISTS 두 갈래 |
| ② | 배지는 **손대지 않았다** | 값순·뒷쪽에서는 보인다.  없애지 않는다 |

★ **순위에 짝 여부를 안 섞었다** (규격 금지). ★ 거르기만 한다.
★ 「같은 차」의 자는 ★ **카드 배지와 같은 것**을 쓴다 — 번호판 **또는** 차대로
다른 사이트에 함께 올라온 것. ★ 다르면 「N대」와 걸러진 수가 어긋난다.

## 조치 후 결과 — ★ 배포를 브라우저로 열어 쟀다

```
  /listings                    머리   161대 · 배지  0/30
  /listings?paired=1           머리   161대 · 배지 ★ 30/30
  /listings?site=encar         머리 ★ 135대 · 배지  0/30   ← ★ 조건 따라 바뀐다
```

| | 앞 | 뒤 |
|---|--:|--:|
| 머리에 짝 수 | 없음 | **161대** (엔카만 걸면 135대) |
| 「짝지어진 것만」 거르개 | 없음 | **있다** — 30/30 배지 |
| 순위 차례 | 348점 | **그대로** (안 건드렸다) |

## 여쭐 것

★ 없다. ★ 규격 3b-2 가 셋을 다 적어 두어 그대로 했다.
