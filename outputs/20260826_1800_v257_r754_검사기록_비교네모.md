# 검사 결과를 `audit_validation` 에 남긴다 · 매물 카드 비교 네모

```
버전     소스 v257 · 지시서 SPEC-2026.08.26-r754
때       2026-08-26 18:00
지시     ★★ 검사 결과를 audit_validation 에 남겨라 — 규격이 이미 그렇게 정해 놨다
           11-store/a-key.md:42 · :219 · c-result.md:149 · ★ 안 남긴 것이 결함이다
         ★ 매물 카드에도 비교 네모를 둬라 (v4m_listings_시안)
         ★ 관리 「탐색」 절은 안 만든다 — 확정 17
         ★ KB 목록 쪽을 안 남긴 것 — 그대로 둬라
앞 작업  07d0ec7
```

---

## 1. ★★★ 남기기 전에 — **그 검사들은 아예 안 돌고 있었다**

가이드 지시를 받고 「어디서 남길까」를 찾다가 알았다.

```
grep -rn "v0_guide" tools/ tests/
  tools/build_index.py  (내가 어제 넣은 두 줄뿐)
★ tools/check_all.py 도 · tools/run_tests.py 도 ★ 안 부른다
★ v0_guide.py 끝 주석 — 「★ 돌리려면 python3.11 -c "from validate.v0_guide import run; …"」
```

★★ **S43~S46 서른셋을 ★ 손으로 쳐야만 돌았다.**
★ 규칙 3 의 두 도구 안에 없었으니 **「고친 뒤 두 도구를 돌린다」로는 한 번도 안 걸렸다.**
★ 색인의 「마지막 통과 ★ 없음」은 **기록을 안 한 것이 아니라 안 돈 것**이었다.

★ 그래서 **셋을 함께 했다** — ① 돌린다 ② 규격의 자료 구조로 만든다 ③ 남긴다.

---

## 2. 무엇을 했나 (고친 파일 · 줄)

### ① `validate/v0_guide.py` — 결과를 규격 구조로 낸다 (1305~1380행)

```
PHASE = "guide"                 ★ V* 가 아니다.  지시서를 지키는가를 보는 검사다
                                ★ report/render.py 의 V 리포트가 phase LIKE 'V%' 로 거른다
_as_check(code, title, kind)    CHECKS 튜플 표 한 줄 → validate.base.Check
                                ★ 등급은 성격이 정한다 — fatal → code · warn → external
results(run_id)                 전부 돌려 CheckResult 목록으로
                                ★ 검사가 죽어도 나머지를 다 돌린다.  죽은 것도 실패로 남긴다
save(conn, run_id, at)          save_results() 로 남긴다
run(run_id)                     화면 출력은 그대로 — results() 를 쓴다
```

★ 표를 튜플로 적어 온 것은 **읽기 쉬워서**다.  ★ 남길 때만 규격 구조로 바꾼다.

### ② `tools/check_all.py` — 함께 돌리고 **전 차수를 남긴다** (22·46·70~78행)

```
phases = (PHASE_GUIDE, *PHASE_ORDER)
    results = guide_results(run_id) if phase == 'guide' else run_phase(...)
    save_results(conn, results, _now())      ★ 전 차수를 남긴다
```

★ 전에는 **V* 차수도 `check_all` 에서는 안 남기고 있었다** (`run.py` 만 남겼다).
★ 이제 `check_all` 을 돌리면 **그 회차가 표에 남는다** — 규격 `c-result.md:149`
「★ 화면 출력만 하면 어제와 비교할 수 없다」 그대로다.

남는 것 — `phase='guide'` · `code` · `passed` · `actual`(한 줄) · **`run_id`** ·
`severity` · `applicable` · `checked_at`.  PK 는 `(run_id, phase, code, target_key)` 다.

### ③ 매물 카드 비교 네모 (`listings.html` · `app.css`)

```
web/templates/listings.html
  카드 무리 전체를 <form method="get" action="/compare"> 로 감쌌다
  사진 왼쪽 위 .kpick — ★ 관심과 같은 꼴이다
  아래 .kcmp 띠 — 「N대 골랐다 · 비교하기 ▸」 · ★ 다섯을 넘으면 그렇게 적는다
web/static/app.css
  .car-rows .kthumbwrap .thumb  72×54 → ★ 104×78 (v4m 시안 크기)
     ★ 72×54 에 44px 네모를 얹으면 사진이 거의 안 보인다.
     ★ 짚는 자리는 44px 여야 한다 (마스터 확정)
  .car-rows .kthumbwrap .nsite  「N곳」 배지를 사진 오른쪽 위로
```

★★ 함께 고친 것 — `.desk` 는 `display:contents` 라 **안의 것이 그대로 그리드 칸**이 된다.
「N곳」 배지가 있고 없고에 따라 **칸 수가 달라져 줄이 밀리고 있었다.**
상자(`.kthumbwrap`) 하나로 묶어 **늘 한 칸**이 되게 했다.

### ④ 관리 「탐색」 절을 없앴다 (`admin.html` 69~92행)

```
전   운영 / [h4 탐색] / 조정 / 계정 / 내려온 것 둘
후   운영 (★ 탐색 문 다섯을 ★ 같은 표에 이어서) / 조정 / 계정 / 내려온 것 둘
```

★★ **문은 하나도 안 지웠다** — 조회 쿼리 · API 조회 · 관리 도구 · 문서 뷰어 · 개발 요청.
★ 절만 없앴다 (개정 427 「화면을 지우지 않는다」).

---

## 3. 1차 결과 (고치기 전 숫자)

```
audit_validation 의 phase='guide'          ★ 0건
check_all 이 부르는 차수                    V1~V11 아홉 (★ guide 없음)
check_all 이 남기는 것                      ★ 없음 (화면 출력만)
docs/CHECKS.md ② 죽은 검사                  68
매물 카드의 비교 네모                        ★ 없음
관리 절                                    다섯 (탐색 포함)
```

---

## 4. 조치 후 결과

```
sqlite> SELECT phase, COUNT(*) FROM audit_validation GROUP BY phase;
  V11 1826 · V1 1137 · V3 934 · V2 586 · V10 545 · V4 453 ·
  V5 228 · V7 223 · V9 42 · V8 38 · ★ guide 33 · V6 19

sqlite> SELECT code, passed, actual FROM audit_validation WHERE phase='guide' LIMIT 4;
  S43-2   1  축 id 가 config 와 같다
  S43-2b  1  config 가 규격 이름을 쓴다
  S43-2c  1  HDA 가 저장소에 없다 (기록 제외)
  S43-3   0  ★ 버전 r749 · 이력 마지막 754
```

```
docs/CHECKS.md ② 죽은 검사   68 → ★ 36
★ 가이드가 말씀하신 참값 그대로다
```

```
python3.11 tools/check_all.py
DB carwatch.db · run 20260825T000022
통과 381 · fatal 6 · warn 12 · 환경 차이 2 · 미실행 8

■ FATAL — 고쳐야 한다
  S43-3     버전이 이력 마지막과 같은가 — 버전 r749 · 이력 마지막 754
  S46-36    폐기된 요구가 규격에 안 살아 있는가 — ★ 폐기된 요구가 규격에 살아 있다 1곳 — 요구 121 → KBCHACHACHA_API.md:142
  V1-08     동일 코드 실패율 100% 인 엔드포인트 없음 — 2건 (예: detail: 328건 전량 실패, inspection: 182건 전량 실패)
  V1-21     받아 두고 안 펼쳐진 원문이 없음 — 182
  V1-23     필요한 조합 대비 받은 카탈로그 비율 — 127조합 · 78.9% · 카탈로그 없는 매물 633건 · 사이트가 안 줌 empty 21조합 · 매물 586건 (예: 8245694202510
  V2-01     ok 원문 수 == CORE 행 수 — 5744

■ warn — 진행은 된다
  S46-24    facet 미확인 차종이 없는가 — ★ facet 미확인 47종 — G80_25T.bobaedream · G80_25T.kbchachacha · G80_EV.bobaedream · G80
  V1-07     매물별 엔드포인트 4종 상태 존재 — 4242
  V4-06     RAW 경로가 등록부에 있는가 — 3
  V4-06b    등록부에 있는데 RAW 에 없는 유령 경로 — 48
  V4-11b    판정에 안 쓰는 미분류 경로 — 319
  V4-21     같은 이름의 공개 함수가 두 모듈에 없음 — 7
  V3-39     이론가와 실제 중앙값의 차가 상한 안 — 최대 33.2%
  V3-47     축별 차종 간 결측률 편차가 상한 안 — 3축
  V3-03     축별 source 값 종류 >= 2 — 1건 (예: taste.fitting)
  V3-04     축별 값 종류 >= 2 — 2건 (예: state.consumable, taste.fitting)
  V3-91     가이드 검산 일곱 줄이 표대로 나옴 — 7줄 다름
  V3-96     value IS NULL 과 source 모름 건수 차 — source 모름 33989 · 차 +32,797

■ 환경 차이 — 결함이 아니다
  V1-05     실행 시작 시각 기준이라 DB 만으로는 재현되지 않는다
  V4-01     HMAC 키가 다르면 plate_hash 가 전건 불일치한다

■ 미실행 — 이번 실행에서 그 단계를 안 돌았다
  V1-20     이번 실행에 카탈로그를 안 받았다
  V4-29     판정을 막는 미분류가 없다 — 잴 것이 없다
  V4-30     판정을 막는 것이 없다
  V3-68     부록 F 에 축 목록이 없다
  V3-64     규격에서 경계를 못 읽었다
  V9-09     비율이 같은 매물 짝이 없다 — 잴 것이 없다
  V10-24    사전 확정 이력이 없다
  V11-121   두 사이트에 같이 올라온 차가 없다 — 나란히 낼 것이 없다

★ 통과 351 → ★ **381** — ★ 서른셋이 새로 들어왔다 (S43~S46).
★ fatal 4 → 6 — ★ 는 것은 ★ **S43-3 · S46-36 둘**이고 ★ 둘 다 ★ 규격 쪽이다 (5장).
  ★ 내가 낸 것이 아니다.  ★ 여태 ★ 안 돌아서 ★ 안 보이던 것이다
★ warn 11 → 12 — ★ S46-24(facet 미확인 47종)가 들어왔다
★ ★ S46-32(생성물이 낡았다)는 ★ build_index 를 돌리면 풀린다 —
  ★ 차례가 있다: ★ **고친다 → build_index → check_all → 커밋**
```

```
python3.11 tools/run_tests.py
  test_admin         통과
  test_admin_flow    통과
  test_collect       통과
  test_crosssite     통과
  test_dict          통과
  test_endtoend      통과
  test_fixtures      통과
  test_integration   통과
  test_invariants    통과
  test_pipeline      통과
  test_registry      통과
  test_report        통과
  test_run           통과
  test_score         통과
  test_screens       통과
  test_spec_ui       통과
  test_store         통과
  test_watch         통과
  test_web           통과
  ruff               통과  (F821,F811,B905,F841,DTZ005,F401)

  임시 DB 62개 치움 (검사가 디스크를 채우지 않는다)

결과: 통과
```

---

## 5. ★★ 여쭐 것 — 규격 셋이 어긋난다 (규칙 2 · 내가 안 고쳤다)

### ① `docs/guide/00_버전.md` — 한 파일 안에서 판번호가 둘이다

```
4행   version  SPEC-2026.08.26-r754
18행  ## 지금 버전 →  SPEC-2026.08.26-r749
```

★ `S43-3` 이 18행을 읽어 「버전 r749 · 이력 마지막 754」로 **fatal** 이다.
★ 가이드께서 「S43-3 은 내가 고쳤다」 하셨는데 **머리(4행)만 올라갔다.**

### ② `docs/KBCHACHACHA_API.md:142` — `S46-36` 이 아직 잡는다

```
★★★ ★ KB 한 곳에서 ★ 수입 대상이 ★ 고유 1,727건
     (★ **전량 15,711건 중** · 마스터 확정은 ★ 전량이다)
```

★ 가이드께서 「넷 다 「전량이다」를 함께 적어 풀었다」 하셨다.
★ 그런데 `S46-36` 의 「죽은 줄」 표시는 다음 여덟뿐이다.

```
DEAD_MARK = ("틀렸다", "폐기", "취소", "물렸다", "아니다", "안 준다", "✘", "정정")
```

★ 「전량이다」·「마스터 확정은」이 **그 목록에 없다.**

```
여쭙는다  어느 말을 ★ 「이 줄은 정정됐다」로 볼까
         ⓐ 「마스터 확정」을 표시로 더할까
         ⓑ 그 줄에 「정정」을 함께 적어 주실까
★ 내가 목록을 넓히지 않았다 — ★ 넓히면 ★ 살아 있는 지시를 죽은 것으로 볼 수 있다
```

### ③ `docs/chapters/61-web.md` STEP 149j ↔ `v4m_admin_시안`

```
STEP 149j   「★ 운영 · 조정 · ★ **탐색** 셋 …
             ★ 금지 — 화면 수가 늘 때 ★ **4번째 분류를 만드는 것**」
v4m 시안 ＋ 가이드 08-26
            「★ 운영 · 조정 · 계정 · 내려온 것 둘」(★ 넷) ·
            「★ 관리 「탐색」 절은 ★ **안 만든다**」
```

★★ 둘 다 지킬 수 없다.  ★ **시안이 화면의 정본**이므로 (`ref/screens/README.md`)
시안을 따랐다.  ★ STEP 149j 의 **목적**은 지켜진다 — 「잠금 단위와 일치한다」.

```
지킨 것   라우팅 표의 3분류(운영·조정·탐색)는 그대로다 — V11-24 가 그것을 본다
         조정만 잠긴다 (b-ops 302행) — 그대로다
         ★ 탐색의 문 다섯이 살아 있는지 시험을 새로 넣었다 (test_spec_ui F-1b)
고쳤다    test_spec_ui F-1 — 3분류 → ★ 시안의 넷을 본다
여쭙는다  STEP 149j 를 시안에 맞춰 주실지
```

---

## 6. 못 한 것

### ★ KB 수집 — **도는 중인데 느리다** (실측)

```
다시 건 지 1시간 14분 · CPU 9초 · 열린 소켓 0 · State S (hrtimer_nanosleep)
★ 아직 ★ 목록 단계다 — ★ 상세를 한 건도 안 받았다
까닭   --narrow 는 ★ 회차마다 ★ 17묶음의 목록을 ★ 처음부터 다시 훑는다
       ★ 묶음마다 최대 400쪽 · 쪽 사이 1.2초 → ★ 목록만 몇 시간이다
       ★ 앞 회차도 그랬다 (1시간 45분 만에 상세로 갔다)
★ 지금은 그대로 둔다 — ★ 받은 매물번호는 DB 에 남아 건너뛴다
★ 다음 바퀴에 여쭙는다 — ★ 목록을 하루 한 번만 훑고 ★ 상세만 회차로 돌릴까
```

```
① KB 전량 (도는 중) · ② 3-2 걸러 저장 다시 받기 · ③ 3-3 되돌리기
④ 멀티 매핑 (축 × 사이트 표) · ⑤ 견주기
⑥ v4m 여덟 장으로 화면 — ★ 절 차례는 다 맞췄다.  ★ 목록·관심·추적은 카드다.
   ★ 상세·현황·관리·미판정·비교는 ★ 아직 표·목록 꼴이 남아 있다
```
