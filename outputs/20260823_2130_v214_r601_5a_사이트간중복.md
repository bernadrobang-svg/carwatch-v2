# r601 5a단계 — 사이트 간 중복

`SPEC-2026.08.23-r601` · 규격 `docs/DEDUP_CROSS_SITE.md`
2026-08-23 21:30 · v214

---

## 1. 무엇을 하려 했나 (지시 원문)

```
5a ★ 사이트 간 중복 — docs/DEDUP_CROSS_SITE.md 를 5단계 전에 읽어라.
   · 그릇은 이미 있다. vehicle_duplicate 를 써라. 새 표를 만들지 마라.
   · 열쇠는 plate_hash 다. 볼보만 차량번호를 안 준다 — 볼보는 짝짓지 마라.
   · 대표는 하나. 축은 합친다. 값을 버리지 마라.
   · source_listing_id 를 반드시 남겨라. 안 남기면 /why 가 거짓말이 된다.
   · kind 에 concurrent_cross_site 를 더해라 (지금 CHECK 에 없다).
```

---

## 2. 무엇을 했나

| 파일 · 줄 | 무엇 |
|---|---|
| `sql/ddl/07_watch.sql:121–125` | `CHECK` 에 ★ `'concurrent_cross_site'` 추가 |
| `store/watch.py:25–27` | `DUP_CROSS_SITE` 상수 |
| `store/watch.py:128–145` | ★ `_cross_site_order()` — 대표 고르는 차례 (규격 2-1) |
| `store/watch.py:168–186` | `classify_duplicates()` 가 ★ 사이트를 넘는 무리를 가른다 |
| `store/watch.py:196` | `sync_duplicates` 의 `stat` 에 새 갈래 (없으면 `KeyError`) |

★ **새 표를 만들지 않았다.** `vehicle_duplicate` 를 그대로 썼다.

### ★ 이미 있던 것 — 지어내지 않았다

```
★ 열쇠 `plate_hash` 는 ★ 이미 1순위다 —
  `store/core.py:514 build_identities()` 가 ★ plate → vin → site_id 차례로 만든다
  → ★ `vehicle_id` 가 ★ 이미 사이트를 넘어 묶인다.  ★ 새로 만들 것이 없었다
★ 모자란 것은 ★ 갈래 이름 하나였다 — ★ 규격 2-3 이 짚은 그것이다
```

---

## 3. 1차 결과

```
vehicle_duplicate               0행
CHECK (kind IN ('concurrent_same_dealer','concurrent_cross_dealer','relist'))
  → ★ 사이트 사이가 없다

plate_hash 있는 매물
  encar          8,751건 중 ★ 3,998건
  hyundai_cert   1,114건 중 ★ 1,114건 (전건)
  kia_cpo        1,020건 중 ★ 0건        ← ★ 아래 6장 ⓐ

같은 plate_hash 무리 — ★ 1,336무리
  그 중 ★ 사이트를 넘는 무리 — ★ 0무리   (KB 가 아직 안 들어왔다)
```

## 4. 조치 후 결과

### 4-1. DDL 이행 — ★ 데이터 보존

```
python3.11 tools/migrate.py
  제약 이행 9건 — core_inspection · core_listing · dict_enum · meta_field_usage ·
                 query_log · raw_facet · raw_response · result_score · vehicle_duplicate
  「데이터는 보존됐다.  원문은 무손실이다 (P3)」
이행 전 core_listing 10,885행 → ★ 이행 뒤 10,885행 (같다)
이행 전 vehicle_duplicate 0행 → 0행
```

### 4-2. ★ 일부러 깨서 확인했다 (원칙 6)

```
concurrent_cross_site      → ★ 들어간다
relist                     → 들어간다
concurrent_same_dealer     → 들어간다
★없는kind                   → ★ 막힌다 (IntegrityError)
```

### 4-3. 갈래·대표 — ★ 네 가지로 재 봤다

| 시험 | 넣은 것 | ★ 나온 것 | 규격 |
|---|---|---|:--:|
| ① 사이트 둘 · 값 다름 | encar 3,000 · KB 2,800 | ★ KB(2,800)가 대표 · `cross_site` · 대표 **하나** | 2-1 ③ ○ |
| ② 싼 쪽이 ★ 리스 | encar 3,000 · KB 2,000(리스) | ★ **encar 가 대표** — 리스는 대표가 못 된다 | 2-1 ① ○ |
| ③ 한 사이트 안 · 딜러 둘 | encar 둘 | ★ `cross_dealer` · 둘 다 대표 (옛 갈래 그대로) | 안 깨짐 ○ |
| ④ 값이 같을 때 5회 | 둘 다 3,000 | ★ 대표가 `[41,41,41,41,41]` — **흔들리지 않는다** | 2-1 ⑤ ○ |

---

## 5. 검사

```
run_tests   ★ 20/20 전부 통과 (ruff 포함)
  test_admin·admin_flow·collect·crosssite·dict·endtoend·fixtures·integration·
  invariants·pipeline·registry·report·run·score·screens·spec_ui·store·watch·web
  ruff 통과 (F821,F811,B905,F841,DTZ005,F401)
  결과: 통과
```

---

## 6. ★ 못 한 것 · 여쭐 것

### ⓐ ★★ 기아 CPO 가 `plate_hash` 가 0건이다 — ★ 파서는 내는데 DB 에 없다

```
★ 실측 — 목록 원문 1,015건 ★ 전건에 `plateNumber` 가 있다
★ 실측 — `parse_list_item()` 이 ★ 전건 `_pii_plate_no` 를 낸다
★ 그런데 ★ DB 의 kia_cpo 1,020건은 ★ plate_hash 가 ★ 0건이다
★ ★ 지금 DB 의 kia 행은 ★ 그 필드가 붙기 전에 받은 것으로 보인다 —
  ★ 다시 받으면 채워질 것이나 ★ 이번에 ★ 다시 받지 않았다 (5단계가 먼저다)
★ ★ 뜻 — ★ 기아 1,020건은 ★ 지금 ★ 짝을 못 짓는다.  ★ 6단계 뒤에 다시 받겠습니다
```

### ⓑ ★ 「채워진 축 수가 많은 것」(규격 2-1 ②)을 ★ 아직 안 넣었다

```
★ `classify_duplicates` 는 ★ 판정(점수) ★ 전에 도는 자리다 —
  ★ 그 자리에서 ★ 「채워진 축 수」를 셀 수가 없다
★ 지금은 ★ ①(리스 제외) ③(싼 쪽) ④(먼저 본 것) ⑤(listing_id) ★ 넷으로 가른다
★ ② 를 넣으려면 ★ 판정 뒤에 대표를 다시 세우는 자리가 있어야 한다 —
  ★ 어디에 둘지 ★ 정해 주십시오.  ★ 임의로 만들지 않았다
```

### ⓒ ★ 축 합치기(2-2)와 `source_listing_id` 는 ★ 아직이다

```
★ 지금은 ★ 「대표 하나 세우기」까지 했다 (화면에 한 번만 뜨는 것)
★ 「축을 합치고 ★ source_listing_id 를 남기는 것」은 ★ 판정 쪽 일이다 —
  ★ ★ 사이트를 넘는 무리가 ★ 아직 0무리라 ★ 넣어도 ★ 지금은 ★ 못 잰다
★ ★ KB 가 들어온 뒤에 ★ 실측하며 넣는 것이 옳다고 보아 ★ 5단계 뒤로 미뤘다.
  ★ 다르게 보시면 말씀해 주십시오
```

### ⓓ 볼보

```
★ 볼보는 ★ 아직 수집 자체가 없다 (7단계).  ★ 들어올 때 ★ 짝짓지 않도록 막는다
★ 지금 막을 것이 없어 ★ 코드를 넣지 않았다 — ★ S46-13 은 ★ 볼보가 들어온 뒤에 뜻이 있다
```
