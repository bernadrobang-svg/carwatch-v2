# 가이드 새 규격을 받은 뒤 — fatal 7 → 5 · `V11-69` 는 내 탓이었다

## 안 한 것

| 무엇 | 왜 못 했나 | 며칠째 | 누가 답해야 하나 |
|---|---|--:|---|
| `sold.html` (V11-30 · test_web) | 설계도 ⑦번이다. 「구조를 손대지 마라」에 걸려 **안 만들었다** | 이틀째 | **마스터** |
| `V11-12` 라우팅 표 중복 | 규격의 중복 행 (`61-web.md:196`·`:209` 둘 다 `/admin/users`). 규칙 2 | 이틀째 | **가이드** |
| `V1-23` · `V2-01` | 앞 회차부터 남은 것 | 여러 회차 | 개발측 |
| `outputs/check-tmp` 12GB | 물음지 #57·#58. **안 지웠다** | 오늘 | **마스터** |
| KB·리본카 `target_key` (40건) | 하루치 타이머(13시)에 맡겼다 | 오늘 | — |

## 1 무엇을 하려 했나

앞 회차 뒤 `git pull` 로 가이드의 새 규격(r923~r930)이 들어왔고,
`check_all` 이 **통과 418 → 453 · fatal 5 → 7** 이 됐다. 늘어난 것을 짚었다.

## 2 무엇을 했나

| 파일 · 줄 | 무엇 | 누구 탓인가 |
|---|---|---|
| `validate/v11_web.py:3496` | `_with_includes()` — 틀을 **조각까지 펼쳐** 읽는다 | ★ **내 탓** |
| `web/templates/why.html:1` | 「★ 이 화면은 무엇을 하는 곳인가」 절 | 가이드 새 시안 |
| `web/templates/why.html:51` | 「★ 우리 판정은 참고입니다」를 **절**로 | 가이드 새 시안 |
| `validate/v0_guide.py:3772` | 안 쓰는 `import re as _re` 삭제 (ruff F401) | 가이드 새 검사 |
| `tools/tmp_contend.py` | 삭제 — 내 계측용 임시 파일 | ★ **내 탓** |

### ★ `V11-69` 는 내가 만든 것이었다 — 적어 둔다

```
V11-69  v1 이 가진 조작이 v2 에도 있음 — 빠진 것 1 · ['watch — 미리보기']
```

까닭 — 08-30 에 `listings`·`recommend`·`watch` 셋이 나눠 갖던 101줄을
`_rowjs.html` **한 조각**으로 모았다 (UI_REVIEW 28장 · `S46-122`).
그랬더니 `data-peek` 이 조각으로 옮겨 가 **`watch.html` 에서 0개**가 됐다.
검사는 **틀 파일만** 읽으므로 「없다」고 했다.

★ 화면이 실제로 무엇을 내는가는 **조각까지 합친 것**이다. 렌더 결과에는
`data-peek` 이 그대로 2개 있었다 — 화면은 안 깨졌고 **검사만 못 본 것**이다.

★★ 이것을 안 고치면 **「공통으로 묶을수록 검사가 실패」**하는 꼴이 된다.
마스터께서 시키신 공통 조각화(28장)와 정면으로 부딪힌다. 그래서 검사가
조각을 펼쳐 보게 했다.

되돌려 확인했다 —

```
조각을 떼면   → 빠진 것 1 ['watch — 미리보기']
되돌리면      → v1 과 같다 []
```

★ **구멍을 안 냈다.** 조각을 정말 떼면 여전히 잡는다.

## 3 왜 그렇게 했나

- `V11-69` — UI_REVIEW 28장(공통 조각)과 `S46-122`. 검사가 틀 하나만 보면
  조각화가 불가능해진다.
- `why.html` 두 절 — `ref/screens/v4m_why_시안.html` (가이드 `8d3b0c9e` · r923).
  **시안 글자를 그대로 옮겼다** — 개발측이 새로 쓰지 않았다.
- ruff — 가이드가 올린 `s46_144_empty_is_split` 이 `re` 를 import 하고 안 쓴다.
  그 함수는 낱말을 `in` 으로만 본다.

## 4 · 5 몇 → 몇

| | 새 규격을 받은 직후 | 지금 |
|---|--:|--:|
| check_all 통과 | **453** | **455** |
| check_all fatal | **7** | **5** |
| run_tests 실패 | 3파일 (`test_endtoend`·`test_web`·`ruff`) | **2파일** (`test_endtoend`·`test_web`) |
| `V11-69` | 빠진 것 1 | **0** |
| `V11-30` | 3 | **1** (남은 것은 `sold.html`) |
| `V11-45` | 1건 | **0** |
| ruff | 실패 | **통과** |

★ 남은 `test_endtoend`·`test_web` 두 파일은 **같은 하나** 때문이다 —
`sold.html` 이 없어서다. 그 화면은 설계도 ⑦번이라 안 만들었다.

## 6 리베이스에서 생긴 일 — 적어 둔다

`git pull --rebase` 가 `docs/CHECKS.md`·`INDEX.md`·`SOURCE.md` 셋에서 충돌했다.
셋 다 **기계가 만드는 것**이라(`tools/build_index.py`) 가이드 것을 받은 뒤
다시 만들어 풀었다. 손으로 고르지 않았다 (규칙 11).
