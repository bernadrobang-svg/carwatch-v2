# 1b — K카가 팔린 차에 영영 안 닿았다 (S46-117)

## 안 한 것

| 무엇 | 왜 못 했나 | 며칠째 | 누가 답해야 하나 |
|---|---|--:|---|
| 0 잠금 | **재현했다.** 고침을 재는 중이다 (다음 기록) | 오늘 | 개발측(이번 회차) |
| 1a·1c·1d | 차례로 간다 | 오늘 | 개발측 |
| V11-12 라우팅 표 중복 | **규격의 중복 행**이다 — `61-web.md:196` 과 `:209` 가 둘 다 `/admin/users` 다 | 오늘 | **가이드** |
| 폴스타 `targets.json` | 1e 가 끝났으니 가이드 차례 | 오늘 | **가이드** |

## 1 무엇을 하려 했나

ORDER r879 1b — 「`collect_kcar.py:176`·`:191` `todo` 가 `detail_status='ok'` 를 빼서
저장된 매물이 gone 에 영영 안 닿는다. `collect_bobaedream.py:238` 꼴로」

## 2 무엇을 했나

| 파일 · 줄 | 무엇 |
|---|---|
| `tools/collect_kcar.py:94` | `fetch_stock` 이 「끝까지 받았나」를 함께 돌려준다 (`listCount` == 받은 수) |
| `tools/collect_kcar.py:123` | 못 읽은 항목을 센다 — 하나라도 있으면 「끝까지」가 거짓이다 |
| `tools/collect_kcar.py:165` | `raw_link_raws` ＋ `sweep_gone_groups` (보배 꼴) |
| `tools/daily_collect.py:32` | ★ `ARGS = {"kcar": ("--list",)}` |

### ★ 함께 잡힌 것 — 하루치가 K카를 헛돌리고 있었다

`tools/collect_kcar.py` 를 **인자 없이** 부르면 목록을 한 건도 안 받는다 —

```
★ carCd 를 주어야 한다 — ★ 목록은 못 만든다
```

`daily_collect.py` 가 그렇게 부르고 있었다. 그래서 08-29 한 바퀴에서 K카가
`0초 · enc 암호화` 로 끝났던 것이다. **`--list` 를 붙였다.**
(★ `enc` 로 막힌 것은 `POST /bc/search/list/drct` 이고, 재고 목록은 따로 있다)

## 3 왜 그렇게 했나

`docs/chapters/11-store/a-key.md` 개정 838 — 「끝까지 받았을 때만 gone 을 매긴다」
ORDER r879 1b — 「`collect_bobaedream.py:238` 꼴로」

★ 「끝까지」를 두 겹으로 뒀다 — ① 사이트가 말한 `listCount` 와 받은 수가 같을 것
② 못 읽은 항목이 0 일 것. 못 읽은 것이 있으면 그 매물번호를 모르므로,
살아 있는데 「목록에 없다」로 보여 gone 이 된다.

## 4 · 5 몇 → 몇 — ★ 돌려서 쟀다

```
python3.11 tools/collect_kcar.py --list

★ 목록 원문을 남겼다 (864,921자 · stored)
★ 저장할 것 105건 · ★ 안 넣는 것 382건 (원문은 남는다)
★ 목록 저장 487건 · site='kcar'
★ 목록에 없어 gone 으로 매긴 것 19건 (5차종) · 끝까지 받았나 예
★ 상세 — 받을 것 4건 (이미 받은 것 463건은 건너뛴다)
★ 상세 — 정상 0 · 없는 매물 4 · 못 받음 0  ★ 팔린 것 4건을 gone 으로
★ 저장된 K카 매물 — 604건
```

| | 고치기 전 | 고친 뒤 |
|---|--:|--:|
| K카 gone | **142** | **156** |
| K카 살아있음 | 462 | **448** |
| 목록으로 gone 을 매기나 | **아니오** — 상세를 다시 받을 때만 | **예** — 목록에 없으면 팔린 것 |
| 한 번 받은 매물이 다시 검사되나 | **아니오** (`detail_status='ok'` 를 뺀다) | 목록에서 매 회차 본다 |
| 하루치가 K카를 부르는 꼴 | `collect_kcar.py` — **0건** | `--list` — **487건** |
| check_all 통과 | 415 | 415 |
| check_all fatal | 5 | 5 |
| run_tests 실패 | 1파일 `test_endtoend` | 1파일 `test_endtoend` |

★ **14건이 새로 gone 이 됐다.** 앞서는 영영 안 닿던 것들이다.

## 6 ★ 검사가 한 번 105 로 떨어졌다 — 적어 둔다

K카가 저장을 끝내고 재판정을 큐에 넣었고, 그것이 도는 동안 `check_all` 을
돌렸더니 **통과 415 → 105 · fatal 5 → 10** 이 나왔다. 코드는 하나도 안 바뀌었다.
까닭은 `database is locked` 로 검사 묶음이 통째로 죽은 것이다.

★ 이것이 0번(잠금)이 왜 급한지를 그대로 보여 준다 — **재판정이 도는 동안에는
검사도 못 믿는다.** 재판정이 끝난 뒤 다시 재니 415 로 돌아왔다.
위 표의 숫자는 그렇게 다시 잰 것이다.
