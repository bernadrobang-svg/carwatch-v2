# SPEC-2026.08.21-r437 — 배포 · 메뉴 27개 · 현황 화면

`v195` · 2026-08-22 01:30

---

# 1. 무엇을 하라 했나 (지시 원문)

```
★ 오늘 화면 다섯을 만들었다 — 메뉴·목록·필터·상세·관심
★ 그런데 ★ 배포가 안 됐다. 마스터 화면은 옛 화면이다

1  ★★ 배포부터 해라
   43.201.16.78.sslip.io 에서 ★ 마스터 화면으로 직접 확인해라
   ★ outputs/render/ 는 파일이지 서버가 아니다
   ★ 「렌더 파일이 맞다」를 완료 근거로 삼지 마라 (S38)
2  메뉴가 화면에 27개다 — 규격은 3개다
   ★ V11-157 이 왜 통과했는지 확인해라. 검사가 렌더 파일만 봤을 것이다
3  현황 화면 — 마지막 하나
```

---

# 2. ① 배포 — 무엇이 잘못돼 있었나

## 1차 결과 (손대기 전 · 실측 08-22 00:2x)

```
carwatch.service  ActiveEnterTimestamp = Thu 2026-08-20 08:46:26 KST
가장 최근 소스     web/views.py · report/screens/build.py = 2026-08-21

→ ★ 돌고 있던 것은 이틀 전 코드다
```

라이브 응답 (`https://43.201.16.78.sslip.io`)

| 경로 | 배포 전 | 배포 후 |
|---|--:|--:|
| `/detail/<id>` | **404** | **200** |
| `/listings` 상단 메뉴 | **6개** | **2개** (익명 · `/watch` 는 로그인 필요) |

★ `/detail` 이 404 였다 — 개정 427 STEP 97a 로 만든 화면이 서버에 아예 없었다.

## 조치

```
sudo journalctl -u carwatch --since "-10min" | grep POST   →  0건 (마스터 미사용 확인)
sudo systemctl restart carwatch
```

## 조치 후 — ★ 마스터 화면에서 직접 확인

```
/listings   200   nav 2개 (매물 · 현황)
/           200
/detail/7781 200  ★ 11절 전부 있음 —
            h3 10개 + <details><summary>판정 근거 — 24축 전부</summary> 1개
/compare /market /dealers /recommend /notready   전부 200
            ★ V11-158 「내린 화면이 여전히 열린다」가 라이브에서도 참이다
목록 실측    막대 350 · 필터 칩 225 · 「고른 조건」 문장 있음 · 「뺀 건수」 있음
```

---

# 3. ② 메뉴 27개 — ★ V11-157 은 틀리지 않았다

## 가이드 추측과 실측이 다르다

```
가이드 추측   「검사가 렌더 파일만 봤을 것이다」
실측         ★ 아니다.  V11-157 은 menu_items(acc) 를 실제로 부른다
             validate/v11_web.py:2303 _menu_shape_checks()
             주석에도 「★ config 만 보지 않는다. ★ menu_items() 를
             실제로 불러 본다 (S43-②)」라고 적혀 있다
```

소스를 그대로 불러 본 값 —

| 역할 | 개수 | 메뉴 |
|---|--:|---|
| admin | **3** | 매물 · 관심 · 현황 |
| user | 3 | 매물 · 관심 · 현황 |
| anonymous | 2 | 매물 · 현황 |

`config/web.json top_menu` = `['/listings', '/watch', '/']` — **셋이다.**

## ★ 그러면 27개는 어디서 나왔나

배포돼 있던 커밋(08-20 08:46 이전)의 `web/app.py` —

```python
def menu_items(account) -> list[dict]:
    """라우팅 표에서 메뉴를 만든다.  ★ 표가 정본이다 (STEP 142)."""
    for r in ROUTES:            # ← ★ 라우팅 표를 통째로 편다
        ...
        if ROLE_RANK[r.role] > rank: continue
        out.append(...)
```

관리자로 보면 `/admin/*` 열아홉이 전부 메뉴가 된다 → **27개.**
마스터가 보신 27개는 **이 옛 코드가 낸 것**이다.

```
★ 결론   검사는 옳았다.  소스도 옳았다.  ★ 돌고 있는 것이 틀렸다
```

## ★ 규격에 안 적힌 것 — 검사 250개 중 「돌고 있는 서버」를 보는 것이 하나도 없다

```
S38 은 「규격·소스·화면·검사 네 칸」을 요구한다
★ 그런데 네 칸이 다 차도 배포를 안 하면 마스터 화면은 옛것이다
★ 이것이 이번 사고의 진짜 구멍이다 — 「선언과 실제의 괴리」의 배포판이다
```

임시로 도구를 하나 만들었다 — `tools/deploy_check.py`

```
$ python3.11 tools/deploy_check.py
○ 배포됨 — 가장 최근 소스 config/field_usage.suggested.json 보다 10분 뒤에 서비스가 떴다
exit 0

배포 안 됐으면   ✗ 배포 안 됨 — <파일> 을 서비스가 뜬 뒤에 고쳤다 (N분 늦음)  · exit 1
```

```
★ 규격 코드(S번호)는 붙이지 않았다.  가이드가 준다 (규칙 2)
★ config/cli_screens.json 의 pending 에 넣었다 —
  화면을 「/admin/audit 이면 되겠지」로 개발측이 정하지 않는다
★ 코드를 주시면 check_all 에 붙인다
```

---

# 4. ③ 현황 화면 (`/` · STEP 95)

## 1차 결과 — 규격 대비 무엇이 비어 있었나

`DashboardView` 에 **자리는 있는데 `view_dashboard` 가 안 채운 필드가 넷** 있었다.

| 필드 | 규격 (STEP 95) | 1차 상태 |
|---|---|---|
| `relax_sim` | 조건 완화 시뮬레이션 | **선언만 · 항상 빈 리스트** |
| `axis_shortfall` | 축별 미달 건수 | **선언만 · 항상 빈 리스트** |
| `watch_summary` | 관심매물 요약 | **선언만 · 항상 빈 리스트** |
| `recent_runs` | 수집 로그 | 선언만 (`steps` 가 대신 낸다) |
| — | ★ 사라짐 목록 (개정 427) | **없음** |
| — | ★ 진행률 (개정 427) | **없음** |
| `attention` | 확인 필요 — pending·unclassified·**검증 warn**·미확정 | **검증 warn 이 빠져 있었다** |

```
★ 이것이 이 프로젝트가 막으려는 바로 그 무늬다 —
  dataclass 에 자리를 만들어 두고 아무도 안 채웠다.  화면은 조용히 비어 있었다
```

라이브 `/` 실측 (배포 후 · 손대기 전) — h2 **6개**
`등급 분포 · 차종별 시세 사분위 · 차종별 · 오늘 변동 · 수집 · 상위 후보`

## 무엇을 했나 — 고친 파일·줄

| 파일 | 무엇 |
|---|---|
| `report/screens/build.py:1784` | 확인 필요 쿼리에 **검증 warn** 을 더했다 (`audit_validation`) — ★ 같은 쿼리에 붙였다 |
| `report/screens/build.py:1810` | `grade_counts` 를 **이미 받아 온 `by_target` 에서 접어** 만든다 — 쿼리 −1 |
| `report/screens/build.py` | `grade_total` = `sum(grade_counts)` — 쿼리 −1 |
| `report/screens/build.py` | `_relax_sim()` 신설 — 조건 완화 (쿼리 0 · 등급 분포에서 유도) |
| `report/screens/build.py` | `_axis_shortfall()` 신설 — 축별 미달 (쿼리 +1) |
| `report/screens/build.py` | `_gone_and_watch()` 신설 — 사라짐 + 관심 요약을 **한 쿼리(UNION ALL)** 로 (쿼리 +1) |
| `report/screens/build.py` | `_progress()` 신설 — 진행률 (쿼리 0 · `steps` 에서 유도) |
| `report/screens/views.py:381` | `DashboardView` 에 `gone_rows` · `progress` 추가 |
| `web/templates/dashboard.html` | 블록 다섯 신설 · 진행률 막대 |
| `config/labels.json:70` | `watching` → 「보는 중」 · `closed` → 「닫음」 (raw 값이 화면에 그대로 나왔다) |
| `config/cli_screens.json` | `tools/deploy_check.py` 를 pending 에 |
| `report/screens/views.py.bak` | **지웠다** — 규칙 8 「.bak 로 백업하지 않는다」 |

## 왜 그렇게 했나 (규격 근거)

```
STEP 95        v1 블록 구성을 유지한다 (조건 완화 · 축별 미달 · 관심매물 · 확인 필요)
개정 427       현황에 차종별 시세표 · 확인 필요 건수 · 사라짐 목록 · 진행률
V11-34         한 화면 쿼리 상한 20.  ★ 그래서 새 블록을 두 쿼리에 욱여넣고
               대신 등급 집계 두 쿼리를 없앴다
V6-06          gone 을 「팔렸다」로 적지 않는다 — 목록에서 사라진 것이다
개정 435·V3-96 축별 미달은 value 가 아니라 ★ source 로 가른다
               (value NULL 로 세면 3건, source 로 세면 15,810건이다)
STEP 152       막대 높이를 화면이 계산하지 않는다 — 진행률 %를 코드가 낸다
```

## 조치 후 결과

라이브 `/` h2 **11개**

```
해야 할 일(확인 필요) · 등급 분포 · ★조건 완화 · ★축별 미달 ·
차종별 시세 사분위 · 차종별 · 오늘 변동 · ★사라짐 · ★관심매물 ·
수집(★진행률 99.8%) · 상위 후보
```

| 잰 것 | 1차 | 조치 후 |
|---|--:|--:|
| `/` 쿼리 수 | **20 / 20** (상한 꽉 참) | **20 / 20** (블록 다섯을 더 넣고도 그대로) |
| `/` 렌더 시간 (콜드) | **14.7초** | **1.4초** |
| `/` 라이브 응답 | **2.45초** (상한 2.0 초과) | **0.57초** |
| `/` 본문 크기 | 23,342 B | 30,429 B |
| 빈 선언 필드 | 4 | **0** |

새 블록이 실제로 내는 숫자 (라이브)

```
조건 완화   S 이상 0건 → 한 단계 낮추면 69건 · A 이상 69 → 283 · B 이상 283 → 683
축별 미달   taste.picked 3,841건 61,456점 · state.frame 1,729건 34,580점
           state.accident 1,339건 32,136점 · state.consumable 3,841건 26,887점
사라짐      G80_25T 3,999만 → 계약 중 (2026-08-21) 외 · sales_status CONTRACT 159건
관심매물    6건 · KOLEOS_HEV 2,860만 「보는 중」 외
진행률      99.8% — 요청 430 가운데 429 성공
```

---

# 5. 검사 결과 — ★ 실제 출력

## `python3.11 tools/check_all.py`

```
1차 (손대기 전)   통과 344 · fatal 3 · warn 11 · 환경 차이 1 · 미실행 9
조치 후          통과 344 · fatal 3 · warn 11 · 환경 차이 1 · 미실행 9

■ FATAL — 고쳐야 한다
  V1-21     받아 두고 안 펼쳐진 원문이 없음 — 9
  V1-23     필요한 조합 대비 받은 카탈로그 비율 — 127조합 · 79.9%
  V11-76    화면 크기·시간이 상한 안 — 5곳
```

★ 셋 다 이번 작업 이전부터 있던 것이다.  V11-76 은 아래 §6-2 를 보시라.

## `python3.11 tools/run_tests.py`

```
1차 (손대기 전)   결과: 통과   (19파일 + ruff 전부 통과)
중간 (한 번 깨졌다) 결과: 실패 — test_endtoend.py, test_integration.py, test_web.py
                 까닭   h2 를 「해야 할 일」 → 「확인 필요」로 바꿨다.
                        ★ 시안(ref/screens/v2_dashboard_시안.html:151)이
                          「해야 할 일」이라 V11-30 이 물었다
                 고침   시안 이름을 h2 로 되돌리고 STEP 95 이름을
                        「해야 할 일 <span class=muted>확인 필요</span>」로 함께 적었다
조치 후          결과: 통과   (19파일 + ruff 전부 통과)
```

---

# 6. ★ 못 한 것 · 막힌 것 · 여쭐 것

## 6-1. ★ 규격이 서로 어긋난다 — STEP 95 (개발측이 안 고친다 · 규칙 2)

```
41-view.md:284  「★ 현황에는 ★ 차종별 시세표(사분위) · 확인 필요 건수 ·
                 사라짐 목록 · 진행률만 둔다」          ← ★ 「만」
41-view.md:290  「v1 블록 구성을 유지한다」 + 9블록 표   ← ★ 아홉을 둔다
```

★ 「만 둔다」와 「아홉을 유지한다」가 같이 있다.
지금은 **더하는 쪽**으로 읽었다 — 「만」은 「목록 위 요약 6칸을 여기로 옮기지 마라」는
뜻으로 보았다.  ★ 아니면 말씀해 주시라.

## 6-2. ★ V11-76 — 라이브가 상한을 크게 넘는다 (fatal · 이번 작업 밖)

라이브 실측 (상한 `screen_max_kb` 300KB · `screen_max_sec` 2.0초)

| 경로 | 시간 | 크기 | 판정 |
|---|--:|--:|:--:|
| `/compare` | **29.0초** | **8,731,728 B (8.7MB)** | ★★ 심각 |
| `/detail/7781` | **11.1초** | 20,938 B | ★ |
| `/recommend` | **6.3초** | 181,388 B | ★ |
| `/notready` | **4.6초** | 13,414 B | ★ |
| `/listings` | 1.0초 | **331,395 B** | 크기 초과 |
| `/` | 2.45 → **0.57초** | 30,429 B | ★ 이번에 고쳤다 |

```
★ /compare 8.7MB · 29초는 마스터가 누르면 화면이 멈춘 것처럼 보인다
★ V11-76 이 실행마다 2·3·4·5곳으로 흔들린다 — 기계 부하를 타는 검사다.
  ★ 「몇 곳이냐」가 아니라 ★ 「어디냐」를 내야 고칠 수 있다
★ 밀린 일 대장 #8 「대청소」에 넣을지 여쭙는다.  지금 손대면 현황이 밀린다
```

## 6-3. ★ `tools/build_index.py` 가 죽는다 (이번 작업 이전부터)

```
IndexError: string index out of range
  tools/build_index.py:194  key()  →  head[0]
  검사 코드 목록에 ★ 빈 문자열이 하나 섞여 있다

확인   git stash 로 내 변경을 뺀 상태에서도 똑같이 죽는다 — 내가 낸 것이 아니다
영향   ★ 규칙 11 의 「검사를 고치면 build_index 를 다시 돌린다」를 지킬 수 없다
       docs/CHECKS.md · docs/SOURCE.md 가 낡은 채로 남는다.  S28 이 이것을 문다
```

## 6-4. 현황 「상위 후보」가 규격과 다르다

```
규격 STEP 95   | 최종후보 | 확정 후보 카드 | watch_candidate (11장) |
지금 코드      view_recommend(...)[:5]  — ★ 폐기된 /recommend 의 함수다 (개정 427)

★ 안 바꿨다.  까닭 —
  ① watch_candidate 이 0건이라 바꾸면 블록이 통째로 빈다
  ② 이 블록이 V11-68·V11-69 (v1 조작 여섯) 를 담고 있다
  ③ view_recommend 가 50건을 만들어 5건만 쓴다 — 6쿼리 4.5초.
     ★ /  가 느렸던 가장 큰 까닭이 이것이다 (지금도 남아 있다)
★ 여쭌다 — 최종후보를 watch_candidate 로 바꿀까, 지금대로 둘까
```

## 6-5. 안 한 것

```
밀린일대장 2a  상세 11절 등급 비교가 뒤집힌 것 (S<A<B<C 를 A<B<C 로 센다)
              ★ 「현황이 끝난 뒤 지적한다」고 대장에 적혀 있다.  현황이 끝났다
밀린일대장 §5  갈래 ② state.integrity 675건 · history.not_join 7건 (파서 한 줄)
밀린일대장 §4  「모름」을 「없음」으로 내는 것 (개정 435) — 15,709건
```

---

# 7. 커밋

```
d6583e4
```
