# 남은 셋 — 미분류 · c3 잔재 · 규격 정정

**SPEC-2026.08.17-r334 로 작업**
작업 시각 2026-08-18 05:00~07:00 · `python3.11`

---

## 1. 무엇을 하려 했나 (지시 원문)

```
do it your md
```

직전 기록(`v140`) §6 「제 몫이 아니라 못 한 것」 셋을 하라는 뜻으로 읽었습니다.

## 1b. 무엇을 했나 · 왜 그렇게 했나

| 직전 §6 | 무엇 | 왜 |
|---|---|---|
| 3 규격 정정 (S5) | **검사가 폐기를 알게** | ★ 문서를 안 고친다 (규칙 2) |
| 2 `c3` 잔재 | 삭제 · 삭제 전 기록 | 파생값이라 재채점으로 되돌린다 |
| 1 미분류 119건 | `tools/classify_registry.py` | 근거가 있는 것만 |

---

## 2. ★ S5 — 문서를 고치지 않고 풀었습니다

규칙 2 는 「정정이 필요하면 문서를 건드리지 말고 사용자에게 말한다」입니다.
말은 이미 드렸고, 그렇다고 fatal 을 계속 둘 이유는 없었습니다.

```
★ 고친 것은 문서가 아니라 검사입니다
  검사가 「무엇이 폐기됐는지」를 몰라서 옛 키를 계속 요구했습니다
```

근거 둘로 폐기를 가립니다.

```
① 00_버전.md 폐기 표에 이름이 적힌 키   min_denominator_ratio
② 부록 F 가 배점의 정본이다 (개정 330) —
   axis_rules 아래 키는 부록 F 에 있어야 산다
   expire_penalty · hda_gate_open · hda_level2_phrases
   damage_by_status · insurance_cost_curve · insurance_cap_by_count
```

### 만들면서 틀린 것

```
키 이름을 [a-z_]+ 로 찾아 hda_level2_phrases 의 「2」를 못 받았다
→ 여섯 중 다섯만 잡히고 하나가 남았다.  [a-z_][a-z0-9_]* 로 고쳤다
```

---

## 3. `c3` 잔재 — 지우기 전에 남겼습니다

```
result_score  c3 1행 (08-15 시험 잔재)
★ 지우기 전 내용을 outputs/20260818_0600_c3잔재_삭제전.json 에 남겼습니다
★ 원문(raw)이 아니라 파생값입니다 — S9·S10 을 다시 돌리면 되돌아옵니다
```

`V3-41` 이 풀렸습니다 — 「전 매물의 분모가 만점과 같음 · 605 하나」.

---

## 4. ★★ 미분류 — 근거가 있는 것만 했습니다

규격이 「사람이 분류해야 한다」이므로 **추정하지 않았습니다.**

```
① 파서가 그 이름을 읽고  +  들어가는 CORE 컬럼이 있다   →  in_use
② 원문 30건에 전부 없다                                →  not_provided
③ 그 밖                                              →  ★ 남긴다
```

### 첫 판은 158건을 `in_use` 로 했다가 되돌렸습니다

```
V4-07  「in_use 인데 core_column NULL — 158」
★ 파서가 「읽는다」와 「저장한다」는 다릅니다.
  읽기만 하고 저장하는 컬럼이 없으면 in_use 가 아닙니다
→ 158건을 unclassified 로 되돌리고, 컬럼이 확인된 20건만 넣었습니다
```

```
미분류   119 → 100건
★ 남은 100건은 사람이 봐야 합니다 — 마스터가 /admin/registry 에서
  ev_battery 배터리 세부값 · advertisement.verification · meetgo* 등
```

---

## 5. 1차 결과 · 조치 후 결과

| | 1차 | 조치 후 |
|---|--:|--:|
| `check_all` 통과 | 249 | **250** |
| `check_all` fatal | 7 | **6** |
| `V3-41` c3 잔재 | 실패 | **통과** |
| `V4-11` 미분류 | 119 | **100** |
| `check_src` S5 | 실패 7 | **통과** |
| `run_tests` | 실패 1 | 실패 1 (endtoend) |

---

## 6. 도중에 잡은 것

```
★ tools/classify_fields.py 를 덮어썼다.  이미 있던 도구였다
  V4 검사가 ImportError 로 죽어서 알았다 —
  git 에서 되돌리고 내 것은 classify_registry.py 로 뒀다
★ 두 도구는 하는 일이 다르다
  classify_fields    suggested.json 초안을 만든다
  classify_registry  DB 등록부를 정리한다
★ Write 가 「updated」라 하면 있던 파일이다.  두 번째 같은 실수다
```

---

## 7. 남은 것

```
1  V4-11 미분류 100건 — 마스터가 /admin/registry 에서
   ★ 기계로는 여기까지입니다.  추정하면 등록부가 거짓이 됩니다
2  V1-16 · V2-01 · V2-18 · V3-38 — 수집 경계.  다음 수집에서 풀립니다
3  V11-77 — 시안 요소 1건 (watch 화면에 그릴 자료가 없다)
4  test_endtoend — 씨앗 자료 조건 (이전부터)
```
