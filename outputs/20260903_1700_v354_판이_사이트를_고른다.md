# v354 — 「왜 엔카만 도나」 (가이드 지적 09-03)

## 1차 결과 — ★ 가이드 말이 맞다

```python
# run.py:113
cfg = load("endpoints.json")["encar"]      ← ★ **엔카가 박혀 있다**
adapter = EncarAdapter(cfg)                ← ★ **어댑터도 박혀 있다**
```

```
endpoints.json 에 있는 사이트  ★ **열두 곳**
어댑터가 있는 사이트          ★ 여섯 곳 (encar · kbchachacha · kia_cpo ·
                                     bobaedream · heydealer · kcar)
판(`run.py collect`)이 도는 곳 ★ **한 곳** (엔카)
```

★ 그래서 ★ `S0~S12` 판을 돌려도 ★ **엔카만** 돌았다.
★ ★ 나머지는 ★ 저마다의 `tools/collect_*.py` 로만 받혔다 —
★ ★ ★ 그래서 ★ `parsed_at` 이 열한 곳에서 0 이었다 (v352 의 뿌리와 같은 자리다).

## 무엇을 · 왜 했나

```
① `--site` 를 열었다 — ★ `run.py collect --site kia_cpo --only S3`
② 어댑터 표(`ADAPTERS`)를 뒀다 — ★ 사이트 이름은 ★ `endpoints.json` 이 정본이다
③ 어댑터가 없으면 ★ **무엇이 되는지 말한다** (금지 12 — 조용히 죽지 않는다)
④ `build_q` 는 ★ 엔카에만 있다 — ★ 있을 때만 낸다
```

## 조치 후 결과

```
$ python3.11 run.py collect --site kia_cpo --dry
★ 사이트: kia_cpo
차종 27종 · collect_group 1개
--dry: 요청하지 않았다

$ python3.11 run.py collect --site volvo_selekt --dry
★ 사이트: volvo_selekt
[X] volvo_selekt 는 판이 못 돈다 — 어댑터가 없다.
    되는 것: bobaedream, encar, heydealer, kbchachacha, kcar, kia_cpo ·
    그 밖은 tools/collect_volvo_selekt.py 가 받는다
```

| | 앞 | 뒤 |
|---|--:|--:|
| 판이 돌 수 있는 사이트 | **1** | **6** |
| 안 되는 사이트가 하는 말 | (죽는다) | **무엇이 되는지 말한다** |

## 여쭐 것

```
★ 어댑터가 없는 여섯(volvo_selekt · lexus_certified · bmw_bps · revolt ·
  hyundai_cert · reborncar)은 ★ 판으로 못 돈다.
★ 안 ① 어댑터를 만든다 — ★ 여섯 개다.  ★ 큰 일이다
★ 안 ② 그대로 둔다 — ★ `tools/collect_*.py` 가 이미 받고 있고
      ★ ★ 넣기는 ★ `tools/load_raw.py` 가 한다 (v352 에서 표까지 읽게 고쳤다)
★ ★ **안 ②로 두고 진행했습니다** — ★ 지금 막힌 것이 아니라서입니다.
   ★ ★ 다만 ★ 「판 한 번에 열두 사이트」를 원하시면 ★ 안 ①이 필요합니다
```
