# 0j — 넷 (r974) · 사이트를 안 두드리는 1,895점

## ★ 안 한 것

| 무엇 | 왜 못 했나 | 며칠째 | 누가 답해야 하나 |
|---|---|---|---|
| 골격 43 · 외판 28 (헤이딜러) | `part`(door_front_driver…) → 등급(`RANK_A`·`RANK_ONE`) 표가 규격에 없다. ★ 내가 옮기면 골격을 외판으로 매긴다 | 오늘 | ★ 마스터 |
| `dict_enum(axis='part')` | STEP 41 축 정책 표에 `part` 행이 없다 — `ValidationError: 축 정책 미정의: part`. 규격 3a②는 넣으라 한다 (규격끼리 어긋난다) | 오늘 | ★ 마스터 |
| 0j 3 — 신차가·트림 536점 | 열쇠 넷 중 **제조사·트림이 빈다** (아래 3절). `dict_model_trim` 표는 **DB 에 없다** | 오늘 | ★ 마스터 |
| 옵션 387점 | `site=?` 는 뺐다. 그런데 **`model_catalog_key` 가 아홉 사이트 전건 NULL** 이라 아직 안 열린다 (아래 2절) | 오늘 | ★ 마스터 |
| 헤이딜러 렌트 1건 용도 축 | 렌트 불리언이 축의 네 입구 어디에도 안 맞는다. **열면 렌트 차가 「자가용 30점」을 받는다** — 안 열었다 | 오늘 | ★ 마스터 |
| KB 부위별 | 사고 있는 매물을 아직 못 찾았다 | 4 | — |
| V1-23 | 0j 넷이 먼저였다 | 3 | — |

## 회차마다 낼 세 수 (화면 기준 · `tools/three_numbers.py`)

| | 지난 회차 | 이번 |
|---|--:|--:|
| ① 아홉 사이트 A 이상 | 0 | **0건** |
| ② 전기차 A 이상 | 13 | **13건** |
| ③ 같은 차 짝 (`/track`) | 161 | **165대** |

---

## ★★ 천장 — 「몇 → 몇」

| 사이트 | 전 | 후 | A 가능 |
|---|--:|--:|:--:|
| ★ **헤이딜러** | 58.6% | ★ **73.2%** | ★ 못 함 → **된다** |
| ★ **렉서스** | 30.1% | ★ **44.8%** | 아직 못 함 |
| 현대인증 | 70.2% | 70.2% | 예 |
| K카 | 71.6% | 71.6% | 예 |
| KB | 53.5% | 53.5% | 못 함 |
| 리본카 | 52.6% | 52.6% | 못 함 |
| 볼보 · 보배 | 43.8% | 43.8% | 못 함 |
| BMW | 42.2% | 38.9% | 못 함 |

★ BMW 가 내린 것은 고침 때문이 아니다 — 매물이 9건뿐이라 갈아든 매물이 다르다.
★ **① 은 아직 0 이다.** 헤이딜러가 A 에 닿을 수 **있게** 됐지만 실제로 닿은 매물은 없다.
1위 매물의 빈 점이 486점이고 그중 **150점(골격 43·외판 28·옵션 43·사이트보증 36)이
위 「안 한 것」에 막혀 있다.**

---

## 1 — 렉서스 파서 (연식 80점)

★ `parse/` 열 곳 중 여기만 없었다. 만들었다 — `parse/lexus_certified/mapping.py` (194줄).

| | 전 | 후 |
|---|--:|--:|
| `year_month` 가 찬 매물 | **0/35** | ★ **34/35** |
| `plate_hash` | 0 | ★ **34** (`split_pii` · 원문은 CORE 에 안 넣었다) |
| `parse/` 폴더 | 10곳 중 9 | **10/10** (`S46-178` 통과) |
| 렉서스 천장 | 30.1% | **44.8%** |

★ 규격 3장 ③ 대로 **`year` 를 연식으로 쓰지 않았다** — `year` 는 모델연도(`form_year`)고
연식은 `car_info.registration_date`(「2025년 8월」→`202508`)다. **그래서 상세를 열어야 한다.**
`tools/collect_lexus.py --detail` 을 붙였다 (72건 · 정상 51 · 없는 차 0 · 못 받음 0).

★ 함께 들어온 것 — 보증(`warranty_body_month/km` · 「2030년 8월까지 (120,000km)」를
등록일과 빼서 총량으로), 배기량·변속기·내장색·외장색 상세명, 사진, 기본사양.

★ 규격 3장 ① 대로 **골격·외판·누유·수리비·압류저당·소유자변경·용도는 NULL 로 뒀다.**
`accident_history`(22/22 전건 「무사고」)는 `site_condition_json` 에만 담고 축에 안 썼다.

★★ **마스터께 여쭙는다 — `payment.isLease` 를 `sell_type` 에 안 넣었다.**
규격 부록은 「`payment.isLease` → `sell_type` · 리스 제외에 태운다」라 합니다.
그런데 이 칸의 다른 값은 「일반 13,491 · 렌트 849 · 리스 587」로 **그 차가 렌트·리스 차였나**(내력)를
뜻하고, 렉서스 `payment` 는 **현금·할부·리스로 살 수 있나**(결제 수단)입니다.
실측 — idx 6047 은 셋이 다 true 입니다. 넣으면 멀쩡한 차가 「리스」로 보이고 거르개에서 빠집니다.
★ 규격을 제가 안 고칩니다 (규칙 2).

## 2 — 옵션 `site=?` (387점)

★ 뺐다 — `tools/verify_axes.py:701`. `S46-177` 통과. 안 쓰는 `site` 칸도 질의에서 뺐다.

★★ **그런데 아직 안 열린다.** 잰 결과 —

| 사이트 | 매물 | `model_catalog_key` | `options_choice_json` |
|---|--:|--:|--:|
| encar | 9,324 | 9,174 | 3,686 |
| ★ **나머지 아홉 곳 전부** | 1,197 | ★ **0** | ★ **0** |

★ `site=?` 가 가두는 것이 맞았지만, **자물쇠가 하나 더 있다** —
아홉 사이트에는 **`model_catalog_key` 자체가 없다.** 열쇠가 없으니 카탈로그 표를 못 짚는다.
★ 운영 경로(`collect/runner.py:1063` · `report/screens/build.py:1695`)는 이미 `site` 없이 읽고 있었다 —
가둬 둔 곳은 `verify_axes.py` 한 군데였다.

★ 그러므로 387점을 열려면 **차종·트림 → `model_catalog_key` 를 붙이는 일**이 먼저다.
★ 어느 표에서 붙일지는 3절과 같은 물음이다.

## 3 — 신차가 75 ＋ 트림 59 = 536점 — **잰 결과 열쇠가 빈다**

★ 먼저 — **`dict_model_trim` 표가 DB 에 없다** (44개 표를 다 셌다).
트림 사다리는 `collect/runner.py:1150 _trim_ladders()` 가 **`core_listing` 자기 자신에서** 만든다.
그리고 트림 축(`taste.trim`)도 값 축(`value.origin`)도 **둘 다 `price_origin_won` 하나에 달렸다** —
`analyze/axis/trim.py:44` 가 `price_origin_won is None` 이면 `origin_price_missing` 으로 0점을 낸다.
★ 그러므로 **75＋59＝134 는 「신차가를 채운다」 하나로 열린다.**

★ 끌어오는 틀은 이미 있다 (`_origin_lend_table` · `_origin_lent` · `origin_lent_from`).
★ 막힌 것은 **열쇠**다 —

| 사이트 | 신차가 빔 | 지금 열쇠로 맞는 것 | `target_key` 로 맞는 것 | ★ 비는 칸 |
|---|--:|--:|--:|---|
| 보배 | 189 | **0** | 91 | 제조사·트림 |
| BMW | 187 | **0** | 22 | 제조사·트림·연식 |
| 볼보 | 149 | **0** | 132 | 제조사·트림 |
| K카 | 27 | 4 | 25 | — |
| 현대인증 | 25 | **0** | 25 | 트림 |
| KB | 11 | **0** | 11 | 트림 |
| 헤이딜러 | 10 | **0** | 1 | 제조사 |
| **합** | **786** | **133** | **485** | |

★★ **여쭙는다 — 열쇠를 `target_key`＋연식으로 바꿔도 됩니까.**
규격 `f-table.md:210` 은 「제조사·모델·트림·연식」으로 못 박혀 있고,
`site_manufacturer`·`trim_badge` 는 **세 사이트에서 전건 NULL** 입니다
(원문이 「M8 Gran Coupe Competition LCI 어벤츄린레드 자동 휘발유」 같은 한 줄이라
갈라 넣으면 제가 짐작으로 이름을 짓는 것이 됩니다 — 금지 6).

★ 바꾸면 **133 → 485건**이 열립니다. ★ **다만 값이 있습니다** —
`target_key` 193가지 중 **115가지가 `_IMPORT` 로 트림을 안 담습니다**
(`XC60_IMPORT` 는 B5 도 B6 도 같은 열쇠입니다). 규격이 트림을 열쇠로 못 박은 것이
바로 그것을 막기 위해서입니다. ★ 그래서 제가 정하지 않습니다.

## 4 — Ⓐ 이미 오는 것 읽기 (헤이딜러 183)

★ **사이트를 한 번도 안 두드렸다.** 저장해 둔 원문을 다시 읽었다 —
새 도구 `tools/backfill_from_raw.py`.

| 무엇 | 건 |
|---|--:|
| 다시 읽은 상세 원문 | 207 |
| 알맹이 없음 (옛 403 봉투) | 85 |
| ★ `core_record` 를 새로 만든 것 | ★ **122** |
| ★ 보증을 채운 것 | ★ **118** |
| 부위(`part`) 값 | 19 (★ 축 정책이 없어 사전에 못 넣었다) |
| 렌트 이력 — 용도 축을 일부러 안 연 것 | 1 |

★ 채운 칸 (규격 `HEYDEALER_API.md` 3-2) — `accident_my_cnt` 122 · `accident_my_cost` 122 ·
`owner_change_cnt` 122 · `use_gov` 122 · `use_business` 122 · `total_loss_cnt` 122
(＋ `accident_other_cnt`·`flood_total_cnt`·`robber_cnt`·`plate_change_cnt`·사고 내역·소유자 변경 날짜)

★ 보증 (규격 3-4) — 헤이딜러는 **남은 양**을 준다 (「49,991km / 10개월 남음」).
축은 「등록부터 몇 달」을 받아 **스스로 경과분을 뺀다** (`analyze/axis/warranty.py`).
그대로 넣으면 **두 번 빠진다.** 그래서 경과분을 도로 더해 **총량**으로 넣었다 — 셈이지 짐작이 아니다.
「보증기간 종료」는 0 이다 (확인한 값이지 모름이 아니다).

★★★ **덤으로 찾은 것 — 현대인증도 같은 자리에서 두 번 뺀다.**
`parse/hyundai_cert/mapping.py:305` 가 「1 년 남음」의 **남은 값을 그대로**
`warranty_body_month` 에 넣습니다. 축은 거기서 경과분을 또 뺍니다.
★ 현대인증 보증 축이 174건 중 0.3% 인 까닭이 이것으로 보입니다.
★ 고치면 판정이 움직이므로 **이번 회차에 손대지 않았습니다** — 다음 지시를 기다립니다.

★★ 렌트 1건을 일부러 안 열었다 — `history.use` 축이 렌트를 가리는 길은 넷
(`advertisement_type`·`usage_change_types`·`record_use`·`plate_use_char`)인데
헤이딜러 불리언은 그 넷 어디에도 안 맞는다. 열어 두면 **렌트 차가 「자가용 30점」을 받는다.**
그래서 「모른다」로 뒀다 (0점＋미확인). `parse/heydealer/mapping.py:161`

---

## 고친 파일

| 파일 | 줄 | 무엇 |
|---|--:|---|
| `parse/lexus_certified/mapping.py` | 새 194 | 렉서스 파서 (0j 1) |
| `tools/collect_lexus.py` | 20·86·167 | 파서 배선 ＋ `--detail` |
| `tools/verify_axes.py` | 685·701 | `site=?` 뺌 (0j 2) |
| `parse/heydealer/mapping.py` | 141~ 새 130줄 | `record_of`·`warranty_of`·`part_enums` (0j 4) |
| `tools/collect_heydealer.py` | 31·146·212·221 | 배선 |
| `tools/backfill_from_raw.py` | 새 117 | 원문 다시 읽기 (통신 없음) |
| `validate/v0_guide.py` | 4240 | ruff F401 (가이드가 r974 에 넣은 줄) |
| `config/cli_screens.json` | pending | 새 도구 넷 — ★ 화면은 안 정했다 (가이드 판단) |

## 도구 · 검사 결과

```
python3.11 tools/check_all.py
  전   통과 462 · fatal 2 · warn 13 · 환경 차이 1 · 미실행 8   (r956 끝)
  중간  통과 467 · fatal 4                                  (ruff·V11-45 가 늘었다)
  후   통과 468 · fatal 3 · warn 13 · 환경 차이 1 · 미실행 8
  ★ 새로 통과   S46-177 (카탈로그를 site 로 안 가둔다) · S46-178 (파서 10/10)
  남은 fatal   V1-23 (카탈로그 17.0%) · V2-01 (−400) · S46-32 (색인 · 커밋 전 재생성)

python3.11 tools/run_tests.py
  전   결과: 실패 — test_endtoend.py, ruff
  후   결과: 통과
```

## 마스터께 올릴 것 (다섯)

1. ★ 헤이딜러 `part` → 등급(`RANK_A`/`RANK_ONE`) 표 — 골격 43·외판 28 이 여기 막혀 있다
2. ★ STEP 41 축 정책 표에 `part` 행 — 규격 3a② 와 어긋난다
3. ★ 끌어오기 열쇠를 `target_key`＋연식으로 바꿀지 (133 → 485건 · 다만 `_IMPORT` 115가지가 트림을 안 담는다)
4. ★ 렉서스 `payment.isLease` → `sell_type` 이 맞는지 (결제 수단 ↔ 내력)
5. ★ 헤이딜러 렌트 불리언을 `history.use` 축에 어떻게 들일지
6. ★ (덤) 현대인증 보증이 두 번 빠지는 것 — 고칠까
