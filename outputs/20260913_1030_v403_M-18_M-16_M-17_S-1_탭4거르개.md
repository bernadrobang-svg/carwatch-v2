# v403 — 탭 4 거르개 · CSS 지문 (지시 09-13)

## 1. 무엇을 하라 하셨나 (지시 원문)

> 마스터 — 「GV70 추천 순위가 저게 맞나. 왜 수집이 미완료된 것이 저렇게 많나」
> M-20(사고 두 줄)은 들어갔다 … 잘했다. 나머지 셋이 그대로다.
> 1. 옛 기준이 새 기준과 겹쳐 떠 있다 — M-18 … `config/targets.json` 의
>    `_탭4_거름` 에서 그려라 … 검사 S46-302 가 이것을 지킨다
> 2. 거르개가 안 붙었다 — M-16 … 1위가 3,950만 · PENDING · 신차가 미조회
> 3. 네 묶음이 안 붙었다 — M-17 … 913대 중 상세를 받은 것은 363대다 …
>    묶음 안에서만 총비용 낮은 순. 섞지 마라
> [CSS 가 아직 옛것이다 — S-1] `v=1ecdbc5297` 이 안 바뀌었다 …
> 배포 뒤 `curl -s 배포/ | grep app.css` 로 `v=` 가 바뀌었는지 확인하고 회차에 적어라

## 2. 「몇 → 몇」 — 한 눈에

| 무엇 | 고치기 전 | 고친 뒤 | 잰 곳 |
|---|--:|--:|---|
| 옛 거르개 문구 (배포 화면) | 2곳 | **0곳** | `curl` 배포 |
| 거른 기준 띠 | 옛것·새것 겹침 | **정본 7개만** | 배포 |
| 잣대를 넘은 차 `passed` | 913대 | **679대** | 배포 |
| 그중 상세 받은 것 `detailed` | 365대 | **413대** | 배포 |
| 아직 못 받은 것 `missing` | 548대 | **266대** | 배포 |
| 묶음 머리글 | 안 보임 (한 덩어리) | **묶음마다 보임** | 배포 |
| 신차출고가 — 전체 | 5건 | **9,204건** | DB |
| 신차출고가 — GV70 2.5T | 5건 | **284건** | DB |
| 골격/교환/판금 — 전체 | 70건 | **7,417건** | DB |
| 골격 — GV70 2.5T | 70건 | 70건 (그대로) | DB · ★ 아래 5번 |
| `app.css?v=` | `1ecdbc5297` | **`28581ca5c3`** | `curl -s 배포/ \| grep app.css` |
| 검사 S46-302 | 통과 (★ 헛잡이) | **통과 (진짜)** | `v0_guide` |
| 너비 넷 390·600·760·900 | — | 가려진 0 · 겹침 0 · 가로넘침 0 | 브라우저 실측 |

지금 묶음 — ① 다 맞음 **0** · ② 미조회 있음 **0** · ③ 하나 아쉬움 **15** · ④ 못 미침 **398**

## 3. 무엇을 했나 (고친 파일 · 줄)

### M-18 — 옛 잣대가 **두 곳**에 글자로 박혀 있었다
- `report/screens/tabs.py` — `"rule"` 열두 줄을 지우고 `_t4_rule(sift)` 로.
  `_탭4_거름` 에서만 그린다.
- `report/screens/tabs.py` — ★ **부제 줄에도** 박혀 있었다.
  「감가 78% 이하 · 2022년 5월↑ · 8만km 이하」 → 같은 `_t4_rule(sift)` 로.
- 배포 띠 = `2023년 이후 · 2.5만km 이하 · 3,999만 이하 · 흰색 ·
  옵션값 400만 이상 · 골격 무사고 · 리스·렌트 승계 제외`
- 커밋 `be7d6b63` · `fd1f931c`

### M-16 — 질의가 아직 옛 `tab4_gv70` 을 보고 있었다
- `report/screens/tabs.py` 의 `WHERE` 가 `cfg["year_from"]` · `cfg["mileage_max_km"]`
  를 쓰고 있었다.  그래서 **「913대가 넘었다」가 옛 잣대의 수**였다.
- `_탭4_거름` 의 `차값_최대_원` 으로 바꿨다.
- ★ 연식·주행은 **질의에서 안 자른다** — 자르면 묶음 ③ (「하나가 아쉽다」)이
  아예 못 생긴다.  묶음에서 가른다.
- 커밋 `be7d6b63`

### M-17 — 줄세우기는 맞았는데 **화면이 한 덩어리로** 그렸다
- 줄세우기는 이미 묶음별이었다 (`packs[1]+packs[2]+packs[3]`, 각 묶음 안에서만 총비용순).
- 그런데 틀 `web/templates/gv70.html` 이 「후보 · N대 · 총비용 낮은 순」 하나로 묶었다.
  ★ **그래서 마스터 눈에 「1위가 3,950만 미조회」로 보였다 — 그것은 ② 의 1위였다.**
- 묶음 첫 장에만 `group_head` · `group_note` 를 얹는다 (틀이 `==` 를 못 쓴다).
- 같이 — 「값·연식·주행을 넘었고」 → 「차값 상한을 넘었고」.
  M-16 뒤로 질의가 연식·주행을 안 자르므로 옛 문장은 **사실이 아니게 됐다**.
- 커밋 `b2a9befd`

### S-1 — CSS 지문이 서버 뜰 때 한 번만 재고 영영 기억했다
- `web/app.py` `static_version()` 이 `{이름: 지문}` 을 캐시했다.
  `app.css` 를 고쳐도 `v=` 가 그대로라 브라우저가 옛 CSS 를 썼다.
- `{이름: (mtime, 지문)}` 으로 바꿔 mtime 이 바뀌면 다시 잰다.
- ★ **실측** — 재시작 없이 `app.css` 내용을 고치자 `28581ca5c3` → `ea02f35152`,
  되돌리자 `28581ca5c3`.  따라온다.
- 배포 확인 — `curl -s 배포/ | grep app.css` → `app.css?v=28581ca5c3`
  (지시서의 `1ecdbc5297` 이 아니다)
- 커밋 `be7d6b63`

### S46-302 가 헛잡이였다 — ★ 스스로 잡아 고쳤다
- 검사가 `web/templates/*.html` 만 훑었는데 옛 문구는
  `report/screens/tabs.py` 에 있었다.  **검사는 파랬고 화면엔 옛 띠가 떴다.**
- `report/screens/*.py` 도 본다 (주석 줄은 뺀다 — 까닭을 적자면 옛 문구를 써야 한다).
- 실패 → 통과: 「옛 거르개 문구가 없다 (틀·화면코드 55개)」
- 커밋 `9bf08236`

## 4. 왜 미조회가 그렇게 많았나 — 마스터 물음에 대한 답

★ **도구는 있었는데 `--write` 를 안 돌린 것**이 절반이다.

- `tools/fill_origin_total.py` — 8,053건을 채울 수 있었는데 DB 엔 **5건**뿐이었다.
  돌렸다 → **9,204건** (GV70 2.5T 는 5 → **284**).
- `tools/fill_accident_parts.py` — 7,370건을 채울 수 있었는데 DB 엔 **70건**뿐.
  돌렸다 → **7,417건**.

## 5. 못 한 것 · 막힌 것

- **GV70 골격은 70 그대로다.**  파싱이 아니라 **수집이 없다**.
  GV70 2.5T 상세 709건 중 성능점검이 붙은 것은 **53건**이다.
  엔카 성능점검은 서버에서 부르면 **407** 이고 (규격에 08-23 부터 있다),
  KB 는 지금 막혀 있다.  ★ 「없다」가 아니라 「못 받았다」로 둔다.
- **R-4 KB `status='new'` 356건** — 질의는 준비돼 있으나 KB 가 막혀 있다.
- 지금 화면에 뜨는 15대 중 14대가 「신차가 미조회」다 — 그것들은
  엔카 등급기준가가 없는 매물이다 (전체 6,667건이 등급기준가가 없다).

## 6. 여쭙는 것 (규칙 2 — 고치지 않고 적는다)

1. **`gv70.html` 을 제가 고쳤습니다.**  묶음 머리글을 낼 자리가 틀에 없었습니다
   (시안 `v4m_gv70_시안.html` 은 09-07 것이라 M-17 보다 앞섭니다).
   `group_head` · `group_note` 라는 이름이 괜찮은지, 아니면 시안을 새로 주실지
   여쭙습니다.
2. **「값·연식·주행을 넘었고」를 「차값 상한을 넘었고」로 바꿨습니다.**
   M-16 뒤로 질의가 연식·주행을 안 자르니 옛 문장이 사실과 달라서입니다.
   문구를 달리 쓰실 것이면 말씀해 주십시오.
3. **`bobaedream` 의 트림** — 사이트가 안 줍니다.  「사이트가 안 주는 곳」으로
   둘까요, 아니면 다른 데서 메울까요.
4. **엔카 목록이 09-01 부터 407** 입니다.  집에서 받아 넣는 길 말고
   다른 길을 아시는지요.
