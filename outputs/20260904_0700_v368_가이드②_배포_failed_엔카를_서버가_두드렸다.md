# 가이드 ② — 판을 배포에서 돌린다 · ★ `failed` 의 뿌리를 찾았다

일시 2026-09-03 · 소스 v368 · 지시 `outputs/ORDER_20260829.md` r1101 ②

## 1. 무엇을 하려 했나 (지시 원문)

> | **②** | ★ **판을 배포에서 돌린다** | ★ 끝 = `/admin/status` 에 `failed` 없음 |

## 2. 1차 결과 (고치기 전 숫자)

`recalc_job` 을 열었다 — **다섯 판이 잇달아 `failed`** 였고 까닭이 하나였다.

```
358c7a0c  trigger=schedule  from_step=S5  failed
  detail: S5: ⑥ not_requested 48405건 — 미완성 매물이 남았다
351f6611  … not_requested 48444건
11331c14  … not_requested 48413건
066b49d6  … not_requested 48437건
a53ded0e  … not_requested 48441건
254833d0  … running (06:04 부터 멈춰 있다)
```

## 3. 무엇을 했나 — ★ 판이 무엇을 두드렸는지 셌다

```
run 20260903T055124 의 audit_request
  ('encar', 'detail',             'error', 5809)
  ('encar', 'inspection',         'error',  289)
  ('encar', 'platform_check',     'error',    9)
  ('encar', 'inspection_summary', 'error',    7)
  ('encar', 'record',             'error',    7)
  ('encar', 'record_summary',     'error',    7)
  ('encar', 'sellingpoint',       'error',    7)
  ('encar', 'ev_battery',         'error',    6)
  ('encar', 'record',             'ok',       2)
  ('encar', 'detail',             'ok',       1)
  합 6,153건 — ★ 거의 전량이 error 다
```

### ★★ 뿌리 — ★ **배포가 규격이 금지한 자리를 두드리고 있었다**

`docs/ENCAR_ROBOTS.md` 「남기는 사실」③ (★ **08-23** 부터 · 요구 추적표 85번) —

```
③ ★ 서버에서 부르면 ★ 407 이다 — ★ 마스터 회선(/admin/collect)으로 받는다
필수  ★ 마스터 회선으로만 받는다.  ★ 서버에서 자동으로 두드리지 않는다
```

★ 예약 판(`trigger='schedule'` · 하루치 `daily_enqueue`)이 **서버에서** 돌면서
엔카를 **6,153번** 두드렸다. 전부 **407**이다. 그래서 아무것도 못 받고
「⑥ not_requested 48,405건」이 남아 **판이 죽었다.**

★ **규격은 11일 전부터 그렇게 적혀 있었고, 코드가 한 번도 안 지켰다.**
규칙 1 — 코드가 규격과 다르면 **코드를 고친다.**

## 4. 고친 곳

| 파일 | 줄 | 무엇 |
|---|--:|---|
| `config/sites.json` | `encar` | ★ `master_line_only: true` ＋ 까닭을 적은 `_master_line_note` |
| `collect/runner.py` | 395-412 | ★ `_master_line_only(root, site)` — ★ **이름을 코드에 박지 않는다** (`S14`) |
| `collect/runner.py` | 442 | `_blocked = 마스터회선전용 and not master_line` |
| `collect/runner.py` | 505·570·813 | ★ S1·S2·S5 가 그 표시를 본다 — ★ 「할 일이 없었다」로 낸다 |
| `run.py` | 366-375 | ★ 소비기는 `master_line=False` — ★ **소비기는 마스터 회선이 아니다** |

★ 「못 받았다」로 내지 않는다 — **안 두드리기로 한 것**이다.
`expected 0 · requested 0` 이면 `halt_if` 가 「처리할 것이 없었다」로 돌려준다
(`collect/pipeline.py:218`). 그래서 **판이 죽지 않는다.**

★ 마스터가 `/admin/collect` 로 누르는 길은 **이 길이 아니다** —
`run.py:250 _collect_urls()` 가 **브라우저가 부를 주소**를 만든다.
곧 마스터 회선은 그대로 살아 있다.

## 5. 조치 후 결과 — ★ **배포에서 확인했다**

`sudo systemctl restart carwatch` 전에 마스터가 쓰는지 먼저 봤다 —
`journalctl … | grep POST` 가 **0건**이었다.
그런데 「멈춘 줄 알았던」 `running` 판이 **그때도 엔카를 두드리고 있었다** —
`S5 27,405/136,431 매물 42001641 detail`. 재시작이 그 폭주를 멈췄다.

```
systemctl is-active carwatch caddy   →  active · active
ss -tln | grep 8765                  →  127.0.0.1:8765
```

### ★ 재시작 뒤 새 판 (`290327d5b6cdd565`)

```
17:32  S5 를 건너뛴다 — 「encar 는 마스터 회선으로만 받는다」 (기록에 한 줄)
17:32~17:48  S6 detail 파싱 —  1,760 → 142,986
17:48  판이 끝났다
```

```
job 290327d5b6cdd565  status = done
detail = S5 ok 0 · S6 ok 142,986 · S9 ok 441,028 · S10 ok 15,751
```

★ **`failed` 가 아니라 `done` 이다.  이 판이 처음이다.**
★ `S5 ok 0` 은 「못 받았다」가 아니라 **「안 두드리기로 한 것」**이다.
★ 그 대신 **S6·S9·S10 이 처음으로 끝까지 돌았다** — 전에는 S5 에서 죽어
  파싱·채점까지 가 본 적이 없다.

### ★ 무엇으로 쟀는가 — ★ **배포의 `/admin/status`** (`secrets/check_login.json` 으로 로그인)

```
② 방금 끝난 것
  290327d5b6cdd565  done    raw_missing · all   1분 전
                    S5 ok 0 · S6 ok 142986 · S9 ok 441028 · S10 ok 15751
  7b5e24bfdcf7fe8f  failed  … 22분 전   ← 고치기 전
  4f464ae31a13d2db  failed  … 28분 전   ← 고치기 전
  5877da94dd439082  failed  … 34분 전   ← 고치기 전
  c2fed0d25813b6ae  failed  … 59분 전   ← 고치기 전
```

```
S46-241  →  (True, '★ 배포에서 마지막 판이 살아 있다 — 290327d5 done
                    (그 앞의 옛 실패 4건은 이력이다)')
```

★ `S46-241` 도 함께 고쳤다. 이 검사의 잣대는 제 글로 「**마지막** 재판정 판이
`failed` 인가」인데, 코드는 **화면 전체**에서 `failed` 를 세고 있었다.
옛 실패는 **이력**이라 지워지지 않으므로, 뿌리를 고쳐도 검사가 **영영 빨갛다** —
그러면 아무도 안 보게 된다. 맨 앞 판으로 가르고, **남은 옛 실패는 세어서 함께 낸다**
(감추지 않는다).

## 6. 못 한 것 · 판단이 필요한 것

### ㉮ ★ 06:04 부터 `running` 인 판 하나가 멈춰 있다

`254833d0b3466c5a` 가 **9시간째** `running` 이다. 관리 화면이 409 로 잠기는
그 자리다(개요의 옛 사고). `collect/worker.py:37 reclaim_stale()` 이 있는데
왜 안 걷어갔는지는 **아직 안 봤다** — 다음 걸음이다.

### ㉯ ★ 가이드께 — ★ 엔카 상세를 앞으로 **어떻게 받는가**

지금 엔카 상세는 9,271/9,704 = **95.5%** 로 이미 목표를 넘겼다.
남은 433건은 **마스터가 `/admin/collect` 를 눌러야** 받는다.
서버가 대신 받을 길은 규격이 막았다 (요구 추적표 85번).

★ 이것이 규격대로라면 ② 의 「끝」은 **엔카를 빼고** 재야 맞다.
아니라면 요구 추적표 85번을 바꿔 주시기를 청한다 — 규칙 2 대로
`docs/` 는 안 고쳤다.
