# S3 차종 가르기 · 0-1·0-2 적재 DB — ★ 살아 있는 매물의 「차종 미정」 583 → 0

```
지시문   SPEC-2026.09.03-r1124 · 0-1·0-2·0-4 · 5절 S3
검산     S46-265 · S46-258 · S46-266 · S46-267
날짜     2026-09-04 01:50 (KST)
```

## 1. 무엇을 하려 했나 (지시 원문)

```
지시문 S3 — 「★ `target_key_of` 가 ★ `trim_contains`·`year_from`·`_갈래` 를 읽게.
            ★ 값은 **가이드가 다 넣었다** · ★ `hood` 를 `HD_PART` 에 | 끝 = `part` pending 0」
지시문 0-1·0-2 — 「★ 파일을 ★ **적재 DB(`tmp/load-{run_id}.db`)** 로 옮긴다 ·
            ★ 적재 DB 에서 파싱해 ★ 제 표에 넣는다 · ★ 적재 DB 를 치운다」
```

## 2. 1차 결과 (고치기 전)

```
`part` pending                     ★ **9건** (헤이딜러 부위명 · `hood` 포함)
헤이딜러 `state.outer` 축           99 / 294
살아있는데 차종 미정                 ★ **583건** (리본카 382 · 헤이딜러 123 · 볼보 34 · KB 13 …)
`target_map.json` 의 by_site        ★ 볼보셀렉트 · 헤이딜러 · 리본카 ★ **0개**
`load_raw.py` 가 원문을 넣는 곳       ★ **본 DB 의 `raw_response`**
`S6 ok`                            ★ **0** — 원문 표를 비운 뒤 파싱이 굶었다
```

## 3. 무엇을 했나 · 왜

### 3-1. `trim_contains`·`year_from` — ★ 이미 돼 있었다

`store/dictionary.py:target_key_of` 가 ★ 둘 다 읽고 있다 (앞 회차가 넣었다).
★ `_갈래` 는 ★ **config 에도 코드에도 없다** — ★ 6절에 여쭐 것으로 적었다.

### 3-2. `hood` — ★ 코드는 이미 있었고 ★ **사전이 안 풀렸다**

`parse/heydealer/mapping.py:290` 에 `"hood": HD_OUTER` 가 이미 있고
`config/dictionaries/fixed_enums.json` 에 열한 부위가 다 있다.
★ 그런데 `dict_enum` 은 ★ 9건이 ★ `pending` 그대로였다.

```
까닭   `store/dictionary.py:seed_fixed_enums` 가 ★ **행이 있으면 그냥 건너뛴다.**
      ★ 그래서 ★ 고정 집합이 ★ **뒤에 정해진** 축은 ★ 먼저 들어온 행이 ★ 영영 pending 이다
고침   고정 집합에 든 값이면 ★ `confirmed` 로 올린다 —
      ★ 지어내는 것이 아니다.  ★ 값이 ★ `fixed_enums.json` **안에** 있다
```

### 3-3. 차종 미정 583건 — ★ **정본은 있었고 코드가 안 읽었다**

```
`target_by_rules` 는 ★ `target_map.json` 의 `by_site` 만 본다.
  ★ 볼보셀렉트 · 헤이딜러 · 리본카는 ★ 거기 ★ **항목이 하나도 없다** → 영영 미정
★★ 그런데 ★ 이름은 ★ **`config/targets.json` 에 이미 있다** —
  ★ 볼보   `site_query.volvo_selekt.slug`      → `xc60` · `s60` · `xc40` · `v60-cross-country` · `ex30`
  ★ 헤이딜러 `site_query.heydealer[]._차명`      → 「테슬라 모델 Y 주니퍼」 · 「벤츠 GLC클래스 (X254)」 …
```

★ `store/dictionary.py` 에 `_with_site_query()` 를 더해 ★ **정본을 읽게** 했다.
★ ★ **지어내지 않는다.**  ★ 그리고 ★ 한 이름을 두 차종이 다투면 ★ **버린다** —
★ ★ ★ 볼보 `xc40` 은 `XC40_IMPORT` ↔ `XC40_EV` 가 다툰다 (config 주석이 「본문 「순수
전기」로 가른다」라 적어 두었다).  ★ 「아마 이것일 것」으로 안 넘긴다 (금지 6).

### 3-4. 0-1·0-2·0-4 — ★ 적재 DB

`tools/load_raw.py` 가 ★ 본 DB 의 `raw_response` 대신 ★ `tmp/load-{run_id}.db` 에 넣고
★ 파싱은 그대로 `core_*` 로 가고 ★ 판이 끝나면 ★ 적재 DB 를 지운다 (`--keep-load-db` 로 남길 수 있다).

## 4. ★★ 내가 낸 흠 셋 — ★ 그대로 적는다

`tools/sync_target_map.py --apply` 를 돌렸는데 ★ 그 도구에 흠이 셋 있었다.

| # | 흠 | 실측 | 막은 자리 |
|:--:|---|--:|---|
| ① | `--dry` 인데 ★ **실제로 고쳤다** | 4,151건에 차종을 붙였다 | `main()` 에 `--dry` 관문 |
| ② | ★ `target_key` 가 **있는** 행까지 `out_of_scope` 로 내렸다 | ★ **4,122건** | else 줄에 `AND target_key IS NULL` |
| ③ | ★ **`gone` 을 되살렸다** — 철학 ② 를 물렸다 | ★ **2,036건** | if 줄에 `AND status <> 'gone'` |

★ ②는 ★ 「우리 차종 열쇠가 붙었는데 ★ 남의 차」라는 ★ **모순**이다
(스포티지 682 · GLC 573 · 그랜저 512 · G80 407 · X3 354 · 모델Y 268 …).
★ ★ `tools/undo_wrong_out_of_scope.py` 를 새로 만들어 ★ **4,122건을 되돌렸다** —
★ ★ ★ 점수가 있는 것만 (점수는 `active` 였다는 자취다).  ★ 점수 없는 181건은 안 건드렸다.
★ ③은 ★ `tools/sweep_sold.py --write` 를 다시 돌려 ★ **2,152건**을 도로 내렸다.

★ 되돌린 것은 ★ `core_listing_change` 에 ★ 까닭과 함께 남겼다 — ★ 조용히 안 고쳤다.

## 5. 조치 후 결과 (실측)

```
`part` pending                 9 → ★ **0**   (남은 pending 은 `target` 184 — 차종 이름 · 마스터 몫)
헤이딜러 `state.outer` 축        99 → ★ **114 / 114 (100%)**
살아있는데 차종 미정             583 → ★ **0**  (열 사이트 전건에 차종이 붙었다)
status                        active 6,429 · gone 11,226 · out_of_scope 8,956
  active 중 점수 있음            ★ 5,249
S6 파싱 (헤이딜러 한 판)         파일 1,296개 → ★ 상세 404건 · 이력 404 · 판 404
적재 DB                        `tmp/load-20260903T163830.db` — ★ 쓰고 ★ **치웠다** (522행)
본 DB 의 몸통 있는 행            ★ **0** (그대로) · DB ★ 0.456 GiB
S46-265 · S46-266 · S46-267    ★ 셋 다 통과
```

★ **「끝」은 배포에서** — ★ 크로미움으로 열어 쟀다

```
/recommend   카드 60 · ★ 전체 **1,302건**
   BMW X3 366 · ★ 볼보 XC60 295 · 테슬라 모델Y 229 · ★ 볼보 XC40 161 ·
   그랑 콜레오스 118 · ★ 볼보 S60 73 · GV70 32 · 폴스타4 20 · XC40리차지 5 · C40 3
   흐림(0건) — iX3 · ID.4 · ID.5 · EV4
/listings    카드 30 · 「이 조건에 짝지어진 차 **6대** — 짝지어진 것만 보기 ▸」
```

## 6. 못 한 것 · ★ 판단이 필요한 것

```
① ★ 여쭐 것 — ★ 지시문 S3 의 ★ `_갈래` 가 ★ **config 에도 코드에도 없다.**
   ★ 내가 보는 안 둘 — ★ ㉮ `trim_contains` 가 이미 그 일을 한다 (그러면 다 된 것이다) ·
   ★ ★ ㉯ 아직 안 넣으신 새 칸이다 (그러면 값을 주시면 코드를 붙인다).
   ★ 어느 쪽이든 진행한 것 — ★ ㉮ 로 보고 넘어갔다
② 아직 점수 없는 `active` ★ **1,180건** — ★ 엔카다.  ★ 파싱은 됐다.  ★ 다음 회차에 판다
③ ★ 「차종 붙은 out_of_scope」 ★ **181건** — ★ 점수가 없어 ★ 안 건드렸다.
   ★ 되살릴지 ★ 진짜 남의 차인지 ★ 못 가른다 — ★ 여쭙니다
④ S1 (KB 90%) · S6 (`run.py:113` 이 `EncarAdapter` 로 못 박힘) · S7·S8 · S11 — ★ 아직이다
⑤ `check_all` 전체 — ★ 타이머를 세울 권한이 막혀 ★ 겹치면 위험해 안 돌렸다.
   ★ 단계별로는 쟀다 — V7 39MB · V10 52MB (전에는 V7 하나가 1,194MB)
```
