# v334 — ★★★ 색인이 통째로 사라져 화면이 멈춰 있었다

일시 2026-09-03 18:00 · 앞 회차 v333

---

## 1. ★★★★★ 무엇이 났나

```
★ 앞 회차에 ★ 「`/recommend` 가 90초에도 안 뜬다」고 적고
★ ★ 「검사 세 판이 DB 를 물어서인지 ★ 화면 탓인지 ★ **못 갈랐다**」고 했다.
★★★ 이번에 ★ 아무것도 안 도는 상태에서 다시 쟀다 —
   ★ ★ **첫 화면조차 10분에 안 왔다.**  ★ 화면 탓이었다
```

## 2. ★ 뿌리 — **내가 냈다**

```
★ 앞 회차에 ★ `vin_hash` 칸을 더하려고 ★ `run.py migrate` 를 돌렸다.
★ ★ `tools/migrate.py` 의 `rebuild_to_ddl` 이 ★ 표를 DDL 대로 다시 만든다 —
★ ★ ★ `DROP TABLE` ＋ `ALTER TABLE … RENAME` 을 한다.
★★★★ ★ **SQLite 는 표를 지우면 ★ 그 표의 색인도 함께 지운다.**
   ★ ★ 그런데 ★ 마이그레이션이 ★ **색인을 다시 안 걸었다**
```

★ 실측 09-03 — ★ `core_listing` 색인이 ★ **여섯 → 하나**(자동 것 하나)로 줄어 있었다.

★★ 그러자 ★ 목록 거르개의 ★ 「같은 차가 여러 곳에 있으면 **싼 것 하나만**」 하위 질의가

```sql
l.listing_id = (SELECT l2.listing_id FROM core_listing l2
                 WHERE l2.plate_hash = l.plate_hash AND l2.status='active'
                 ORDER BY l2.price_current_won, l2.listing_id LIMIT 1)
```

★ 매물마다 ★ **20,684행을 통째로 훑었다** — ★ 20,684 × 20,684 이다.

## 3. 고친 것

| 파일 | 무엇 |
|---|---|
| `tools/migrate.py` | ★ 표를 다시 만든 뒤 ★ **DDL 을 한 번 더 돌려 색인을 되건다** |
| `validate/v0_guide.py` `S46-238` | ★ **새 검사** — DDL 색인이 DB 에 다 있는가 |

★ `CREATE INDEX IF NOT EXISTS` 라 ★ 있는 것은 안 건드린다.

### ★ 일부러 깨서 잡는 것을 봤다 (명령서가 시킨 것)

```
★ `ix_listing_plate` 를 지우고 검사를 돌렸다
★ ★ → ★ **「DDL 이 말한 색인 23개 중 1개가 DB 에 없다 —
     ix_listing_plate(core_listing)  ★ 화면이 통째로 느려진다」**
★ ★ ★ 되돌린 뒤 → ★ 통과
```

---

## 4. 「몇 → 몇」 [실측 09-03]

| 화면 | 전 | 후 |
|---|--:|--:|
| `/` 현황 | ★ **10분에도 안 옴** | ★ **3.7초** |
| `/listings` 매물 | 〃 | ★ **0.71초** |
| `/recommend` 추천 | 〃 | ★ **0.16초** |
| `/track` 추적 | 〃 | ★ **0.32초** |

| 잣대 | 전 | 후 |
|---|--:|--:|
| `core_listing` 색인 | **1개** | ★ **6개** |
| DDL 이 말한 색인 중 DB 에 있는 것 | 18 / 23 | ★ **23 / 23** |

## 5. ★ 곁들여 — 타이머 판에 시간 상한

```
★ 이번에 ★ `light_check`(타이머가 부르는 판)가 ★ **34분째 물려** 있었다.
★ ★ 예산이 ★ 「180초」라 글로 적혀 있는데 ★ **시간 상한이 없었다** —
★ ★ ★ `check_all` 이 막히면 ★ 타이머 판이 ★ **영영 안 끝난다**.
★ ★ ★ ★ 그동안 ★ DB 를 물어 ★ 마스터 화면까지 함께 느려진다.
★★ `tools/light_check.py` 에 ★ 걸음마다 ★ **900초 상한**을 걸었다 —
  ★ ★ 넘으면 ★ 그 걸음만 접고 ★ 남은 걸음을 잇는다.
  ★ ★ ★ 조용히 넘기지 않는다 — ★ 「몇 초에 끊었다」를 ★ 글로 남긴다
```

## 6. ★ 내가 배운 것

```
★ 앞 회차에 ★ 「화면이 느린지 ★ 검사 탓인지 ★ 못 갈랐다」고 적고 넘어갔다.
★ ★ **그것이 틀렸다** — ★ 그때 이미 ★ 마스터 화면이 죽어 있었다.
★ ★ ★ 「못 갈랐다」로 두지 말고 ★ **그 자리에서 갈랐어야 했다**.
★★ 「고쳤습니다」만큼이나 ★ 「못 갈랐습니다」도 ★ 위험하다 —
  ★ ★ 둘 다 ★ **숫자를 안 낸 것**이다
```
