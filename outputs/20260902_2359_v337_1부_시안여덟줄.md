# v337 — 1부 시안 여덟 줄을 다 닫았다

명령서 `outputs/ORDER_20260829.md` (SPEC-2026.09.02-r1098 · 새로 씀)
「★ 시안대로 먼저 작업하고 ★ STEP 1부터 다시 하라」 — 마스터 09-02

## 1차 결과 (고치기 전)

```
통과 496 · fatal 11 · warn 13 · 환경 차이 2 · 미실행 9
```

여덟 줄을 **먼저 다 재고** 손댔다. 「없다」고 적기 전에 두드렸다 (지키는 법 ⑤).

| # | 잰 값 | 무엇이었나 |
|:--:|---|---|
| 1-1 | 목록 배지 **0** · `/track` 30 | ★ **틀은 멀쩡했다** — 아래 |
| 1-2 | 목록·추적 88/104/180/240 · **추천만 104 고정** | 규칙 하나가 덮고 있었다 |
| 1-3 | 순위 30개 | ★ 이미 됐다 (앞 회차) |
| 1-4 | 사진 빈 자리에 「사진 없음」 | 까닭을 안 낸다 |
| 1-5 | 단추 **12** / 매물 있는 사이트 **12** | ★ 이미 됐다 |
| 1-6 | 세 차례 90행 중 판매완료 **0** | ★ 이미 됐다 |
| 1-7 | 합 910 / 분모 910 | 막이가 **걸릴 수 없는 자리**에 있었다 |
| 1-8 | 표 미분류 **333** / 파일 미분류 **0** | 저장이 표에 안 닿았다 |

---

## 1-1 「N곳」 배지 — **8회차째** · 틀은 멀쩡했다

★ 여덟 회차 동안 「배지가 0개다」라고 적혔다. **재 보니 틀은 맞았다** —

```
/listings                 배지 0개   ← 기본 쪽 (차례 = 순위)
/listings?order=price     배지 3개   ← ★ 난다
```

★ 까닭 — **같은 차가 여러 곳에 있는 매물은 순위가 낮다.**
화면에 나오는 5,522행 중 배지 대상은 **154행(2.8%)** 이라
앞 30행에 하나도 안 들어온다. **틀이 아니라 차례의 문제다.**

★★ 그런데 **진짜 어긋남을 하나 찾았다** —

```
목록 화면   같은 차 = ★ **번호판**만
/track     같은 차 = ★ **번호판 ＋ 차대** (union-find)
★ 같은 「같은 차」를 ★ **두 자로 재고 있었다**
```

이 판이 막으려는 것이 바로 그것이다(「선언과 실제의 괴리」·`docs/guide/00_개요.md`).
화면도 차대를 보게 했다.

| | 앞 | 뒤 |
|---|--:|--:|
| 배지가 붙는 행 (화면 5,522행 중) | 154 | **161** |

★ `/track` 의 **176대**와는 아직 다르다 — union-find 는
「A—번호판—B—차대—C」까지 잇는데 SQL 한 판으로는 못 잇는다.
**그 차이 15대는 지어내 메우지 않았다.** 아래 물음 23.

## 1-2 사진 104×78 고정 — **7회차째** · 추천만 그랬다

브라우저로 다섯 폭을 실측했다 —

```
        360px      900px       1200px
목록    104x78  →  180x135  →  240x180     ★ 커진다
추적    104x78  →  180x135  →  240x180     ★ 커진다
추천    104x78  →  104x78   →  104x78      ★ ★ 안 커진다
```

★ 까닭 — 추천 카드는 `<div class="desk pickthumb">` 다.
폭 규칙은 `.pickthumb`(클래스 하나)인데
`.row .desk`(클래스 **둘**)가 **더 세서** 104px 로 되돌렸다.
폭 규칙에 `.row .desk` 를 함께 적었다.

```
뒤   추천  360:104x78  900:180x135  1200:240x180   ★ 같아졌다
```

## 1-4 「왜 없나」를 글로 (`RULES.md` 2)

★ 사진이 없는 매물을 **갈래로 세어** 봤다 —

```
현대인증 174 · 헤이딜러 92 · 보배드림 47 · BMW BPS 9 = ★ 322건
   → ★ 그 사이트에서 ★ **한 장도 못 받았다** (우리 잘못)
엔카 28건
   → ★ 9,319장을 받고 있다.  ★ **그 매물에 사진이 없다** (사실)
```

둘을 가르는 글을 낸다 — `store.core.photo_ready_sites()` 로 잰다.

```
확인   /listings?site=bobaedream   「보배드림은 사진을 아직 못 받았습니다」 60
      /listings?site=heydealer    「헤이딜러는 사진을 아직 못 받았습니다」 60
      /listings?site=encar        0  (사진이 다 있다 — 쓸데없는 말을 안 낸다)
```

★ 조사도 맞췄다 — 「보배드림**는**」이 아니라 「보배드림**은**」이다
(받침 셈 `(ord(글자) − 0xAC00) % 28`).
★ 목록·관심·상세 세 틀에 넣었다.

## 1-7 배점 911 저장 — **5회차째** · 막이가 걸릴 수 없었다

`/admin/scoring` 의 문서에는 **「합계가 total_points 와 다르면 저장할 수 없다」**
고 적혀 있었다. 코드는 반대였다 —

```python
change = apply_config(..., also={"total_points": total_of(new)})
                                  ★ ★ 총점을 ★ **합에 맞춰 올린다**
```

★ `_validate_blob` 의 막이는 `Σ > total_points` 인데
★ **늘 같은 수를 견주고 있었다.** 그래서 911 이 그냥 저장됐다.

★ 총점을 안 건드리게 하고, 넘으면 **무엇을 왜 안 했는지 수로** 낸다.

```
실측 (처리기를 직접 눌렀다)
   선루프 8 → 11 (합 913)   →  ★ 저장 안 됨.  파일 910/910 그대로
   선루프 8 →  7 (합 909)   →  ★ 저장 됨.    파일 909/910
   되돌림                    →  910/910
```

★ 합이 **작은** 것은 그대로 둔다 — 「닿을 수 없는 자리」다 (08-30 확정).

## 1-8 등록부 303 — **5회차째** · 파일만 고치고 표를 안 고쳤다

```
config/field_usage.json   미분류  ★ 0건    ← 저장이 여기에만 썼다
meta_field_usage (표)     미분류  ★ 333건  ← ★ **화면은 여기를 센다**
                                            (report/screens/admin.py:193)
```

★ 그래서 눌러도 **333 → 333** 이었다. `303` 은 왔는데 수가 안 움직였다.

★ 파일과 **표를 함께** 고치게 했다. 표는 사이트마다 한 행이라
같은 `(endpoint, json_path)` 를 다 고친다 (`sync_registry` 도 그렇게 편다).

★★ 그리고 **저장 뒤 다시 읽어** 수를 낸다 (지키는 법 ③) —

```
「분류를 저장했습니다 — 미분류 333 → 332건」
실측  core:accident_my_cnt 를 unused_by_policy 로  →  ★ 333 → 332
```

## 못 한 것 · 판단이 필요한 것

| | 무엇 |
|---|---|
| **물음 23** | 목록의 「N곳」과 `/track` 의 짝짓기가 **아직 15대 다릅니다**(161 vs 176). SQL 로는 「A—번호판—B—차대—C」를 못 잇습니다. `core_listing` 에 `pair_key` 칸을 두고 판정 때 한 번 채울까요? 그러면 두 화면이 **한 자**를 씁니다 |
| **물음 24** | 1-1 은 틀이 아니라 **차례** 문제입니다. 같은 차가 여러 곳에 있는 매물은 순위가 낮아 앞 30행에 안 옵니다. 「여러 곳에 있는 차를 앞에 낸다」는 차례를 넣을까요? 배점·차례는 규격이라 제가 안 정했습니다 |
| 물음 20·22 | 앞 회차 것 (`S46-98` 남은 여섯 낱말 · `S46-100` 절 차례) |


---

# 2부 — STEP 1 부터

## S1 을 재다 — ★ 「저기 몇 대 / 우리 몇 대」

```
  사이트                   저기      우리      비율
  bmw_bps              130     382  293.8%
  bobaedream         3,286     241    7.3%
  heydealer            254     275  108.3%
  hyundai_cert       1,163   1,156   99.4%
  kbchachacha        6,261     592    9.5%
  kcar                 461     604  131.0%
  kia_cpo              964   1,025  106.3%
  lexus_certified       63      64  101.6%
  reborncar          1,084   1,111  102.5%
  revolt               281      57   20.3%
  volvo_selekt         237     160   67.5%
```

## ★★ 그런데 ★ **자가 틀려 있었다** (`S46-247`)

★ 비율이 100% 를 넘는 곳이 넷이다 — ★ 그것부터 이상하다.
★ `outputs/site_coverage.json` 을 열어 보니 **두 갈래**였다 —

```
차종별로 잰 곳   kbchachacha 29종 → 6,261   ← ★ **우리 차종**의 수다
전 재고만 잰 곳  bobaedream `_전체` 3,286   ← ★ ★ **그 사이트가 파는 전부**다
                volvo_selekt 237 · revolt 281 · kia_cpo 964 …  (10곳)
```

★★ 마스터 확정 08-23 — 「★ `targets.json` 의 **차종만 받는다.
★ 전량을 받지 않는다**」.
★ 그런데 검사는 ★ **우리 32종**을 ★ **그 사이트 전 재고**와 견주고 있었다.
★ ★ 보배드림은 ★ **우리 차종을 하나도 안 빠뜨려도 7%** 로 나온다.

★ 잣대를 고쳤다 — ★ 차종별로 잰 곳만 셈하고,
나머지는 **「차종별로 아직 안 쟀다」**로 낸다 (「괜찮다」로 넘기지 않는다).

```
앞   ★ 많이 못 받은 사이트 revolt 57/281 (20%) · kbchachacha 592/6,261 (9%) · bobaedream 241/3,286 (7%)
뒤   ★ 많이 못 받은 사이트 kbchachacha 592/6,261 (9%)
    ★ 차종별로 아직 안 잰 사이트 10곳 — revolt · kia_cpo · lexus_certified …
```

★★ **진짜 모자란 곳은 KB 하나**다.

## KB 를 파고들다 — ★ 조건은 35개인데 3종만 들어와 있었다

```
DB   KB 621건 · 차종 ★ **3종** (＋ 차종 미정 260)
config  site_query.kbchachacha 가 있는 차종 ★ 29종 · 쓸 수 있는 조건 ★ 35묶음
```

★ 수집기를 그냥 돌리면 ★ **조건을 안 쓴다** —

```
python3.11 tools/collect_kbchachacha.py --pages 2
   → 「목록 2쪽 · 매물번호 80건」   ★ `list_url(None, page=…)` — ★ 조건이 `None` 이다
★ 우리 차종만 받는 길은 ★ **`--narrow`** 하나다 (코드 주석에 그렇게 적혀 있다)
```

★ `--narrow` 를 돌렸다. ★ **한 번 잘렸고, 왜 잘렸는지 쟀다.**

```
앞   KB 619건 · 차종 3종
뒤   KB 627건 · 차종 3종   ← ★ 겨우 +8
```

★ 까닭을 팠다 — ★ **질의는 멀쩡하다.** 한 묶음을 손으로 훑어 봤다:

```
G80_25T (makerCode 189 · classCode 2684 · carCode 3077)
  1쪽 — id 40건 · 282,194B · 진짜끝 False
  2쪽 — id 40건 · 282,300B · 진짜끝 False
  …  8쪽까지 ★ **쪽마다 40건** · 끝 신호 없음        합 320건
★ 가이드가 센 1,801건 = 46쪽.  ★ 쪽은 얼마든지 넘어간다
```

★★ **막은 것은 시간이었다** — 수집기가 ★ **묶음 4개마다 300초를 쉰다**.
35묶음이면 ★ 쉬는 시간만 **45분**이다. 내가 3,000초로 끊어 4묶음에서 잘렸다.

```
로그   「★ 300초 쉰다 — 몫이 돌아오는 참이다 (4/4 묶음)」
      「★ 목록에 없어 gone 으로 매긴 것 0건 (끝까지 받은 묶음 34/35)」
```

★ 끊지 않고 다시 걸었다 (`nohup … --narrow`). ★ **뒤에서 돌고 있다.**
★ 끝나면 다음 회차 첫 줄에 「몇 → 몇」을 적는다.

★ ★ 마스터께서 그 사이 화면을 보실 수 있다 — ★ 다섯 화면 다 살아 있다:

```
/listings 200 1.64초 · /recommend 200 0.23초 · /track 200 1.68초 · /sold 200 0.41초
/watch 403  ← ★ 로그인이 있어야 보는 화면이다 (제 것만 보여 주므로)
carwatch·caddy  둘 다 active
```

## ★★ 내가 낸 사고 — 후보 차종 하나가 검사 42개를 죽였다

★ 앞 회차 끝에 ★ 「시험이 남긴 것」이라며 ★ `config/targets.json` 의
`GV70_25T` 를 **그대로 커밋했다.** ★ 그 항목의 `site_query` 가 ★ **글월**이었다
(다른 차종은 다 ★ 사이트별 **묶음**이다).

```
앞(그 항목이 든 채)   통과 ★ **454** · fatal 14 · 시험 실패 ★ **5파일**
   S46-137 · S46-77 · V1   AttributeError: 'str' object has no attribute 'items'
뒤(빼고)              통과 496 로 돌아옴
```

★★ 뺐다. 그리고 ★ **재발을 막았다** —
`/admin/targets` 로 후보를 넣으면 `site_query` 가 글월로 들어온다
(`tests/test_admin_flow.py:243` 이 그렇게 넣는다 · **사람도 그렇게 넣는다**).
검사 네 곳이 그때 `.items()` 를 불러 죽었다. **갈래가 안 정해진 후보는 건너뛴다** —
`load_filters` 가 이미 그렇게 하고 있었다.

★ 배운 것 — **「시험이 남긴 것」이라고 넘긴 것이 판을 죽였다.**
★ `git status` 에 뜬 것을 ★ **열어 보지 않고 커밋했다.** 다시는 안 한다.

## 물음 (이어서)

| | 무엇 |
|---|---|
| **물음 25** | 열 사이트가 ★ **차종별로 안 재져** 있습니다. `tools/site_coverage.py` 의 `TOTAL_FIELD` 갈래라 전 재고만 셉니다. S1 의 「90%」를 판정하려면 그 열 곳도 차종별로 재야 합니다 — 제가 각 사이트 검색 규격을 캐도 될까요, 아니면 가이드가 재 주시겠습니까? |
| 물음 23·24 | 위 (짝 열쇠 · 배지 차례) |


## 회차 끝 숫자

```
통과 503 · fatal 14 · warn 13 · 환경 차이 2 · 미실행 9
결과: 실패 — test_admin_flow.py, test_endtoend.py, test_integration.py
```

| | 회차 처음 | 회차 끝 |
|---|--:|--:|
| `check_all` 통과 | 496 | **503** |
| fatal | 11 | 14 |
| 1부 여덟 줄 | 5 남음 | **0** |
| 「N곳」 배지가 붙는 행 | 154 | **161** |
| 추천 사진 (1200px) | 104×78 | **240×180** |
| 사진 빈 자리에 까닭 | 없음 | **322건에 낸다** |
| 배점 911 저장 | ★ 된다 | **막힌다** (909 는 저장됨) |
| 등록부 저장 뒤 미분류 | 333 → 333 | **333 → 332** |

★ fatal 이 11 → 14 로 늘어 보이나 ★ **셋은 새 검사가 처음 도는 것**이다 —
`S46-247`(수집 덮개 · 가이드 신설) · `S46-32`(색인 · 마지막에 다시 만든다) ·
`S46-65`(재판정이 하루 넘게 안 돎 — ★ KB 수집이 큐에 넣었다).
★ 시험 셋은 ★ 다음 회차 첫 일이다.
