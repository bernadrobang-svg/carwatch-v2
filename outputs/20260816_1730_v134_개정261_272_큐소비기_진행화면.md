# 개정 261 · 272 — 큐 소비기(A) · 잠금 범위 · `/admin/status`

작업 시각 2026-08-16 17:15~17:35 · 소스 v134 · 실행 `python3.11`
커밋 `539b90e` · `<이번 커밋>`

---

## 1. 무엇을 하려 했나 (지시 원문)

> ```
> 1  밀린 답 2건 (NULL 원인 · 큐 A/B)   ← 먼저.  5분이면 됩니다
> 2  ★ /admin/status 신설 (STEP 136f)
> 3  중단·재개 확인
> 4  S7 카탈로그 필요 건수 산정
> 5  전 차종 S5~S10 + S7
> ```
> (정정) 실행하는 곳과 지켜보는 곳은 달라야 합니다.
> `/admin/run` 시작한다 · `/admin/status` 지켜본다.

## 2. 무엇을 했나

### ① 밀린 답 — NULL 원인

**커밋 `1b2838c` 는 `origin/main` 에 있다.** 검토 9 가 못 찾은 것은 검토 시점이
push 직전이었기 때문이다.

```
git branch -r --contains 1b2838c  →  origin/main
```

**다만 `outputs/` 기록이 없던 것은 사실이다 (규칙 6 위반).**
`outputs/20260816_1700_v134_개정271_차종미정_원인확정.md` 로 채웠다.

결론: **③ 이되 「규칙이 안 맞다」가 아니라 「규칙이 맞게 걸러낸 것」.**

### ② 밀린 답 — 큐 A/B → **A 로 정했다. 다만 systemd 가 아니라 웹 서버 안의 스레드.**

근거 셋:

```
1  전 차종 3.8시간을 HTTP 요청이 붙들 수 없다 → 백그라운드가 필수다
   그 순간 「진행·중단·재개를 어디 적나」가 생기고, 그것이 곧 큐다
   B(큐 폐기)는 큐를 없애는 게 아니라 이름만 없애고 다시 만드는 선택이 된다
2  기반이 이미 있다 — recalc_job 에 current_step·step_done·step_total·detail 이
   있고 job_progress()·db_progress() 도 있다.  없는 건 가져가는 쪽 하나였다
3  systemd 는 이 환경에 안 맞다 — 개정 250 이 「CLI 없이 돌아간다」고 했는데
   타이머는 터미널 관리가 필요하다.  웹 서버 안의 스레드는 화면만으로 완결된다
```

| 파일 | 무엇 |
|---|---|
| `collect/worker.py` (신규) | `take_next` — `UPDATE` 로 집는다 (SELECT 뒤 UPDATE 사이 끼어듦 방지) · `run_once` · `start` |
| `run.py` `cmd_web` | 소비기를 함께 띄운다 · `make_worker_ctx` · `make_worker_executors` |
| `config/web.json` | `worker_poll_sec` 2 |

**CLI 와 같은 실행기를 쓴다** — 화면으로 돌린 것과 터미널로 돌린 것이 다르면 안 된다 (B-6).

### ③ 잠금 범위 — A/B 와 무관하게 고쳐야 했던 것

```
표시(LOCKED_GROUPS)  조정만 잠근다
관문(_gate)          전부 잠갔다        ← 어긋나 있었다
```

`_gate(group=)` 를 받아 **「조정」에만** 건다 (STEP 132a 「금지 큐가 밀렸다고
관리 화면 전체가 잠기는 것」).

### ④ `/admin/status` 신설 (STEP 136f · 개정 272)

| 파일 | 무엇 |
|---|---|
| `web/routes.py` | `/admin/status` **GET 만** · `ROLE_ADMIN` · 운영 |
| `report/screens/admin.py` `status_view` | ①도는 것 ②끝난 것 ③숫자 ④실패 |
| `web/views.py` `admin_status` | 읽기 전용. POST 없음 |
| `web/templates/admin_status.html` (신규) | 화면 |
| `web/context.py` · `base.html` · `page()` | `refresh_sec` → `<meta http-equiv="refresh">` |
| `config/web.json` | `status_poll_sec` 5 |
| `validate/v11_web.py` | `V11-51`(스스로 갱신) · `V11-52`(실행 단추 없음) |
| `validate/v10_admin.py` | `V10-22`(소비기 있음) · `V10-23`(오래된 대기 표시) |

**JS 를 안 썼다.** `meta refresh` 라 JS 가 꺼져도 돌고, 갱신이 실패해도
서버가 만든 화면이라 지금까지의 진행이 그대로 남는다 (`V11-19` 정신).

## 3. 왜 그렇게 했나 (규격)

- `STEP 132a` (개정 261) — 「큐에 넣은 것은 누군가 가져간다」 · 「금지 큐가 밀렸다고
  관리 화면 전체가 잠기는 것 — 조정만 잠근다」 · 검산 `V10-22`·`V10-23`
- `STEP 136f` (개정 272) — 지켜보는 곳과 실행하는 곳을 나눈다 · 스스로 갱신 ·
  「대기 중」과 「멈춘 것」을 가른다 · **「금지 실행 단추를 이 화면에 두는 것」** ·
  검산 `V11-51`·`V11-52`
- 개정 250 — CLI 없이 돌아간다 (systemd 를 안 쓴 근거)
- 개정 268 — 「받았다」와 「쓰였다」를 함께 낸다

## 4. 1차 결과 (작업 전 숫자)

```
큐 소비기        없음.  run.py 가 recalc_job 을 한 번도 읽지 않았다
잠금            큐가 밀리면 운영·조정·탐색이 전부 409
                (큐를 푸는 화면조차 못 열어 두 번 갇혔다)
진행 화면        없음.  /admin/run 을 손으로 새로고침해야 했다
카탈로그         21건 / 판정 대상 3,472건 — 몇 건이 필요한지 몰랐다

check_all 통과 196 · fatal 4
check_src S16 실패 3 — V10-22 · V10-23 · V11-45
```

## 5. 조치 후 결과

### 소비기가 실제로 큐를 가져간다

```
큐에 1건 넣음 (reason=scoring · scope=KOLEOS_HEV)
25초 뒤 → status='failed'
         detail='S10: 선행 조건 미충족: 선행 단계 미완료: S9'
         run_id='20260816T082005'
```

**넣은 것이 소비됐고, 실패 사유와 run_id 가 남았다.** 삼키지 않았다.

### 잠금이 규격대로 갈렸다

```
큐가 밀린 상태에서
  운영 /admin/import 미리보기 → 200      ← 열린다
  조정 /admin/dict  확정      → 409      ← 잠긴다
전에는 둘 다 409 였다
```

### `/admin/status` 가 돈다

```
GET /admin/status → 200
  http-equiv="refresh" content="5"
  「마지막 확인 2026-08-16T08:24:11+00:00」
  「도는 것이 없습니다 — 「멈춘 것」이 아니라 「할 일이 없는 것」입니다」
  마지막 처리: 매물 849755320260501 · catalog · 45분 전
  최근 작업 2건 · 실패 2건 (무엇이 왜)
  받은 원문 6종 · 매물 7,661 · 판정 238

V11-51 passed=True  갱신
V11-52 passed=True  읽기 전용
V10-22 passed=True  소비 흔적 2건
V10-23 passed=True  표시
```

### S7 카탈로그 필요 건수 (지시 4)

```
지금 받은 카탈로그          21건
판정 대상의 (차종 · 트림) 조합  26  ← 상한 추정
active 매물 3,472 중 model_catalog_key 가 있는 것 208
                           없는 것 3,264
```

**★ 지금은 정확히 못 센다.** `model_catalog_key` 는 **상세(detail)에서 온다.**
상세를 받은 매물이 208건뿐이라 키가 22종만 보인다.
**상세를 전부 받아야 진짜 모델 수가 나온다** — 그때 카탈로그를 받으면 된다.
트림 조합 기준 상한은 26이므로 **추가로 받을 것은 많아야 5건 안팎**으로 보인다.

| 지표 | 작업 전 | 작업 후 |
|---|--:|--:|
| `check_all` 통과 | 196 | **200** |
| `S16` 미구현 검사 | 3 | **1** (`V11-45` 만 남음) |
| 큐 소비 | 없음 | **동작 확인** |
| 잠금 범위 | 전 화면 | **조정만** |
| 진행 화면 | 없음 | **`/admin/status` 5초 자동 갱신** |

## 6. 못 한 것 · 막힌 것 · 판단이 필요한 것

### ① 중단·재개 확인 (지시 3) 과 전 차종 실행 (지시 5) 을 아직 안 했다

지시 순서상 1·2·4 를 먼저 했다. **다음은 중단·재개 확인이다.**
`STEP 52` 의 `resume_point` 와 `raw_response` 가 남으므로 구조는 있으나,
**실제로 끊고 이어 보는 것**은 아직 안 했다.

### ② 인증 확인 (모니터 지시 4절)

```
엔카   headers = User-Agent 뿐 (config/endpoints.json).  토큰·키 없음
GitHub 토큰   ~/.git-credentials.  수집 코드와 무관
```

**수집 경로에 어떤 키도 들어 있지 않다.** `secrets/plate_hmac.key` 는
번호판 해시용이고 사이트 인증과 무관하다.

### ③ 새 config 키 3개는 부록 B 등재가 필요하다

```
worker_poll_sec 2 · progress_refresh_sec 5 · status_poll_sec 5
```

규칙 2 대로 내가 `docs/` 를 안 고쳤다. **부록 B(STEP 6 표)에 넣어 주십시오.**

### ④ 남은 fatal 4건 · `V11-45`

```
V1-16 · V2-18 · V4-11(27) · V4-20(10)
V11-45  CLI 로만 되는 기능이 없음 — STEP 136d 와 함께
```
