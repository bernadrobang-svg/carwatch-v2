# 밤샘 일괄 작업 보고 — 2026-08-17 아침

작업 시각 2026-08-17 01:50~09:00 · 소스 v134 · 실행 `python3.11`
시작 커밋 `c2c711f` → 이번 커밋 `<이번 커밋>` (커밋 10개)

---

## 1. ①~⑦ 중 어디까지 했나

| | 무엇 | 상태 |
|:--:|---|:--:|
| ① | 엔카 API 전수 조사 · `docs/ENCAR_API.md` | **끝** |
| ② | 인증 없이 되는 것 전건 수집 | **도는 중 10,737/31,230 (34%)** |
| ③ | 파싱 · 축 추가 | **파서 5종 끝 · 축 추가는 못 함** |
| ④ | 밀린 검사 15개 | **11개 끝** (V3-39·40·45·47 · V11-73~76·79·80·81) |
| ⑤ | 밀린 실측 5건 | **끝** |
| ⑥ | 화면 — 마스터 지적 | **끝** |
| ⑦ | 색인 갱신 | **끝** (검사 261개) |

---

## 2. ★ ① 엔카 API — **더 부를 것이 없었습니다**

브라우저를 열지 않고 답을 냈습니다. 세 화면이 **같은 번들 하나**를 쓰고,
거기 부르는 주소가 전부 문자열로 있습니다 (`app-DATxvME_.js` 548KB · `/vN/` 47종).

```
인증 없이 200   기존 6 + 새로 8종
  record/{id}/summary      ★ use(용도) · 전손 · 침수 · 도난 · 소유자변경 · 저당
  inspection/{id}/summary    점검자 이름
  clean-encar/{id}           플랫폼 클린 판정
  diagnosis/{id}/sellingpoint · vehicle/ev-battery/{id}
  vehicles/car/options/standard  ★ 옵션 코드→이름 전체 사전 28KB

401 로그인 필요  /v2/verification/{id}/unified-report/{free,lite,paid}
                /v2/verification/{id}/report-analysis/*
  ★ 틴팅 · 차량 키 수량 · 소화기 · 리페어 장비가 여기 있다
    번들에 항목표까지 있다 — "틴팅 (정면 유리)": {category:"basic", id:16}
  ★ 407(IP 차단)이 아니라 401 이다.  브라우저 수집으로 받을 수 있다
```

**보증 · 신차가 · 옵션가는 이미 받아서 저장까지 돼 있었습니다.** 화면이 안 냈을 뿐입니다.

## 3. ② 수집 — 34% 진행 중

```
요청 종류 4 → 9종.  어댑터가 정본이고 runner 가 가져다 쓴다
core_listing 에 상태 열 6개 + 값 열 13개 (migrate · 데이터 보존 확인)

새 원문   platform_check 1,158 · record_summary 827 · inspection_summary 827
         sellingpoint 826 · ev_battery 826
```

**중단·재개가 됩니다.** 도중에 두 번 끊겼는데(아래 5절) `--resume` 으로 이었습니다.

## 4. ⑤ 실측 — 다섯 가지

### ⑤-1 등급 인플레 (개정 287)

```
price 를 못 봤는데 등급을 매긴 것        202건
history.damage 를 못 봤는데 매긴 것    1,182건
warranty.general                       37건
→ V3-40 이 이제 잡는다
```

### ⑤-2 배점 현황 (개정 290)

| 갈래 | 지금 | 개정 292 |
|---|--:|--:|
| price | 200 (36%) | 값 250 (45%) |
| warranty | 100 (18%) | — |
| **spec** | **90 (16%)** | 사양 75 + 취향 50 |
| **history** | **55 (10%)** | **상태 180 (32%)** |
| safety · color · mileage | 110 (20%) | — |

**★ 지금은 상태(history 55)가 사양(spec 90)보다 작습니다.** 개정 292 가 뒤집으려는 것이 이것입니다.

### ⑤-3 축 × 차종 채움률 (개정 293) — `V3-47` 신설

```
★ 전기차 safety.warranty_product 0%   (G80_EV · GV60 · GV70_EV · MODEL_Y)
★ SPORTAGE_LPI price 0%               보정계수 1.517 이 범위 밖
★ MODEL_Y spec.hda 0% · spec.tinting 은 전 차종 0~12%
```

### ⑤-4 새 배점(개정 292)으로 재계산 — **505점 기준**

```
       최저    P10    P25    중앙    P75    P90    P99    최고
비율   9.2%  36.4%  42.7%  50.2%  58.3%  65.3%  75.5%  80.0%
```

### ⑤-5 등급 경계 제안

| 등급 | 경계 | 근거 |
|---|--:|---|
| **S** | 75% | 상위 1% |
| **A** | 69% | 상위 5% |
| **B** | 60% | 상위 20% |
| **C** | 53% | 상위 40% |

**★ 백분위를 먼저 정하고 그 자리의 비율을 반올림했습니다.**
분모 고정판(어제)의 제안(77·72·64·56)보다 낮습니다 — 새 배점이 더 깐깐하기 때문입니다.

**★ 아직 코드에 안 넣었습니다.** 개정 292 배점을 구현해야 이 숫자가 실제가 됩니다.

## 5. 막힌 것 · 판단한 것

### ① `extend_warrant` 를 뺐다 — 「덜 하는 쪽」

160건 전량 404 였고 `V1-08` 이 「경로 오류」로 잡았습니다. 경로는 맞지만
쿠폰이 붙은 매물에만 있는 자원입니다. **3,470 요청을 쓸 값이 없어 뺐습니다.**

### ② `CORE` 열에 사이트명을 넣었다가 시험이 잡았다

`encar_cleaned` · `clean_encar_status` → `platform_verified` · `platform_check`.
**이미 받은 원문 827행의 `endpoint` 도 함께 맞췄습니다 — 버리지 않았습니다.**

### ③ 검사가 디스크를 채워 V11 전체가 죽었다

검사마다 231MB DB 를 복사해 `/tmp`(여유 0.8GB)가 찼습니다.
**한 번만 복사하게 고쳤습니다** (통과 148 → 229).

### ④ 씨앗 DB 가 안 만들어졌다

`StubEncar` 가 새 6종을 몰라 「형식 검증 거부 24건」이 났습니다.
**실측한 모양 그대로 가르쳤고** `test_screens`·`test_admin_flow`·`test_report`·
`test_invariants` 가 돌아왔습니다.

### ⑤ ★ 스크린샷이 통째로 민무늬로 나왔다

`V11-82`(CSS 지문)를 붙이자 스크린샷 도구의 치환이 `?v=` 를 못 따라가
**CSS 가 하나도 안 걸린 화면**을 찍고 있었습니다. 무늬로 바꿔 고쳤습니다.
**실서비스는 멀쩡했고 스크린샷만 깨져 있었습니다.**

### ⑥ 못 한 것 — ③의 「축 추가」

새 값(용도 · 전손 · 침수 · 플랫폼 판정)을 **저장까지 했고 축은 안 만들었습니다.**
개정 292 가 축을 통째로 다시 정하므로 **지금 축을 만들면 곧 버립니다.**
「덜 하는 쪽」을 골랐습니다. 배점이 확정되면 함께 만듭니다.

## 6. 숫자

```
check_all   통과 229 · fatal 7 · warn 8
  V1-11 · V1-16 · V2-18   재처리 경계 (수집이 도는 중이라 늘 뜬다)
  V4-11   unclassified 32 — 자료
  V3-41   분모 — c3 잔재 1행 (삭제 여부를 물어 둔 그것)
  V3-40   핵심 축 excluded 인데 등급 — ⑤-1 이 잰 그것
  V5-03   분모 시험 — 0장 7.1 ↔ 개정 289 규격 충돌 (어제 보고)
  ★ V4-20 176건이 사라졌다 — options.etc 가 딜러 자유 입력이었다

check_src   S7 0 · S14 0 · S27 25 · S28 261 · S16 미구현 11
run_tests   실패 5 — test_endtoend · test_integration · test_run · test_score · test_spec_ui
            ★ 전부 분모 규격 충돌(0장 7.1)에서 나온 것이다
ruff        통과

새 원문      4,464행 (5종)
검사        250 → 261개
outputs/    5.1MB (렌더 29화면 · 스크린샷 31장)
```

## 7. ★ 마스터가 볼 화면

```
https://43.201.16.78.sslip.io/listings
```

**강제 새로고침(Ctrl+Shift+R)을 한 번만 해 주십시오.** CSS 가 바뀌었습니다.

### 무엇이 달라졌나

```
사진        64px → 128px (넓으면 160px).  차가 보인다
축          「0」 → 상태로
            HUD 있음 · 선루프 없음 · 일반보증 1년 10개월 · 65천km
            사고 1회 · 보험 111만 · 렌트 아님
값 셋       신차가 5,787만 · 시세 4,090만 · 가격 2,990만 · 시세차 -1,100만
            ★ 시세는 같은 차종·트림·연식 실매물 중앙값이다 (이론가가 아니다)
              렌트·리스 승계를 뺐다
시세 화면    트림 단추 — 고르면 분포가 그 트림만 (1,713 → 1,223건)
관심 목록    사진 · 차종 · 어디를 눌러도 상세로 · 엔카 링크
관리 화면    14개 표가 좁은 폭에서 카드가 된다
```

**스크린샷 31장이 `outputs/shot/` 에 있습니다** — 관리 화면 포함, 전부 내용이 있습니다.

## 8. 다음에 필요한 것

```
1  ★ 개정 292 배점 구현 — ⑤-4·5 숫자가 그때 실제가 된다
2  0장 7.1 ↔ 개정 289 분모 규격 충돌 정리 (시험 5개가 여기서 실패한다)
3  c3 잔재 1행 삭제 여부
4  /v2/verification/* 를 브라우저 수집에 넣을지 (엔카 이용약관 확인 필요)
5  시세 막대는 아직 못 찾았다 — 그 화면 네트워크 탭이 필요하다
6  전기차 배점 (safety.warranty_product 0%)
```
