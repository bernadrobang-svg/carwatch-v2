# 수집기 열이 원문을 안 남기고 있었다 · 검사 색인이 S4x 를 못 보고 있었다

```
버전     소스 v256 · 지시서 SPEC-2026.08.26-r754
때       2026-08-26 16:00
지시     남은 일 3 「3-2 걸러 저장 — raw_response 에는 남겨라」
         남은 일 6 「build_index.py 를 바퀴마다 돌려라」
앞 작업  f348dcb (v4m 여덟 장)
```

---

## 1. ★★★ 찾은 것 — 엔카 말고는 원문이 **한 건도 없었다**

v4m 작업 중에 KB 수집이 잘 도는지 보다가 알았다.

```
sqlite> SELECT site, COUNT(*), MAX(fetched_at) FROM raw_response GROUP BY site;
encar  147,061  2026-08-25T00:00:28

sqlite> SELECT COUNT(*) FROM core_listing WHERE site='kbchachacha';   265
sqlite> SELECT COUNT(*) FROM raw_response WHERE site='kbchachacha';     0
```

★ `tools/collect_*.py` **열 개를 다 훑었다.**
★★ `save_raw` 를 부르는 것이 **하나도 없었다.**

```
collect_bmw · bobaedream · heydealer · hyundai_cert · kbchachacha ·
kcar · kia_cpo · lexus · reborncar · volvo      ★ 열 개 다
★★ 셋은 주석에 「raw_response 에는 남는다」고 ★ **적어만** 두었다
   kcar 135행 · reborncar 113행 · kia_cpo 8행
```

★ 이것이 명령서 3-2 의 **필수**와 정면으로 어긋난다.

```
필수  ★ ★ **`raw_response` 에는 남긴다** — ★ 갈래를 넓히시면 ★ 다시 판다
      ★ ★ 다시 받을 일이 없다.  ★ 그것이 ★ 「보관만 한다」는 뜻이다
```

★★★ **무엇이 걸려 있었나** — 파싱이 틀리면 **다시 받는 수밖에 없다.**
KB 는 그것이 **엿새**다 (하루 2,880건 · 15,711건).

★ `V2-01`(ok 원문 수 == CORE 행 수 — 5,744)이 **여태 이것을 말하고 있었다.**
나는 그것을 「수집이 도는 중이라 그렇다」로 **두 회차 넘겼다. 내 잘못이다.**

---

## 2. 무엇을 했나 (고친 파일 · 줄)

### ① `store/raw.py` — `save_site_raw()` 를 새로 만들었다 (141~186행)

```
왜 save_raw 를 안 쓰나
  ★ 그것은 ★ 엔카의 수집 계약(EndpointSpec · FetchResult · verify)에 맞춰 있다.
  ★ 사이트 도구는 ★ 몸통 글자만 든다.
  ★★ 껍데기를 지어 맞추면 ★ 「검증했다」는 거짓말이 된다 — ★ 안 한다

지킨 것
  ★ 못 받은 것은 안 넣는다 — body 가 없으면 아무것도 안 한다 (금지 12)
  ★ 같은 자리(site·endpoint·source_id)에 이미 있으면 건너뛴다
    — ★ 회차를 나눠 받으므로 ★ 다시 부를 수 있다
```

### ② 수집기 열에 넣었다

| 도구 | 어디에 | endpoint |
|---|---|---|
| kbchachacha | 상세 — ★ **파싱보다 먼저** | `detail` |
| reborncar | 상세 — 파싱보다 먼저 | `detail` |
| bobaedream | 상세 — 파싱보다 먼저 | `detail` |
| hyundai_cert | `fetch_detail` 이 원문도 돌려주게 고쳤다 | `detail` |
| kcar | 상세 (JSON) | `detail` |
| heydealer | 상세 (JSON) | `detail` |
| kia_cpo | ★ 상세가 없다 — 목록 항목이 원문의 전부다 | `list` |
| lexus | 〃 · ★ **안 넣는 것도 남긴다** | `list` |
| bmw · volvo | ★ 목록 쪽 HTML 이 원문이다 | `list` |

★ 파싱보다 **앞에** 둔다 — 파싱이 실패해도 원문은 남아야 한다.

### ③ 검사 `S46-76`

```
validate/v0_guide.py  s46_76_collectors_keep_raw()
  ★ tools/collect_*.py 가 다 save_site_raw(또는 save_raw)를 부르는가
  ★ 「부르는가」만 본다 — 「실제로 들어갔는가」는 V2-01 이 센다
```

### ④ `tools/build_index.py` — 검사 색인이 **S4x 를 못 보고 있었다**

```
실측  grep -c "S46-" docs/CHECKS.md   →  0
```

★ `validate/v0_guide.py` 는 `Check(...)` 를 안 쓴다 —
`CHECKS = (("S46-75", "제목", 함수, "warn"), …)` 꼴의 **튜플 표**다.
★ 색인 생성기는 `Check(...)` 호출만 AST 로 읽는다.
★★ 그래서 **S43~S46 서른일곱 개를 한 번도 못 봤다.**

★ 규칙 11 은 「검사를 더하거나 고치면 다시 돌린다」인데,
**다시 돌려도 안 들어오니 색인이 거짓말을 하고 있었다.**

```
고쳤다  _guide_checks() — CHECKS 튜플 표를 AST 로 읽는다
        ★ 함수 이름으로 ★ 그 함수가 사는 줄을 찾아 적는다
        ★ 손으로 적지 않는다 (규칙 11)
```

---

## 3. 1차 결과 · 조치 후 결과

```
                                    1차        조치 후
raw_response 에 엔카 밖 사이트        0건        ★ 도구 열이 다 남긴다 (수집 중)
save_site_raw 를 부르는 수집기        0 / 10     ★ 10 / 10
docs/CHECKS.md 의 검사 수            393        ★ 425
docs/CHECKS.md 의 S46-* 항목          0          ★ 37
② 죽은 검사 (통과·실패 기록 없음)     36         ★ 68
```

★★ ②가 **36 → 68 로 늘었다.**  나쁜 일이 아니다 —
**여태 안 보이던 S4x 서른둘이 「기록이 없다」는 채로 드러난 것**이다.
★ 그 까닭은 `v0_guide.run()` 이 결과를 `audit_validation` 에 **안 적기 때문**이다.
★ 그것은 다음 바퀴에 잡는다 (6장).

```
python3.11 tools/run_tests.py
  test_admin         통과
  test_admin_flow    통과
  test_collect       통과
  test_crosssite     통과
  test_dict          통과
  test_endtoend      통과
  test_fixtures      통과
  test_integration   통과
  test_invariants    통과
  test_pipeline      통과
  test_registry      통과
  test_report        통과
  test_run           통과
  test_score         통과
  test_screens       통과
  test_spec_ui       통과
  test_store         통과
  test_watch         통과
  test_web           통과
  ruff               통과  (F821,F811,B905,F841,DTZ005,F401)

  임시 DB 62개 치움 (검사가 디스크를 채우지 않는다)

결과: 통과
```

```
python3.11 tools/check_all.py
DB carwatch.db · run 20260825T000022
통과 351 · fatal 4 · warn 11 · 환경 차이 2 · 미실행 8

■ FATAL — 고쳐야 한다
  V1-08     동일 코드 실패율 100% 인 엔드포인트 없음 — 2건 (예: detail: 328건 전량 실패, inspection: 182건 전량 실패)
  V1-21     받아 두고 안 펼쳐진 원문이 없음 — 182
  V1-23     필요한 조합 대비 받은 카탈로그 비율 — 127조합 · 78.9% · 카탈로그 없는 매물 633건 · 사이트가 안 줌 empty 21조합 · 매물 586건 (예: 8238609202404
  V2-01     ok 원문 수 == CORE 행 수 — 5744

■ warn — 진행은 된다
  V1-07     매물별 엔드포인트 4종 상태 존재 — 4242
  V4-06     RAW 경로가 등록부에 있는가 — 3
  V4-06b    등록부에 있는데 RAW 에 없는 유령 경로 — 48
  V4-11b    판정에 안 쓰는 미분류 경로 — 319
  V4-21     같은 이름의 공개 함수가 두 모듈에 없음 — 7
  V3-39     이론가와 실제 중앙값의 차가 상한 안 — 최대 33.2%
  V3-47     축별 차종 간 결측률 편차가 상한 안 — 3축
  V3-03     축별 source 값 종류 >= 2 — 1건 (예: taste.fitting)
  V3-04     축별 값 종류 >= 2 — 2건 (예: state.consumable, taste.fitting)
  V3-91     가이드 검산 일곱 줄이 표대로 나옴 — 7줄 다름
  V3-96     value IS NULL 과 source 모름 건수 차 — source 모름 33989 · 차 +32,797

■ 환경 차이 — 결함이 아니다
  V1-05     실행 시작 시각 기준이라 DB 만으로는 재현되지 않는다
  V4-01     HMAC 키가 다르면 plate_hash 가 전건 불일치한다

■ 미실행 — 이번 실행에서 그 단계를 안 돌았다
  V1-20     이번 실행에 카탈로그를 안 받았다
  V4-29     판정을 막는 미분류가 없다 — 잴 것이 없다
  V4-30     판정을 막는 것이 없다
  V3-68     부록 F 에 축 목록이 없다
  V3-64     규격에서 경계를 못 읽었다
  V9-09     비율이 같은 매물 짝이 없다 — 잴 것이 없다
  V10-24    사전 확정 이력이 없다
  V11-121   두 사이트에 같이 올라온 차가 없다 — 나란히 낼 것이 없다

★ fatal 넷은 그대로다 — ★ 이번 고침은 ★ **앞으로 받을 것**에 걸린다.
  ★ 이미 받아 둔 5,744건의 원문은 ★ 없다.  ★ 지어내지 않는다.
  ★ 다시 받을지는 ★ 3-3 「되돌리기」와 함께 여쭙는다
```

---

## 4. 못 한 것 · 판단이 필요한 것

### ★ 남았다 — KB **목록 쪽**은 아직 안 남긴다

```
지금   상세만 남긴다.  ★ 목록 쪽(list)은 안 남긴다
까닭   목록을 받는 함수에 conn 이 없다 — 넘기려면 결을 바꿔야 한다
       ★ 목록 쪽은 ★ 봇 차단 0건이라 ★ 언제든 다시 받는다 (실측 08-24 · 53쪽)
       ★ 막아야 할 것은 ★ 상세다 — ★ 그것이 하루 2,880건짜리다
★ 줄여 잡은 것을 ★ 적어 둔다 — ★ 조용히 넘기지 않는다
```

### ★ 여쭐 것 — `v0_guide` 의 검사 결과를 남길까

```
지금   S43~S46 서른일곱은 ★ 화면에 찍기만 하고 ★ DB 에 안 남는다
       ★ 그래서 색인에 「마지막 통과 ★ 없음」으로 나온다
여쭙는다  audit_validation 에 남길까 — ★ 남기면 ② 죽은 검사가 68 → 36 로 돌아간다
★ 검사 결과를 남기는 것은 ★ 규격이 정하는 일이라 ★ 내가 정하지 않았다
```

### ★ 앞 회차에서 넘어온 것 (그대로)

```
① KB 전량 — 도는 중.  ★ 이제 원문이 함께 쌓인다
② 3-2 걸러 저장 — K카 526 · 리본카 1,038 · 렉서스 36 을 다시 받는 것
③ 3-3 되돌리기 · 미분류 다시 세기
④ 멀티 매핑 (45-2) — ★ 축 × 사이트 갈래표 · 견주기 (45-3)
⑤ 관리 「탐색」 절 · 매물 카드 비교 네모 — ★ 앞 기록 6장에서 여쭈었다
```
