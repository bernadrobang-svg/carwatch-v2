# L — `/fetch` 를 세웠습니다. 마스터께서 단추 한 번으로 받으십니다

지시   `outputs/ORDER_20260829.md` L-1 ~ L-9 · `SPEC-2026.09.06-r1200`
버전   v388
가이드 물음 — 「★ `/fetch` 를 개발측 첫 번째로 올리겠습니다. 그렇게 할까요」 → **했습니다.**

---

## 1. ★ 「몇 → 몇」

| 길 | 전 | 후 |
|---|--:|--:|
| `/fetch` | **404** | **200 · 10,843B** |
| `/fetch/queue?step=2&n=2` | **404** | **200 · 주소를 준다** |
| `/fetch/put` | 없음 | **200 · 원문을 남긴다** |
| 바닥 메뉴 「받기」 | 없음 | **있다** |

### 한 바퀴를 실제로 돌려 확인했습니다

```
큐        /fetch/queue?step=2&n=1
          → https://api.encar.com/v1/readside/vehicle/39554653
올림      /fetch/put  → {"status": "ok", "bytes": 9083}
파일      raw/encar/detail/2026-09-06/39554653.json  (origin=browser)
상태      detail_status  error → ok
```

★ **그 시험 자료는 지웠습니다.** 진짜 원문 넷만 남겼습니다 —
★ 지어낸 것을 저장소에 남기지 않습니다.

---

## 2. L-1 ~ L-9 — 다 했습니다

| # | 무엇 | 어디 | 상태 |
|:--:|---|---|:--:|
| L-1 | `/fetch` 화면 | `web/templates/fetch.html` · `report/screens/fetch.py` | **끝** |
| L-2 | `/fetch/queue?step=N&n=500` | `queue()` | **끝** |
| L-3 | 이미 받은 것은 뺀다 (`ok`·`not_found`) | `DONE` | **끝** |
| L-4 | 0.1초 간격 · 진행 막대 · 남은 시간 | 화면 JS | **끝** |
| L-5 | `/fetch/put` — 원문 그대로. 파싱은 서버가 | `put_one()` | **끝** |
| L-6 | 멈춤 · 이어받기 | `stop` ＋ 큐가 매번 다시 센다 | **끝** |
| L-7 | 실패한 것만 다시 | `retry=1` → `error` 만 | **끝** |
| L-8 | 100 · 500 · 2,000 · 전부 | `CHUNKS` | **끝** |
| L-9 | 바닥 메뉴에 「받기」 | `config/web.json` `top_menu` | **끝** |

### 지금 화면이 내는 수 (실측)

```
1 목록       받을 것     0 / 9,317
2 상세       받을 것 8,929 / 9,317
3 성능점검    받을 것 1,804 / 9,317
4 보험이력    받을 것 1,798 / 9,317
5 catalog   받을 것 9,317 / 9,317
```

★ 큐 크기를 **미리 정하지 않습니다** — 그때그때 세어서 냅니다 (지시대로).

### ★ 크기로 가릅니다 (가이드 잣대 ①)

```
http 404        → not_found   (팔린 차)
http != 200     → error       (다시 받는다)
200 인데 작다    → empty       ← ★ 「없다」가 아니라 안내문이다
그 밖           → ok
```

기준은 `config/endpoints.json` 의 `min_detail_bytes` 입니다 — 코드에 안 박았습니다.

---

## 3. 만들면서 제가 깬 것 넷 — 다 고쳤습니다

| 검사 | 무엇이 문제였나 | 어떻게 고쳤나 |
|---|---|---|
| `V4-22` 역방향 import | `report/` 가 `adapters/` 를 불렀다 | 주소를 `config/endpoints.json` 의 `base_url`＋`paths` 에서 만든다 |
| `test_admin` config 쓰기 | `outputs/fetch_last.json` 에 `json.dump` | 표 `fetch_run` 으로 옮겼다 (파일 안 쓴다) |
| `V2-19` NOT NULL | `fetch_run` 이 여섯 칸에 NOT NULL | 뺐다 — 「모르면 멈춘다」로 저장하지 않는다 |
| `V11-19` 폴링 JS | 흰목록에 없었다 | `fetch.html` 을 넣었다 — **브라우저만 할 수 있는 일**이다 (STEP 136c) |
| `test_web` 길 수 | 46 ↔ 표 45 | `/fetch/done` 을 없애고 `put` 에 `done=1` 로 합쳤다 — 규격 표는 **셋**이다 |

★ 이름도 규격 표대로 맞췄습니다 — `api_fetch_queue` · `api_fetch_put`.

---

## 4. 두 도구

```
python3.11 tools/run_tests.py
결과: 실패 — test_endtoend.py   (그 밖 열여덟 파일 전부 통과 · ruff 통과)

fatal 넷 — ★ 넷 다 이번 작업이 만든 것이 아닙니다
  V4-28   관리 사전 화면 문구      web/templates/ — 가이드 몫
  V11-12  라우팅 표 ↔ 10·13장     규격 표 — 가이드 몫
  V3-77   갈래 합 ↔ 갈래 표기      ★ 규격이 스스로 어긋난다 (v384 7-2)
  V11-34  /detail 이 35 쿼리       ★ v385 전에도 35 였다
```

★ 제가 만든 붉음 `V4-22` · `V2-19` · `V11-19` 셋은 **이 회차에서 다 닫았습니다.**

---

## 5. 여쭐 것

### 1  ★ 이제 마스터께서 눌러 주시면 됩니다

```
{base_url}/fetch   → 「2단계 상세 500건 받기 ▸」
```

★ 500건이면 약 50초입니다. ★ 멈추셔도 받은 데까지 남고 ★ 다시 누르면 이어받습니다.
★ 8,929건을 다 받으시면 약 **15분**입니다.
★ 그러면 **탭 3 「조건에 다 맞는 차」가 채워집니다** — 지금 0 인 까닭이 그것뿐입니다.

### 2  ★ 상단 메뉴가 일곱이 됐습니다 (`V11-157` 은 다섯까지)

L-9 가 「바닥 메뉴에 **받기**를 더한다」이고 시안도 여섯을 그렸습니다.
`V11-157`(상단 메뉴가 다섯을 넘지 않음)과 어긋납니다.
**최근 지시를 따라 더했고**, 규격끼리 어긋난 것을 여기 적습니다.
① 메뉴 상한을 일곱으로 올릴지 ② 「받기」를 관리 안으로 넣을지 정해 주십시오.

### 3  `/fetch` 시안의 클래스가 `app.css` 에 없습니다 (`V11-59` 52종)

`.v3-step` · `.v3-bar` · `.v3-chunk` · `.v3-go` · `.v3-live` · `.v3-res` 등입니다.
시안 안에는 있는데 `app.css` 에는 아직 없습니다 — 그래서 지금은 **꼴이 밋밋합니다**.
`web/static/app.css` 는 가이드 몫이라 손대지 않았습니다.

### 4  KB·K카 탭은 아직 주소를 못 만듭니다

`/fetch?site=kcar` 는 200 이지만 큐가 빕니다 —
`config/endpoints.json` 에 그 사이트의 `paths` 가 엔카처럼 갖춰져야 합니다.
엔카부터 채우시는 것이 먼저라 그대로 두었습니다.

## 6. 이견

없습니다.
