# D1 · D2 · D3관문 · D4파서 · D5① — ★ 붉은 것 **13 → 10**

```
지시문   개발팀 명령 r1168 (차례 D1 → D3·D4 → D5 → D6)
검산     S46-277 · S46-278 · S46-282 · S46-283 · S46-32
날짜     2026-09-05 13:28 (KST)
```

## 0. ★ 내 몫 194개 중 붉은 것

```
판 앞    ★ **13개**
판 뒤    ★ **10개**   (S46-277 · S46-278 · S46-32 를 껐다)
```

★ 남은 열 — `S46-280`(열두 곳 구멍) · `S46-279`(KB) · `S46-276`(엔카) ·
`S46-261`(회차↔배포) · `S46-247`(수집률) · `S46-207`(커밋 제목) · `S46-24`(facet · 마스터 몫) ·
`S46-92`(브라우저 수집 0건) · `S46-98`·`S46-100`(시안 낱말 · 가이드 몫).

## 1. D1 — 막힌 응답도 원문으로 남긴다 ✅ `S46-278`

```
1차   KB 원문 목록 ★ **0개** — ★ 막힌 것도 ★ **성공한 쪽도** 안 남겼다
고침  `fetch_ok` 이 ★ **막힌 몸통을 함께 돌려준다** (넷째 값)
      부르는 자리 넷이 ★ `save_file(..., status="blocked")` 로 남긴다
      ★ 「없음으로 저장하지 않는다」 → ★ 「없음을 **값으로 삼지** 않는다」로 고쳐 적었다 (뜻 그대로)
시험  `status=blocked` 로 써지고 되읽힌다 (목록 `page-0003.json` · 상세 `28661592.json`)
```

★★ **그리고 ★ 성공한 목록 쪽도 안 남기고 있었다** — ★ 매물번호만 뽑고 버렸다.
★ ★ 그것이 ★ **D4 의 바탕이 없던 까닭**이다.  ★ 함께 고쳤다.

### ★ 내가 낸 흠 하나 — ★ 자료를 보고 잡았다

```
★ 처음에 ★ 그 `save_file` 을 ★ **`break` 뒤**에 넣었다 — ★ **죽은 코드**였다.
  ★ ★ `S46-278` 은 ★ **통과**했다 (글자만 봤다).
  ★ ★ ★ 그런데 ★ 파일이 ★ **세 개**뿐이라 ★ 알았다 — ★ 옮기니 ★ **233개**로 늘었다.
★ 검사가 통과해도 ★ **자료를 봐야 한다**
```

## 2. D2 — 관리 질의가 조회를 막던 것 ✅ `S46-277`

```
1차   `SELECT … GROUP BY … ORDER BY … LIMIT` 가 ★ 「쓰기 연산이 포함됐다」로 막혔다
까닭  정렬이 ★ **임시 표**를 쓴다 — ★ `OpenEphemeral P1=1` → `Delete P1=1` → `IdxInsert P1=1`
고침  ★ **커서를 따라간다** (`_write_opcodes`) —
      TEMP  `OpenEphemeral`·`SorterOpen`·`OpenAutoindex`·`OpenDup`·`OpenPseudo` 가 연 `P1`
      HARD  `Clear`·`Destroy`·`DropTable`·`DropIndex`·`CreateBtree`·`OpenWrite` 는 커서를 안 본다
```

★ 아홉 가지로 재 봤다

```
조회 여섯 ★ 다 열린다 — GROUP BY＋ORDER BY＋LIMIT · ORDER BY＋LIMIT · LIMIT ·
          HAVING＋ORDER BY · WITH · JOIN＋ORDER BY
쓰기 다섯 ★ 다 막힌다 — DELETE · UPDATE · DROP · INSERT · CREATE
PII      ★ 그대로 막힌다 — 「개인정보 표는 조회할 수 없습니다」
```

## 3. ★★★ D3 — 엔카 상세 · ★ **407 은 목록뿐이 아니다** (증거)

★ 관문을 먼저 고쳤다 — ★ `server_may_fetch` 를 ★ **창구마다** 읽는다
(`sites.json` 의 `master_line_only` 는 사이트 통째라 너무 굵다).
★ ★ 엔카는 ★ `{list: false, detail: true}` 다.

★ 그리고 ★ **두드려 봤다.**

```
① 세 건만 두드렸을 때        ★ **200 · 7,892B / 7,805B / 7,942B**   ← 지시대로다
② 판을 돌렸을 때 (0.3초 간격) ★ 14,204건 중 ★ **ok 3 · error 14,195**
   봉투 — ★ `407 error` **14,122** · `404 not_found` 3 · `200 ok` 1
③ 잣대를 세워 다시 쟀다
   ★ 사이 **0.3초** · 12건 → ★ 200 **6** · 막힘 6 · ★ **다섯째부터** 막힌다
   ★ 사이 **2.0초** · 12건 → ★ 200 **0** · 막힘 12 · ★ **첫째부터** 막힌다
```

★★ 곧 ★ **속도가 아니라 ★ 누적이다.**  ★ 네 건쯤 되고 나면 ★ 그 IP 가 잠긴다 —
★ ★ 간격을 늘려도 안 된다 (★ KB 의 「누적이라 묶음을 늘려도 안 된다」와 같은 꼴).

★★★ **여쭐 것** — ★ 6,933건을 ★ **서버로는 못 채운다.**

```
★ 내가 생각하는 안 둘 —
   안 ①  ★ `/admin/collect`(마스터 회선)로 채운다 — ★ 엔카 상세는 그 길뿐이다
   안 ②  ★ 하루에 네 건씩 받는다 — ★ 6,933건이면 ★ **4년 9개월**이다.  ★ 안 된다
★ 어느 쪽이든 진행한 것 — ★ 판은 세웠다.  ★ 0건 받으며 도는 것은 ★ 사이트만 두드린다
★ ★ 다만 ★ 그 판이 ★ **철학 ②를 함께 돌려** ★ 264건을 내리고 ★ **291건을 되살렸다**
```

## 4. D4 — KB 목록 파서 ✅ (자료는 아직 차는 중)

```
1차   `parse/kbchachacha/` 에 ★ **목록 파서가 없었다** (`parse_detail` 뿐)
새로  `parse_list(html)` — ★ 카드에서 ★ 번호·차명·트림·연식·주행·지역·값·사진
      `<div class="area" data-car-seq>` · `<strong class="tit">` ·
      `<div class="data-line">「24/08식(24년형)」「13,748km」「경기」` ·
      `<span class="price">3,970<span class="unit">만원` · `img.kbchachacha.com`
시험  한 쪽 **40장 전부** — ★ 값 40 · 주행 40 · 연식 40 · **트림 40** · 사진 40
★★   **색·옵션은 카드에 없다** — ★ 상세가 준다.  ★ 지어내지 않았다
`load_raw` 도 ★ HTML 목록 파서를 ★ **먼저 본다** (전에는 JSON 만 알아 쪽을 통째로 버렸다)
```

★ 목록 원문이 ★ 3 → ★ **233개**로 쌓이는 중이다 (판이 아직 돈다).
★ ★ 다 받으면 ★ `load_raw kbchachacha` 로 칸을 채우고 ★ 다음 회차에 수를 적는다.

★ 그리고 ★ **KB 목록은 열려 있다** — ★ 두 쪽을 두드려 ★ 200 · 291KB · 293KB 를 받았다.
★ ★ 막힌 것은 ★ **상세뿐**이다 (앞 회차 40묶음 · 120회 · 새 상세 0건).

## 5. D5 ① — 매핑표를 등록부에 넣었다 ✅ `S46-282`

```
1차   `meta_field_usage` ★ **858줄인데 ★ 엔카가 850줄** · ★ 일곱 곳은 한 줄도 없다
새로  `tools/load_field_map.py` — ★ 가이드 표 **126줄**을 넣는다
      ★ 이미 있는 줄은 ★ **안 덮는다** (마스터가 정하신 분류를 안 밀어낸다)
      ★ 빈 `core_column` 만 채운다
조치 후 ★ **981줄 · 열두 곳이 다 들어왔다**
      encar 866 · bmw_bps 14 · kcar 13 · kia_cpo 13 · hyundai_cert 12 · kbchachacha 12 ·
      heydealer 11 · revolt 10 · bobaedream 9 · volvo_selekt 9 · lexus_certified 7 · reborncar 5
```

★ 흠 하나 — ★ 처음에 ★ 길 없는 줄이 ★ **서로 겹쳤다** (열쇠가 `(site, endpoint, json_path)`
인데 ★ 코드에 박힌 칸은 길이 다 같다).  ★ **칸 이름을 붙여** 갈랐다 (126 → 68 → 123줄).

★★ **D5 ② (파서가 표를 읽게) 는 아직이다** — ★ 열두 파서를 함께 고치는 일이라
★ 다음 회차에 ★ 한 자리(공용 읽개)를 만들고 ★ 한 사이트로 증명한 뒤 넓힌다.

## 6. ★ 번호가 겹쳤다 — 물렸다

```
★ 내가 앞 회차에 세운 `S46-273` 이 ★ 가이드의 `S46-273`(엔카 배너)과 ★ **겹쳤다**.
  ★ ★ **`S46-283` 으로 옮겼다** — ★ 지금 겹친 번호 ★ **없다** (검사 194개).
★ 그리고 ★ 가이드의 `S46-275` 가 ★ 같은 일을 본다 — ★ 다만 ★ **코드**만 본다.
  ★ ★ `S46-283` 은 ★ **자료**를 본다 (표가 실제로 찼는가).  ★ 둘이 짝이다
```

## 7. 규칙 3 의 두 도구

```
python3.11 tools/run_tests.py
  실패 1파일 — test_endtoend.py
    FAIL  fatal 실패가 없다  ['V1-27', 'V4-28', 'V3-77', 'V11-34']
  ★ 이 회차 앞과 ★ **같다**
```

## 8. 여쭐 것

```
① ★ **엔카 상세 6,933건** — ★ 서버로는 못 채운다 (3절 증거).  ★ 마스터 회선이 필요하다
② D5 ② — ★ 파서가 표를 읽게 하는 것을 ★ 다음 회차에 한 자리부터 하겠다.  ★ 그 차례가 맞는지
③ 앞 회차의 것 — ★ BMW 사진(https 없음) · `sold.html` 사진 자리 · KB 상세 회선 ·
   `check_all` 타이머 권한
```
