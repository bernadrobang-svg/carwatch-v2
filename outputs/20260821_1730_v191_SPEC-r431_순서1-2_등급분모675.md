# ★ SPEC-2026.08.21-r431 — 순서 1·2 · 개정 431 등급 분모 675

2026-08-21 17:30 · v191

```
★ 오늘의 핵심 숫자 —  A 이상 = 0 건
   분모를 599 → 675 로 올렸는데도 0 이다.  최고가 78.8% 로 A 문턱에서 8.2점 모자라다
```

---

## 1. 무엇을 하려 했나 (지시 원문)

> **★★ 개정 431 — 취향을 등급 분모에 넣는다.** 개정 292 「취향은 등급에서 뺀다」를
> 폐기한다. `grade_base = 675` 로 고정. ★ 빼는 갈래가 없다. 축 온·오프로 끈 축만
> 뺀다 (개정 426). **V3-90 · V3-91 신설**.
> ★ 근거 — S 와 A 를 가르는 것이 취향 하나인데 그 취향이 등급 분모에 없었다.
> 그래서 A 이상 0건이었다
>
> **★★ S9 부터 recalc 다시.** ★ 등급 분포 전후 표 — A 이상이 몇 건 나오는지가
> 오늘의 핵심 숫자다. ★ 가이드 검산 일곱 줄이 표대로 나오는지 확인해라 (V3-91).
> **★ 안 맞으면 그대로 적어라. 맞추려고 배점을 만지지 마라.**
> ★ 「사고 없다고 A」가 안 나오는지 표본 5건으로 보여라

---

## 2. 1차 결과 (고치기 전 숫자)

```
check_all   통과 325 · fatal 7 · warn 9 · 환경 차이 1 · 미실행 7
run_tests   실패 3 — test_endtoend.py · test_report.py · test_score.py
grade_base  599        (675 − 취향 145 + … 개정 292 로 취향을 빼던 값)
A 이상      0 건
등급 분포   B 51 · C 181 · D 2833 · E 776
```

---

## 3. 무엇을 했나 · 왜 그렇게 했나

### ① `GRADE_EXCLUDED_AXES` 를 비웠다 — `analyze/axes.py`

```python
# ★★ 개정 431 — 등급에서 빼는 갈래가 **없다**.  분모는 늘 675 다
#   전   ("taste",) — 취향 50 을 뺐다 (개정 292)
#   후   ()          — 취향 145 는 등급에 들어간다.  S 와 A 를 가르는 축이다
#   ★ 축 온·오프로 끈 축만 뺀다 (개정 426) — policy.skipped 가 그 일을 한다
GRADE_EXCLUDED_AXES: tuple[str, ...] = ()
```

`config/scoring.json` `grade_base_points` 625 → **675**.

### ② S9 부터 recalc 를 돌렸다 (3,841 매물 · 92,184 축)

### ③ `V3-90` · `V3-91` 을 만들었다 — `validate/v3_logic.py`

```
V3-90  등급 분모가 675 로 고정돼 있는가  (FATAL)
      ① config grade_base_points == 675
      ② GRADE_EXCLUDED_AXES 가 비어 있는가
      ③ DB 전건의 grade_base 가 675 인가 (recalc 를 돌렸는가)
V3-91  가이드 검산 일곱 줄이 표대로 나오는가  (WARN)
      ★ WARN 으로 둔 이유는 6절 ①
```

**검사를 되살려 확인했다 (S42-2)**

| 되살린 결함 | V3-90 |
|---|---|
| `GRADE_EXCLUDED_AXES = ("taste",)` 로 되돌림 | **실패** 「등급에서 빼는 갈래가 있다 — ('taste',)」 |
| DB 만 `grade_base=599` 로 되돌림 | **실패** 「DB 분모 599 인데 config 는 675 다 (recalc 를 안 돌렸다)」 |
| 되돌린 뒤 | **통과** |

### ④ 폐기된 표를 보던 검사 셋을 5장-2a 로 돌렸다

규격이 **「★ 아래 표가 정본이다. 이 절 아래의 옛 배점은 폐기다」**(5장-2a)라
적었는데, 검사 셋이 그 **폐기된 아래쪽 표**를 읽고 있었다.

| 검사 | 전 — 무엇을 보고 있었나 | 후 |
|---|---|---|
| `V3-66` | f-table 아래쪽 **옛 곡선 표**와 대조 → 9곡선 중 7 거짓 실패 | 곡선의 **최대값이 그 축 배점과 같은가** (8곡선) |
| `V3-77` | f-table 아래쪽 **옛 갈래 요약 줄**(⑤ 사이트 보증 50) | 5장-2a 갈래 표의 **「후」 칸** (5갈래) |
| `V9-10` | 사이트 보증 항목을 **더해서** 만점과 비교 | 개정 428 은 **단계**다 — 맨 위 단계(52)가 만점인가 · 내림차순인가 |

`V9-10` 은 그냥 두면 52+40+26 = 118 이 되어 만점 52 를 넘는다.

### ⑤ 배점을 코드에 박아 둔 시험 넷을 config 참조로 바꿨다

```
tests/test_score.py   「제조사 보증은 일반 20 + 동력계 30」 → 갈래 나뉨 + 합 78
                      「HUD 095 → 취향 15점」            → POLICY.comp("taste.hud")
                      「taste.hud == 15」(파서 두 형태)   → POLICY.comp 참조
                      「색은 등급(505)에 안 들어간다」     → 개정 431 로 들어간다
tests/test_report.py  「value.market(100)·taste.hud(15)」 → config 에서 읽는다
```

★ 숫자를 박아 두면 **배점이 바뀔 때마다 시험이 거짓으로 죽는다.**
개정 428 에서 24축이 전부 바뀌었는데 시험은 개정 365 숫자를 붙들고 있었다.

---

## 4. ★★ 오늘의 핵심 숫자 — 등급 분포 전후

| 등급 | 전 (분모 599 · 취향 제외) | **후 (분모 675 · 취향 포함)** |
|---|--:|--:|
| S | 0 | **0** |
| A | 0 | **0** |
| B | 51 | **59** |
| C | 181 | **206** |
| D | 2,833 | **2,800** |
| E | 776 | **776** |
| **A 이상** | **0** | **★ 0** |

**분모를 넓혔는데도 A 이상이 한 건도 안 나온다.**

### 맨 위 5건 — A 문턱(540점 = 80%)까지

| # | 매물 | 점수 | 등급 | A 까지 |
|--:|---|---|---|--:|
| 1 | 7781 | 531.8/675 = **78.8%** | B | **8.2점** |
| 2 | 2593 | 528.8/675 = 78.3% | B | 11.2점 |
| 3 | 1984 | 526.8/675 = 78.0% | B | 13.2점 |
| 4 | 8126 | 526.8/675 = 78.0% | B | 13.2점 |
| 5 | 1897 | 524.8/675 = 77.7% | E | 15.2점 |

### 왜 8점이 모자란가 — 갈래별 채움율 (중앙값)

| 갈래 | 중앙값 | 채움율 | 최대 |
|---|---|--:|--:|
| 값 250 | 76.0 | **30.4%** | 219.0 |
| 차량 150 | 70.0 | 46.7% | 142.0 |
| 제조사 보증 78 | 47.0 | 60.3% | 78.0 |
| 사이트 검증 52 | 52.0 | **100.0%** | 52.0 |
| 취향 145 | 91.0 | 62.8% | 140.0 |

```
★ 막고 있는 것은 취향이 아니라 ** 값 갈래 250점**이다 — 중앙 30.4%.
  개정 431 로 취향을 넣어도 A 가 안 나오는 이유가 여기 있다
★ 차량 최대가 142/150 이다 — 150 만점을 받은 매물이 하나도 없다
```

---

## 5. ★ 「사고 없다고 A」가 안 나오는지 — 표본 5건

**사고 이력이 깨끗하고 검증 만점인 매물 5건**을 골랐다.
다섯 다 `차량 142/150` · `사이트 검증 52/52` 로 「사고 없음」의 만점권이다.

| 매물 | 등급 | 점수 | 값 | 차량 | 보증 | 검증 | 취향 |
|---|---|---|--:|--:|--:|--:|--:|
| 1897 | **E** | 524.8/675 = 77.7% | 123/250 | 142/150 | 77/78 | 52/52 | 131/145 |
| 8167 | **B** | 520.8/675 = 77.2% | 118/250 | 142/150 | 78/78 | 52/52 | 131/145 |
| 2414 | **B** | 513.8/675 = 76.1% | 116/250 | 142/150 | 76/78 | 52/52 | 128/145 |
| 5749 | **B** | 511.5/675 = 75.8% | 117/250 | 142/150 | 78/78 | 52/52 | 122/145 |
| 2203 | **B** | 510.8/675 = 75.7% | 128/250 | 142/150 | 61/78 | 52/52 | 128/145 |

```
★ 다섯 다 A 가 아니다.  「사고 없다고 A」는 나오지 않는다 — 확인했다
★ 1897 은 77.7% 인데 E 다.  절대 탈락(absolute_fail)이 걸렸기 때문이다 —
  점수가 높아도 관문에서 떨어진다.  이 갈래가 살아 있다
★ 다섯 다 값 갈래가 116~128/250 이다.  값이 A 를 막는다
```

---

## 6. ★ 가이드가 틀린 곳 · 규격에 안 적힌 것

### ① ★★ 가이드 검산 일곱 줄 중 셋이 **정확히 +13** 어긋난다 (`V3-91`)

```
V3-91  3줄 다름
  「보증X · 검증X · 값·취향 만점」   표 532점  ·  config 545점   (+13)
  「검증만 · 값·취향 만점」          표 584점  ·  config 597점   (+13)
  「다 갖춤」                        표 662점  ·  config 675점   (+13)
  「값 0 · 나머지 만점」             표 425점  ·  config 425점   ★ 딱 맞는다
```

**값이 0 인 줄만 정확히 맞는다.** 나머지 셋은 전부 +13 이다.
→ 어긋남이 **값 갈래 250점 안에** 있다. 값 축 넷(시세 100 · 신차가 80 ·
주행 40 · 옵션가 30)의 합은 250 인데, 가이드가 검산할 때 쓴 값 만점이 237
이었던 것으로 보인다 (250 − 13).

```
★ 배점을 만지지 않았다 (지시).  V3-91 을 WARN 으로 두었다 —
  FATAL 로 두면 「가이드 표가 틀렸다」는 이유로 매 검사가 붉어진다.
  어느 쪽이 정본인지 정해 주시면 그때 FATAL 로 올리겠다
```

### ② ★★ 개정 431 이 마스터의 「깡통에 HUD」 지적을 되살렸다

개정 292 는 취향을 등급에서 빼서 **「깡통에 HUD 만 있어도 만점」을 막고
있었다.** 개정 431 이 취향을 등급에 넣으면서 그 막이 없어졌다.

실측 (`tests/test_score.py` 사양·취향) —

```
G80_25T 트림 사다리 4,000 / 5,000 / 6,000 / 7,000만

  깡통(맨 아래) + HUD    spec.trim 21.7  taste.hud 23   →  등급점 67.7
  풀옵션(맨 위) · HUD 없음  spec.trim 38.0  taste.hud  0   →  등급점 61.0

★ 트림 사다리 맨 아래↔맨 위 격차 16.3점  <  HUD 한 옵션 23점
  그래서 「깡통에 HUD」가 「풀옵션에 HUD 없음」을 6.7점 이긴다
```

★ **배점을 만지지 않았다.** 시험을 실측대로 고치고 여기 적는다.
`taste.hud` 23 이 `spec.trim` 38 의 격차보다 큰 것이 맞습니까.

### ③ ★★ 규격에 안 적힌 것 — 5장-2a 아래의 옛 표가 안 지워졌다

`docs/chapters/30-score/f-table.md` 는 5장-2a 에서 「이 절 아래의 옛 배점은
폐기다」라 적었는데, **아래쪽 옛 표가 그대로 남아 있다.**

```
남아 있는 것   옛 곡선 표 9개 (general 20 · power 30 · accident 40 …)
              옛 갈래 요약 줄 (⑤ 사이트 보증 50 · ⑦ 제조사 보증 50)
              옛 24축 표 (번호 매긴 것)
겪은 일        검사 V3-66 · V3-77 이 그 폐기된 표를 읽고 거짓 실패를 냈다
              — 코드는 정상인데 검사가 붉었다
★ 규칙 2 로 제가 docs 를 못 고칩니다.  지워 주시겠습니까
```

### ④ ★ `check_src` `S35-1` 이 **가이드의 커밋을 제 것으로 셉니다** (제 결함)

```
S35-1  ✗ 개발측이 지시서를 고쳤다 — docs/guide/00_버전.md
실제   그 커밋 10cff0c 는 git reflog 상 「pull -q: Fast-forward」 —
      가이드가 GitHub 에 올린 것이지 제가 고친 것이 아닙니다
원인   S35-1 이 마지막 커밋만 보고 「받아온 커밋」과 「제 커밋」을 안 가릅니다
★ 이건 제 도구 결함입니다.  따로 커밋해 고치겠습니다 (이 묶음에 섞지 않습니다)
```

---

## 7. 조치 후 결과 — ★ 실제 출력

```
python3.11 tools/check_all.py

통과 329 · fatal 4 · warn 10 · 환경 차이 1 · 미실행 7

■ FATAL — 고쳐야 한다
  V1-21     받아 두고 안 펼쳐진 원문이 없음 — 1
  V1-23     필요한 조합 대비 받은 카탈로그 비율 — 127조합 · 81.4% · 카탈로그 없는 매물 587건
  V11-60    시안 CSS 를 다시 만들지 않음 — 다름
  V11-76    화면 크기·시간이 상한 안 — 1곳

■ warn — 진행은 된다
  V1-07     매물별 엔드포인트 4종 상태 존재 — 3302
  V4-06     RAW 경로가 등록부에 있는가 — 1
  V4-06b    등록부에 있는데 RAW 에 없는 유령 경로 — 48
  V4-11b    판정에 안 쓰는 미분류 경로 — 318
  V4-21     같은 이름의 공개 함수가 두 모듈에 없음 — 1
  V3-39     이론가와 실제 중앙값의 차가 상한 안 — 최대 13.2%
  V3-03     축별 source 값 종류 >= 2 — 2건
  V3-04     축별 값 종류 >= 2 — 2건
  V3-85     옵션 보정 없이 원 중앙값으로 견준 매물 — 40/3841
  V3-91     가이드 검산 일곱 줄이 표대로 나옴 — 3줄 다름   ★ 6절 ①
```

```
python3.11 tools/run_tests.py
결과: 실패 — test_endtoend.py

★ test_endtoend 의 실패 둘은 배점과 무관하다 —
  「중단 없이 끝난다」 ['S11: V11-60 시안 CSS 를 다시 만들지 않음: 다름']
  「fatal 실패가 없다」 ['V11-60', 'STEP53-S11']
  V11-60 은 순서 5(화면 전면 개편)가 손댈 자리다.  거기서 닫는다
```

| | 전 | 후 |
|---|--:|--:|
| check_all 통과 | 325 | **329** |
| fatal | 7 | **4** |
| warn | 9 | **10** (V3-91 신설) |
| 시험 실패 | 3 | **1** |
| `grade_base` | 599 | **675** |
| **A 이상** | **0** | **★ 0** |

---

## 8. 못 한 것 · 막힌 것 · 판단이 필요한 것

| # | 무엇 | 상태 |
|---|---|---|
| ① | 가이드 검산 3줄이 +13 어긋남 | **여쭙니다** (6절 ①) — 배점 안 만짐 |
| ② | 「깡통에 HUD」가 되살아남 | **여쭙니다** (6절 ②) — 배점 안 만짐 |
| ③ | f-table 폐기 표가 안 지워짐 | **여쭙니다** (6절 ③) — 규칙 2 로 제가 못 고침 |
| ④ | `S35-1` 이 가이드 커밋을 제 것으로 셈 | 제 결함 — **따로 커밋해 고칩니다** |
| ⑤ | `V11-60` 시안 CSS | 순서 5 에서 닫습니다 |
| ⑥ | 순서 3 틴팅 | **내일** — 마스터가 리포트 주소를 주십니다. 지어내지 않았습니다 |
| ⑦ | 순서 5~10 | 아직입니다. 순서 5 부터 이어서 합니다 |

---

## 9. 고친 파일

```
analyze/axes.py            GRADE_EXCLUDED_AXES = ()          (개정 431)
config/scoring.json        grade_base_points 625 → 675
validate/v3_logic.py       V3-90 · V3-91 신설
                           V3-66 을 5장-2a 축 배점과 대조하게
                           V3-77 을 5장-2a 갈래 표 「후」 칸을 읽게
validate/v9_multisite.py   V9-10 을 단계(warranty_grades)로   (개정 428)
tests/test_score.py        박아 둔 배점 넷을 config 참조로
tests/test_report.py       csv 머리말 배점을 config 에서
```

**커밋** `b69708f`
