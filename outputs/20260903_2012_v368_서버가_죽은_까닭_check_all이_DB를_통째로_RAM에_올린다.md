# 서버가 죽은 까닭 — `check_all` 이 운영 DB 를 통째로 RAM 에 올린다

작성 2026-09-03 20:12 · 소스 v368 · 작성자 개발측

> **한 줄** — 장비가 메모리를 다 써서 먹통이 됐고 사람이 전원을 눌러 껐다.
> 다 쓴 쪽은 **carwatch 서비스가 아니라 내 세션**이고,
> 그 안에서 가장 크게 먹은 것은 **규칙 3 이 돌리라고 한 `tools/check_all.py`** 다.
> 뿌리는 `validate/v7_watch.py:137` — 검사 **한 건**을 하려고
> 운영 `carwatch.db` **1.08 GiB 를 `:memory:` 로 복사**한다. 장비 RAM 은 1.8 GB 다.

---

## 1. 무엇을 하려 했나 (지시 원문)

> 「서버가 왜 죽었는지 먼저 분석해 이유는 너가 직전작업이 원인이야.
>  그거 리포트 만들어서 가이드가 알도록 해」

---

## 2. 장비

| 항목 | 값 |
|---|---|
| 인스턴스 | EC2 `t4g.small` · 2 vCPU |
| RAM | **1,841 MB** |
| swap | 2,047 MB |
| RAM+swap 합 | **3,888 MB** |
| `carwatch.service` | `MemoryMax=infinity` · `OOMScoreAdjust=-500` · `Restart=always` |
| `user-1000.slice` (내 셸) | `MemoryMax=infinity` — ★ **한도가 없다** |

---

## 3. 죽은 경과 — 2026-09-03

| 시각 | 일 | 근거 |
|---|---|---|
| 16:25:20 | **OOM ①** `python3.11` pid 1127871 anon-rss **916 MB** 를 커널이 죽였다 | `kernel: Out of memory: Killed process 1127871` |
| 17:30:39 | 내가 `carwatch` 를 재시작 → 큰 재판정 판 `290327d5` 가 떴다 (S6 142,986 · S9 441,028 · S10 15,751) | 커밋 `f8adddc6` |
| 18:28:39 | **OOM ②** `python3.11` pid 1137373 anon-rss **972 MB**.  이때 `Free swap = 0kB` | `kernel: Out of memory: Killed process 1137373` |
| 18:44:49 | carwatch 마지막 로그 (`S6 142960 sellingpoint 파싱`).  **이후 한 줄도 없다** | `journalctl -u carwatch` |
| 18:56 | Caddy 응답이 **31.2초** | `caddy[1839] duration:31.202373345` |
| 18:59 | `/admin/query` 응답 **113.2초** | `caddy[1839] duration:113.160179251` |
| 19:13:41 | 장비가 `169.254.169.254`(IMDS) 이름풀이도 **5,929ms 만에 실패** | `ec2net: curl: (28) Failed to resolve host` |
| 19:18:59 | **사람이 전원 버튼을 눌렀다** | `systemd-logind: Power key pressed short` |
| 19:19:41 | 정상 종료조차 시간초과 — `poweroff.target` 을 못 읽었다 | `systemd-logind: Failed to get load state of poweroff.target: Connection timed out` |
| 19:23:00 | 부팅 | `journalctl --list-boots` |
| 19:25:23 | 다시 부팅 (지금 도는 판) | 〃 |

★ 즉 장비는 **꺼진 게 아니라 스래싱으로 먹통이 됐고**, 사람이 손으로 껐다.
  18:44 부터 19:19 까지 **35분** 동안 마스터 화면이 사실상 죽어 있었다.

---

## 4. 누가 메모리를 먹었나 — OOM ② 시점 실측

커널이 남긴 프로세스 표를 그대로 옮긴다 (4KB 쪽 → MB 환산).

| pid | 이름 | RSS | swap | 합 | 누구 |
|---|---|--:|--:|--:|---|
| 1137373 | `python3.11` | 950 MB | 165 MB | **1,115 MB** | ★ 내 셸 |
| 329022 | `2.1.250` | 461 MB | 492 MB | **953 MB** | ★ claude CLI |
| 2377 | `claude` | 62 MB | 573 MB | **635 MB** | ★ claude CLI |
| 1137576 | `python3.11` | 83 MB | 72 MB | 155 MB | ★ 내 셸 |
| 329125 | `claude` | 28 MB | 84 MB | 112 MB | ★ claude CLI |
| 329016 | `2.1.250` | 9 MB | 42 MB | 51 MB | ★ claude CLI |
| | **내 세션 합** | | | **3,021 MB** | **장비 3,888 MB 의 78%** |
| 1134636 | `python3.11` | 30 MB | 234 MB | 264 MB | carwatch.service |

★★ **carwatch 서비스는 범인이 아니다.** 264 MB 를 썼고 `oom_score_adj = -500` 으로 지켜져 있었다.
   커널이 죽인 두 프로세스는 모두 `user.slice/user-1000.slice/session-1.scope`,
   즉 **내가 띄운 것**이다 (`oom_score_adj = 0`).

---

## 5. 뿌리 — `check_all` 최대 RSS 904 MB

### 5-1. 도구를 재 봤다

```
$ systemd-run --user --scope -p MemoryMax=900M /usr/bin/time -v python3.11 tools/check_all.py

통과 516 · fatal 19 · warn 13 · 환경 차이 2 · 미실행 9
Elapsed (wall clock) time: 18:53.62
Maximum resident set size (kbytes): 925744        ←  ★ 904 MB
```

★ OOM 으로 죽은 두 프로세스가 916 MB · 972 MB 였다. **같은 크기다.**

> 처음에 `ps` 로 8초마다 표본을 떠 52 MB 만 보고 「check_all 은 범인이 아니다」라고
> 적었다 — 틀렸다. 19분 실행의 **뒤쪽에서만** 치솟기 때문에 표본에 안 걸렸다.
> `/usr/bin/time -v` 의 최고점으로 다시 재서 바로잡았다.

### 5-2. 어느 단계가 먹는지 단계마다 따로 쟀다

| 단계 | 최대 RSS | 걸린 시간 | 검사 수 |
|---|--:|--:|--:|
| **V7** | **1,194 MB** | 14.6초 | **13건** |
| V11 | 244 MB | 115.6초 | 130건 |
| V4 | 141 MB | 32.7초 | 27건 |
| V1 | 96 MB | 29.0초 | 57건 |
| V10 | 56 MB | 17.6초 | 37건 |
| V2 | 42 MB | 2.0초 | 31건 |
| V3 | 34 MB | 10.1초 | 73건 |
| V9 | 31 MB | 7.1초 | 6건 |
| V5 | 26 MB | 0.1초 | 12건 |

★ **V7 하나가 검사 13건에 1,194 MB 를 쓴다.** 나머지를 다 합쳐도 그 절반이 안 된다.

### 5-3. 그 줄

`validate/v7_watch.py:136-138` — `_progress_note_check()` (검사 `V7-15`)

```python
    # ★ 운영 DB 를 건드리지 않는다.  메모리 사본에서 시험한다
    probe = _s.connect(":memory:")
    conn.backup(probe)
```

* 뜻은 옳다 — 운영 DB 를 안 건드리려는 것이다.
* 그런데 옮기는 것이 **운영 `carwatch.db` 통째**다.
  지금 `1,156,988,928` 바이트 = **1.08 GiB**. 장비 RAM 은 1,841 MB 다.
* 목적은 **「진행 메모를 자유롭게 적을 수 있는가」 한 건**을 시험하는 것이다.
  그 한 건 때문에 1.08 GiB 를 RAM 에 올린다.

### 5-4. 같은 저장소 안에 이미 옳은 꼴이 있다

| 자리 | 어디로 복사하나 | 최대 RSS |
|---|---|--:|
| `validate/v7_watch.py:137` | `:memory:` ★ | 1,194 MB |
| `validate/v10_admin.py:346` | `:memory:` ★ | — |
| `validate/v10_admin.py:676` | **파일** `_scratch()/pii.db` | 56 MB |
| `validate/v11_web.py:5034` | **파일** `_scratch()/probe.db` | 244 MB |

★ 파일로 복사하는 두 곳은 아무 문제가 없다. `:memory:` 두 곳만 위험하다.

### 5-5. 왜 하필 지금 터졌나 — DB 가 다시 자랐다

* 08-28 커밋 `1a6cf69f` 「STEP 33: 원문을 눌러 둔다 — **DB 1.01GB → 528MB**」
* 오늘 09-03 `carwatch.db` = **1.08 GiB** (`raw_response` 372,571행 · 상세 ok 32,506건 295 MB)

★ **눌러 놓은 DB 가 엿새 만에 원래 크기로 돌아왔다.**
  이 검사가 쓰는 메모리는 **DB 크기에 정비례**하므로,
  DB 가 대략 900 MB 를 넘어선 순간부터 `check_all` 은 이 장비에서 터지게 돼 있었다.
  08-28 에 안 터지고 09-03 에 터진 까닭이 이것이다.

---

## 6. 내 잘못 — 직전 작업이 어떻게 방아쇠를 당겼나

1. **규칙 3·4 를 그대로 지킨 것이 방아쇠였다.**
   「고친 뒤 반드시 `check_all` 을 돌린다」를 따랐는데, 그 도구가 904 MB 를 든다.
   09-03 하루에 최소 두 번(16:25 · 18:28) OOM 이 났다.
2. **17:30:39 에 carwatch 를 재시작**해 큰 재판정 판(`290327d5`)을 띄워 놓고,
   그 판이 도는 **위에서** `check_all` 을 돌렸다. 둘이 같은 1.8 GB 를 나눠 썼다.
3. **내 세션이 장비 메모리의 78%(3,021 MB / 3,888 MB)를 쥐고 있었다.**
   `user-1000.slice` 에 한도가 없어 내 도구가 장비 전체를 끌어내렸다.
4. **16:25 에 이미 OOM 이 한 번 났는데 나는 그것을 못 보고 18:28 에 똑같이 되풀이했다.**
   `journalctl -k | grep -i oom` 을 그날 한 번도 안 봤다. 이것이 가장 나쁜 대목이다.
5. 커밋 `f8adddc6` 에서 나는 「재시작 전 마스터 POST 0건을 먼저 확인했다」고 적었다.
   **POST 는 봤지만 메모리는 안 봤다.** 지켜야 할 것을 반만 지켰다.

---

## 7. 덜 중요하지만 함께 본 것

* **로그 폭주** — S6 이 20행마다 한 줄을 찍는다. 18:44:49 **한 초에 88줄**,
  17:52~18:45 에 **7,150줄**. 지금 저널이 디스크에서 **697 MB** 다.
  죽인 원인은 아니지만 스래싱 중인 장비에 디스크 쓰기를 더 얹었다.
* **외부 크롤러** — GPTBot(`74.7.227.51`)이 18:56~18:59 에
  `/listings?axis=taste.size&bucket=1&page=187~194` 를 계속 긁고 있었고
  응답이 30초씩 걸렸다. 죽은 원인은 아니나 죽어 가는 장비에 얹힌 짐이다.
* **수집기의 같은 꼴** — `tools/collect_kbchachacha.py:440` 의 `rows = []` 는
  원문을 **전부 담고 나서** 처리한다. 실측 **원문 하나당 488 KB** ·
  KB 297건 = 142 MB. 되살린차는 상세 1,158건이라 같은 식이면 **565 MB** 다.
  같은 꼴이 `collect_revolt.py:154` · `collect_lexus.py:88` · `collect_bmw.py:121` ·
  `collect_kia_cpo.py:132` · `load_raw.py:173` 에도 있다.
  이번 사고의 원인은 아니지만 **다음 사고의 씨앗**이라 함께 적는다.

---

## 8. 1차 결과 / 조치 후 결과

★ **이 회차에서 소스를 한 줄도 안 고쳤다.** 지시가 「분석하고 리포트를 내라」였기 때문이다.
  그래서 아래 숫자는 「고치기 전 / 고친 뒤」가 아니라 **지금 상태의 실측**이다.

```
python3.11 tools/check_all.py
  통과 516 · fatal 19 · warn 13 · 환경 차이 2 · 미실행 9
  최대 RSS 925,744 kB (904 MB) · 18분 53초
```

```
python3.11 tools/run_tests.py
  결과: 실패 — test_endtoend.py
    test_endtoend  FAIL  fatal 실패가 없다  ['V1-27', 'V4-28', 'V3-77', 'V11-34']
  나머지 15파일 + ruff 통과
  최대 RSS 107,892 kB (105 MB) · 2분 12초
```

★ `run_tests` 는 **105 MB** 다. 이 사고와 관계없다.
  메모리를 먹는 것은 `check_all` 한쪽뿐이다.

---

## 9. 못 한 것 · 판단이 필요한 것

### 9-1. 고치지 않았다 — 가이드의 판단을 기다린다 (규칙 2)

| # | 무엇 | 왜 내가 안 정하나 |
|---|---|---|
| ① | `validate/v7_watch.py:137` 의 `:memory:` 를 **파일 사본**으로 바꾼다 (`v10:676` · `v11:5034` 과 같은 꼴). `v10_admin.py:346` 도 같다 | 검사의 뜻(운영 DB 를 안 건드린다)은 그대로 지켜지지만, **검사 코드를 바꾸는 일**이라 규격 확인이 필요하다 |
| ② | `user-1000.slice` 에 `MemoryMax` 를 건다 (예: 1,200 MB) | 걸면 내가 도구를 잘못 돌려도 **장비가 아니라 내 도구가 죽는다**. 배포 설정을 바꾸는 일이다 |
| ③ | DB 를 다시 누른다 — 08-28 `1a6cf69f` 의 「원문 누르기」 | 1.01GB→528MB 로 줄였던 것이 엿새 만에 1.08GB 로 돌아왔다. **누르기를 회차마다 돌릴 것인지**가 판단거리다 |
| ④ | 규칙 3 의 「고칠 때마다 `check_all`」 | 19분 · 904 MB 다. 이대로면 고칠 때마다 장비가 위태롭다. ①을 먼저 고치면 사라지는 문제라 ① 뒤에 다시 본다 |

### 9-2. 확인하지 못한 것

* pid 1137373(950 MB)이 **정확히 어느 명령이었는지**는 장비가 재부팅돼 확인할 수 없다.
  `check_all` 의 실측 최고점(904 MB)과 크기가 같고 시각도 맞지만,
  **「그것이었다」고 단정하지 않는다.** 크기·시각·주체(내 세션)까지가 확인된 사실이다.
* 16:25 의 pid 1127871(916 MB) 도 같다.

### 9-3. 남은 실패 (이 사고와 별개)

`run_tests` 가 `test_endtoend.py` 하나에서 실패한다 —
「fatal 실패가 없다」인데 `V1-27 · V4-28 · V3-77 · V11-34` 가 fatal 로 남아 있다.
`check_all` 의 fatal 19건과 같은 뿌리다. **이 사고와는 별개**이고 이 회차에서 손대지 않았다.

---

## 10. 커밋

`ad9604f3` — 장애: 서버가 죽은 까닭 — check_all 이 운영 DB 1.08GiB 를 :memory: 로 올린다
  (리베이스로 밀림 — 처음 적었던 `16c49ef9` 는 가이드의 `83ee505b` 를 당기며 `ad9604f3` 가 됐다)

---

## 11. ★ 덧붙임 — 배포는 **두 번** 죽었다.  두 번째는 주소가 바뀐 것이다

리포트를 커밋한 뒤 `git pull` 로 가이드의 `83ee505b`
「배포가 503 이다 — 이것부터다」를 받았다. 지시 0 대로 **배포를 두드려 봤더니 여전히 안 열렸다.**
까닭이 위의 메모리 사고와 **별개**였다.

### 11-1. 실측

```
$ curl -m 30 https://43.201.16.78.sslip.io/listings      →  000 · 30초 무응답
$ curl -m 25 http://127.0.0.1:8765/listings              →  200 · 1.5초   ★ 앱은 멀쩡하다
```

★ 앱은 살아 있는데 바깥 주소만 안 열렸다.

```
$ curl http://169.254.169.254/latest/meta-data/public-ipv4
54.180.227.109                    ←  ★ 지금 공인 IP

$ config/deploy.json
"public_ip": "43.201.16.78"       ←  ★ 적혀 있던 것
```

### 11-2. 까닭

★★ **19:18:59 의 강제 전원 종료가 EC2 인스턴스를 stop/start 시켰고,
   탄력적 IP(Elastic IP)가 없어 공인 IP 가 바뀌었다.**
   `43.201.16.78` → `54.180.227.109`

주소가 `sslip.io` 라 **IP 가 주소 안에 들어 있다.** IP 가 바뀌면 주소 자체가 죽는다.
`/etc/caddy/Caddyfile` 이 `43.201.16.78.sslip.io` 로 못박혀 있어 Caddy 는
아무도 안 부르는 이름을 듣고 있었다.

### 11-3. 되살렸다

| 고친 곳 | 무엇 |
|---|---|
| `deploy/Caddyfile` | 이름을 `54.180.227.109.sslip.io` 로 (주석의 옛 IP 포함) |
| `/etc/caddy/Caddyfile` | 위 파일을 그대로 설치 |
| `config/deploy.json` | `public_ip` · `base_url` |
| — | `sudo systemctl reload caddy` → Let's Encrypt 인증서 새로 받음 (20:20:16 성공) |

**되살린 뒤 두드린 것 — `deploy.json` 의 `health_screens` 계약 그대로**

| 화면 | 규격 | 실측 |
|---|--:|--:|
| `/` | 200 | **200** (1.03초) |
| `/listings` | 200 | **200** (0.91초) |
| `/track` | 200 | **200** (0.62초) |
| `/notready` | 200 | **200** (0.21초) |
| `/login` | 200 | **200** (0.05초) |
| `/watch` | 403 | **403** |
| `/admin` | 403 | **403** |

★ 일곱 화면이 모두 규격과 같다. **배포가 살아났다** (2026-09-03 20:21 실측).

### 11-4. 가이드께 — 이것이 판단거리다

```
★ 이 장비에 탄력적 IP 가 없다.
★ 껐다 켤 때마다 주소가 또 바뀐다 — 오늘 같은 일이 그대로 되풀이된다
★ 탄력적 IP 를 붙이는 것은 ★ 콘솔 작업이라 ★ 마스터 몫이다 (개발측이 못 한다)
★ 붙이면  ① 주소가 고정되고 ② config/deploy.json 을 다시 고칠 일이 없고
         ③ 인증서를 다시 받을 일도 없다
```

★ 그리고 `docs/` 여러 곳에 옛 IP `43.201.16.78` 이 박혀 있다 —
  `docs/guide/00_인수인계.md` · `03_이력.md` · `05_가이드역할.md` · `06_오판대장.md` ·
  `08_인수인계.md` · `09_인계_20260829.md`.
  **규칙 2 대로 개발측이 안 고쳤다.** 보고만 한다.
  (`adapters/*.py` 의 옛 IP 는 「언제 어디서 쟀나」를 적은 주석이라 그대로 두는 것이 맞다.)

### 11-5. 두 사고의 관계

```
① 메모리   check_all 이 904MB → OOM → 18:44 부터 장비 먹통
                                    ↓
②          19:18:59 사람이 전원 강제 종료 (다른 수가 없었다)
                                    ↓
③ 주소     stop/start 로 공인 IP 가 바뀜 → 배포 주소가 죽음
```

★ ②③ 은 ① 의 **결과**다. ① 을 안 고치면 ②③ 도 되풀이된다.

