# ★ SPEC-2026.08.21-r434 — 순서 5 · 「확인 안 됨」을 ①②③ 으로 가른다 (개정 434)

2026-08-21 21:30 · v193

```
★ 오늘의 답 —  확인 안 됨 22,208건 중
                 ① 원문에 없다 (딜러)  17,235   77.6%
                 ★② 못 읽는다 (우리)    1,132    5.1%
                 ★③ 안 받는다 (우리)    3,841   17.3%
                   모르겠습니다             0    0.0%

★★ 우리 부실을 다 고쳐도 값 갈래 중앙은 27.6% → 28.0% 다 (+0.4%p)
   ★ 값 갈래가 낮은 것은 우리 탓이 아니다.  6절 ① 에 적었다
```

---

## 1. 무엇을 하려 했나 (지시 원문)

> **★★★ 「확인 안 됨」을 ①②③ 으로 가른다** — `inbox/ORDER_unknown_split.md`
> ★ 24축 전부. 판정 축만이 아니다 · ★ 원문 body 를 직접 열어 가른다.
> 파서 결과만 보지 마라 · ★ ② 면 어느 키에 있는지 원문 조각을 함께 내라
> ★ 「모르겠습니다」도 답이다 · ★ 이미 아는 여섯을 표에 반드시 넣어라
> ★ 낼 것 — ②③ 이 몇 건 · 몇 점어치 · 다 고치면 값 갈래 중앙이 얼마로 오르나
> V1-27 · V1-28 신설

---

## 2. 1차 결과 (고치기 전 숫자)

```
check_all   통과 330 · fatal 4 · warn 10
run_tests   실패 1 — test_endtoend.py
「확인 안 됨」  ★ 갈래가 없다.  한 덩어리다
V1-27 · V1-28  없음
```

---

## 3. ★★ 먼저 — 「확인 안 됨」이 DB 의 어디에 있나

지시가 「★ result_axis 의 어느 값이 「확인 안 됨」인가.
`source` · `prio` · `excluded` 중 무엇으로 가르나」를 먼저 밝히라 했습니다.

```
실측
  value IS NULL      108건
  excluded = 1       108건    ← 같은 108건이다
  source = 'missing' 15,817건  ★ 이쪽이 진짜다
  전체 축 행         92,184건
```

★ **「확인 안 됨」은 `source` 에 있습니다.** `value` 나 `excluded` 로는 못 가릅니다.

### ★★ 그래서 화면이 「모름」을 「없음」으로 내고 있었습니다

`report/screens/build.py:150` 이 **`value` 로** O · · · ? 를 가릅니다.

```python
elif value is None:  → ? 확인 못 함      ← 108건뿐이다
elif value > 0:      → O 있음
else:                → · 없음            ← ★ 여기에 15,709건이 들어간다
```

```
★ source='missing' 인 15,709건은 value=0 · excluded=0 이다
→ 화면이 「· 없음」으로 낸다.  실제로는 「? 모름」이다
```

★ **이것이 v1 사고와 같은 모양입니다** — 「없음」과 「모름」을 한 기호로 냅니다.
`build.py:159` 에 「★ 「없음」과 「모름」을 같은 기호로 내면 v1 사고가
되풀이된다」라 **적어 두고도** 그렇게 하고 있었습니다.
★ 순서 6 화면 개편에서 고치겠습니다 (6절 ⑤).

---

## 4. ★ 24축 표 — ①②③ 으로 가른 것

`python3.11 tools/unknown_split.py`

| 축 | 배점 | 확인 안 됨 | ① 원문에 없다 | **② 못 읽는다** | **③ 안 받는다** | 모름 |
|---|--:|--:|--:|--:|--:|--:|
| `history.lien` | 7 | 522 | 522 | 0 | 0 | 0 |
| `history.not_join` | 16 | 1,361 | 1,354 | **7** | 0 | 0 |
| `history.owner` | 10 | 1,354 | 1,354 | 0 | 0 | 0 |
| `history.usage` | 20 | 1,281 | 1,281 | 0 | 0 | 0 |
| `spec.options` | 38 | 40 | 40 | 0 | 0 | 0 |
| `spec.trim` | 52 | 48 | 48 | 0 | 0 | 0 |
| `state.accident` | 24 | 1,339 | 1,339 | 0 | 0 | 0 |
| `state.consumable` | 7 | 3,841 | 0 | 0 | **3,841** | 0 |
| `state.frame` | 20 | 1,729 | 1,729 | 0 | 0 | 0 |
| `state.integrity` | 3 | 3,841 | 3,166 | **675** | 0 | 0 |
| `state.leak` | 7 | 1,729 | 1,729 | 0 | 0 | 0 |
| `state.outer` | 13 | 1,729 | 1,729 | 0 | 0 | 0 |
| `state.repair` | 13 | 1,354 | 1,354 | 0 | 0 | 0 |
| `state.special` | 10 | 1,354 | 1,354 | 0 | 0 | 0 |
| `taste.color` | 13 | 28 | 28 | 0 | 0 | 0 |
| `taste.hud` | 16 | 40 | 40 | 0 | 0 | 0 |
| `taste.sunroof` | 10 | 40 | 40 | 0 | 0 | 0 |
| `value.depreciation` | 80 | 48 | 48 | 0 | 0 | 0 |
| `value.market` | 100 | 450 | 0 | **450** | 0 | 0 |
| `warranty.general` | 31 | 40 | 40 | 0 | 0 | 0 |
| `warranty.power` | 47 | 40 | 40 | 0 | 0 | 0 |
| **합계** | | **22,208** | **17,235** | **1,132** | **3,841** | **0** |

```
확인 안 됨이 아닌 것 셋 — 확인해서 「없다」로 안 것이다.  표에서 뺐다
  no_choice_option  810   선택 옵션이 없음을 확인했다
  no_warranty        40   보증이 없음을 확인했다
  nothing_picked  3,841   지정 옵션을 안 골랐다 — ★ 만점을 준다 (감점 아님)
축 셋은 확인 안 됨이 0 이다 — value.mileage · warranty.site · taste.picked
합 검산   ① + ② + ③ + 모름 = 확인 안 됨.  전 축에서 맞다 (V1-27 이 본다)
표본     state.integrity 은 400건을 열어 그 비율로 늘렸다 — ★ 「추정」이다
        history.lien 도 400건을 열었다 (②가 하나도 없어 전부 ①)
```

---

## 5. ★★ ② 원문에 있는데 못 읽는다 — 원문 조각

### ①-a `state.integrity` — 계기판 상태가 **원문에 있습니다** (675건 · 1점)

`analyze/axis/state.py:280` 이 **조건 없이** 「계기판 확인 못 함」을 답니다.

```python
# ★ 계기판 교환은 원문에 없다 — 그 2점은 확인 못 한 것이다
why.append("계기판 확인 못 함")
```

**원문을 800건 열어 보니 있습니다.**

```
inspection : master.detail.mileageStateType  →  800건 전부 null   ★ 코드 말이 맞다
inspection : master.detail.boardStateType    →  757/800 에 값이 있다
   ★ 원문 조각 (매물 208)
   master.detail.boardStateType = {"code": "1", "title": "양호"}
```

★ **키를 잘못 보고 있었습니다.** `mileageStateType` 은 비어 있지만
`boardStateType` 이 채워져 있습니다. **우리 잘못입니다.**

### ①-b `history.not_join` — `openData` 가 있는데 안 읽습니다 (7건 · 16점)

```
★ 원문 조각 (매물 5702)   record : openData = true
```

### ①-c `value.market` — ★ 「못 읽는다」와는 다릅니다 (450건 · 100점)

```
★ 이 매물의 값은 원문에 있다 (advertisement.price = 2400).
  같은 차종·트림·연식 매물이 ★ 표본 5건에 못 미쳐 우리가 중앙값을 못 만든다
★ 우리 잘못이지만 「원문을 못 읽는다」가 아니다 — 계산 쪽이다
★ 순서 8 「시세를 세 겹으로」(개정 430) 가 이것을 푼다
```

★ 지시의 ② 정의(「키는 있는데 파서가 `None` 을 받는다」)로는 이 갈래가
안 잡힙니다. `config/unknown_split.json` 에 `kind_override` 와 까닭을 적어
**짐작이 아니라 판단 근거를 남겼습니다** (6절 ②).

---

## 6-1. ★★ ③ 그 엔드포인트를 안 받는다

### `state.consumable` 타이어 트레드 — 3,841건 전건 (7점)

```
왜   ★ tire_tread_mm 을 **아무 파서도 안 채운다**.
     contracts.py:208 에 필드가 있고 analyze/axis/state.py:243 이 읽는데
     ★ 쓰는 곳이 없다.  그래서 늘 None → 전건 site_unavailable
받을 수 있나  ★ 아직 모릅니다 — 엔카 unified-report 가 401 입니다.
             마스터가 내일 주소를 주십니다.  ★ 주소를 지어내지 않았습니다
```

### ★ 함께 찾은 것 — `diagnosis` 3,302건을 안 받고 있습니다

```
raw_response   diagnosis ok 2,269 · not_found 113
판정 매물 3,841 중 ★ 3,302건은 diagnosis 를 **한 번도 안 불렀다**
   (V1-07 warn 「매물별 엔드포인트 4종 상태 존재 — 3302」이 이것이다)
★ 그런데 점수 영향은 0 이다 — core_diagnosis 를 읽는 축이 하나도 없다
   (analyze/ · store/ 어디서도 안 읽는다.  store/core.py:748 은 화면용이다)
★ 순서 11 「판넬 0개 1,458건」이 이 원문을 쓴다.  그때 필요해진다
```

---

## 6-2. ★ 이미 아는 여섯 — 지시대로 표에 넣었습니다

| # | 무엇 | 건수 | 갈래 | 왜 |
|---|---|--:|:--:|---|
| 1 | 틴팅·트레드·차 키 리포트 | 3,841 (축) | **③** | `unified_report` 401 · ★ 내일 주소 |
| 2 | 판넬 0개 | 1,458 | **③** | `diagnosis` 3,302건 미요청 · ★ 지금은 점수 영향 0 |
| 3 | 카탈로그 없는 매물 | 587 | **①** | 엔카가 `200 []` 를 준다 (v144 실측 · `empty` 21조합) |
| 4 | 받아 두고 안 펼친 원문 | 1 | **②** | `V1-21` — 우리가 안 펼쳤다 |
| 5 | 유령 경로 (등록부에 있는데 RAW 에 없음) | 48 | **①** | 사이트가 안 준다 — 등록부가 낡았다 |
| 6 | 판정에 안 쓰는 미분류 경로 | 318 | **②** | 원문에 있는데 우리가 안 쓴다 |

★ 2 는 「갈래는 ③ 인데 **점수 영향이 0**」이라 4절 표에 안 들어갑니다 —
축이 그 원문을 안 읽기 때문입니다. 여기 따로 적습니다.
★ 3 은 v144 에서 대표를 셋까지 다시 물어 「없다」로 확정한 것입니다 — ① 입니다.

---

## 7. ★★ ②③ 을 다 고치면 얼마나 오르나

### 점수어치 — ★ 상한입니다 (다 만점을 받는다고 볼 때)

| 갈래 | 건수 | 배점 상한 | **실제로 받을 만한 값** |
|---|--:|--:|--:|
| ② `state.integrity` | 675 | 2,025 | **675** (계기판은 3점 중 1점) |
| ② `history.not_join` | 7 | 112 | **91** (지금 받는 값 중앙 13) |
| ② `value.market` | 450 | 45,000 | **16,200** (지금 받는 값 중앙 36) |
| ③ `state.consumable` | 3,841 | 26,887 | **모릅니다** — 트레드 값을 아직 못 봤다 |
| **합** | **4,973** | **74,024** | **약 17,000 + 트레드 몫** |

```
매물당   17,000 / 3,841 = 4.4점  =  675 의 0.65%p
트레드까지 다 받으면  (17,000 + 26,887) / 3,841 = 11.4점  =  1.7%p
```

### ★★ 값 갈래 중앙이 얼마로 오르나 — **거의 안 오릅니다**

```
지금   중앙 69.0 / 250 = 27.6%
고치면 중앙 70.0 / 250 = 28.0%      ★ +0.4%p
왜     값 갈래에 걸린 우리 부실은 value.market 450건뿐이다.
      450 은 3,065건의 14.7% 라 ★ 중앙값을 못 움직인다
```

### 등급 분포는 어떻게 바뀌나 (추정)

```
매물당 +4.4점 (트레드 빼고) 이면 컷 하나를 넘는 매물은 소수다
A 이상 67건 → ★ 70건 안팎으로 본다.  ★ 그림이 안 바뀐다
★ 이것은 추정이다.  ②③ 을 실제로 고친 뒤 recalc 해서 다시 재겠다
```

---

## 8. ★ 고칠 목록 — 점수 영향이 큰 순

| 순위 | 갈래 | 축 | 건수 | 실제 회복 | 무엇을 하나 | 드는 것 |
|--:|:--:|---|--:|--:|---|---|
| 1 | ③ | `state.consumable` | 3,841 | ≤26,887 | ★ 리포트 주소를 받아 트레드를 읽는다 | **막힘 — 내일 주소** |
| 2 | ② | `value.market` | 450 | 16,200 | 시세를 세 겹으로 (차종+연식+트림) | 순서 8 이 한다 |
| 3 | ② | `state.integrity` | 675 | 675 | `boardStateType` 을 읽는다 | ★ 작다 — 파서 한 줄 |
| 4 | ② | `history.not_join` | 7 | 91 | `openData` 를 읽는다 | 작다 |

```
★ 드는 품이 아니라 점수 영향으로 줄 세웠습니다 (지시)
★ 3·4 는 품이 가장 작은데 순위가 아래입니다 — 그래도 이 차례가 맞습니다
★ 이번 묶음에서 고치지 않았습니다.  「★ 갈래를 정하지 않고 코드를 고치는 것」이
  금지라, 먼저 갈래를 내고 판단을 받으려 합니다 (9절 ①)
```

---

## 9. ★ 가이드가 틀린 곳 · 규격에 안 적힌 것

### ① ★★ 「우리 부실을 컷으로 덮는 것이 된다」 — 실측은 아닙니다

지시서 0절이 이렇게 적었습니다.

> 실측 (v191) — 값 갈래 중앙 30.4% 라서 A 이상 0건이었다
> ★ 그 30.4% 중 얼마가 우리 탓인지 지금은 모른다
> ★ 그 전에 컷을 내리면 ★ 우리 부실을 컷으로 덮는 것이 된다

**쟀습니다. 우리 탓은 값 갈래에서 +0.4%p 뿐입니다.**

```
값 갈래 확인 안 됨   value.market 450 (②)  ·  value.depreciation 48 (①)
                    ★ 그 밖은 없다
다 고쳐도            27.6% → 28.0%
★ 값 갈래가 낮은 것은 우리 부실이 아니다 —
  개정 419 가 「시세와 같으면 0점」이라 정했기 때문이다.  ★ 시장이 그런 것이다
```

★ **컷을 내린 것이 우리 부실을 덮은 것이 아닙니다.** 그 걱정은 실측으로
해소됩니다. 다만 「사고 없다고 A」는 여전히 남습니다 (v192 기록 6절 ①).

### ② ★ ② 의 정의가 한 갈래를 못 담습니다

```
지시   ② 원문에 있는데 못 읽는다 — 키는 있는데 파서가 None 을 받는다
실측   value.market 450건은 ★ 이 매물의 원문에는 값이 다 있다.
      ★ 다른 매물이 모자라 우리가 중앙값을 못 만든다
      → 「우리 잘못」인데 「못 읽는다」가 아니다
```

★ 「계산 쪽 부실」을 ④ 로 따로 두시겠습니까. 지금은 `kind_override` 로
② 에 넣고 까닭을 config 에 적어 두었습니다.

★ 반대 갈래도 있었습니다 — `history.lien` 482건은 원문에
`condition.seizing = {"seizingCount": null, "pledgeCount": null}` 로
**키는 있는데 값이 null** 입니다. 지시 문구대로면 ② 인데, 실제로는
사이트가 값을 안 준 것이라 **① 로 넣었습니다.**
★ 「키가 있다」가 아니라 **「값이 있다」**로 갈랐습니다.

### ③ ★ 규격에 안 적힌 것 — 「확인 안 됨」이 `source` 에 있습니다

```
규격    「못 본 축은 0점 + 확인 안 됨」 (개정 289 · 325)
안 적힌 것  ★ 그것이 DB 의 어디에 남는지가 안 적혀 있다
실측    source 에 있다.  value · excluded 로는 못 가른다 (3절)
결과    ★ 화면이 15,709건을 「없음」으로 내고 있었다
```

### ④ ★ 규격에 안 적힌 것 — 죽은 필드 `tire_tread_mm`

```
contracts.py:208     tire_tread_mm 필드가 있다
analyze/axis/state.py:243  축이 그것을 읽는다
★ 쓰는 곳이 없다 — 어느 파서도 안 채운다
결과   7점짜리 축이 3,841건 전건 0점인데 ★ 아무 검사도 안 걸렸다
```

★ **「읽기만 하고 아무도 안 쓰는 필드」를 잡는 검사가 없습니다.**
`V1-26`(축이 통째로 비지 않음)은 `excluded` 로 보는데 이 축은
`excluded=0 · value=0` 이라 안 걸립니다. 검사를 만들까요.

### ⑤ 화면이 「모름」을 「없음」으로 냅니다 — 15,709건

3절에 적었습니다. ★ 순서 6 화면 개편의 상세 ⑤절 「아직 모르는 것」이
받을 자리입니다. **이번 묶음에서 화면은 안 고쳤습니다.**

---

## 10. 조치 후 결과 — ★ 실제 출력

```
python3.11 tools/check_all.py

통과 331 · fatal 4 · warn 10 · 환경 차이 1 · 미실행 10

■ FATAL
  V1-21     받아 두고 안 펼쳐진 원문이 없음 — 1
  V1-23     필요한 조합 대비 받은 카탈로그 비율 — 127조합 · 81.4%
  V11-60    시안 CSS 를 다시 만들지 않음 — 다름          ★ 순서 6
  V11-76    화면 크기·시간이 상한 안 — 2곳               ★ 실행마다 다름 (v192 6절 ④)

V1-27  통과  24축 · 확인 안 됨 22208 · ② 1132 · ③ 3841
V1-28  미실행 1회째다 — 다음 실행부터 견준다
```

```
python3.11 tools/run_tests.py
결과: 실패 — test_endtoend.py     ★ V11-60 하나 (순서 6)
```

**검사를 되살려 확인했습니다 (S42-2)**

| 검사 | 되살린 결함 | 결과 |
|---|---|---|
| `V1-27` | 이력의 `state.frame` ② 를 99 로 (합을 깨뜨림) | **실패** 「①②③모름 합 1828 != 확인 안 됨 1729」 |
| `V1-28` | 지난번을 ② 5 · ③ 5 로 넣음 | **실패** 「② 못 읽는다 5 → 1132 (늘었다)」 · 「③ 5 → 3841 (늘었다)」 |
| 둘 다 | 되돌린 뒤 | **통과** |

| | 전 | 후 |
|---|--:|--:|
| check_all 통과 | 330 | **331** |
| fatal | 4 | **4** |
| 「확인 안 됨」 갈래 | 없음 (한 덩어리) | **①17,235 · ②1,132 · ③3,841 · 모름 0** |
| ★ 우리 잘못으로 확인된 것 | 「모름」 | **4,973건** |
| 값 갈래 중앙 (고치면) | — | **27.6% → 28.0%** |

---

## 11. 못 한 것 · 막힌 것 · 판단이 필요한 것

| # | 무엇 | 상태 |
|---|---|---|
| ① | ②③ 을 아직 **안 고쳤습니다** | 「갈래를 정하지 않고 코드를 고치는 것」이 금지라 갈래만 냈습니다. 8절 3·4 는 품이 작으니 지금 고칠까요 |
| ② | ② 정의가 「계산 쪽 부실」을 못 담습니다 | **여쭙니다** (9절 ②) |
| ③ | `tire_tread_mm` — 죽은 필드 검사 | **여쭙니다** (9절 ④) |
| ④ | 화면이 「모름」을 「없음」으로 냅니다 | 순서 6 에서 고칩니다 (9절 ⑤) |
| ⑤ | 트레드 리포트 | **막힘** — 내일 주소. ★ 지어내지 않았습니다 |
| ⑥ | 표본 400건 | `state.integrity` 675 · `history.lien` 은 추정입니다. 전건을 열면 1GB 를 다 읽습니다 — 표본 수를 표에 밝혔습니다 |
| ⑦ | 순서 6~11 | 아직입니다. **순서 6 (화면)이 다음이고 가장 큽니다** |

---

## 12. 고친 파일

```
config/unknown_split.json    ★ 새로 — 축 24개 → 확인 안 됨 source · 엔드포인트 · 키
tools/unknown_split.py       ★ 새로 — ①②③ 을 가른다 (원문 body 를 연다)
validate/v1_collect.py       V1-27 · V1-28 신설
config/cli_screens.json      tools/unknown_split.py 를 pending 에 (V11-45)
outputs/unknown_split_history.json  ★ 기계가 만든다 — V1-28 이 견준다
```

**커밋** `7207031`
