# r601 3단계 — 현대 인증: 보증 두 축 · 차종 조건 170건

`SPEC-2026.08.23-r601` · 명령서 4a장 · 규격 `docs/HYUNDAI_CERTIFIED_API.md` 2b·2c·2d
2026-08-23 19:00 · v212

---

## 1. 무엇을 하려 했나 (지시 원문)

```
3  현대 인증 (4a장 + docs/HYUNDAI_CERTIFIED_API.md 2b·2c·2d)
   · 잔여 보증을 일반·동력 두 축으로 나눠 읽어라. 표본 65건 중 43건(66.2%)이 다르다.
     그릇도 이미 네 칼럼으로 갈라져 있다
   · 「만료」면 0, 못 읽으면 NULL. 0 과 NULL 을 섞지 마라.
   · 소모품은 사이트 보장(②)으로 채운다. 「잔여 횟수」 쿠폰은 쓰지 마라.
   · 차종 조건 — 목록 제목만으로 170건이 붙는다. 상세가 필요 없다.
```

---

## 2. 무엇을 했나

| 파일 · 줄 | 무엇 |
|---|---|
| `parse/hyundai_cert/mapping.py:136–150` | ★ 남은 기간 정규식을 셋으로 나눴다 (`RE_W_YM`·`RE_W_Y`·`RE_W_M`) |
| `parse/hyundai_cert/mapping.py:250–283` | ★ `_warranty()` 가 ★ 블록 경계를 잡아 읽는다 |
| `config/dictionaries/target_map.json` | `hyundai_cert` 에 ★ `collect_group` · ★ `fuel_normalize` 추가 |
| `store/dictionary.py:171–230` | `fuel_normalize()` · `collect_group_of()` · `target_by_rules()` |
| `tools/collect_hyundai_cert.py:69` | `target_of()` 가 ★ 규칙으로 갈래를 고른다 |
| `tools/sync_target_map.py` | 차종군이 있으면 규칙으로 붙인다 · `mapped` 에 차종군을 적는다 |

### 2-1. ★★ 보증 — 결함 둘을 찾았다 (배포에서 상세를 받아 실측)

★ 두 축을 ★ 따로 읽는 것은 ★ 이미 돼 있었다 (`warranty_body_*` · `warranty_power_*`).
★ 그러나 ★ **읽는 값이 틀렸다.** ★ 배포 서버에서 상세를 받아 눈으로 맞춰 보고 찾았다.

**결함 ① 「1 년 남음」(개월이 없는 꼴)을 못 읽는다**

```
옛 정규식  (?:(\d+)\s*년\s*)?(\d+)\s*개월\s*남음     ← ★ `개월` 이 반드시 있어야 한다
실측       「차체 · 일반 · 냉난방 부품 ★ 1 년 남음 2027 년 8 월 까지 …」
결과       ★ month = None  (참값은 12)
```

**결함 ② 블록이 서로 샌다 — 일반이 동력의 값을 읽는다**

```
옛 코드   near = text[got.end() : got.end() + 90]      ← ★ 90자 창
실측 HGN260618029076 —
  「차체 · 일반 · 냉난방 부품 ★1 년 남음★ … 60,000 km 까지 엔진 및 동력전달 부품 ★1 년 4 개월 남음★ …」
  ★ 90자 창이 ★ 다음 블록까지 덮어 ★ 일반 = 16개월이 됐다.  ★ 참값은 12다
```

★ 고친 뒤 — 블록의 ★ 시작과 끝을 먼저 잡고, ★ 「기간 혹은 주행거리」·「워런티 플러스」 앞에서 멈춘다.
★ 광고 문구 「최대 12개월 / 20,000 km 구매 가능」을 읽지 않게 하는 것도 겸한다.

### 2-2. 차종 조건 — ★ 새 규칙을 만들지 않았다

규격 2d 가 「★ 이미 있는 `targets.json` 의 `fuel_match`·`trim_include`·`displacement_range` 를 쓴다.
★ 새 규칙을 만들지 않는다 — ★ 엔카가 쓰는 그 칸이다」라 했다. ★ 그대로 했다.

```
target_map.json   차종 이름 → ★ collect_group 만 준다 (「그랜저」 → encar:GRANDEUR)
targets.json      ★ 갈래를 고른다 — parse/classify.py 의 classify() 를 ★ 그대로 부른다
fuel_normalize    ★ 사이트 말 → 엔카 말.  「하이브리드」→「가솔린+전기」 · 「LPG」→「LPG(일반인 구입)」
                  ★ classify 의 _fuel_ok 는 ★ 완전 일치라 (STEP 43) ★ 안 맞추면 0건이 된다
```

---

## 3. 1차 결과 (고치기 전)

```
현대 인증 1,114건
  warranty_body_month    값 있음   644건 · 0 이 119건 · NULL   470건
  warranty_body_km       값 있음 1,114건 · 0 이 119건 · NULL     0건    ← ★ 월과 km 이 어긋난다
  warranty_power_month   값 있음   540건 · 0 이 118건 · NULL   574건
  warranty_power_km      값 있음 1,006건 · 0 이 118건 · NULL   108건
  S46-1  일반=동력 269건 · 일반≠동력 271건

차종 미정 (2단계 끝난 뒤)  6,836건
현대 인증에 붙은 차종       6건 (GRANDEUR_LPG)
```

## 4. 조치 후 결과

### 보증 — 배포에서 상세 5건을 받아 ★ 눈으로 맞췄다

| 매물 | 고치기 전 (월/km) | ★ 고친 뒤 | ★ 화면 글자에서 읽은 참값 |
|---|---|---|---|
| `HIG260706029646` | ★ **None**/20000 · **None**/20000 | **12**/20000 · **12**/20000 | 일반 1년남음 · 동력 1년남음 |
| `HNX260518027872` | ★ **None**/20000 · **None**/50469 | **12**/20000 · **12**/50469 | 일반 1년남음 · 동력 1년남음 |
| `HGN260618029076` | ★ **16**/20000 · 16/20000 | ★ **12**/20000 · **16**/20000 | 일반 1년남음(12) · 동력 1년4개월(16) |
| `HDN260612028842` | 19/45141 · 43/85141 | 19/45141 · 43/85141 | 일반 1년7개월 · 동력 3년7개월 |
| `HLX260719030069` | 0/0 · 0/0 | 0/0 · 0/0 | 둘 다 만료 → 0 |

★ 「만료」는 **0**, 못 읽으면 **NULL** — 섞지 않았다 (지시 그대로).

### 차종 조건 — ★ 규격 2d 표와 ★ 하나씩 맞췄다

전수 1,114건에 규칙을 걸었다.

| 우리 차종 | ★ 우리 실측 | 규격 2d 장 | |
|---|--:|--:|:--:|
| `G80_25T` | **151** | 151 | ○ |
| `G70_20T` | **10** | 10 | ○ |
| `G70_25T` | **1** | 1 | ○ |
| `GV60` | **2** | 2 | ○ |
| `GRANDEUR_LPG` | **6** | 6 | ○ |
| `G80_EV` | **0** | 0 | ○ |
| `GV70_EV` | ★ **3** | 0 (★ 「3건은 아래」 각주) | ★ 차이 |
| **합** | ★ **173** | 170 | |

```
★ GV70_EV 3건은 ★ 규격 3장이 스스로 잰 「Electrified GV70 ★ 3대」다.
★ 2d 표는 그것을 0 으로 적고 ★ 「(★ 3건은 아래)」 각주로 넘겼다.
★ 그래서 ★ 173 = 170 + 3 이다.  ★ 우리가 지어낸 3건이 아니다
★ 어느 쪽이 정본인지는 ★ 가이드가 정해 주십시오 (아래 6장 ⓑ)
```

### 차종 미정

```
★ 6,836 → ★ 6,669건   (−167)
   현대 인증  1,108 → ★ 941건 미정 · ★ 붙은 것 173건
2단계 시작(6,841) 부터 세면 ★ −172건
```

---

## 5. 검사

```
run_tests   (아래 6장 뒤에 이어 적는다 — 도는 중)
```

---

## 6. ★ 못 한 것 · 여쭐 것

### ⓐ ★★★ 소모품(2c)은 ★ 안 했다 — ★ 규격 둘이 정면으로 어긋난다

```
HYUNDAI_CERTIFIED_API 2c ②
   「필수 ★ site='hyundai_cert' 이면 ★ state.consumable 을 ★ 사이트 보장으로 채운다」
   「★ f-table 「사이트별 채우기」 ★ ②가 ★ 정확히 이 자리다」

f-table 「사이트별로 못 받는 축을 어떻게 채우나」(개정 468) 표
   | ⑤ 아무 데도 없다 | ★ 소모품 | ★ 0점 + 분모 유지 (개정 289·434) |
   ★ 소모품이 ★ ② 가 아니라 ★ ⑤ 로 ★ 이름까지 박혀 있다
```

★ **둘 다 필수인데 서로 반대다.** 명령서 머리가 「★ 숫자가 어긋나면 f-table 이 맞다」고 했으나,
★ 이것은 숫자가 아니라 ★ **갈래(② vs ⑤)** 라 그 규칙으로 못 가른다.

★ 더구나 —
```
· `source='site_guarantee'` 라는 근거값이 ★ 코드에 아직 없다 (`analyze/axis/state.py` 는 'missing'·SITE_UNAVAILABLE 뿐)
· 「어느 등급을 줄지는 `site_grade_rule` 이 정한다」는데 ★ 지금 hyundai_cert 의 그 칸은
  `{"all_of": {"정밀점검": true}}` — ★ 사이트 검증 등급이지 ★ 소모품 점수가 아니다
· 그러므로 ★ 지금 넣으면 ★ 점수를 ★ 내가 지어내는 것이 된다 (금지 12)
```
★ **규칙 2 대로 ★ 임의로 메우지 않고 ★ 보고만 한다.** ★ 가이드가 ② 인지 ⑤ 인지 정해 주십시오.
★ 정해 주시면 ★ 한 번에 넣겠습니다 (근거값 신설 + `site_grade_rule` 에 소모품 칸).

### ⓑ ★ `GV70_EV` — 173 이냐 170 이냐

```
규격 2d 표는 0 · 규격 3장 실측은 「Electrified GV70 3대」
★ 우리 규칙은 ★ 3건을 GV70_EV 로 붙인다 (연료 「전기」 · collect_group encar:GV70)
★ 붙이는 것이 맞다고 보나 ★ 2d 표와 어긋나므로 ★ 숫자만 남긴다
```

### ⓒ 안 한 것

```
· ★ 이미 저장된 1,114건의 보증값은 ★ 아직 옛 파서가 넣은 값이다 —
  ★ 4단계에서 좁혀 다시 받을 때 ★ 새 파서로 다시 들어온다.  ★ 지금 1,114건을 다시 안 받았다
· 「잔여 횟수(소모품 교환)」 쿠폰은 ★ 쓰지 않았다 (지시 그대로)
· `/detail/{id}` 404 예외는 ★ 이번에도 못 봤다
```

---

## 5. 검사 (이어 적음)

```
run_tests   1차 — ★ test_endtoend 실패
              FAIL  중단 없이 끝난다  ['S11: V4-22 역방향 · 순환 import 없음: 3']
              ★ 내가 낸 것이다 — target_by_rules 를 store/ 에 뒀다 (4단계 기록 5장)
            조치 — parse/target_rules.py 로 옮겼다
            2차 — ★ test_endtoend 통과 · V4-22 actual 3 → 0
```
