# r744 — 3-4 XHR · 리본카 토큰을 찾았다 (그러나 999) · K카 `enc` 꼴

`SPEC-2026.08.25-r743` · 2026-08-26 04:00 · v251

## 0. ★ `build_index.py` — ★ 알겠습니다

★ 가이드 — 「★ 죽지 않았다.  ★ 생성물이 낡은 것이지 ★ 도구가 죽은 게 아니다」

```
★ 앞으로 ★ **바퀴마다** ★ `python3.11 tools/build_index.py` 를 돌리고 ★ 함께 커밋합니다
★ 손으로 안 고칩니다 (생성물 다섯)
★ 이번 바퀴에도 돌렸습니다 — ★ 소스 173 · DB 표 44 · 지시서 113파일
```

## 1. ★★★ 3-4 리본카 XHR — ★ ① 주소 ② 요청 꼴 ③ 토큰을 다 찾았습니다

### ★ ① 주소

```
POST https://www.reborncar.co.kr/api/v1/car/carFilterList.rb
```

★ 찾은 길 — ★ 목록 화면(`/m/smartbuy/SB1001.rb`)이 부르는 ★ js 열일곱을 훑었습니다.

```
/js/carData.js   function getRebornCarFilterData(param) {
                     return deferredAjaxToken('/api/v1/car/carFilterList.rb', param); }
```

★ 곁가지도 다 나왔습니다 — `carModelMaker` · `carModelModel` · `carModelFuel` ·
`carModelColor` · `carModelSeater` · `carModelTrim` · `carModelYear` · `carOption`.

### ★ ② 요청 꼴

```
/js/app.cust.js   function deferredAjaxToken(url, param, _beforeSend) {
  $.ajax({ url: url, type: 'POST',
           headers: { 'X-Ajax-call': true,
                      Authorization: RB_TOKEN,
                      'X-CSRF-TOKEN': $('#logout-form input[name="_csrf"]').val() },
           data: param, traditional: true })
```

```
★ 몸통은 ★ 평문 폼이다 (`traditional: true`) — ★ `enc` 가 ★ **아니다**
★ 머리 셋 — `X-Ajax-call: true` · `Authorization: {토큰}` · `X-CSRF-TOKEN: {csrf}`
```

### ★ ③ 토큰 — ★ **화면에 그대로 박혀 옵니다**

```
목록 화면 안에   RB_TOKEN = "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9…"   ★ JWT · 209자
                <input name="_csrf" value="…">                      ★ 96자
```

```
★★ ★ 헤이딜러와 ★ **같은 꼴**입니다 — ★ 화면을 한 번 열면 ★ 토큰이 손에 들어옵니다
★ ★ 토큰이 늙으면 ★ `resultCode 1020` 이 오고 ★ 스스로 새 토큰으로 다시 부릅니다
   (`deferredAjaxToken` 이 그렇게 적혀 있습니다)
```

### ★★ 그런데 — ★ 불러 보니 ★ **999** 입니다

| 부른 꼴 | 결과 |
|---|---|
| 머리 없이 POST | 200 · ★ **몸통 0B** |
| 머리 셋을 다 붙여 POST | ★ **HTTP 999** |

```
★★ ★ `999` 는 ★ 사이트가 ★ **막는 신호**입니다.
★ ★ **여기서 멈췄습니다** — ★ 우회를 만들지 않았습니다 (금지)
★ ★ 지금은 ★ 사이트맵 1,036건으로 ★ 잘 받고 있습니다 — ★ 급한 것이 아닙니다
```

★ ★ 무엇이 더 필요한지는 ★ **사람이 브라우저로 한 번 눌러** 봐야 압니다 —
★ 쿠키(세션)일 수도, ★ `Referer` 밖의 무엇일 수도 있습니다.

## 2. ★ 3-4 K카 — ★ `enc` **꼴만** 적습니다

```
/_nuxt/42c52aa.js
  searchDrctList: function(param) {
      e.$AxiosBuilder.data(e.setEncParam(param)).build().post("/bc/search/list/drct") }
  searchList:     … .data(e.setEncParam(param)) … .post("/bc/search/list")
  (건수)          … .post("/api/v1/ds/carSearchListCount", this.setEncParam(t))
```

```
★ 몸통 = ★ `setEncParam(param)` — ★ 조건 객체를 ★ 통째로 싼 것이다
★★ ★ **풀지 않았습니다 · 우회를 만들지 않았습니다** (금지).  ★ 꼴만 적습니다
★ ★ 목록은 ★ 이미 ★ `mapi.kcar.com` 평문으로 ★ 527건을 받고 있습니다 —
   ★ ★ 이 길은 ★ 안 써도 됩니다
```

## 3. ★ 보배 — ★ 말씀대로 합니다

```
★ `maker_no` 만으로 받고 ★ 우리 쪽에서 거릅니다 — ★ 이미 그렇게 고쳤습니다 (r742)
★ `model_no` 코드표는 ★ **안 버렸습니다** — ★ `targets.json` 에 그대로 있습니다.
  ★ 상세의 차명과 맞대는 데 씁니다
★ 제조사 ★ 아홉(실측 10)으로 좁혀 ★ 보배 ★ 23 → 97건이 됐습니다
```

## 4. ★ KB 전량 — ★ 돌고 있습니다

```
★ 박자대로 ★ 48묶음(480건)을 ★ 뒤에서 돌리는 중입니다 (10건 · 5분)
★ ★ **수입만이 아니라 전량**입니다 (마스터 확정)
```

## 5. 규칙 3 — 두 도구

```
run_tests   ★ 통과 (19파일)
v0_guide    실패 2 — ★ 둘 다 `docs/` 쪽입니다
   ★ S43-3   버전 r684 · 이력 743   ← ★ 맞추셨다 하셨는데 ★ 아직 어긋납니다 (6장)
   ★ S46-36  폐기된 요구 121 이 ★ `KBCHACHACHA_API.md` 네 줄에 살아 있습니다
```

★ 생성물 다섯은 ★ 이번 바퀴에 다시 만들어 ★ 함께 커밋합니다 — ★ `S46-32` 통과입니다.

## 6. ★ 지적 — ★ `S43-3` 이 아직 어긋납니다

```
docs/guide/00_버전.md 4행   `SPEC-2026.08.25-r743`   ← ★ 머리
같은 파일 18행               `SPEC-2026.08.24-r684`   ← ★ 본문
★ ★ 검사는 ★ 「버전 r684 · 이력 마지막 743」이라 말합니다
★ ★ 제가 `docs/` 를 안 고칩니다 (규칙 2) — ★ 18행을 봐 주십시오
```

## 7. ★ 마스터께 드릴 물음 — ★ **없습니다**

## 7a. 쌓아 둔 것

```
ⓐ ★ `KBCHACHACHA_API.md` 네 줄 — ★ 폐기된 요구 121 (r743 4장)
ⓑ `S43-3` — ★ `00_버전.md` 18행 (6장)
ⓒ 리본카 XHR 이 ★ 999 로 막힙니다 — ★ 사람이 브라우저로 한 번 눌러 주셔야
   ★ 무엇이 더 필요한지 압니다 (쿠키인지 · 그 밖인지)
```

## 8. 다음

```
① KB 전량 — ★ 도는 중 (엿새)
② 3-2 걸러 저장 — ★ 코드는 넣었습니다.  ★ 다시 받는 것이 남았습니다
③ 멀티 매핑 · 견주기
④ ★ 화면을 ★ **모바일 기준**으로 다시 (v4m 시안) — ★ 새로 주신 것
```

커밋 해시 — `6d10e0a`
